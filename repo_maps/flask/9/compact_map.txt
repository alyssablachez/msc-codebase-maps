src/flask/app.py
class Flask(Scaffold) L103
  "The flask object implements a WSGI application and acts as the central"
  __init__(self, import_name, static_url_path=None, static_folder='static', static_host=None, host_matching=False, subdomain_matching=False, template_folder='templates', instance_path=None, instance_relative_config=False, root_path=None) L388
  _is_setup_finished(self) L526
  name(self) L530
  "The name of the application.  This is usually the import name"
  propagate_exceptions(self) L547
  "Returns the value of the ``PROPAGATE_EXCEPTIONS`` configuration"
  preserve_context_on_exception(self) L559
  "Returns the value of the ``PRESERVE_CONTEXT_ON_EXCEPTION``"
  logger(self) L572
  "A standard Python :class:`~logging.Logger` for the app, with"
  jinja_env(self) L599
  "The Jinja environment used to load templates."
  got_first_request(self) L609
  "This attribute is set to ``True`` if the application started"
  make_config(self, instance_relative=False) L617
  "Used to create the config attribute by the Flask constructor."
  auto_find_instance_path(self) L634
  "Tries to locate the instance path if it was not provided to the"
  open_instance_resource(self, resource, mode='rb') L647
  "Opens a resource from the application's instance folder"
  templates_auto_reload(self) L660
  "Reload templates when they are changed. Used by"
  templates_auto_reload(self, value) L675
  create_jinja_environment(self) L678
  "Create the Jinja environment based on :attr:`jinja_options`"
  create_global_jinja_loader(self) L713
  "Creates the loader for the Jinja2 environment.  Can be used to"
  select_jinja_autoescape(self, filename) L726
  "Returns ``True`` if autoescaping should be active for the given"
  update_template_context(self, context) L736
  "Update the template context with some commonly used variables."
  make_shell_context(self) L763
  "Returns the shell context for an interactive shell for this"
  debug(self) L787
  "Whether debug mode is enabled. When using ``flask run`` to start"
  debug(self, value) L804
  run(self, host=None, port=None, debug=None, load_dotenv=True, **options) L808
  "Runs the application on a local development server."
  test_client(self, use_cookies=True, **kwargs) L930
  "Creates a test client for this application.  For information"
  test_cli_runner(self, **kwargs) L988
  "Create a CLI runner for testing CLI commands."
  register_blueprint(self, blueprint, **options) L1006
  "Register a :class:`~flask.Blueprint` on the application. Keyword"
  iter_blueprints(self) L1033
  "Iterates over all blueprints by the order they were registered."
  add_url_rule(self, rule, endpoint=None, view_func=None, provide_automatic_options=None, **options) L1041
  template_filter(self, name=None) L1100
  "A decorator that is used to register custom template filter."
  decorator(f) L1115
  add_template_filter(self, f, name=None) L1122
  "Register a custom template filter.  Works exactly like the"
  template_test(self, name=None) L1134
  "A decorator that is used to register custom template test."
  decorator(f) L1156
  add_template_test(self, f, name=None) L1163
  "Register a custom template test.  Works exactly like the"
  template_global(self, name=None) L1177
  "A decorator that is used to register a custom template global function."
  decorator(f) L1194
  add_template_global(self, f, name=None) L1201
  "Register a custom template global function. Works exactly like the"
  before_first_request(self, f) L1215
  "Registers a function to be run before the first request to this"
  teardown_appcontext(self, f) L1230
  "Registers a function to be called when the application context"
  shell_context_processor(self, f) L1263
  "Registers a shell context processor function."
  _find_error_handler(self, e) L1271
  "Return a registered error handler for an exception in this order:"
  handle_http_exception(self, e) L1293
  "Handles an HTTP exception.  By default this will invoke the"
  trap_http_exception(self, e) L1328
  "Checks if an HTTP exception should be trapped or not.  By default"
  handle_user_exception(self, e) L1363
  "This method is called whenever an exception occurs that"
  handle_exception(self, e) L1395
  "Handle an exception that did not have an error handler"
  log_exception(self, exc_info) L1444
  "Logs an exception.  This is called by :meth:`handle_exception`"
  raise_routing_exception(self, request) L1461
  "Exceptions that are recording during routing are reraised with"
  dispatch_request(self) L1480
  "Does the request dispatching.  Matches the URL and returns the"
  full_dispatch_request(self) L1504
  "Dispatches the request and on top of that performs request"
  finalize_request(self, rv, from_error_handler=False) L1521
  "Given the return value from a view function this finalizes"
  try_trigger_before_first_request_functions(self) L1550
  "Called before each request and will ensure that it triggers"
  make_default_options_response(self) L1566
  "This method is called to create the default ``OPTIONS`` response."
  should_ignore_error(self, error) L1579
  "This is called to figure out if an error should be ignored"
  ensure_sync(self, func) L1589
  "Ensure that the function is synchronous for WSGI workers."
  async_to_sync(self, func) L1603
  "Return a sync function that will run the coroutine function."
  make_response(self, rv) L1633
  "Convert the return value from a view function to an instance of"
  create_url_adapter(self, request) L1751
  "Creates a URL adapter for the given request. The URL adapter"
  inject_url_defaults(self, endpoint, values) L1793
  "Injects the URL defaults for the given endpoint directly into"
  handle_url_build_error(self, error, endpoint, values) L1812
  "Handle :class:`~werkzeug.routing.BuildError` on"
  preprocess_request(self) L1835
  "Called before the request is dispatched. Calls"
  process_response(self, response) L1866
  "Can be overridden in order to modify the response object"
  do_teardown_request(self, exc=_sentinel) L1892
  "Called after the request is dispatched and the response is"
  do_teardown_appcontext(self, exc=_sentinel) L1927
  "Called right before the application context is popped."
  app_context(self) L1950
  "Create an :class:`~flask.ctx.AppContext`. Use as a ``with``"
  request_context(self, environ) L1971
  "Create a :class:`~flask.ctx.RequestContext` representing a"
  test_request_context(self, *args, **kwargs) L1987
  "Create a :class:`~flask.ctx.RequestContext` for a WSGI"
  wsgi_app(self, environ, start_response) L2043
  "The actual WSGI application. This is not implemented in"
  __call__(self, environ, start_response) L2086
  "The WSGI server calls the Flask application object as the"

iscoroutinefunction(func) L86
_make_timedelta(value) L96

src/flask/blueprints.py
class BlueprintSetupState L26
  "Temporary holder object for registering a blueprint with the"
  __init__(self, blueprint, app, options, first_registration) L33
  add_url_rule(self, rule, endpoint=None, view_func=None, **options) L79
  "A helper method to register a rule (and optionally a view function)"

class Blueprint(Scaffold) L111
  "Represents a blueprint, a collection of routes and other"
  __init__(self, name, import_name, static_folder=None, static_url_path=None, template_folder=None, url_prefix=None, subdomain=None, url_defaults=None, root_path=None, cli_group=_sentinel) L174
  _is_setup_finished(self) L210
  record(self, func) L213
  "Registers a function that is called when the blueprint is"
  record_once(self, func) L231
  "Works like :meth:`record` but wraps the function in another"
  wrapper(state) L238
  make_setup_state(self, app, options, first_registration=False) L244
  "Creates an instance of :meth:`~flask.blueprints.BlueprintSetupState`"
  register_blueprint(self, blueprint, **options) L253
  "Register a :class:`~flask.Blueprint` on this blueprint. Keyword"
  register(self, app, options) L270
  "Called by :meth:`Flask.register_blueprint` to register all"
  extend(bp_dict, parent_dict) L335
  add_url_rule(self, rule, endpoint=None, view_func=None, provide_automatic_options=None, **options) L400
  "Like :meth:`Flask.add_url_rule` but for a blueprint.  The endpoint for"
  app_template_filter(self, name=None) L427
  "Register a custom template filter, available application wide.  Like"
  decorator(f) L437
  add_app_template_filter(self, f, name=None) L443
  "Register a custom template filter, available application wide.  Like"
  register_template(state) L454
  app_template_test(self, name=None) L459
  "Register a custom template test, available application wide.  Like"
  decorator(f) L471
  add_app_template_test(self, f, name=None) L477
  "Register a custom template test, available application wide.  Like"
  register_template(state) L490
  app_template_global(self, name=None) L495
  "Register a custom template global, available application wide.  Like"
  decorator(f) L507
  add_app_template_global(self, f, name=None) L513
  "Register a custom template global, available application wide.  Like"
  register_template(state) L526
  before_app_request(self, f) L531
  "Like :meth:`Flask.before_request`.  Such a function is executed"
  before_app_first_request(self, f) L540
  "Like :meth:`Flask.before_first_request`.  Such a function is"
  after_app_request(self, f) L549
  "Like :meth:`Flask.after_request` but for a blueprint.  Such a function"
  teardown_app_request(self, f) L558
  "Like :meth:`Flask.teardown_request` but for a blueprint.  Such a"
  app_context_processor(self, f) L568
  "Like :meth:`Flask.context_processor` but for a blueprint.  Such a"
  app_errorhandler(self, code) L579
  "Like :meth:`Flask.errorhandler` but for a blueprint.  This"
  decorator(f) L584
  app_url_value_preprocessor(self, f) L590
  "Same as :meth:`url_value_preprocessor` but application wide."
  app_url_defaults(self, f) L599
  "Same as :meth:`url_defaults` but application wide."

src/flask/cli.py
class NoAppException(click.UsageError) L33
  "Raised if an application cannot be found or loaded."

class DispatchingApp L304
  "Special application that dispatches to a Flask application which"
  __init__(self, loader, use_eager_loading=None) L311
  _load_in_background(self) L325
  _load_app() L326
  _flush_bg_loading_exception(self) L337
  _load_unlocked(self) L344
  __call__(self, environ, start_response) L350

class ScriptInfo L363
  "Helper object to deal with Flask applications.  This is usually not"
  __init__(self, app_import_path=None, create_app=None, set_debug_flag=True) L372
  load_app(self) L384
  "Loads the Flask app (if not yet loaded) and returns it.  Calling"

class AppGroup(click.Group) L445
  "This works similar to a regular click :class:`~click.Group` but it"
  command(self, *args, **kwargs) L453
  "This works exactly like the method of the same name on a regular"
  decorator(f) L460
  group(self, *args, **kwargs) L467
  "This works exactly like the method of the same name on a regular"

class FlaskGroup(AppGroup) L476
  "Special subclass of the :class:`AppGroup` group that supports"
  __init__(self, add_default_commands=True, create_app=None, add_version_option=True, load_dotenv=True, set_debug_flag=True, **extra) L499
  _load_plugin_commands(self) L525
  get_command(self, ctx, name) L538
  list_commands(self, ctx) L556
  main(self, *args, **kwargs) L577

class CertParamType(click.ParamType) L692
  "Click option type for the ``--cert`` option. Allows either an"
  __init__(self) L700
  convert(self, value, param, ctx) L703

class SeparatedPathType(click.Path) L767
  "Click option type that accepts a list of values separated by the"
  convert(self, value, param, ctx) L773

find_best_app(script_info, module) L37
"Given a module instance this tries to find the best possible"
call_factory(script_info, app_factory, args=None, kwargs=None) L89
"Takes an app factory, a ``script_info` object and  optionally a tuple"
_called_with_wrong_args(f) L122
"Check whether calling a function raised a ``TypeError`` because"
find_app_by_string(script_info, module, app_name) L148
"Check if the given string is a variable name or a function. Call"
prepare_import(path) L223
"Given a filename this will try to calculate the python path, add it"
locate_app(script_info, module_name, app_name, raise_if_not_found=True) L252
get_version(ctx, param, value) L278
with_appcontext(f) L430
"Wraps a callback so that it's guaranteed to be executed with the"
decorator(__ctx, *args, **kwargs) L438
_path_is_ancestor(path, other) L599
"Take ``other`` and remove the length of ``path`` from it. Then join it"
load_dotenv(path=None) L606
"Load "dotenv" files in order of precedence to set environment variables."
show_server_banner(env, debug, app_import_path, eager_loading) L663
"Show extra startup messages the first time the server is run,"
_validate_key(ctx, param, value) L736
"The ``--key`` option must be specified when ``--cert`` is a file."
run_command(info, host, port, reload, debugger, eager_loading, with_threads, cert, extra_files) L825
"Run a local development server."
shell_command() L863
"Run an interactive Python shell in the context of a given"
routes_command(sort, all_methods) L925
"Show all registered routes with endpoints and methods."
main() L981

src/flask/config.py
class ConfigAttribute L9
  "Makes an attribute forward to the config"
  __init__(self, name, get_converter=None) L12
  __get__(self, obj, owner=None) L16
  __set__(self, obj, value) L24

class Config(dict) L28
  "Works exactly like a dict but provides ways to fill it from files"
  __init__(self, root_path, defaults=None) L72
  from_envvar(self, variable_name, silent=False) L76
  "Loads a configuration from an environment variable pointing to"
  from_pyfile(self, filename, silent=False) L100
  "Updates the values in the config from a Python file.  This function"
  from_object(self, obj) L128
  "Updates the values from the given object.  An object can be of one"
  from_file(self, filename, load, silent=False) L166
  "Update the values in the config from a file that is loaded"
  from_json(self, filename, silent=False) L205
  "Update the values in the config from a JSON file. The loaded"
  from_mapping(self, mapping=None, **kwargs) L230
  "Updates the config like :meth:`update` ignoring items with non-upper"
  get_namespace(self, namespace, lowercase=True, trim_namespace=True) L247
  "Returns a dictionary containing a subset of configuration options"
  __repr__(self) L290

src/flask/ctx.py
class _AppCtxGlobals L24
  "A plain object. Used as a namespace for storing data during an"
  __getattr__(self, name) L47
  __setattr__(self, name, value) L53
  __delattr__(self, name) L56
  get(self, name, default=None) L62
  "Get an attribute by name, or a default value. Like"
  pop(self, name, default=_sentinel) L73
  "Get and remove an attribute by name. Like :meth:`dict.pop`."
  setdefault(self, name, default=None) L87
  "Get the value of an attribute if it is present, otherwise"
  __contains__(self, item) L99
  __iter__(self) L102
  __repr__(self) L105

class AppContext L219
  "The application context binds an application object implicitly"
  __init__(self, app) L228
  push(self) L237
  "Binds the app context to the current context."
  pop(self, exc=_sentinel) L243
  "Pops the app context."
  __enter__(self) L256
  __exit__(self, exc_type, exc_value, tb) L260

class RequestContext L266
  "The request context contains all request relevant information.  It is"
  __init__(self, app, environ, request=None, session=None) L296
  g(self) L335
  g(self, value) L339
  copy(self) L342
  "Creates a copy of this request context with the same request object."
  match_request(self) L362
  "Can be overridden by a subclass to hook into the matching"
  push(self) L372
  "Binds the request context to the current context."
  pop(self, exc=_sentinel) L414
  "Pops the request context and unbinds it by doing that.  This will"
  auto_pop(self, exc) L453
  __enter__(self) L462
  __exit__(self, exc_type, exc_value, tb) L466
  __repr__(self) L476

after_this_request(f) L112
"Executes a function after this request.  This is useful to modify"
copy_current_request_context(f) L137
"A helper function that decorates a function to retain the current"
wrapper(*args, **kwargs) L170
has_request_context() L177
"If you have code that wants to test if a request context is there or"
has_app_context() L209
"Works like :func:`has_request_context` but for the application"

src/flask/debughelpers.py
class UnexpectedUnicodeError(AssertionError, UnicodeError) L10
  "Raised in places where we want some better error reporting for"

class DebugFilesKeyError(KeyError, AssertionError) L16
  "Raised from request.files during debugging.  The idea is that it can"
  __init__(self, request, key) L21
  __str__(self) L39

class FormDataRoutingRedirect(AssertionError) L43
  "This exception is raised by Flask in debug mode if it detects a"
  __init__(self, request) L49

class newcls(oldcls) L82
  __getitem__(self, key) L83

attach_enctype_error_multidict(request) L75
"Since Flask 0.8 we're monkeypatching the files object in case a"
_dump_loader_info(loader) L96
explain_template_loading_attempts(app, template, attempts) L113
"This should help developers understand what failed"
explain_ignored_app_run() L161

src/flask/globals.py
_lookup_req_object(name) L30
_lookup_app_object(name) L37
_find_app() L44

src/flask/helpers.py
class locked_cached_property(werkzeug.utils.cached_property) L754
  "A :func:`property` that is only evaluated once. Like"
  __init__(self, fget, name=None, doc=None) L763
  __get__(self, obj, type=None) L772
  __set__(self, obj, value) L779
  __delete__(self, obj) L783

get_env() L29
"Get the environment the app is running in, indicated by the"
get_debug_flag() L37
"Get whether debug mode should be enabled for the app, indicated"
get_load_dotenv(default=True) L51
"Get whether the user has disabled loading dotenv files by setting"
stream_with_context(generator_or_function) L66
"Request contexts disappear when the response is started on the server."
decorator(*args, **kwargs) L108
generator() L114
make_response(*args) L145
"Sometimes it is necessary to set additional headers in a view.  Because"
url_for(endpoint, **values) L194
"Generates a URL to the given endpoint with the method provided."
get_template_attribute(template_name, attribute) L345
"Loads a macro (or variable) a template exports.  This can be used to"
flash(message, category='message') L367
"Flashes a message to the next request.  In order to remove the"
get_flashed_messages(with_categories=False, category_filter=()) L399
"Pulls all flashed messages from the session and returns them."
_prepare_send_file_kwargs(download_name=None, attachment_filename=None, etag=None, add_etags=None, max_age=None, cache_timeout=None, **kwargs) L442
send_file(path_or_file, mimetype=None, as_attachment=False, download_name=None, attachment_filename=None, conditional=True, etag=True, add_etags=None, last_modified=None, max_age=None, cache_timeout=None) L496
"Send the contents of a file to the client."
safe_join(directory, *pathnames) L630
"Safely join zero or more untrusted path components to a base"
send_from_directory(directory, path, filename=None, **kwargs) L653
"Send a file from within a directory using :func:`send_file`."
get_root_path(import_name) L705
"Find the root path of a package, or the path that contains a"
total_seconds(td) L788
"Returns the total seconds from a timedelta object."
is_ip(value) L809
"Determine if the given string is an IP address."
_split_blueprint_path(name) L830

src/flask/json/__init__.py
class JSONEncoder(_json.JSONEncoder) L25
  "The default JSON encoder. Handles extra types compared to the"
  default(self, o) L42
  "Convert ``o`` to a JSON serializable type. See"

class JSONDecoder(_json.JSONDecoder) L59
  "The default JSON decoder."

_dump_arg_defaults(kwargs, app=None) L70
"Inject default arguments for dump functions."
_load_arg_defaults(kwargs, app=None) L91
"Inject default arguments for load functions."
dumps(obj, app=None, **kwargs) L109
"Serialize an object to a string of JSON."
dump(obj, fp, app=None, **kwargs) L144
"Serialize an object to JSON written to a file object."
loads(s, app=None, **kwargs) L183
"Deserialize an object from a string of JSON."
load(fp, app=None, **kwargs) L219
"Deserialize an object from JSON read from a file object."
htmlsafe_dumps(obj, **kwargs) L252
"Serialize an object to a string of JSON with :func:`dumps`, then"
htmlsafe_dump(obj, fp, **kwargs) L276
"Serialize an object to JSON written to a file object, replacing"
jsonify(*args, **kwargs) L284
"Serialize data to JSON and wrap it in a :class:`~flask.Response`"

src/flask/json/tag.py
class JSONTag L57
  "Base class for defining type tags for :class:`TaggedJSONSerializer`."
  __init__(self, serializer) L66
  "Create a tagger for the given serializer."
  check(self, value) L70
  "Check if the given value should be tagged by this tag."
  to_json(self, value) L74
  "Convert the Python object to an object that is a valid JSON type."
  to_python(self, value) L79
  "Convert the JSON representation back to the correct type. The tag"
  tag(self, value) L84
  "Convert the value to a valid JSON type and add the tag structure"

class TagDict(JSONTag) L90
  "Tag for 1-item dicts whose only key matches a registered tag."
  check(self, value) L100
  to_json(self, value) L107
  to_python(self, value) L111

class PassDict(JSONTag) L116
  check(self, value) L119
  to_json(self, value) L122

class TagTuple(JSONTag) L130
  check(self, value) L134
  to_json(self, value) L137
  to_python(self, value) L140

class PassList(JSONTag) L144
  check(self, value) L147
  to_json(self, value) L150

class TagBytes(JSONTag) L156
  check(self, value) L160
  to_json(self, value) L163
  to_python(self, value) L166

class TagMarkup(JSONTag) L170
  "Serialize anything matching the :class:`~markupsafe.Markup` API by"
  check(self, value) L178
  to_json(self, value) L181
  to_python(self, value) L184

class TagUUID(JSONTag) L188
  check(self, value) L192
  to_json(self, value) L195
  to_python(self, value) L198

class TagDateTime(JSONTag) L202
  check(self, value) L206
  to_json(self, value) L209
  to_python(self, value) L212

class TaggedJSONSerializer L216
  "Serializer that uses a tag system to compactly represent objects that"
  __init__(self) L246
  register(self, tag_class, force=False, index=None) L253
  "Register a new tag with this serializer."
  tag(self, value) L286
  "Convert a value to a tagged representation if necessary."
  untag(self, value) L294
  "Convert a tagged representation back to the original type."
  dumps(self, value) L306
  "Tag the value and dump it to a compact JSON string."
  loads(self, value) L310
  "Load data from a JSON string and deserialized any tagged objects."

src/flask/logging.py
wsgi_errors_stream() L14
"Find the most appropriate error stream for the application. If a request"
has_level_handler(logger) L26
"Check if there is a handler in the logging chain that will handle the"
create_logger(app) L53
"Get the Flask app's logger and configure it if needed."

src/flask/scaffold.py
class Scaffold L61
  "Common behavior shared between :class:`~flask.Flask` and"
  __init__(self, import_name, static_folder=None, static_url_path=None, template_folder=None, root_path=None) L91
  __repr__(self) L239
  _is_setup_finished(self) L242
  static_folder(self) L246
  "The absolute path to the configured static folder. ``None``"
  static_folder(self, value) L256
  has_static_folder(self) L263
  "``True`` if :attr:`static_folder` is set."
  static_url_path(self) L271
  "The URL prefix that the static route will be accessible from."
  static_url_path(self, value) L287
  get_send_file_max_age(self, filename) L293
  "Used by :func:`send_file` to determine the ``max_age`` cache"
  send_static_file(self, filename) L314
  "The view function used to serve files from"
  jinja_loader(self) L333
  "The Jinja loader for this object's templates. By default this"
  open_resource(self, resource, mode='rb') L345
  "Open a resource file relative to :attr:`root_path` for"
  _method_route(self, method, rule, options) L368
  get(self, rule, **options) L374
  "Shortcut for :meth:`route` with ``methods=["GET"]``."
  post(self, rule, **options) L381
  "Shortcut for :meth:`route` with ``methods=["POST"]``."
  put(self, rule, **options) L388
  "Shortcut for :meth:`route` with ``methods=["PUT"]``."
  delete(self, rule, **options) L395
  "Shortcut for :meth:`route` with ``methods=["DELETE"]``."
  patch(self, rule, **options) L402
  "Shortcut for :meth:`route` with ``methods=["PATCH"]``."
  route(self, rule, **options) L409
  "Decorate a view function to register it with the given URL"
  decorator(f) L433
  add_url_rule(self, rule, endpoint=None, view_func=None, provide_automatic_options=None, **options) L441
  "Register a rule for routing incoming requests and building"
  endpoint(self, endpoint) L508
  "Decorate a view function to register it for the given"
  decorator(f) L525
  before_request(self, f) L532
  "Register a function to run before each request."
  after_request(self, f) L554
  "Register a function to run after each request to this object."
  teardown_request(self, f) L570
  "Register a function to be run at the end of each request,"
  context_processor(self, f) L609
  "Registers a template context processor function."
  url_value_preprocessor(self, f) L617
  "Register a URL value preprocessor function for all view"
  url_defaults(self, f) L636
  "Callback function for URL defaults for all view functions of the"
  errorhandler(self, code_or_exception) L645
  "Register a function to handle errors by code or exception class."
  decorator(f) L677
  register_error_handler(self, code_or_exception, f) L684
  "Alternative error attach function to the :meth:`errorhandler`"
  _get_exc_class_and_code(exc_class_or_code) L714
  "Get the exception class being handled. For HTTP status codes"

setupmethod(f) L39
"Wraps a method so that it performs a check in debug mode if the"
wrapper_func(self, *args, **kwargs) L44
_endpoint_from_view_func(view_func) L740
"Internal helper that returns the default endpoint for a given"
_matching_loader_thinks_module_is_package(loader, mod_name) L748
"Attempt to figure out if the given name is a package or a module."
_find_package_path(root_mod_name) L773
"Find the path that contains the package or module."
find_package(import_name) L825
"Find the prefix that a package is installed under, and the path"

src/flask/sessions.py
class SessionMixin(MutableMapping) L20
  "Expands a basic dictionary with session attributes."
  permanent(self) L24
  "This reflects the ``'_permanent'`` key in the dict."
  permanent(self, value) L29

class SecureCookieSession(CallbackDict, SessionMixin) L48
  "Base class for sessions based on signed cookies."
  __init__(self, initial=None) L70
  on_update(self) L71
  __getitem__(self, key) L77
  get(self, key, default=None) L81
  setdefault(self, key, default=None) L85

class NullSession(SecureCookieSession) L90
  "Class used to generate nicer error messages if sessions are not"
  _fail(self, *args, **kwargs) L96

class SessionInterface L107
  "The basic interface you have to implement in order to replace the"
  make_null_session(self, app) L150
  "Creates a null session which acts as a replacement object if the"
  is_null_session(self, obj) L162
  "Checks if a given object is a null session.  Null sessions are"
  get_cookie_name(self, app) L171
  "Returns the name of the session cookie."
  get_cookie_domain(self, app) L178
  "Returns the domain that should be set for the session cookie."
  get_cookie_path(self, app) L236
  "Returns the path for which the cookie should be valid.  The"
  get_cookie_httponly(self, app) L244
  "Returns True if the session cookie should be httponly.  This"
  get_cookie_secure(self, app) L251
  "Returns True if the cookie should be secure.  This currently"
  get_cookie_samesite(self, app) L257
  "Return ``'Strict'`` or ``'Lax'`` if the cookie should use the"
  get_expiration_time(self, app, session) L264
  "A helper method that returns an expiration date for the session"
  should_set_cookie(self, app, session) L276
  "Used by session backends to determine if a ``Set-Cookie`` header"
  open_session(self, app, request) L292
  "This method has to be implemented and must either return ``None``"
  save_session(self, app, session, response) L302
  "This is called for actual sessions returned by :meth:`open_session`"

class SecureCookieSessionInterface(SessionInterface) L316
  "The default session interface that stores sessions in signed cookies"
  get_signing_serializer(self, app) L335
  open_session(self, app, request) L350
  save_session(self, app, session, response) L366

src/flask/signals.py
class Namespace L10
  signal(self, name, doc=None) L11

class _FakeSignal L14
  "If blinker is unavailable, create a fake class with the same"
  __init__(self, name, doc=None) L21
  send(self, *args, **kwargs) L25
  _fail(self, *args, **kwargs) L28

src/flask/templating.py
class Environment(BaseEnvironment) L33
  "Works like a regular Jinja2 environment but has some additional"
  __init__(self, app, **options) L39

class DispatchingJinjaLoader(BaseLoader) L46
  "A loader that looks for templates in the application and all"
  __init__(self, app) L51
  get_source(self, environment, template) L54
  _get_source_explained(self, environment, template) L61
  _get_source_fast(self, environment, template) L87
  _iter_loaders(self, template) L97
  list_templates(self) L109

_default_template_ctx_processor() L18
"Default template context processor.  Injects `request`,"
_render(template, context, app) L124
"Renders the template and fires the signal"
render_template(template_name_or_list, **context) L133
"Renders a template from the template folder with the given"
render_template_string(source, **context) L154
"Renders a template from the given template source string"

src/flask/testing.py
class EnvironBuilder(werkzeug.test.EnvironBuilder) L22
  "An :class:`~werkzeug.test.EnvironBuilder`, that takes defaults from the"
  __init__(self, app, path='/', base_url=None, subdomain=None, url_scheme=None, *args, **kwargs) L44
  json_dumps(self, obj, **kwargs) L84
  "Serialize ``obj`` to a JSON-formatted string."

class FlaskClient(Client) L94
  "Works like a regular Werkzeug test client but has some knowledge about"
  __init__(self, *args, **kwargs) L112
  session_transaction(self, *args, **kwargs) L120
  "When used in combination with a ``with`` statement this opens a"
  open(self, *args, as_tuple=False, buffered=False, follow_redirects=False, **kwargs) L174
  copy_environ(other) L185
  __enter__(self) L223
  __exit__(self, exc_type, exc_value, tb) L229

class FlaskCliRunner(CliRunner) L247
  "A :class:`~click.testing.CliRunner` for testing a Flask app's"
  __init__(self, app, **kwargs) L253
  invoke(self, cli=None, args=None, **kwargs) L257
  "Invokes a CLI command in an isolated environment. See"

src/flask/views.py
class View L12
  "Alternative way to use view functions.  A subclass has to implement"
  dispatch_request(self) L61
  "Subclasses have to override this method to implement the"
  as_view(cls, name, *class_args, **class_kwargs) L69
  "Converts the class into an actual view function that can be used"
  view(*args, **kwargs) L81

class MethodViewType(type) L105
  "Metaclass for :class:`MethodView` that determines what methods the view"
  __init__(cls, name, bases, d) L110

class MethodView(View) L132
  "A class-based view that dispatches request methods to the corresponding"
  dispatch_request(self, *args, **kwargs) L148

src/flask/wrappers.py
class Request(RequestBase) L16
  "The request object used by default in Flask.  Remembers the"
  max_content_length(self) L54
  "Read-only view of the ``MAX_CONTENT_LENGTH`` config key."
  endpoint(self) L62
  "The endpoint that matched the request URL."
  blueprint(self) L77
  "The registered name of the current blueprint."
  blueprints(self) L96
  "The registered names of the current blueprint upwards through"
  _load_form_data(self) L112
  on_json_loading_failed(self, e) L127

class Response(ResponseBase) L134
  "The response object that is used by default in Flask.  Works like the"
  max_cookie_size(self) L157
  "Read-only view of the :data:`MAX_COOKIE_SIZE` config key."
