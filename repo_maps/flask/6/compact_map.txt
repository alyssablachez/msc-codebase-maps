src/flask/app.py
class Flask(Scaffold) L92
  "The flask object implements a WSGI application and acts as the central"
  __init__(self, import_name, static_url_path=None, static_folder='static', static_host=None, host_matching=False, subdomain_matching=False, template_folder='templates', instance_path=None, instance_relative_config=False, root_path=None) L378
  _is_setup_finished(self) L516
  name(self) L520
  "The name of the application."
  propagate_exceptions(self) L537
  "Returns the value of the ``PROPAGATE_EXCEPTIONS`` configuration"
  preserve_context_on_exception(self) L549
  "Returns the value of the ``PRESERVE_CONTEXT_ON_EXCEPTION``"
  logger(self) L562
  "A standard Python :class:`~logging.Logger` for the app, with"
  jinja_env(self) L589
  "The Jinja environment used to load templates."
  got_first_request(self) L599
  "This attribute is set to ``True`` if the application started"
  make_config(self, instance_relative=False) L607
  "Used to create the config attribute by the Flask constructor."
  auto_find_instance_path(self) L624
  "Tries to locate the instance path if it was not provided to the"
  open_instance_resource(self, resource, mode='rb') L637
  "Opens a resource from the application's instance folder"
  templates_auto_reload(self) L650
  "Reload templates when they are changed."
  templates_auto_reload(self, value) L665
  create_jinja_environment(self) L668
  "Create the Jinja environment based on :attr:`jinja_options`"
  create_global_jinja_loader(self) L703
  "Creates the loader for the Jinja2 environment."
  select_jinja_autoescape(self, filename) L716
  "Returns ``True`` if autoescaping should be active for the given"
  update_template_context(self, context) L726
  "Update the template context with some commonly used variables."
  make_shell_context(self) L754
  "Returns the shell context for an interactive shell for this"
  debug(self) L778
  "Whether debug mode is enabled."
  debug(self, value) L795
  run(self, host=None, port=None, debug=None, load_dotenv=True, **options) L799
  "Runs the application on a local development server."
  test_client(self, use_cookies=True, **kwargs) L921
  "Creates a test client for this application."
  test_cli_runner(self, **kwargs) L979
  "Create a CLI runner for testing CLI commands."
  register_blueprint(self, blueprint, **options) L997
  "Register a :class:`~flask.Blueprint` on the application."
  iter_blueprints(self) L1024
  "Iterates over all blueprints by the order they were registered."
  add_url_rule(self, rule, endpoint=None, view_func=None, provide_automatic_options=None, **options) L1032
  template_filter(self, name=None) L1091
  "A decorator that is used to register custom template filter."
  decorator(f) L1106
  add_template_filter(self, f, name=None) L1113
  "Register a custom template filter."
  template_test(self, name=None) L1125
  "A decorator that is used to register custom template test."
  decorator(f) L1147
  add_template_test(self, f, name=None) L1154
  "Register a custom template test."
  template_global(self, name=None) L1168
  "A decorator that is used to register a custom template global function."
  decorator(f) L1185
  add_template_global(self, f, name=None) L1192
  "Register a custom template global function."
  before_first_request(self, f) L1206
  "Registers a function to be run before the first request to this"
  teardown_appcontext(self, f) L1221
  "Registers a function to be called when the application context"
  shell_context_processor(self, f) L1254
  "Registers a shell context processor function."
  _find_error_handler(self, e) L1262
  "Return a registered error handler for an exception in this order:"
  handle_http_exception(self, e) L1285
  "Handles an HTTP exception."
  trap_http_exception(self, e) L1320
  "Checks if an HTTP exception should be trapped or not."
  handle_user_exception(self, e) L1355
  "This method is called whenever an exception occurs that"
  handle_exception(self, e) L1387
  "Handle an exception that did not have an error handler"
  log_exception(self, exc_info) L1436
  "Logs an exception."
  raise_routing_exception(self, request) L1453
  "Intercept routing exceptions and possibly do something else."
  dispatch_request(self) L1481
  "Does the request dispatching."
  full_dispatch_request(self) L1505
  "Dispatches the request and on top of that performs request"
  finalize_request(self, rv, from_error_handler=False) L1522
  "Given the return value from a view function this finalizes"
  try_trigger_before_first_request_functions(self) L1551
  "Called before each request and will ensure that it triggers"
  make_default_options_response(self) L1567
  "This method is called to create the default ``OPTIONS`` response."
  should_ignore_error(self, error) L1580
  "This is called to figure out if an error should be ignored"
  ensure_sync(self, func) L1590
  "Ensure that the function is synchronous for WSGI workers."
  async_to_sync(self, func) L1604
  "Return a sync function that will run the coroutine function."
  make_response(self, rv) L1627
  "Convert the return value from a view function to an instance of"
  create_url_adapter(self, request) L1751
  "Creates a URL adapter for the given request."
  inject_url_defaults(self, endpoint, values) L1793
  "Injects the URL defaults for the given endpoint directly into"
  handle_url_build_error(self, error, endpoint, values) L1814
  "Handle :class:`~werkzeug.routing.BuildError` on"
  preprocess_request(self) L1837
  "Called before the request is dispatched."
  process_response(self, response) L1864
  "Can be overridden in order to modify the response object"
  do_teardown_request(self, exc=_sentinel) L1892
  "Called after the request is dispatched and the response is"
  do_teardown_appcontext(self, exc=_sentinel) L1925
  "Called right before the application context is popped."
  app_context(self) L1950
  "Create an :class:`~flask.ctx.AppContext`."
  request_context(self, environ) L1971
  "Create a :class:`~flask.ctx.RequestContext` representing a"
  test_request_context(self, *args, **kwargs) L1987
  "Create a :class:`~flask.ctx.RequestContext` for a WSGI"
  wsgi_app(self, environ, start_response) L2043
  "The actual WSGI application."
  __call__(self, environ, start_response) L2086
  "The WSGI server calls the Flask application object as the"

iscoroutinefunction(func) L75
_make_timedelta(value) L85

src/flask/blueprints.py
class BlueprintSetupState L17
  "Temporary holder object for registering a blueprint with the"
  __init__(self, blueprint, app, options, first_registration) L24
  add_url_rule(self, rule, endpoint=None, view_func=None, **options) L70
  "A helper method to register a rule (and optionally a view function)"

class Blueprint(Scaffold) L102
  "Represents a blueprint, a collection of routes and other"
  __init__(self, name, import_name, static_folder=None, static_url_path=None, template_folder=None, url_prefix=None, subdomain=None, url_defaults=None, root_path=None, cli_group=_sentinel) L165
  _is_setup_finished(self) L201
  record(self, func) L204
  "Registers a function that is called when the blueprint is"
  record_once(self, func) L222
  "Works like :meth:`record` but wraps the function in another"
  wrapper(state) L229
  make_setup_state(self, app, options, first_registration=False) L235
  "Creates an instance of :meth:`~flask.blueprints.BlueprintSetupState`"
  register_blueprint(self, blueprint, **options) L244
  "Register a :class:`~flask.Blueprint` on this blueprint."
  register(self, app, options) L261
  "Called by :meth:`Flask.register_blueprint` to register all"
  extend(bp_dict, parent_dict) L318
  add_url_rule(self, rule, endpoint=None, view_func=None, provide_automatic_options=None, **options) L383
  "Like :meth:`Flask.add_url_rule` but for a blueprint."
  app_template_filter(self, name=None) L410
  "Register a custom template filter, available application wide."
  decorator(f) L420
  add_app_template_filter(self, f, name=None) L426
  "Register a custom template filter, available application wide."
  register_template(state) L437
  app_template_test(self, name=None) L442
  "Register a custom template test, available application wide."
  decorator(f) L454
  add_app_template_test(self, f, name=None) L460
  "Register a custom template test, available application wide."
  register_template(state) L473
  app_template_global(self, name=None) L478
  "Register a custom template global, available application wide."
  decorator(f) L490
  add_app_template_global(self, f, name=None) L496
  "Register a custom template global, available application wide."
  register_template(state) L509
  before_app_request(self, f) L514
  "Like :meth:`Flask.before_request`."
  before_app_first_request(self, f) L525
  "Like :meth:`Flask.before_first_request`."
  after_app_request(self, f) L534
  "Like :meth:`Flask.after_request` but for a blueprint."
  teardown_app_request(self, f) L543
  "Like :meth:`Flask.teardown_request` but for a blueprint."
  app_context_processor(self, f) L553
  "Like :meth:`Flask.context_processor` but for a blueprint."
  app_errorhandler(self, code) L564
  "Like :meth:`Flask.errorhandler` but for a blueprint."
  decorator(f) L571
  app_url_value_preprocessor(self, f) L577
  "Same as :meth:`url_value_preprocessor` but application wide."
  app_url_defaults(self, f) L586
  "Same as :meth:`url_defaults` but application wide."

src/flask/cli.py
class NoAppException(click.UsageError) L22
  "Raised if an application cannot be found or loaded."

class DispatchingApp L262
  "Special application that dispatches to a Flask application which"
  __init__(self, loader, use_eager_loading=None) L269
  _load_in_background(self) L283
  _load_app() L288
  _flush_bg_loading_exception(self) L303
  _load_unlocked(self) L311
  __call__(self, environ, start_response) L317

class ScriptInfo L330
  "Helper object to deal with Flask applications."
  __init__(self, app_import_path=None, create_app=None, set_debug_flag=True) L339
  load_app(self) L351
  "Loads the Flask app (if not yet loaded) and returns it."

class AppGroup(click.Group) L412
  "This works similar to a regular click :class:`~click.Group` but it"
  command(self, *args, **kwargs) L420
  "This works exactly like the method of the same name on a regular"
  decorator(f) L427
  group(self, *args, **kwargs) L434
  "This works exactly like the method of the same name on a regular"

class FlaskGroup(AppGroup) L443
  "Special subclass of the :class:`AppGroup` group that supports"
  __init__(self, add_default_commands=True, create_app=None, add_version_option=True, load_dotenv=True, set_debug_flag=True, **extra) L466
  _load_plugin_commands(self) L492
  get_command(self, ctx, name) L509
  list_commands(self, ctx) L527
  main(self, *args, **kwargs) L548

class CertParamType(click.ParamType) L665
  "Click option type for the ``--cert`` option."
  __init__(self) L673
  convert(self, value, param, ctx) L676

class SeparatedPathType(click.Path) L748
  "Click option type that accepts a list of values separated by the"
  convert(self, value, param, ctx) L754

find_best_app(module) L26
"Given a module instance this tries to find the best possible"
_called_with_wrong_args(f) L79
"Check whether calling a function raised a ``TypeError`` because"
find_app_by_string(module, app_name) L105
"Check if the given string is a variable name or a function."
prepare_import(path) L181
"Given a filename this will try to calculate the python path, add it"
locate_app(module_name, app_name, raise_if_not_found=True) L210
get_version(ctx, param, value) L236
with_appcontext(f) L397
"Wraps a callback so that it's guaranteed to be executed with the"
decorator(__ctx, *args, **kwargs) L405
_path_is_ancestor(path, other) L570
"Take ``other`` and remove the length of ``path`` from it."
load_dotenv(path=None) L577
"Load "dotenv" files in order of precedence to set environment variables."
show_server_banner(env, debug, app_import_path, eager_loading) L636
"Show extra startup messages the first time the server is run,"
_validate_key(ctx, param, value) L711
"The ``--key`` option must be specified when ``--cert`` is a file."
run_command(info, host, port, reload, debugger, eager_loading, with_threads, cert, extra_files, exclude_patterns) L819
"Run a local development server."
shell_command() L867
"Run an interactive Python shell in the context of a given"
routes_command(sort, all_methods) L929
"Show all registered routes with endpoints and methods."
main() L985

src/flask/config.py
class ConfigAttribute L10
  "Makes an attribute forward to the config"
  __init__(self, name, get_converter=None) L13
  __get__(self, obj, owner=None) L17
  __set__(self, obj, value) L25

class Config(dict) L29
  "Works exactly like a dict but provides ways to fill it from files"
  __init__(self, root_path, defaults=None) L73
  from_envvar(self, variable_name, silent=False) L77
  "Loads a configuration from an environment variable pointing to"
  from_prefixed_env(self, prefix='FLASK', *, loads=json.loads) L101
  "Load any environment variables that start with ``FLASK_``,"
  from_pyfile(self, filename, silent=False) L165
  "Updates the values in the config from a Python file."
  from_object(self, obj) L194
  "Updates the values from the given object."
  from_file(self, filename, load, silent=False) L232
  "Update the values in the config from a file that is loaded"
  from_mapping(self, mapping=None, **kwargs) L275
  "Updates the config like :meth:`update` ignoring items with non-upper"
  get_namespace(self, namespace, lowercase=True, trim_namespace=True) L293
  "Returns a dictionary containing a subset of configuration options"
  __repr__(self) L336

src/flask/ctx.py
class _AppCtxGlobals L24
  "A plain object."
  __getattr__(self, name) L47
  __setattr__(self, name, value) L53
  __delattr__(self, name) L56
  get(self, name, default=None) L62
  "Get an attribute by name, or a default value."
  pop(self, name, default=_sentinel) L73
  "Get and remove an attribute by name."
  setdefault(self, name, default=None) L87
  "Get the value of an attribute if it is present, otherwise"
  __contains__(self, item) L99
  __iter__(self) L102
  __repr__(self) L105

class AppContext L228
  "The application context binds an application object implicitly"
  __init__(self, app) L237
  push(self) L246
  "Binds the app context to the current context."
  pop(self, exc=_sentinel) L252
  "Pops the app context."
  __enter__(self) L265
  __exit__(self, exc_type, exc_value, tb) L269

class RequestContext L278
  "The request context contains all request relevant information."
  __init__(self, app, environ, request=None, session=None) L308
  g(self) L347
  g(self, value) L360
  copy(self) L372
  "Creates a copy of this request context with the same request object."
  match_request(self) L392
  "Can be overridden by a subclass to hook into the matching"
  push(self) L402
  "Binds the request context to the current context."
  pop(self, exc=_sentinel) L444
  "Pops the request context and unbinds it by doing that."
  auto_pop(self, exc) L483
  __enter__(self) L492
  __exit__(self, exc_type, exc_value, tb) L496
  __repr__(self) L509

after_this_request(f) L112
"Executes a function after this request."
copy_current_request_context(f) L145
"A helper function that decorates a function to retain the current"
wrapper(*args, **kwargs) L179
has_request_context() L186
"If you have code that wants to test if a request context is there or"
has_app_context() L218
"Works like :func:`has_request_context` but for the application"

src/flask/debughelpers.py
class UnexpectedUnicodeError(AssertionError, UnicodeError) L10
  "Raised in places where we want some better error reporting for"

class DebugFilesKeyError(KeyError, AssertionError) L16
  "Raised from request.files during debugging."
  __init__(self, request, key) L21
  __str__(self) L39

class FormDataRoutingRedirect(AssertionError) L43
  "This exception is raised in debug mode if a routing redirect"
  __init__(self, request) L50

class newcls(oldcls) L82
  __getitem__(self, key) L83

attach_enctype_error_multidict(request) L73
"Patch ``request.files.__getitem__`` to raise a descriptive error"
_dump_loader_info(loader) L99
explain_template_loading_attempts(app, template, attempts) L116
"This should help developers understand what failed"
explain_ignored_app_run() L164

src/flask/globals.py
_lookup_req_object(name) L30
_lookup_app_object(name) L37
_find_app() L44

src/flask/helpers.py
class locked_cached_property(werkzeug.utils.cached_property) L729
  "A :func:`property` that is only evaluated once."
  __init__(self, fget, name=None, doc=None) L738
  __get__(self, obj, type=None) L747
  __set__(self, obj, value) L754
  __delete__(self, obj) L758

get_env() L27
"Get the environment the app is running in, indicated by the"
get_debug_flag() L35
"Get whether debug mode should be enabled for the app, indicated"
get_load_dotenv(default=True) L49
"Get whether the user has disabled loading dotenv files by setting"
stream_with_context(generator_or_function) L64
"Request contexts disappear when the response is started on the server."
decorator(*args, **kwargs) L106
generator() L112
make_response(*args) L143
"Sometimes it is necessary to set additional headers in a view."
url_for(endpoint, **values) L192
"Generates a URL to the given endpoint with the method provided."
get_template_attribute(template_name, attribute) L343
"Loads a macro (or variable) a template exports."
flash(message, category='message') L365
"Flashes a message to the next request."
get_flashed_messages(with_categories=False, category_filter=()) L397
"Pulls all flashed messages from the session and returns them."
_prepare_send_file_kwargs(download_name=None, attachment_filename=None, etag=None, add_etags=None, max_age=None, cache_timeout=None, **kwargs) L440
send_file(path_or_file, mimetype=None, as_attachment=False, download_name=None, attachment_filename=None, conditional=True, etag=True, add_etags=None, last_modified=None, max_age=None, cache_timeout=None) L494
"Send the contents of a file to the client."
send_from_directory(directory, path, filename=None, **kwargs) L628
"Send a file from within a directory using :func:`send_file`."
get_root_path(import_name) L680
"Find the root path of a package, or the path that contains a"
is_ip(value) L763
"Determine if the given string is an IP address."
_split_blueprint_path(name) L784

src/flask/json/__init__.py
class JSONEncoder(_json.JSONEncoder) L19
  "The default JSON encoder."
  default(self, o) L36
  "Convert ``o`` to a JSON serializable type."

class JSONDecoder(_json.JSONDecoder) L53
  "The default JSON decoder."

_dump_arg_defaults(kwargs, app=None) L64
"Inject default arguments for dump functions."
_load_arg_defaults(kwargs, app=None) L90
"Inject default arguments for load functions."
dumps(obj, app=None, **kwargs) L109
"Serialize an object to a string of JSON."
dump(obj, fp, app=None, **kwargs) L134
"Serialize an object to JSON written to a file object."
loads(s, app=None, **kwargs) L156
"Deserialize an object from a string of JSON."
load(fp, app=None, **kwargs) L183
"Deserialize an object from JSON read from a file object."
htmlsafe_dumps(obj, **kwargs) L202
"Serialize an object to a string of JSON with :func:`dumps`, then"
htmlsafe_dump(obj, fp, **kwargs) L226
"Serialize an object to JSON written to a file object, replacing"
jsonify(*args, **kwargs) L234
"Serialize data to JSON and wrap it in a :class:`~flask.Response`"

src/flask/json/tag.py
class JSONTag L57
  "Base class for defining type tags for :class:`TaggedJSONSerializer`."
  __init__(self, serializer) L66
  "Create a tagger for the given serializer."
  check(self, value) L70
  "Check if the given value should be tagged by this tag."
  to_json(self, value) L74
  "Convert the Python object to an object that is a valid JSON type."
  to_python(self, value) L79
  "Convert the JSON representation back to the correct type."
  tag(self, value) L84
  "Convert the value to a valid JSON type and add the tag structure"

class TagDict(JSONTag) L90
  "Tag for 1-item dicts whose only key matches a registered tag."
  check(self, value) L100
  to_json(self, value) L107
  to_python(self, value) L111

class PassDict(JSONTag) L116
  check(self, value) L119
  to_json(self, value) L122

class TagTuple(JSONTag) L130
  check(self, value) L134
  to_json(self, value) L137
  to_python(self, value) L140

class PassList(JSONTag) L144
  check(self, value) L147
  to_json(self, value) L150

class TagBytes(JSONTag) L156
  check(self, value) L160
  to_json(self, value) L163
  to_python(self, value) L166

class TagMarkup(JSONTag) L170
  "Serialize anything matching the :class:`~markupsafe.Markup` API by"
  check(self, value) L178
  to_json(self, value) L181
  to_python(self, value) L184

class TagUUID(JSONTag) L188
  check(self, value) L192
  to_json(self, value) L195
  to_python(self, value) L198

class TagDateTime(JSONTag) L202
  check(self, value) L206
  to_json(self, value) L209
  to_python(self, value) L212

class TaggedJSONSerializer L216
  "Serializer that uses a tag system to compactly represent objects that"
  __init__(self) L246
  register(self, tag_class, force=False, index=None) L253
  "Register a new tag with this serializer."
  tag(self, value) L286
  "Convert a value to a tagged representation if necessary."
  untag(self, value) L294
  "Convert a tagged representation back to the original type."
  dumps(self, value) L306
  "Tag the value and dump it to a compact JSON string."
  loads(self, value) L310
  "Load data from a JSON string and deserialized any tagged objects."

src/flask/logging.py
wsgi_errors_stream() L14
"Find the most appropriate error stream for the application."
has_level_handler(logger) L26
"Check if there is a handler in the logging chain that will handle the"
create_logger(app) L53
"Get the Flask app's logger and configure it if needed."

src/flask/scaffold.py
class Scaffold L54
  "Common behavior shared between :class:`~flask.Flask` and"
  __init__(self, import_name, static_folder=None, static_url_path=None, template_folder=None, root_path=None) L84
  __repr__(self) L232
  _is_setup_finished(self) L235
  static_folder(self) L239
  "The absolute path to the configured static folder."
  static_folder(self, value) L249
  has_static_folder(self) L256
  "``True`` if :attr:`static_folder` is set."
  static_url_path(self) L264
  "The URL prefix that the static route will be accessible from."
  static_url_path(self, value) L280
  get_send_file_max_age(self, filename) L286
  "Used by :func:`send_file` to determine the ``max_age`` cache"
  send_static_file(self, filename) L307
  "The view function used to serve files from"
  jinja_loader(self) L326
  "The Jinja loader for this object's templates."
  open_resource(self, resource, mode='rb') L338
  "Open a resource file relative to :attr:`root_path` for"
  _method_route(self, method, rule, options) L361
  get(self, rule, **options) L372
  "Shortcut for :meth:`route` with ``methods=["GET"]``."
  post(self, rule, **options) L381
  "Shortcut for :meth:`route` with ``methods=["POST"]``."
  put(self, rule, **options) L390
  "Shortcut for :meth:`route` with ``methods=["PUT"]``."
  delete(self, rule, **options) L399
  "Shortcut for :meth:`route` with ``methods=["DELETE"]``."
  patch(self, rule, **options) L408
  "Shortcut for :meth:`route` with ``methods=["PATCH"]``."
  route(self, rule, **options) L417
  "Decorate a view function to register it with the given URL"
  decorator(f) L443
  add_url_rule(self, rule, endpoint=None, view_func=None, provide_automatic_options=None, **options) L451
  "Register a rule for routing incoming requests and building"
  endpoint(self, endpoint) L518
  "Decorate a view function to register it for the given"
  decorator(f) L535
  before_request(self, f) L542
  "Register a function to run before each request."
  after_request(self, f) L564
  "Register a function to run after each request to this object."
  teardown_request(self, f) L580
  "Register a function to be run at the end of each request,"
  context_processor(self, f) L619
  "Registers a template context processor function."
  url_value_preprocessor(self, f) L627
  "Register a URL value preprocessor function for all view"
  url_defaults(self, f) L646
  "Callback function for URL defaults for all view functions of the"
  errorhandler(self, code_or_exception) L655
  "Register a function to handle errors by code or exception class."
  decorator(f) L687
  register_error_handler(self, code_or_exception, f) L694
  "Alternative error attach function to the :meth:`errorhandler`"
  _get_exc_class_and_code(exc_class_or_code) L724
  "Get the exception class being handled."

setupmethod(f) L32
"Wraps a method so that it performs a check in debug mode if the"
wrapper_func(self, *args, **kwargs) L37
_endpoint_from_view_func(view_func) L750
"Internal helper that returns the default endpoint for a given"
_matching_loader_thinks_module_is_package(loader, mod_name) L758
"Attempt to figure out if the given name is a package or a module."
_find_package_path(root_mod_name) L783
"Find the path that contains the package or module."
find_package(import_name) L835
"Find the prefix that a package is installed under, and the path"

src/flask/sessions.py
class SessionMixin(MutableMapping) L20
  "Expands a basic dictionary with session attributes."
  permanent(self) L24
  "This reflects the ``'_permanent'`` key in the dict."
  permanent(self, value) L29

class SecureCookieSession(CallbackDict, SessionMixin) L48
  "Base class for sessions based on signed cookies."
  __init__(self, initial=None) L70
  on_update(self) L71
  __getitem__(self, key) L77
  get(self, key, default=None) L81
  setdefault(self, key, default=None) L85

class NullSession(SecureCookieSession) L90
  "Class used to generate nicer error messages if sessions are not"
  _fail(self, *args, **kwargs) L96

class SessionInterface L107
  "The basic interface you have to implement in order to replace the"
  make_null_session(self, app) L157
  "Creates a null session which acts as a replacement object if the"
  is_null_session(self, obj) L169
  "Checks if a given object is a null session."
  get_cookie_name(self, app) L178
  "Returns the name of the session cookie."
  get_cookie_domain(self, app) L185
  "Returns the domain that should be set for the session cookie."
  get_cookie_path(self, app) L243
  "Returns the path for which the cookie should be valid."
  get_cookie_httponly(self, app) L251
  "Returns True if the session cookie should be httponly."
  get_cookie_secure(self, app) L258
  "Returns True if the cookie should be secure."
  get_cookie_samesite(self, app) L264
  "Return ``'Strict'`` or ``'Lax'`` if the cookie should use the"
  get_expiration_time(self, app, session) L271
  "A helper method that returns an expiration date for the session"
  should_set_cookie(self, app, session) L283
  "Used by session backends to determine if a ``Set-Cookie`` header"
  open_session(self, app, request) L299
  "This is called at the beginning of each request, after"
  save_session(self, app, session, response) L315
  "This is called at the end of each request, after generating"

class SecureCookieSessionInterface(SessionInterface) L328
  "The default session interface that stores sessions in signed cookies"
  get_signing_serializer(self, app) L347
  open_session(self, app, request) L362
  save_session(self, app, session, response) L378

src/flask/signals.py
class Namespace L10
  signal(self, name, doc=None) L11

class _FakeSignal L14
  "If blinker is unavailable, create a fake class with the same"
  __init__(self, name, doc=None) L21
  send(self, *args, **kwargs) L25
  _fail(self, *args, **kwargs) L28

src/flask/templating.py
class Environment(BaseEnvironment) L33
  "Works like a regular Jinja2 environment but has some additional"
  __init__(self, app, **options) L39

class DispatchingJinjaLoader(BaseLoader) L46
  "A loader that looks for templates in the application and all"
  __init__(self, app) L51
  get_source(self, environment, template) L54
  _get_source_explained(self, environment, template) L61
  _get_source_fast(self, environment, template) L87
  _iter_loaders(self, template) L97
  list_templates(self) L109

_default_template_ctx_processor() L18
"Default template context processor."
_render(template, context, app) L124
"Renders the template and fires the signal"
render_template(template_name_or_list, **context) L133
"Renders a template from the template folder with the given"
render_template_string(source, **context) L155
"Renders a template from the given template source string"

src/flask/testing.py
class EnvironBuilder(werkzeug.test.EnvironBuilder) L23
  "An :class:`~werkzeug.test.EnvironBuilder`, that takes defaults from the"
  __init__(self, app, path='/', base_url=None, subdomain=None, url_scheme=None, *args, **kwargs) L45
  json_dumps(self, obj, **kwargs) L85
  "Serialize ``obj`` to a JSON-formatted string."

class FlaskClient(Client) L95
  "Works like a regular Werkzeug test client but has some knowledge about"
  __init__(self, *args, **kwargs) L113
  session_transaction(self, *args, **kwargs) L121
  "When used in combination with a ``with`` statement this opens a"
  _copy_environ(self, other) L175
  _request_from_builder_args(self, args, kwargs) L182
  open(self, *args, buffered=False, follow_redirects=False, **kwargs) L191
  __enter__(self) L223
  __exit__(self, exc_type, exc_value, tb) L229

class FlaskCliRunner(CliRunner) L250
  "A :class:`~click.testing.CliRunner` for testing a Flask app's"
  __init__(self, app, **kwargs) L256
  invoke(self, cli=None, args=None, **kwargs) L260
  "Invokes a CLI command in an isolated environment."

src/flask/views.py
class View L13
  "Alternative way to use view functions."
  dispatch_request(self) L62
  "Subclasses have to override this method to implement the"
  as_view(cls, name, *class_args, **class_kwargs) L70
  "Converts the class into an actual view function that can be used"
  view(*args, **kwargs) L82

class MethodViewType(type) L106
  "Metaclass for :class:`MethodView` that determines what methods the view"
  __init__(cls, name, bases, d) L111

class MethodView(View) L133
  "A class-based view that dispatches request methods to the corresponding"
  dispatch_request(self, *args, **kwargs) L149

src/flask/wrappers.py
class Request(RequestBase) L15
  "The request object used by default in Flask."
  max_content_length(self) L53
  "Read-only view of the ``MAX_CONTENT_LENGTH`` config key."
  endpoint(self) L61
  "The endpoint that matched the request URL."
  blueprint(self) L76
  "The registered name of the current blueprint."
  blueprints(self) L95
  "The registered names of the current blueprint upwards through"
  _load_form_data(self) L111
  on_json_loading_failed(self, e) L126

class Response(ResponseBase) L136
  "The response object that is used by default in Flask."
  max_cookie_size(self) L161
  "Read-only view of the :data:`MAX_COOKIE_SIZE` config key."
