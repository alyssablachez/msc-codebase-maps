src/flask/_compat.py
class metaclass(type) L65
  __new__(metacls, name, this_bases, d) L66

class _Mgr(object) L86
  __enter__(self) L87
  __exit__(self, *args) L90

reraise(tp, value, tb=None) L36
implements_to_string(cls) L54
with_metaclass(meta, *bases) L60
"Create a base class with a metaclass."
fspath(path) L114

src/flask/app.py
class Flask(_PackageBoundObject) L103
  "The flask object implements a WSGI application and acts as the central"
  __init__(self, import_name, static_url_path=None, static_folder='static', static_host=None, host_matching=False, subdomain_matching=False, template_folder='templates', instance_path=None, instance_relative_config=False, root_path=None) L402
  name(self) L613
  "The name of the application."
  propagate_exceptions(self) L630
  "Returns the value of the ``PROPAGATE_EXCEPTIONS`` configuration"
  preserve_context_on_exception(self) L642
  "Returns the value of the ``PRESERVE_CONTEXT_ON_EXCEPTION``"
  logger(self) L655
  "The ``'flask.app'`` logger, a standard Python"
  jinja_env(self) L678
  "The Jinja environment used to load templates."
  got_first_request(self) L688
  "This attribute is set to ``True`` if the application started"
  make_config(self, instance_relative=False) L696
  "Used to create the config attribute by the Flask constructor."
  auto_find_instance_path(self) L713
  "Tries to locate the instance path if it was not provided to the"
  open_instance_resource(self, resource, mode='rb') L726
  "Opens a resource from the application's instance folder"
  templates_auto_reload(self) L739
  "Reload templates when they are changed."
  templates_auto_reload(self, value) L754
  create_jinja_environment(self) L757
  "Create the Jinja environment based on :attr:`jinja_options`"
  create_global_jinja_loader(self) L792
  "Creates the loader for the Jinja2 environment."
  select_jinja_autoescape(self, filename) L805
  "Returns ``True`` if autoescaping should be active for the given"
  update_template_context(self, context) L815
  "Update the template context with some commonly used variables."
  make_shell_context(self) L840
  "Returns the shell context for an interactive shell for this"
  debug(self) L864
  "Whether debug mode is enabled."
  debug(self, value) L881
  run(self, host=None, port=None, debug=None, load_dotenv=True, **options) L885
  "Runs the application on a local development server."
  test_client(self, use_cookies=True, **kwargs) L993
  "Creates a test client for this application."
  test_cli_runner(self, **kwargs) L1049
  "Create a CLI runner for testing CLI commands."
  open_session(self, request) L1066
  "Creates or opens a new session."
  save_session(self, session, response) L1087
  "Saves the session if it needs updates."
  make_null_session(self) L1110
  "Creates a new instance of a missing session."
  register_blueprint(self, blueprint, **options) L1130
  "Register a :class:`~flask.Blueprint` on the application."
  iter_blueprints(self) L1165
  "Iterates over all blueprints by the order they were registered."
  add_url_rule(self, rule, endpoint=None, view_func=None, provide_automatic_options=None, **options) L1173
  "Connects a URL rule."
  route(self, rule, **options) L1283
  "A decorator that is used to register a view function for a"
  decorator(f) L1308
  endpoint(self, endpoint) L1316
  "A decorator to register a function as an endpoint."
  decorator(f) L1327
  _get_exc_class_and_code(exc_class_or_code) L1334
  "Get the exception class being handled."
  errorhandler(self, code_or_exception) L1355
  "Register a function to handle errors by code or exception class."
  decorator(f) L1385
  register_error_handler(self, code_or_exception, f) L1392
  "Alternative error attach function to the :meth:`errorhandler`"
  _register_error_handler(self, key, code_or_exception, f) L1402
  ":type key: None|str"
  template_filter(self, name=None) L1427
  "A decorator that is used to register custom template filter."
  decorator(f) L1440
  add_template_filter(self, f, name=None) L1447
  "Register a custom template filter."
  template_test(self, name=None) L1457
  "A decorator that is used to register custom template test."
  decorator(f) L1477
  add_template_test(self, f, name=None) L1484
  "Register a custom template test."
  template_global(self, name=None) L1496
  "A decorator that is used to register a custom template global function."
  decorator(f) L1511
  add_template_global(self, f, name=None) L1518
  "Register a custom template global function."
  before_request(self, f) L1530
  "Registers a function to run before each request."
  before_first_request(self, f) L1544
  "Registers a function to be run before the first request to this"
  after_request(self, f) L1557
  "Register a function to be run after each request."
  teardown_request(self, f) L1571
  "Register a function to be run at the end of each request,"
  teardown_appcontext(self, f) L1610
  "Registers a function to be called when the application context"
  context_processor(self, f) L1643
  "Registers a template context processor function."
  shell_context_processor(self, f) L1649
  "Registers a shell context processor function."
  url_value_preprocessor(self, f) L1658
  "Register a URL value preprocessor function for all view"
  url_defaults(self, f) L1675
  "Callback function for URL defaults for all view functions of the"
  _find_error_handler(self, e) L1683
  "Return a registered error handler for an exception in this order:"
  handle_http_exception(self, e) L1708
  "Handles an HTTP exception."
  trap_http_exception(self, e) L1741
  "Checks if an HTTP exception should be trapped or not."
  handle_user_exception(self, e) L1776
  "This method is called whenever an exception occurs that"
  handle_exception(self, e) L1819
  "Handle an exception that did not have an error handler"
  log_exception(self, exc_info) L1878
  "Logs an exception."
  raise_routing_exception(self, request) L1890
  "Exceptions that are recording during routing are reraised with"
  dispatch_request(self) L1909
  "Does the request dispatching."
  full_dispatch_request(self) L1933
  "Dispatches the request and on top of that performs request"
  finalize_request(self, rv, from_error_handler=False) L1950
  "Given the return value from a view function this finalizes"
  try_trigger_before_first_request_functions(self) L1975
  "Called before each request and will ensure that it triggers"
  make_default_options_response(self) L1991
  "This method is called to create the default ``OPTIONS`` response."
  should_ignore_error(self, error) L2014
  "This is called to figure out if an error should be ignored"
  make_response(self, rv) L2024
  "Convert the return value from a view function to an instance of"
  create_url_adapter(self, request) L2142
  "Creates a URL adapter for the given request."
  inject_url_defaults(self, endpoint, values) L2180
  "Injects the URL defaults for the given endpoint directly into"
  handle_url_build_error(self, error, endpoint, values) L2194
  "Handle :class:`~werkzeug.routing.BuildError` on :meth:`url_for`."
  preprocess_request(self) L2214
  "Called before the request is dispatched."
  process_response(self, response) L2241
  "Can be overridden in order to modify the response object"
  do_teardown_request(self, exc=_sentinel) L2267
  "Called after the request is dispatched and the response is"
  do_teardown_appcontext(self, exc=_sentinel) L2298
  "Called right before the application context is popped."
  app_context(self) L2319
  "Create an :class:`~flask.ctx.AppContext`."
  request_context(self, environ) L2340
  "Create a :class:`~flask.ctx.RequestContext` representing a"
  test_request_context(self, *args, **kwargs) L2356
  "Create a :class:`~flask.ctx.RequestContext` for a WSGI"
  wsgi_app(self, environ, start_response) L2412
  "The actual WSGI application."
  __call__(self, environ, start_response) L2455
  "The WSGI server calls the Flask application object as the"
  __repr__(self) L2461

_make_timedelta(value) L76
setupmethod(f) L82
"Wraps a method so that it performs a check in debug mode if the"
wrapper_func(self, *args, **kwargs) L87

src/flask/blueprints.py
class BlueprintSetupState(object) L21
  "Temporary holder object for registering a blueprint with the"
  __init__(self, blueprint, app, options, first_registration) L28
  add_url_rule(self, rule, endpoint=None, view_func=None, **options) L65
  "A helper method to register a rule (and optionally a view function)"

class Blueprint(_PackageBoundObject) L90
  "Represents a blueprint, a collection of routes and other"
  __init__(self, name, import_name, static_folder=None, static_url_path=None, template_folder=None, url_prefix=None, subdomain=None, url_defaults=None, root_path=None, cli_group=_sentinel) L168
  record(self, func) L195
  "Registers a function that is called when the blueprint is"
  record_once(self, func) L213
  "Works like :meth:`record` but wraps the function in another"
  wrapper(state) L220
  make_setup_state(self, app, options, first_registration=False) L226
  "Creates an instance of :meth:`~flask.blueprints.BlueprintSetupState`"
  register(self, app, options, first_registration=False) L233
  "Called by :meth:`Flask.register_blueprint` to register all views"
  route(self, rule, **options) L272
  "Like :meth:`Flask.route` but for a blueprint."
  decorator(f) L277
  add_url_rule(self, rule, endpoint=None, view_func=None, **options) L284
  "Like :meth:`Flask.add_url_rule` but for a blueprint."
  endpoint(self, endpoint) L296
  "Like :meth:`Flask.endpoint` but for a blueprint."
  decorator(f) L304
  register_endpoint(state) L305
  app_template_filter(self, name=None) L313
  "Register a custom template filter, available application wide."
  decorator(f) L321
  add_app_template_filter(self, f, name=None) L327
  "Register a custom template filter, available application wide."
  register_template(state) L336
  app_template_test(self, name=None) L341
  "Register a custom template test, available application wide."
  decorator(f) L351
  add_app_template_test(self, f, name=None) L357
  "Register a custom template test, available application wide."
  register_template(state) L368
  app_template_global(self, name=None) L373
  "Register a custom template global, available application wide."
  decorator(f) L383
  add_app_template_global(self, f, name=None) L389
  "Register a custom template global, available application wide."
  register_template(state) L400
  before_request(self, f) L405
  "Like :meth:`Flask.before_request` but for a blueprint."
  before_app_request(self, f) L415
  "Like :meth:`Flask.before_request`."
  before_app_first_request(self, f) L424
  "Like :meth:`Flask.before_first_request`."
  after_request(self, f) L431
  "Like :meth:`Flask.after_request` but for a blueprint."
  after_app_request(self, f) L441
  "Like :meth:`Flask.after_request` but for a blueprint."
  teardown_request(self, f) L450
  "Like :meth:`Flask.teardown_request` but for a blueprint."
  teardown_app_request(self, f) L462
  "Like :meth:`Flask.teardown_request` but for a blueprint."
  context_processor(self, f) L472
  "Like :meth:`Flask.context_processor` but for a blueprint."
  app_context_processor(self, f) L483
  "Like :meth:`Flask.context_processor` but for a blueprint."
  app_errorhandler(self, code) L492
  "Like :meth:`Flask.errorhandler` but for a blueprint."
  decorator(f) L497
  url_value_preprocessor(self, f) L503
  "Registers a function as URL value preprocessor for this"
  url_defaults(self, f) L513
  "Callback function for URL defaults for this blueprint."
  app_url_value_preprocessor(self, f) L523
  "Same as :meth:`url_value_preprocessor` but application wide."
  app_url_defaults(self, f) L531
  "Same as :meth:`url_defaults` but application wide."
  errorhandler(self, code_or_exception) L539
  "Registers an error handler that becomes active for this blueprint"
  decorator(f) L551
  register_error_handler(self, code_or_exception, f) L559
  "Non-decorator version of the :meth:`errorhandler` error attach"

src/flask/cli.py
class NoAppException(click.UsageError) L48
  "Raised if an application cannot be found or loaded."

class DispatchingApp(object) L292
  "Special application that dispatches to a Flask application which"
  __init__(self, loader, use_eager_loading=False) L299
  _load_in_background(self) L309
  _load_app() L310
  _flush_bg_loading_exception(self) L321
  _load_unlocked(self) L328
  __call__(self, environ, start_response) L334

class ScriptInfo(object) L347
  "Helper object to deal with Flask applications."
  __init__(self, app_import_path=None, create_app=None, set_debug_flag=True) L356
  load_app(self) L368
  "Loads the Flask app (if not yet loaded) and returns it."

class AppGroup(click.Group) L431
  "This works similar to a regular click :class:`~click.Group` but it"
  command(self, *args, **kwargs) L439
  "This works exactly like the method of the same name on a regular"
  decorator(f) L446
  group(self, *args, **kwargs) L453
  "This works exactly like the method of the same name on a regular"

class FlaskGroup(AppGroup) L462
  "Special subclass of the :class:`AppGroup` group that supports"
  __init__(self, add_default_commands=True, create_app=None, add_version_option=True, load_dotenv=True, set_debug_flag=True, **extra) L487
  _load_plugin_commands(self) L513
  get_command(self, ctx, name) L526
  list_commands(self, ctx) L548
  main(self, *args, **kwargs) L567

class CertParamType(click.ParamType) L685
  "Click option type for the ``--cert`` option."
  __init__(self) L693
  convert(self, value, param, ctx) L696

class SeparatedPathType(click.Path) L766
  "Click option type that accepts a list of values separated by the"
  convert(self, value, param, ctx) L772

find_best_app(script_info, module) L52
"Given a module instance this tries to find the best possible"
call_factory(script_info, app_factory, arguments=()) L103
"Takes an app factory, a ``script_info` object and  optionally a tuple"
_called_with_wrong_args(factory) L122
"Check whether calling a function raised a ``TypeError`` because"
find_app_by_string(script_info, module, app_name) L148
"Checks if the given string is a variable name or a function."
prepare_import(path) L207
"Given a filename this will try to calculate the python path, add it"
locate_app(script_info, module_name, app_name, raise_if_not_found=True) L236
get_version(ctx, param, value) L262
with_appcontext(f) L416
"Wraps a callback so that it's guaranteed to be executed with the"
decorator(__ctx, *args, **kwargs) L424
_path_is_ancestor(path, other) L589
"Take ``other`` and remove the length of ``path`` from it."
load_dotenv(path=None) L596
"Load "dotenv" files in order of precedence to set environment variables."
show_server_banner(env, debug, app_import_path, eager_loading) L656
"Show extra startup messages the first time the server is run,"
_validate_key(ctx, param, value) L731
"The ``--key`` option must be specified when ``--cert`` is a file."
run_command(info, host, port, reload, debugger, eager_loading, with_threads, cert, extra_files) L824
"Run a local development server."
shell_command() L865
"Run an interactive Python shell in the context of a given"
routes_command(sort, all_methods) L911
"Show all registered routes with endpoints and methods."
main(as_module=False) L964

src/flask/config.py
class ConfigAttribute(object) L22
  "Makes an attribute forward to the config"
  __init__(self, name, get_converter=None) L25
  __get__(self, obj, type=None) L29
  __set__(self, obj, value) L37

class Config(dict) L41
  "Works exactly like a dict but provides ways to fill it from files"
  __init__(self, root_path, defaults=None) L85
  from_envvar(self, variable_name, silent=False) L89
  "Loads a configuration from an environment variable pointing to"
  from_pyfile(self, filename, silent=False) L113
  "Updates the values in the config from a Python file."
  from_object(self, obj) L141
  "Updates the values from the given object."
  from_json(self, filename, silent=False) L179
  "Updates the values in the config from a JSON file."
  from_mapping(self, *mapping, **kwargs) L204
  "Updates the config like :meth:`update` ignoring items with non-upper"
  get_namespace(self, namespace, lowercase=True, trim_namespace=True) L227
  "Returns a dictionary containing a subset of configuration options"
  __repr__(self) L268

src/flask/ctx.py
class _AppCtxGlobals(object) L28
  "A plain object."
  get(self, name, default=None) L48
  "Get an attribute by name, or a default value."
  pop(self, name, default=_sentinel) L59
  "Get and remove an attribute by name."
  setdefault(self, name, default=None) L73
  "Get the value of an attribute if it is present, otherwise"
  __contains__(self, item) L85
  __iter__(self) L88
  __repr__(self) L91

class AppContext(object) L205
  "The application context binds an application object implicitly"
  __init__(self, app) L214
  push(self) L223
  "Binds the app context to the current context."
  pop(self, exc=_sentinel) L231
  "Pops the app context."
  __enter__(self) L244
  __exit__(self, exc_type, exc_value, tb) L248

class RequestContext(object) L255
  "The request context contains all request relevant information."
  __init__(self, app, environ, request=None, session=None) L285
  g(self) L318
  g(self, value) L322
  copy(self) L325
  "Creates a copy of this request context with the same request object."
  match_request(self) L345
  "Can be overridden by a subclass to hook into the matching"
  push(self) L355
  "Binds the request context to the current context."
  pop(self, exc=_sentinel) L398
  "Pops the request context and unbinds it by doing that."
  auto_pop(self, exc) L445
  __enter__(self) L454
  __exit__(self, exc_type, exc_value, tb) L458
  __repr__(self) L469

after_this_request(f) L98
"Executes a function after this request."
copy_current_request_context(f) L123
"A helper function that decorates a function to retain the current"
wrapper(*args, **kwargs) L156
has_request_context() L163
"If you have code that wants to test if a request context is there or"
has_app_context() L195
"Works like :func:`has_request_context` but for the application"

src/flask/debughelpers.py
class UnexpectedUnicodeError(AssertionError, UnicodeError) L21
  "Raised in places where we want some better error reporting for"

class DebugFilesKeyError(KeyError, AssertionError) L28
  "Raised from request.files during debugging."
  __init__(self, request, key) L33
  __str__(self) L50

class FormDataRoutingRedirect(AssertionError) L54
  "This exception is raised by Flask in debug mode if it detects a"
  __init__(self, request) L60

class newcls(oldcls) L93
  __getitem__(self, key) L94

attach_enctype_error_multidict(request) L86
"Since Flask 0.8 we're monkeypatching the files object in case a"
_dump_loader_info(loader) L107
explain_template_loading_attempts(app, template, attempts) L124
"This should help developers understand what failed"
explain_ignored_app_run() L172

src/flask/globals.py
_lookup_req_object(name) L35
_lookup_app_object(name) L42
_find_app() L49

src/flask/helpers.py
class locked_cached_property(object) L936
  "A decorator that converts a function into a lazy property."
  __init__(self, func, name=None, doc=None) L944
  __get__(self, obj, type=None) L951

class _PackageBoundObject(object) L962
  __init__(self, import_name, template_folder=None, root_path=None) L975
  static_folder(self) L996
  "The absolute path to the configured static folder."
  static_folder(self, value) L1002
  static_url_path(self) L1006
  "The URL prefix that the static route will be accessible from."
  static_url_path(self, value) L1020
  has_static_folder(self) L1027
  "This is ``True`` if the package bound object's container has a"
  jinja_loader(self) L1036
  "The Jinja loader for this package bound object."
  get_send_file_max_age(self, filename) L1044
  "Provides default cache_timeout for the :func:`send_file` functions."
  send_static_file(self, filename) L1070
  "Function used internally to send static files from the static"
  open_resource(self, resource, mode='rb') L1085
  "Opens a resource from the application's resource folder."

get_env() L56
"Get the environment the app is running in, indicated by the"
get_debug_flag() L64
"Get whether debug mode should be enabled for the app, indicated"
get_load_dotenv(default=True) L78
"Get whether the user has disabled loading dotenv files by setting"
_endpoint_from_view_func(view_func) L93
"Internal helper that returns the default endpoint for a given"
stream_with_context(generator_or_function) L101
"Request contexts disappear when the response is started on the server."
decorator(*args, **kwargs) L139
generator() L145
make_response(*args) L177
"Sometimes it is necessary to set additional headers in a view."
url_for(endpoint, **values) L226
"Generates a URL to the given endpoint with the method provided."
get_template_attribute(template_name, attribute) L377
"Loads a macro (or variable) a template exports."
flash(message, category='message') L399
"Flashes a message to the next request."
get_flashed_messages(with_categories=False, category_filter=()) L429
"Pulls all flashed messages from the session and returns them."
send_file(filename_or_fp, mimetype=None, as_attachment=False, attachment_filename=None, add_etags=True, cache_timeout=None, conditional=False, last_modified=None) L469
"Sends the contents of a file to the client."
safe_join(directory, *pathnames) L696
"Safely join `directory` and zero or more untrusted `pathnames`"
send_from_directory(directory, filename, **options) L733
"Send a file from a given directory with :func:`send_file`."
get_root_path(import_name) L774
"Returns the path to a package or cwd if that cannot be found."
_matching_loader_thinks_module_is_package(loader, mod_name) L822
"Given the loader that loaded a module and the module this function"
_find_package_path(root_mod_name) L850
"Find the path where the module's root exists in"
find_package(import_name) L908
"Finds a package and returns the prefix (or None if the package is"
total_seconds(td) L1115
"Returns the total seconds from a timedelta object."
is_ip(value) L1126
"Determine if the given string is an IP address."

src/flask/json/__init__.py
class JSONEncoder(_json.JSONEncoder) L61
  "The default Flask JSON encoder."
  default(self, o) L73
  "Implement this method in a subclass such that it returns a"

class JSONDecoder(_json.JSONDecoder) L103
  "The default JSON decoder."

_wrap_reader_for_text(fp, encoding) L47
_wrap_writer_for_text(fp, encoding) L53
_dump_arg_defaults(kwargs, app=None) L111
"Inject default arguments for dump functions."
_load_arg_defaults(kwargs, app=None) L131
"Inject default arguments for load functions."
detect_encoding(data) L145
"Detect which UTF codec was used to encode the given bytes."
dumps(obj, app=None, **kwargs) L188
"Serialize ``obj`` to a JSON-formatted string."
dump(obj, fp, app=None, **kwargs) L217
"Like :func:`dumps` but writes into a file object."
loads(s, app=None, **kwargs) L226
"Deserialize an object from a JSON-formatted string ``s``."
load(fp, app=None, **kwargs) L256
"Like :func:`loads` but reads from a file object."
htmlsafe_dumps(obj, **kwargs) L264
"Works exactly like :func:`dumps` but is safe for use in ``<script>``"
htmlsafe_dump(obj, fp, **kwargs) L301
"Like :func:`htmlsafe_dumps` but writes into a file object."
jsonify(*args, **kwargs) L306
"This function wraps :func:`dumps` to add a few enhancements that make"
tojson_filter(obj, **kwargs) L375

src/flask/json/tag.py
class JSONTag(object) L59
  "Base class for defining type tags for :class:`TaggedJSONSerializer`."
  __init__(self, serializer) L68
  "Create a tagger for the given serializer."
  check(self, value) L72
  "Check if the given value should be tagged by this tag."
  to_json(self, value) L76
  "Convert the Python object to an object that is a valid JSON type."
  to_python(self, value) L81
  "Convert the JSON representation back to the correct type."
  tag(self, value) L86
  "Convert the value to a valid JSON type and add the tag structure"

class TagDict(JSONTag) L92
  "Tag for 1-item dicts whose only key matches a registered tag."
  check(self, value) L102
  to_json(self, value) L109
  to_python(self, value) L113

class PassDict(JSONTag) L118
  check(self, value) L121
  to_json(self, value) L124

class TagTuple(JSONTag) L132
  check(self, value) L136
  to_json(self, value) L139
  to_python(self, value) L142

class PassList(JSONTag) L146
  check(self, value) L149
  to_json(self, value) L152

class TagBytes(JSONTag) L158
  check(self, value) L162
  to_json(self, value) L165
  to_python(self, value) L168

class TagMarkup(JSONTag) L172
  "Serialize anything matching the :class:`~flask.Markup` API by"
  check(self, value) L180
  to_json(self, value) L183
  to_python(self, value) L186

class TagUUID(JSONTag) L190
  check(self, value) L194
  to_json(self, value) L197
  to_python(self, value) L200

class TagDateTime(JSONTag) L204
  check(self, value) L208
  to_json(self, value) L211
  to_python(self, value) L214

class TaggedJSONSerializer(object) L218
  "Serializer that uses a tag system to compactly represent objects that"
  __init__(self) L248
  register(self, tag_class, force=False, index=None) L255
  "Register a new tag with this serializer."
  tag(self, value) L283
  "Convert a value to a tagged representation if necessary."
  untag(self, value) L291
  "Convert a tagged representation back to the original type."
  dumps(self, value) L303
  "Tag the value and dump it to a compact JSON string."
  loads(self, value) L307
  "Load data from a JSON string and deserialized any tagged objects."

src/flask/logging.py
wsgi_errors_stream() L20
"Find the most appropriate error stream for the application."
has_level_handler(logger) L32
"Check if there is a handler in the logging chain that will handle the"
create_logger(app) L59
"Get the ``'flask.app'`` logger and configure it if needed."

src/flask/sessions.py
class SessionMixin(collections_abc.MutableMapping) L25
  "Expands a basic dictionary with session attributes."
  permanent(self) L29
  "This reflects the ``'_permanent'`` key in the dict."
  permanent(self, value) L34

class SecureCookieSession(CallbackDict, SessionMixin) L53
  "Base class for sessions based on signed cookies."
  __init__(self, initial=None) L75
  on_update(self) L76
  __getitem__(self, key) L82
  get(self, key, default=None) L86
  setdefault(self, key, default=None) L90

class NullSession(SecureCookieSession) L95
  "Class used to generate nicer error messages if sessions are not"
  _fail(self, *args, **kwargs) L101

class SessionInterface(object) L112
  "The basic interface you have to implement in order to replace the"
  make_null_session(self, app) L155
  "Creates a null session which acts as a replacement object if the"
  is_null_session(self, obj) L167
  "Checks if a given object is a null session."
  get_cookie_domain(self, app) L176
  "Returns the domain that should be set for the session cookie."
  get_cookie_path(self, app) L234
  "Returns the path for which the cookie should be valid."
  get_cookie_httponly(self, app) L242
  "Returns True if the session cookie should be httponly."
  get_cookie_secure(self, app) L249
  "Returns True if the cookie should be secure."
  get_cookie_samesite(self, app) L255
  "Return ``'Strict'`` or ``'Lax'`` if the cookie should use the"
  get_expiration_time(self, app, session) L262
  "A helper method that returns an expiration date for the session"
  should_set_cookie(self, app, session) L271
  "Used by session backends to determine if a ``Set-Cookie`` header"
  open_session(self, app, request) L287
  "This method has to be implemented and must either return ``None``"
  save_session(self, app, session, response) L295
  "This is called for actual sessions returned by :meth:`open_session`"

class SecureCookieSessionInterface(SessionInterface) L307
  "The default session interface that stores sessions in signed cookies"
  get_signing_serializer(self, app) L326
  open_session(self, app, request) L339
  save_session(self, app, session, response) L353

src/flask/signals.py
class Namespace(object) L19
  signal(self, name, doc=None) L20

class _FakeSignal(object) L23
  "If blinker is unavailable, create a fake class with the same"
  __init__(self, name, doc=None) L30
  send(self, *args, **kwargs) L34
  _fail(self, *args, **kwargs) L37

src/flask/templating.py
class Environment(BaseEnvironment) L36
  "Works like a regular Jinja2 environment but has some additional"
  __init__(self, app, **options) L42

class DispatchingJinjaLoader(BaseLoader) L49
  "A loader that looks for templates in the application and all"
  __init__(self, app) L54
  get_source(self, environment, template) L57
  _get_source_explained(self, environment, template) L62
  _get_source_fast(self, environment, template) L83
  _iter_loaders(self, template) L91
  list_templates(self) L101

_default_template_ctx_processor() L21
"Default template context processor."
_render(template, context, app) L116
"Renders the template and fires the signal"
render_template(template_name_or_list, **context) L125
"Renders a template from the template folder with the given"
render_template_string(source, **context) L144
"Renders a template from the given template source string"

src/flask/testing.py
class EnvironBuilder(werkzeug.test.EnvironBuilder) L25
  "An :class:`~werkzeug.test.EnvironBuilder`, that takes defaults from the"
  __init__(self, app, path='/', base_url=None, subdomain=None, url_scheme=None, *args, **kwargs) L47
  json_dumps(self, obj, **kwargs) L88
  "Serialize ``obj`` to a JSON-formatted string."

class FlaskClient(Client) L115
  "Works like a regular Werkzeug test client but has some knowledge about"
  __init__(self, *args, **kwargs) L132
  session_transaction(self, *args, **kwargs) L140
  "When used in combination with a ``with`` statement this opens a"
  open(self, *args, **kwargs) L192
  __enter__(self) L230
  __exit__(self, exc_type, exc_value, tb) L236

class FlaskCliRunner(CliRunner) L252
  "A :class:`~click.testing.CliRunner` for testing a Flask app's"
  __init__(self, app, **kwargs) L258
  invoke(self, cli=None, args=None, **kwargs) L262
  "Invokes a CLI command in an isolated environment."

make_test_environ_builder(*args, **kwargs) L98
"Create a :class:`flask.testing.EnvironBuilder`."

src/flask/views.py
class View(object) L20
  "Alternative way to use view functions."
  dispatch_request(self) L69
  "Subclasses have to override this method to implement the"
  as_view(cls, name, *class_args, **class_kwargs) L77
  "Converts the class into an actual view function that can be used"
  view(*args, **kwargs) L87

class MethodViewType(type) L111
  "Metaclass for :class:`MethodView` that determines what methods the view"
  __init__(cls, name, bases, d) L116

class MethodView L138
  "A class-based view that dispatches request methods to the corresponding"
  dispatch_request(self, *args, **kwargs) L154

src/flask/wrappers.py
class JSONMixin(_JSONMixin) L20
  on_json_loading_failed(self, e) L23

class Request(RequestBase, JSONMixin) L30
  "The request object used by default in Flask."
  max_content_length(self) L66
  "Read-only view of the ``MAX_CONTENT_LENGTH`` config key."
  endpoint(self) L72
  "The endpoint that matched the request."
  blueprint(self) L82
  "The name of the current blueprint"
  _load_form_data(self) L87

class Response(ResponseBase, JSONMixin) L103
  "The response object that is used by default in Flask."
  _get_data_for_json(self, cache) L123
  max_cookie_size(self) L127
  "Read-only view of the :data:`MAX_COOKIE_SIZE` config key."
