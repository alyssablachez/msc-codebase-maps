modules/api/api.py
class Api L199
  __init__(self, app, queue_lock) L200
  add_api_route(self, path, endpoint, **kwargs) L254
  auth(self, credentials=Depends(HTTPBasic())) L259
  get_selectable_script(self, script_name, script_runner) L266
  get_scripts_list(self) L274
  get_script_info(self) L280
  get_script(self, script_name, script_runner) L288
  init_default_script_args(self, script_runner) L295
  init_script_args(self, request, default_script_args, selectable_scripts, selectable_idx, script_runner) L315
  text2imgapi(self, txt2imgreq) L338
  img2imgapi(self, img2imgreq) L388
  extras_single_image_api(self, req) L453
  extras_batch_images_api(self, req) L463
  pnginfoapi(self, req) L474
  progressapi(self, req=Depends()) L488
  interrogateapi(self, interrogatereq) L516
  interruptapi(self) L535
  unloadapi(self) L540
  reloadapi(self) L545
  skip(self) L550
  get_config(self) L553
  set_config(self, req) L564
  get_cmd_flags(self) L575
  get_samplers(self) L578
  get_upscalers(self) L581
  get_latent_upscale_modes(self) L593
  get_sd_models(self) L601
  get_sd_vaes(self) L605
  get_hypernetworks(self) L609
  get_face_restorers(self) L612
  get_realesrgan_models(self) L615
  get_prompt_styles(self) L618
  get_embeddings(self) L626
  convert_embedding(embedding) L629
  convert_embeddings(embeddings) L638
  refresh_checkpoints(self) L646
  refresh_vae(self) L650
  create_embedding(self, args) L654
  create_hypernetwork(self, args) L666
  train_embedding(self, args) L676
  train_hypernetwork(self, args) L697
  get_memory(self) L722
  get_extensions_list(self) L757
  launch(self, server_name, port, root_path) L776
  kill_webui(self) L780
  restart_webui(self) L783
  stop_webui(request) L788

script_name_to_index(name, scripts) L36
validate_sampler_name(name) L43
setUpscalers(req) L51
verify_url(url) L58
"Returns True if the url refers to a global resource."
decode_base64_to_image(encoding) L77
encode_pil_to_base64(image) L102
api_middleware(app) L135
log_and_time(req, call_next) L148
handle_exception(request, e) L167
exception_handling(request, call_next) L184
fastapi_exception_handler(request, e) L191
http_exception_handler(request, e) L195

modules/api/models.py
class ModelDef(BaseModel) L27
  "Assistance Class for Pydantic Dynamic Model Generation"

class PydanticModelGenerator L37
  "Takes in created classes and stubs them out in a way FastAPI/Pydantic is happy about:"
  __init__(self, model_name=None, class_instance=None, additional_fields=None) L44
  field_type_generator(k, v) L50
  merge_class_params(class_) L59
  generate_model(self) L87
  "Creates a pydantic BaseModel"

class TextToImageResponse(BaseModel) L130

class ImageToImageResponse(BaseModel) L135

class ExtrasBaseRequest(BaseModel) L140

class ExtraBaseResponse(BaseModel) L155

class ExtrasSingleImageRequest(ExtrasBaseRequest) L158

class ExtrasSingleImageResponse(ExtraBaseResponse) L161

class FileData(BaseModel) L164

class ExtrasBatchImagesRequest(ExtrasBaseRequest) L168

class ExtrasBatchImagesResponse(ExtraBaseResponse) L171

class PNGInfoRequest(BaseModel) L174

class PNGInfoResponse(BaseModel) L177

class ProgressRequest(BaseModel) L182

class ProgressResponse(BaseModel) L185

class InterrogateRequest(BaseModel) L192

class InterrogateResponse(BaseModel) L196

class TrainResponse(BaseModel) L199

class CreateResponse(BaseModel) L202

class SamplerItem(BaseModel) L229

class UpscalerItem(BaseModel) L234

class LatentUpscalerModeItem(BaseModel) L241

class SDModelItem(BaseModel) L244

class SDVaeItem(BaseModel) L252

class HypernetworkItem(BaseModel) L256

class FaceRestorerItem(BaseModel) L260

class RealesrganItem(BaseModel) L264

class PromptStyleItem(BaseModel) L269

class EmbeddingItem(BaseModel) L275

class EmbeddingsResponse(BaseModel) L282

class MemoryResponse(BaseModel) L286

class ScriptsList(BaseModel) L291

class ScriptArg(BaseModel) L296

class ScriptInfo(BaseModel) L305

class ExtensionItem(BaseModel) L311

modules/cache.py
dump_cache() L17
"Marks cache for writing to disk."
thread_func() L25
cache(subsection) L49
"Retrieves or initializes a cache for a specific subsection."
cached_data_for_file(subsection, title, filename, func) L82
"Retrieves or generates data for a specific file, using a caching mechanism."

modules/call_queue.py
wrap_queued_call(func) L10
f(*args, **kwargs) L11
wrap_gradio_gpu_call(func, extra_outputs=None) L20
f(*args, **kwargs) L22
wrap_gradio_call(func, extra_outputs=None, add_stats=False) L48
f(*args, extra_outputs_array=extra_outputs, **kwargs) L50

modules/codeformer_model.py
class FaceRestorerCodeFormer(modules.face_restoration.FaceRestoration) L37
  name(self) L38
  __init__(self, dirname) L41
  create_models(self) L46
  send_model_to(self, device) L71
  restore(self, np_image, w=None) L76

setup_model(dirname) L21

modules/config_states.py
list_config_states() L18
get_webui_config() L48
get_extension_config() L80
get_config() L103
restore_webui_config(config) L115
restore_extension_config(config) L146

modules/deepbooru.py
class DeepDanbooru L12
  __init__(self) L13
  load(self) L16
  start(self) L33
  stop(self) L37
  tag(self, pil_image) L42
  tag_multi(self, pil_image, force_disable_ranks=False) L49

modules/deepbooru_model.py
class DeepDanbooruModel(nn.Module) L10
  __init__(self) L11
  forward(self, *inputs) L197
  load_state_dict(self, state_dict, **kwargs) L674

modules/devices.py
class NansException(Exception) L123

has_xpu() L15
has_mps() L19
get_cuda_device_string() L26
get_optimal_device_name() L33
get_optimal_device() L46
get_device_for(task) L50
torch_gc() L57
enable_tf32() L71
cond_cast_unet(input) L98
cond_cast_float(input) L102
autocast(disable=False) L109
without_autocast(disable=False) L119
test_for_nans(x, where) L127
first_time_calculation() L154
"just do any calculation with pytorch layers - the first time this is done it allocaltes about 700MB of memory and"

modules/errors.py
format_traceback(tb) L9
format_exception(e, tb) L13
get_exceptions() L17
record_exception() L24
report(message, *, exc_info=False) L38
"Print an error message to stderr, with optional traceback."
print_error_explanation(message) L52
display(e, task, *, full_traceback=False) L64
display_once(e, task) L85
run(code, task) L96
check_versions() L103

modules/esrgan_model.py
class UpscalerESRGAN(Upscaler) L121
  __init__(self, dirname) L122
  do_upscale(self, img, selected_model) L143
  load_model(self, path) L153

mod2normal(state_dict) L13
resrgan2normal(state_dict, nb=23) L46
infer_params(state_dict) L89
upscale_without_tiling(model, img) L193
esrgan_upscale(model, img) L208

modules/esrgan_model_arch.py
class RRDBNet(nn.Module) L14
  __init__(self, in_nc, out_nc, nf, nb, nr=3, gc=32, upscale=4, norm_type=None, act_type='leakyrelu', mode='CNA', upsample_mode='upconv', convtype='Conv2D', finalact=None, gaussian_noise=False, plus=False) L15
  forward(self, x, outm=None) L53

class RRDB(nn.Module) L64
  "Residual in Residual Dense Block"
  __init__(self, nf, nr=3, kernel_size=3, gc=32, stride=1, bias=1, pad_type='zero', norm_type=None, act_type='leakyrelu', mode='CNA', convtype='Conv2D', spectral_norm=False, gaussian_noise=False, plus=False) L70
  forward(self, x) L91

class ResidualDenseBlock_5C(nn.Module) L101
  "Residual Dense Block"
  __init__(self, nf=64, kernel_size=3, gc=32, stride=1, bias=1, pad_type='zero', norm_type=None, act_type='leakyrelu', mode='CNA', convtype='Conv2D', spectral_norm=False, gaussian_noise=False, plus=False) L112
  forward(self, x) L140

class GaussianNoise(nn.Module) L160
  __init__(self, sigma=0.1, is_relative_detach=False) L161
  forward(self, x) L167

class SRVGGNetCompact(nn.Module) L183
  "A compact VGG-style network structure for super-resolution."
  __init__(self, num_in_ch=3, num_out_ch=3, num_feat=64, num_conv=16, upscale=4, act_type='prelu') L188
  forward(self, x) L226

class Upsample(nn.Module) L242
  "Upsamples a given multi-channel 1D (temporal), 2D (spatial) or 3D (volumetric) data."
  __init__(self, size=None, scale_factor=None, mode='nearest', align_corners=None) L248
  forward(self, x) L258
  extra_repr(self) L261

class Identity(nn.Module) L356
  __init__(self, *kwargs) L357
  forward(self, x, *kwargs) L360

class ShortcutBlock(nn.Module) L400
  "Elementwise sum the output of a submodule to its input"
  __init__(self, submodule) L402
  forward(self, x) L406
  __repr__(self) L410

conv1x1(in_planes, out_planes, stride=1) L175
pixel_unshuffle(x, scale) L270
"Pixel unshuffle."
pixelshuffle_block(in_nc, out_nc, upscale_factor=2, kernel_size=3, stride=1, bias=True, pad_type='zero', norm_type=None, act_type='relu', convtype='Conv2D') L287
"Pixel shuffle layer"
upconv_block(in_nc, out_nc, upscale_factor=2, kernel_size=3, stride=1, bias=True, pad_type='zero', norm_type=None, act_type='relu', mode='nearest', convtype='Conv2D') L303
"Upconv layer"
make_layer(basic_block, num_basic_block, **kwarg) L324
"Make layers by stacking the same blocks."
act(act_type, inplace=True, neg_slope=0.2, n_prelu=1, beta=1.0) L338
"activation helper"
norm(norm_type, nc) L364
"Return a normalization layer"
norm_layer(x) L372
pad(pad_type, padding) L378
"padding layer helper"
get_valid_padding(kernel_size, dilation) L394
sequential(*args) L414
"Flatten Sequential."
conv_block(in_nc, out_nc, kernel_size, stride=1, dilation=1, groups=1, bias=True, pad_type='zero', norm_type=None, act_type='relu', mode='CNA', convtype='Conv2D', spectral_norm=False) L430
"Conv layer with padding, normalization, activation"

modules/extensions.py
class ExtensionMetadata L25
  __init__(self, path, canonical_name) L31
  get_script_requirements(self, field, section, extra_section=None) L46
  "reads a list of requirements from the config; field is the name of the field in the ini file,"
  parse_list(self, text) L58
  "converts a line from config ("ext1 ext2, ext3  ") into a python list (["ext1", "ext2", "ext3"])"

class Extension L68
  __init__(self, name, path, enabled=True, is_builtin=False, metadata=None) L73
  to_dict(self) L89
  from_dict(self, d) L92
  read_info_from_repo(self) L96
  read_from_repo() L100
  do_read_info_from_repo(self) L116
  list_files(self, subdir, extension) L142
  check_updates(self) L155
  fetch_and_reset_hard(self, commit='origin') L177

active() L16
list_extensions() L186

modules/extra_networks.py
class ExtraNetworkParams L31
  __init__(self, items=None) L32
  __eq__(self, other) L44

class ExtraNetwork L48
  __init__(self, name) L49
  activate(self, p, params_list) L52
  "Called by processing on every run."
  deactivate(self, p) L82
  "Called at the end of processing for housekeeping."

initialize() L13
register_extra_network(extra_network) L18
register_extra_network_alias(extra_network, alias) L22
register_default_extra_networks() L26
lookup_extra_networks(extra_network_data) L90
"returns a dict mapping ExtraNetwork objects to lists of arguments for those extra networks."
activate(p, extra_network_data) L126
"call activate for extra networks in extra_network_data in specified order, then call"
deactivate(p, extra_network_data) L153
"call deactivate for extra networks in extra_network_data in specified order, then call"
parse_prompt(prompt) L178
found(m) L181
parse_prompts(prompts) L194
get_user_metadata(filename) L209

modules/extra_networks_hypernet.py
class ExtraNetworkHypernet(extra_networks.ExtraNetwork) L5
  __init__(self) L6
  activate(self, p, params_list) L9
  deactivate(self, p) L27

modules/extras.py
run_pnginfo(image) L16
create_config(ckpt_result, config_source, a, b, c) L39
config(x) L40
to_half(tensor, enable) L68
read_metadata(primary_model_name, secondary_model_name, tertiary_model_name) L75
run_modelmerger(id_task, primary_model_name, secondary_model_name, tertiary_model_name, interp_method, multiplier, save_as_half, custom_name, checkpoint_format, config_source, bake_in_vae, discard_weights, save_metadata, add_merge_recipe, copy_metadata_fields, metadata_json) L88
fail(message) L91
weighted_sum(theta0, theta1, alpha) L96
get_difference(theta1, theta2) L99
add_difference(theta0, theta1_2_diff, alpha) L102
filename_weighted_sum() L105
filename_add_difference() L113
filename_nothing() L121
add_model_metadata(checkpoint_info) L294

modules/face_restoration.py
class FaceRestoration L4
  name(self) L5
  restore(self, np_image) L8

restore_faces(np_image) L12

modules/fifo_lock.py
class FIFOLock(object) L6
  __init__(self) L7
  acquire(self, blocking=True) L12
  release(self) L26
  __exit__(self, t, v, tb) L36

modules/generation_parameters_copypaste.py
class ParamBinding L20
  __init__(self, paste_button, tabname, source_text_component=None, source_image_component=None, source_tabname=None, override_settings_component=None, paste_field_names=None) L21

reset() L35
quote(text) L40
unquote(text) L47
image_from_url_text(filedata) L57
add_paste_fields(tabname, init_img, fields, override_settings_component=None) L86
create_buttons(tabs_list) L97
bind_buttons(buttons, send_image, send_generate_info) L104
"old function for backwards compatibility; do not use this, use register_paste_params_button"
register_paste_params_button(binding) L113
connect_paste_params_buttons() L117
send_image_and_dimensions(x) L163
restore_old_hires_fix_params(res) L179
"for infotexts that specify old First pass size parameter, convert it into"
parse_generation_parameters(x) L211
"parses generation parameters string, the one you see in text field under the picture in UI:"
create_override_settings_dict(text_pairs) L338
"creates processing's override_settings parameters from gradio's multiselect"
connect_paste(button, paste_fields, input_comp, override_settings_component, tabname) L368
paste_func(prompt) L369
paste_settings(params) L408

modules/gfpgan_model.py
class FaceRestorerGFPGAN(modules.face_restoration.FaceRestoration) L116
  name(self) L117
  restore(self, np_image) L120

gfpgann() L18
send_model_to(model, device) L53
gfpgan_fix_faces(np_image) L59
setup_model(dirname) L81
my_load_file_from_url(**kwargs) L100
facex_load_file_from_url(**kwargs) L103
facex_load_file_from_url2(**kwargs) L106

modules/gitpython_hack.py
class Git(git.Git) L9
  "Git subclassed to never use persistent processes."
  _get_persistent_cmd(self, attr_name, cmd_name, *args, **kwargs) L14
  get_object_header(self, ref) L17
  stream_object_data(self, ref) L26

class Repo(git.Repo) L41

modules/gradio_extensons.py
add_classes_to_gradio_component(comp) L6
"this adds gradio-* to the component for css styling (ie gradio-button to gr.Button), as well as some others"
IOComponent_init(self, *args, **kwargs) L17
Block_get_config(self) L37
BlockContext_init(self, *args, **kwargs) L49
Blocks_get_config_file(self, *args, **kwargs) L67

modules/hashes.py
calculate_sha256(filename) L11
sha256_from_cache(filename, title, use_addnet_hash=False) L22
sha256(filename, title, use_addnet_hash=False) L38
addnet_hash_safetensors(b) L66
"kohya-ss hash for safetensors from https://github.com/kohya-ss/sd-scripts/blob/main/library/train_util.py"

modules/hypernetworks/hypernetwork.py
class HypernetworkModule(torch.nn.Module) L25
  __init__(self, dim, state_dict=None, layer_structure=None, activation_func=None, weight_init='Normal', add_layer_norm=False, activate_output=False, dropout_structure=None) L37
  fix_old_state_dict(self, state_dict) L100
  forward(self, x) L116
  trainables(self) L119

class Hypernetwork L143
  __init__(self, name=None, enable_sizes=None, layer_structure=None, activation_func=None, weight_init=None, add_layer_norm=False, use_dropout=False, activate_output=False, **kwargs) L147
  weights(self) L177
  train(self, mode=True) L184
  to(self, device) L191
  set_multiplier(self, multiplier) L198
  eval(self) L205
  save(self, filename) L212
  load(self, filename) L242
  shorthash(self) L305

parse_dropout_structure(layer_structure, use_dropout, last_layer_dropout) L128
list_hypernetworks(path) L311
load_hypernetwork(name) L321
load_hypernetworks(names, multipliers=None) L336
apply_single_hypernetwork(hypernetwork, context_k, context_v, layer=None) L357
apply_hypernetworks(hypernetworks, context, layer=None) L372
attention_CrossAttention_forward(self, x, context=None, mask=None, **kwargs) L381
stack_conds(conds) L409
statistics(data) L424
create_hypernetwork(name, enable_sizes, overwrite_old, layer_structure=None, activation_func=None, weight_init=None, add_layer_norm=False, use_dropout=False, dropout_structure=None) L439
train_hypernetwork(id_task, hypernetwork_name, learn_rate, batch_size, gradient_step, data_root, log_directory, training_width, training_height, varsize, steps, clip_grad_mode, clip_grad_value, shuffle_tags, tag_drop_out, latent_sampling_method, use_weight, create_image_every, save_hypernetwork_every, template_filename, preview_from_txt2img, preview_prompt, preview_negative_prompt, preview_steps, preview_sampler_name, preview_cfg_scale, preview_seed, preview_width, preview_height) L471
save_hypernetwork(hypernetwork, checkpoint, hypernetwork_name, filename) L769

modules/hypernetworks/ui.py
create_hypernetwork(name, enable_sizes, overwrite_old, layer_structure=None, activation_func=None, weight_init=None, add_layer_norm=False, use_dropout=False, dropout_structure=None) L11
train_hypernetwork(*args) L17

modules/images.py
class GridAnnotation L134
  __init__(self, text='', is_active=True) L135

class FilenameGenerator L342
  __init__(self, p, seed, prompt, image, zip=False) L378
  get_vae_filename(self) L385
  "Get the name of the VAE file."
  hasprompt(self, *args) L401
  prompt_no_style(self) L417
  prompt_words(self) L431
  datetime(self, *args) L437
  image_hash(self, *args) L454
  string_hash(self, text, *args) L458
  apply(self, x) L462

get_font(fontsize) L27
image_grid(imgs, batch_size=1, rows=None) L34
split_grid(image, tile_w=512, tile_h=512, overlap=64) L67
combine_grid(grid) L104
make_mask_image(r) L105
draw_grid_annotations(im, width, height, hor_texts, ver_texts, margin=0) L141
wrap(drawing, text, font, line_length) L147
draw_texts(drawing, draw_x, draw_y, lines, initial_fnt, initial_fontsize) L157
draw_prompt_matrix(im, width, height, all_prompts, margin=0) L229
resize_image(resize_mode, im, width, height, upscaler_name=None) L242
"Resizes an image with the specified resize_mode, width, and height."
resize(im, w, h) L259
sanitize_filename_part(text, replace_spaces=True) L329
get_next_sequence_number(path, basename) L500
"Determines and returns the next sequence number to use when saving an image in the specified directory."
save_image_with_geninfo(image, geninfo, filename, extension=None, existing_pnginfo=None, pnginfo_section_name='parameters') L522
"Saves image to filename, including geninfo as text information for generation info."
save_image(image, path, basename, seed=None, prompt=None, extension='png', info=None, short_filename=False, no_prompt=False, grid=False, pnginfo_section_name='parameters', p=None, existing_info=None, forced_filename=None, suffix='', save_to_dirs=None) L570
"Save an image."
_atomically_save_image(image_to_save, filename_without_extension, extension) L658
"save image with .tmp extension to avoid race condition when another process detects new image in the directory"
read_info_from_image(image) L723
image_data(data) L764
flatten(img, bgcolor) L785
"replaces transparency with bgcolor (example: "#ffffff"), returning an RGB mode image with no transparency"

modules/img2img.py
process_batch(p, input_dir, output_dir, inpaint_mask_dir, args, to_scale=False, scale_by=1.0, use_png_info=False, png_info_props=None, png_info_dir=None) L20
img2img(id_task, mode, prompt, negative_prompt, prompt_styles, init_img, sketch, init_img_with_mask, inpaint_color_sketch, inpaint_color_sketch_orig, init_img_inpaint, init_mask_inpaint, steps, sampler_name, mask_blur, mask_alpha, inpainting_fill, n_iter, batch_size, cfg_scale, image_cfg_scale, denoising_strength, selected_scale_tab, height, width, scale_by, resize_mode, inpaint_full_res, inpaint_full_res_padding, inpainting_mask_invert, img2img_batch_input_dir, img2img_batch_output_dir, img2img_batch_inpaint_mask_dir, override_settings_texts, img2img_batch_use_png_info, img2img_batch_png_info_props, img2img_batch_png_info_dir, request, *args) L149

modules/initialize.py
imports() L10
check_versions() L41
initialize() L49
initialize_rest(*, reload_script_modules=False) L78
"Called both from initialize() and when reloading the webui."
load_model() L139
"Accesses shared.sd_model property to load model."

modules/initialize_util.py
class AnyThreadEventLoopPolicy(_BasePolicy) L47
  "Event loop policy that allows loop creation on any thread."
  get_event_loop(self) L54

gradio_server_name() L10
fix_torch_version() L19
fix_asyncio_event_loop_policy() L28
"The default `asyncio` event loop policy only automatically creates"
restore_config_state_file() L68
validate_tls_options() L88
get_gradio_auth_creds() L107
"Convert the gradio_auth and gradio_auth_path commandline arguments into"
process_credential_line(s) L114
dumpstacks() L135
configure_sigint_handler() L151
sigint_handler(sig, frame) L156
configure_opts_onchange() L170
setup_middleware(app) L183
configure_cors_middleware(app) L192

modules/interrogate.py
class InterrogateModels L45
  __init__(self, content_dir) L52
  categories(self) L58
  create_fake_fairscale(self) L83
  load_blip_model(self) L90
  load_clip_model(self) L106
  load(self) L119
  send_clip_to_ram(self) L136
  send_blip_to_ram(self) L141
  unload(self) L146
  rank(self, image_features, text_array, top_count=1) L152
  generate_caption(self, pil_image) L173
  interrogate(self, pil_image) L185

class FakeFairscale L84
  checkpoint_wrapper(self) L85

category_types() L22
download_default_clip_interrogate_categories(content_dir) L26

modules/launch_utils.py
check_python_version() L34
commit_hash() L66
git_tag() L74
run(command, desc=None, errdesc=None, custom_env=None, live=default_command_live) L89
is_installed(package) L121
repo_dir(name) L135
run_pip(command, desc=None, live=default_command_live) L139
check_run_python(code) L147
git_fix_workspace(dir, name) L152
run_git(dir, name, command, desc=None, errdesc=None, custom_env=None, live=default_command_live, autofix=True) L158
git_clone(url, dir, name, commithash=None) L171
git_pull_recursive(dir) L201
version_check(commit) L211
run_extension_installer(extension_dir) L228
list_extensions(settings_file) L244
run_extensions_installers(settings_file) L263
requirements_met(requirements_file) L281
"Does a simple parse of a requirements.txt file to determine if all rerqirements in it"
prepare_environment() L316
configure_for_tests() L444
start() L458
dump_sysinfo() L467

modules/localization.py
list_localizations(dirname) L9
localization_js(current_localization_name) L26

modules/logging_config.py
class TqdmLoggingHandler(logging.Handler) L7
  __init__(self, level=logging.INFO) L8
  emit(self, record) L11

setup_logging(loglevel) L25

modules/lowvram.py
send_everything_to_cpu() L8
is_needed(sd_model) L17
apply(sd_model) L21
setup_for_low_vram(sd_model, use_medvram) L31
send_me_to_gpu(module, _) L39
"send this module to GPU; send whatever tracked module was previous in GPU to CPU;"
first_stage_model_encode_wrap(x) L65
first_stage_model_decode_wrap(z) L69
is_enabled(sd_model) L146

modules/mac_specific.py
check_for_mps() L17
torch_mps_gc() L33
cumsum_fix(input, cumsum_func, *args, **kwargs) L45
interpolate_with_fp32_fallback(orig_func, *args, **kwargs) L56

modules/masking.py
get_crop_region(mask, pad=0) L4
"finds a rectangular region that contains all masked ares in an image."
expand_crop_region(crop_region, processing_width, processing_height, image_width, image_height) L42
"expands crop region get_crop_region() to match the ratio of the image the region will processed in; returns expanded region"
fill(image, mask) L83
"fills masked regions with colors from image using blur."

modules/memmon.py
class MemUsageMonitor(threading.Thread) L8
  __init__(self, name, device, opts) L15
  cuda_mem_get_info(self) L32
  run(self) L36
  dump_debug(self) L58
  monitor(self) L72
  read(self) L75
  stop(self) L90

modules/modelloader.py
load_file_from_url(url, *, model_dir, progress=True, file_name=None) L13
"Download a file from `url` into `model_dir`, using the file present if possible."
load_models(model_path, model_url=None, command_path=None, ext_filter=None, download_name=None, ext_blacklist=None) L36
"A one-and done loader to try finding the desired models in specified directories."
friendly_name(file) L84
cleanup_models() L93
move_files(src_path, dest_path, ext_filter=None) L119
load_upscalers() L141

modules/models/diffusion/ddpm_edit.py
class VQModelInterface L34

class DDPM(pl.LightningModule) L52
  __init__(self, unet_config, timesteps=1000, beta_schedule='linear', loss_type='l2', ckpt_path=None, ignore_keys=None, load_only_unet=False, monitor='val/loss', use_ema=True, first_stage_key='image', image_size=256, channels=3, log_every_t=100, clip_denoised=True, linear_start=0.0001, linear_end=0.02, cosine_s=0.008, given_betas=None, original_elbo_weight=0.0, v_posterior=0.0, l_simple_weight=1.0, conditioning_key=None, parameterization='eps', scheduler_config=None, use_positional_encodings=False, learn_logvar=False, logvar_init=0.0, load_ema=True) L54
  register_schedule(self, given_betas=None, beta_schedule='linear', timesteps=1000, linear_start=0.0001, linear_end=0.02, cosine_s=0.008) L133
  ema_scope(self, context=None) L188
  init_from_ckpt(self, path, ignore_keys=None, only_model=False) L202
  q_mean_variance(self, x_start, t) L243
  "Get the distribution q(x_t | x_0)."
  predict_start_from_noise(self, x_t, t, noise) L255
  q_posterior(self, x_start, x_t, t) L261
  p_mean_variance(self, x, t, clip_denoised) L270
  p_sample(self, x, t, clip_denoised=True, repeat_noise=False) L283
  p_sample_loop(self, shape, return_intermediates=False) L292
  sample(self, batch_size=16, return_intermediates=False) L307
  q_sample(self, x_start, t, noise=None) L313
  get_loss(self, pred, target, mean=True) L318
  p_losses(self, x_start, t, noise=None) L333
  forward(self, x, *args, **kwargs) L362
  get_input(self, batch, k) L368
  shared_step(self, batch) L371
  training_step(self, batch, batch_idx) L376
  validation_step(self, batch, batch_idx) L392
  on_train_batch_end(self, *args, **kwargs) L400
  _get_rows_from_list(self, samples) L404
  log_images(self, batch, N=8, n_row=2, sample=True, return_keys=None, **kwargs) L412
  configure_optimizers(self) L449

class LatentDiffusion(DDPM) L458
  "main class"
  __init__(self, first_stage_config, cond_stage_config, num_timesteps_cond=None, cond_stage_key='image', cond_stage_trainable=False, concat_mode=True, cond_stage_forward=None, conditioning_key=None, scale_factor=1.0, scale_by_std=False, load_ema=True, *args, **kwargs) L460
  make_cond_schedule(self) L510
  on_train_batch_start(self, batch, batch_idx, dataloader_idx) L517
  register_schedule(self, given_betas=None, beta_schedule='linear', timesteps=1000, linear_start=0.0001, linear_end=0.02, cosine_s=0.008) L532
  instantiate_first_stage(self, config) L541
  instantiate_cond_stage(self, config) L548
  _get_denoise_row_from_list(self, samples, desc='', force_no_decoder_quantization=False) L569
  get_first_stage_encoding(self, encoder_posterior) L581
  get_learned_conditioning(self, c) L590
  meshgrid(self, h, w) L603
  delta_border(self, h, w) L610
  ":param h: height"
  get_weighting(self, h, w, Ly, Lx, device) L624
  get_fold_unfold(self, x, kernel_size, stride, uf=1, df=1) L640
  ":param x: img of size (bs, c, h, w)"
  get_input(self, batch, k, return_first_stage_outputs=False, force_c_encode=False, cond_key=None, return_original_cond=False, bs=None, uncond=0.05) L693
  decode_first_stage(self, z, predict_cids=False, force_not_quantize=False) L726
  differentiable_decode_first_stage(self, z, predict_cids=False, force_not_quantize=False) L786
  encode_first_stage(self, x) L846
  shared_step(self, batch, **kwargs) L885
  forward(self, x, c, *args, **kwargs) L890
  apply_model(self, x_noisy, t, cond, return_ids=False) L901
  _predict_eps_from_xstart(self, x_t, t, pred_xstart) L1004
  _prior_bpd(self, x_start) L1008
  "Get the prior KL term for the variational lower-bound, measured in"
  p_losses(self, x_start, cond, t, noise=None) L1022
  p_mean_variance(self, x, c, t, clip_denoised, return_codebook_ids=False, quantize_denoised=False, return_x0=False, score_corrector=None, corrector_kwargs=None) L1057
  p_sample(self, x, c, t, clip_denoised=False, repeat_noise=False, return_codebook_ids=False, quantize_denoised=False, return_x0=False, temperature=1.0, noise_dropout=0.0, score_corrector=None, corrector_kwargs=None) L1089
  progressive_denoising(self, cond, shape, verbose=True, callback=None, quantize_denoised=False, img_callback=None, mask=None, x0=None, temperature=1.0, noise_dropout=0.0, score_corrector=None, corrector_kwargs=None, batch_size=None, x_T=None, start_T=None, log_every_t=None) L1120
  p_sample_loop(self, cond, shape, return_intermediates=False, x_T=None, verbose=True, callback=None, timesteps=None, quantize_denoised=False, mask=None, x0=None, img_callback=None, start_T=None, log_every_t=None) L1178
  sample(self, cond, batch_size=16, return_intermediates=False, x_T=None, verbose=True, timesteps=None, quantize_denoised=False, mask=None, x0=None, shape=None, **kwargs) L1231
  sample_log(self, cond, batch_size, ddim, ddim_steps, **kwargs) L1249
  log_images(self, batch, N=4, n_row=4, sample=True, ddim_steps=200, ddim_eta=1.0, return_keys=None, quantize_denoised=True, inpaint=False, plot_denoise_rows=False, plot_progressive_rows=False, plot_diffusion_rows=False, **kwargs) L1265
  configure_optimizers(self) L1376
  to_rgb(self, x) L1401

class DiffusionWrapper(pl.LightningModule) L1410
  __init__(self, diff_model_config, conditioning_key) L1411
  forward(self, x, t, c_concat=None, c_crossattn=None) L1417

class Layout2ImgDiffusion(LatentDiffusion) L1439
  __init__(self, cond_stage_key, *args, **kwargs) L1441
  log_images(self, batch, N=8, *args, **kwargs) L1445

disabled_train(self, mode=True) L42
"Overwrite model.train with this function to make sure train/eval mode"
uniform_on_device(r1, r2, shape, device) L48

modules/models/diffusion/uni_pc/sampler.py
class UniPCSampler(object) L9
  __init__(self, model, **kwargs) L10
  register_buffer(self, name, attr) L18
  set_hooks(self, before_sample, after_sample, after_update) L24
  sample(self, S, batch_size, shape, conditioning=None, callback=None, normals_sequence=None, img_callback=None, quantize_x0=False, eta=0.0, mask=None, x0=None, temperature=1.0, noise_dropout=0.0, score_corrector=None, corrector_kwargs=None, verbose=True, x_T=None, log_every_t=100, unconditional_guidance_scale=1.0, unconditional_conditioning=None, **kwargs) L30

modules/models/diffusion/uni_pc/uni_pc.py
class NoiseScheduleVP L6
  __init__(self, schedule='discrete', betas=None, alphas_cumprod=None, continuous_beta_0=0.1, continuous_beta_1=20.0) L7
  "Create a wrapper class for the forward SDE (VP type)."
  marginal_log_mean_coeff(self, t) L125
  "Compute log(alpha_t) of a given continuous-time label t in [0, T]."
  marginal_alpha(self, t) L138
  "Compute alpha_t of a given continuous-time label t in [0, T]."
  marginal_std(self, t) L144
  "Compute sigma_t of a given continuous-time label t in [0, T]."
  marginal_lambda(self, t) L150
  "Compute lambda_t = log(alpha_t) - log(sigma_t) of a given continuous-time label t in [0, T]."
  inverse_lambda(self, lamb) L158
  "Compute the continuous-time label t in [0, T] of a given half-logSNR lambda_t."

class UniPC L372
  __init__(self, model_fn, noise_schedule, predict_x0=True, thresholding=False, max_val=1.0, variant='bh1', condition=None, unconditional_condition=None, before_sample=None, after_sample=None, after_update=None) L373
  "Construct a UniPC."
  dynamic_thresholding_fn(self, x0, t=None) L403
  "The dynamic thresholding method."
  model(self, x, t) L414
  noise_prediction_fn(self, x, t) L429
  "Return the noise prediction model."
  data_prediction_fn(self, x, t) L435
  "Return the data prediction model (with thresholding)."
  model_fn(self, x, t) L450
  "Convert the model to the noise prediction model or the data prediction model."
  get_time_steps(self, skip_type, t_T, t_0, N, device) L459
  "Compute the intermediate time steps for sampling."
  get_orders_and_timesteps_for_singlestep_solver(self, steps, order, skip_type, t_T, t_0, device) L476
  "Get the order of each step for sampling by the singlestep DPM-Solver."
  denoise_to_zero_fn(self, x, s) L507
  "Denoise at the final step, which is equivalent to solve the ODE from lambda_s to infty by first-order discretization."
  multistep_uni_pc_update(self, x, model_prev_list, t_prev_list, t, order, **kwargs) L513
  multistep_uni_pc_vary_update(self, x, model_prev_list, t_prev_list, t, order, use_corrector=True) L522
  multistep_uni_pc_bh_update(self, x, model_prev_list, t_prev_list, t, order, x_t=None, use_corrector=True) L625
  sample(self, x, steps=20, t_start=None, t_end=None, order=3, skip_type='time_uniform', method='singlestep', lower_order_final=True, denoise_to_zero=False, solver_type='dpm_solver', atol=0.0078, rtol=0.05, corrector=False) L746

model_wrapper(model, noise_schedule, model_type='noise', model_kwargs=None, guidance_type='uncond', guidance_scale=1.0, classifier_fn=None, classifier_kwargs=None) L177
"Create a wrapper function for the noise prediction model."
get_model_input_time(t_continuous) L281
"Convert the continuous-time `t_continuous` (in [epsilon, T]) to the model input time."
noise_pred_fn(x, t_continuous, cond=None) L292
cond_grad_fn(x, t_input, condition) L315
"Compute the gradient of the classifier, i.e. nabla_{x} log p_t(cond | x_t)."
model_fn(x, t_continuous, condition, unconditional_condition) L324
"The noise predicition model function that is used for DPM-Solver."
interpolate_fn(x, xp, yp) L811
"A piecewise linear function y = f(x), using xp and yp as keypoints."
expand_dims(v, dims) L853
"Expand the tensor `v` to the dim `dims`."

modules/ngrok.py
connect(token, port, options) L4

modules/options.py
class OptionInfo L11
  __init__(self, default=None, label='', component=None, component_args=None, onchange=None, section=None, refresh=None, comment_before='', comment_after='', infotext=None, restrict_api=False, category_id=None) L12
  link(self, label, url) L34
  js(self, label, js_func) L38
  info(self, info) L42
  html(self, html) L46
  needs_restart(self) L50
  needs_reload_ui(self) L54

class OptionHTML(OptionInfo) L59
  __init__(self, text) L60

class Options L80
  __init__(self, data_labels, restricted_opts) L83
  __setattr__(self, key, value) L88
  __getattr__(self, item) L112
  set(self, key, value, is_api=False, run_callbacks=True) L125
  "sets an option and calls its onchange callback, returning True if the option changed and False otherwise"
  get_default(self, key) L154
  "returns the default value for the key"
  save(self, filename) L163
  same_type(self, x, y) L169
  load(self, filename) L178
  onchange(self, key, func, call=True) L204
  dumpjson(self) L211
  add_option(self, key, info) L228
  reorder(self) L233
  "Reorder settings so that:"
  sort_key(x) L260
  cast_value(self, key, value) L269
  "casts an arbitrary to the same type as this setting's value with key"

class OptionsCategory L293

class OptionsCategories L297
  __init__(self) L298
  register_category(self, category_id, label) L301

options_section(section_identifier, options_dict) L66

modules/patches.py
patch(key, obj, field, replacement) L4
"Replaces a function in a module or a class."
undo(key, obj, field) L32
"Undoes the peplacement by the patch()."
original(key, obj, field) L57
"Returns the original function for the patch created by the patch() function"

modules/paths.py
class Dummy L11

mute_sdxl_imports() L8
"create fake modules that SDXL wants to import but doesn't actually use for our purposes"

modules/postprocessing.py
run_postprocessing(extras_mode, image, image_folder, input_dir, output_dir, show_extras_results, *args, save_output=True) L9
get_images(extras_mode, image, image_folder, input_dir) L16
run_postprocessing_webui(id_task, *args, **kwargs) L131
run_extras(extras_mode, resize_mode, image, image_folder, input_dir, output_dir, show_extras_results, gfpgan_visibility, codeformer_visibility, codeformer_weight, upscaling_resize, upscaling_resize_w, upscaling_resize_h, upscaling_crop, extras_upscaler_1, extras_upscaler_2, extras_upscaler_2_visibility, upscale_first, save_output=True) L135
"old handler for API"

modules/processing.py
class StableDiffusionProcessing L116
  __post_init__(self) L205
  sd_model(self) L237
  sd_model(self, value) L241
  scripts(self) L245
  scripts(self, value) L249
  script_args(self) L256
  script_args(self, value) L260
  setup_scripts(self) L266
  comment(self, text) L271
  txt2img_image_conditioning(self, x, width=None, height=None) L274
  depth2img_image_conditioning(self, source_image) L279
  edit_image_conditioning(self, source_image) L298
  unclip_image_conditioning(self, source_image) L303
  inpainting_image_conditioning(self, source_image, latent_image, image_mask=None) L311
  img2img_image_conditioning(self, source_image, latent_image, image_mask=None) L348
  init(self, all_prompts, all_seeds, all_subseeds) L368
  sample(self, conditioning, unconditional_conditioning, seeds, subseeds, subseed_strength, prompts) L371
  close(self) L374
  get_token_merging_ratio(self, for_hr=False) L382
  setup_prompts(self) L388
  cached_params(self, required_prompts, steps, extra_network_data, hires_steps=None, use_old_scheduling=False) L410
  "Returns parameters that invalidate the cond cache if changed"
  get_conds_with_caching(self, function, required_prompts, steps, caches, extra_network_data, hires_steps=None) L427
  "Returns the result of calling function(shared.sd_model, required_prompts, steps)"
  setup_conds(self) L460
  get_conds(self) L472
  parse_extra_network_prompts(self) L475
  save_samples(self) L478
  "Returns whether generated images need to be written to disk"

class Processed L483
  __init__(self, p, images_list, seed=-1, info='', subseed=None, all_prompts=None, all_negative_prompts=None, all_seeds=None, all_subseeds=None, index_of_first_image=0, infotexts=None, comments='') L484
  js(self) L538
  infotext(self, p, index) L576
  get_token_merging_ratio(self, for_hr=False) L579

class DecodedSamples(list) L588

class StableDiffusionProcessingTxt2Img(StableDiffusionProcessing) L1014
  __post_init__(self) L1047
  calculate_target_resolution(self) L1059
  init(self, all_prompts, all_seeds, all_subseeds) L1098
  sample(self, conditioning, unconditional_conditioning, seeds, subseeds, subseed_strength, prompts) L1138
  sample_hr_pass(self, samples, decoded_samples, seeds, subseeds, subseed_strength, prompts) L1159
  save_intermediate(image, index) L1167
  "saves image before applying hires fix, if enabled in options; takes as an argument either an image or batch with latent space images"
  close(self) L1254
  setup_prompts(self) L1262
  calculate_hr_conds(self) L1287
  setup_conds(self) L1301
  get_conds(self) L1326
  parse_extra_network_prompts(self) L1332

class StableDiffusionProcessingImg2Img(StableDiffusionProcessing) L1345
  __post_init__(self) L1369
  mask_blur(self) L1377
  mask_blur(self, value) L1383
  init(self, all_prompts, all_seeds, all_subseeds) L1388
  sample(self, conditioning, unconditional_conditioning, seeds, subseeds, subseed_strength, prompts) L1520
  get_token_merging_ratio(self, for_hr=False) L1537

setup_color_correction(image) L43
apply_color_correction(correction, original_image) L49
apply_overlay(image, paste_loc, index, overlays) L65
create_binary_mask(image) L84
txt2img_image_conditioning(sd_model, x, width, height) L91
create_random_tensors(shape, seeds, subseeds=None, subseed_strength=0.0, seed_resize_from_h=0, seed_resize_from_w=0, p=None) L583
decode_latent_batch(model, batch, target_device=None, check_for_nans=False) L592
get_fixed_seed(seed) L626
fix_seed(p) L641
program_version() L646
create_infotext(p, all_prompts, all_seeds, all_subseeds, comments=None, iteration=0, position_in_batch=0, use_main_prompt=False, index=None, all_negative_prompts=None) L656
process_images(p) L710
process_images_inner(p) L750
"this is the main loop that both txt2img and img2img use; it calls func_init once inside all the scopes and func_sample once per batch"
infotext(index=0, use_main_prompt=False) L899
old_hires_fix_first_pass_dimensions(width, height) L1001
"old algorithm for auto-calculating first pass size"

modules/processing_scripts/refiner.py
class ScriptRefiner(scripts.ScriptBuiltinUI) L8
  __init__(self) L12
  title(self) L15
  show(self, is_img2img) L18
  ui(self, is_img2img) L21
  lookup_checkpoint(title) L29
  setup(self, p, enable_refiner, refiner_checkpoint, refiner_switch_at) L41

modules/processing_scripts/seed.py
class ScriptSeed(scripts.ScriptBuiltinUI) L10
  __init__(self) L14
  title(self) L19
  show(self, is_img2img) L22
  ui(self, is_img2img) L25
  setup(self, p, seed, seed_checkbox, subseed, subseed_strength, seed_resize_from_w, seed_resize_from_h) L67

connect_reuse_seed(seed, reuse_seed, generation_info, is_subseed) L80
"Connects a 'reuse (sub)seed' button's click event so that it copies last used"
copy_seed(gen_info_string, index) L85

modules/progress.py
class ProgressRequest(BaseModel) L48

class ProgressResponse(BaseModel) L54

start_task(id_task) L20
finish_task(id_task) L27
record_results(id_task, res) L38
add_task_to_queue(id_job) L44
setup_progress_api(app) L65
progressapi(req) L69
restore_progress(id_task) L126

modules/prompt_parser.py
class CollectSteps(lark.Visitor) L78
  scheduled(self, tree) L79
  alternate(self, tree) L93

class AtStep(lark.Transformer) L100
  scheduled(self, args) L101
  alternate(self, args) L104
  start(self, args) L107
  flatten(x) L108
  plain(self, args) L115
  __default__(self, data, children, meta) L117

class SdConditioning(list) L139
  "A list with prompts for stable diffusion's conditioner model."
  __init__(self, prompts, is_negative_prompt=False, width=None, height=None, copy_from=None) L144

class ComposableScheduledPromptConditioning L240
  __init__(self, schedules, weight=1.0) L241

class MulticondLearnedConditioning L246
  __init__(self, shape, batch) L247

class DictWithShape(dict) L270
  __init__(self, x, shape) L271
  shape(self) L276

get_learned_conditioning_prompt_schedules(prompts, base_steps, hires_steps=None, use_old_scheduling=False) L28
">>> g = lambda p: get_learned_conditioning_prompt_schedules([p], 10)[0]"
collect_steps(steps, tree) L75
at_step(step, tree) L99
get_schedule(prompt) L122
get_learned_conditioning(model, prompts, steps, hires_steps=None, use_old_scheduling=False) L157
"converts a list of prompts into a list of prompt schedules - each schedule is a list of ScheduledPromptConditioning, specifying the comdition (cond),"
get_multicond_prompt_list(prompts) L209
get_multicond_learned_conditioning(model, prompts, steps, hires_steps=None, use_old_scheduling=False) L252
"same as get_learned_conditioning, but returns a list of ScheduledPromptConditioning along with the weight objects for each prompt."
reconstruct_cond_batch(c, current_step) L280
stack_conds(tensors) L307
reconstruct_multicond_batch(c, current_step) L321
parse_prompt_attention(text) L370
"Parses a string with attention tokens and returns a list of pairs: text and its associated weight."
multiply_range(start_position, multiplier) L413

modules/realesrgan_model.py
class UpscalerRealESRGAN(Upscaler) L12
  __init__(self, path) L13
  do_upscale(self, img, path) L41
  load_model(self, path) L66
  load_models(self, _) L79

get_realesrgan_models(scaler) L83

modules/restart.py
is_restartable() L7
"Return True if the webui is restartable (i.e. there is something watching to restart it with)"
restart_program() L14
"creates file tmp/restart and immediately stops the process, which webui.bat/webui.sh interpret as a command to start webui again"
stop_program() L24

modules/rng.py
class ImageRNG L99
  __init__(self, shape, seeds, subseeds=None, subseed_strength=0.0, seed_resize_from_h=0, seed_resize_from_w=0) L100
  first(self) L112
  next(self) L153

randn(seed, shape, generator=None) L6
"Generate a tensor with random numbers from a normal distribution using seed."
randn_local(seed, shape) L22
"Generate a tensor with random numbers from a normal distribution using seed."
randn_like(x) L36
"Generate a tensor with random numbers from a normal distribution using the previously initialized genrator."
randn_without_seed(shape, generator=None) L50
"Generate a tensor with random numbers from a normal distribution using the previously initialized genrator."
manual_seed(seed) L64
"Set up a global random number generator using the specified seed."
create_generator(seed) L75
slerp(val, low, high) L85

modules/rng_philox.py
class Generator L77
  "RNG that produces same outputs as torch.randn(..., device='cuda') on CPU"
  __init__(self, seed) L80
  randn(self, shape) L84
  "Generate a sequence of n standard normal random variables using the Philox 4x32 random number generator and the Box-Muller transform."

uint32(x) L27
"Converts (N,) np.uint64 array into (2, N) np.unit32 array."
philox4_round(counter, key) L32
"A single round of the Philox 4x32 random number generator."
philox4_32(counter, key, rounds=10) L44
"Generates 32-bit random numbers using the Philox 4x32 random number generator."
box_muller(x, y) L66
"Returns just the first out of two numbers generated by Box–Muller transform algorithm."

modules/safe.py
class RestrictedUnpickler(pickle.Unpickler) L23
  persistent_load(self, saved_id) L26
  find_class(self, module, name) L34

class Extra L159
  "A class for temporarily setting the global handler for when you can't explicitly call load_with_extra"
  __init__(self, handler) L179
  __enter__(self) L182
  __exit__(self, exc_type, exc_val, exc_tb) L188

encode(*args) L18
check_zip_filenames(filename, names) L71
check_pt(filename, extra_handler) L79
load(filename, *args, **kwargs) L107
load_with_extra(filename, extra_handler=None, *args, **kwargs) L111
"this function is intended to be used by extensions that want to load models with"

modules/script_callbacks.py
class ImageSaveParams L16
  __init__(self, image, p, filename, pnginfo) L17

class ExtraNoiseParams L31
  __init__(self, noise, x, xi) L32

class CFGDenoiserParams L43
  __init__(self, x, image_cond, sigma, sampling_step, total_sampling_steps, text_cond, text_uncond) L44

class CFGDenoisedParams L67
  __init__(self, x, sampling_step, total_sampling_steps, inner_model) L68

class AfterCFGCallbackParams L82
  __init__(self, x, sampling_step, total_sampling_steps) L83

class UiTrainTabParams L94
  __init__(self, txt2img_preview_params) L95

class ImageGridLoopParams L99
  __init__(self, imgs, cols, rows) L100

report_exception(c, job) L12
clear_callbacks() L131
app_started_callback(demo, app) L136
app_reload_callback() L145
model_loaded_callback(sd_model) L153
ui_tabs_callback() L161
ui_train_tabs_callback(params) L173
ui_settings_callback() L181
before_image_saved_callback(params) L189
image_saved_callback(params) L197
extra_noise_callback(params) L205
cfg_denoiser_callback(params) L213
cfg_denoised_callback(params) L221
cfg_after_cfg_callback(params) L229
before_component_callback(component, **kwargs) L237
after_component_callback(component, **kwargs) L245
image_grid_callback(params) L253
infotext_pasted_callback(infotext, params) L261
script_unloaded_callback() L269
before_ui_callback() L277
list_optimizers_callback() L285
list_unets_callback() L297
add_callback(callbacks, fun) L309
remove_current_script_callbacks() L316
remove_callbacks_for_function(callback_func) L326
on_app_started(callback) L332
"register a function to be called when the webui started, the gradio `Block` component and"
on_before_reload(callback) L338
"register a function to be called just before the server reloads."
on_model_loaded(callback) L343
"register a function to be called when the stable diffusion model is created; the model is"
on_ui_tabs(callback) L349
"register a function to be called when the UI is creating new tabs."
on_ui_train_tabs(callback) L362
"register a function to be called when the UI is creating new tabs for the train tab."
on_ui_settings(callback) L369
"register a function to be called before UI settings are populated; add your settings"
on_before_image_saved(callback) L375
"register a function to be called before an image is saved to a file."
on_image_saved(callback) L383
"register a function to be called after an image is saved to a file."
on_extra_noise(callback) L391
"register a function to be called before adding extra noise in img2img or hires fix;"
on_cfg_denoiser(callback) L399
"register a function to be called in the kdiffussion cfg_denoiser method after building the inner model inputs."
on_cfg_denoised(callback) L407
"register a function to be called in the kdiffussion cfg_denoiser method after building the inner model inputs."
on_cfg_after_cfg(callback) L415
"register a function to be called in the kdiffussion cfg_denoiser method after cfg calculations are completed."
on_before_component(callback) L423
"register a function to be called before a component is created."
on_after_component(callback) L435
"register a function to be called after a component is created."
on_image_grid(callback) L440
"register a function to be called before making an image grid."
on_infotext_pasted(callback) L448
"register a function to be called before applying an infotext."
on_script_unloaded(callback) L457
"register a function to be called before the script is unloaded."
on_before_ui(callback) L464
"register a function to be called before the UI is created."
on_list_optimizers(callback) L470
"register a function to be called when UI is making a list of cross attention optimization options."
on_list_unets(callback) L478
"register a function to be called when UI is making a list of alternative options for unet."

modules/script_loading.py
load_module(path) L7
preload_extensions(extensions_dir, parser, extension_list=None) L15

modules/scripts.py
class PostprocessImageArgs L15
  __init__(self, image) L16

class PostprocessBatchListArgs L20
  __init__(self, images) L21

class OnComponent L26

class Script L30
  title(self) L74
  "this function should return the title of the script."
  ui(self, is_img2img) L79
  "this function should create gradio UI elements."
  show(self, is_img2img) L87
  "is_img2img is True if this function is called for the img2img interface, and Fasle otherwise"
  run(self, p, *args) L99
  "This function is called if the script has been selected in the script dropdown."
  setup(self, p, *args) L112
  "For AlwaysVisible scripts, this function is called when the processing object is set up, before any processing starts."
  before_process(self, p, *args) L119
  "This function is called very early during processing begins for AlwaysVisible scripts."
  process(self, p, *args) L128
  "This function is called before processing begins for AlwaysVisible scripts."
  before_process_batch(self, p, *args, **kwargs) L137
  "Called before extra networks are parsed from the prompt, so you can add"
  after_extra_networks_activate(self, p, *args, **kwargs) L151
  "Called after extra networks activation, before conds calculation"
  process_batch(self, p, *args, **kwargs) L166
  "Same as process(), but called for every batch."
  postprocess_batch(self, p, *args, **kwargs) L179
  "Same as process_batch(), but called for every batch after it has been generated."
  postprocess_batch_list(self, p, pp, *args, **kwargs) L190
  "Same as postprocess_batch(), but receives batch images as a list of 3D tensors instead of a 4D tensor."
  postprocess_image(self, p, pp, *args) L209
  "Called for every image after it has been generated."
  postprocess(self, p, processed, *args) L216
  "This function is called after processing ends for AlwaysVisible scripts."
  before_component(self, component, **kwargs) L224
  "Called before a component is created."
  after_component(self, component, **kwargs) L234
  "Called after a component is created."
  on_before_component(self, callback, *, elem_id) L241
  "Calls callback before a component is created."
  on_after_component(self, callback, *, elem_id) L255
  "Calls callback after a component is created."
  describe(self) L264
  "unused"
  elem_id(self, item_id) L268
  "helper function to generate id for a HTML element, constructs final id out of script name, tab and user-supplied item_id"
  before_hr(self, p, *args) L278
  "This function is called before hires fix start."

class ScriptBuiltinUI(Script) L285
  elem_id(self, item_id) L288
  "helper function to generate id for a HTML element, constructs final id out of tab and user-supplied item_id"

class ScriptWithDependencies L339

class ScriptRunner L496
  __init__(self) L497
  initialize_scripts(self, is_img2img) L513
  apply_on_before_component_callbacks(self) L542
  create_script_ui(self, script) L562
  create_script_ui_inner(self, script) L572
  setup_ui_for_section(self, section, scriptlist=None) L616
  prepare_ui(self) L632
  setup_ui(self) L635
  select_script(script_index) L647
  init_field(title) L652
  "called when an initial value is set from ui-config.json to show script's UI components"
  onload_script_visibility(params) L671
  run(self, p, *args) L688
  before_process(self, p) L706
  process(self, p) L714
  before_process_batch(self, p, **kwargs) L722
  after_extra_networks_activate(self, p, **kwargs) L730
  process_batch(self, p, **kwargs) L738
  postprocess(self, p, processed) L746
  postprocess_batch(self, p, images, **kwargs) L754
  postprocess_batch_list(self, p, pp, **kwargs) L762
  postprocess_image(self, p, pp) L770
  before_component(self, component, **kwargs) L778
  after_component(self, component, **kwargs) L791
  script(self, title) L804
  reload_sources(self, cache) L807
  before_hr(self, p) L825
  setup_scrips(self, p, *, is_ui=True) L833

basedir() L300
"returns the base directory for the current script."
topological_sort(dependencies) L314
"Accepts a dictionary mapping name to its dependencies, returns a list of names ordered according to dependencies."
inner(name) L322
list_scripts(scriptdirname, extension, *, include_extensions=True) L347
list_files_with_name(filename) L425
load_scripts() L441
register_scripts_from_module(module) L451
wrap_call(func, filename, funcname, *args, default=None, **kwargs) L487
reload_script_body_only() L851

modules/scripts_auto_postprocessing.py
class ScriptPostprocessingForMainUI(scripts.Script) L4
  __init__(self, script_postproc) L5
  title(self) L9
  show(self, is_img2img) L12
  ui(self, is_img2img) L15
  postprocess_image(self, p, script_pp, *args) L19

create_auto_preprocessing_script_data() L29

modules/scripts_postprocessing.py
class PostprocessedImageSharedInfo L9

class PostprocessedImage L14
  __init__(self, image) L15
  get_suffix(self, used_suffixes=None) L24
  create_copy(self, new_image, *, nametags=None, disable_processing=False) L43

class ScriptPostprocessing L56
  ui(self) L71
  "This function should create gradio UI elements."
  process(self, pp, **args) L80
  "This function is called to postprocess the image."
  process_firstpass(self, pp, **args) L88
  "Called for all scripts before calling process()."
  image_changed(self) L97

class ScriptPostprocessingRunner L111
  __init__(self) L112
  initialize_scripts(self, scripts_data) L116
  create_script_ui(self, script, inputs) L128
  scripts_in_preferred_order(self) L140
  script_score(name) L147
  setup_ui(self) L158
  run(self, pp, args) L170
  create_args_for_run(self, scripts_args) L208
  image_changed(self) L225

wrap_call(func, filename, funcname, *args, default=None, **kwargs) L101

modules/sd_disable_initialization.py
class ReplaceHelper L9
  __init__(self) L10
  replace(self, obj, field, func) L13
  restore(self) L23

class DisableInitialization(ReplaceHelper) L30
  "When an object of this class enters a `with` block, it starts:"
  __init__(self, disable_clip=True) L46
  replace(self, obj, field, func) L50
  __enter__(self) L60
  do_nothing(*args, **kwargs) L61
  create_model_and_transforms_without_pretrained(*args, pretrained=None, **kwargs) L64
  CLIPTextModel_from_pretrained(pretrained_model_name_or_path, *model_args, **kwargs) L67
  transformers_modeling_utils_load_pretrained_model(*args, **kwargs) L72
  transformers_utils_hub_get_file_from_cache(original, url, *args, **kwargs) L76
  transformers_utils_hub_get_from_cache(url, *args, local_files_only=False, **kwargs) L90
  transformers_tokenization_utils_base_cached_file(url, *args, local_files_only=False, **kwargs) L93
  transformers_configuration_utils_cached_file(url, *args, local_files_only=False, **kwargs) L96
  __exit__(self, exc_type, exc_val, exc_tb) L111

class InitializeOnMeta(ReplaceHelper) L115
  "Context manager that causes all parameters for linear/conv2d/mha layers to be allocated on meta device,"
  __enter__(self) L128
  set_device(x) L132
  __exit__(self, exc_type, exc_val, exc_tb) L141

class LoadStateDictOnMeta(ReplaceHelper) L145
  "Context manager that allows to read parameters from state_dict into a model that has some of its parameters in the meta device."
  __init__(self, state_dict, device, weight_dtype_conversion=None) L158
  get_weight_dtype(self, key) L165
  __enter__(self) L169
  load_from_state_dict(original, module, state_dict, prefix, *args, **kwargs) L176
  load_state_dict(original, module, state_dict, strict=True) L206
  "torch makes a lot of copies of the dictionary with weights, so just deleting entries from state_dict does not help"
  __exit__(self, exc_type, exc_val, exc_tb) L231

modules/sd_hijack.py
class StableDiffusionModelHijack L168
  __init__(self) L175
  apply_optimizations(self, option=None) L184
  convert_sdxl_to_ssd(self, m) L191
  "Converts an SDXL model to a Segmind Stable Diffusion model (see https://huggingface.co/segmind/SSD-1B)"
  hijack(self, m) L205
  flatten(el) L254
  undo_hijack(self, m) L275
  apply_circular(self, enable) L311
  clear_comments(self) L320
  get_prompt_lengths(self, text) L324
  redo_hijack(self, m) L332

class EmbeddingsWithFixes(torch.nn.Module) L337
  __init__(self, wrapped, embeddings, textual_inversion_key='clip_l') L338
  forward(self, input_ids) L344

list_optimizers() L48
apply_optimizations(option=None) L59
undo_optimizations() L103
fix_checkpoint() L113
"checkpoints are now added and removed in embedding/hypernet code, since torch doesn't want"
weighted_loss(sd_model, pred, target, mean=True) L120
weighted_forward(sd_model, x, c, w, *args, **kwargs) L132
apply_weighted_forward(sd_model) L157
undo_weighted_forward(sd_model) L161
add_circular_option_to_conv_2d() L366
conv2d_constructor_circular(self, *args, **kwargs) L369
register_buffer(self, name, attr) L378
"Fix register buffer bug for Mac OS."

modules/sd_hijack_checkpoint.py
BasicTransformerBlock_forward(self, x, context=None) L7
AttentionBlock_forward(self, x) L11
ResBlock_forward(self, x, emb) L15
add() L22
remove() L37

modules/sd_hijack_clip.py
class PromptChunk L10
  "This object contains token ids, weight (multipliers:1.4) and textual inversion embedding info for a chunk of prompt."
  __init__(self) L18

class FrozenCLIPEmbedderWithCustomWordsBase(torch.nn.Module) L30
  "A pytorch module that is a wrapper for FrozenCLIPEmbedder module."
  __init__(self, wrapped, hijack) L35
  empty_chunk(self) L49
  "creates an empty PromptChunk and returns it"
  get_target_prompt_token_count(self, token_count) L57
  "returns the maximum number of tokens a prompt of a known length can have before it requires one more PromptChunk to be represented"
  tokenize(self, texts) L62
  "Converts a batch of texts into a batch of token ids"
  encode_with_transformers(self, tokens) L67
  "converts a batch of token ids (in python lists) into a single tensor with numeric respresentation of those tokens;"
  encode_embedding_init_text(self, init_text, nvpt) L78
  "Converts text into a tensor with this text's tokens' embeddings."
  tokenize_line(self, line) L84
  "this transforms a single prompt into a list of PromptChunk objects - as many as needed to"
  next_chunk(is_last=False) L103
  "puts current chunk into the list of results and produces the next one - empty;"
  process_texts(self, texts) L179
  "Accepts a list of texts and calls tokenize_line() on each, with cache."
  forward(self, texts) L202
  "Accepts an array of texts; Passes texts through transformers network to create a tensor with numerical representation of those texts."
  process_tokens(self, remade_batch_tokens, batch_multipliers) L257
  "sends one single prompt chunk to be encoded by transformers neural network."

class FrozenCLIPEmbedderWithCustomWords(FrozenCLIPEmbedderWithCustomWordsBase) L290
  __init__(self, wrapped, hijack) L291
  tokenize(self, texts) L320
  encode_with_transformers(self, tokens) L325
  encode_embedding_init_text(self, init_text, nvpt) L336

class FrozenCLIPEmbedderForSDXLWithCustomWords(FrozenCLIPEmbedderWithCustomWords) L344
  __init__(self, wrapped, hijack) L345
  encode_with_transformers(self, tokens) L348

modules/sd_hijack_clip_old.py
process_text_old(self, texts) L5
forward_old(self, texts) L72

modules/sd_hijack_ip2p.py
should_hijack_ip2p(checkpoint_info) L4

modules/sd_hijack_open_clip.py
class FrozenOpenCLIPEmbedderWithCustomWords(sd_hijack_clip.FrozenCLIPEmbedderWithCustomWordsBase) L10
  __init__(self, wrapped, hijack) L11
  tokenize(self, texts) L19
  encode_with_transformers(self, tokens) L26
  encode_embedding_init_text(self, init_text, nvpt) L32

class FrozenOpenCLIPEmbedder2WithCustomWords(sd_hijack_clip.FrozenCLIPEmbedderWithCustomWordsBase) L40
  __init__(self, wrapped, hijack) L41
  tokenize(self, texts) L49
  encode_with_transformers(self, tokens) L56
  encode_embedding_init_text(self, init_text, nvpt) L66

modules/sd_hijack_optimizations.py
class SdOptimization L25
  title(self) L31
  is_available(self) L37
  apply(self) L40
  undo(self) L43

class SdOptimizationXformers(SdOptimization) L51
  is_available(self) L56
  apply(self) L59

class SdOptimizationSdpNoMem(SdOptimization) L66
  is_available(self) L72
  apply(self) L75

class SdOptimizationSdp(SdOptimizationSdpNoMem) L82
  apply(self) L88

class SdOptimizationSubQuad(SdOptimization) L95
  priority(self) L100
  apply(self) L103

class SdOptimizationV1(SdOptimization) L110
  apply(self) L116

class SdOptimizationInvokeAI(SdOptimization) L121
  priority(self) L126
  apply(self) L129

class SdOptimizationDoggettx(SdOptimization) L134
  apply(self) L139

list_optimizers(res) L146
get_available_vram() L166
split_cross_attention_forward_v1(self, x, context=None, mask=None, **kwargs) L180
split_cross_attention_forward(self, x, context=None, mask=None, **kwargs) L221
einsum_op_compvis(q, k, v) L288
einsum_op_slice_0(q, k, v, slice_size) L294
einsum_op_slice_1(q, k, v, slice_size) L302
einsum_op_mps_v1(q, k, v) L310
einsum_op_mps_v2(q, k, v) L320
einsum_op_tensor_mem(q, k, v, max_tensor_mb) L327
einsum_op_cuda(q, k, v) L337
einsum_op(q, k, v) L348
split_cross_attention_forward_invokeAI(self, x, context=None, mask=None, **kwargs) L362
sub_quad_attention_forward(self, x, context=None, mask=None, **kwargs) L390
sub_quad_attention(q, k, v, q_chunk_size=1024, kv_chunk_size=None, kv_chunk_size_min=None, chunk_threshold=None, use_checkpoint=True) L427
get_xformers_flash_attention_op(q, k, v) L465
xformers_attention_forward(self, x, context=None, mask=None, **kwargs) L480
scaled_dot_product_attention_forward(self, x, context=None, mask=None, **kwargs) L506
scaled_dot_product_no_mem_attention_forward(self, x, context=None, mask=None, **kwargs) L547
cross_attention_attnblock_forward(self, x) L552
xformers_attnblock_forward(self, x) L611
sdp_attnblock_forward(self, x) L635
sdp_no_mem_attnblock_forward(self, x) L656
sub_quad_attnblock_forward(self, x) L661

modules/sd_hijack_unet.py
class TorchHijackForUnet L8
  "This is torch, but with cat that resizes tensors to appropriate dimensions if they do not match;"
  __getattr__(self, item) L14
  cat(self, tensors, *args, **kwargs) L23

class GELUHijack(torch.nn.GELU, torch.nn.Module) L51
  __init__(self, *args, **kwargs) L52
  forward(self, x) L54

apply_model(orig_func, self, x_noisy, t, cond, **kwargs) L38
hijack_ddpm_edit() L62

modules/sd_hijack_utils.py
class CondFunc L3
  __new__(cls, orig_func, sub_func, cond_func) L4
  __init__(self, orig_func, sub_func, cond_func) L20
  __call__(self, *args, **kwargs) L24

modules/sd_hijack_xlmr.py
class FrozenXLMREmbedderWithCustomWords(sd_hijack_clip.FrozenCLIPEmbedderWithCustomWords) L6
  __init__(self, wrapped, hijack) L7
  encode_with_transformers(self, tokens) L16
  encode_embedding_init_text(self, init_text, nvpt) L27

modules/sd_models.py
class CheckpointInfo L48
  __init__(self, filename) L49
  read_metadata() L66
  register(self) L94
  calculate_shorthash(self) L99

class SkipWritingToConfig L336
  "This context manager prevents load_model_weights from writing checkpoint name to the config when it loads weight."
  __enter__(self) L342
  __exit__(self, exc_type, exc_value, exc_traceback) L347

class SdModelData L514
  __init__(self) L515
  get_sd_model(self) L521
  set_sd_model(self, v, already_loaded=False) L541

replace_key(d, key, new_key, value) L30
setup_model() L132
"called once at startup to do various one-time tasks related to SD models"
checkpoint_tiles(use_short=False) L141
list_models() L145
get_closet_checkpoint_match(search_string) L173
model_hash(filename) L193
"old hash that only looks at a small part of the file and is prone to collisions"
select_checkpoint() L208
"Raises `FileNotFoundError` if no checkpoints are found."
transform_checkpoint_dict_key(k, replacements) L244
get_state_dict_from_checkpoint(pl_sd) L252
read_metadata_from_safetensors(filename) L274
read_state_dict(checkpoint_file, print_global_state=False, map_location=None) L298
get_checkpoint_state_dict(checkpoint_info, timer) L318
load_model_weights(model, checkpoint_info, state_dict, timer) L351
enable_midas_autodownload() L431
"Gives the ldm.modules.midas.api.load_model function automatic downloading."
load_model_wrapper(model_type) L459
patch_given_betas() L474
patched_register_schedule(*args, **kwargs) L477
"a modified version of register_schedule function that converts plain list from Omegaconf into numpy"
repair_config(sd_config) L488
get_empty_cond(sd_model) L560
send_model_to_cpu(m) L572
model_target_device(m) L581
send_model_to_device(m) L588
send_model_to_trash(m) L595
load_model(checkpoint_info=None, already_loaded_state_dict=None) L600
reuse_model_from_already_loaded(sd_model, checkpoint_info, timer) L690
"Checks if the desired checkpoint from checkpoint_info is not already loaded in model_data.loaded_sd_models."
reload_model_weights(sd_model=None, info=None) L749
unload_model_weights(sd_model=None, info=None) L811
apply_token_merging(sd_model, token_merging_ratio) L817
"Applies speed and memory optimizations from tomesd."

modules/sd_models_config.py
is_using_v_parameterization_for_sd2(state_dict) L26
"Detects whether unet in state_dict is using v-parameterization."
guess_model_config_from_state_dict(sd, filename) L68
find_checkpoint_config(state_dict, info) L107
find_checkpoint_config_near_filename(info) L118

modules/sd_models_types.py
class WebuiSdModel(LatentDiffusion) L9
  "This class is not actually instantinated, but its fields are created and fieeld by webui"

modules/sd_models_xl.py
get_learned_conditioning(self, batch) L11
apply_model(self, x, t, cond) L36
get_first_stage_encoding(self, x) L40
encode_embedding_init_text(self, init_text, nvpt) L49
tokenize(self, texts) L59
process_texts(self, texts) L67
get_target_prompt_token_count(self, token_count) L72
extend_sdxl(model) L84
"this adds a bunch of parameters to make SDXL model look a bit more like SD1.5 to the rest of the codebase."

modules/sd_samplers.py
find_sampler_config(name) L18
create_sampler(name, model) L27
set_samplers() L41
visible_sampler_names() L55

modules/sd_samplers_cfg_denoiser.py
class CFGDenoiser(torch.nn.Module) L33
  "Classifier free guidance denoiser."
  __init__(self, sampler) L41
  inner_model(self) L62
  combine_denoised(self, x_out, conds_list, uncond, cond_scale) L65
  combine_denoised_for_edit_model(self, x_out, cond_scale) L75
  get_pred_x0(self, x_in, x_out, sigma) L81
  update_inner_model(self) L84
  forward(self, x, sigma, uncond, cond, cond_scale, s_min_uncond, image_cond) L91

catenate_conds(conds) L11
subscript_cond(cond, a, b) L18
pad_cond(tensor, repeats, empty) L25

modules/sd_samplers_common.py
class SamplerData(SamplerDataTuple) L14
  total_steps(self, steps) L15

class InterruptedException(BaseException) L142

class TorchHijack L194
  "This is here to replace torch.randn_like of k-diffusion."
  __init__(self, p) L202
  __getattr__(self, item) L205
  randn_like(self, x) L214

class Sampler L218
  __init__(self, funcname) L219
  callback_state(self, d) L245
  launch_sampling(self, steps, func) L254
  number_of_needed_noises(self, p) L272
  initialize(self, p) L275
  create_noise_sampler(self, x, sigmas, p) L323
  "For DPM++ SDE: manually create noise sampler to enable deterministic results across different batch sizes"
  sample(self, p, x, conditioning, unconditional_conditioning, steps=None, image_conditioning=None) L333
  sample_img2img(self, p, x, noise, conditioning, unconditional_conditioning, steps=None, image_conditioning=None) L336

setup_img2img_steps(p, steps=None) L22
samples_to_images_tensor(sample, approximation=None, model=None) L37
"Transforms 4-channel latent space images into 3-channel RGB image tensors, with values in range [-1, 1]."
single_sample_to_image(sample, approximation=None) L63
decode_first_stage(model, x) L73
sample_to_image(samples, index=0, approximation=None) L79
samples_to_image_grid(samples, approximation=None) L83
images_tensor_to_samples(image, approximation=None, model=None) L87
"image[0, 1] -> latent"
store_latent(decoded) L115
is_sampler_using_eta_noise_seed_delta(p) L123
"returns whether sampler from config will use eta noise seed delta for image creation"
replace_torchsde_browinan() L146
torchsde_randn(size, dtype, device, seed) L149
apply_refiner(cfg_denoiser) L158

modules/sd_samplers_extra.py
restart_sampler(model, x, sigmas, extra_args=None, callback=None, disable=None, s_noise=1.0, restart_list=None) L7
"Implements restart sampling in Restart Sampling for Improving Generative Processes (2023)"
heun_step(x, old_sigma, new_sigma, second_order=True) L17

modules/sd_samplers_kdiffusion.py
class CFGDenoiserKDiffusion(sd_samplers_cfg_denoiser.CFGDenoiser) L69
  inner_model(self) L71

class KDiffusionSampler(sd_samplers_common.Sampler) L79
  __init__(self, funcname, sd_model, options=None) L80
  get_sigmas(self, p, steps) L91
  sample_img2img(self, p, x, noise, conditioning, unconditional_conditioning, steps=None, image_conditioning=None) L141
  sample(self, p, x, conditioning, unconditional_conditioning, steps=None, image_conditioning=None) L195

modules/sd_samplers_timesteps.py
class CompVisTimestepsDenoiser(torch.nn.Module) L24
  __init__(self, model, *args, **kwargs) L25
  forward(self, input, timesteps, **kwargs) L29

class CompVisTimestepsVDenoiser(torch.nn.Module) L33
  __init__(self, model, *args, **kwargs) L34
  predict_eps_from_z_and_v(self, x_t, t, v) L38
  forward(self, input, timesteps, **kwargs) L41

class CFGDenoiserTimesteps(CFGDenoiser) L47
  __init__(self, sampler) L49
  get_pred_x0(self, x_in, x_out, sigma) L55
  inner_model(self) L66

class CompVisSampler(sd_samplers_common.Sampler) L74
  __init__(self, funcname, sd_model) L75
  get_timesteps(self, p, steps) L84
  sample_img2img(self, p, x, noise, conditioning, unconditional_conditioning, steps=None, image_conditioning=None) L96
  sample(self, p, x, conditioning, unconditional_conditioning, steps=None, image_conditioning=None) L140

modules/sd_samplers_timesteps_impl.py
class UniPCCFG(uni_pc.UniPC) L104
  __init__(self, cfg_model, extra_args, callback, *args, **kwargs) L105
  after_update(x, model_x) L108
  get_model_input_time(self, t_continuous) L118
  model(self, x, t) L121

ddim(model, x, timesteps, extra_args=None, callback=None, disable=None, eta=0.0) L11
plms(model, x, timesteps, extra_args=None, callback=None, disable=None) L43
get_x_prev_and_pred_x0(e_t, index) L54
unipc(model, x, timesteps, extra_args=None, callback=None, disable=None, is_img2img=False) L129

modules/sd_unet.py
class SdUnetOption L63
  create_unet(self) L70
  "returns SdUnet object to be used as a Unet instead of built-in unet when making pictures"

class SdUnet(torch.nn.Module) L75
  forward(self, x, timesteps, context, *args, **kwargs) L76
  activate(self) L79
  deactivate(self) L82

list_unets() L10
get_unet_option(option=None) L17
apply_unet(option=None) L33
create_unet_forward(original_forward) L86
UNetModel_forward(self, x, timesteps=None, context=None, *args, **kwargs) L87

modules/sd_vae.py
class VaeResolution L119
  tuple(self) L124

get_loaded_vae_name() L23
get_loaded_vae_hash() L30
get_base_vae(model) L39
store_base_vae(model) L45
delete_base_vae() L53
restore_base_vae(model) L59
get_filename(filepath) L68
refresh_vae_list() L72
find_vae_near_checkpoint(checkpoint_file) L109
is_automatic() L128
resolve_vae_from_setting() L132
resolve_vae_from_user_metadata(checkpoint_file) L146
resolve_vae_near_checkpoint(checkpoint_file) L160
resolve_vae(checkpoint_file) L168
load_vae_dict(filename, map_location) L188
load_vae(model, vae_file=None, vae_source='from unknown source') L194
_load_vae_dict(model, vae_dict_1) L238
clear_loaded_vae() L243
reload_vae_weights(sd_model=None, vae_file=unspecified) L251

modules/sd_vae_approx.py
class VAEApprox(nn.Module) L10
  __init__(self) L11
  forward(self, x) L22

download_model(model_path, model_url) L34
model() L42
cheap_approximation(sample) L64

modules/sd_vae_taesd.py
class Clamp(nn.Module) L20
  forward(x) L22

class Block(nn.Module) L26
  __init__(self, n_in, n_out) L27
  forward(self, x) L33

class TAESDDecoder(nn.Module) L57
  __init__(self, decoder_path='taesd_decoder.pth') L61
  "Initialize pretrained TAESD on the given device from the given checkpoints."

class TAESDEncoder(nn.Module) L69
  __init__(self, encoder_path='taesd_encoder.pth') L73
  "Initialize pretrained TAESD on the given device from the given checkpoints."

conv(n_in, n_out, **kwargs) L16
decoder() L37
encoder() L47
download_model(model_path, model_url) L81
decoder_model() L89
encoder_model() L108

modules/shared_gradio_themes.py
reload_gradio_theme(theme_name=None) L44

modules/shared_init.py
initialize() L9
"Initializes fields inside the shared module in a controlled manner."

modules/shared_items.py
class Shared(sys.modules[__name__].__class__) L116
  "this class is here to provide sd_model field as a property, so that it can be created and loaded on demand rather than"
  sd_model(self) L125
  sd_model(self, value) L131

realesrgan_models_names() L6
postprocessing_scripts() L11
sd_vae_items() L17
refresh_vae_list() L23
cross_attention_optimizations() L29
sd_unet_items() L35
refresh_unet_list() L41
list_checkpoint_tiles(use_short=False) L47
refresh_checkpoints() L52
list_samplers() L57
reload_hypernetworks() L62
get_infotext_names() L69
ui_reorder_categories() L101

modules/shared_state.py
class State L12
  __init__(self) L32
  need_restart(self) L36
  need_restart(self, value) L41
  server_command(self) L47
  server_command(self, value) L51
  "Set the server command to `value` and signal that it's been set."
  wait_for_server_command(self, timeout=None) L58
  "Wait for server command to get set; return and clear the value and signal."
  request_restart(self) L69
  skip(self) L74
  interrupt(self) L78
  nextjob(self) L82
  dict(self) L90
  begin(self, job='(unknown)') L104
  end(self) L122
  set_current_image(self) L130
  "if enough sampling steps have been made after the last call to this, sets self.current_image from self.current_latent, and modifies self.id_live_preview accordingly"
  do_set_current_image(self) L138
  assign_current_image(self, image) L157

modules/shared_total_tqdm.py
class TotalTQDM L6
  __init__(self) L7
  reset(self) L10
  update(self) L18
  updateTotal(self, new_total) L25
  clear(self) L32

modules/styles.py
class PromptStyle(typing.NamedTuple) L9

class StyleDatabase L94
  __init__(self, path) L95
  reload(self) L110
  "Clears the style database and reloads the styles from the CSV file(s)"
  load_from_csv(self, path) L143
  get_style_paths(self) L158
  "Returns a list of all distinct paths, including the default path, of"
  get_style_prompts(self, styles) L179
  get_negative_style_prompts(self, styles) L182
  apply_styles_to_prompt(self, prompt, styles) L185
  apply_negative_styles_to_prompt(self, prompt, styles) L190
  save_styles(self, path=None) L195
  extract_styles_from_prompt(self, prompt, negative_prompt) L234

merge_prompts(style_prompt, prompt) L16
apply_styles_to_prompt(prompt, styles) L26
unwrap_style_text_from_prompt(style_text, prompt) L33
"Checks the prompt to see if the style text is wrapped around it."
extract_original_prompts(style, prompt, negative_prompt) L70
"Takes a style and compares it to the prompt and negative prompt."

modules/sub_quadratic_attention.py
class AttnChunk(NamedTuple) L30

class SummarizeChunk L36
  __call__(query, key, value) L38

class ComputeQueryChunkAttn L45
  __call__(query, key, value) L47

class ScannedChunk(NamedTuple) L136

narrow_trunc(input, dim, start, length) L21
_summarize_chunk(query, key, value, scale) L54
_query_chunk_attention(query, key, value, summarize_chunk, kv_chunk_size) L75
chunk_scanner(chunk_idx) L85
_get_attention_scores_no_kv_chunking(query, key, value, scale) L117
efficient_dot_product_attention(query, key, value, query_chunk_size=1024, kv_chunk_size=None, kv_chunk_size_min=None, use_checkpoint=True) L141
"Computes efficient dot-product attention given query, key, and value."
get_query_chunk(chunk_idx) L175

modules/sysinfo.py
pretty_bytes(num, suffix='B') L40
get() L47
check(x) L61
get_dict() L72
get_environment() L106
get_argv() L110
get_torch_sysinfo() L129
get_extensions(*, enabled) L139
to_json(x) L142
get_config() L156

modules/textual_inversion/autocrop.py
class PointOfInterest L320
  __init__(self, x, y, weight=1.0, size=10) L321
  bounding(self, size) L327

class Settings L336
  __init__(self, crop_width=512, crop_height=512, corner_points_weight=0.5, entropy_points_weight=0.5, face_points_weight=0.5, annotate_image=False, dnn_model_path=None) L337

crop_image(im, settings) L14
"Intelligently crop an image to the subject matter"
focal_point(im, settings) L74
image_face_points(im, settings) L142
image_corner_points(im, settings) L200
image_entropy_points(im, settings) L228
image_entropy(im) L260
centroid(pois) L269
poi_average(pois, settings) L275
is_landscape(w, h) L289
is_portrait(w, h) L293
is_square(w, h) L297
download_and_cache_models() L310

modules/textual_inversion/dataset.py
class DatasetEntry L21
  __init__(self, filename=None, filename_text=None, latent_dist=None, latent_sample=None, cond=None, cond_text=None, pixel_values=None, weight=None) L22

class PersonalizedBase(Dataset) L33
  __init__(self, data_root, width, height, repeats, flip_p=0.5, placeholder_token='*', model=None, cond_model=None, device=None, template_file=None, include_cond=False, batch_size=1, gradient_step=1, shuffle_tags=False, tag_drop_out=0, latent_sampling_method='once', varsize=False, use_weight=False) L34
  create_text(self, filename_text) L153
  __len__(self) L164
  __getitem__(self, i) L167

class GroupedBatchSampler(Sampler) L176
  __init__(self, data_source, batch_size) L177
  __len__(self) L189
  __iter__(self) L192

class PersonalizedDataLoader(DataLoader) L210
  __init__(self, dataset, latent_sampling_method='once', batch_size=1, pin_memory=False) L211

class BatchLoader L219
  __init__(self, data) L220
  pin_memory(self) L231

class BatchLoaderRandom(BatchLoader) L238
  __init__(self, data) L239
  pin_memory(self) L242

collate_wrapper(batch) L235
collate_wrapper_random(batch) L245

modules/textual_inversion/image_embedding.py
class EmbeddingEncoder(json.JSONEncoder) L11
  default(self, obj) L12

class EmbeddingDecoder(json.JSONDecoder) L18
  __init__(self, *args, **kwargs) L19
  object_hook(self, d) L22

embedding_to_b64(data) L28
embedding_from_b64(data) L33
lcg(m=2 ** 32, a=1664525, c=1013904223, seed=0) L38
xor_block(block) L44
style_block(block, sequence) L50
insert_image_data_embed(image, data) L68
crop_black(img, tol=0) L104
extract_image_data_embed(image) L112
caption_image_overlay(srcimage, title, footerLeft, footerMid, footerRight, textfont=None) L133

modules/textual_inversion/learn_schedule.py
class LearnScheduleIterator L4
  __init__(self, learn_rate, max_steps, cur_step=0) L5
  "specify learn_rate as "0.001:100, 0.00001:1000, 1e-5:10000" to have lr of 0.001 until step 100, 0.00001 until 1000, and 1e-5 until 10000"
  __iter__(self) L39
  __next__(self) L42

class LearnRateScheduler L50
  __init__(self, learn_rate, max_steps, cur_step=0, verbose=True) L51
  step(self, step_number) L61
  apply(self, optimizer, step_number) L72

modules/textual_inversion/logging.py
save_settings_to_file(log_directory, all_params) L52

modules/textual_inversion/textual_inversion.py
class Embedding L40
  __init__(self, vec, name, step=None) L41
  save(self, filename) L55
  checksum(self) L74
  const_hash(a) L78
  set_hash(self, v) L87

class DirWithTextualInversionEmbeddings L92
  __init__(self, path) L93
  has_changed(self) L97
  update(self) L105

class EmbeddingDatabase L112
  __init__(self) L113
  add_embedding_dir(self, path) L121
  clear_embedding_dirs(self) L124
  register_embedding(self, embedding, model) L127
  register_embedding_by_name(self, embedding, model, name) L130
  get_expected_shape(self) L153
  load_from_file(self, path, filename) L157
  load_from_dir(self, embdir) L191
  load_textual_inversion_embeddings(self, force_reload=False) L208
  find_embedding_at_position(self, tokens, offset) L241

list_textual_inversion_templates() L28
create_embedding(name, num_vectors_per_token, overwrite_old, init_text='*') L255
create_embedding_from_data(data, name, filename='unknown embedding file', filepath=None) L283
write_loss(log_directory, filename, step, epoch_len, values) L322
tensorboard_setup(log_directory) L346
tensorboard_add(tensorboard_writer, loss, global_step, step, learn_rate, epoch_num) L352
tensorboard_add_scaler(tensorboard_writer, tag, value, step) L358
tensorboard_add_image(tensorboard_writer, tag, pil_image, step) L362
validate_train_inputs(model_name, learn_rate, batch_size, gradient_step, data_root, template_file, template_filename, steps, save_model_every, create_image_every, log_directory, name='embedding') L371
train_embedding(id_task, embedding_name, learn_rate, batch_size, gradient_step, data_root, log_directory, training_width, training_height, varsize, steps, clip_grad_mode, clip_grad_value, shuffle_tags, tag_drop_out, latent_sampling_method, use_weight, create_image_every, save_embedding_every, template_filename, save_image_with_stored_embedding, preview_from_txt2img, preview_prompt, preview_negative_prompt, preview_steps, preview_sampler_name, preview_cfg_scale, preview_seed, preview_width, preview_height) L395
save_embedding(embedding, optimizer, checkpoint, embedding_name, filename, remove_cached_checksum=True) L682

modules/textual_inversion/ui.py
create_embedding(name, initialization_text, nvpt, overwrite_old) L9
train_embedding(*args) L17

modules/timer.py
class TimerSubcategory L5
  __init__(self, timer, category) L6
  __enter__(self) L12
  __exit__(self, exc_type, exc_val, exc_tb) L20

class Timer L28
  __init__(self, print_log=False) L29
  elapsed(self) L37
  add_time_to_record(self, category, amount) L43
  record(self, category, extra_time=0, disable_log=False) L49
  subcategory(self, name) L59
  summary(self) L65
  dump(self) L78
  reset(self) L81

modules/txt2img.py
txt2img(id_task, prompt, negative_prompt, prompt_styles, steps, sampler_name, n_iter, batch_size, cfg_scale, height, width, enable_hr, denoising_strength, hr_scale, hr_upscaler, hr_second_pass_steps, hr_resize_x, hr_resize_y, hr_checkpoint_name, hr_sampler_name, hr_prompt, hr_negative_prompt, override_settings_texts, request, *args) L12

modules/ui.py
class QuicksettingsHint(BaseModel) L1187

gr_show(visible=True) L60
send_gradio_gallery_to_image(x) L85
calc_resolution_hires(enable, width, height, hr_scale, hr_resize_x, hr_resize_y) L91
resize_from_to_html(width, height, scale_by) L101
process_interrogate(interrogation_function, mode, ii_input_dir, ii_output_dir, *ii_singles) L111
interrogate(image) L134
interrogate_deepbooru(image) L139
connect_clear_prompt(button) L144
"Given clear button, prompt, and token_counter objects, setup clear prompt button click event"
update_token_counter(text, steps, *, is_positive=True) L154
update_negative_prompt_token_counter(text, steps) L176
setup_progressbar(*args, **kwargs) L181
apply_setting(key, value) L185
create_output_panel(tabname, outdir, toprow=None) L218
create_sampler_and_steps_selection(choices, tabname) L222
ordered_ui_categories() L235
create_override_settings_dropdown(tabname, row) L242
create_ui() L254
add_copy_image_controls(tab_name, elem) L505
update_orig(image, state) L545
copy_image(img) L579
select_img2img_tab(tab) L704
get_textual_inversion_template_names() L915
versions_html() L1155
setup_ui_api(app) L1184
quicksettings_hint() L1191
download_sysinfo(attachment=False) L1200

modules/ui_checkpoint_merger.py
class UiCheckpointMerger L29
  __init__(self) L30
  setup_ui(self, dummy_component, sd_model_checkpoint_component) L86

update_interp_description(value) L9
modelmerger(*args) L19

modules/ui_common.py
class MyObject L44
  __init__(self, d=None) L45

update_generation_info(generation_info, html_info, img_index) L20
plaintext_to_html(text, classname=None) L32
save_files(js_data, images, do_make_zip, index) L38
create_output_panel(tabname, outdir, toprow=None) L107
open_folder(f) L109
create_refresh_button(refresh_component, refresh_method, refreshed_args, elem_id) L230
refresh() L239
setup_dialog(button_show, dialog, *, button_close=None) L258
"Sets up the UI so that the dialog (gr.Box) is invisible, and is only shown when buttons_show is clicked, in a fullscreen modal window."

modules/ui_components.py
class FormComponent L4
  get_expected_parent(self) L5

class ToolButton(FormComponent, gr.Button) L12
  "Small button with single emoji as text, fits inside gradio forms"
  __init__(self, *args, **kwargs) L15
  get_block_name(self) L19

class ResizeHandleRow(gr.Row) L23
  "Same as gr.Row but fits inside gradio forms"
  __init__(self, **kwargs) L26
  get_block_name(self) L31

class FormRow(FormComponent, gr.Row) L35
  "Same as gr.Row but fits inside gradio forms"
  get_block_name(self) L38

class FormColumn(FormComponent, gr.Column) L42
  "Same as gr.Column but fits inside gradio forms"
  get_block_name(self) L45

class FormGroup(FormComponent, gr.Group) L49
  "Same as gr.Group but fits inside gradio forms"
  get_block_name(self) L52

class FormHTML(FormComponent, gr.HTML) L56
  "Same as gr.HTML but fits inside gradio forms"
  get_block_name(self) L59

class FormColorPicker(FormComponent, gr.ColorPicker) L63
  "Same as gr.ColorPicker but fits inside gradio forms"
  get_block_name(self) L66

class DropdownMulti(FormComponent, gr.Dropdown) L70
  "Same as gr.Dropdown but always multiselect"
  __init__(self, **kwargs) L72
  get_block_name(self) L75

class DropdownEditable(FormComponent, gr.Dropdown) L79
  "Same as gr.Dropdown but allows editing value"
  __init__(self, **kwargs) L81
  get_block_name(self) L84

class InputAccordion(gr.Checkbox) L88
  "A gr.Accordion that can be used as an input - returns True if open, False if closed."
  __init__(self, value, **kwargs) L96
  extra(self) L120
  "Allows you to put something into the label of the accordion."
  __enter__(self) L136
  __exit__(self, exc_type, exc_val, exc_tb) L140
  get_block_name(self) L143

modules/ui_extensions.py
check_access() L22
apply_and_restart(disable_list, update_list, disable_all) L26
save_config_state(name) L59
restore_config_state(confirmed, config_state_name, restore_type) L75
check_updates(id_task, disable_list) L99
make_commit_link(commit_hash, remote, text=None) L124
extension_table() L136
update_config_states_table(state_name) L193
normalize_git_url(url) L330
get_extension_dirname_from_url(url) L338
install_extension_from_url(dirname, url, branch_name=None) L343
install_extension_from_index(url, hide_tags, sort_column, filter_text) L398
refresh_available_extensions(url, hide_tags, sort_column) L406
refresh_available_extensions_for_tags(hide_tags, sort_column, filter_text) L420
search_extensions(filter_text, hide_tags, sort_column) L426
get_date(info, key) L445
refresh_available_extensions_from_data(hide_tags, sort_column, filter_text='') L452
preload_extensions_git_metadata() L528
create_ui() L533

modules/ui_extra_networks.py
class ExtraNetworksPage L100
  __init__(self, title) L101
  refresh(self) L111
  read_user_metadata(self, item) L114
  link_preview(self, filename) L124
  search_terms_from_path(self, filename, possible_directories=None) L129
  create_html(self, tabname) L139
  create_item(self, name, index=None) L208
  list_items(self) L211
  allowed_directories_for_previews(self) L214
  create_html_for_item(self, item, tabname) L217
  "Create HTML for card item in tab tabname; can return empty string if the item is not meant to be shown."
  get_sort_keys(self, path) L277
  "List of default keys used for sorting in the UI."
  find_preview(self, path) L290
  "Find a preview PNG for a given path (without extension) and call link_preview on it."
  find_description(self, path) L303
  "Find and read a description file for a given path (without extension)."
  create_user_metadata_editor(self, ui, tabname) L315

class ExtraNetworksUi L332
  __init__(self) L333

allowed_preview_extensions_with_extra(extra_extensions=None) L23
allowed_preview_extensions() L27
register_page(page) L31
"registers extra networks page for the UI; recommend doing it in on_before_ui() callback for extensions"
fetch_file(filename='') L39
get_metadata(page='', item='') L56
get_single_card(page='', tabname='', name='') L70
add_pages_to_demo(app) L88
quote_js(s) L94
initialize() L319
register_default_pages() L323
pages_in_preferred_order(pages) L348
tab_name_score(name) L351
create_ui(interface, unrelated_tabs, tabname) L364
pages_html() L417
refresh() L423
path_is_parent(parent_path, child_path) L437
setup_ui(ui, gallery) L444
save_preview(index, images, filename) L445

modules/ui_extra_networks_checkpoints.py
class ExtraNetworksPageCheckpoints(ui_extra_networks.ExtraNetworksPage) L9
  __init__(self) L10
  refresh(self) L15
  create_item(self, name, index=None, enable_filter=True) L18
  list_items(self) L37
  allowed_directories_for_previews(self) L45
  create_user_metadata_editor(self, ui, tabname) L48

modules/ui_extra_networks_checkpoints_user_metadata.py
class CheckpointUserMetadataEditor(ui_extra_networks_user_metadata.UserMetadataEditor) L7
  __init__(self, ui, tabname, page) L8
  save_user_metadata(self, name, desc, notes, vae) L13
  update_vae(self, name) L21
  put_values_into_components(self, name) L25
  create_editor(self) L34

modules/ui_extra_networks_hypernets.py
class ExtraNetworksPageHypernetworks(ui_extra_networks.ExtraNetworksPage) L8
  __init__(self) L9
  refresh(self) L12
  create_item(self, name, index=None, enable_filter=True) L15
  list_items(self) L36
  allowed_directories_for_previews(self) L44

modules/ui_extra_networks_textual_inversion.py
class ExtraNetworksPageTextualInversion(ui_extra_networks.ExtraNetworksPage) L7
  __init__(self) L8
  refresh(self) L12
  create_item(self, name, index=None, enable_filter=True) L15
  list_items(self) L33
  allowed_directories_for_previews(self) L41

modules/ui_extra_networks_user_metadata.py
class UserMetadataEditor L11
  __init__(self, ui, tabname, page) L13
  get_user_metadata(self, name) L35
  create_extra_default_items_in_left_column(self) L45
  create_default_editor_elems(self) L48
  create_default_buttons(self) L60
  get_card_html(self, name) L71
  relative_path(self, path) L92
  get_metadata_table(self, name) L99
  put_values_into_components(self, name) L118
  write_user_metadata(self, name, metadata) L131
  save_user_metadata(self, name, desc, notes) L139
  setup_save_handler(self, button, func, components) L146
  create_editor(self) L151
  create_ui(self) L164
  save_preview(self, index, gallery, name) L173
  setup_ui(self, gallery) L191

modules/ui_gradio_extensions.py
webpath(fn) L8
javascript_html() L17
css_html() L36
stylesheet(fn) L39
reload_javascript() L54
template_response(*args, **kwargs) L58

modules/ui_loadsave.py
class UiLoadsave L14
  "allows saving and restoring default values for gradio components"
  __init__(self, filename) L17
  add_component(self, path, x) L35
  "adds component to the registry of tracked components"
  apply_field(obj, field, condition=None, init_field=None) L40
  check_dropdown(val) L96
  check_tab_id(tab_id) L111
  add_block(self, x, path='') L124
  "adds all components inside a gradio block x to the registry of tracked components"
  read_from_file(self) L138
  write_to_file(self, current_ui_settings) L142
  dump_defaults(self) L146
  "saves default values to a file unless tjhe file is present and there was an error loading default values at start"
  iter_changes(self, current_ui_settings, values) L154
  "given a dictionary with defaults from a file and current values from gradio elements, returns"
  ui_view(self, *values) L181
  ui_apply(self, *values) L196
  create_ui(self) L212
  "creates ui elements for editing defaults UI, without adding any logic to them"
  setup_ui(self) L228
  "adds logic to elements created with create_ui; all add_block class must be made before this"

radio_choices(comp) L10

modules/ui_postprocessing.py
create_ui() L6

modules/ui_prompt_styles.py
class UiPromptStyles L53
  __init__(self, tabname, main_ui_prompt, main_ui_negative_prompt) L54
  setup_apply_button(self, button) L114

select_style(name) L10
save_style(name, prompt, negative_prompt) L21
delete_style(name) L32
materialize_styles(prompt, negative_prompt, styles) L42
refresh_styles() L49

modules/ui_settings.py
class UiSettings L57
  run_settings(self, *args) L71
  run_settings_single(self, value, key) L90
  create_ui(self, loadsave, dummy_component) L101
  call_func_and_return_text(func, text) L197
  handler() L198
  reload_scripts() L233
  check_file(x) L250
  calculate_all_checkpoint_hash_fn(max_thread) L265
  add_quicksettings(self) L282
  add_functionality(self, demo) L288
  get_settings_values() L324
  search(self, text) L334

get_value_for_setting(key) L11
create_setting_component(key, is_quicksettings=False) L21
fun() L22

modules/ui_tempdir.py
register_tmp_file(gradio, filename) L16
check_tmp_file(gradio, filename) L24
save_pil_to_file(self, pil_image, dir=None, format='png') L34
install_ui_tempdir_override() L62
"override save to file function so that it also writes PNG info"
on_tmpdir_changed() L67
cleanup_tmpdr() L76

modules/ui_toprow.py
class Toprow L9
  "Creates a top row UI with prompts, generate button, styles, extra little buttons for things, and enables some functionality related to their operation"
  __init__(self, is_img2img, is_compact=False, id_part=None) L37
  create_classic_toprow(self) L51
  create_inline_toprow_prompts(self) L61
  create_inline_toprow_image(self) L73
  create_prompts(self) L79
  create_submit_box(self) L95
  create_tools_row(self) L115
  create_styles_ui(self) L141

modules/upscaler.py
class Upscaler L14
  __init__(self, create_dirs=False) L26
  do_upscale(self, img, selected_model) L51
  upscale(self, img, scale, selected_model=None) L54
  load_model(self, path) L76
  find_models(self, ext_filter=None) L79
  update_status(self, prompt) L82

class UpscalerData L86
  __init__(self, name, path, upscaler=None, scale=4, model=None) L93

class UpscalerNone(Upscaler) L102
  load_model(self, path) L106
  do_upscale(self, img, selected_model=None) L109
  __init__(self, dirname=None) L112

class UpscalerLanczos(Upscaler) L117
  do_upscale(self, img, selected_model=None) L120
  load_model(self, _) L123
  __init__(self, dirname=None) L126

class UpscalerNearest(Upscaler) L132
  do_upscale(self, img, selected_model=None) L135
  load_model(self, _) L138
  __init__(self, dirname=None) L141

modules/util.py
natural_sort_key(s, regex=re.compile('([0-9]+)')) L8
listfiles(dirname) L12
html_path(filename) L17
html(filename) L21
walk_files(path, allowed_extensions=None) L31
ldm_print(*args, **kwargs) L54

modules/xlmr.py
class BertSeriesConfig(BertConfig) L8
  __init__(self, vocab_size=30522, hidden_size=768, num_hidden_layers=12, num_attention_heads=12, intermediate_size=3072, hidden_act='gelu', hidden_dropout_prob=0.1, attention_probs_dropout_prob=0.1, max_position_embeddings=512, type_vocab_size=2, initializer_range=0.02, layer_norm_eps=1e-12, pad_token_id=0, position_embedding_type='absolute', use_cache=True, classifier_dropout=None, project_dim=512, pooler_fn='average', learn_encoder=False, model_type='bert', **kwargs) L9

class RobertaSeriesConfig(XLMRobertaConfig) L16
  __init__(self, pad_token_id=1, bos_token_id=0, eos_token_id=2, project_dim=512, pooler_fn='cls', learn_encoder=False, **kwargs) L17

class BertSeriesModelWithTransformation(BertPreTrainedModel) L24
  __init__(self, config=None, **kargs) L30
  encode(self, c) L64
  forward(self, input_ids=None, attention_mask=None, token_type_ids=None, position_ids=None, head_mask=None, inputs_embeds=None, encoder_hidden_states=None, encoder_attention_mask=None, output_attentions=None, return_dict=None, output_hidden_states=None) L79

class RobertaSeriesModelWithTransformation(BertSeriesModelWithTransformation) L135

modules/xlmr_m18.py
class BertSeriesConfig(BertConfig) L8
  __init__(self, vocab_size=30522, hidden_size=768, num_hidden_layers=12, num_attention_heads=12, intermediate_size=3072, hidden_act='gelu', hidden_dropout_prob=0.1, attention_probs_dropout_prob=0.1, max_position_embeddings=512, type_vocab_size=2, initializer_range=0.02, layer_norm_eps=1e-12, pad_token_id=0, position_embedding_type='absolute', use_cache=True, classifier_dropout=None, project_dim=512, pooler_fn='average', learn_encoder=False, model_type='bert', **kwargs) L9

class RobertaSeriesConfig(XLMRobertaConfig) L16
  __init__(self, pad_token_id=1, bos_token_id=0, eos_token_id=2, project_dim=512, pooler_fn='cls', learn_encoder=False, **kwargs) L17

class BertSeriesModelWithTransformation(BertPreTrainedModel) L24
  __init__(self, config=None, **kargs) L30
  encode(self, c) L70
  forward(self, input_ids=None, attention_mask=None, token_type_ids=None, position_ids=None, head_mask=None, inputs_embeds=None, encoder_hidden_states=None, encoder_attention_mask=None, output_attentions=None, return_dict=None, output_hidden_states=None) L85

class RobertaSeriesModelWithTransformation(BertSeriesModelWithTransformation) L162

modules/xpu_specific.py
check_for_xpu() L13
get_xpu_device_string() L17
torch_xpu_gc() L23
