modules/api/api.py
class Api L165
  __init__(self, app, queue_lock) L166
  add_api_route(self, path, endpoint, **kwargs) L220
  auth(self, credentials=Depends(HTTPBasic())) L225
  get_selectable_script(self, script_name, script_runner) L232
  get_scripts_list(self) L240
  get_script_info(self) L246
  get_script(self, script_name, script_runner) L254
  init_default_script_args(self, script_runner) L261
  init_script_args(self, request, default_script_args, selectable_scripts, selectable_idx, script_runner) L281
  text2imgapi(self, txt2imgreq) L304
  img2imgapi(self, img2imgreq) L353
  extras_single_image_api(self, req) L417
  extras_batch_images_api(self, req) L427
  pnginfoapi(self, req) L438
  progressapi(self, req=Depends()) L454
  interrogateapi(self, interrogatereq) L482
  interruptapi(self) L501
  unloadapi(self) L506
  reloadapi(self) L511
  skip(self) L516
  get_config(self) L519
  set_config(self, req) L530
  get_cmd_flags(self) L541
  get_samplers(self) L544
  get_upscalers(self) L547
  get_latent_upscale_modes(self) L559
  get_sd_models(self) L567
  get_sd_vaes(self) L570
  get_hypernetworks(self) L573
  get_face_restorers(self) L576
  get_realesrgan_models(self) L579
  get_prompt_styles(self) L582
  get_embeddings(self) L590
  convert_embedding(embedding) L593
  convert_embeddings(embeddings) L602
  refresh_checkpoints(self) L610
  refresh_vae(self) L614
  create_embedding(self, args) L618
  create_hypernetwork(self, args) L630
  preprocess(self, args) L640
  train_embedding(self, args) L653
  train_hypernetwork(self, args) L674
  get_memory(self) L699
  launch(self, server_name, port, root_path) L734
  kill_webui(self) L738
  restart_webui(self) L741
  stop_webui(request) L746

script_name_to_index(name, scripts) L37
validate_sampler_name(name) L44
setUpscalers(req) L52
decode_base64_to_image(encoding) L59
encode_pil_to_base64(image) L69
api_middleware(app) L101
log_and_time(req, call_next) L114
handle_exception(request, e) L133
exception_handling(request, call_next) L150
fastapi_exception_handler(request, e) L157
http_exception_handler(request, e) L161

modules/api/models.py
class ModelDef(BaseModel) L29
  "Assistance Class for Pydantic Dynamic Model Generation"

class PydanticModelGenerator L39
  "Takes in created classes and stubs them out in a way FastAPI/Pydantic is happy a..."
  __init__(self, model_name=None, class_instance=None, additional_fields=None) L46
  field_type_generator(k, v) L52
  merge_class_params(class_) L59
  generate_model(self) L88
  "Creates a pydantic BaseModel"

class TextToImageResponse(BaseModel) L131

class ImageToImageResponse(BaseModel) L136

class ExtrasBaseRequest(BaseModel) L141

class ExtraBaseResponse(BaseModel) L156

class ExtrasSingleImageRequest(ExtrasBaseRequest) L159

class ExtrasSingleImageResponse(ExtraBaseResponse) L162

class FileData(BaseModel) L165

class ExtrasBatchImagesRequest(ExtrasBaseRequest) L169

class ExtrasBatchImagesResponse(ExtraBaseResponse) L172

class PNGInfoRequest(BaseModel) L175

class PNGInfoResponse(BaseModel) L178

class ProgressRequest(BaseModel) L182

class ProgressResponse(BaseModel) L185

class InterrogateRequest(BaseModel) L192

class InterrogateResponse(BaseModel) L196

class TrainResponse(BaseModel) L199

class CreateResponse(BaseModel) L202

class PreprocessResponse(BaseModel) L205

class SamplerItem(BaseModel) L232

class UpscalerItem(BaseModel) L237

class LatentUpscalerModeItem(BaseModel) L244

class SDModelItem(BaseModel) L247

class SDVaeItem(BaseModel) L255

class HypernetworkItem(BaseModel) L259

class FaceRestorerItem(BaseModel) L263

class RealesrganItem(BaseModel) L267

class PromptStyleItem(BaseModel) L272

class EmbeddingItem(BaseModel) L278

class EmbeddingsResponse(BaseModel) L285

class MemoryResponse(BaseModel) L289

class ScriptsList(BaseModel) L294

class ScriptArg(BaseModel) L299

class ScriptInfo(BaseModel) L308

modules/cache.py
dump_cache() L17
"Marks cache for writing to disk. 5 seconds after no one else flags the cache for..."
thread_func() L25
cache(subsection) L46
"Retrieves or initializes a cache for a specific subsection."
cached_data_for_file(subsection, title, filename, func) L79
"Retrieves or generates data for a specific file, using a caching mechanism."

modules/call_queue.py
wrap_queued_call(func) L11
f(*args, **kwargs) L12
wrap_gradio_gpu_call(func, extra_outputs=None) L21
f(*args, **kwargs) L23
wrap_gradio_call(func, extra_outputs=None, add_stats=False) L49
f(*args, extra_outputs_array=extra_outputs, **kwargs) L51

modules/codeformer/codeformer_arch.py
class PositionEmbeddingSine(nn.Module) L46
  "This is a more standard version of the position embedding, very similar to the o..."
  __init__(self, num_pos_feats=64, temperature=10000, normalize=False, scale=None) L52
  forward(self, x, mask=None) L63

class TransformerSALayer(nn.Module) L99
  __init__(self, embed_dim, nhead=8, dim_mlp=2048, dropout=0.0, activation='gelu') L100
  with_pos_embed(self, tensor, pos) L115
  forward(self, tgt, tgt_mask=None, tgt_key_padding_mask=None, query_pos=None) L118

class Fuse_sft_block(nn.Module) L136
  __init__(self, in_ch, out_ch) L137
  forward(self, enc_feat, dec_feat, w=1) L151

class CodeFormer(VQAutoEncoder) L161
  __init__(self, dim_embd=512, n_head=8, n_layers=9, codebook_size=1024, latent_size=256, connect_list=('32', '64', '128', '256'), fix_modules=('quantize', 'generator')) L162
  _init_weights(self, module) L210
  forward(self, x, w=0, detach_16=True, code_only=False, adain=False) L219

calc_mean_std(feat, eps=1e-05) L12
"Calculate mean and std for adaptive_instance_normalization."
adaptive_instance_normalization(content_feat, style_feat) L29
"Adaptive instance normalization."
_get_activation_fn(activation) L88
"Return an activation function given a string"

modules/codeformer/vqgan_arch.py
class VectorQuantizer(nn.Module) L24
  __init__(self, codebook_size, emb_dim, beta) L25
  forward(self, z) L33
  get_codebook_feat(self, indices, shape) L73

class GumbelQuantizer(nn.Module) L88
  __init__(self, codebook_size, emb_dim, num_hiddens, straight_through=False, kl_weight=0.0005, temp_init=1.0) L89
  forward(self, z) L99

class Downsample(nn.Module) L118
  __init__(self, in_channels) L119
  forward(self, x) L123

class Upsample(nn.Module) L130
  __init__(self, in_channels) L131
  forward(self, x) L135

class ResBlock(nn.Module) L142
  __init__(self, in_channels, out_channels=None) L143
  forward(self, x_in) L154

class AttnBlock(nn.Module) L168
  __init__(self, in_channels) L169
  forward(self, x) L203

class Encoder(nn.Module) L230
  __init__(self, in_channels, nf, emb_dim, ch_mult, num_res_blocks, resolution, attn_resolutions) L231
  forward(self, x) L270

class Generator(nn.Module) L277
  __init__(self, nf, emb_dim, ch_mult, res_blocks, img_size, attn_resolutions) L278
  forward(self, x) L320

class VQAutoEncoder(nn.Module) L328
  __init__(self, img_size, nf, ch_mult, quantizer='nearest', res_blocks=2, attn_resolutions=None, codebook_size=1024, emb_dim=256, beta=0.25, gumbel_straight_through=False, gumbel_kl_weight=1e-08, model_path=None) L329
  forward(self, x) L386

class VQGANDiscriminator(nn.Module) L396
  __init__(self, nc=3, ndf=64, n_layers=4, model_path=None) L397
  forward(self, x) L434

normalize(in_channels) L14
swish(x) L19

modules/codeformer_model.py
class FaceRestorerCodeFormer(modules.face_restoration.FaceRestoration) L37
  name(self) L38
  __init__(self, dirname) L41
  create_models(self) L46
  send_model_to(self, device) L71
  restore(self, np_image, w=None) L76

setup_model(dirname) L21

modules/config_states.py
list_config_states() L21
get_webui_config() L47
get_extension_config() L79
get_config() L102
restore_webui_config(config) L114
restore_extension_config(config) L145

modules/deepbooru.py
class DeepDanbooru L12
  __init__(self) L13
  load(self) L16
  start(self) L33
  stop(self) L37
  tag(self, pil_image) L42
  tag_multi(self, pil_image, force_disable_ranks=False) L49

modules/deepbooru_model.py
class DeepDanbooruModel(nn.Module) L10
  __init__(self) L11
  forward(self, *inputs) L197
  load_state_dict(self, state_dict, **kwargs) L674

modules/devices.py
class NansException(Exception) L109

has_mps() L12
get_cuda_device_string() L19
get_optimal_device_name() L26
get_optimal_device() L36
get_device_for(task) L40
torch_gc() L47
enable_tf32() L58
cond_cast_unet(input) L84
cond_cast_float(input) L88
autocast(disable=False) L95
without_autocast(disable=False) L105
test_for_nans(x, where) L113
first_time_calculation() L140
"just do any calculation with pytorch layers - the first time this is done it all..."

modules/errors.py
record_exception() L9
report(message, *, exc_info=False) L24
"Print an error message to stderr, with optional traceback."
print_error_explanation(message) L38
display(e, task, *, full_traceback=False) L50
display_once(e, task) L71
run(code, task) L82
check_versions() L89

modules/esrgan_model.py
class UpscalerESRGAN(Upscaler) L121
  __init__(self, dirname) L122
  do_upscale(self, img, selected_model) L143
  load_model(self, path) L153

mod2normal(state_dict) L13
resrgan2normal(state_dict, nb=23) L46
infer_params(state_dict) L89
upscale_without_tiling(model, img) L193
esrgan_upscale(model, img) L208

modules/esrgan_model_arch.py
class RRDBNet(nn.Module) L14
  __init__(self, in_nc, out_nc, nf, nb, nr=3, gc=32, upscale=4, norm_type=None, act_type='leakyrelu', mode='CNA', upsample_mode='upconv', convtype='Conv2D', finalact=None, gaussian_noise=False, plus=False) L15
  forward(self, x, outm=None) L53

class RRDB(nn.Module) L64
  "Residual in Residual Dense Block"
  __init__(self, nf, nr=3, kernel_size=3, gc=32, stride=1, bias=1, pad_type='zero', norm_type=None, act_type='leakyrelu', mode='CNA', convtype='Conv2D', spectral_norm=False, gaussian_noise=False, plus=False) L70
  forward(self, x) L91

class ResidualDenseBlock_5C(nn.Module) L101
  "Residual Dense Block"
  __init__(self, nf=64, kernel_size=3, gc=32, stride=1, bias=1, pad_type='zero', norm_type=None, act_type='leakyrelu', mode='CNA', convtype='Conv2D', spectral_norm=False, gaussian_noise=False, plus=False) L112
  forward(self, x) L140

class GaussianNoise(nn.Module) L160
  __init__(self, sigma=0.1, is_relative_detach=False) L161
  forward(self, x) L167

class SRVGGNetCompact(nn.Module) L183
  "A compact VGG-style network structure for super-resolution."
  __init__(self, num_in_ch=3, num_out_ch=3, num_feat=64, num_conv=16, upscale=4, act_type='prelu') L188
  forward(self, x) L226

class Upsample(nn.Module) L242
  "Upsamples a given multi-channel 1D (temporal), 2D (spatial) or 3D (volumetric) d..."
  __init__(self, size=None, scale_factor=None, mode='nearest', align_corners=None) L248
  forward(self, x) L258
  extra_repr(self) L261

class Identity(nn.Module) L356
  __init__(self, *kwargs) L357
  forward(self, x, *kwargs) L360

class ShortcutBlock(nn.Module) L400
  "Elementwise sum the output of a submodule to its input"
  __init__(self, submodule) L402
  forward(self, x) L406
  __repr__(self) L410

conv1x1(in_planes, out_planes, stride=1) L175
pixel_unshuffle(x, scale) L270
"Pixel unshuffle."
pixelshuffle_block(in_nc, out_nc, upscale_factor=2, kernel_size=3, stride=1, bias=True, pad_type='zero', norm_type=None, act_type='relu', convtype='Conv2D') L287
"Pixel shuffle layer"
upconv_block(in_nc, out_nc, upscale_factor=2, kernel_size=3, stride=1, bias=True, pad_type='zero', norm_type=None, act_type='relu', mode='nearest', convtype='Conv2D') L303
"Upconv layer"
make_layer(basic_block, num_basic_block, **kwarg) L324
"Make layers by stacking the same blocks."
act(act_type, inplace=True, neg_slope=0.2, n_prelu=1, beta=1.0) L338
"activation helper"
norm(norm_type, nc) L364
"Return a normalization layer"
norm_layer(x) L372
pad(pad_type, padding) L378
"padding layer helper"
get_valid_padding(kernel_size, dilation) L394
sequential(*args) L414
"Flatten Sequential. It unwraps nn.Sequential."
conv_block(in_nc, out_nc, kernel_size, stride=1, dilation=1, groups=1, bias=True, pad_type='zero', norm_type=None, act_type='relu', mode='CNA', convtype='Conv2D', spectral_norm=False) L430
"Conv layer with padding, normalization, activation"

modules/extensions.py
class Extension L22
  __init__(self, name, path, enabled=True, is_builtin=False) L26
  to_dict(self) L40
  from_dict(self, d) L43
  read_info_from_repo(self) L47
  read_from_repo() L51
  do_read_info_from_repo(self) L66
  list_files(self, subdir, extension) L92
  check_updates(self) L105
  fetch_and_reset_hard(self, commit='origin') L127

active() L13
list_extensions() L136

modules/extra_networks.py
class ExtraNetworkParams L30
  __init__(self, items=None) L31
  __eq__(self, other) L43

class ExtraNetwork L47
  __init__(self, name) L48
  activate(self, p, params_list) L51
  "Called by processing on every run. Whatever the extra network is meant to do sho..."
  deactivate(self, p) L81
  "Called at the end of processing for housekeeping. No need to do anything here."

initialize() L12
register_extra_network(extra_network) L17
register_extra_network_alias(extra_network, alias) L21
register_default_extra_networks() L25
activate(p, extra_network_data) L89
"call activate for extra networks in extra_network_data in specified order, then ..."
deactivate(p, extra_network_data) L124
"call deactivate for extra networks in extra_network_data in specified order, the..."
parse_prompt(prompt) L152
found(m) L155
parse_prompts(prompts) L168
get_user_metadata(filename) L183

modules/extra_networks_hypernet.py
class ExtraNetworkHypernet(extra_networks.ExtraNetwork) L5
  __init__(self) L6
  activate(self, p, params_list) L9
  deactivate(self, p) L27

modules/extras.py
run_pnginfo(image) L16
create_config(ckpt_result, config_source, a, b, c) L39
config(x) L40
to_half(tensor, enable) L68
read_metadata(primary_model_name, secondary_model_name, tertiary_model_name) L75
run_modelmerger(id_task, primary_model_name, secondary_model_name, tertiary_model_name, interp_method, multiplier, save_as_half, custom_name, checkpoint_format, config_source, bake_in_vae, discard_weights, save_metadata, add_merge_recipe, copy_metadata_fields, metadata_json) L88
fail(message) L91
weighted_sum(theta0, theta1, alpha) L96
get_difference(theta1, theta2) L99
add_difference(theta0, theta1_2_diff, alpha) L102
filename_weighted_sum() L105
filename_add_difference() L113
filename_nothing() L121
add_model_metadata(checkpoint_info) L294

modules/face_restoration.py
class FaceRestoration L4
  name(self) L5
  restore(self, np_image) L8

restore_faces(np_image) L12

modules/generation_parameters_copypaste.py
class ParamBinding L22
  __init__(self, paste_button, tabname, source_text_component=None, source_image_component=None, source_tabname=None, override_settings_component=None, paste_field_names=None) L23

reset() L33
quote(text) L37
unquote(text) L44
image_from_url_text(filedata) L54
add_paste_fields(tabname, init_img, fields, override_settings_component=None) L83
create_buttons(tabs_list) L94
bind_buttons(buttons, send_image, send_generate_info) L101
"old function for backwards compatibility; do not use this, use register_paste_pa..."
register_paste_params_button(binding) L110
connect_paste_params_buttons() L114
send_image_and_dimensions(x) L161
restore_old_hires_fix_params(res) L177
"for infotexts that specify old First pass size parameter, convert it into"
parse_generation_parameters(x) L209
"parses generation parameters string, the one you see in text field under the pic..."
create_override_settings_dict(text_pairs) L333
"creates processing's override_settings parameters from gradio's multiselect"
connect_paste(button, paste_fields, input_comp, override_settings_component, tabname) L363
paste_func(prompt) L364
paste_settings(params) L403

modules/gfpgan_model.py
class FaceRestorerGFPGAN(modules.face_restoration.FaceRestoration) L101
  name(self) L102
  restore(self, np_image) L105

gfpgann() L17
send_model_to(model, device) L44
gfpgan_fix_faces(np_image) L50
setup_model(dirname) L72
my_load_file_from_url(**kwargs) L85
facex_load_file_from_url(**kwargs) L88
facex_load_file_from_url2(**kwargs) L91

modules/gitpython_hack.py
class Git(git.Git) L9
  "Git subclassed to never use persistent processes."
  _get_persistent_cmd(self, attr_name, cmd_name, *args, **kwargs) L14
  get_object_header(self, ref) L17
  stream_object_data(self, ref) L26

class Repo(git.Repo) L41

modules/gradio_extensons.py
add_classes_to_gradio_component(comp) L5
"this adds gradio-* to the component for css styling (ie gradio-button to gr.Butt..."
IOComponent_init(self, *args, **kwargs) L16
Block_get_config(self) L36
BlockContext_init(self, *args, **kwargs) L46

modules/hashes.py
calculate_sha256(filename) L11
sha256_from_cache(filename, title, use_addnet_hash=False) L22
sha256(filename, title, use_addnet_hash=False) L38
addnet_hash_safetensors(b) L66
"kohya-ss hash for safetensors from https://github.com/kohya-ss/sd-scripts/blob/m..."

modules/hypernetworks/hypernetwork.py
class HypernetworkModule(torch.nn.Module) L25
  __init__(self, dim, state_dict=None, layer_structure=None, activation_func=None, weight_init='Normal', add_layer_norm=False, activate_output=False, dropout_structure=None) L37
  fix_old_state_dict(self, state_dict) L100
  forward(self, x) L116
  trainables(self) L119

class Hypernetwork L143
  __init__(self, name=None, enable_sizes=None, layer_structure=None, activation_func=None, weight_init=None, add_layer_norm=False, use_dropout=False, activate_output=False, **kwargs) L147
  weights(self) L177
  train(self, mode=True) L184
  to(self, device) L191
  set_multiplier(self, multiplier) L198
  eval(self) L205
  save(self, filename) L212
  load(self, filename) L242
  shorthash(self) L305

parse_dropout_structure(layer_structure, use_dropout, last_layer_dropout) L128
list_hypernetworks(path) L311
load_hypernetwork(name) L321
load_hypernetworks(names, multipliers=None) L336
apply_single_hypernetwork(hypernetwork, context_k, context_v, layer=None) L357
apply_hypernetworks(hypernetworks, context, layer=None) L372
attention_CrossAttention_forward(self, x, context=None, mask=None, **kwargs) L381
stack_conds(conds) L409
statistics(data) L424
create_hypernetwork(name, enable_sizes, overwrite_old, layer_structure=None, activation_func=None, weight_init=None, add_layer_norm=False, use_dropout=False, dropout_structure=None) L439
train_hypernetwork(id_task, hypernetwork_name, learn_rate, batch_size, gradient_step, data_root, log_directory, training_width, training_height, varsize, steps, clip_grad_mode, clip_grad_value, shuffle_tags, tag_drop_out, latent_sampling_method, use_weight, create_image_every, save_hypernetwork_every, template_filename, preview_from_txt2img, preview_prompt, preview_negative_prompt, preview_steps, preview_sampler_index, preview_cfg_scale, preview_seed, preview_width, preview_height) L471
save_hypernetwork(hypernetwork, checkpoint, hypernetwork_name, filename) L769

modules/hypernetworks/ui.py
create_hypernetwork(name, enable_sizes, overwrite_old, layer_structure=None, activation_func=None, weight_init=None, add_layer_norm=False, use_dropout=False, dropout_structure=None) L11
train_hypernetwork(*args) L17

modules/images.py
class GridAnnotation L134
  __init__(self, text='', is_active=True) L135

class FilenameGenerator L342
  __init__(self, p, seed, prompt, image, zip=False) L375
  get_vae_filename(self) L382
  "Get the name of the VAE file."
  hasprompt(self, *args) L398
  prompt_no_style(self) L414
  prompt_words(self) L428
  datetime(self, *args) L434
  apply(self, x) L451

get_font(fontsize) L27
image_grid(imgs, batch_size=1, rows=None) L34
split_grid(image, tile_w=512, tile_h=512, overlap=64) L67
combine_grid(grid) L104
make_mask_image(r) L105
draw_grid_annotations(im, width, height, hor_texts, ver_texts, margin=0) L141
wrap(drawing, text, font, line_length) L147
draw_texts(drawing, draw_x, draw_y, lines, initial_fnt, initial_fontsize) L157
draw_prompt_matrix(im, width, height, all_prompts, margin=0) L229
resize_image(resize_mode, im, width, height, upscaler_name=None) L242
"Resizes an image with the specified resize_mode, width, and height."
resize(im, w, h) L259
sanitize_filename_part(text, replace_spaces=True) L329
get_next_sequence_number(path, basename) L489
"Determines and returns the next sequence number to use when saving an image in t..."
save_image_with_geninfo(image, geninfo, filename, extension=None, existing_pnginfo=None, pnginfo_section_name='parameters') L511
"Saves image to filename, including geninfo as text information for generation in..."
save_image(image, path, basename, seed=None, prompt=None, extension='png', info=None, short_filename=False, no_prompt=False, grid=False, pnginfo_section_name='parameters', p=None, existing_info=None, forced_filename=None, suffix='', save_to_dirs=None) L557
"Save an image."
_atomically_save_image(image_to_save, filename_without_extension, extension) L640
"save image with .tmp extension to avoid race condition when another process dete..."
read_info_from_image(image) L699
image_data(data) L733
flatten(img, bgcolor) L754
"replaces transparency with bgcolor (example: "#ffffff"), returning an RGB mode i..."

modules/img2img.py
process_batch(p, input_dir, output_dir, inpaint_mask_dir, args, to_scale=False, scale_by=1.0, use_png_info=False, png_info_props=None, png_info_dir=None) L19
img2img(id_task, mode, prompt, negative_prompt, prompt_styles, init_img, sketch, init_img_with_mask, inpaint_color_sketch, inpaint_color_sketch_orig, init_img_inpaint, init_mask_inpaint, steps, sampler_name, mask_blur, mask_alpha, inpainting_fill, n_iter, batch_size, cfg_scale, image_cfg_scale, denoising_strength, seed, subseed, subseed_strength, seed_resize_from_h, seed_resize_from_w, seed_enable_extras, selected_scale_tab, height, width, scale_by, resize_mode, inpaint_full_res, inpaint_full_res_padding, inpainting_mask_invert, img2img_batch_input_dir, img2img_batch_output_dir, img2img_batch_inpaint_mask_dir, override_settings_texts, img2img_batch_use_png_info, img2img_batch_png_info_props, img2img_batch_png_info_dir, request, *args) L119

modules/initialize.py
imports() L10
check_versions() L41
initialize() L49
initialize_rest(*, reload_script_modules=False) L78
"Called both from initialize() and when reloading the webui."
load_model() L139
"Accesses shared.sd_model property to load model."

modules/initialize_util.py
class AnyThreadEventLoopPolicy(_BasePolicy) L47
  "Event loop policy that allows loop creation on any thread."
  get_event_loop(self) L54

gradio_server_name() L10
fix_torch_version() L19
fix_asyncio_event_loop_policy() L28
"The default `asyncio` event loop policy only automatically creates"
restore_config_state_file() L68
validate_tls_options() L88
get_gradio_auth_creds() L107
"Convert the gradio_auth and gradio_auth_path commandline arguments into"
process_credential_line(s) L114
configure_sigint_handler() L135
sigint_handler(sig, frame) L137
configure_opts_onchange() L147
setup_middleware(app) L160
configure_cors_middleware(app) L169

modules/interrogate.py
class InterrogateModels L45
  __init__(self, content_dir) L52
  categories(self) L58
  create_fake_fairscale(self) L83
  load_blip_model(self) L90
  load_clip_model(self) L106
  load(self) L119
  send_clip_to_ram(self) L136
  send_blip_to_ram(self) L141
  unload(self) L146
  rank(self, image_features, text_array, top_count=1) L152
  generate_caption(self, pil_image) L173
  interrogate(self, pil_image) L185

class FakeFairscale L84
  checkpoint_wrapper(self) L85

category_types() L22
download_default_clip_interrogate_categories(content_dir) L26

modules/launch_utils.py
check_python_version() L33
commit_hash() L65
git_tag() L73
run(command, desc=None, errdesc=None, custom_env=None, live=default_command_live) L88
is_installed(package) L120
repo_dir(name) L129
run_pip(command, desc=None, live=default_command_live) L133
check_run_python(code) L141
git_fix_workspace(dir, name) L146
run_git(dir, name, command, desc=None, errdesc=None, custom_env=None, live=default_command_live, autofix=True) L152
git_clone(url, dir, name, commithash=None) L165
git_pull_recursive(dir) L192
version_check(commit) L202
run_extension_installer(extension_dir) L219
list_extensions(settings_file) L233
run_extensions_installers(settings_file) L252
requirements_met(requirements_file) L270
"Does a simple parse of a requirements.txt file to determine if all rerqirements ..."
prepare_environment() L305
configure_for_tests() L420
start() L434

modules/localization.py
list_localizations(dirname) L9
localization_js(current_localization_name) L24

modules/logging_config.py
setup_logging(loglevel) L5

modules/lowvram.py
send_everything_to_cpu() L8
setup_for_low_vram(sd_model, use_medvram) L17
send_me_to_gpu(module, _) L25
"send this module to GPU; send whatever tracked module was previous in GPU to CPU..."
first_stage_model_encode_wrap(x) L51
first_stage_model_decode_wrap(z) L55
is_enabled(sd_model) L132

modules/mac_specific.py
check_for_mps() L16
torch_mps_gc() L32
cumsum_fix(input, cumsum_func, *args, **kwargs) L44

modules/masking.py
get_crop_region(mask, pad=0) L4
"finds a rectangular region that contains all masked ares in an image. Returns (x..."
expand_crop_region(crop_region, processing_width, processing_height, image_width, image_height) L42
"expands crop region get_crop_region() to match the ratio of the image the region..."
fill(image, mask) L83
"fills masked regions with colors from image using blur. Not extremely effective."

modules/memmon.py
class MemUsageMonitor(threading.Thread) L8
  __init__(self, name, device, opts) L15
  cuda_mem_get_info(self) L32
  run(self) L36
  dump_debug(self) L58
  monitor(self) L72
  read(self) L75
  stop(self) L90

modules/modelloader.py
load_file_from_url(url, *, model_dir, progress=True, file_name=None) L13
"Download a file from `url` into `model_dir`, using the file present if possible."
load_models(model_path, model_url=None, command_path=None, ext_filter=None, download_name=None, ext_blacklist=None) L36
"A one-and done loader to try finding the desired models in specified directories..."
friendly_name(file) L84
cleanup_models() L93
move_files(src_path, dest_path, ext_filter=None) L119
load_upscalers() L141

modules/models/diffusion/ddpm_edit.py
class DDPM(pl.LightningModule) L47
  __init__(self, unet_config, timesteps=1000, beta_schedule='linear', loss_type='l2', ckpt_path=None, ignore_keys=None, load_only_unet=False, monitor='val/loss', use_ema=True, first_stage_key='image', image_size=256, channels=3, log_every_t=100, clip_denoised=True, linear_start=0.0001, linear_end=0.02, cosine_s=0.008, given_betas=None, original_elbo_weight=0.0, v_posterior=0.0, l_simple_weight=1.0, conditioning_key=None, parameterization='eps', scheduler_config=None, use_positional_encodings=False, learn_logvar=False, logvar_init=0.0, load_ema=True) L49
  register_schedule(self, given_betas=None, beta_schedule='linear', timesteps=1000, linear_start=0.0001, linear_end=0.02, cosine_s=0.008) L128
  ema_scope(self, context=None) L183
  init_from_ckpt(self, path, ignore_keys=None, only_model=False) L197
  q_mean_variance(self, x_start, t) L238
  "Get the distribution q(x_t | x_0)."
  predict_start_from_noise(self, x_t, t, noise) L250
  q_posterior(self, x_start, x_t, t) L256
  p_mean_variance(self, x, t, clip_denoised) L265
  p_sample(self, x, t, clip_denoised=True, repeat_noise=False) L278
  p_sample_loop(self, shape, return_intermediates=False) L287
  sample(self, batch_size=16, return_intermediates=False) L302
  q_sample(self, x_start, t, noise=None) L308
  get_loss(self, pred, target, mean=True) L313
  p_losses(self, x_start, t, noise=None) L328
  forward(self, x, *args, **kwargs) L357
  get_input(self, batch, k) L363
  shared_step(self, batch) L366
  training_step(self, batch, batch_idx) L371
  validation_step(self, batch, batch_idx) L387
  on_train_batch_end(self, *args, **kwargs) L395
  _get_rows_from_list(self, samples) L399
  log_images(self, batch, N=8, n_row=2, sample=True, return_keys=None, **kwargs) L407
  configure_optimizers(self) L444

class LatentDiffusion(DDPM) L453
  "main class"
  __init__(self, first_stage_config, cond_stage_config, num_timesteps_cond=None, cond_stage_key='image', cond_stage_trainable=False, concat_mode=True, cond_stage_forward=None, conditioning_key=None, scale_factor=1.0, scale_by_std=False, load_ema=True, *args, **kwargs) L455
  make_cond_schedule(self) L505
  on_train_batch_start(self, batch, batch_idx, dataloader_idx) L512
  register_schedule(self, given_betas=None, beta_schedule='linear', timesteps=1000, linear_start=0.0001, linear_end=0.02, cosine_s=0.008) L527
  instantiate_first_stage(self, config) L536
  instantiate_cond_stage(self, config) L543
  _get_denoise_row_from_list(self, samples, desc='', force_no_decoder_quantization=False) L564
  get_first_stage_encoding(self, encoder_posterior) L576
  get_learned_conditioning(self, c) L585
  meshgrid(self, h, w) L598
  delta_border(self, h, w) L605
  ":param h: height"
  get_weighting(self, h, w, Ly, Lx, device) L619
  get_fold_unfold(self, x, kernel_size, stride, uf=1, df=1) L635
  ":param x: img of size (bs, c, h, w)"
  get_input(self, batch, k, return_first_stage_outputs=False, force_c_encode=False, cond_key=None, return_original_cond=False, bs=None, uncond=0.05) L688
  decode_first_stage(self, z, predict_cids=False, force_not_quantize=False) L721
  differentiable_decode_first_stage(self, z, predict_cids=False, force_not_quantize=False) L781
  encode_first_stage(self, x) L841
  shared_step(self, batch, **kwargs) L880
  forward(self, x, c, *args, **kwargs) L885
  apply_model(self, x_noisy, t, cond, return_ids=False) L896
  _predict_eps_from_xstart(self, x_t, t, pred_xstart) L999
  _prior_bpd(self, x_start) L1003
  "Get the prior KL term for the variational lower-bound, measured in"
  p_losses(self, x_start, cond, t, noise=None) L1017
  p_mean_variance(self, x, c, t, clip_denoised, return_codebook_ids=False, quantize_denoised=False, return_x0=False, score_corrector=None, corrector_kwargs=None) L1052
  p_sample(self, x, c, t, clip_denoised=False, repeat_noise=False, return_codebook_ids=False, quantize_denoised=False, return_x0=False, temperature=1.0, noise_dropout=0.0, score_corrector=None, corrector_kwargs=None) L1084
  progressive_denoising(self, cond, shape, verbose=True, callback=None, quantize_denoised=False, img_callback=None, mask=None, x0=None, temperature=1.0, noise_dropout=0.0, score_corrector=None, corrector_kwargs=None, batch_size=None, x_T=None, start_T=None, log_every_t=None) L1115
  p_sample_loop(self, cond, shape, return_intermediates=False, x_T=None, verbose=True, callback=None, timesteps=None, quantize_denoised=False, mask=None, x0=None, img_callback=None, start_T=None, log_every_t=None) L1173
  sample(self, cond, batch_size=16, return_intermediates=False, x_T=None, verbose=True, timesteps=None, quantize_denoised=False, mask=None, x0=None, shape=None, **kwargs) L1226
  sample_log(self, cond, batch_size, ddim, ddim_steps, **kwargs) L1244
  log_images(self, batch, N=4, n_row=4, sample=True, ddim_steps=200, ddim_eta=1.0, return_keys=None, quantize_denoised=True, inpaint=False, plot_denoise_rows=False, plot_progressive_rows=False, plot_diffusion_rows=False, **kwargs) L1260
  configure_optimizers(self) L1371
  to_rgb(self, x) L1396

class DiffusionWrapper(pl.LightningModule) L1405
  __init__(self, diff_model_config, conditioning_key) L1406
  forward(self, x, t, c_concat=None, c_crossattn=None) L1412

class Layout2ImgDiffusion(LatentDiffusion) L1434
  __init__(self, cond_stage_key, *args, **kwargs) L1436
  log_images(self, batch, N=8, *args, **kwargs) L1440

disabled_train(self, mode=True) L37
"Overwrite model.train with this function to make sure train/eval mode"
uniform_on_device(r1, r2, shape, device) L43

modules/models/diffusion/uni_pc/sampler.py
class UniPCSampler(object) L9
  __init__(self, model, **kwargs) L10
  register_buffer(self, name, attr) L18
  set_hooks(self, before_sample, after_sample, after_update) L24
  sample(self, S, batch_size, shape, conditioning=None, callback=None, normals_sequence=None, img_callback=None, quantize_x0=False, eta=0.0, mask=None, x0=None, temperature=1.0, noise_dropout=0.0, score_corrector=None, corrector_kwargs=None, verbose=True, x_T=None, log_every_t=100, unconditional_guidance_scale=1.0, unconditional_conditioning=None, **kwargs) L30

modules/models/diffusion/uni_pc/uni_pc.py
class NoiseScheduleVP L6
  __init__(self, schedule='discrete', betas=None, alphas_cumprod=None, continuous_beta_0=0.1, continuous_beta_1=20.0) L7
  "Create a wrapper class for the forward SDE (VP type)."
  marginal_log_mean_coeff(self, t) L125
  "Compute log(alpha_t) of a given continuous-time label t in [0, T]."
  marginal_alpha(self, t) L138
  "Compute alpha_t of a given continuous-time label t in [0, T]."
  marginal_std(self, t) L144
  "Compute sigma_t of a given continuous-time label t in [0, T]."
  marginal_lambda(self, t) L150
  "Compute lambda_t = log(alpha_t) - log(sigma_t) of a given continuous-time label ..."
  inverse_lambda(self, lamb) L158
  "Compute the continuous-time label t in [0, T] of a given half-logSNR lambda_t."

class UniPC L372
  __init__(self, model_fn, noise_schedule, predict_x0=True, thresholding=False, max_val=1.0, variant='bh1', condition=None, unconditional_condition=None, before_sample=None, after_sample=None, after_update=None) L373
  "Construct a UniPC."
  dynamic_thresholding_fn(self, x0, t=None) L403
  "The dynamic thresholding method."
  model(self, x, t) L414
  noise_prediction_fn(self, x, t) L429
  "Return the noise prediction model."
  data_prediction_fn(self, x, t) L435
  "Return the data prediction model (with thresholding)."
  model_fn(self, x, t) L450
  "Convert the model to the noise prediction model or the data prediction model."
  get_time_steps(self, skip_type, t_T, t_0, N, device) L459
  "Compute the intermediate time steps for sampling."
  get_orders_and_timesteps_for_singlestep_solver(self, steps, order, skip_type, t_T, t_0, device) L476
  "Get the order of each step for sampling by the singlestep DPM-Solver."
  denoise_to_zero_fn(self, x, s) L507
  "Denoise at the final step, which is equivalent to solve the ODE from lambda_s to..."
  multistep_uni_pc_update(self, x, model_prev_list, t_prev_list, t, order, **kwargs) L513
  multistep_uni_pc_vary_update(self, x, model_prev_list, t_prev_list, t, order, use_corrector=True) L522
  multistep_uni_pc_bh_update(self, x, model_prev_list, t_prev_list, t, order, x_t=None, use_corrector=True) L625
  sample(self, x, steps=20, t_start=None, t_end=None, order=3, skip_type='time_uniform', method='singlestep', lower_order_final=True, denoise_to_zero=False, solver_type='dpm_solver', atol=0.0078, rtol=0.05, corrector=False) L746

model_wrapper(model, noise_schedule, model_type='noise', model_kwargs=None, guidance_type='uncond', guidance_scale=1.0, classifier_fn=None, classifier_kwargs=None) L177
"Create a wrapper function for the noise prediction model."
get_model_input_time(t_continuous) L281
"Convert the continuous-time `t_continuous` (in [epsilon, T]) to the model input ..."
noise_pred_fn(x, t_continuous, cond=None) L292
cond_grad_fn(x, t_input, condition) L315
"Compute the gradient of the classifier, i.e. nabla_{x} log p_t(cond | x_t)."
model_fn(x, t_continuous, condition, unconditional_condition) L324
"The noise predicition model function that is used for DPM-Solver."
interpolate_fn(x, xp, yp) L811
"A piecewise linear function y = f(x), using xp and yp as keypoints."
expand_dims(v, dims) L853
"Expand the tensor `v` to the dim `dims`."

modules/ngrok.py
connect(token, port, options) L4

modules/options.py
class OptionInfo L10
  __init__(self, default=None, label='', component=None, component_args=None, onchange=None, section=None, refresh=None, comment_before='', comment_after='', infotext=None) L11
  link(self, label, url) L29
  js(self, label, js_func) L33
  info(self, info) L37
  html(self, html) L41
  needs_restart(self) L45
  needs_reload_ui(self) L49

class OptionHTML(OptionInfo) L54
  __init__(self, text) L55

class Options L71
  __init__(self, data_labels, restricted_opts) L74
  __setattr__(self, key, value) L79
  __getattr__(self, item) L103
  set(self, key, value) L116
  "sets an option and calls its onchange callback, returning True if the option cha..."
  get_default(self, key) L141
  "returns the default value for the key"
  save(self, filename) L150
  same_type(self, x, y) L156
  load(self, filename) L165
  onchange(self, key, func, call=True) L191
  dumpjson(self) L198
  add_option(self, key, info) L204
  reorder(self) L207
  "reorder settings so that all items related to section always go together"
  cast_value(self, key, value) L218
  "casts an arbitrary to the same type as this setting's value with key"

options_section(section_identifier, options_dict) L61

modules/paths.py
class Dummy L11

mute_sdxl_imports() L8
"create fake modules that SDXL wants to import but doesn't actually use for our p..."

modules/postprocessing.py
run_postprocessing(extras_mode, image, image_folder, input_dir, output_dir, show_extras_results, *args, save_output=True) L9
run_extras(extras_mode, resize_mode, image, image_folder, input_dir, output_dir, show_extras_results, gfpgan_visibility, codeformer_visibility, codeformer_weight, upscaling_resize, upscaling_resize_w, upscaling_resize_h, upscaling_crop, extras_upscaler_1, extras_upscaler_2, extras_upscaler_2_visibility, upscale_first, save_output=True) L86
"old handler for API"

modules/processing.py
class StableDiffusionProcessing L107
  "The first set of paramaters: sd_models -> do_not_reload_embeddings represent the..."
  __init__(self, sd_model=None, outpath_samples=None, outpath_grids=None, prompt='', styles=None, seed=-1, subseed=-1, subseed_strength=0, seed_resize_from_h=-1, seed_resize_from_w=-1, seed_enable_extras=True, sampler_name=None, batch_size=1, n_iter=1, steps=50, cfg_scale=7.0, width=512, height=512, restore_faces=None, tiling=None, do_not_save_samples=False, do_not_save_grid=False, extra_generation_params=None, overlay_images=None, negative_prompt=None, eta=None, do_not_reload_embeddings=False, denoising_strength=0, ddim_discretize=None, s_min_uncond=0.0, s_churn=0.0, s_tmax=None, s_tmin=0.0, s_noise=None, override_settings=None, override_settings_restore_afterwards=True, sampler_index=None, script_args=None) L114
  sd_model(self) L195
  txt2img_image_conditioning(self, x, width=None, height=None) L198
  depth2img_image_conditioning(self, source_image) L203
  edit_image_conditioning(self, source_image) L222
  unclip_image_conditioning(self, source_image) L227
  inpainting_image_conditioning(self, source_image, latent_image, image_mask=None) L235
  img2img_image_conditioning(self, source_image, latent_image, image_mask=None) L272
  init(self, all_prompts, all_seeds, all_subseeds) L292
  sample(self, conditioning, unconditional_conditioning, seeds, subseeds, subseed_strength, prompts) L295
  close(self) L298
  get_token_merging_ratio(self, for_hr=False) L306
  setup_prompts(self) L312
  cached_params(self, required_prompts, steps, extra_network_data) L329
  "Returns parameters that invalidate the cond cache if changed"
  get_conds_with_caching(self, function, required_prompts, steps, caches, extra_network_data) L344
  "Returns the result of calling function(shared.sd_model, required_prompts, steps)"
  setup_conds(self) L371
  get_conds(self) L380
  parse_extra_network_prompts(self) L383
  save_samples(self) L386
  "Returns whether generated images need to be written to disk"

class Processed L391
  __init__(self, p, images_list, seed=-1, info='', subseed=None, all_prompts=None, all_negative_prompts=None, all_seeds=None, all_subseeds=None, index_of_first_image=0, infotexts=None, comments='') L392
  js(self) L442
  infotext(self, p, index) L476
  get_token_merging_ratio(self, for_hr=False) L479

class DecodedSamples(list) L488

class StableDiffusionProcessingTxt2Img(StableDiffusionProcessing) L909
  __init__(self, enable_hr=False, denoising_strength=0.75, firstphase_width=0, firstphase_height=0, hr_scale=2.0, hr_upscaler=None, hr_second_pass_steps=0, hr_resize_x=0, hr_resize_y=0, hr_checkpoint_name=None, hr_sampler_name=None, hr_prompt='', hr_negative_prompt='', **kwargs) L914
  calculate_target_resolution(self) L953
  init(self, all_prompts, all_seeds, all_subseeds) L992
  sample(self, conditioning, unconditional_conditioning, seeds, subseeds, subseed_strength, prompts) L1032
  sample_hr_pass(self, samples, decoded_samples, seeds, subseeds, subseed_strength, prompts) L1060
  save_intermediate(image, index) L1066
  "saves image before applying hires fix, if enabled in options; takes as an argume..."
  close(self) L1151
  setup_prompts(self) L1159
  calculate_hr_conds(self) L1184
  setup_conds(self) L1194
  get_conds(self) L1213
  parse_extra_network_prompts(self) L1220

class StableDiffusionProcessingImg2Img(StableDiffusionProcessing) L1232
  __init__(self, init_images=None, resize_mode=0, denoising_strength=0.75, image_cfg_scale=None, mask=None, mask_blur=None, mask_blur_x=4, mask_blur_y=4, inpainting_fill=0, inpaint_full_res=True, inpaint_full_res_padding=0, inpainting_mask_invert=0, initial_noise_multiplier=None, **kwargs) L1235
  init(self, all_prompts, all_seeds, all_subseeds) L1260
  sample(self, conditioning, unconditional_conditioning, seeds, subseeds, subseed_strength, prompts) L1388
  get_token_merging_ratio(self, for_hr=False) L1405

setup_color_correction(image) L41
apply_color_correction(correction, original_image) L47
apply_overlay(image, paste_loc, index, overlays) L63
txt2img_image_conditioning(sd_model, x, width, height) L83
create_random_tensors(shape, seeds, subseeds=None, subseed_strength=0.0, seed_resize_from_h=0, seed_resize_from_w=0, p=None) L483
decode_latent_batch(model, batch, target_device=None, check_for_nans=False) L492
get_fixed_seed(seed) L526
fix_seed(p) L541
program_version() L546
create_infotext(p, all_prompts, all_seeds, all_subseeds, comments=None, iteration=0, position_in_batch=0, use_main_prompt=False, index=None, all_negative_prompts=None) L556
process_images(p) L610
process_images_inner(p) L653
"this is the main loop that both txt2img and img2img use; it calls func_init once..."
infotext(index=0, use_main_prompt=False) L794
old_hires_fix_first_pass_dimensions(width, height) L897
"old algorithm for auto-calculating first pass size"

modules/progress.py
class ProgressRequest(BaseModel) L48

class ProgressResponse(BaseModel) L53

start_task(id_task) L20
finish_task(id_task) L27
record_results(id_task, res) L38
add_task_to_queue(id_job) L44
setup_progress_api(app) L64
progressapi(req) L68
restore_progress(id_task) L121

modules/prompt_parser.py
class CollectSteps(lark.Visitor) L65
  scheduled(self, tree) L66
  alternate(self, tree) L73

class AtStep(lark.Transformer) L80
  scheduled(self, args) L81
  alternate(self, args) L84
  start(self, args) L87
  flatten(x) L88
  plain(self, args) L95
  __default__(self, data, children, meta) L97

class SdConditioning(list) L119
  "A list with prompts for stable diffusion's conditioner model."
  __init__(self, prompts, is_negative_prompt=False, width=None, height=None, copy_from=None) L124

class ComposableScheduledPromptConditioning L220
  __init__(self, schedules, weight=1.0) L221

class MulticondLearnedConditioning L226
  __init__(self, shape, batch) L227

class DictWithShape(dict) L250
  __init__(self, x, shape) L251
  shape(self) L256

get_learned_conditioning_prompt_schedules(prompts, steps) L29
">>> g = lambda p: get_learned_conditioning_prompt_schedules([p], 10)[0]"
collect_steps(steps, tree) L62
at_step(step, tree) L79
get_schedule(prompt) L102
get_learned_conditioning(model, prompts, steps) L137
"converts a list of prompts into a list of prompt schedules - each schedule is a ..."
get_multicond_prompt_list(prompts) L189
get_multicond_learned_conditioning(model, prompts, steps) L232
"same as get_learned_conditioning, but returns a list of ScheduledPromptCondition..."
reconstruct_cond_batch(c, current_step) L260
stack_conds(tensors) L287
reconstruct_multicond_batch(c, current_step) L301
parse_prompt_attention(text) L350
"Parses a string with attention tokens and returns a list of pairs: text and its ..."
multiply_range(start_position, multiplier) L393

modules/realesrgan_model.py
class UpscalerRealESRGAN(Upscaler) L12
  __init__(self, path) L13
  do_upscale(self, img, path) L41
  load_model(self, path) L65
  load_models(self, _) L78

get_realesrgan_models(scaler) L82

modules/restart.py
is_restartable() L7
"Return True if the webui is restartable (i.e. there is something watching to res..."
restart_program() L14
"creates file tmp/restart and immediately stops the process, which webui.bat/webu..."
stop_program() L22

modules/rng.py
class ImageRNG L99
  __init__(self, shape, seeds, subseeds=None, subseed_strength=0.0, seed_resize_from_h=0, seed_resize_from_w=0) L100
  first(self) L112
  next(self) L153

randn(seed, shape, generator=None) L6
"Generate a tensor with random numbers from a normal distribution using seed."
randn_local(seed, shape) L22
"Generate a tensor with random numbers from a normal distribution using seed."
randn_like(x) L36
"Generate a tensor with random numbers from a normal distribution using the previ..."
randn_without_seed(shape, generator=None) L50
"Generate a tensor with random numbers from a normal distribution using the previ..."
manual_seed(seed) L64
"Set up a global random number generator using the specified seed."
create_generator(seed) L75
slerp(val, low, high) L85

modules/rng_philox.py
class Generator L77
  "RNG that produces same outputs as torch.randn(..., device='cuda') on CPU"
  __init__(self, seed) L80
  randn(self, shape) L84
  "Generate a sequence of n standard normal random variables using the Philox 4x32 ..."

uint32(x) L27
"Converts (N,) np.uint64 array into (2, N) np.unit32 array."
philox4_round(counter, key) L32
"A single round of the Philox 4x32 random number generator."
philox4_32(counter, key, rounds=10) L44
"Generates 32-bit random numbers using the Philox 4x32 random number generator."
box_muller(x, y) L66
"Returns just the first out of two numbers generated by Box–Muller transform algo..."

modules/safe.py
class RestrictedUnpickler(pickle.Unpickler) L23
  persistent_load(self, saved_id) L26
  find_class(self, module, name) L34

class Extra L159
  "A class for temporarily setting the global handler for when you can't explicitly..."
  __init__(self, handler) L179
  __enter__(self) L182
  __exit__(self, exc_type, exc_val, exc_tb) L188

encode(*args) L18
check_zip_filenames(filename, names) L71
check_pt(filename, extra_handler) L79
load(filename, *args, **kwargs) L107
load_with_extra(filename, extra_handler=None, *args, **kwargs) L111
"this function is intended to be used by extensions that want to load models with"

modules/script_callbacks.py
class ImageSaveParams L16
  __init__(self, image, p, filename, pnginfo) L17

class CFGDenoiserParams L31
  __init__(self, x, image_cond, sigma, sampling_step, total_sampling_steps, text_cond, text_uncond) L32

class CFGDenoisedParams L55
  __init__(self, x, sampling_step, total_sampling_steps, inner_model) L56

class AfterCFGCallbackParams L70
  __init__(self, x, sampling_step, total_sampling_steps) L71

class UiTrainTabParams L82
  __init__(self, txt2img_preview_params) L83

class ImageGridLoopParams L87
  __init__(self, imgs, cols, rows) L88

report_exception(c, job) L12
clear_callbacks() L118
app_started_callback(demo, app) L123
app_reload_callback() L132
model_loaded_callback(sd_model) L140
ui_tabs_callback() L148
ui_train_tabs_callback(params) L160
ui_settings_callback() L168
before_image_saved_callback(params) L176
image_saved_callback(params) L184
cfg_denoiser_callback(params) L192
cfg_denoised_callback(params) L200
cfg_after_cfg_callback(params) L208
before_component_callback(component, **kwargs) L216
after_component_callback(component, **kwargs) L224
image_grid_callback(params) L232
infotext_pasted_callback(infotext, params) L240
script_unloaded_callback() L248
before_ui_callback() L256
list_optimizers_callback() L264
list_unets_callback() L276
add_callback(callbacks, fun) L288
remove_current_script_callbacks() L295
remove_callbacks_for_function(callback_func) L305
on_app_started(callback) L311
"register a function to be called when the webui started, the gradio `Block` comp..."
on_before_reload(callback) L317
"register a function to be called just before the server reloads."
on_model_loaded(callback) L322
"register a function to be called when the stable diffusion model is created; the..."
on_ui_tabs(callback) L328
"register a function to be called when the UI is creating new tabs."
on_ui_train_tabs(callback) L341
"register a function to be called when the UI is creating new tabs for the train ..."
on_ui_settings(callback) L348
"register a function to be called before UI settings are populated; add your sett..."
on_before_image_saved(callback) L354
"register a function to be called before an image is saved to a file."
on_image_saved(callback) L362
"register a function to be called after an image is saved to a file."
on_cfg_denoiser(callback) L370
"register a function to be called in the kdiffussion cfg_denoiser method after bu..."
on_cfg_denoised(callback) L378
"register a function to be called in the kdiffussion cfg_denoiser method after bu..."
on_cfg_after_cfg(callback) L386
"register a function to be called in the kdiffussion cfg_denoiser method after cf..."
on_before_component(callback) L394
"register a function to be called before a component is created."
on_after_component(callback) L406
"register a function to be called after a component is created. See on_before_com..."
on_image_grid(callback) L411
"register a function to be called before making an image grid."
on_infotext_pasted(callback) L419
"register a function to be called before applying an infotext."
on_script_unloaded(callback) L428
"register a function to be called before the script is unloaded. Any hooks/hijack..."
on_before_ui(callback) L435
"register a function to be called before the UI is created."
on_list_optimizers(callback) L441
"register a function to be called when UI is making a list of cross attention opt..."
on_list_unets(callback) L449
"register a function to be called when UI is making a list of alternative options..."

modules/script_loading.py
load_module(path) L7
preload_extensions(extensions_dir, parser, extension_list=None) L15

modules/scripts.py
class PostprocessImageArgs L14
  __init__(self, image) L15

class PostprocessBatchListArgs L19
  __init__(self, images) L20

class Script L24
  title(self) L55
  "this function should return the title of the script. This is what will be displa..."
  ui(self, is_img2img) L60
  "this function should create gradio UI elements. See https://gradio.app/docs/#com..."
  show(self, is_img2img) L68
  "is_img2img is True if this function is called for the img2img interface, and Fas..."
  run(self, p, *args) L80
  "This function is called if the script has been selected in the script dropdown."
  before_process(self, p, *args) L93
  "This function is called very early before processing begins for AlwaysVisible sc..."
  process(self, p, *args) L102
  "This function is called before processing begins for AlwaysVisible scripts."
  before_process_batch(self, p, *args, **kwargs) L111
  "Called before extra networks are parsed from the prompt, so you can add"
  after_extra_networks_activate(self, p, *args, **kwargs) L125
  "Called after extra networks activation, before conds calculation"
  process_batch(self, p, *args, **kwargs) L140
  "Same as process(), but called for every batch."
  postprocess_batch(self, p, *args, **kwargs) L153
  "Same as process_batch(), but called for every batch after it has been generated."
  postprocess_batch_list(self, p, pp, *args, **kwargs) L164
  "Same as postprocess_batch(), but receives batch images as a list of 3D tensors i..."
  postprocess_image(self, p, pp, *args) L183
  "Called for every image after it has been generated."
  postprocess(self, p, processed, *args) L190
  "This function is called after processing ends for AlwaysVisible scripts."
  before_component(self, component, **kwargs) L198
  "Called before a component is created."
  after_component(self, component, **kwargs) L208
  "Called after a component is created. Same as above."
  describe(self) L215
  "unused"
  elem_id(self, item_id) L219
  "helper function to generate id for a HTML element, constructs final id out of sc..."
  before_hr(self, p, *args) L229
  "This function is called before hires fix start."

class ScriptRunner L346
  __init__(self) L347
  initialize_scripts(self, is_img2img) L356
  create_script_ui(self, script) L382
  setup_ui_for_section(self, section, scriptlist=None) L424
  prepare_ui(self) L437
  setup_ui(self) L440
  select_script(script_index) L450
  init_field(title) L455
  "called when an initial value is set from ui-config.json to show script's UI comp..."
  onload_script_visibility(params) L474
  run(self, p, *args) L489
  before_process(self, p) L507
  process(self, p) L515
  before_process_batch(self, p, **kwargs) L523
  after_extra_networks_activate(self, p, **kwargs) L531
  process_batch(self, p, **kwargs) L539
  postprocess(self, p, processed) L547
  postprocess_batch(self, p, images, **kwargs) L555
  postprocess_batch_list(self, p, pp, **kwargs) L563
  postprocess_image(self, p, pp) L571
  before_component(self, component, **kwargs) L579
  after_component(self, component, **kwargs) L586
  reload_sources(self, cache) L593
  before_hr(self, p) L612

basedir() L238
"returns the base directory for the current script. For scripts in the main scrip..."
list_scripts(scriptdirname, extension) L253
list_files_with_name(filename) L269
load_scripts() L285
register_scripts_from_module(module) L295
orderby(basedir) L305
wrap_call(func, filename, funcname, *args, default=None, **kwargs) L337
reload_script_body_only() L627

modules/scripts_auto_postprocessing.py
class ScriptPostprocessingForMainUI(scripts.Script) L4
  __init__(self, script_postproc) L5
  title(self) L9
  show(self, is_img2img) L12
  ui(self, is_img2img) L15
  postprocess_image(self, p, script_pp, *args) L19

create_auto_preprocessing_script_data() L29

modules/scripts_postprocessing.py
class PostprocessedImage L7
  __init__(self, image) L8

class ScriptPostprocessing L13
  ui(self) L28
  "This function should create gradio UI elements. See https://gradio.app/docs/#com..."
  process(self, pp, **args) L37
  "This function is called to postprocess the image."
  image_changed(self) L45

class ScriptPostprocessingRunner L61
  __init__(self) L62
  initialize_scripts(self, scripts_data) L66
  create_script_ui(self, script, inputs) L78
  scripts_in_preferred_order(self) L90
  script_score(name) L97
  setup_ui(self) L108
  run(self, pp, args) L120
  create_args_for_run(self, scripts_args) L132
  image_changed(self) L149

wrap_call(func, filename, funcname, *args, default=None, **kwargs) L51

modules/sd_disable_initialization.py
class ReplaceHelper L9
  __init__(self) L10
  replace(self, obj, field, func) L13
  restore(self) L23

class DisableInitialization(ReplaceHelper) L30
  "When an object of this class enters a `with` block, it starts:"
  __init__(self, disable_clip=True) L46
  replace(self, obj, field, func) L50
  __enter__(self) L60
  do_nothing(*args, **kwargs) L61
  create_model_and_transforms_without_pretrained(*args, pretrained=None, **kwargs) L64
  CLIPTextModel_from_pretrained(pretrained_model_name_or_path, *model_args, **kwargs) L67
  transformers_modeling_utils_load_pretrained_model(*args, **kwargs) L72
  transformers_utils_hub_get_file_from_cache(original, url, *args, **kwargs) L76
  transformers_utils_hub_get_from_cache(url, *args, local_files_only=False, **kwargs) L90
  transformers_tokenization_utils_base_cached_file(url, *args, local_files_only=False, **kwargs) L93
  transformers_configuration_utils_cached_file(url, *args, local_files_only=False, **kwargs) L96
  __exit__(self, exc_type, exc_val, exc_tb) L111

class InitializeOnMeta(ReplaceHelper) L115
  "Context manager that causes all parameters for linear/conv2d/mha layers to be al..."
  __enter__(self) L128
  set_device(x) L132
  __exit__(self, exc_type, exc_val, exc_tb) L141

class LoadStateDictOnMeta(ReplaceHelper) L145
  "Context manager that allows to read parameters from state_dict into a model that..."
  __init__(self, state_dict, device) L158
  __enter__(self) L163
  load_from_state_dict(original, self, state_dict, prefix, *args, **kwargs) L170
  __exit__(self, exc_type, exc_val, exc_tb) L188

modules/sd_hijack.py
class StableDiffusionModelHijack L161
  __init__(self) L168
  apply_optimizations(self, option=None) L177
  hijack(self, m) L184
  flatten(el) L233
  undo_hijack(self, m) L247
  apply_circular(self, enable) L270
  clear_comments(self) L279
  get_prompt_lengths(self, text) L283
  redo_hijack(self, m) L291

class EmbeddingsWithFixes(torch.nn.Module) L296
  __init__(self, wrapped, embeddings, textual_inversion_key='clip_l') L297
  forward(self, input_ids) L303

list_optimizers() L41
apply_optimizations(option=None) L52
undo_optimizations() L96
fix_checkpoint() L106
"checkpoints are now added and removed in embedding/hypernet code, since torch do..."
weighted_loss(sd_model, pred, target, mean=True) L113
weighted_forward(sd_model, x, c, w, *args, **kwargs) L125
apply_weighted_forward(sd_model) L150
undo_weighted_forward(sd_model) L154
add_circular_option_to_conv_2d() L325
conv2d_constructor_circular(self, *args, **kwargs) L328
register_buffer(self, name, attr) L337
"Fix register buffer bug for Mac OS."

modules/sd_hijack_checkpoint.py
BasicTransformerBlock_forward(self, x, context=None) L7
AttentionBlock_forward(self, x) L11
ResBlock_forward(self, x, emb) L15
add() L22
remove() L37

modules/sd_hijack_clip.py
class PromptChunk L10
  "This object contains token ids, weight (multipliers:1.4) and textual inversion e..."
  __init__(self) L18

class FrozenCLIPEmbedderWithCustomWordsBase(torch.nn.Module) L30
  "A pytorch module that is a wrapper for FrozenCLIPEmbedder module. it enhances Fr..."
  __init__(self, wrapped, hijack) L35
  empty_chunk(self) L49
  "creates an empty PromptChunk and returns it"
  get_target_prompt_token_count(self, token_count) L57
  "returns the maximum number of tokens a prompt of a known length can have before ..."
  tokenize(self, texts) L62
  "Converts a batch of texts into a batch of token ids"
  encode_with_transformers(self, tokens) L67
  "converts a batch of token ids (in python lists) into a single tensor with numeri..."
  encode_embedding_init_text(self, init_text, nvpt) L78
  "Converts text into a tensor with this text's tokens' embeddings. Note that those..."
  tokenize_line(self, line) L84
  "this transforms a single prompt into a list of PromptChunk objects - as many as ..."
  next_chunk(is_last=False) L103
  "puts current chunk into the list of results and produces the next one - empty;"
  process_texts(self, texts) L179
  "Accepts a list of texts and calls tokenize_line() on each, with cache. Returns t..."
  forward(self, texts) L202
  "Accepts an array of texts; Passes texts through transformers network to create a..."
  process_tokens(self, remade_batch_tokens, batch_multipliers) L257
  "sends one single prompt chunk to be encoded by transformers neural network."

class FrozenCLIPEmbedderWithCustomWords(FrozenCLIPEmbedderWithCustomWordsBase) L290
  __init__(self, wrapped, hijack) L291
  tokenize(self, texts) L320
  encode_with_transformers(self, tokens) L325
  encode_embedding_init_text(self, init_text, nvpt) L336

class FrozenCLIPEmbedderForSDXLWithCustomWords(FrozenCLIPEmbedderWithCustomWords) L344
  __init__(self, wrapped, hijack) L345
  encode_with_transformers(self, tokens) L348

modules/sd_hijack_clip_old.py
process_text_old(self, texts) L5
forward_old(self, texts) L72

modules/sd_hijack_ip2p.py
should_hijack_ip2p(checkpoint_info) L4

modules/sd_hijack_open_clip.py
class FrozenOpenCLIPEmbedderWithCustomWords(sd_hijack_clip.FrozenCLIPEmbedderWithCustomWordsBase) L10
  __init__(self, wrapped, hijack) L11
  tokenize(self, texts) L19
  encode_with_transformers(self, tokens) L26
  encode_embedding_init_text(self, init_text, nvpt) L32

class FrozenOpenCLIPEmbedder2WithCustomWords(sd_hijack_clip.FrozenCLIPEmbedderWithCustomWordsBase) L40
  __init__(self, wrapped, hijack) L41
  tokenize(self, texts) L49
  encode_with_transformers(self, tokens) L56
  encode_embedding_init_text(self, init_text, nvpt) L66

modules/sd_hijack_optimizations.py
class SdOptimization L24
  title(self) L30
  is_available(self) L36
  apply(self) L39
  undo(self) L42

class SdOptimizationXformers(SdOptimization) L50
  is_available(self) L55
  apply(self) L58

class SdOptimizationSdpNoMem(SdOptimization) L65
  is_available(self) L71
  apply(self) L74

class SdOptimizationSdp(SdOptimizationSdpNoMem) L81
  apply(self) L87

class SdOptimizationSubQuad(SdOptimization) L94
  apply(self) L99

class SdOptimizationV1(SdOptimization) L106
  apply(self) L112

class SdOptimizationInvokeAI(SdOptimization) L117
  priority(self) L122
  apply(self) L125

class SdOptimizationDoggettx(SdOptimization) L130
  apply(self) L135

list_optimizers(res) L142
get_available_vram() L162
split_cross_attention_forward_v1(self, x, context=None, mask=None, **kwargs) L176
split_cross_attention_forward(self, x, context=None, mask=None, **kwargs) L217
einsum_op_compvis(q, k, v) L284
einsum_op_slice_0(q, k, v, slice_size) L290
einsum_op_slice_1(q, k, v, slice_size) L298
einsum_op_mps_v1(q, k, v) L306
einsum_op_mps_v2(q, k, v) L316
einsum_op_tensor_mem(q, k, v, max_tensor_mb) L323
einsum_op_cuda(q, k, v) L333
einsum_op(q, k, v) L344
split_cross_attention_forward_invokeAI(self, x, context=None, mask=None, **kwargs) L358
sub_quad_attention_forward(self, x, context=None, mask=None, **kwargs) L386
sub_quad_attention(q, k, v, q_chunk_size=1024, kv_chunk_size=None, kv_chunk_size_min=None, chunk_threshold=None, use_checkpoint=True) L423
get_xformers_flash_attention_op(q, k, v) L458
xformers_attention_forward(self, x, context=None, mask=None, **kwargs) L473
scaled_dot_product_attention_forward(self, x, context=None, mask=None, **kwargs) L499
scaled_dot_product_no_mem_attention_forward(self, x, context=None, mask=None, **kwargs) L540
cross_attention_attnblock_forward(self, x) L545
xformers_attnblock_forward(self, x) L604
sdp_attnblock_forward(self, x) L628
sdp_no_mem_attnblock_forward(self, x) L649
sub_quad_attnblock_forward(self, x) L654

modules/sd_hijack_unet.py
class TorchHijackForUnet L8
  "This is torch, but with cat that resizes tensors to appropriate dimensions if th..."
  __getattr__(self, item) L14
  cat(self, tensors, *args, **kwargs) L23

class GELUHijack(torch.nn.GELU, torch.nn.Module) L51
  __init__(self, *args, **kwargs) L52
  forward(self, x) L54

apply_model(orig_func, self, x_noisy, t, cond, **kwargs) L38
hijack_ddpm_edit() L62

modules/sd_hijack_utils.py
class CondFunc L3
  __new__(cls, orig_func, sub_func, cond_func) L4
  __init__(self, orig_func, sub_func, cond_func) L20
  __call__(self, *args, **kwargs) L24

modules/sd_hijack_xlmr.py
class FrozenXLMREmbedderWithCustomWords(sd_hijack_clip.FrozenCLIPEmbedderWithCustomWords) L6
  __init__(self, wrapped, hijack) L7
  encode_with_transformers(self, tokens) L16
  encode_embedding_init_text(self, init_text, nvpt) L27

modules/sd_models.py
class CheckpointInfo L30
  __init__(self, filename) L31
  read_metadata() L47
  register(self) L75
  calculate_shorthash(self) L80

class SkipWritingToConfig L298
  "This context manager prevents load_model_weights from writing checkpoint name to..."
  __enter__(self) L304
  __exit__(self, exc_type, exc_value, exc_traceback) L309

class SdModelData L455
  __init__(self) L456
  get_sd_model(self) L462
  set_sd_model(self, v) L482

setup_model() L111
checkpoint_tiles(use_short=False) L117
list_models() L121
get_closet_checkpoint_match(search_string) L149
model_hash(filename) L166
"old hash that only looks at a small part of the file and is prone to collisions"
select_checkpoint() L181
"Raises `FileNotFoundError` if no checkpoints are found."
transform_checkpoint_dict_key(k) L213
get_state_dict_from_checkpoint(pl_sd) L221
read_metadata_from_safetensors(filename) L238
read_state_dict(checkpoint_file, print_global_state=False, map_location=None) L262
get_checkpoint_state_dict(checkpoint_info, timer) L282
load_model_weights(model, checkpoint_info, state_dict, timer) L313
enable_midas_autodownload() L386
"Gives the ldm.modules.midas.api.load_model function automatic downloading."
load_model_wrapper(model_type) L414
repair_config(sd_config) L429
get_empty_cond(sd_model) L497
send_model_to_cpu(m) L509
send_model_to_device(m) L518
send_model_to_trash(m) L525
load_model(checkpoint_info=None, already_loaded_state_dict=None) L530
reuse_model_from_already_loaded(sd_model, checkpoint_info, timer) L612
"Checks if the desired checkpoint from checkpoint_info is not already loaded in m..."
reload_model_weights(sd_model=None, info=None) L666
unload_model_weights(sd_model=None, info=None) L728
apply_token_merging(sd_model, token_merging_ratio) L744
"Applies speed and memory optimizations from tomesd."

modules/sd_models_config.py
is_using_v_parameterization_for_sd2(state_dict) L26
"Detects whether unet in state_dict is using v-parameterization. Returns True if ..."
guess_model_config_from_state_dict(sd, filename) L68
find_checkpoint_config(state_dict, info) L104
find_checkpoint_config_near_filename(info) L115

modules/sd_models_xl.py
get_learned_conditioning(self, batch) L11
apply_model(self, x, t, cond) L36
get_first_stage_encoding(self, x) L40
encode_embedding_init_text(self, init_text, nvpt) L49
tokenize(self, texts) L59
process_texts(self, texts) L67
get_target_prompt_token_count(self, token_count) L72
extend_sdxl(model) L84
"this adds a bunch of parameters to make SDXL model look a bit more like SD1.5 to..."

modules/sd_samplers.py
find_sampler_config(name) L18
create_sampler(name, model) L27
set_samplers() L41
visible_sampler_names() L55

modules/sd_samplers_cfg_denoiser.py
class CFGDenoiser(torch.nn.Module) L33
  "Classifier free guidance denoiser. A wrapper for stable diffusion model (specifi..."
  __init__(self, sampler) L41
  inner_model(self) L56
  combine_denoised(self, x_out, conds_list, uncond, cond_scale) L60
  combine_denoised_for_edit_model(self, x_out, cond_scale) L70
  get_pred_x0(self, x_in, x_out, sigma) L76
  update_inner_model(self) L79
  forward(self, x, sigma, uncond, cond, cond_scale, s_min_uncond, image_cond) L86

catenate_conds(conds) L11
subscript_cond(cond, a, b) L18
pad_cond(tensor, repeats, empty) L25

modules/sd_samplers_common.py
class InterruptedException(BaseException) L118

class TorchHijack L163
  "This is here to replace torch.randn_like of k-diffusion."
  __init__(self, p) L171
  __getattr__(self, item) L174
  randn_like(self, x) L183

class Sampler L187
  __init__(self, funcname) L188
  callback_state(self, d) L212
  launch_sampling(self, steps, func) L221
  number_of_needed_noises(self, p) L238
  initialize(self, p) L241
  create_noise_sampler(self, x, sigmas, p) L289
  "For DPM++ SDE: manually create noise sampler to enable deterministic results acr..."

setup_img2img_steps(p, steps=None) L13
samples_to_images_tensor(sample, approximation=None, model=None) L28
"latents -> images [-1, 1]"
single_sample_to_image(sample, approximation=None) L49
decode_first_stage(model, x) L59
sample_to_image(samples, index=0, approximation=None) L65
samples_to_image_grid(samples, approximation=None) L69
images_tensor_to_samples(image, approximation=None, model=None) L73
"image[0, 1] -> latent"
store_latent(decoded) L91
is_sampler_using_eta_noise_seed_delta(p) L99
"returns whether sampler from config will use eta noise seed delta for image crea..."
replace_torchsde_browinan() L122
torchsde_randn(size, dtype, device, seed) L125
apply_refiner(sampler) L134

modules/sd_samplers_extra.py
restart_sampler(model, x, sigmas, extra_args=None, callback=None, disable=None, s_noise=1.0, restart_list=None) L7
"Implements restart sampling in Restart Sampling for Improving Generative Process..."
heun_step(x, old_sigma, new_sigma, second_order=True) L17

modules/sd_samplers_kdiffusion.py
class CFGDenoiserKDiffusion(sd_samplers_cfg_denoiser.CFGDenoiser) L56
  inner_model(self) L58

class KDiffusionSampler(sd_samplers_common.Sampler) L66
  __init__(self, funcname, sd_model) L67
  get_sigmas(self, p, steps) L75
  sample_img2img(self, p, x, noise, conditioning, unconditional_conditioning, steps=None, image_conditioning=None) L125
  sample(self, p, x, conditioning, unconditional_conditioning, steps=None, image_conditioning=None) L169

modules/sd_samplers_timesteps.py
class CompVisTimestepsDenoiser(torch.nn.Module) L23
  __init__(self, model, *args, **kwargs) L24
  forward(self, input, timesteps, **kwargs) L28

class CompVisTimestepsVDenoiser(torch.nn.Module) L32
  __init__(self, model, *args, **kwargs) L33
  predict_eps_from_z_and_v(self, x_t, t, v) L37
  forward(self, input, timesteps, **kwargs) L40

class CFGDenoiserTimesteps(CFGDenoiser) L46
  __init__(self, sampler) L48
  get_pred_x0(self, x_in, x_out, sigma) L53
  inner_model(self) L65

class CompVisSampler(sd_samplers_common.Sampler) L73
  __init__(self, funcname, sd_model) L74
  get_timesteps(self, p, steps) L82
  sample_img2img(self, p, x, noise, conditioning, unconditional_conditioning, steps=None, image_conditioning=None) L94
  sample(self, p, x, conditioning, unconditional_conditioning, steps=None, image_conditioning=None) L131

modules/sd_samplers_timesteps_impl.py
class UniPCCFG(uni_pc.UniPC) L102
  __init__(self, cfg_model, extra_args, callback, *args, **kwargs) L103
  after_update(x, model_x) L106
  get_model_input_time(self, t_continuous) L116
  model(self, x, t) L119

ddim(model, x, timesteps, extra_args=None, callback=None, disable=None, eta=0.0) L11
plms(model, x, timesteps, extra_args=None, callback=None, disable=None) L42
get_x_prev_and_pred_x0(e_t, index) L52
unipc(model, x, timesteps, extra_args=None, callback=None, disable=None, is_img2img=False) L127

modules/sd_unet.py
class SdUnetOption L64
  create_unet(self) L71
  "returns SdUnet object to be used as a Unet instead of built-in unet when making ..."

class SdUnet(torch.nn.Module) L76
  forward(self, x, timesteps, context, *args, **kwargs) L77
  activate(self) L80
  deactivate(self) L83

list_unets() L11
get_unet_option(option=None) L18
apply_unet(option=None) L34
UNetModel_forward(self, x, timesteps=None, context=None, *args, **kwargs) L87

modules/sd_vae.py
class VaeResolution L118
  tuple(self) L123

get_loaded_vae_name() L23
get_loaded_vae_hash() L30
get_base_vae(model) L37
store_base_vae(model) L43
delete_base_vae() L51
restore_base_vae(model) L57
get_filename(filepath) L66
refresh_vae_list() L70
find_vae_near_checkpoint(checkpoint_file) L108
is_automatic() L127
resolve_vae_from_setting() L131
resolve_vae_from_user_metadata(checkpoint_file) L145
resolve_vae_near_checkpoint(checkpoint_file) L159
resolve_vae(checkpoint_file) L167
load_vae_dict(filename, map_location) L187
load_vae(model, vae_file=None, vae_source='from unknown source') L193
_load_vae_dict(model, vae_dict_1) L235
clear_loaded_vae() L240
reload_vae_weights(sd_model=None, vae_file=unspecified) L248

modules/sd_vae_approx.py
class VAEApprox(nn.Module) L10
  __init__(self) L11
  forward(self, x) L22

download_model(model_path, model_url) L34
model() L42
cheap_approximation(sample) L64

modules/sd_vae_taesd.py
class Clamp(nn.Module) L20
  forward(x) L22

class Block(nn.Module) L26
  __init__(self, n_in, n_out) L27
  forward(self, x) L33

class TAESDDecoder(nn.Module) L57
  __init__(self, decoder_path='taesd_decoder.pth') L61
  "Initialize pretrained TAESD on the given device from the given checkpoints."

class TAESDEncoder(nn.Module) L69
  __init__(self, encoder_path='taesd_encoder.pth') L73
  "Initialize pretrained TAESD on the given device from the given checkpoints."

conv(n_in, n_out, **kwargs) L16
decoder() L37
encoder() L47
download_model(model_path, model_url) L81
decoder_model() L89
encoder_model() L108

modules/shared_gradio_themes.py
reload_gradio_theme(theme_name=None) L43

modules/shared_init.py
initialize() L9
"Initializes fields inside the shared module in a controlled manner."

modules/shared_items.py
class Shared(sys.modules[__name__].__class__) L97
  "this class is here to provide sd_model field as a property, so that it can be cr..."
  sd_model(self) L106
  sd_model(self, value) L112

realesrgan_models_names() L6
postprocessing_scripts() L11
sd_vae_items() L17
refresh_vae_list() L23
cross_attention_optimizations() L29
sd_unet_items() L35
refresh_unet_list() L41
list_checkpoint_tiles() L47
refresh_checkpoints() L52
list_samplers() L57
reload_hypernetworks() L62
ui_reorder_categories() L82

modules/shared_state.py
class State L12
  __init__(self) L32
  need_restart(self) L36
  need_restart(self, value) L41
  server_command(self) L47
  server_command(self, value) L51
  "Set the server command to `value` and signal that it's been set."
  wait_for_server_command(self, timeout=None) L58
  "Wait for server command to get set; return and clear the value and signal."
  request_restart(self) L69
  skip(self) L74
  interrupt(self) L78
  nextjob(self) L82
  dict(self) L90
  begin(self, job='(unknown)') L104
  end(self) L122
  set_current_image(self) L130
  "sets self.current_image from self.current_latent if enough sampling steps have b..."
  do_set_current_image(self) L138
  assign_current_image(self, image) L157

modules/shared_total_tqdm.py
class TotalTQDM L6
  __init__(self) L7
  reset(self) L10
  update(self) L18
  updateTotal(self, new_total) L25
  clear(self) L32

modules/styles.py
class PromptStyle(typing.NamedTuple) L9

class StyleDatabase L70
  __init__(self, path) L71
  reload(self) L78
  get_style_prompts(self, styles) L92
  get_negative_style_prompts(self, styles) L95
  apply_styles_to_prompt(self, prompt, styles) L98
  apply_negative_styles_to_prompt(self, prompt, styles) L101
  save_styles(self, path) L104
  extract_styles_from_prompt(self, prompt, negative_prompt) L114

merge_prompts(style_prompt, prompt) L15
apply_styles_to_prompt(prompt, styles) L25
extract_style_text_from_prompt(style_text, prompt) L35
extract_style_from_prompts(style, prompt, negative_prompt) L55

modules/sub_quadratic_attention.py
class AttnChunk(NamedTuple) L30

class SummarizeChunk L36
  __call__(query, key, value) L38

class ComputeQueryChunkAttn L45
  __call__(query, key, value) L47

class ScannedChunk(NamedTuple) L136

narrow_trunc(input, dim, start, length) L21
_summarize_chunk(query, key, value, scale) L54
_query_chunk_attention(query, key, value, summarize_chunk, kv_chunk_size) L75
chunk_scanner(chunk_idx) L85
_get_attention_scores_no_kv_chunking(query, key, value, scale) L117
efficient_dot_product_attention(query, key, value, query_chunk_size=1024, kv_chunk_size=None, kv_chunk_size_min=None, use_checkpoint=True) L141
"Computes efficient dot-product attention given query, key, and value."
get_query_chunk(chunk_idx) L175

modules/sysinfo.py
pretty_bytes(num, suffix='B') L41
get() L48
check(x) L62
get_dict() L73
format_traceback(tb) L107
format_exception(e, tb) L111
get_exceptions() L115
get_environment() L122
get_torch_sysinfo() L129
get_extensions(*, enabled) L139
to_json(x) L142
get_config() L156

modules/textual_inversion/autocrop.py
class PointOfInterest L315
  __init__(self, x, y, weight=1.0, size=10) L316
  bounding(self, size) L322

class Settings L331
  __init__(self, crop_width=512, crop_height=512, corner_points_weight=0.5, entropy_points_weight=0.5, face_points_weight=0.5, annotate_image=False, dnn_model_path=None) L332

crop_image(im, settings) L12
"Intelligently crop an image to the subject matter"
focal_point(im, settings) L72
image_face_points(im, settings) L140
image_corner_points(im, settings) L196
image_entropy_points(im, settings) L224
image_entropy(im) L256
centroid(pois) L265
poi_average(pois, settings) L271
is_landscape(w, h) L285
is_portrait(w, h) L289
is_square(w, h) L293
download_and_cache_models(dirname) L297

modules/textual_inversion/dataset.py
class DatasetEntry L21
  __init__(self, filename=None, filename_text=None, latent_dist=None, latent_sample=None, cond=None, cond_text=None, pixel_values=None, weight=None) L22

class PersonalizedBase(Dataset) L33
  __init__(self, data_root, width, height, repeats, flip_p=0.5, placeholder_token='*', model=None, cond_model=None, device=None, template_file=None, include_cond=False, batch_size=1, gradient_step=1, shuffle_tags=False, tag_drop_out=0, latent_sampling_method='once', varsize=False, use_weight=False) L34
  create_text(self, filename_text) L153
  __len__(self) L164
  __getitem__(self, i) L167

class GroupedBatchSampler(Sampler) L176
  __init__(self, data_source, batch_size) L177
  __len__(self) L189
  __iter__(self) L192

class PersonalizedDataLoader(DataLoader) L210
  __init__(self, dataset, latent_sampling_method='once', batch_size=1, pin_memory=False) L211

class BatchLoader L219
  __init__(self, data) L220
  pin_memory(self) L231

class BatchLoaderRandom(BatchLoader) L238
  __init__(self, data) L239
  pin_memory(self) L242

collate_wrapper(batch) L235
collate_wrapper_random(batch) L245

modules/textual_inversion/image_embedding.py
class EmbeddingEncoder(json.JSONEncoder) L11
  default(self, obj) L12

class EmbeddingDecoder(json.JSONDecoder) L18
  __init__(self, *args, **kwargs) L19
  object_hook(self, d) L22

embedding_to_b64(data) L28
embedding_from_b64(data) L33
lcg(m=2 ** 32, a=1664525, c=1013904223, seed=0) L38
xor_block(block) L44
style_block(block, sequence) L50
insert_image_data_embed(image, data) L68
crop_black(img, tol=0) L104
extract_image_data_embed(image) L112
caption_image_overlay(srcimage, title, footerLeft, footerMid, footerRight, textfont=None) L133

modules/textual_inversion/learn_schedule.py
class LearnScheduleIterator L4
  __init__(self, learn_rate, max_steps, cur_step=0) L5
  "specify learn_rate as "0.001:100, 0.00001:1000, 1e-5:10000" to have lr of 0.001 ..."
  __iter__(self) L39
  __next__(self) L42

class LearnRateScheduler L50
  __init__(self, learn_rate, max_steps, cur_step=0, verbose=True) L51
  step(self, step_number) L61
  apply(self, optimizer, step_number) L72

modules/textual_inversion/logging.py
save_settings_to_file(log_directory, all_params) L52

modules/textual_inversion/preprocess.py
class PreprocessParams L33

preprocess(id_task, process_src, process_dst, process_width, process_height, preprocess_txt_action, process_keep_original_size, process_flip, process_split, process_caption, process_caption_deepbooru=False, split_threshold=0.5, overlap_ratio=0.2, process_focal_crop=False, process_focal_crop_face_weight=0.9, process_focal_crop_entropy_weight=0.15, process_focal_crop_edges_weight=0.5, process_focal_crop_debug=False, process_multicrop=None, process_multicrop_mindim=None, process_multicrop_maxdim=None, process_multicrop_minarea=None, process_multicrop_maxarea=None, process_multicrop_objective=None, process_multicrop_threshold=None) L10
listfiles(dirname) L29
save_pic_with_caption(image, index, params, existing_caption=None) L43
save_pic(image, index, params, existing_caption=None) L77
split_pic(image, inverse_xy, width, height, overlap_ratio) L84
center_crop(image, w, h) L108
multicrop_pic(image, mindim, maxdim, minarea, maxarea, objective, threshold) L119
preprocess_work(process_src, process_dst, process_width, process_height, preprocess_txt_action, process_keep_original_size, process_flip, process_split, process_caption, process_caption_deepbooru=False, split_threshold=0.5, overlap_ratio=0.2, process_focal_crop=False, process_focal_crop_face_weight=0.9, process_focal_crop_entropy_weight=0.3, process_focal_crop_edges_weight=0.5, process_focal_crop_debug=False, process_multicrop=None, process_multicrop_mindim=None, process_multicrop_maxdim=None, process_multicrop_minarea=None, process_multicrop_maxarea=None, process_multicrop_objective=None, process_multicrop_threshold=None) L130

modules/textual_inversion/textual_inversion.py
class Embedding L40
  __init__(self, vec, name, step=None) L41
  save(self, filename) L55
  checksum(self) L74
  const_hash(a) L78
  set_hash(self, v) L87

class DirWithTextualInversionEmbeddings L92
  __init__(self, path) L93
  has_changed(self) L97
  update(self) L105

class EmbeddingDatabase L112
  __init__(self) L113
  add_embedding_dir(self, path) L121
  clear_embedding_dirs(self) L124
  register_embedding(self, embedding, model) L127
  register_embedding_by_name(self, embedding, model, name) L130
  get_expected_shape(self) L153
  load_from_file(self, path, filename) L157
  load_from_dir(self, embdir) L224
  load_textual_inversion_embeddings(self, force_reload=False) L241
  find_embedding_at_position(self, tokens, offset) L274

list_textual_inversion_templates() L28
create_embedding(name, num_vectors_per_token, overwrite_old, init_text='*') L288
write_loss(log_directory, filename, step, epoch_len, values) L316
tensorboard_setup(log_directory) L340
tensorboard_add(tensorboard_writer, loss, global_step, step, learn_rate, epoch_num) L346
tensorboard_add_scaler(tensorboard_writer, tag, value, step) L352
tensorboard_add_image(tensorboard_writer, tag, pil_image, step) L356
validate_train_inputs(model_name, learn_rate, batch_size, gradient_step, data_root, template_file, template_filename, steps, save_model_every, create_image_every, log_directory, name='embedding') L365
train_embedding(id_task, embedding_name, learn_rate, batch_size, gradient_step, data_root, log_directory, training_width, training_height, varsize, steps, clip_grad_mode, clip_grad_value, shuffle_tags, tag_drop_out, latent_sampling_method, use_weight, create_image_every, save_embedding_every, template_filename, save_image_with_stored_embedding, preview_from_txt2img, preview_prompt, preview_negative_prompt, preview_steps, preview_sampler_index, preview_cfg_scale, preview_seed, preview_width, preview_height) L389
save_embedding(embedding, optimizer, checkpoint, embedding_name, filename, remove_cached_checksum=True) L676

modules/textual_inversion/ui.py
create_embedding(name, initialization_text, nvpt, overwrite_old) L10
preprocess(*args) L18
train_embedding(*args) L24

modules/timer.py
class TimerSubcategory L5
  __init__(self, timer, category) L6
  __enter__(self) L12
  __exit__(self, exc_type, exc_val, exc_tb) L20

class Timer L28
  __init__(self, print_log=False) L29
  elapsed(self) L37
  add_time_to_record(self, category, amount) L43
  record(self, category, extra_time=0, disable_log=False) L49
  subcategory(self, name) L59
  summary(self) L65
  dump(self) L78
  reset(self) L81

modules/txt2img.py
txt2img(id_task, prompt, negative_prompt, prompt_styles, steps, sampler_name, n_iter, batch_size, cfg_scale, seed, subseed, subseed_strength, seed_resize_from_h, seed_resize_from_w, seed_enable_extras, height, width, enable_hr, denoising_strength, hr_scale, hr_upscaler, hr_second_pass_steps, hr_resize_x, hr_resize_y, hr_checkpoint_name, hr_sampler_name, hr_prompt, hr_negative_prompt, override_settings_texts, request, *args) L12

modules/ui.py
class Toprow L245
  "Creates a top row UI with prompts, generate button, styles, extra little buttons..."
  __init__(self, is_img2img) L248

class QuicksettingsHint(BaseModel) L1437

gr_show(visible=True) L61
send_gradio_gallery_to_image(x) L86
calc_resolution_hires(enable, width, height, hr_scale, hr_resize_x, hr_resize_y) L92
resize_from_to_html(width, height, scale_by) L102
process_interrogate(interrogation_function, mode, ii_input_dir, ii_output_dir, *ii_singles) L112
interrogate(image) L135
interrogate_deepbooru(image) L140
create_seed_inputs(target_interface) L145
change_visibility(show) L175
connect_clear_prompt(button) L184
"Given clear button, prompt, and token_counter objects, setup clear prompt button..."
connect_reuse_seed(seed, reuse_seed, generation_info, dummy_component, is_subseed) L194
"Connects a 'reuse (sub)seed' button's click event so that it copies last used"
copy_seed(gen_info_string, index) L198
update_token_counter(text, steps) L227
setup_progressbar(*args, **kwargs) L317
apply_setting(key, value) L321
create_output_panel(tabname, outdir) L354
create_sampler_and_steps_selection(choices, tabname) L358
ordered_ui_categories() L371
create_override_settings_dropdown(tabname, row) L378
create_ui() L390
add_copy_image_controls(tab_name, elem) L640
update_orig(image, state) L673
copy_image(img) L707
select_img2img_tab(tab) L830
get_textual_inversion_template_names() L1119
versions_html() L1404
setup_ui_api(app) L1433
quicksettings_hint() L1441
download_sysinfo(attachment=False) L1450

modules/ui_checkpoint_merger.py
class UiCheckpointMerger L29
  __init__(self) L30
  setup_ui(self, dummy_component, sd_model_checkpoint_component) L86

update_interp_description(value) L9
modelmerger(*args) L19

modules/ui_common.py
class MyObject L44
  __init__(self, d=None) L45

update_generation_info(generation_info, html_info, img_index) L20
plaintext_to_html(text, classname=None) L32
save_files(js_data, images, do_make_zip, index) L38
create_output_panel(tabname, outdir) L107
open_folder(f) L109
create_refresh_button(refresh_component, refresh_method, refreshed_args, elem_id) L223
refresh() L232
setup_dialog(button_show, dialog, *, button_close=None) L251
"Sets up the UI so that the dialog (gr.Box) is invisible, and is only shown when ..."

modules/ui_components.py
class FormComponent L4
  get_expected_parent(self) L5

class ToolButton(FormComponent, gr.Button) L12
  "Small button with single emoji as text, fits inside gradio forms"
  __init__(self, *args, **kwargs) L15
  get_block_name(self) L19

class FormRow(FormComponent, gr.Row) L23
  "Same as gr.Row but fits inside gradio forms"
  get_block_name(self) L26

class FormColumn(FormComponent, gr.Column) L30
  "Same as gr.Column but fits inside gradio forms"
  get_block_name(self) L33

class FormGroup(FormComponent, gr.Group) L37
  "Same as gr.Group but fits inside gradio forms"
  get_block_name(self) L40

class FormHTML(FormComponent, gr.HTML) L44
  "Same as gr.HTML but fits inside gradio forms"
  get_block_name(self) L47

class FormColorPicker(FormComponent, gr.ColorPicker) L51
  "Same as gr.ColorPicker but fits inside gradio forms"
  get_block_name(self) L54

class DropdownMulti(FormComponent, gr.Dropdown) L58
  "Same as gr.Dropdown but always multiselect"
  __init__(self, **kwargs) L60
  get_block_name(self) L63

class DropdownEditable(FormComponent, gr.Dropdown) L67
  "Same as gr.Dropdown but allows editing value"
  __init__(self, **kwargs) L69
  get_block_name(self) L72

class InputAccordion(gr.Checkbox) L76
  "A gr.Accordion that can be used as an input - returns True if open, False if clo..."
  __init__(self, value, **kwargs) L84
  extra(self) L98
  "Allows you to put something into the label of the accordion."
  __enter__(self) L114
  __exit__(self, exc_type, exc_val, exc_tb) L118
  get_block_name(self) L121

modules/ui_extensions.py
check_access() L22
apply_and_restart(disable_list, update_list, disable_all) L26
save_config_state(name) L59
restore_config_state(confirmed, config_state_name, restore_type) L75
check_updates(id_task, disable_list) L99
make_commit_link(commit_hash, remote, text=None) L124
extension_table() L136
update_config_states_table(state_name) L193
normalize_git_url(url) L320
install_extension_from_url(dirname, url, branch_name=None) L328
install_extension_from_index(url, hide_tags, sort_column, filter_text) L386
refresh_available_extensions(url, hide_tags, sort_column) L394
refresh_available_extensions_for_tags(hide_tags, sort_column, filter_text) L408
search_extensions(filter_text, hide_tags, sort_column) L414
get_date(info, key) L433
refresh_available_extensions_from_data(hide_tags, sort_column, filter_text='') L440
preload_extensions_git_metadata() L515
create_ui() L520

modules/ui_extra_networks.py
class ExtraNetworksPage L88
  __init__(self, title) L89
  refresh(self) L98
  read_user_metadata(self, item) L101
  link_preview(self, filename) L111
  search_terms_from_path(self, filename, possible_directories=None) L116
  create_html(self, tabname) L126
  create_item(self, name, index=None) L190
  list_items(self) L193
  allowed_directories_for_previews(self) L196
  create_html_for_item(self, item, tabname) L199
  "Create HTML for card item in tab tabname; can return empty string if the item is..."
  get_sort_keys(self, path) L259
  "List of default keys used for sorting in the UI."
  find_preview(self, path) L271
  "Find a preview PNG for a given path (without extension) and call link_preview on..."
  find_description(self, path) L288
  "Find and read a description file for a given path (without extension)."
  create_user_metadata_editor(self, ui, tabname) L300

class ExtraNetworksUi L317
  __init__(self) L318

register_page(page) L19
"registers extra networks page for the UI; recommend doing it in on_before_ui() c..."
fetch_file(filename='') L27
get_metadata(page='', item='') L44
get_single_card(page='', tabname='', name='') L58
add_pages_to_demo(app) L76
quote_js(s) L82
initialize() L304
register_default_pages() L308
pages_in_preferred_order(pages) L333
tab_name_score(name) L336
create_ui(interface, unrelated_tabs, tabname) L349
pages_html() L390
refresh() L396
path_is_parent(parent_path, child_path) L410
setup_ui(ui, gallery) L417
save_preview(index, images, filename) L418

modules/ui_extra_networks_checkpoints.py
class ExtraNetworksPageCheckpoints(ui_extra_networks.ExtraNetworksPage) L9
  __init__(self) L10
  refresh(self) L13
  create_item(self, name, index=None, enable_filter=True) L16
  list_items(self) L31
  allowed_directories_for_previews(self) L35
  create_user_metadata_editor(self, ui, tabname) L38

modules/ui_extra_networks_checkpoints_user_metadata.py
class CheckpointUserMetadataEditor(ui_extra_networks_user_metadata.UserMetadataEditor) L7
  __init__(self, ui, tabname, page) L8
  save_user_metadata(self, name, desc, notes, vae) L13
  update_vae(self, name) L21
  put_values_into_components(self, name) L25
  create_editor(self) L34

modules/ui_extra_networks_hypernets.py
class ExtraNetworksPageHypernetworks(ui_extra_networks.ExtraNetworksPage) L7
  __init__(self) L8
  refresh(self) L11
  create_item(self, name, index=None, enable_filter=True) L14
  list_items(self) L29
  allowed_directories_for_previews(self) L33

modules/ui_extra_networks_textual_inversion.py
class ExtraNetworksPageTextualInversion(ui_extra_networks.ExtraNetworksPage) L7
  __init__(self) L8
  refresh(self) L12
  create_item(self, name, index=None, enable_filter=True) L15
  list_items(self) L30
  allowed_directories_for_previews(self) L34

modules/ui_extra_networks_user_metadata.py
class UserMetadataEditor L11
  __init__(self, ui, tabname, page) L13
  get_user_metadata(self, name) L35
  create_extra_default_items_in_left_column(self) L45
  create_default_editor_elems(self) L48
  create_default_buttons(self) L60
  get_card_html(self, name) L71
  get_metadata_table(self, name) L92
  put_values_into_components(self, name) L109
  write_user_metadata(self, name, metadata) L122
  save_user_metadata(self, name, desc, notes) L130
  setup_save_handler(self, button, func, components) L137
  create_editor(self) L142
  create_ui(self) L155
  save_preview(self, index, gallery, name) L164
  setup_ui(self, gallery) L182

modules/ui_gradio_extensions.py
webpath(fn) L8
javascript_html() L17
css_html() L36
stylesheet(fn) L39
reload_javascript() L54
template_response(*args, **kwargs) L58

modules/ui_loadsave.py
class UiLoadsave L10
  "allows saving and restoring default values for gradio components"
  __init__(self, filename) L13
  add_component(self, path, x) L31
  "adds component to the registry of tracked components"
  apply_field(obj, field, condition=None, init_field=None) L36
  check_dropdown(val) L88
  check_tab_id(tab_id) L96
  add_block(self, x, path='') L109
  "adds all components inside a gradio block x to the registry of tracked component..."
  read_from_file(self) L123
  write_to_file(self, current_ui_settings) L127
  dump_defaults(self) L131
  "saves default values to a file unless tjhe file is present and there was an erro..."
  iter_changes(self, current_ui_settings, values) L139
  "given a dictionary with defaults from a file and current values from gradio elem..."
  ui_view(self, *values) L164
  ui_apply(self, *values) L179
  create_ui(self) L195
  "creates ui elements for editing defaults UI, without adding any logic to them"
  setup_ui(self) L211
  "adds logic to elements created with create_ui; all add_block class must be made ..."

modules/ui_postprocessing.py
create_ui() L6

modules/ui_prompt_styles.py
class UiPromptStyles L52
  __init__(self, tabname, main_ui_prompt, main_ui_negative_prompt) L53

select_style(name) L9
save_style(name, prompt, negative_prompt) L20
delete_style(name) L31
materialize_styles(prompt, negative_prompt, styles) L41
refresh_styles() L48

modules/ui_settings.py
class UiSettings L56
  run_settings(self, *args) L67
  run_settings_single(self, value, key) L86
  create_ui(self, loadsave, dummy_component) L97
  reload_scripts() L212
  check_file(x) L229
  add_quicksettings(self) L246
  add_functionality(self, demo) L252
  get_settings_values() L288

get_value_for_setting(key) L10
create_setting_component(key, is_quicksettings=False) L20
fun() L21

modules/ui_tempdir.py
register_tmp_file(gradio, filename) L16
check_tmp_file(gradio, filename) L24
save_pil_to_file(self, pil_image, dir=None, format='png') L34
install_ui_tempdir_override() L60
"override save to file function so that it also writes PNG info"
on_tmpdir_changed() L65
cleanup_tmpdr() L74

modules/upscaler.py
class Upscaler L14
  __init__(self, create_dirs=False) L26
  do_upscale(self, img, selected_model) L51
  upscale(self, img, scale, selected_model=None) L54
  load_model(self, path) L76
  find_models(self, ext_filter=None) L79
  update_status(self, prompt) L82

class UpscalerData L86
  __init__(self, name, path, upscaler=None, scale=4, model=None) L93

class UpscalerNone(Upscaler) L102
  load_model(self, path) L106
  do_upscale(self, img, selected_model=None) L109
  __init__(self, dirname=None) L112

class UpscalerLanczos(Upscaler) L117
  do_upscale(self, img, selected_model=None) L120
  load_model(self, _) L123
  __init__(self, dirname=None) L126

class UpscalerNearest(Upscaler) L132
  do_upscale(self, img, selected_model=None) L135
  load_model(self, _) L138
  __init__(self, dirname=None) L141

modules/util.py
natural_sort_key(s, regex=re.compile('([0-9]+)')) L8
listfiles(dirname) L12
html_path(filename) L17
html(filename) L21
walk_files(path, allowed_extensions=None) L31
ldm_print(*args, **kwargs) L54

modules/xlmr.py
class BertSeriesConfig(BertConfig) L8
  __init__(self, vocab_size=30522, hidden_size=768, num_hidden_layers=12, num_attention_heads=12, intermediate_size=3072, hidden_act='gelu', hidden_dropout_prob=0.1, attention_probs_dropout_prob=0.1, max_position_embeddings=512, type_vocab_size=2, initializer_range=0.02, layer_norm_eps=1e-12, pad_token_id=0, position_embedding_type='absolute', use_cache=True, classifier_dropout=None, project_dim=512, pooler_fn='average', learn_encoder=False, model_type='bert', **kwargs) L9

class RobertaSeriesConfig(XLMRobertaConfig) L16
  __init__(self, pad_token_id=1, bos_token_id=0, eos_token_id=2, project_dim=512, pooler_fn='cls', learn_encoder=False, **kwargs) L17

class BertSeriesModelWithTransformation(BertPreTrainedModel) L24
  __init__(self, config=None, **kargs) L30
  encode(self, c) L64
  forward(self, input_ids=None, attention_mask=None, token_type_ids=None, position_ids=None, head_mask=None, inputs_embeds=None, encoder_hidden_states=None, encoder_attention_mask=None, output_attentions=None, return_dict=None, output_hidden_states=None) L79

class RobertaSeriesModelWithTransformation(BertSeriesModelWithTransformation) L135
