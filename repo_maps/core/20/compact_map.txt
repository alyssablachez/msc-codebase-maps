homeassistant/bootstrap.py
from_config_dict(config, hass=None, config_dir=None, enable_log=True, verbose=False, skip_pip=False, log_rotate_days=None) L34
"Try to configure Home Assistant from a config dict."
async_from_config_dict(config, hass, config_dir=None, enable_log=True, verbose=False, skip_pip=False, log_rotate_days=None) L64
"Try to configure Home Assistant from a config dict."
from_config_file(config_path, hass=None, verbose=False, skip_pip=True, log_rotate_days=None) L151
"Read the configuration file and try to start all the functionality."
async_from_config_file(config_path, hass, verbose=False, skip_pip=True, log_rotate_days=None) L174
"Read the configuration file and try to start all the functionality."
async_enable_logging(hass, verbose=False, log_rotate_days=None) L207
"Setup the logging."
async_stop_async_handler(event) L263
"Cleanup async handler."
mount_local_lib_path(config_dir) L280
"Add local library to Python Path."

homeassistant/components/__init__.py
is_on(hass, entity_id=None) L30
"Load up the module to call the is_on method."
turn_on(hass, entity_id=None, **service_data) L59
"Turn specified entity on if possible."
turn_off(hass, entity_id=None, **service_data) L67
"Turn specified entity off."
toggle(hass, entity_id=None, **service_data) L75
"Toggle specified entity."
stop(hass) L83
"Stop Home Assistant."
restart(hass) L88
"Stop Home Assistant."
check_config(hass) L93
"Check the config files."
reload_core_config(hass) L98
"Reload the core config."
async_setup(hass, config) L104
"Setup general services related to Home Assistant."
async_handle_turn_service(service) L107
"Method to handle calls to homeassistant.turn_on/off."
async_handle_core_service(call) L152
"Service handler for handling core services."
async_handle_reload_config(call) L182
"Service handler for reloading core config."

homeassistant/components/alarm_control_panel/__init__.py
class AlarmControlPanel(Entity) L139
  "An abstract class for alarm control devices."
  code_format(self) L143
  "Regex for code format or None if no code is required."
  changed_by(self) L148
  "Last change triggered by."
  alarm_disarm(self, code=None) L152
  "Send disarm command."
  async_alarm_disarm(self, code=None) L156
  "Send disarm command."
  alarm_arm_home(self, code=None) L164
  "Send arm home command."
  async_alarm_arm_home(self, code=None) L168
  "Send arm home command."
  alarm_arm_away(self, code=None) L176
  "Send arm away command."
  async_alarm_arm_away(self, code=None) L180
  "Send arm away command."
  alarm_trigger(self, code=None) L188
  "Send alarm trigger command."
  async_alarm_trigger(self, code=None) L192
  "Send alarm trigger command."
  state_attributes(self) L201
  "Return the state attributes."

alarm_disarm(hass, code=None, entity_id=None) L47
"Send the alarm the command for disarm."
alarm_arm_home(hass, code=None, entity_id=None) L58
"Send the alarm the command for arm home."
alarm_arm_away(hass, code=None, entity_id=None) L69
"Send the alarm the command for arm away."
alarm_trigger(hass, code=None, entity_id=None) L80
"Send the alarm the command for trigger."
async_setup(hass, config) L92
"Track states and offer events for sensors."
async_alarm_service_handler(service) L100
"Map services to methods on Alarm."

homeassistant/components/alarm_control_panel/alarmdecoder.py
class AlarmDecoderAlarmPanel(alarm.AlarmControlPanel) L38
  "Representation of an AlarmDecoder-based alarm panel."
  __init__(self, name, hass) L41
  "Initialize the alarm panel."
  async_added_to_hass(self) L50
  "Register callbacks."
  _message_callback(self, message) L56
  name(self) L75
  "Return the name of the device."
  should_poll(self) L80
  "No polling needed."
  code_format(self) L85
  "Regex for code format or None if no code is required."
  state(self) L90
  "Return the state of the device."
  async_alarm_disarm(self, code=None) L95
  "Send disarm command."
  async_alarm_arm_away(self, code=None) L104
  "Send arm away command."
  async_alarm_arm_home(self, code=None) L113
  "Send arm home command."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L27
"Perform the setup for AlarmDecoder alarm panels."

homeassistant/components/alarm_control_panel/alarmdotcom.py
class AlarmDotCom(alarm.AlarmControlPanel) L47
  "Represent an Alarm.com status."
  __init__(self, hass, name, code, username, password) L50
  "Initialize the Alarm.com status."
  async_login(self) L67
  "Login to Alarm.com."
  async_update(self) L72
  "Fetch the latest state."
  name(self) L78
  "Return the name of the alarm."
  code_format(self) L83
  "One or more characters if code is defined."
  state(self) L88
  "Return the state of the device."
  async_alarm_disarm(self, code=None) L100
  "Send disarm command."
  async_alarm_arm_home(self, code=None) L106
  "Send arm hom command."
  async_alarm_arm_away(self, code=None) L112
  "Send arm away command."
  _validate_code(self, code) L117
  "Validate given code."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L35
"Setup a Alarm.com control panel."

homeassistant/components/alarm_control_panel/concord232.py
class Concord232Alarm(alarm.AlarmControlPanel) L53
  "Represents the Concord232-based alarm panel."
  __init__(self, hass, url, name) L56
  "Initialize the Concord232 alarm panel."
  name(self) L76
  "Return the name of the device."
  code_format(self) L81
  "The characters if code is defined."
  state(self) L86
  "Return the state of the device."
  update(self) L90
  "Update values from API."
  alarm_disarm(self, code=None) L114
  "Send disarm command."
  alarm_arm_home(self, code=None) L118
  "Send arm home command."
  alarm_arm_away(self, code=None) L122
  "Send arm away command."

setup_platform(hass, config, add_devices, discovery_info=None) L38
"Set up the Concord232 alarm control panel platform."

homeassistant/components/alarm_control_panel/demo.py
setup_platform(hass, config, add_devices, discovery_info=None) L10
"Setup the Demo alarm control panel platform."

homeassistant/components/alarm_control_panel/envisalink.py
class EnvisalinkAlarm(EnvisalinkDevice, alarm.AlarmControlPanel) L84
  "Representation of an Envisalink-based alarm panel."
  __init__(self, hass, partition_number, alarm_name, code, panic_type, info, controller) L87
  "Initialize the alarm panel."
  async_added_to_hass(self) L98
  "Register callbacks."
  _update_callback(self, partition) L106
  "Update HA state, if needed."
  code_format(self) L112
  "Regex for code format or None if no code is required."
  state(self) L120
  "Return the state of the device."
  async_alarm_disarm(self, code=None) L139
  "Send disarm command."
  async_alarm_arm_home(self, code=None) L149
  "Send arm home command."
  async_alarm_arm_away(self, code=None) L159
  "Send arm away command."
  async_alarm_trigger(self, code=None) L169
  "Alarm trigger command."
  async_alarm_keypress(self, keypress=None) L174
  "Send custom keypress."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L38
"Perform the setup for Envisalink alarm panels."
alarm_keypress_handler(service) L61
"Map services to methods on Alarm."

homeassistant/components/alarm_control_panel/manual.py
class ManualAlarm(alarm.AlarmControlPanel) L53
  "Represents an alarm status."
  __init__(self, hass, name, code, pending_time, trigger_time, disarm_after_trigger) L63
  "Initalize the manual alarm panel."
  should_poll(self) L77
  "No polling needed."
  name(self) L82
  "Return the name of the device."
  state(self) L87
  "Return the state of the device."
  code_format(self) L108
  "One or more characters."
  alarm_disarm(self, code=None) L112
  "Send disarm command."
  alarm_arm_home(self, code=None) L121
  "Send arm home command."
  alarm_arm_away(self, code=None) L135
  "Send arm away command."
  alarm_trigger(self, code=None) L149
  "Send alarm trigger command."
  _validate_code(self, code, state) L165
  "Validate given code."

setup_platform(hass, config, add_devices, discovery_info=None) L41
"Setup the manual alarm platform."

homeassistant/components/alarm_control_panel/mqtt.py
class MqttAlarm(alarm.AlarmControlPanel) L60
  "Representation of a MQTT alarm status."
  __init__(self, name, state_topic, command_topic, qos, payload_disarm, payload_arm_home, payload_arm_away, code) L63
  "Initalize the MQTT alarm panel."
  async_added_to_hass(self) L76
  "Subscribe mqtt events."
  message_received(topic, payload, qos) L82
  "A new MQTT message has been received."
  should_poll(self) L96
  "No polling needed."
  name(self) L101
  "Return the name of the device."
  state(self) L106
  "Return the state of the device."
  code_format(self) L111
  "One or more characters if code is defined."
  async_alarm_disarm(self, code=None) L116
  "Send disarm command."
  async_alarm_arm_home(self, code=None) L127
  "Send arm home command."
  async_alarm_arm_away(self, code=None) L138
  "Send arm away command."
  _validate_code(self, code, state) L148
  "Validate given code."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L47
"Setup the MQTT platform."

homeassistant/components/alarm_control_panel/nx584.py
class NX584Alarm(alarm.AlarmControlPanel) L49
  "Represents the NX584-based alarm panel."
  __init__(self, hass, url, name) L52
  "Initalize the nx584 alarm panel."
  name(self) L66
  "Return the name of the device."
  code_format(self) L71
  "The characters if code is defined."
  state(self) L76
  "Return the state of the device."
  update(self) L80
  "Process new events from panel."
  alarm_disarm(self, code=None) L111
  "Send disarm command."
  alarm_arm_home(self, code=None) L115
  "Send arm home command."
  alarm_arm_away(self, code=None) L119
  "Send arm away command."

setup_platform(hass, config, add_devices, discovery_info=None) L34
"Setup nx584 platform."

homeassistant/components/alarm_control_panel/simplisafe.py
class SimpliSafeAlarm(alarm.AlarmControlPanel) L69
  "Representation a SimpliSafe alarm."
  __init__(self, simplisafe, name, code) L72
  "Initialize the SimpliSafe alarm."
  name(self) L79
  "Return the name of the device."
  code_format(self) L87
  "One or more characters if code is defined."
  state(self) L92
  "Return the state of the device."
  device_state_attributes(self) L106
  "Return the state attributes."
  update(self) L117
  "Update alarm status."
  alarm_disarm(self, code=None) L121
  "Send disarm command."
  alarm_arm_home(self, code=None) L128
  "Send arm home command."
  alarm_arm_away(self, code=None) L135
  "Send arm away command."
  _validate_code(self, code, state) L142
  "Validate given code."

setup_platform(hass, config, add_devices, discovery_info=None) L37
"Set up the SimpliSafe platform."
logout(event) L62
"Logout of the SimpliSafe API."

homeassistant/components/alarm_control_panel/totalconnect.py
class TotalConnect(alarm.AlarmControlPanel) L37
  "Represent an TotalConnect status."
  __init__(self, name, username, password) L40
  "Initialize the TotalConnect status."
  name(self) L53
  "Return the name of the device."
  state(self) L58
  "Return the state of the device."
  update(self) L62
  "Return the state of the device."
  alarm_disarm(self, code=None) L77
  "Send disarm command."
  alarm_arm_home(self, code=None) L81
  "Send arm home command."
  alarm_arm_away(self, code=None) L85
  "Send arm away command."

setup_platform(hass, config, add_devices, discovery_info=None) L27
"Setup a TotalConnect control panel."

homeassistant/components/alarm_control_panel/verisure.py
class VerisureAlarm(alarm.AlarmControlPanel) L31
  "Represent a Verisure alarm status."
  __init__(self, device_id) L34
  "Initalize the Verisure alarm panel."
  name(self) L42
  "Return the name of the device."
  state(self) L47
  "Return the state of the device."
  available(self) L52
  "Return True if entity is available."
  code_format(self) L57
  "The code format as regex."
  changed_by(self) L62
  "Last change triggered by."
  update(self) L66
  "Update alarm status."
  alarm_disarm(self, code=None) L82
  "Send disarm command."
  alarm_arm_home(self, code=None) L88
  "Send arm home command."
  alarm_arm_away(self, code=None) L94
  "Send arm away command."

setup_platform(hass, config, add_devices, discovery_info=None) L19
"Setup the Verisure platform."

homeassistant/components/alarm_control_panel/wink.py
class WinkCameraDevice(WinkDevice, alarm.AlarmControlPanel) L37
  "Representation a Wink camera alarm."
  __init__(self, wink, hass) L40
  "Initialize the Wink alarm."
  state(self) L45
  "Return the state of the device."
  alarm_disarm(self, code=None) L58
  "Send disarm command."
  alarm_arm_home(self, code=None) L62
  "Send arm home command."
  alarm_arm_away(self, code=None) L66
  "Send arm away command."
  device_state_attributes(self) L71
  "Return the state attributes."

setup_platform(hass, config, add_devices, discovery_info=None) L22
"Setup the Wink platform."

homeassistant/components/alarmdecoder.py
async_setup(hass, config) L89
"Common setup for AlarmDecoder devices."
handle_open(device) L108
"Callback for a successful connection."
stop_alarmdecoder(event) L115
"Callback to handle shutdown alarmdecoder."
handle_message(sender, message) L121
"Callback to handle message from alarmdecoder."
zone_fault_callback(sender, zone) L125
"Callback to handle zone fault from alarmdecoder."
zone_restore_callback(sender, zone) L129
"Callback to handle zone restore from alarmdecoder."

homeassistant/components/alert.py
class Alert(ToggleEntity) L154
  "Representation of an alert."
  __init__(self, hass, entity_id, name, watched_entity_id, state, repeat, skip_first, notifiers, can_ack) L157
  "Initialize the alert."
  name(self) L179
  "Return the name of the alert."
  should_poll(self) L184
  "HASS need not poll these entities."
  state(self) L189
  "Return the alert status."
  hidden(self) L198
  "Hide the alert when it is not firing."
  watched_entity_change(self, entity, from_state, to_state) L203
  "Determine if the alert should start or stop."
  begin_alerting(self) L212
  "Begin the alert procedures."
  end_alerting(self) L227
  "End the alert procedures."
  _schedule_notify(self) L236
  "Schedule a notification."
  _notify(self, *args) L245
  "Send the alert notification."
  async_turn_on(self) L258
  "Async Unacknowledge alert."
  async_turn_off(self) L265
  "Async Acknowledge alert."
  async_toggle(self) L272
  "Async toggle alert."

is_on(hass, entity_id) L57
"Return if the alert is firing and not acknowledged."
turn_on(hass, entity_id) L62
"Reset the alert."
async_turn_on(hass, entity_id) L68
"Async reset the alert."
turn_off(hass, entity_id) L75
"Acknowledge alert."
async_turn_off(hass, entity_id) L81
"Async acknowledge the alert."
toggle(hass, entity_id) L88
"Toggle acknowledgement of alert."
async_toggle(hass, entity_id) L94
"Async toggle acknowledgement of alert."
async_setup(hass, config) L102
"Set up the Alert component."
async_handle_alert_service(service_call) L108
"Handle calls to alert services."

homeassistant/components/alexa.py
class SpeechType(enum.Enum) L58
  "The Alexa speech types."

class CardType(enum.Enum) L65
  "The Alexa card types."

class AlexaIntentsView(HomeAssistantView) L113
  "Handle Alexa requests."
  __init__(self, hass, intents) L119
  "Initialize Alexa view."
  post(self, request) L134
  "Handle Alexa."

class AlexaResponse(object) L195
  "Help generating the response for Alexa."
  __init__(self, hass, intent=None) L198
  "Initialize the response."
  add_card(self, card_type, title, content) L213
  "Add a card to the response."
  add_speech(self, speech_type, text) L229
  "Add speech to the response."
  add_reprompt(self, speech_type, text) L243
  "Add reprompt if user does not answer."
  as_dict(self) L254
  "Return response in an Alexa valid dict."

class AlexaFlashBriefingView(HomeAssistantView) L278
  "Handle Alexa Flash Briefing skill requests."
  __init__(self, hass, flash_briefings) L284
  "Initialize Alexa view."
  get(self, request, briefing_id) L291
  "Handle Alexa Flash Briefing request."

setup(hass, config) L102
"Activate Alexa component."

homeassistant/components/android_ip_webcam.py
class AndroidIPCamEntity(Entity) L250
  "The Android device running IP Webcam."
  __init__(self, host, ipcam) L253
  "Initialize the data oject."
  async_added_to_hass(self) L259
  "Register update dispatcher."
  async_ipcam_update(host) L262
  "Update callback."
  should_poll(self) L272
  "Return True if entity has to be polled for state."
  available(self) L277
  "Return True if entity is available."
  device_state_attributes(self) L282
  "Return the state attributes."

async_setup(hass, config) L153
"Set up the IP Webcam component."
async_setup_ipcamera(cam_config) L161
"Set up an IP camera."
async_update_data(now) L192
"Update data from IP camera in SCAN_INTERVAL."

homeassistant/components/apcupsd.py
class APCUPSdData(object) L61
  "Stores the data retrieved from APCUPSd."
  __init__(self, host, port) L68
  "Initialize the data oject."
  status(self) L78
  "Get latest update if throttle allows."
  _get_status(self) L83
  "Get the status from APCUPSd and parse it into a dict."
  update(self, **kwargs) L88
  "Fetch the latest status from APCUPSd."

setup(hass, config) L41
"Use config values to set up a function enabling status retrieval."

homeassistant/components/api.py
class APIStatusView(HomeAssistantView) L60
  "View to handle Status requests."
  get(self, request) L67
  "Retrieve if API is running."

class APIEventStream(HomeAssistantView) L72
  "View to handle EventStream requests."
  get(self, request) L79
  "Provide a streaming interface for the event bus."
  forward_events(event) L91
  "Forward events to the open request."

class APIConfigView(HomeAssistantView) L145
  "View to handle Config requests."
  get(self, request) L152
  "Get current configuration."

class APIDiscoveryView(HomeAssistantView) L157
  "View to provide discovery info."
  get(self, request) L165
  "Get discovery info."

class APIStatesView(HomeAssistantView) L177
  "View to handle States requests."
  get(self, request) L184
  "Get current states."

class APIEntityStateView(HomeAssistantView) L189
  "View to handle EntityState requests."
  get(self, request, entity_id) L196
  "Retrieve state of entity."
  post(self, request, entity_id) L205
  "Update state of entity."
  delete(self, request, entity_id) L236
  "Remove entity."

class APIEventListenersView(HomeAssistantView) L244
  "View to handle EventListeners requests."
  get(self, request) L251
  "Get event listeners."

class APIEventView(HomeAssistantView) L256
  "View to handle Event requests."
  post(self, request, event_type) L263
  "Fire events."

class APIServicesView(HomeAssistantView) L287
  "View to handle Services requests."
  get(self, request) L294
  "Get registered services."

class APIDomainServicesView(HomeAssistantView) L299
  "View to handle DomainServices requests."
  post(self, request, domain, service) L306
  "Call a service."

class APIComponentsView(HomeAssistantView) L321
  "View to handle Components requests."
  get(self, request) L328
  "Get current loaded components."

class APITemplateView(HomeAssistantView) L333
  "View to handle requests."
  post(self, request) L340
  "Render a template."

setup(hass, config) L39
"Register the API with the HTTP interface."
async_services_json(hass) L351
"Generate services data to JSONify."
async_events_json(hass) L357
"Generate event data to JSONify."

homeassistant/components/apiai.py
class ApiaiIntentsView(HomeAssistantView) L54
  "Handle API.AI requests."
  __init__(self, hass, intents) L60
  "Initialize API.AI view."
  post(self, request) L76
  "Handle API.AI."

class ApiaiResponse(object) L145
  "Help generating the response for API.AI."
  __init__(self, parameters) L148
  "Initialize the response."
  add_speech(self, text) L157
  "Add speech to the response."
  as_dict(self) L166
  "Return response in an API.AI valid dict."

setup(hass, config) L45
"Activate API.AI component."

homeassistant/components/arduino.py
class ArduinoBoard(object) L65
  "Representation of an Arduino board."
  __init__(self, port) L68
  "Initialize the board."
  set_mode(self, pin, direction, mode) L74
  "Set the mode and the direction of a given pin."
  get_analog_inputs(self) L92
  "Get the values from the pins."
  set_digital_out_high(self, pin) L97
  "Set a given digital pin to high."
  set_digital_out_low(self, pin) L101
  "Set a given digital pin to low."
  get_digital_in(self, pin) L105
  "Get the value from a given digital pin."
  get_analog_in(self, pin) L109
  "Get the value from a given analog pin."
  get_firmata(self) L113
  "Return the version of the Firmata firmware."
  disconnect(self) L117
  "Disconnect the board and close the serial connection."

setup(hass, config) L31
"Set up the Arduino component."
stop_arduino(event) L52
"Stop the Arduino service."
start_arduino(event) L56
"Start the Arduino service."

homeassistant/components/automation/__init__.py
class AutomationEntity(ToggleEntity) L221
  "Entity to show status of entity."
  __init__(self, name, async_attach_triggers, cond_func, async_action, hidden, initial_state) L224
  "Initialize an automation entity."
  name(self) L237
  "Name of the automation."
  should_poll(self) L242
  "No polling needed for automation entities."
  state_attributes(self) L247
  "Return the entity state attributes."
  hidden(self) L254
  "Return True if the automation entity should be hidden from UIs."
  is_on(self) L259
  "Return True if entity is on."
  async_added_to_hass(self) L264
  "Startup with initial state or previous state."
  async_enable_automation(event) L290
  "Start automation on startup."
  async_turn_on(self, **kwargs) L302
  "Turn the entity on and update the state."
  async_turn_off(self, **kwargs) L310
  "Turn the entity off."
  async_trigger(self, variables, skip_condition=False) L320
  "Trigger automation."
  async_remove(self) L331
  "Remove automation from HASS."
  async_enable(self) L337
  "Enable this automation entity."

_platform_validator(config) L59
"Validate it is a valid  platform."
is_on(hass, entity_id) L104
"Return true if specified automation entity_id is on."
turn_on(hass, entity_id=None) L113
"Turn on specified automation or all."
turn_off(hass, entity_id=None) L119
"Turn off specified automation or all."
toggle(hass, entity_id=None) L125
"Toggle specified automation or all."
trigger(hass, entity_id=None) L131
"Trigger specified automation or all."
reload(hass) L137
"Reload the automation from config."
async_setup(hass, config) L143
"Setup the automation."
trigger_service_handler(service_call) L159
"Handle automation triggers."
turn_onoff_service_handler(service_call) L170
"Handle automation turn on/off service calls."
toggle_service_handler(service_call) L181
"Handle automation toggle service calls."
reload_service_handler(service_call) L194
"Remove all automations and load new ones from config."
_async_process_config(hass, config, component) L351
"Process config and add automations."
cond_func(variables) L377
"Condition will always pass."
_async_get_action(hass, config, name) L397
"Return an action based on a configuration."
action(entity_id, variables) L402
"Action to be executed."
_async_process_if(hass, config, p_config) L412
"Process if checks."
if_action(variables=None) L424
"AND all conditions."
_async_process_trigger(hass, config, trigger_configs, name, action) L432
"Setup the triggers."
remove_triggers() L458
"Remove attached triggers."

homeassistant/components/automation/event.py
async_trigger(hass, config, action) L29
"Listen for events based on configuration."
handle_event(event) L48
"Listen for events and calls the action when data matches."

homeassistant/components/automation/homeassistant.py
async_trigger(hass, config, action) L27
"Listen for events based on configuration."
hass_shutdown(event) L33
"Called when Home Assistant is shutting down."

homeassistant/components/automation/litejet.py
async_trigger(hass, config, action) L37
"Listen for events based on configuration."
call_action() L46
"Call action with right context."
pressed_more_than_satisfied(now) L63
"Handle the LiteJet's switch's button pressed >= held_more_than."
pressed() L67
"Handle the press of the LiteJet switch's button."
released() L80
"Handle the release of the LiteJet switch's button."
async_remove() L96
"Remove all subscriptions used for this trigger."

homeassistant/components/automation/mqtt.py
async_trigger(hass, config, action) L29
"Listen for state changes based on configuration."
mqtt_automation_listener(msg_topic, msg_payload, qos) L35
"Listen for MQTT messages."

homeassistant/components/automation/numeric_state.py
async_trigger(hass, config, action) L31
"Listen for state changes based on configuration."
state_automation_listener(entity, from_s, to_s) L41
"Listen for state changes and calls action."

homeassistant/components/automation/state.py
async_trigger(hass, config, action) L39
"Listen for state changes based on configuration."
clear_listener() L49
"Clear all unsub listener."
state_automation_listener(entity, from_s, to_s) L62
"Listen for state changes and calls action."
call_action() L66
"Call action with right context."
state_for_listener(now) L83
"Fire on state changes after a delay and calls action."
state_for_cancel_listener(entity, inner_from_s, inner_to_s) L91
"Fire on changes and cancel for listener if changed."
async_remove() L110
"Remove state listeners async."

homeassistant/components/automation/sun.py
async_trigger(hass, config, action) L31
"Listen for events based on configuration."
call_action() L37
"Call action with right context."

homeassistant/components/automation/template.py
async_trigger(hass, config, action) L27
"Listen for state changes based on configuration."
template_listener(entity_id, from_s, to_s) L33
"Listen for state changes and calls action."

homeassistant/components/automation/time.py
async_trigger(hass, config, action) L34
"Listen for state changes based on configuration."
time_automation_listener(now) L45
"Listen for time changes and calls action."

homeassistant/components/automation/zone.py
async_trigger(hass, config, action) L31
"Listen for state changes based on configuration."
zone_automation_listener(entity, from_s, to_s) L38
"Listen for state changes and calls action."

homeassistant/components/bbb_gpio.py
setup(hass, config) L20
"Set up the BeagleBone Black GPIO component."
cleanup_gpio(event) L25
"Stuff to do before stopping."
prepare_gpio(event) L29
"Stuff to do when home assistant starts."
setup_output(pin) L39
"Setup a GPIO as output."
setup_input(pin, pull_mode) L46
"Setup a GPIO as input."
write_output(pin, value) L55
"Write a value to a GPIO."
read_input(pin) L62
"Read a value from a GPIO."
edge_detect(pin, event_callback, bounce) L69
"Add detection for RISING and FALLING events."

homeassistant/components/binary_sensor/__init__.py
class BinarySensorDevice(Entity) L55
  "Represent a binary sensor."
  is_on(self) L59
  "Return True if the binary sensor is on."
  state(self) L64
  "Return the state of the binary sensor."
  device_class(self) L70
  "Return the class of this device, from component DEVICE_CLASSES."

async_setup(hass, config) L45
"Track states and offer events for binary sensors."

homeassistant/components/binary_sensor/alarmdecoder.py
class AlarmDecoderBinarySensor(BinarySensorDevice) L50
  "Representation of an AlarmDecoder binary sensor."
  __init__(self, hass, zone_number, zone_name, zone_type) L53
  "Initialize the binary_sensor."
  async_added_to_hass(self) L64
  "Register callbacks."
  state(self) L73
  "Return the state of the binary sensor."
  name(self) L81
  "Return the name of the entity."
  icon(self) L86
  "Icon for device by its type."
  should_poll(self) L97
  "No polling needed."
  is_on(self) L102
  "Return true if sensor is on."
  device_class(self) L107
  "Return the class of this sensor, from DEVICE_CLASSES."
  _fault_callback(self, zone) L112
  "Update the zone's state, if needed."
  _restore_callback(self, zone) L119
  "Update the zone's state, if needed."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L29
"Setup AlarmDecoder binary sensor devices."

homeassistant/components/binary_sensor/android_ip_webcam.py
class IPWebcamBinarySensor(AndroidIPCamEntity, BinarySensorDevice) L30
  "Represents an IP Webcam binary sensor."
  __init__(self, name, host, ipcam, sensor) L33
  "Initialize the binary sensor."
  name(self) L44
  "Return the name of the binary sensor, if any."
  is_on(self) L49
  "True if the binary sensor is on."
  async_update(self) L54
  "Retrieve latest state."
  device_class(self) L60
  "Return the class of this device, from component DEVICE_CLASSES."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L17
"Setup IP Webcam binary sensors."

homeassistant/components/binary_sensor/apcupsd.py
class OnlineStatus(BinarySensorDevice) L28
  "Representation of an UPS online status."
  __init__(self, config, data) L31
  "Initialize the APCUPSd binary device."
  name(self) L39
  "Return the name of the UPS online status sensor."
  is_on(self) L44
  "Return true if the UPS is online, else false."
  update(self) L48
  "Get the status report from APCUPSd and set this entity's state."

setup_platform(hass, config, add_entities, discovery_info=None) L23
"Setup an Online Status binary sensor."

homeassistant/components/binary_sensor/arest.py
class ArestBinarySensor(BinarySensorDevice) L57
  "Implement an aREST binary sensor for a pin."
  __init__(self, arest, resource, name, device_class, pin) L60
  "Initialize the aREST device."
  name(self) L76
  "Return the name of the binary sensor."
  is_on(self) L81
  "Return true if the binary sensor is on."
  device_class(self) L86
  "Return the class of this sensor."
  update(self) L90
  "Get the latest data from aREST API."

class ArestData(object) L95
  "Class for handling the data retrieval for pins."
  __init__(self, resource, pin) L98
  "Initialize the aREST data object."
  update(self) L105
  "Get the latest data from aREST device."

setup_platform(hass, config, add_devices, discovery_info=None) L34
"Set up the aREST binary sensor."

homeassistant/components/binary_sensor/aurora.py
class AuroraSensor(BinarySensorDevice) L59
  "Implementation of an aurora sensor."
  __init__(self, aurora_data, name) L62
  "Initialize the sensor."
  name(self) L68
  "Return the name of the sensor."
  is_on(self) L73
  "Return true if aurora is visible."
  device_class(self) L78
  "Return the class of this device."
  device_state_attributes(self) L83
  "Return the state attributes."
  update(self) L93
  "Get the latest data from Aurora API and updates the states."

class AuroraData(object) L98
  "Get aurora forecast."
  __init__(self, latitude, longitude, threshold) L101
  "Initialize the data object."
  update(self) L117
  "Get the latest data from the Aurora service."
  get_aurora_forecast(self) L133
  "Get forecast data and parse for given long/lat."

setup_platform(hass, config, add_devices, discovery_info=None) L35
"Set up the aurora sensor."

homeassistant/components/binary_sensor/bbb_gpio.py
class BBBGPIOBinarySensor(BinarySensorDevice) L55
  "Represent a binary sensor that uses Beaglebone Black GPIO."
  __init__(self, pin, params) L58
  "Initialize the Beaglebone Black binary sensor."
  read_gpio(pin) L69
  "Read state from GPIO."
  should_poll(self) L77
  "No polling needed."
  name(self) L82
  "Return the name of the sensor."
  is_on(self) L87
  "Return the state of the entity."

setup_platform(hass, config, add_devices, discovery_info=None) L44
"Setup the Beaglebone Black GPIO devices."

homeassistant/components/binary_sensor/blink.py
class BlinkCameraMotionSensor(BinarySensorDevice) L26
  "A representation of a Blink binary sensor."
  __init__(self, name, data) L29
  "Initialize the sensor."
  name(self) L37
  "Return the name of the blink sensor."
  is_on(self) L42
  "Return the status of the sensor."
  update(self) L46
  "Update sensor state."

class BlinkSystemSensor(BinarySensorDevice) L52
  "A representation of a Blink system sensor."
  __init__(self, data) L55
  "Initialize the sensor."
  name(self) L62
  "Return the name of the blink sensor."
  is_on(self) L67
  "Return the status of the sensor."
  update(self) L71
  "Update sensor state."

setup_platform(hass, config, add_devices, discovery_info=None) L13
"Set up the blink binary sensors."

homeassistant/components/binary_sensor/bloomsky.py
class BloomSkySensor(BinarySensorDevice) L44
  "Represent a single binary sensor in a BloomSky device."
  __init__(self, bs, device, sensor_name) L47
  "Initialize a BloomSky binary sensor."
  name(self) L57
  "The name of the BloomSky device and this sensor."
  unique_id(self) L62
  "Return the unique ID for this sensor."
  device_class(self) L67
  "Return the class of this sensor, from DEVICE_CLASSES."
  is_on(self) L72
  "Return true if binary sensor is on."
  update(self) L76
  "Request an update from the BloomSky API."

setup_platform(hass, config, add_devices, discovery_info=None) L33
"Setup the available BloomSky weather binary sensors."

homeassistant/components/binary_sensor/command_line.py
class CommandBinarySensor(BinarySensorDevice) L58
  "Represent a command line binary sensor."
  __init__(self, hass, data, name, device_class, payload_on, payload_off, value_template) L61
  "Initialize the Command line binary sensor."
  name(self) L75
  "Return the name of the sensor."
  is_on(self) L80
  "Return true if the binary sensor is on."
  device_class(self) L85
  "Return the class of the binary sensor."
  update(self) L89
  "Get the latest data and updates the state."

setup_platform(hass, config, add_devices, discovery_info=None) L41
"Setup the Command line Binary Sensor."

homeassistant/components/binary_sensor/concord232.py
class Concord232ZoneSensor(BinarySensorDevice) L92
  "Representation of a Concord232 zone as a sensor."
  __init__(self, hass, client, zone, zone_type) L95
  "Initialize the Concord232 binary sensor."
  device_class(self) L105
  "Return the class of this sensor, from DEVICE_CLASSES."
  should_poll(self) L110
  "No polling needed."
  name(self) L115
  "Return the name of the binary sensor."
  is_on(self) L120
  "Return true if the binary sensor is on."
  update(self) L125
  ""Get updated stats from API."

setup_platform(hass, config, add_devices, discovery_info=None) L45
"Set up the Concord232 binary sensor platform."
get_opening_type(zone) L79
"Helper function to try to guess sensor type from name."

homeassistant/components/binary_sensor/demo.py
class DemoBinarySensor(BinarySensorDevice) L18
  "A Demo binary sensor."
  __init__(self, name, state, device_class) L21
  "Initialize the demo sensor."
  device_class(self) L28
  "Return the class of this sensor."
  should_poll(self) L33
  "No polling needed for a demo binary sensor."
  name(self) L38
  "Return the name of the binary sensor."
  is_on(self) L43
  "Return true if the binary sensor is on."

setup_platform(hass, config, add_devices, discovery_info=None) L10
"Setup the Demo binary sensor platform."

homeassistant/components/binary_sensor/digital_ocean.py
class DigitalOceanBinarySensor(BinarySensorDevice) L48
  "Representation of a Digital Ocean droplet sensor."
  __init__(self, do, droplet_id) L51
  "Initialize a new Digital Ocean sensor."
  name(self) L59
  "Return the name of the sensor."
  is_on(self) L64
  "Return true if the binary sensor is on."
  device_class(self) L69
  "Return the class of this sensor."
  device_state_attributes(self) L74
  "Return the state attributes of the Digital Ocean droplet."
  update(self) L88
  "Update state of sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L31
"Set up the Digital Ocean droplet sensor."

homeassistant/components/binary_sensor/ecobee.py
class EcobeeBinarySensor(BinarySensorDevice) L32
  "Representation of an Ecobee sensor."
  __init__(self, sensor_name, sensor_index) L35
  "Initialize the sensor."
  name(self) L45
  "Return the name of the Ecobee sensor."
  is_on(self) L50
  "Return the status of the sensor."
  unique_id(self) L55
  "Return the unique ID of this sensor."
  device_class(self) L60
  "Return the class of this sensor, from DEVICE_CLASSES."
  update(self) L64
  "Get the latest state of the sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L15
"Setup the Ecobee sensors."

homeassistant/components/binary_sensor/enocean.py
class EnOceanBinarySensor(enocean.EnOceanDevice, BinarySensorDevice) L41
  "Representation of EnOcean binary sensors such as wall switches."
  __init__(self, dev_id, devname, device_class) L44
  "Initialize the EnOcean binary sensor."
  name(self) L55
  "The default name for the binary sensor."
  device_class(self) L60
  "Return the class of this sensor."
  value_changed(self, value, value2) L64
  "Fire an event with the data that have changed."

setup_platform(hass, config, add_devices, discovery_info=None) L32
"Setup the Binary Sensor platform fo EnOcean."

homeassistant/components/binary_sensor/envisalink.py
class EnvisalinkBinarySensor(EnvisalinkDevice, BinarySensorDevice) L43
  "Representation of an Envisalink binary sensor."
  __init__(self, hass, zone_number, zone_name, zone_type, info, controller) L46
  "Initialize the binary_sensor."
  async_added_to_hass(self) L56
  "Register callbacks."
  device_state_attributes(self) L62
  "Return the state attributes."
  is_on(self) L69
  "Return true if sensor is on."
  device_class(self) L74
  "Return the class of this sensor, from DEVICE_CLASSES."
  _update_callback(self, zone) L79
  "Update the zone's state, if needed."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L23
"Setup Envisalink binary sensor devices."

homeassistant/components/binary_sensor/ffmpeg_motion.py
class FFmpegBinarySensor(FFmpegBase, BinarySensorDevice) L63
  "A binary sensor which use ffmpeg for noise detection."
  __init__(self, config) L66
  "Constructor for binary sensor noise detection."
  _async_callback(self, state) L75
  "HA-FFmpeg callback for noise detection."
  is_on(self) L81
  "True if the binary sensor is on."
  name(self) L86
  "Return the name of the entity."

class FFmpegMotion(FFmpegBinarySensor) L91
  "A binary sensor which use ffmpeg for noise detection."
  __init__(self, hass, manager, config) L94
  "Initialize ffmpeg motion binary sensor."
  _async_start_ffmpeg(self, entity_ids) L103
  "Start a FFmpeg instance."
  device_class(self) L126
  "Return the class of this sensor, from DEVICE_CLASSES."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L50
"Create the binary sensor."

homeassistant/components/binary_sensor/ffmpeg_noise.py
class FFmpegNoise(FFmpegBinarySensor) L60
  "A binary sensor which use ffmpeg for noise detection."
  __init__(self, hass, manager, config) L63
  "Initialize ffmpeg noise binary sensor."
  _async_start_ffmpeg(self, entity_ids) L72
  "Start a FFmpeg instance."
  device_class(self) L95
  "Return the class of this sensor, from DEVICE_CLASSES."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L47
"Create the binary sensor."

homeassistant/components/binary_sensor/flic.py
class FlicButton(BinarySensorDevice) L122
  "Representation of a flic button."
  __init__(self, hass, client, address, timeout, ignored_click_types) L125
  "Initialize the flic button."
  _create_channel(self) L144
  "Create a new connection channel to the button."
  name(self) L168
  "Return the name of the device."
  address(self) L173
  "Return the bluetooth address of the device."
  is_on(self) L178
  "Return true if sensor is on."
  should_poll(self) L183
  "No polling needed."
  device_state_attributes(self) L188
  "Return device specific state attributes."
  _queued_event_check(self, click_type, time_diff) L192
  "Generate a log message and returns true if timeout exceeded."
  _on_up_down(self, channel, click_type, was_queued, time_diff) L208
  "Update device state, if event was not queued."
  _on_click(self, channel, click_type, was_queued, time_diff) L218
  "Fire click event, if event was not queued."
  _connection_status_changed(self, channel, connection_status, disconnect_reason) L236
  "Remove device, if button disconnects."

setup_platform(hass, config, add_entities, discovery_info=None) L51
"Set up the flic platform."
new_button_callback(address) L67
"Set up newly verified button as device in Home Assistant."
get_info_callback(items) L81
"Add entities for already verified buttons."
start_scanning(config, add_entities, client) L91
"Start a new flic client for scanning and connecting to new buttons."
scan_completed_callback(scan_wizard, result, address, name) L97
"Restart scan wizard to constantly check for new buttons."
setup_button(hass, config, add_entities, client, address) L112
"Setup single button device."

homeassistant/components/binary_sensor/hikvision.py
class HikvisionData(object) L116
  "Hikvision device event stream object."
  __init__(self, hass, url, port, name, username, password) L119
  "Initialize the data oject."
  stop_hik(self, event) L138
  "Shutdown Hikvision subscriptions and subscription thread on exit."
  start_hik(self, event) L142
  "Start Hikvision event stream thread."
  sensors(self) L147
  "Return list of available sensors and their states."
  cam_id(self) L152
  "Return device id."
  name(self) L157
  "Return device name."
  type(self) L162
  "Return device type."
  get_attributes(self, sensor, channel) L166
  "Return attribute list for sensor/channel."

class HikvisionBinarySensor(BinarySensorDevice) L171
  "Representation of a Hikvision binary sensor."
  __init__(self, hass, sensor, channel, cam, delay) L174
  "Initialize the binary_sensor."
  _sensor_state(self) L198
  "Extract sensor state."
  _sensor_last_update(self) L202
  "Extract sensor last update time."
  name(self) L207
  "Return the name of the Hikvision sensor."
  unique_id(self) L212
  "Return an unique ID."
  is_on(self) L217
  "Return true if sensor is on."
  device_class(self) L222
  "Return the class of this sensor, from DEVICE_CLASSES."
  should_poll(self) L231
  "No polling needed."
  device_state_attributes(self) L236
  "Return the state attributes."
  _update_callback(self, msg) L246
  "Update the sensor's state, if needed."
  _delay_update(now) L252
  "Timer callback for sensor update."

setup_platform(hass, config, add_entities, discovery_info=None) L69
"Setup Hikvision binary sensor devices."

homeassistant/components/binary_sensor/homematic.py
class HMBinarySensor(HMDevice, BinarySensorDevice) L45
  "Representation of a binary Homematic device."
  is_on(self) L49
  "Return true if switch is on."
  device_class(self) L56
  "Return the class of this sensor, from DEVICE_CLASSES."
  _init_data_struct(self) L63
  "Generate a data struct (self._data) from the Homematic metadata."

setup_platform(hass, config, add_devices, discovery_info=None) L31
"Setup the Homematic binary sensor platform."

homeassistant/components/binary_sensor/insteon_plm.py
class InsteonPLMBinarySensorDevice(BinarySensorDevice) L38
  "A Class for an Insteon device."
  __init__(self, hass, plm, address, name) L41
  "Initialize the binarysensor."
  should_poll(self) L52
  "No polling needed."
  address(self) L57
  "Return the the address of the node."
  name(self) L62
  "Return the the name of the node."
  is_on(self) L67
  "Return the boolean response if the node is on."
  device_state_attributes(self) L74
  "Provide attributes for display on device card."
  get_attr(self, key) L79
  "Return specified attribute for this device."
  async_binarysensor_update(self, message) L84
  "Receive notification from transport that new data exists."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L20
"Set up the INSTEON PLM device class for the hass platform."

homeassistant/components/binary_sensor/iss.py
class IssBinarySensor(BinarySensorDevice) L58
  "Implementation of the ISS binary sensor."
  __init__(self, iss_data, name, show) L61
  "Initialize the sensor."
  name(self) L70
  "Return the name of the sensor."
  is_on(self) L75
  "Return true if the binary sensor is on."
  device_class(self) L80
  "Return the class of this sensor."
  device_state_attributes(self) L85
  "Return the state attributes."
  update(self) L101
  "Get the latest data from ISS API and updates the states."

class IssData(object) L106
  "Get data from the ISS API."
  __init__(self, latitude, longitude) L109
  "Initialize the data object."
  update(self) L119
  "Get the latest data from the ISS API."

setup_platform(hass, config, add_devices, discovery_info=None) L39
"Set up the ISS sensor."

homeassistant/components/binary_sensor/isy994.py
class ISYBinarySensorDevice(isy.ISYDevice, BinarySensorDevice) L52
  "Representation of an ISY994 binary sensor device."
  __init__(self, node) L55
  "Initialize the ISY994 binary sensor device."
  is_on(self) L60
  "Get whether the ISY994 binary sensor device is on."

class ISYBinarySensorProgram(ISYBinarySensorDevice) L65
  "Representation of an ISY994 binary sensor program."
  __init__(self, name, node) L68
  "Initialize the ISY994 binary sensor program."

setup_platform(hass, config, add_devices, discovery_info=None) L28
"Setup the ISY994 binary sensor platform."

homeassistant/components/binary_sensor/knx.py
class KNXSwitch(KNXGroupAddress, BinarySensorDevice) L18
  "Representation of a KNX binary sensor device."

setup_platform(hass, config, add_devices, discovery_info=None) L13
"Setup the KNX binary sensor platform."

homeassistant/components/binary_sensor/maxcube.py
class MaxCubeShutter(BinarySensorDevice) L33
  "Representation of a MAX!"
  __init__(self, hass, name, rf_address) L36
  "Initialize MAX!"
  should_poll(self) L45
  "Polling is required."
  name(self) L50
  "Return the name of the BinarySensorDevice."
  device_class(self) L55
  "Return the class of this sensor."
  is_on(self) L60
  "Return true if the binary sensor is on/open."
  update(self) L64
  "Get latest data from MAX!"

setup_platform(hass, config, add_devices, discovery_info=None) L16
"Iterate through all MAX!"

homeassistant/components/binary_sensor/modbus.py
class ModbusCoilSensor(BinarySensorDevice) L43
  "Modbus coil sensor."
  __init__(self, name, slave, coil) L46
  "Initialize the modbus coil sensor."
  is_on(self) L54
  "Return the state of the sensor."
  update(self) L58
  "Update the state of the sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L32
"Setup Modbus binary sensors."

homeassistant/components/binary_sensor/mqtt.py
class MqttBinarySensor(BinarySensorDevice) L60
  "Representation a binary sensor that is updated by MQTT."
  __init__(self, name, state_topic, device_class, qos, payload_on, payload_off, value_template) L63
  "Initialize the MQTT binary sensor."
  async_added_to_hass(self) L75
  "Subscribe mqtt events."
  message_received(topic, payload, qos) L81
  "A new MQTT message has been received."
  should_poll(self) L97
  "No polling needed."
  name(self) L102
  "Return the name of the binary sensor."
  is_on(self) L107
  "Return true if the binary sensor is on."
  device_class(self) L112
  "Return the class of this sensor."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L40
"Set up the MQTT binary sensor."

homeassistant/components/binary_sensor/mysensors.py
class MySensorsBinarySensor(mysensors.MySensorsDeviceEntity, BinarySensorDevice) L53
  "Represent the value of a MySensors Binary Sensor child node."
  is_on(self) L58
  "Return True if the binary sensor is on."
  device_class(self) L65
  "Return the class of this sensor, from DEVICE_CLASSES."

setup_platform(hass, config, add_devices, discovery_info=None) L18
"Setup the mysensors platform for sensors."

homeassistant/components/binary_sensor/nest.py
class NestBinarySensor(NestSensor, BinarySensorDevice) L91
  "Represents a Nest binary sensor."
  is_on(self) L95
  "True if the binary sensor is on."
  update(self) L99
  "Retrieve latest state."

class NestActivityZoneSensor(NestBinarySensor) L104
  "Represents a Nest binary sensor for activity in a zone."
  __init__(self, structure, device, zone) L107
  "Initialize the sensor."
  name(self) L114
  "Return the name of the nest, if any."
  update(self) L118
  "Retrieve latest state."

setup_platform(hass, config, add_devices, discovery_info=None) L45
"Setup Nest binary sensors."

homeassistant/components/binary_sensor/netatmo.py
class NetatmoBinarySensor(BinarySensorDevice) L119
  "Represent a single binary sensor in a Netatmo Camera device."
  __init__(self, data, camera_name, module_name, home, timeout, offset, camera_type, sensor) L122
  "Setup for access to the Netatmo camera events."
  name(self) L147
  "The name of the Netatmo device and this sensor."
  unique_id(self) L152
  "Return the unique ID for this sensor."
  device_class(self) L157
  "Return the class of this sensor, from DEVICE_CLASSES."
  is_on(self) L167
  "Return true if binary sensor is on."
  update(self) L171
  "Request an update from the Netatmo API."

setup_platform(hass, config, add_devices, discovery_info=None) L63
"Setup access to Netatmo binary sensor."

homeassistant/components/binary_sensor/nx584.py
class NX584ZoneSensor(BinarySensorDevice) L79
  "Representation of a NX584 zone as a sensor."
  __init__(self, zone, zone_type) L82
  "Initialize the nx594 binary sensor."
  device_class(self) L88
  "Return the class of this sensor, from DEVICE_CLASSES."
  should_poll(self) L93
  "No polling needed."
  name(self) L98
  "Return the name of the binary sensor."
  is_on(self) L103
  "Return true if the binary sensor is on."

class NX584Watcher(threading.Thread) L109
  "Event listener thread to process NX584 events."
  __init__(self, client, zone_sensors) L112
  "Initialize NX584 watcher thread."
  _process_zone_event(self, event) L119
  _process_events(self, events) L128
  _run(self) L133
  "Throw away any existing events so we don't replay history."
  run(self) L141
  "Run the watcher."

setup_platform(hass, config, add_devices, discovery_info=None) L43
"Setup the NX584 binary sensor platform."

homeassistant/components/binary_sensor/octoprint.py
class OctoPrintBinarySensor(BinarySensorDevice) L61
  "Representation an OctoPrint binary sensor."
  __init__(self, api, condition, sensor_type, sensor_name, unit, endpoint, group, tool=None) L64
  "Initialize a new OctoPrint sensor."
  name(self) L84
  "Return the name of the sensor."
  state(self) L89
  "Return the state of the sensor."
  is_on(self) L94
  "Return true if binary sensor is on."
  device_class(self) L102
  "Return the class of this sensor, from DEVICE_CLASSES."
  update(self) L106
  "Update state of sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L40
"Setup the available OctoPrint binary sensors."

homeassistant/components/binary_sensor/ping.py
class PingBinarySensor(BinarySensorDevice) L57
  "Representation of a Ping Binary sensor."
  __init__(self, name, ping) L60
  "Initialize the Ping Binary sensor."
  name(self) L66
  "Return the name of the device."
  device_class(self) L71
  "Return the class of this sensor."
  is_on(self) L76
  "Return true if the binary sensor is on."
  device_state_attributes(self) L81
  "Return the state attributes of the ICMP checo request."
  update(self) L91
  "Get the latest data."

class PingData(object) L96
  "The Class for handling the data retrieval."
  __init__(self, host, count) L99
  "Initialize the data object."
  ping(self) L114
  "Send ICMP echo request and return details if success."
  update(self) L140
  "Retrieve the latest details from the host."

setup_platform(hass, config, add_devices, discovery_info=None) L48
"Set up the Ping Binary sensor."

homeassistant/components/binary_sensor/rest.py
class RestBinarySensor(BinarySensorDevice) L80
  "Representation of a REST binary sensor."
  __init__(self, hass, rest, name, device_class, value_template) L83
  "Initialize a REST binary sensor."
  name(self) L95
  "Return the name of the binary sensor."
  device_class(self) L100
  "Return the class of this sensor."
  is_on(self) L105
  "Return true if the binary sensor is on."
  update(self) L120
  "Get the latest data from REST API and updates the state."

setup_platform(hass, config, add_devices, discovery_info=None) L46
"Setup the REST binary sensor."

homeassistant/components/binary_sensor/ring.py
class RingBinarySensor(BinarySensorDevice) L57
  "A binary sensor implementation for Ring device."
  __init__(self, hass, data, sensor_type) L60
  "Initialize a sensor for Ring device."
  name(self) L71
  "Return the name of the sensor."
  is_on(self) L76
  "Return True if the binary sensor is on."
  device_class(self) L81
  "Return the class of the binary sensor."
  device_state_attributes(self) L86
  "Return the state attributes."
  update(self) L101
  "Get the latest data and updates the state."

setup_platform(hass, config, add_devices, discovery_info=None) L42
"Set up a sensor for a Ring device."

homeassistant/components/binary_sensor/rpi_gpio.py
class RPiGPIOBinarySensor(BinarySensorDevice) L57
  "Represent a binary sensor that uses Raspberry Pi GPIO."
  __init__(self, name, port, pull_mode, bouncetime, invert_logic) L60
  "Initialize the RPi binary sensor."
  read_gpio(port) L72
  "Read state from GPIO."
  should_poll(self) L80
  "No polling needed."
  name(self) L85
  "Return the name of the sensor."
  is_on(self) L90
  "Return the state of the entity."
  update(self) L94
  "Update the GPIO state."

setup_platform(hass, config, add_devices, discovery_info=None) L43
"Setup the Raspberry PI GPIO devices."

homeassistant/components/binary_sensor/sleepiq.py
class IsInBedBinarySensor(sleepiq.SleepIQSensor, BinarySensorDevice) L28
  "Implementation of a SleepIQ presence sensor."
  __init__(self, sleepiq_data, bed_id, side) L31
  "Initialize the sensor."
  is_on(self) L40
  "Return the status of the sensor."
  device_class(self) L45
  "Return the class of this sensor."
  update(self) L49
  "Get the latest data from SleepIQ and updates the states."

setup_platform(hass, config, add_devices, discovery_info=None) L13
"Setup the SleepIQ sensors."

homeassistant/components/binary_sensor/tcp.py
class TcpBinarySensor(BinarySensorDevice, TcpSensor) L23
  "A binary sensor which is on when its state == CONF_VALUE_ON."
  is_on(self) L29
  "True if the binary sensor is on."

setup_platform(hass, config, add_devices, discovery_info=None) L18
"Set up the TCP binary sensor."

homeassistant/components/binary_sensor/template.py
class BinarySensorTemplate(BinarySensorDevice) L75
  "A virtual binary sensor that triggers from another sensor."
  __init__(self, hass, device, friendly_name, device_class, value_template, entity_ids) L78
  "Initialize the Template binary sensor."
  async_added_to_hass(self) L91
  "Register callbacks."
  template_bsensor_state_listener(entity, old_state, new_state) L98
  "Called when the target device changes state."
  template_bsensor_startup(event) L103
  "Update template on startup."
  name(self) L114
  "Return the name of the sensor."
  is_on(self) L119
  "Return true if sensor is on."
  device_class(self) L124
  "Return the sensor class of the sensor."
  should_poll(self) L129
  "No polling needed."
  async_update(self) L134
  "Update the state from the template."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L43
"Setup template binary sensors."

homeassistant/components/binary_sensor/threshold.py
class ThresholdSensor(BinarySensorDevice) L61
  "Representation of a Threshold sensor."
  __init__(self, hass, entity_id, name, threshold, limit_type, device_class) L64
  "Initialize the Threshold sensor."
  async_threshold_sensor_state_listener(entity, old_state, new_state) L78
  "Called when the sensor changes state."
  name(self) L95
  "Return the name of the sensor."
  is_on(self) L100
  "Return true if sensor is on."
  should_poll(self) L105
  "No polling needed."
  device_class(self) L110
  "Return the sensor class of the sensor."
  device_state_attributes(self) L115
  "Return the state attributes of the sensor."
  async_update(self) L125
  "Get the latest data and updates the states."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L47
"Set up the Threshold sensor."

homeassistant/components/binary_sensor/trend.py
class SensorTrend(BinarySensorDevice) L78
  "Representation of a trend Sensor."
  __init__(self, hass, device_id, friendly_name, target_entity, attribute, device_class, invert) L81
  "Initialize the sensor."
  trend_sensor_state_listener(entity, old_state, new_state) L97
  "Called when the target device changes state."
  name(self) L107
  "Return the name of the sensor."
  is_on(self) L112
  "Return true if sensor is on."
  device_class(self) L117
  "Return the sensor class of the sensor."
  should_poll(self) L122
  "No polling needed."
  async_update(self) L127
  "Get the latest data and update the states."

setup_platform(hass, config, add_devices, discovery_info=None) L49
"Setup the trend sensors."

homeassistant/components/binary_sensor/vera.py
class VeraBinarySensor(VeraDevice, BinarySensorDevice) L26
  "Representation of a Vera Binary Sensor."
  __init__(self, vera_device, controller) L29
  "Initialize the binary_sensor."
  is_on(self) L36
  "Return true if sensor is on."
  update(self) L40
  "Get the latest data and update the state."

setup_platform(hass, config, add_devices, discovery_info=None) L19
"Perform the setup for Vera controller devices."

homeassistant/components/binary_sensor/volvooncall.py
class VolvoSensor(VolvoEntity, BinarySensorDevice) L23
  "Representation of a Volvo sensor."
  is_on(self) L27
  "Return True if the binary sensor is on."
  device_class(self) L38
  "Return the class of this sensor, from SENSOR_CLASSES."

setup_platform(hass, config, add_devices, discovery_info=None) L16
"Setup Volvo sensors."

homeassistant/components/binary_sensor/wemo.py
class WemoBinarySensor(BinarySensorDevice) L31
  "Represents a WeMo binary sensor."
  __init__(self, device) L34
  "Initialize the WeMo sensor."
  _update_callback(self, _device, _type, _params) L43
  "Called by the Wemo device callback to update state."
  should_poll(self) L56
  "No polling needed with subscriptions."
  unique_id(self) L61
  "Return the id of this WeMo device."
  name(self) L66
  "Return the name of the sevice if any."
  is_on(self) L71
  "True if sensor is on."
  update(self) L75
  "Update WeMo state."
  _update(self, force_update=True) L79

setup_platform(hass, config, add_devices_callback, discovery_info=None) L18
"Register discovered WeMo binary sensors."

homeassistant/components/binary_sensor/wink.py
class WinkBinarySensorDevice(WinkDevice, BinarySensorDevice, Entity) L89
  "Representation of a Wink binary sensor."
  __init__(self, wink, hass) L92
  "Initialize the Wink binary sensor."
  is_on(self) L105
  "Return true if the binary sensor is on."
  device_class(self) L110
  "Return the class of this sensor, from DEVICE_CLASSES."

class WinkSmokeDetector(WinkBinarySensorDevice) L115
  "Representation of a Wink Smoke detector."
  __init__(self, wink, hass) L118
  "Initialize the Wink binary sensor."
  device_state_attributes(self) L123
  "Return the state attributes."

class WinkHub(WinkBinarySensorDevice) L130
  "Representation of a Wink Hub."
  __init__(self, wink, hass) L133
  "Initialize the Wink binary sensor."
  device_state_attributes(self) L138
  "Return the state attributes."

class WinkRemote(WinkBinarySensorDevice) L146
  "Representation of a Wink Lutron Connected bulb remote."
  __init__(self, wink, hass) L149
  "Initialize the Wink binary sensor."
  device_state_attributes(self) L154
  "Return the state attributes."
  device_class(self) L164
  "Return the class of this sensor, from DEVICE_CLASSES."

class WinkButton(WinkBinarySensorDevice) L169
  "Representation of a Wink Relay button."
  __init__(self, wink, hass) L172
  "Initialize the Wink binary sensor."
  device_state_attributes(self) L177
  "Return the state attributes."

class WinkGang(WinkBinarySensorDevice) L185
  "Representation of a Wink Relay gang."
  __init__(self, wink, hass) L188
  "Initialize the Wink binary sensor."
  is_on(self) L193
  "Return true if the gang is connected."

setup_platform(hass, config, add_devices, discovery_info=None) L34
"Setup the Wink binary sensor platform."

homeassistant/components/binary_sensor/workday.py
class IsWorkdaySensor(Entity) L95
  "Implementation of a Workday sensor."
  __init__(self, obj_holidays, workdays, excludes, name) L98
  "Initialize the Workday sensor."
  name(self) L107
  "Return the name of the sensor."
  state(self) L112
  "Return the state of the device."
  is_include(self, day, now) L116
  "Check if given day is in the includes list."
  is_exclude(self, day, now) L125
  "Check if given day is in the excludes list."
  async_update(self) L135
  "Get date and look whether it is a holiday."

setup_platform(hass, config, add_devices, discovery_info=None) L55
"Set up the Workday sensor."
day_to_string(day) L87
"Convert day index 0 - 7 to string."

homeassistant/components/binary_sensor/zha.py
class BinarySensor(zha.Entity, BinarySensorDevice) L57
  "ZHA Binary Sensor."
  __init__(self, device_class, **kwargs) L62
  "Initialize ZHA binary sensor."
  is_on(self) L70
  "Return True if entity is on."
  device_class(self) L77
  "Return the class of this device, from component DEVICE_CLASSES."
  cluster_command(self, aps_frame, tsn, command_id, args) L81
  "Handle commands received to this cluster."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L29
"Setup Zigbee Home Automation binary sensors."

homeassistant/components/binary_sensor/zigbee.py
class ZigBeeBinarySensor(ZigBeeDigitalIn, BinarySensorDevice) L31
  "Use ZigBeeDigitalIn as binary sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L25
"Setup the ZigBee binary sensor platform."

homeassistant/components/binary_sensor/zwave.py
class ZWaveBinarySensor(BinarySensorDevice, zwave.ZWaveDeviceEntity) L38
  "Representation of a binary sensor within Z-Wave."
  __init__(self, values, device_class) L41
  "Initialize the sensor."
  update_properties(self) L47
  "Callback on data changes for node values."
  is_on(self) L52
  "Return True if the binary sensor is on."
  device_class(self) L57
  "Return the class of this sensor, from DEVICE_CLASSES."

class ZWaveTriggerSensor(ZWaveBinarySensor) L62
  "Representation of a stateless sensor within Z-Wave."
  __init__(self, values, device_class, re_arm_sec=60) L65
  "Initialize the sensor."
  update_properties(self) L71
  "Called when a value for this entity's node has changed."
  is_on(self) L85
  "Return True if movement has happened within the rearm time."

get_device(values, **kwargs) L22
"Create zwave entity device."

homeassistant/components/blink.py
class BlinkSystem(object) L43
  "Blink System class."
  __init__(self, config_info) L46
  "Initialize the system."

setup(hass, config) L54
"Set up Blink System."
snap_picture(call) L61
"Take a picture."
arm_camera(call) L68
"Arm a camera."
arm_system(call) L76
"Arm the system."

homeassistant/components/bloomsky.py
class BloomSky(object) L53
  "Handle all communication with the BloomSky API."
  __init__(self, api_key) L59
  "Initialize the BookSky."
  refresh_devices(self) L67
  "Use the API to retrieve a list of devices."

setup(hass, config) L37
"Setup BloomSky component."

homeassistant/components/browser.py
setup(hass, config) L21
"Listen for browse_url events."

homeassistant/components/calendar/__init__.py
class CalendarEventDevice(Entity) L45
  "A calendar event device."
  __init__(self, hass, data) L53
  "Create the Calendar Event Device."
  offset_reached(self) L74
  "Have we reached the offset time specified in the event title."
  name(self) L84
  "Return the name of the entity."
  device_state_attributes(self) L89
  "State Attributes for HA."
  state(self) L107
  "Return the state of the calendar event."
  cleanup(self) L124
  "Cleanup any start/end listeners that were setup."
  update(self) L136
  "Search for the next event."
  _get_date(date) L147
  "Get the dateTime from date or dateTime as a local."

async_setup(hass, config) L31
"Track states and offer events for calendars."

homeassistant/components/calendar/demo.py
class DemoGoogleCalendarData(object) L29
  "Setup base class for data."
  update(self) L33
  "Return true so entity knows we have new data."

class DemoGoogleCalendarDataFuture(DemoGoogleCalendarData) L38
  "Setup future data event."
  __init__(self) L41
  "Set the event to a future event."

class DemoGoogleCalendarDataCurrent(DemoGoogleCalendarData) L57
  "Create a current event we're in the middle of."
  __init__(self) L60
  "Set the event data."

class DemoGoogleCalendar(CalendarEventDevice) L76
  "A Demo binary sensor."
  __init__(self, hass, calendar_data, data) L79
  "The same as a google calendar but without the api calls."

setup_platform(hass, config, add_devices, discovery_info=None) L12
"Setup the Demo binary sensor platform."

homeassistant/components/calendar/google.py
class GoogleCalendarEventDevice(CalendarEventDevice) L44
  "A calendar event device."
  __init__(self, hass, calendar_service, calendar, data) L47
  "Create the Calendar event device."

class GoogleCalendarData(object) L54
  "Class to utilize calendar service object to get next event."
  __init__(self, calendar_service, calendar_id, search=None) L57
  "Setup how we are going to search the google calendar."
  update(self) L65
  "Get the latest data."

setup_platform(hass, config, add_devices, disc_info=None) L29
"Setup the calendar platform for event devices."

homeassistant/components/camera/__init__.py
class Camera(Entity) L98
  "The base class for camera entities."
  __init__(self) L101
  "Initialize a camera."
  should_poll(self) L108
  "No need to poll cameras."
  entity_picture(self) L113
  "Return a link to the camera feed as entity picture."
  is_recording(self) L118
  "Return true if the device is recording."
  brand(self) L123
  "Camera brand."
  model(self) L128
  "Camera model."
  camera_image(self) L132
  "Return bytes of camera image."
  async_camera_image(self) L136
  "Return bytes of camera image."
  handle_async_mjpeg_stream(self, request) L144
  "Generate an HTTP MJPEG stream from camera images."
  write(img_bytes) L155
  "Write image to stream."
  state(self) L193
  "Camera state."
  state_attributes(self) L203
  "Camera state attributes."
  async_update_token(self) L218
  "Update the used token."

class CameraView(HomeAssistantView) L225
  "Base CameraView."
  __init__(self, entities) L230
  "Initialize a basic camera view."
  get(self, request, entity_id) L235
  "Start a get request."
  handle(self, request, camera) L253
  "Hanlde the camera request."

class CameraImageView(CameraView) L258
  "Camera view to serve an image."
  handle(self, request, camera) L265
  "Serve camera image."

class CameraMjpegStream(CameraView) L277
  "Camera View to serve an MJPEG stream."
  handle(self, request, camera) L284
  "Serve camera image."

async_get_image(hass, entity_id, timeout=10) L48
"Fetch a image from a camera entity."
async_setup(hass, config) L78
"Setup the camera component."
update_tokens(time) L88
"Update tokens of the entities."

homeassistant/components/camera/amcrest.py
class AmcrestCam(Camera) L87
  "An implementation of an Amcrest IP camera."
  __init__(self, hass, device_info, camera) L90
  "Initialize an Amcrest camera."
  camera_image(self) L106
  "Return a still image reponse from the camera."
  handle_async_mjpeg_stream(self, request) L113
  "Return an MJPEG stream."
  name(self) L131
  "Return the name of this camera."

setup_platform(hass, config, add_devices, discovery_info=None) L62
"Set up an Amcrest IP Camera."

homeassistant/components/camera/blink.py
class BlinkCamera(Camera) L36
  "An implementation of a Blink Camera."
  __init__(self, hass, config, data, name) L39
  "Initialize a camera."
  name(self) L51
  "A camera name."
  request_image(self) L56
  "An image request from Blink servers."
  check_for_motion(self) L63
  "A method to check if motion has been detected since last update."
  camera_image(self) L78
  "Return a still image reponse from the camera."

setup_platform(hass, config, add_devices, discovery_info=None) L23
"Setup a Blink Camera."

homeassistant/components/camera/bloomsky.py
class BloomSkyCamera(Camera) L25
  "Representation of the images published from the BloomSky's camera."
  __init__(self, bs, device) L28
  "Setup for access to the BloomSky camera images."
  camera_image(self) L42
  "Update the camera's image if it has changed."
  name(self) L59
  "Return the name of this BloomSky device."

setup_platform(hass, config, add_devices, discovery_info=None) L18
"Setup access to BloomSky cameras."

homeassistant/components/camera/demo.py
class DemoCamera(Camera) L20
  "The representation of a Demo camera."
  __init__(self, name) L23
  "Initialize demo camera component."
  camera_image(self) L28
  "Return a faked still image response."
  name(self) L38
  "Return the name of this camera."

setup_platform(hass, config, add_devices, discovery_info=None) L13
"Setup the Demo camera platform."

homeassistant/components/camera/ffmpeg.py
class FFmpegCamera(Camera) L40
  "An implementation of an FFmpeg camera."
  __init__(self, hass, config) L43
  "Initialize a FFmpeg camera."
  async_camera_image(self) L53
  "Return a still image response from the camera."
  handle_async_mjpeg_stream(self, request) L64
  "Generate an HTTP MJPEG stream from the camera."
  name(self) L78
  "Return the name of this camera."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L33
"Setup a FFmpeg Camera."

homeassistant/components/camera/foscam.py
class FoscamCamera(Camera) L39
  "An implementation of a Foscam IP camera."
  __init__(self, device_info) L42
  "Initialize a Foscam camera."
  camera_image(self) L66
  "Return a still image reponse from the camera."
  name(self) L78
  "Return the name of this camera."

setup_platform(hass, config, add_devices, discovery_info=None) L34
"Setup a Foscam IP Camera."

homeassistant/components/camera/local_file.py
class LocalFile(Camera) L40
  "Local camera."
  __init__(self, name, file_path) L43
  "Initialize Local File Camera component."
  camera_image(self) L50
  "Return image response."
  name(self) L60
  "Return the name of this camera."

setup_platform(hass, config, add_devices, discovery_info=None) L28
"Setup the Camera."

homeassistant/components/camera/mjpeg.py
class MjpegCamera(Camera) L65
  "An implementation of an IP camera that is reachable over a URL."
  __init__(self, hass, device_info) L68
  "Initialize a MJPEG camera."
  async_camera_image(self) L86
  "Return a still image response from the camera."
  camera_image(self) L110
  "Return a still image response from the camera."
  handle_async_mjpeg_stream(self, request) L126
  "Generate an HTTP MJPEG stream from the camera."
  name(self) L140
  "Return the name of this camera."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L46
"Setup a MJPEG IP Camera."
extract_image_from_mjpeg(stream) L53
"Take in a MJPEG stream object, return the jpg from it."

homeassistant/components/camera/mqtt.py
class MqttCamera(Camera) L41
  "MQTT camera."
  __init__(self, name, topic) L44
  "Initialize Local File Camera component."
  async_camera_image(self) L54
  "Return image response."
  name(self) L59
  "Return the name of this camera."
  async_added_to_hass(self) L63
  "Subscribe mqtt events."
  message_received(topic, payload, qos) L69
  "A new MQTT message has been received."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L34
"Setup the Camera."

homeassistant/components/camera/neato.py
class NeatoCleaningMap(Camera) L30
  "Neato cleaning map for last clean."
  __init__(self, hass, robot) L33
  "Initialize Neato cleaning map."
  camera_image(self) L43
  "Return image response."
  update(self) L49
  "Check the contents of the map list."
  name(self) L63
  "Return the name of this camera."

setup_platform(hass, config, add_devices, discovery_info=None) L20
"Setup the Camera."

homeassistant/components/camera/nest.py
class NestCamera(Camera) L36
  "Representation of a Nest Camera."
  __init__(self, structure, device) L39
  "Initialize a Nest Camera."
  name(self) L55
  "Return the name of the nest, if any."
  should_poll(self) L60
  "Nest camera should poll periodically."
  is_recording(self) L65
  "Return true if the device is recording."
  brand(self) L70
  "Return the brand of the camera."
  update(self) L75
  "Cache value from Python-nest."
  _ready_for_snapshot(self, now) L90
  camera_image(self) L94
  "Return a still image response from the camera."

setup_platform(hass, config, add_devices, discovery_info=None) L25
"Set up a Nest Cam."

homeassistant/components/camera/netatmo.py
class NetatmoCamera(Camera) L54
  "Representation of the images published from a Netatmo camera."
  __init__(self, data, camera_name, home, camera_type, verify_ssl) L57
  "Setup for access to the Netatmo camera images."
  camera_image(self) L76
  "Return a still image response from the camera."
  name(self) L100
  "Return the name of this Netatmo camera device."
  brand(self) L105
  "Camera brand."
  model(self) L110
  "Camera model."
  unique_id(self) L120
  "Return the unique ID for this sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L34
"Setup access to Netatmo cameras."

homeassistant/components/camera/rpi_camera.py
class RaspberryCamera(Camera) L102
  "Representation of a Raspberry Pi camera."
  __init__(self, device_info) L105
  "Initialize Raspberry Pi camera component."
  camera_image(self) L133
  "Return raspstill image response."
  name(self) L139
  "Return the name of this camera."

kill_raspistill(*args) L57
"Kill any previously running raspistill process.."
setup_platform(hass, config, add_devices, discovery_info=None) L64
"Setup the Raspberry Camera."

homeassistant/components/camera/uvc.py
class UnifiVideoCamera(Camera) L67
  "A Ubiquiti Unifi Video Camera."
  __init__(self, nvr, uuid, name) L70
  "Initialize an Unifi camera."
  name(self) L81
  "Return the name of this camera."
  is_recording(self) L86
  "Return true if the camera is recording."
  brand(self) L92
  "Return the brand of this camera."
  model(self) L97
  "Return the model of this camera."
  _login(self) L102
  "Login to the camera."
  camera_image(self) L149
  "Return the image of this camera."
  _get_image(retry=True) L156

setup_platform(hass, config, add_devices, discovery_info=None) L33
"Discover cameras on a Unifi NVR."

homeassistant/components/camera/verisure.py
class VerisureSmartcam(Camera) L35
  "Local camera."
  __init__(self, hass, device_id, directory_path) L38
  "Initialize Verisure File Camera component."
  camera_image(self) L49
  "Return image response."
  check_imagelist(self) L59
  "Check the contents of the image list."
  delete_image(self, event) L87
  "Delete an old image."
  name(self) L101
  "Return the name of this camera."

setup_platform(hass, config, add_devices, discovery_info=None) L19
"Setup the Camera."

homeassistant/components/camera/zoneminder.py
class ZoneMinderCamera(MjpegCamera) L84
  "Representation of a ZoneMinder Monitor Stream."
  __init__(self, hass, device_info, monitor) L87
  "Initialize as a subclass of MjpegCamera."
  should_poll(self) L94
  "Update the recording state periodically."
  update(self) L98
  "Update our recording state from the ZM API."
  is_recording(self) L118
  "Return whether the monitor is in alarm mode."

_get_image_url(hass, monitor, mode) L26
async_setup_platform(hass, config, async_add_devices, discovery_info=None) L53
"Setup ZoneMinder cameras."

homeassistant/components/climate/__init__.py
class ClimateDevice(Entity) L391
  "Representation of a climate device."
  state(self) L396
  "Return the current state."
  precision(self) L404
  "Return the precision of the system."
  state_attributes(self) L412
  "Return the optional state attributes."
  unit_of_measurement(self) L469
  "The unit of measurement to display."
  temperature_unit(self) L474
  "The unit of measurement used by the platform."
  current_humidity(self) L479
  "Return the current humidity."
  target_humidity(self) L484
  "Return the humidity we try to reach."
  current_operation(self) L489
  "Return current operation ie."
  operation_list(self) L494
  "List of available operation modes."
  current_temperature(self) L499
  "Return the current temperature."
  target_temperature(self) L504
  "Return the temperature we try to reach."
  target_temperature_high(self) L509
  "Return the highbound target temperature we try to reach."
  target_temperature_low(self) L514
  "Return the lowbound target temperature we try to reach."
  is_away_mode_on(self) L519
  "Return true if away mode is on."
  current_hold_mode(self) L524
  "Return the current hold mode, e.g., home, away, temp."
  is_aux_heat_on(self) L529
  "Return true if aux heater."
  current_fan_mode(self) L534
  "Return the fan setting."
  fan_list(self) L539
  "List of available fan modes."
  current_swing_mode(self) L544
  "Return the fan setting."
  swing_list(self) L549
  "List of available swing modes."
  set_temperature(self, **kwargs) L553
  "Set new target temperature."
  async_set_temperature(self, **kwargs) L557
  "Set new target temperature."
  set_humidity(self, humidity) L565
  "Set new target humidity."
  async_set_humidity(self, humidity) L569
  "Set new target humidity."
  set_fan_mode(self, fan) L577
  "Set new target fan mode."
  async_set_fan_mode(self, fan) L581
  "Set new target fan mode."
  set_operation_mode(self, operation_mode) L589
  "Set new target operation mode."
  async_set_operation_mode(self, operation_mode) L593
  "Set new target operation mode."
  set_swing_mode(self, swing_mode) L601
  "Set new target swing operation."
  async_set_swing_mode(self, swing_mode) L605
  "Set new target swing operation."
  turn_away_mode_on(self) L613
  "Turn away mode on."
  async_turn_away_mode_on(self) L617
  "Turn away mode on."
  turn_away_mode_off(self) L625
  "Turn away mode off."
  async_turn_away_mode_off(self) L629
  "Turn away mode off."
  set_hold_mode(self, hold_mode) L637
  "Set new target hold mode."
  async_set_hold_mode(self, hold_mode) L641
  "Set new target hold mode."
  turn_aux_heat_on(self) L649
  "Turn auxillary heater on."
  async_turn_aux_heat_on(self) L653
  "Turn auxillary heater on."
  turn_aux_heat_off(self) L661
  "Turn auxillary heater off."
  async_turn_aux_heat_off(self) L665
  "Turn auxillary heater off."
  min_temp(self) L674
  "Return the minimum temperature."
  max_temp(self) L679
  "Return the maximum temperature."
  min_humidity(self) L684
  "Return the minimum humidity."
  max_humidity(self) L689
  "Return the maximum humidity."
  _convert_for_display(self, temp) L693
  "Convert temperature into preferred units for display purposes."

set_away_mode(hass, away_mode, entity_id=None) L116
"Turn all or specified climate devices away mode on."
set_hold_mode(hass, hold_mode, entity_id=None) L128
"Set new hold mode."
set_aux_heat(hass, aux_heat, entity_id=None) L140
"Turn all or specified climate devices auxillary heater on."
set_temperature(hass, temperature=None, entity_id=None, target_temp_high=None, target_temp_low=None, operation_mode=None) L152
"Set new target temperature."
set_humidity(hass, humidity, entity_id=None) L169
"Set new target humidity."
set_fan_mode(hass, fan, entity_id=None) L179
"Set all or specified climate devices fan mode on."
set_operation_mode(hass, operation_mode, entity_id=None) L189
"Set new target operation mode."
set_swing_mode(hass, swing_mode, entity_id=None) L199
"Set new target swing mode."
async_setup(hass, config) L210
"Setup climate devices."
_async_update_climate(target_climate) L220
"Update climate entity after service stuff."
async_away_mode_set_service(service) L238
"Set away mode on target climate devices."
async_hold_mode_set_service(service) L258
"Set hold mode on target climate devices."
async_aux_heat_set_service(service) L275
"Set auxillary heater on target climate devices."
async_temperature_set_service(service) L295
"Set temperature on the target climate devices."
async_humidity_set_service(service) L321
"Set humidity on the target climate devices."
async_fan_mode_set_service(service) L338
"Set fan mode on target climate devices."
async_operation_set_service(service) L355
"Set operating mode on the target climate devices."
async_swing_set_service(service) L372
"Set swing mode on the target climate devices."

homeassistant/components/climate/demo.py
class DemoClimate(ClimateDevice) L24
  "Representation of a demo climate device."
  __init__(self, name, target_temperature, unit_of_measurement, away, hold, current_temperature, current_fan_mode, target_humidity, current_humidity, current_swing_mode, current_operation, aux, target_temp_high, target_temp_low) L27
  "Initialize the climate device."
  should_poll(self) L51
  "Polling not needed for a demo climate device."
  name(self) L56
  "Return the name of the climate device."
  temperature_unit(self) L61
  "Return the unit of measurement."
  current_temperature(self) L66
  "Return the current temperature."
  target_temperature(self) L71
  "Return the temperature we try to reach."
  target_temperature_high(self) L76
  "Return the highbound target temperature we try to reach."
  target_temperature_low(self) L81
  "Return the lowbound target temperature we try to reach."
  current_humidity(self) L86
  "Return the current humidity."
  target_humidity(self) L91
  "Return the humidity we try to reach."
  current_operation(self) L96
  "Return current operation ie."
  operation_list(self) L101
  "List of available operation modes."
  is_away_mode_on(self) L106
  "Return if away mode is on."
  current_hold_mode(self) L111
  "Return hold mode setting."
  is_aux_heat_on(self) L116
  "Return true if away mode is on."
  current_fan_mode(self) L121
  "Return the fan setting."
  fan_list(self) L126
  "List of available fan modes."
  set_temperature(self, **kwargs) L130
  "Set new target temperatures."
  set_humidity(self, humidity) L140
  "Set new target temperature."
  set_swing_mode(self, swing_mode) L145
  "Set new target temperature."
  set_fan_mode(self, fan) L150
  "Set new target temperature."
  set_operation_mode(self, operation_mode) L155
  "Set new target temperature."
  current_swing_mode(self) L161
  "Return the swing setting."
  swing_list(self) L166
  "List of available swing modes."
  turn_away_mode_on(self) L170
  "Turn away mode on."
  turn_away_mode_off(self) L175
  "Turn away mode off."
  set_hold_mode(self, hold) L180
  "Update hold mode on."
  turn_aux_heat_on(self) L185
  "Turn away auxillary heater on."
  turn_aux_heat_off(self) L190
  "Turn auxillary heater off."

setup_platform(hass, config, add_devices, discovery_info=None) L12
"Setup the Demo climate devices."

homeassistant/components/climate/ecobee.py
class Thermostat(ClimateDevice) L106
  "A thermostat class for Ecobee."
  __init__(self, data, thermostat_index, hold_temp) L109
  "Initialize the thermostat."
  update(self) L123
  "Get the latest state from the thermostat."
  name(self) L135
  "Return the name of the Ecobee Thermostat."
  temperature_unit(self) L140
  "Return the unit of measurement."
  current_temperature(self) L145
  "Return the current temperature."
  target_temperature_low(self) L150
  "Return the lower bound temperature we try to reach."
  target_temperature_high(self) L158
  "Return the upper bound temperature we try to reach."
  target_temperature(self) L166
  "Return the temperature we try to reach."
  desired_fan_mode(self) L178
  "Return the desired fan mode of operation."
  fan(self) L183
  "Return the current fan state."
  current_hold_mode(self) L191
  "Return current hold mode."
  current_operation(self) L220
  "Return current operation."
  operation_list(self) L229
  "Return the operation modes list."
  operation_mode(self) L234
  "Return current operation ie."
  mode(self) L239
  "Return current mode, as the user-visible name."
  fan_min_on_time(self) L247
  "Return current fan minimum on time."
  device_state_attributes(self) L252
  "Return device specific state attributes."
  is_away_mode_on(self) L277
  "Return true if away mode is on."
  turn_away_mode_on(self) L281
  "Turn away on."
  turn_away_mode_off(self) L285
  "Turn away off."
  set_hold_mode(self, hold_mode) L289
  "Set hold mode (away, home, temp, sleep, etc.)."
  set_auto_temp_hold(self, heat_temp, cool_temp) L311
  "Set temperature hold in auto mode."
  set_temp_hold(self, temp) L322
  "Set temperature hold in modes other than auto."
  set_temperature(self, **kwargs) L341
  "Set new target temperature."
  set_operation_mode(self, operation_mode) L356
  "Set HVAC mode (auto, auxHeatOnly, cool, heat, off)."
  set_fan_min_on_time(self, fan_min_on_time) L361
  "Set the minimum fan on time."
  resume_program(self, resume_all) L367
  "Resume the thermostat schedule program."
  hold_preference(self) L373
  "Return user preference setting for hold time."
  climate_list(self) L387
  "Return the list of climates currently available."

setup_platform(hass, config, add_devices, discovery_info=None) L47
"Setup the Ecobee Thermostat Platform."
fan_min_on_time_set_service(service) L60
"Set the minimum fan on time on the target thermostats."
resume_program_set_service(service) L76
"Resume the program on the target thermostats."

homeassistant/components/climate/eq3btsmart.py
class EQ3BTSmartThermostat(ClimateDevice) L56
  "Representation of a eQ-3 Bluetooth Smart thermostat."
  __init__(self, _mac, _name) L59
  "Initialize the thermostat."
  available(self) L77
  "Return if thermostat is available."
  name(self) L82
  "Return the name of the device."
  temperature_unit(self) L87
  "Return the unit of measurement that is used."
  precision(self) L92
  "Return eq3bt's precision 0.5."
  current_temperature(self) L97
  "Can not report temperature, so return target_temperature."
  target_temperature(self) L102
  "Return the temperature we try to reach."
  set_temperature(self, **kwargs) L106
  "Set new target temperature."
  current_operation(self) L114
  "Current mode."
  operation_list(self) L121
  "List of available operation modes."
  set_operation_mode(self, operation_mode) L125
  "Set operation mode."
  turn_away_mode_off(self) L129
  "Away mode off turns to AUTO mode."
  turn_away_mode_on(self) L133
  "Set away mode on."
  is_away_mode_on(self) L138
  "Return if we are away."
  min_temp(self) L143
  "Return the minimum temperature."
  max_temp(self) L148
  "Return the maximum temperature."
  device_state_attributes(self) L153
  "Return the device specific state attributes."
  update(self) L165
  "Update the data from the thermostat."

setup_platform(hass, config, add_devices, discovery_info=None) L44
"Setup the eQ-3 BLE thermostats."

homeassistant/components/climate/generic_thermostat.py
class GenericThermostat(ClimateDevice) L76
  "Representation of a GenericThermostat device."
  __init__(self, hass, name, heater_entity_id, sensor_entity_id, min_temp, max_temp, target_temp, ac_mode, min_cycle_duration, tolerance, keep_alive) L79
  "Initialize the thermostat."
  should_poll(self) L112
  "No polling needed."
  name(self) L117
  "Return the name of the thermostat."
  temperature_unit(self) L122
  "Return the unit of measurement."
  current_temperature(self) L127
  "Return the sensor temperature."
  current_operation(self) L132
  "Return current operation ie."
  target_temperature(self) L142
  "Return the temperature we try to reach."
  async_set_temperature(self, **kwargs) L147
  "Set new target temperature."
  min_temp(self) L157
  "Return the minimum temperature."
  max_temp(self) L167
  "Return the maximum temperature."
  _async_sensor_changed(self, entity_id, old_state, new_state) L177
  "Called when temperature changes."
  _async_switch_changed(self, entity_id, old_state, new_state) L187
  "Called when heater switch changes state."
  _async_keep_alive(self, time) L194
  "Called at constant intervals for keep-alive purposes."
  _async_update_temp(self, state) L202
  "Update thermostat with latest state from sensor."
  _async_control_heating(self) L213
  "Check if we need to turn heating on or off."
  _is_device_active(self) L262
  "If the toggleable device is currently active."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L58
"Setup the generic thermostat."

homeassistant/components/climate/heatmiser.py
class HeatmiserV3Thermostat(ClimateDevice) L56
  "Representation of a HeatmiserV3 thermostat."
  __init__(self, heatmiser, device, name, serport) L59
  "Initialize the thermostat."
  name(self) L72
  "Return the name of the thermostat, if any."
  temperature_unit(self) L77
  "Return the unit of measurement which this thermostat uses."
  current_temperature(self) L82
  "Return the current temperature."
  target_temperature(self) L94
  "Return the temperature we try to reach."
  set_temperature(self, **kwargs) L98
  "Set new target temperature."
  update(self) L111
  "Get the latest data."

setup_platform(hass, config, add_devices, discovery_info=None) L37
"Setup the heatmiser thermostat."

homeassistant/components/climate/homematic.py
class HMThermostat(HMDevice, ClimateDevice) L57
  "Representation of a Homematic thermostat."
  temperature_unit(self) L61
  "Return the unit of measurement that is used."
  current_operation(self) L66
  "Return current operation ie."
  operation_list(self) L78
  "List of available operation modes."
  current_humidity(self) L90
  "Return the current humidity."
  current_temperature(self) L97
  "Return the current temperature."
  target_temperature(self) L104
  "Return the target temperature."
  set_temperature(self, **kwargs) L108
  "Set new target temperature."
  set_operation_mode(self, operation_mode) L116
  "Set new target operation mode."
  min_temp(self) L124
  "Return the minimum temperature - 4.5 means off."
  max_temp(self) L129
  "Return the maximum temperature - 30.5 means on."
  _init_data_struct(self) L133
  "Generate a data dict (self._data) from the Homematic metadata."

setup_platform(hass, config, add_devices, discovery_info=None) L43
"Setup the Homematic thermostat platform."

homeassistant/components/climate/honeywell.py
class RoundThermostat(ClimateDevice) L116
  "Representation of a Honeywell Round Connected thermostat."
  __init__(self, device, zone_id, master, away_temp) L119
  "Initialize the thermostat."
  name(self) L133
  "Return the name of the honeywell, if any."
  temperature_unit(self) L138
  "Return the unit of measurement."
  current_temperature(self) L143
  "Return the current temperature."
  target_temperature(self) L148
  "Return the temperature we try to reach."
  set_temperature(self, **kwargs) L154
  "Set new target temperature."
  current_operation(self) L162
  "Get the current operation of the system."
  is_away_mode_on(self) L167
  "Return true if away mode is on."
  set_operation_mode(self, operation_mode) L171
  "Set the HVAC mode for the thermostat."
  turn_away_mode_on(self) L176
  "Turn away on."
  turn_away_mode_off(self) L186
  "Turn away off."
  update(self) L191
  "Get the latest date."

class HoneywellUSThermostat(ClimateDevice) L215
  "Representation of a Honeywell US Thermostat."
  __init__(self, client, device, cool_away_temp, heat_away_temp, username, password) L218
  "Initialize the thermostat."
  is_fan_on(self) L230
  "Return true if fan is on."
  name(self) L235
  "Return the name of the honeywell, if any."
  temperature_unit(self) L240
  "Return the unit of measurement."
  current_temperature(self) L246
  "Return the current temperature."
  target_temperature(self) L251
  "Return the temperature we try to reach."
  current_operation(self) L259
  "Return current operation ie."
  set_temperature(self, **kwargs) L266
  "Set target temperature."
  device_state_attributes(self) L295
  "Return the device specific state attributes."
  is_away_mode_on(self) L308
  "Return true if away mode is on."
  turn_away_mode_on(self) L312
  "Turn away on."
  turn_away_mode_off(self) L341
  "Turn away off."
  set_operation_mode(self, operation_mode) L352
  "Set the system mode (Cool, Heat, etc)."
  update(self) L357
  "Update the state."
  _retry(self) L375
  "Recreate a new somecomfort client."

setup_platform(hass, config, add_devices, discovery_info=None) L56
"Setup the Honeywell thermostat."
_setup_round(username, password, config, add_devices) L68
"Setup rounding function."
_setup_us(username, password, config, add_devices) L89
"Setup user."

homeassistant/components/climate/knx.py
class KNXThermostat(KNXMultiAddressDevice, ClimateDevice) L38
  "Representation of a KNX thermostat."
  __init__(self, hass, config) L51
  "Initialize the thermostat based on the given configuration."
  should_poll(self) L63
  "Polling is needed for the KNX thermostat."
  temperature_unit(self) L68
  "Return the unit of measurement."
  current_temperature(self) L73
  "Return the current temperature."
  target_temperature(self) L78
  "Return the temperature we try to reach."
  set_temperature(self, **kwargs) L82
  "Set new target temperature."
  set_operation_mode(self, operation_mode) L92
  "Set operation mode."
  update(self) L96
  "Update KNX climate."

setup_platform(hass, config, add_devices, discovery_info=None) L33
"Create and add an entity based on the configuration."

homeassistant/components/climate/maxcube.py
class MaxCubeClimate(ClimateDevice) L41
  "MAX!"
  __init__(self, hass, name, rf_address) L44
  "Initialize MAX!"
  should_poll(self) L54
  "Polling is required."
  name(self) L59
  "Return the name of the ClimateDevice."
  min_temp(self) L64
  "Return the minimum temperature."
  max_temp(self) L70
  "Return the maximum temperature."
  temperature_unit(self) L76
  "Return the unit of measurement."
  current_temperature(self) L81
  "Return the current temperature."
  current_operation(self) L89
  "Return current operation (auto, manual, boost, vacation)."
  operation_list(self) L95
  "List of available operation modes."
  target_temperature(self) L100
  "Return the temperature we try to reach."
  set_temperature(self, **kwargs) L105
  "Set new target temperatures."
  set_operation_mode(self, operation_mode) L122
  "Set new operation mode."
  update(self) L137
  "Get latest data from MAX!"
  map_temperature_max_hass(temperature) L142
  "Map Temperature from MAX!"
  map_mode_hass_max(operation_mode) L150
  "Map HASS Operation Modes to MAX!"
  map_mode_max_hass(mode) L172
  "Map MAX!"

setup_platform(hass, config, add_devices, discovery_info=None) L22
"Iterate through all MAX!"

homeassistant/components/climate/mysensors.py
class MySensorsHVAC(mysensors.MySensorsDeviceEntity, ClimateDevice) L45
  "Representation of a MySensorsHVAC hvac."
  assumed_state(self) L49
  "Return True if unable to access real state of entity."
  temperature_unit(self) L54
  "Return the unit of measurement."
  current_temperature(self) L60
  "Return the current temperature."
  target_temperature(self) L65
  "Return the temperature we try to reach."
  target_temperature_high(self) L77
  "Return the highbound target temperature we try to reach."
  target_temperature_low(self) L84
  "Return the lowbound target temperature we try to reach."
  current_operation(self) L91
  "Return current operation ie."
  operation_list(self) L96
  "List of available operation modes."
  current_fan_mode(self) L101
  "Return the fan setting."
  fan_list(self) L106
  "List of available fan modes."
  set_temperature(self, **kwargs) L110
  "Set new target temperature."
  set_fan_mode(self, fan) L140
  "Set new target temperature."
  set_operation_mode(self, operation_mode) L150
  "Set new target temperature."
  update(self) L161
  "Update the controller with the latest value from a sensor."
  set_humidity(self, humidity) L174
  "Set new target humidity."
  set_swing_mode(self, swing_mode) L178
  "Set new target swing operation."
  turn_away_mode_on(self) L182
  "Turn away mode on."
  turn_away_mode_off(self) L186
  "Turn away mode off."
  turn_aux_heat_on(self) L190
  "Turn auxillary heater on."
  turn_aux_heat_off(self) L194
  "Turn auxillary heater off."

setup_platform(hass, config, add_devices, discovery_info=None) L23
"Setup the mysensors climate."

homeassistant/components/climate/nest.py
class NestThermostat(ClimateDevice) L48
  "Representation of a Nest thermostat."
  __init__(self, structure, device, temp_unit) L51
  "Initialize the thermostat."
  name(self) L93
  "Return the name of the nest, if any."
  temperature_unit(self) L98
  "Return the unit of measurement."
  current_temperature(self) L103
  "Return the current temperature."
  current_operation(self) L108
  "Return current operation ie."
  target_temperature(self) L118
  "Return the temperature we try to reach."
  target_temperature_low(self) L126
  "Return the lower bound temperature we try to reach."
  target_temperature_high(self) L138
  "Return the upper bound temperature we try to reach."
  is_away_mode_on(self) L150
  "Return if away mode is on."
  set_temperature(self, **kwargs) L154
  "Set new target temperature."
  set_operation_mode(self, operation_mode) L166
  "Set operation mode."
  operation_list(self) L175
  "List of available operation modes."
  turn_away_mode_on(self) L179
  "Turn away on."
  turn_away_mode_off(self) L183
  "Turn away off."
  current_fan_mode(self) L188
  "Return whether the fan is on."
  fan_list(self) L198
  "List of available fan modes."
  set_fan_mode(self, fan) L202
  "Turn fan on/off."
  min_temp(self) L207
  "Identify min_temp in Nest API or defaults if not available."
  max_temp(self) L212
  "Identify max_temp in Nest API or defaults if not available."
  update(self) L216
  "Cache value from Python-nest."

setup_platform(hass, config, add_devices, discovery_info=None) L32
"Setup the Nest thermostat."

homeassistant/components/climate/netatmo.py
class NetatmoThermostat(ClimateDevice) L57
  "Representation a Netatmo thermostat."
  __init__(self, data, module_name, away_temp=None) L60
  "Initialize the sensor."
  name(self) L70
  "Return the name of the sensor."
  state(self) L75
  "Return the state of the device."
  temperature_unit(self) L80
  "Return the unit of measurement."
  current_temperature(self) L85
  "Return the current temperature."
  target_temperature(self) L90
  "Return the temperature we try to reach."
  current_operation(self) L95
  "Return the current state of the thermostat."
  is_away_mode_on(self) L104
  "Return true if away mode is on."
  turn_away_mode_on(self) L108
  "Turn away on."
  turn_away_mode_off(self) L115
  "Turn away off."
  set_temperature(self, endTimeOffset=DEFAULT_TIME_OFFSET, **kwargs) L122
  "Set new target temperature for 2 hours."
  update(self) L134
  "Get the latest data from NetAtmo API and updates the states."

class ThermostatData(object) L141
  "Get the latest data from Netatmo."
  __init__(self, auth, device=None) L144
  "Initialize the data object."
  get_module_names(self) L155
  "Return all module available on the API as a list."
  update(self) L168
  "Call the NetAtmo API to update the data."

setup_platform(hass, config, add_callback_devices, discovery_info=None) L39
"Setup the NetAtmo Thermostat."

homeassistant/components/climate/oem.py
class ThermostatDevice(ClimateDevice) L66
  "Interface class for the oemthermostat module and HA."
  __init__(self, hass, thermostat, name, away_temp) L69
  "Initialize the device."
  name(self) L89
  "Name of this Thermostat."
  temperature_unit(self) L94
  "The unit of measurement used by the platform."
  current_operation(self) L99
  "Return current operation i.e. heat, cool, idle."
  current_temperature(self) L107
  "Return the current temperature."
  target_temperature(self) L112
  "Return the temperature we try to reach."
  set_temperature(self, **kwargs) L116
  "Change the setpoint of the thermostat."
  is_away_mode_on(self) L125
  "Return true if away mode is on."
  turn_away_mode_on(self) L129
  "Turn away mode on."
  turn_away_mode_off(self) L137
  "Turn away mode off."
  update(self) L144
  "Update local state."

setup_platform(hass, config, add_devices, discovery_info=None) L41
"Setup oemthermostat."

homeassistant/components/climate/proliphix.py
class ProliphixThermostat(ClimateDevice) L40
  "Representation a Proliphix thermostat."
  __init__(self, pdp) L43
  "Initialize the thermostat."
  should_poll(self) L51
  "Polling needed for thermostat."
  update(self) L55
  "Update the data from the thermostat."
  name(self) L60
  "Return the name of the thermostat."
  precision(self) L65
  "Return the precision of the system."
  device_state_attributes(self) L74
  "Return the device specific state attributes."
  temperature_unit(self) L81
  "Return the unit of measurement."
  current_temperature(self) L86
  "Return the current temperature."
  target_temperature(self) L91
  "Return the temperature we try to reach."
  current_operation(self) L96
  "Return the current state of the thermostat."
  set_temperature(self, **kwargs) L106
  "Set new target temperature."

setup_platform(hass, config, add_devices, discovery_info=None) L27
"Setup the Proliphix thermostats."

homeassistant/components/climate/radiotherm.py
class RadioThermostat(ClimateDevice) L74
  "Representation of a Radio Thermostat."
  __init__(self, device, hold_temp, away_temps) L77
  "Initialize the thermostat."
  name(self) L95
  "Return the name of the Radio Thermostat."
  temperature_unit(self) L100
  "Return the unit of measurement."
  device_state_attributes(self) L105
  "Return the device specific state attributes."
  current_temperature(self) L113
  "Return the current temperature."
  current_operation(self) L118
  "Return the current operation."
  operation_list(self) L123
  "Return the operation modes list."
  target_temperature(self) L128
  "Return the temperature we try to reach."
  is_away_mode_on(self) L133
  "Return true if away mode is on."
  update(self) L137
  "Update the data from the thermostat."
  set_temperature(self, **kwargs) L153
  "Set new target temperature."
  set_time(self) L167
  "Set device time."
  set_operation_mode(self, operation_mode) L176
  "Set operation mode (auto, cool, heat, off)."
  turn_away_mode_on(self) L187
  "Turn away on."
  turn_away_mode_off(self) L202
  "Turn away off."

setup_platform(hass, config, add_devices, discovery_info=None) L42
"Setup the Radio Thermostat."

homeassistant/components/climate/vera.py
class VeraThermostat(VeraDevice, ClimateDevice) L34
  "Representation of a Vera Thermostat."
  __init__(self, vera_device, controller) L37
  "Initialize the Vera device."
  current_operation(self) L43
  "Return current operation ie."
  operation_list(self) L57
  "List of available operation modes."
  current_fan_mode(self) L62
  "Return the fan setting."
  fan_list(self) L74
  "List of available fan modes."
  set_fan_mode(self, mode) L78
  "Set new target temperature."
  current_power_w(self) L88
  "Current power usage in W."
  update(self) L94
  "Called by the vera device callback to update state."
  temperature_unit(self) L99
  "Return the unit of measurement."
  current_temperature(self) L110
  "Return the current temperature."
  operation(self) L115
  "Return current operation ie."
  target_temperature(self) L120
  "Return the temperature we try to reach."
  set_temperature(self, **kwargs) L124
  "Set new target temperatures."
  set_operation_mode(self, operation_mode) L129
  "Set HVAC mode (auto, cool, heat, off)."
  turn_fan_on(self) L140
  "Turn fan on."
  turn_fan_off(self) L144
  "Turn fan off."

setup_platform(hass, config, add_devices_callback, discovery_info=None) L27
"Find and return Vera thermostats."

homeassistant/components/climate/wink.py
class WinkThermostat(WinkDevice, ClimateDevice) L47
  "Representation of a Wink thermostat."
  __init__(self, wink, hass, temp_unit) L50
  "Initialize the Wink device."
  temperature_unit(self) L56
  "Return the unit of measurement."
  device_state_attributes(self) L62
  "Return the optional state attributes."
  current_temperature(self) L94
  "Return the current temperature."
  current_humidity(self) L99
  "Return the current humidity."
  external_temperature(self) L111
  "Return the current external temperature."
  smart_temperature(self) L116
  "Return the current average temp of all remote sensor."
  eco_target(self) L121
  "Return status of eco target (Is the termostat in eco mode)."
  occupied(self) L126
  "Return status of if the thermostat has detected occupancy."
  current_operation(self) L131
  "Return current operation ie."
  target_humidity(self) L150
  "Return the humidity we try to reach."
  target_temperature(self) L164
  "Return the temperature we try to reach."
  target_temperature_low(self) L177
  "Return the lower bound temperature we try to reach."
  target_temperature_high(self) L184
  "Return the higher bound temperature we try to reach."
  is_away_mode_on(self) L191
  "Return if away mode is on."
  is_aux_heat_on(self) L196
  "Return true if aux heater."
  set_temperature(self, **kwargs) L205
  "Set new target temperature."
  set_operation_mode(self, operation_mode) L221
  "Set operation mode."
  operation_list(self) L237
  "List of available operation modes."
  turn_away_mode_on(self) L251
  "Turn away on."
  turn_away_mode_off(self) L255
  "Turn away off."
  current_fan_mode(self) L260
  "Return whether the fan is on."
  fan_list(self) L271
  "List of available fan modes."
  set_fan_mode(self, fan) L277
  "Turn fan on/off."
  turn_aux_heat_on(self) L281
  "Turn auxillary heater on."
  turn_aux_heat_off(self) L285
  "Turn auxillary heater off."
  min_temp(self) L290
  "Return the minimum temperature."
  max_temp(self) L316
  "Return the maximum temperature."

class WinkAC(WinkDevice, ClimateDevice) L342
  "Representation of a Wink air conditioner."
  __init__(self, wink, hass, temp_unit) L345
  "Initialize the Wink device."
  temperature_unit(self) L351
  "Return the unit of measurement."
  device_state_attributes(self) L357
  "Return the optional state attributes."
  current_temperature(self) L374
  "Return the current temperature."
  current_operation(self) L379
  "Return current operation ie."
  operation_list(self) L394
  "List of available operation modes."
  set_temperature(self, **kwargs) L406
  "Set new target temperature."
  set_operation_mode(self, operation_mode) L411
  "Set operation mode."
  target_temperature(self) L423
  "Return the temperature we try to reach."
  target_temperature_low(self) L428
  "Only supports cool."
  target_temperature_high(self) L433
  "Only supports cool."
  current_fan_mode(self) L438
  "Return the current fan mode."
  fan_list(self) L451
  "List of available fan modes."
  set_fan_mode(self, mode) L455
  "Set fan speed."

setup_platform(hass, config, add_devices, discovery_info=None) L32
"Setup the Wink thermostat."

homeassistant/components/climate/zwave.py
class ZWaveClimate(ZWaveDeviceEntity, ClimateDevice) L41
  "Representation of a Z-Wave Climate device."
  __init__(self, values, temp_unit) L44
  "Initialize the Z-Wave climate device."
  update_properties(self) L73
  "Callback on data changes for node values."
  current_fan_mode(self) L131
  "Return the fan speed set."
  fan_list(self) L136
  "List of available fan modes."
  current_swing_mode(self) L141
  "Return the swing mode set."
  swing_list(self) L146
  "List of available swing modes."
  temperature_unit(self) L151
  "Return the unit of measurement."
  current_temperature(self) L161
  "Return the current temperature."
  current_operation(self) L166
  "Return the current operation mode."
  operation_list(self) L171
  "List of available operation modes."
  target_temperature(self) L176
  "Return the temperature we try to reach."
  set_temperature(self, **kwargs) L180
  "Set new target temperature."
  set_fan_mode(self, fan) L189
  "Set new target fan mode."
  set_operation_mode(self, operation_mode) L194
  "Set new target operation mode."
  set_swing_mode(self, swing_mode) L199
  "Set new target swing mode."
  device_state_attributes(self) L206
  "Return the device specific state attributes."

get_device(hass, values, **kwargs) L35
"Create zwave entity device."

homeassistant/components/config/__init__.py
class EditKeyBasedConfigView(HomeAssistantView) L63
  "Configure a Group endpoint."
  __init__(self, component, config_type, path, key_schema, data_schema, *, post_write_hook=None) L66
  "Initialize a config view."
  get(self, request, config_key) L77
  "Fetch device specific config."
  post(self, request, config_key) L85
  "Validate config and return results."

async_setup(hass, config) L22
"Setup the config component."
setup_panel(panel_name) L27
"Setup a panel."
component_loaded(event) L52
"Respond to components being loaded."
_read(path) L120
"Read YAML helper."
_write(path, data) L130
"Write YAML helper."

homeassistant/components/config/core.py
class CheckConfigView(HomeAssistantView) L15
  "Hassbian packages endpoint."
  post(self, request) L22
  "Validate config and return results."

async_setup(hass) L9
"Setup the hassbian config."

homeassistant/components/config/group.py
async_setup(hass) L13
"Setup the Group config API."

homeassistant/components/config/hassbian.py
class HassbianSuitesView(HomeAssistantView) L59
  "Hassbian packages endpoint."
  __init__(self, test_mode) L65
  "Initialize suites view."
  get(self, request) L70
  "Request suite status."

class HassbianSuiteInstallView(HomeAssistantView) L77
  "Hassbian packages endpoint."
  __init__(self, test_mode) L83
  "Initialize suite view."
  post(self, request, suite) L88
  "Request suite status."

async_setup(hass) L34
"Setup the hassbian config."
hassbian_status(hass, test_mode=False) L50
"Query for the Hassbian status."

homeassistant/components/config/zwave.py
async_setup(hass) L13
"Setup the Z-Wave config API."

homeassistant/components/configurator.py
class Configurator(object) L92
  "The class to keep track of current configuration requests."
  __init__(self, hass) L95
  "Initialize the configurator."
  request_config(self, name, callback, description, description_image, submit_caption, fields, link_name, link_url, entity_picture) L103
  "Setup a request for configuration."
  notify_errors(self, request_id, error) L138
  "Update the state with errors."
  request_done(self, request_id) L152
  "Remove the configuration request."
  deferred_remove(event) L165
  "Remove the request state."
  handle_service_call(self, call) L171
  "Handle a configure service call."
  _generate_unique_id(self) L185
  "Generate a unique configurator ID."
  _validate_request_id(self, request_id) L190
  "Validate that the request belongs to this instance."

request_config(hass, name, callback, description=None, description_image=None, submit_caption=None, fields=None, link_name=None, link_url=None, entity_picture=None) L38
"Create a new request for configuration."
notify_errors(request_id, error) L58
"Add errors to a config request."
request_done(request_id) L67
"Mark a configuration request as done."
async_setup(hass, config) L77
"Setup the configurator component."
_get_instance(hass) L82
"Get an instance per hass object."

homeassistant/components/conversation.py
setup(hass, config) L37
"Register the process service."
process(service) L44
"Parse text into commands."

homeassistant/components/cover/__init__.py
class CoverDevice(Entity) L193
  "Representation a cover."
  current_cover_position(self) L198
  "Return current position of cover."
  current_cover_tilt_position(self) L206
  "Return current position of cover tilt."
  state(self) L214
  "Return the state of the cover."
  state_attributes(self) L224
  "Return the state attributes."
  supported_features(self) L239
  "Flag supported features."
  is_closed(self) L254
  "Return if the cover is closed or not."
  open_cover(self, **kwargs) L258
  "Open the cover."
  async_open_cover(self, **kwargs) L262
  "Open the cover."
  close_cover(self, **kwargs) L270
  "Close cover."
  async_close_cover(self, **kwargs) L274
  "Close cover."
  set_cover_position(self, **kwargs) L282
  "Move the cover to a specific position."
  async_set_cover_position(self, **kwargs) L286
  "Move the cover to a specific position."
  stop_cover(self, **kwargs) L294
  "Stop the cover."
  async_stop_cover(self, **kwargs) L298
  "Stop the cover."
  open_cover_tilt(self, **kwargs) L306
  "Open the cover tilt."
  async_open_cover_tilt(self, **kwargs) L310
  "Open the cover tilt."
  close_cover_tilt(self, **kwargs) L318
  "Close the cover tilt."
  async_close_cover_tilt(self, **kwargs) L322
  "Close the cover tilt."
  set_cover_tilt_position(self, **kwargs) L330
  "Move the cover tilt to a specific position."
  async_set_cover_tilt_position(self, **kwargs) L334
  "Move the cover tilt to a specific position."
  stop_cover_tilt(self, **kwargs) L342
  "Stop the cover."
  async_stop_cover_tilt(self, **kwargs) L346
  "Stop the cover."

is_closed(hass, entity_id=None) L87
"Return if the cover is closed based on the statemachine."
open_cover(hass, entity_id=None) L93
"Open all or specified cover."
close_cover(hass, entity_id=None) L99
"Close all or specified cover."
set_cover_position(hass, position, entity_id=None) L105
"Move to specific position all or specified cover."
stop_cover(hass, entity_id=None) L112
"Stop all or specified cover."
open_cover_tilt(hass, entity_id=None) L118
"Open all or specified cover tilt."
close_cover_tilt(hass, entity_id=None) L124
"Close all or specified cover tilt."
set_cover_tilt_position(hass, tilt_position, entity_id=None) L130
"Move to specific tilt position all or specified cover."
stop_cover_tilt(hass, entity_id=None) L137
"Stop all or specified cover tilt."
async_setup(hass, config) L144
"Track states and offer events for covers."
async_handle_cover_service(service) L152
"Handle calls to the cover services."

homeassistant/components/cover/command_line.py
class CommandCover(CoverDevice) L63
  "Representation a command line cover."
  __init__(self, hass, name, command_open, command_close, command_stop, command_state, value_template) L66
  "Initialize the cover."
  _move_cover(command) L79
  "Execute the actual commands."
  _query_state_value(command) L91
  "Execute state command for return value."
  should_poll(self) L102
  "Only poll if we have state command."
  name(self) L107
  "Return the name of the cover."
  is_closed(self) L112
  "Return if the cover is closed."
  current_cover_position(self) L121
  "Return current position of cover."
  _query_state(self) L128
  "Query for the state."
  update(self) L135
  "Update device state."
  open_cover(self, **kwargs) L144
  "Open the cover."
  close_cover(self, **kwargs) L148
  "Close the cover."
  stop_cover(self, **kwargs) L152
  "Stop the cover."

setup_platform(hass, config, add_devices, discovery_info=None) L34
"Setup cover controlled by shell commands."

homeassistant/components/cover/demo.py
class DemoCover(CoverDevice) L23
  "Representation of a demo cover."
  __init__(self, hass, name, position=None, tilt_position=None, device_class=None, supported_features=None) L27
  "Initialize the cover."
  name(self) L48
  "Return the name of the cover."
  should_poll(self) L53
  "No polling needed for a demo cover."
  current_cover_position(self) L58
  "Return the current position of the cover."
  current_cover_tilt_position(self) L63
  "Return the current tilt position of the cover."
  is_closed(self) L68
  "Return if the cover is closed."
  device_class(self) L73
  "Return the class of this device, from component DEVICE_CLASSES."
  supported_features(self) L78
  "Flag supported features."
  close_cover(self, **kwargs) L85
  "Close the cover."
  close_cover_tilt(self, **kwargs) L97
  "Close the cover tilt."
  open_cover(self, **kwargs) L105
  "Open the cover."
  open_cover_tilt(self, **kwargs) L117
  "Open the cover tilt."
  set_cover_position(self, position, **kwargs) L125
  "Move the cover to a specific position."
  set_cover_tilt_position(self, tilt_position, **kwargs) L134
  "Move the cover til to a specific position."
  stop_cover(self, **kwargs) L143
  "Stop the cover."
  stop_cover_tilt(self, **kwargs) L152
  "Stop the cover tilt."
  _listen_cover(self) L162
  "Listen for changes in cover."
  _time_changed_cover(self, now) L168
  "Track time changes."
  _listen_cover_tilt(self) L182
  "Listen for changes in cover tilt."
  _time_changed_cover_tilt(self, now) L188
  "Track time changes."

setup_platform(hass, config, add_devices, discovery_info=None) L12
"Setup the Demo covers."

homeassistant/components/cover/garadget.py
class GaradgetCover(CoverDevice) L78
  "Representation of a demo cover."
  __init__(self, hass, args) L82
  "Initialize the cover."
  __del__(self) L123
  "Try to remove token."
  name(self) L130
  "Return the name of the cover."
  should_poll(self) L135
  "No polling needed for a demo cover."
  available(self) L140
  "Return True if entity is available."
  device_state_attributes(self) L145
  "Return the device state attributes."
  is_closed(self) L164
  "Return if the cover is closed."
  device_class(self) L172
  "Return the class of this device, from component DEVICE_CLASSES."
  get_token(self) L176
  "Get new token for usage during this session."
  remove_token(self) L190
  "Remove authorization token from API."
  _start_watcher(self, command) L198
  "Start watcher."
  _check_state(self, now) L205
  "Check the state of the service during an operation."
  close_cover(self) L209
  "Close the cover."
  open_cover(self) L216
  "Open the cover."
  stop_cover(self) L223
  "Stop the door where it is."
  update(self) L230
  "Get updated status from API."
  _get_variable(self, var) L254
  "Get latest status."
  _put_command(self, func, arg=None) L269
  "Send commands to API."

setup_platform(hass, config, add_devices, discovery_info=None) L57
"Setup the Demo covers."

homeassistant/components/cover/homematic.py
class HMCover(HMDevice, CoverDevice) L35
  "Represents a Homematic Cover in Home Assistant."
  current_cover_position(self) L39
  "Return current position of cover."
  set_cover_position(self, **kwargs) L47
  "Move the cover to a specific position."
  is_closed(self) L56
  "Return if the cover is closed."
  open_cover(self, **kwargs) L64
  "Open the cover."
  close_cover(self, **kwargs) L68
  "Close the cover."
  stop_cover(self, **kwargs) L72
  "Stop the device if in motion."
  _init_data_struct(self) L76
  "Generate a data dict (self._data) from hm metadata."

setup_platform(hass, config, add_devices, discovery_info=None) L21
"Setup the platform."

homeassistant/components/cover/isy994.py
class ISYCoverDevice(isy.ISYDevice, CoverDevice) L54
  "Representation of an ISY994 cover device."
  __init__(self, node) L57
  "Initialize the ISY994 cover device."
  current_cover_position(self) L62
  "Get the current cover position."
  is_closed(self) L67
  "Get whether the ISY994 cover device is closed."
  state(self) L72
  "Get the state of the ISY994 cover device."
  open_cover(self, **kwargs) L76
  "Send the open cover command to the ISY994 cover device."
  close_cover(self, **kwargs) L81
  "Send the close cover command to the ISY994 cover device."

class ISYCoverProgram(ISYCoverDevice) L87
  "Representation of an ISY994 cover program."
  __init__(self, name, node, actions) L90
  "Initialize the ISY994 cover program."
  state(self) L97
  "Get the state of the ISY994 cover program."
  open_cover(self, **kwargs) L101
  "Send the open cover command to the ISY994 cover program."
  close_cover(self, **kwargs) L106
  "Send the close cover command to the ISY994 cover program."

setup_platform(hass, config, add_devices, discovery_info=None) L28
"Setup the ISY994 cover platform."

homeassistant/components/cover/mqtt.py
class MqttCover(CoverDevice) L73
  "Representation of a cover that can be controlled using MQTT."
  __init__(self, name, state_topic, command_topic, qos, retain, state_open, state_closed, payload_open, payload_close, payload_stop, optimistic, value_template) L76
  "Initialize the cover."
  async_added_to_hass(self) L96
  "Subscribe mqtt events."
  message_received(topic, payload, qos) L102
  "A new MQTT message has been received."
  should_poll(self) L134
  "No polling needed."
  name(self) L139
  "Return the name of the cover."
  is_closed(self) L144
  "Return if the cover is closed."
  current_cover_position(self) L149
  "Return current position of cover."
  async_open_cover(self, **kwargs) L157
  "Move the cover up."
  async_close_cover(self, **kwargs) L171
  "Move the cover down."
  async_stop_cover(self, **kwargs) L185
  "Stop the device."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L51
"Setup the MQTT Cover."

homeassistant/components/cover/myq.py
class MyQDevice(CoverDevice) L55
  "Representation of a MyQ cover."
  __init__(self, myq, device) L58
  "Initialize with API object, device id."
  should_poll(self) L66
  "Poll for state."
  name(self) L71
  "Return the name of the garage door if any."
  is_closed(self) L76
  "Return True if cover is closed, else False."
  close_cover(self) L80
  "Issue close command to cover."
  open_cover(self) L84
  "Issue open command to cover."
  update(self) L88
  "Update status of cover."

setup_platform(hass, config, add_devices, discovery_info=None) L29
"Setup the MyQ component."

homeassistant/components/cover/mysensors.py
class MySensorsCover(mysensors.MySensorsDeviceEntity, CoverDevice) L41
  "Representation of the value of a MySensors Cover child node."
  assumed_state(self) L45
  "Return True if unable to access real state of entity."
  is_closed(self) L50
  "Return True if cover is closed."
  current_cover_position(self) L59
  "Return current position of cover."
  open_cover(self, **kwargs) L67
  "Move the cover up."
  close_cover(self, **kwargs) L80
  "Move the cover down."
  set_cover_position(self, **kwargs) L93
  "Move the cover to a specific position."
  stop_cover(self, **kwargs) L104
  "Stop the device."

setup_platform(hass, config, add_devices, discovery_info=None) L17
"Setup the mysensors platform for covers."

homeassistant/components/cover/rfxtrx.py
class RfxtrxCover(rfxtrx.RfxtrxDevice, CoverDevice) L43
  "Representation of an rfxtrx cover."
  should_poll(self) L47
  "No polling available in rfxtrx cover."
  is_closed(self) L52
  "Return if the cover is closed."
  open_cover(self, **kwargs) L56
  "Move the cover up."
  close_cover(self, **kwargs) L60
  "Move the cover down."
  stop_cover(self, **kwargs) L64
  "Stop the cover."

setup_platform(hass, config, add_devices_callback, discovery_info=None) L16
"Setup the RFXtrx cover."
cover_update(event) L25
"Callback for cover updates from the RFXtrx gateway."

homeassistant/components/cover/rpi_gpio.py
class RPiGPIOCover(CoverDevice) L66
  "Representation of a Raspberry GPIO cover."
  __init__(self, name, relay_pin, state_pin, state_pull_mode, relay_time) L69
  "Initialize the cover."
  unique_id(self) L83
  "Return the ID of this cover."
  name(self) L88
  "Return the name of the cover if any."
  update(self) L92
  "Update the state of the cover."
  is_closed(self) L97
  "Return true if cover is closed."
  _trigger(self) L101
  "Trigger the cover."
  close_cover(self) L107
  "Close the cover."
  open_cover(self) L112
  "Open the cover."

setup_platform(hass, config, add_devices, discovery_info=None) L52
"Setup the RPi cover platform."

homeassistant/components/cover/scsgate.py
class SCSGateCover(CoverDevice) L48
  "Representation of SCSGate cover."
  __init__(self, scs_id, name, logger) L51
  "Initialize the cover."
  scs_id(self) L58
  "Return the SCSGate ID."
  should_poll(self) L63
  "No polling needed."
  name(self) L68
  "Return the name of the cover."
  is_closed(self) L73
  "Return if the cover is closed."
  open_cover(self, **kwargs) L77
  "Move the cover."
  close_cover(self, **kwargs) L84
  "Move the cover down."
  stop_cover(self, **kwargs) L91
  "Stop the cover."
  process_event(self, message) L97
  "Handle a SCSGate message related with this cover."

setup_platform(hass, config, add_devices, discovery_info=None) L25
"Setup the SCSGate cover."

homeassistant/components/cover/tellduslive.py
class TelldusLiveCover(TelldusLiveEntity, CoverDevice) L25
  "Representation of a cover."
  is_closed(self) L29
  "Return the current position of the cover."
  close_cover(self, **kwargs) L33
  "Close the cover."
  open_cover(self, **kwargs) L38
  "Open the cover."
  stop_cover(self, **kwargs) L43
  "Stop the cover."

setup_platform(hass, config, add_devices, discovery_info=None) L18
"Setup covers."

homeassistant/components/cover/vera.py
class VeraCover(VeraDevice, CoverDevice) L25
  "Represents a Vera Cover in Home Assistant."
  __init__(self, vera_device, controller) L28
  "Initialize the Vera device."
  current_cover_position(self) L34
  "Return current position of cover."
  set_cover_position(self, position, **kwargs) L47
  "Move the cover to a specific position."
  is_closed(self) L53
  "Return if the cover is closed."
  open_cover(self, **kwargs) L61
  "Open the cover."
  close_cover(self, **kwargs) L66
  "Close the cover."
  stop_cover(self, **kwargs) L71
  "Stop the cover."

setup_platform(hass, config, add_devices, discovery_info=None) L18
"Find and return Vera covers."

homeassistant/components/cover/wink.py
class WinkCoverDevice(WinkDevice, CoverDevice) L28
  "Representation of a Wink cover device."
  __init__(self, wink, hass) L31
  "Initialize the cover."
  close_cover(self) L35
  "Close the shade."
  open_cover(self) L39
  "Open the shade."
  set_cover_position(self, position, **kwargs) L43
  "Move the roller shutter to a specific position."
  current_cover_position(self) L48
  "Return the current position of roller shutter."
  is_closed(self) L53
  "Return if the cover is closed."

setup_platform(hass, config, add_devices, discovery_info=None) L14
"Setup the Wink cover platform."

homeassistant/components/cover/zwave.py
class ZwaveRollershutter(zwave.ZWaveDeviceEntity, CoverDevice) L37
  "Representation of an Zwave roller shutter."
  __init__(self, hass, values, invert_buttons) L40
  "Initialize the zwave rollershutter."
  update_properties(self) L55
  "Callback on data changes for node values."
  is_closed(self) L70
  "Return if the cover is closed."
  current_cover_position(self) L80
  "Return the current position of Zwave roller shutter."
  open_cover(self, **kwargs) L92
  "Move the roller shutter up."
  close_cover(self, **kwargs) L96
  "Move the roller shutter down."
  set_cover_position(self, position, **kwargs) L100
  "Move the roller shutter to a specific position."
  stop_cover(self, **kwargs) L104
  "Stop the roller shutter."

class ZwaveGarageDoor(zwave.ZWaveDeviceEntity, CoverDevice) L109
  "Representation of an Zwave garage door device."
  __init__(self, values) L112
  "Initialize the zwave garage door."
  update_properties(self) L117
  "Callback on data changes for node values."
  is_closed(self) L122
  "Return the current position of Zwave garage door."
  close_cover(self) L126
  "Close the garage door."
  open_cover(self) L130
  "Open the garage door."
  device_class(self) L135
  "Return the class of this device, from component DEVICE_CLASSES."
  supported_features(self) L140
  "Flag supported features."

get_device(hass, values, node_config, **kwargs) L23
"Create zwave entity device."

homeassistant/components/demo.py
async_setup(hass, config) L39
"Setup a demo environment."
hue_configuration_callback(data) L192
"Fake callback, mark config as done."
setup_configurator() L206
"Setup configurator."

homeassistant/components/device_sun_light_trigger.py
async_setup(hass, config) L47
"The triggers to turn lights on or off based on device presence."
calc_time_for_light_when_sunset() L74
"Calculate the time when to start fading lights in when sun sets."
async_turn_on_before_sunset(light_id) L86
"Helper function to turn on lights."
async_turn_on_factory(light_id) L94
"Factory to generate turn on callbacks."
async_turn_on_light(now) L97
"Turn on specific light."
schedule_light_turn_on(entity, old_state, new_state) L106
"The moment sun sets we want to have all the lights on."
check_light_on_dev_state_change(entity, old_state, new_state) L130
"Handle tracked device state changes."
turn_off_lights_when_all_leave(entity, old_state, new_state) L170
"Handle device group state change."

homeassistant/components/device_tracker/actiontec.py
class ActiontecDeviceScanner(DeviceScanner) L50
  "This class queries a an actiontec router for connected devices."
  __init__(self, config) L53
  "Initialize the scanner."
  scan_devices(self) L64
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L69
  "Return the name of the given device or None if we don't know."
  _update_info(self) L79
  "Ensure the information from the router is up to date."
  get_actiontec_data(self) L99
  "Retrieve data from Actiontec MI424WR and return parsed result."

get_scanner(hass, config) L41
"Validate the configuration and return an Actiontec scanner."

homeassistant/components/device_tracker/aruba.py
class ArubaDeviceScanner(DeviceScanner) L46
  "This class queries a Aruba Access Point for connected devices."
  __init__(self, config) L49
  "Initialize the scanner."
  scan_devices(self) L63
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L68
  "Return the name of the given device or None if we don't know."
  _update_info(self) L78
  "Ensure the information from the Aruba Access Point is up to date."
  get_aruba_data(self) L94
  "Retrieve data from Aruba Access Point and return parsed result."

get_scanner(hass, config) L39
"Validate the configuration and return a Aruba scanner."

homeassistant/components/device_tracker/asuswrt.py
class AsusWrtDeviceScanner(DeviceScanner) L106
  "This class queries a router running ASUSWRT firmware."
  __init__(self, config) L110
  "Initialize the scanner."
  scan_devices(self) L146
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L151
  "Return the name of the given device or None if we don't know."
  _update_info(self) L161
  "Ensure the information from the ASUSWRT router is up to date."
  ssh_connection(self) L183
  "Retrieve data from ASUSWRT via the ssh protocol."
  telnet_connection(self) L224
  "Retrieve data from ASUSWRT via the telnet protocol."
  get_asuswrt_data(self) L267
  "Retrieve data from ASUSWRT and return parsed result."

get_scanner(hass, config) L96
"Validate the configuration and return an ASUS-WRT scanner."

homeassistant/components/device_tracker/automatic.py
class AutomaticData(object) L62
  "A class representing an Automatic cloud service connection."
  __init__(self, hass, session, devices, async_see) L65
  "Initialize the automatic device scanner."
  update(self, now=None) L75
  "Update the device info."
  update_vehicle(self, vehicle, name) L99
  "Updated the specified vehicle's data."

async_setup_scanner(hass, config, async_see, discovery_info=None) L41
"Validate the configuration and return an Automatic scanner."

homeassistant/components/device_tracker/bbox.py
class BboxDeviceScanner(DeviceScanner) L32
  "This class scans for devices connected to the bbox."
  __init__(self, config) L35
  "Initialize the scanner."
  scan_devices(self) L42
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, mac) L48
  "Return the name of the given device or None if we don't know."
  _update_info(self) L59
  "Check the bbox for devices."

get_scanner(hass, config) L22
"Validate the configuration and return a Bbox scanner."

homeassistant/components/device_tracker/bluetooth_le_tracker.py
setup_scanner(hass, config, see, discovery_info=None) L28
"Setup the Bluetooth LE Scanner."
see_device(address, name, new_device=False) L35
"Mark a device as seen."
discover_ble_devices() L54
"Discover Bluetooth LE devices."
update_ble(now) L95
"Lookup Bluetooth LE devices and update status."

homeassistant/components/device_tracker/bluetooth_tracker.py
setup_scanner(hass, config, see, discovery_info=None) L24
"Setup the Bluetooth Scanner."
see_device(device) L29
"Mark a device as seen."
discover_devices() L33
"Discover bluetooth devices."
update_bluetooth(now) L68
"Lookup bluetooth device and update status."

homeassistant/components/device_tracker/bt_home_hub_5.py
class BTHomeHub5DeviceScanner(DeviceScanner) L44
  "This class queries a BT Home Hub 5."
  __init__(self, config) L47
  "Initialise the scanner."
  scan_devices(self) L62
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L68
  "Return the name of the given device or None if we don't know."
  _update_info(self) L81
  "Ensure the information from the BT Home Hub 5 is up to date."

get_scanner(hass, config) L37
"Return a BT Home Hub 5 scanner if successful."
_get_homehub_data(url) L103
"Retrieve data from BT Home Hub 5 and return parsed result."
_parse_homehub_response(data_str) L116
"Parse the BT Home Hub 5 data format."

homeassistant/components/device_tracker/cisco_ios.py
class CiscoDeviceScanner(DeviceScanner) L43
  "This class queries a wireless router running Cisco IOS firmware."
  __init__(self, config) L46
  "Initialize the scanner."
  get_device_name(self, device) L59
  "The firmware doesn't save the name of the wireless device."
  scan_devices(self) L63
  "Scan for new devices and return a list with found device IDs."
  _update_info(self) L70
  "Ensure the information from the Cisco router is up to date."
  _get_arp_data(self) L112
  "Open connection to the router and get arp entries."

get_scanner(hass, config) L36
"Validate the configuration and return a Cisco scanner."
_parse_cisco_mac_address(cisco_hardware_addr) L146
"Parse a Cisco formatted HW address to normal MAC."

homeassistant/components/device_tracker/ddwrt.py
class DdWrtDeviceScanner(DeviceScanner) L45
  "This class queries a wireless router running DD-WRT firmware."
  __init__(self, config) L48
  "Initialize the scanner."
  scan_devices(self) L65
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L71
  "Return the name of the given device or None if we don't know."
  _update_info(self) L105
  "Ensure the information from the DD-WRT router is up to date."
  get_ddwrt_data(self, url) L136
  "Retrieve data from DD-WRT and return parsed result."

get_scanner(hass, config) L37
"Validate the configuration and return a DD-WRT scanner."
_parse_ddwrt_response(data_str) L158
"Parse the DD-WRT data format."

homeassistant/components/device_tracker/demo.py
setup_scanner(hass, config, see, discovery_info=None) L7
"Setup the demo tracker."
offset() L9
"Return random offset."
random_see(dev_id, name) L13
"Randomize a sighting."
observe(call=None) L24
"Observe three entities."

homeassistant/components/device_tracker/fritz.py
class FritzBoxScanner(DeviceScanner) L40
  "This class queries a FRITZ!Box router."
  __init__(self, config) L43
  "Initialize the scanner."
  scan_devices(self) L75
  "Scan for new devices and return a list of found device ids."
  get_device_name(self, mac) L84
  "Return the name of the given device or None if is not known."
  _update_info(self) L94
  "Retrieve latest information from the FRITZ!Box."

get_scanner(hass, config) L34
"Validate the configuration and return FritzBoxScanner."

homeassistant/components/device_tracker/gpslogger.py
class GPSLoggerView(HomeAssistantView) L29
  "View to handle gpslogger requests."
  __init__(self, see) L35
  "Initialize GPSLogger url endpoints."
  get(self, request) L40
  "A GPSLogger message received as GET."
  _handle(self, hass, data) L46
  "Handle gpslogger request."

setup_scanner(hass, config, see, discovery_info=None) L22
"Setup an endpoint for the GPSLogger application."

homeassistant/components/device_tracker/icloud.py
class Icloud(DeviceScanner) L134
  "Represent an icloud account in Home Assistant."
  __init__(self, hass, username, password, name, see) L137
  "Initialize an iCloud account."
  reset_account_icloud(self) L164
  "Reset an icloud account."
  icloud_trusted_device_callback(self, callback_data) L198
  "The trusted device is chosen."
  icloud_need_trusted_device(self) L216
  "We need a trusted device."
  icloud_verification_callback(self, callback_data) L241
  "The trusted device is chosen."
  icloud_need_verification_code(self) L264
  "We need a verification code."
  keep_alive(self, now) L279
  "Keep the api alive."
  determine_interval(self, devicename, latitude, longitude, battery) L317
  "Calculate new interval."
  update_device(self, devicename) L352
  "Update the device_tracker entity."
  lost_iphone(self, devicename) L402
  "Call the lost iphone function if the device is found."
  update_icloud(self, devicename=None) L413
  "Authenticate against iCloud and scan for devices."
  setinterval(self, interval=None, devicename=None) L433
  "Set the interval of the given devices."

setup_scanner(hass, config, see, discovery_info=None) L74
"Set up the iCloud Scanner."
lost_iphone(call) L89
"Call the lost iphone function if the device is found."
update_icloud(call) L99
"Call the update function of an icloud account."
reset_account_icloud(call) L109
"Reset an icloud account."
setinterval(call) L118
"Call the update function of an icloud account."

homeassistant/components/device_tracker/linksys_ap.py
class LinksysAPDeviceScanner(object) L45
  "This class queries a Linksys Access Point."
  __init__(self, config) L48
  "Initialize the scanner."
  scan_devices(self) L63
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, mac) L70
  "Return the name (if known) of the device."
  _update_info(self) L80
  "Check for connected devices."
  _make_request(self, unit=0) L98

get_scanner(hass, config) L37
"Validate the configuration and return a Linksys AP scanner."

homeassistant/components/device_tracker/locative.py
class LocativeView(HomeAssistantView) L31
  "View to handle Locative requests."
  __init__(self, see) L37
  "Initialize Locative URL endpoints."
  get(self, request) L42
  "Locative message received as GET."
  post(self, request) L48
  "Locative message received."
  _handle(self, hass, data) L55
  "Handle locative request."

setup_scanner(hass, config, see, discovery_info=None) L24
"Set up an endpoint for the Locative application."

homeassistant/components/device_tracker/luci.py
class InvalidLuciTokenError(HomeAssistantError) L35
  "When an invalid token is detected."

class LuciDeviceScanner(DeviceScanner) L48
  "This class queries a wireless router running OpenWrt firmware."
  __init__(self, config) L54
  "Initialize the scanner."
  refresh_token(self) L71
  "Get a new token."
  scan_devices(self) L75
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L80
  "Return the name of the given device or None if we don't know."
  _update_info(self) L100
  "Ensure the information from the Luci router is up to date."

get_scanner(hass, config) L41
"Validate the configuration and return a Luci scanner."
_req_json_rpc(url, method, *args, **kwargs) L134
"Perform one JSON RPC operation."
_get_token(host, username, password) L169
"Get authentication token for the given host+username+password."

homeassistant/components/device_tracker/mqtt.py
async_setup_scanner(hass, config, async_see, discovery_info=None) L29
"Setup the MQTT tracker."
async_tracker_message_received(topic, payload, qos) L37
"MQTT message received."

homeassistant/components/device_tracker/mqtt_json.py
async_setup_scanner(hass, config, async_see, discovery_info=None) L39
"Setup the MQTT tracker."
async_tracker_message_received(topic, payload, qos) L47
"MQTT message received."
_parse_see_args(dev_id, data) L74
"Parse the payload location parameters, into the format see expects."

homeassistant/components/device_tracker/mysensors.py
setup_scanner(hass, config, see, discovery_info=None) L17
"Setup the MySensors tracker."
mysensors_callback(gateway, msg) L19
"Callback for mysensors platform."

homeassistant/components/device_tracker/netgear.py
class NetgearDeviceScanner(DeviceScanner) L51
  "Queries a Netgear wireless router using the SOAP-API."
  __init__(self, host, username, password, port) L54
  "Initialize the scanner."
  scan_devices(self) L74
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, mac) L80
  "Return the name of the given device or None if we don't know."
  _update_info(self) L89
  "Retrieve latest information from the Netgear router."

get_scanner(hass, config) L38
"Validate the configuration and returns a Netgear scanner."

homeassistant/components/device_tracker/nmap_tracker.py
class NmapDeviceScanner(DeviceScanner) L67
  "This class scans for devices using nmap."
  __init__(self, config) L72
  "Initialize the scanner."
  scan_devices(self) L85
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, mac) L91
  "Return the name of the given device or None if we don't know."
  _update_info(self) L102
  "Scan the network for devices."

get_scanner(hass, config) L45
"Validate the configuration and return a Nmap scanner."
_arp(ip_address) L55
"Get the MAC address for a given IP."

homeassistant/components/device_tracker/owntracks.py
get_cipher() L58
"Return decryption function and length of key."
decrypt(ciphertext, key) L66
"Decrypt ciphertext using key."
async_setup_scanner(hass, config, async_see, discovery_info=None) L73
"Set up an OwnTracks tracker."
decrypt_payload(topic, ciphertext) L83
"Decrypt encrypted payload."
validate_payload(topic, payload, data_type) L120
"Validate the OwnTracks payload."
async_owntracks_location_update(topic, payload, qos) L160
"MQTT message received."
async_owntracks_event_update(topic, payload, qos) L180
"MQTT event (geofences) received."
enter_event() L201
"Execute enter event."
leave_event() L221
"Execute leave event."
async_owntracks_waypoint_update(topic, payload, qos) L275
"List of waypoints published by a user."
async_see_beacons(dev_id, kwargs_param) L305
"Set active beacons to the current location."
parse_topic(topic, pretty=False) L334
"Parse an MQTT topic owntracks/user/dev, return (user, dev) tuple."
_parse_see_args(topic, data) L350
"Parse the OwnTracks location parameters, into the format see expects."
_set_gps_from_zone(kwargs, location, zone) L368
"Set the see parameters from the zone parameters."

homeassistant/components/device_tracker/ping.py
class Host(object) L31
  "Host object with ping detection."
  __init__(self, ip_address, dev_id, hass, config) L34
  "Initialize the Host pinger."
  ping(self) L46
  "Send an ICMP echo request and return True if success."
  update(self, see) L57
  "Update device state by sending one or more ping messages."

setup_scanner(hass, config, see, discovery_info=None) L69
"Setup the Host objects and return the update function."
update(now) L78
"Update all the hosts on every interval time."

homeassistant/components/device_tracker/sky_hub.py
class SkyHubDeviceScanner(DeviceScanner) L39
  "This class queries a Sky Hub router."
  __init__(self, config) L42
  "Initialise the scanner."
  scan_devices(self) L57
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L63
  "Return the name of the given device or None if we don't know."
  _update_info(self) L76
  "Ensure the information from the Sky Hub is up to date."

get_scanner(hass, config) L32
"Return a Sky Hub scanner if successful."
_get_skyhub_data(url) L98
"Retrieve data from Sky Hub and return parsed result."
_parse_skyhub_response(data_str) L111
"Parse the Sky Hub data format."

homeassistant/components/device_tracker/snmp.py
class SnmpScanner(DeviceScanner) L50
  "Queries any SNMP capable Access Point for connected devices."
  __init__(self, config) L53
  "Initialize the scanner."
  scan_devices(self) L80
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L88
  "Return the name of the given device or None if we don't know."
  _update_info(self) L94
  "Ensure the information from the device is up to date."
  get_snmp_data(self) L110
  "Fetch MAC addresses from access point via SNMP."

get_scanner(hass, config) L43
"Validate the configuration and return an snmp scanner."

homeassistant/components/device_tracker/swisscom.py
class SwisscomDeviceScanner(DeviceScanner) L39
  "This class queries a router running Swisscom Internet-Box firmware."
  __init__(self, config) L42
  "Initialize the scanner."
  scan_devices(self) L54
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L59
  "Return the name of the given device or None if we don't know."
  _update_info(self) L69
  "Ensure the information from the Swisscom router is up to date."
  get_swisscom_data(self) L88
  "Retrieve data from Swisscom and return parsed result."

get_scanner(hass, config) L32
"Return the Swisscom device scanner."

homeassistant/components/device_tracker/tado.py
class TadoDeviceScanner(DeviceScanner) L45
  "This class gets geofenced devices from Tado."
  __init__(self, hass, config) L48
  "Initialize the scanner."
  async_scan_devices(self) L75
  "Scan for devices and return a list containing found device ids."
  async_get_device_name(self, mac) L86
  "Return the name of the given device or None if we don't know."
  _update_info(self) L97
  "Query Tado for device marked as at home."

get_scanner(hass, config) L36
"Return a Tado scanner."

homeassistant/components/device_tracker/thomson.py
class ThomsonDeviceScanner(DeviceScanner) L50
  "This class queries a router running THOMSON firmware."
  __init__(self, config) L53
  "Initialize the scanner."
  scan_devices(self) L67
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L72
  "Return the name of the given device or None if we don't know."
  _update_info(self) L82
  "Ensure the information from the THOMSON router is up to date."
  get_thomson_data(self) L102
  "Retrieve data from THOMSON and return parsed result."

get_scanner(hass, config) L43
"Validate the configuration and return a THOMSON scanner."

homeassistant/components/device_tracker/tomato.py
class TomatoDeviceScanner(DeviceScanner) L42
  "This class queries a wireless router running Tomato firmware."
  __init__(self, config) L45
  "Initialize the scanner."
  scan_devices(self) L66
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L72
  "Return the name of the given device or None if we don't know."
  _update_tomato_info(self) L83
  "Ensure the information from the Tomato router is up to date."

get_scanner(hass, config) L37
"Validate the configuration and returns a Tomato scanner."

homeassistant/components/device_tracker/tplink.py
class TplinkDeviceScanner(DeviceScanner) L46
  "This class queries a wireless router running TP-Link firmware."
  __init__(self, config) L49
  "Initialize the scanner."
  scan_devices(self) L65
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L71
  "The firmware doesn't save the name of the wireless device."
  _update_info(self) L76
  "Ensure the information from the TP-Link router is up to date."

class Tplink2DeviceScanner(TplinkDeviceScanner) L98
  "This class queries a router with newer version of TP-Link firmware."
  scan_devices(self) L101
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L107
  "The firmware doesn't save the name of the wireless device."
  _update_info(self) L112
  "Ensure the information from the TP-Link router is up to date."

class Tplink3DeviceScanner(TplinkDeviceScanner) L153
  "This class queries the Archer C9 router with version 150811 or high."
  __init__(self, config) L156
  "Initialize the scanner."
  scan_devices(self) L162
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L168
  "The firmware doesn't save the name of the wireless device."
  _get_auth_tokens(self) L175
  "Retrieve auth tokens from the router."
  _update_info(self) L203
  "Ensure the information from the TP-Link router is up to date."

class Tplink4DeviceScanner(TplinkDeviceScanner) L254
  "This class queries an Archer C7 router with TP-Link firmware 150427."
  __init__(self, config) L257
  "Initialize the scanner."
  scan_devices(self) L263
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L269
  "The firmware doesn't save the name of the wireless device."
  _get_auth_tokens(self) L273
  "Retrieve auth tokens from the router."
  _update_info(self) L305
  "Ensure the information from the TP-Link router is up to date."

get_scanner(hass, config) L35
"Validate the configuration and return a TP-Link scanner."

homeassistant/components/device_tracker/trackr.py
class TrackRDeviceScanner(object) L33
  "A class representing a TrackR device."
  __init__(self, hass, config, see) L36
  "Initialize the TrackR device scanner."
  _update_info(self, now=None) L49
  "Update the device info."

setup_scanner(hass, config, see, discovery_info=None) L27
"Validate the configuration and return a TrackR scanner."

homeassistant/components/device_tracker/ubus.py
class UbusDeviceScanner(DeviceScanner) L41
  "This class queries a wireless router running OpenWrt firmware."
  __init__(self, config) L48
  "Initialize the scanner."
  scan_devices(self) L64
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L69
  "Return the name of the given device or None if we don't know."
  _update_info(self) L98
  "Ensure the information from the Luci router is up to date."

get_scanner(hass, config) L34
"Validate the configuration and return an ubus scanner."
_req_json_rpc(url, session_id, rpcmethod, subsystem, method, **params) L127
"Perform one JSON RPC operation."
_get_session_id(url, username, password) L152
"Get the authentication token for the given host+username+password."

homeassistant/components/device_tracker/unifi.py
class UnifiScanner(DeviceScanner) L68
  "Provide device_tracker support from Unifi WAP client data."
  __init__(self, controller) L71
  "Initialize the scanner."
  _update(self) L76
  "Get the clients from the device."
  scan_devices(self) L86
  "Scan for devices."
  get_device_name(self, mac) L91
  "Return the name (if known) of the device."

get_scanner(hass, config) L39
"Setup Unifi device_tracker."

homeassistant/components/device_tracker/upc_connect.py
class UPCDeviceScanner(DeviceScanner) L46
  "This class queries a router running UPC ConnectBox firmware."
  __init__(self, hass, config) L49
  "Initialize the scanner."
  async_logout(event) L69
  "Logout from upc connect box."
  async_scan_devices(self) L78
  "Scan for new devices and return a list with found device IDs."
  async_get_device_name(self, device) L97
  "The firmware doesn't save the name of the wireless device."
  async_login(self) L102
  "Login into firmware and get first token."
  _async_ws_function(self, function, additional_form=None) L129
  "Execute a command on UPC firmware webservice."

async_get_scanner(hass, config) L38
"Return the UPC device scanner."

homeassistant/components/device_tracker/volvooncall.py
setup_scanner(hass, config, see, discovery_info=None) L15
"Setup Volvo tracker."
see_vehicle(vehicle) L26
"Callback for reporting vehicle position."

homeassistant/components/device_tracker/xiaomi.py
class XiaomiDeviceScanner(DeviceScanner) L39
  "This class queries a Xiaomi Mi router."
  __init__(self, config) L45
  "Initialize the scanner."
  scan_devices(self) L59
  "Scan for new devices and return a list with found device IDs."
  get_device_name(self, device) L64
  "Return the name of the given device or None if we don't know."
  _update_info(self) L81
  "Ensure the informations from the router are up to date."
  _retrieve_list_with_retry(self) L96
  "Retrieve the device list with a retry if token is invalid."
  _store_result(self, result) L110
  "Extract and store the device list in self.last_results."

get_scanner(hass, config) L32
"Validate the configuration and return a Xiaomi Device Scanner."
_retrieve_list(host, token, **kwargs) L119
""Get device list for the given host."
_get_token(host, username, password) L158
"Get authentication token for the given host+username+password."

homeassistant/components/digital_ocean.py
class DigitalOcean(object) L60
  "Handle all communication with the Digital Ocean API."
  __init__(self, access_token) L63
  "Initialize the Digital Ocean connection."
  get_droplet_id(self, droplet_name) L71
  "Get the status of a Digital Ocean droplet."
  update(self) L83
  "Use the data from Digital Ocean API."

setup(hass, config) L45
"Set up the Digital Ocean component."

homeassistant/components/discovery.py
async_setup(hass, config) L70
"Start a discovery service."
new_service_found(service, info) L85
"Called when a new service is found."
scan_devices(now) L114
"Scan for devices."
schedule_first(event) L126
"Schedule the first discovery when Home Assistant starts up."
_discover(netdisco) L139
"Discover devices."

homeassistant/components/downloader.py
setup(hass, config) L41
"Listen for download events to download files."
download_file(service) L56
"Start thread to download file specified in the URL."
do_download() L58
"Download the file."

homeassistant/components/dweet.py
setup(hass, config) L35
"Setup the Dweet.io component."
dweet_event_listener(event) L42
"Listen for new messages on the bus and sends them to Dweet.io."
send_data(name, msg) L64
"Send the collected data to Dweet.io."

homeassistant/components/ecobee.py
class EcobeeData(object) L89
  "Get the latest data and update the states."
  __init__(self, config_file) L92
  "Initialize the Ecobee data object."
  update(self) L98
  "Get the latest data from pyecobee."

request_configuration(network, hass, config) L44
"Request configuration steps from the user."
ecobee_configuration_callback(callback_data) L54
"The actions to do when our configuration callback is called."
setup_ecobee(hass, network, config) L70
"Setup Ecobee thermostat."
setup(hass, config) L104
"Setup Ecobee."

homeassistant/components/emoncms_history.py
setup(hass, config) L37
"Set up the Emoncms history component."
send_data(url, apikey, node, payload) L42
"Send payload data to Emoncms."
update_emoncms(time) L61
"Send whitelisted entities states reguarly to Emoncms."

homeassistant/components/emulated_hue/__init__.py
class Config(object) L122
  "Holds configuration variables for the emulated hue bridge."
  __init__(self, hass, conf) L125
  "Initialize the instance."
  entity_id_to_number(self, entity_id) L185
  "Get a unique number for the entity id."
  number_to_entity_id(self, number) L203
  "Convert unique number to entity id."
  is_entity_exposed(self, entity) L215
  "Determine if an entity should be exposed on the emulated bridge."
  _load_numbers_json(self) L238
  "Helper method to load numbers json."
  _save_numbers_json(self) L251
  "Helper method to save numbers json."

setup(hass, yaml_config) L73
"Activate the emulated_hue component."
stop_emulated_hue_bridge(event) L104
"Stop the emulated hue bridge."
start_emulated_hue_bridge(event) L110
"Start the emulated hue bridge."

homeassistant/components/emulated_hue/hue_api.py
class HueUsernameView(HomeAssistantView) L34
  "Handle requests to create a username for the emulated hue bridge."
  post(self, request) L43
  "Handle a POST request."

class HueAllLightsStateView(HomeAssistantView) L57
  "Handle requests for getting and setting info about entities."
  __init__(self, config) L64
  "Initialize the instance of the view."
  get(self, request, username) L69
  "Process a request to get the list of available lights."

class HueOneLightStateView(HomeAssistantView) L85
  "Handle requests for getting and setting info about entities."
  __init__(self, config) L92
  "Initialize the instance of the view."
  get(self, request, username, entity_id) L97
  "Process a request to get the state of an individual light."

class HueOneLightChangeView(HomeAssistantView) L118
  "Handle requests for getting and setting info about entities."
  __init__(self, config) L125
  "Initialize the instance of the view."
  put(self, request, username, entity_number) L130
  "Process a request to set the state of an individual light."

parse_hue_api_put_light_body(request_json, entity) L257
"Parse the body of a request to change the state of a light."
get_entity_state(config, entity) L302
"Retrieve and convert state and brightness values for an entity."
entity_to_json(entity, is_on=None, brightness=None) L342
"Convert an entity to its Hue bridge JSON representation."
create_hue_success_response(entity_id, attr, value) L361
"Create a success response for an attribute set on a light."

homeassistant/components/emulated_hue/upnp.py
class DescriptionXmlView(HomeAssistantView) L16
  "Handles requests for the description.xml file."
  __init__(self, config) L23
  "Initialize the instance of the view."
  get(self, request) L28
  "Handle a GET request."

class UPNPResponderThread(threading.Thread) L58
  "Handle responding to UPNP/SSDP discovery requests."
  __init__(self, host_ip_addr, listen_port, upnp_bind_multicast, advertise_ip, advertise_port) L63
  "Initialize the class."
  run(self) L101
  "Run the server."
  stop(self) L160
  "Stop the server."

clean_socket_close(sock) L168
"Close a socket connection and logs its closure."

homeassistant/components/enocean.py
class EnOceanDongle L40
  "Representation of an EnOcean dongle."
  __init__(self, hass, ser) L43
  "Initialize the EnOcean dongle."
  register_device(self, dev) L51
  "Register another device."
  send_command(self, command) L55
  "Send a command from the EnOcean dongle."
  _combine_hex(self, data) L60
  "Combine list of integer values to one big integer."
  callback(self, temp) L67
  "Callback function for EnOcean Device."

class EnOceanDevice L115
  "Parent class for all devices associated with the EnOcean component."
  __init__(self) L118
  "Initialize the device."
  send_command(self, data, optional, packet_type) L125
  "Send a command via the EnOcean dongle."

setup(hass, config) L29
"Setup the EnOcean component."

homeassistant/components/envisalink.py
class EnvisalinkDevice(Entity) L193
  "Representation of an Envisalink device."
  __init__(self, name, info, controller) L196
  "Initialize the device."
  name(self) L203
  "Return the name of the device."
  should_poll(self) L208
  "No polling needed."

async_setup(hass, config) L85
"Common setup for Envisalink devices."
login_fail_callback(data) L111
"Callback for when the evl rejects our login."
connection_fail_callback(data) L117
"Network failure callback."
connection_success_callback(data) L123
"Callback for a successful connection."
zones_updated_callback(data) L130
"Handle zone timer updates."
alarm_data_updated_callback(data) L136
"Handle non-alarm based info updates."
partition_updated_callback(data) L142
"Handle partition changes thrown by evl (including alarms)."
stop_envisalink(event) L148
"Shutdown envisalink connection and thread on exit."

homeassistant/components/fan/__init__.py
class FanEntity(ToggleEntity) L245
  "Representation of a fan."
  set_speed(self, speed) L248
  "Set the speed of the fan."
  async_set_speed(self, speed) L252
  "Set the speed of the fan."
  set_direction(self, direction) L261
  "Set the direction of the fan."
  async_set_direction(self, direction) L265
  "Set the direction of the fan."
  turn_on(self, speed=None, **kwargs) L273
  "Turn on the fan."
  async_turn_on(self, speed=None, **kwargs) L277
  "Turn on the fan."
  oscillate(self, oscillating) L287
  "Oscillate the fan."
  async_oscillate(self, oscillating) L291
  "Oscillate the fan."
  is_on(self) L300
  "Return true if the entity is on."
  speed(self) L305
  "Return the current speed."
  speed_list(self) L310
  "Get the list of available speeds."
  current_direction(self) L315
  "Return the current direction of the fan."
  state_attributes(self) L320
  "Return optional state attributes."
  supported_features(self) L335
  "Flag supported features."

is_on(hass, entity_id=None) L121
"Return if the fans are on based on the statemachine."
turn_on(hass, entity_id=None, speed=None) L128
"Turn all or specified fan on."
turn_off(hass, entity_id=None) L140
"Turn all or specified fan off."
toggle(hass, entity_id=None) L149
"Toggle all or specified fans."
oscillate(hass, entity_id=None, should_oscillate=True) L158
"Set oscillation on all or specified fan."
set_speed(hass, entity_id=None, speed=None) L170
"Set speed for all or specified fan."
set_direction(hass, entity_id=None, direction=None) L182
"Set direction for all or specified fan."
async_setup(hass, config) L195
"Expose fan control via statemachine and services."
async_handle_fan_service(service) L203
"Hande service call for fans."

homeassistant/components/fan/demo.py
class DemoFan(FanEntity) L28
  "A demonstration fan component."
  __init__(self, hass, name, initial_state) L31
  "Initialize the entity."
  name(self) L40
  "Get entity name."
  should_poll(self) L45
  "No polling needed for a demo fan."
  speed(self) L50
  "Return the current speed."
  speed_list(self) L55
  "Get the list of available speeds."
  turn_on(self, speed=None) L59
  "Turn on the entity."
  turn_off(self) L65
  "Turn off the entity."
  set_speed(self, speed) L70
  "Set the speed of the fan."
  set_direction(self, direction) L75
  "Set the direction of the fan."
  oscillate(self, oscillating) L80
  "Set oscillation."
  current_direction(self) L86
  "Fan direction."
  supported_features(self) L91
  "Flag supported features."

setup_platform(hass, config, add_devices_callback, discovery_info=None) L21
"Setup demo fan platform."

homeassistant/components/fan/isy994.py
class ISYFanDevice(isy.ISYDevice, FanEntity) L61
  "Representation of an ISY994 fan device."
  __init__(self, node) L64
  "Initialize the ISY994 fan device."
  speed(self) L69
  "Return the current speed."
  state(self) L74
  "Get the state of the ISY994 fan device."
  set_speed(self, speed) L78
  "Send the set speed command to the ISY994 fan device."
  turn_on(self, speed=None, **kwargs) L85
  "Send the turn on command to the ISY994 fan device."
  turn_off(self, **kwargs) L89
  "Send the turn off command to the ISY994 fan device."

class ISYFanProgram(ISYFanDevice) L97
  "Representation of an ISY994 fan program."
  __init__(self, name, node, actions) L100
  "Initialize the ISY994 fan program."
  state(self) L108
  "Get the state of the ISY994 fan program."
  turn_off(self, **kwargs) L112
  "Send the turn on command to ISY994 fan program."
  turn_on(self, **kwargs) L119
  "Send the turn off command to ISY994 fan program."

setup_platform(hass, config, add_devices, discovery_info=None) L36
"Setup the ISY994 fan platform."

homeassistant/components/fan/mqtt.py
class MqttFan(FanEntity) L114
  "A MQTT fan component."
  __init__(self, name, topic, templates, qos, retain, payload, speed_list, optimistic) L117
  "Initialize the MQTT fan."
  async_added_to_hass(self) L142
  "Subscribe mqtt events."
  state_received(topic, payload, qos) L156
  "A new MQTT message has been received."
  speed_received(topic, payload, qos) L171
  "A new MQTT message for the speed has been received."
  oscillation_received(topic, payload, qos) L189
  "A new MQTT message has been received."
  should_poll(self) L205
  "No polling needed for a MQTT fan."
  assumed_state(self) L210
  "Return true if we do optimistic updates."
  is_on(self) L215
  "Return true if device is on."
  name(self) L220
  "Get entity name."
  speed_list(self) L225
  "Get the list of available speeds."
  supported_features(self) L230
  "Flag supported features."
  speed(self) L235
  "Return the current speed."
  oscillating(self) L240
  "Return the oscillation state."
  async_turn_on(self, speed=None) L245
  "Turn on the entity."
  async_turn_off(self) L257
  "Turn off the entity."
  async_set_speed(self, speed) L267
  "Set the speed of the fan."
  async_oscillate(self, oscillating) L293
  "Set oscillation."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L79
"Setup MQTT fan platform."

homeassistant/components/fan/wink.py
class WinkFanDevice(WinkDevice, FanEntity) L30
  "Representation of a Wink fan."
  __init__(self, wink, hass) L33
  "Initialize the fan."
  set_direction(self, direction) L37
  "Set the direction of the fan."
  set_speed(self, speed) L41
  "Set the speed of the fan."
  turn_on(self, speed=None, **kwargs) L45
  "Turn on the fan."
  turn_off(self, **kwargs) L49
  "Turn off the fan."
  is_on(self) L54
  "Return true if the entity is on."
  speed(self) L59
  "Return the current speed."
  current_direction(self) L75
  "Return direction of the fan [forward, reverse]."
  speed_list(self) L80
  "Get the list of available speeds."

setup_platform(hass, config, add_devices, discovery_info=None) L21
"Setup the Wink platform."

homeassistant/components/feedreader.py
class FeedManager(object) L47
  "Abstraction over Feedparser module."
  __init__(self, url, hass, storage) L50
  "Initialize the FeedManager object, poll every hour."
  _log_no_entries(self) L64
  "Send no entries log at debug level."
  _update(self) L68
  "Update the feed and publish new entries to the event bus."
  _update_and_fire_entry(self, entry) L99
  "Update last_entry_timestamp and fire entry."
  _publish_new_entries(self) L114
  "Publish new entries to the event bus."

class StoredData(object) L137
  "Abstraction over pickle data storage."
  __init__(self, data_file) L140
  "Initialize pickle data storage."
  _fetch_data(self) L148
  "Fetch data stored into pickle file."
  get_timestamp(self, url) L161
  "Return stored timestamp for given url."
  put_timestamp(self, url, timestamp) L166
  "Update timestamp for given URL."

setup(hass, config) L38
"Set up the Feedreader component."

homeassistant/components/ffmpeg.py
class FFmpegManager(object) L125
  "Helper for ha-ffmpeg."
  __init__(self, hass, ffmpeg_bin, run_test) L128
  "Initialize helper."
  binary(self) L136
  "Return ffmpeg binary from config."
  async_run_test(self, input_source) L141
  "Run test on this input."

class FFmpegBase(Entity) L164
  "Interface object for ffmpeg."
  __init__(self, initial_state=True) L167
  "Initialize ffmpeg base object."
  async_added_to_hass(self) L173
  "Register dispatcher & events."
  available(self) L189
  "Return True if entity is available."
  should_poll(self) L194
  "Return True if entity has to be polled for state."
  _async_start_ffmpeg(self, entity_ids) L199
  "Start a ffmpeg process."
  _async_stop_ffmpeg(self, entity_ids) L207
  "Stop a ffmpeg process."
  _async_restart_ffmpeg(self, entity_ids) L216
  "Stop a ffmpeg process."
  _async_register_events(self) L226
  "Register a ffmpeg process/device."
  async_shutdown_handle(event) L229
  "Stop ffmpeg process."
  async_start_handle(event) L241
  "Start ffmpeg process."

async_start(hass, entity_id=None) L60
"Start a ffmpeg process on entity."
async_stop(hass, entity_id=None) L67
"Stop a ffmpeg process on entity."
async_restart(hass, entity_id=None) L74
"Restart a ffmpeg process on entity."
async_setup(hass, config) L81
"Setup the FFmpeg component."
async_service_handle(service) L97
"Handle service ffmpeg process."

homeassistant/components/foursquare.py
class FoursquarePushReceiver(HomeAssistantView) L83
  "Handle pushes from the Foursquare API."
  __init__(self, push_secret) L90
  "Initialize the OAuth callback view."
  post(self, request) L95
  "Accept the POST from Foursquare."

setup(hass, config) L51
"Setup the Foursquare component."
checkin_user(call) L58
"Check a user in on Swarm."

homeassistant/components/frontend/__init__.py
class BootstrapView(HomeAssistantView) L186
  "View to bootstrap frontend with all needed data."
  get(self, request) L193
  "Return all data needed to bootstrap Home Assistant."

class IndexView(HomeAssistantView) L206
  "Serve the frontend."
  __init__(self) L214
  "Initialize the frontend view."
  get(self, request, entity_id=None) L225
  "Serve the index view."

class ManifestJSONView(HomeAssistantView) L283
  "View to return a manifest.json."
  get(self, request) L291
  "Return the manifest.json."

register_built_in_panel(hass, component_name, sidebar_title=None, sidebar_icon=None, url_path=None, config=None) L51
"Register a built-in panel."
register_panel(hass, component_name, path, md5=None, sidebar_title=None, sidebar_icon=None, url_path=None, url=None, config=None) L72
"Register a panel for the frontend."
add_manifest_json_key(key, val) L140
"Add a keyval to the manifest.json."
setup(hass, config) L145
"Setup serving the frontend."

homeassistant/components/google.py
class GoogleCalendarService(object) L236
  "Calendar service interface to google."
  __init__(self, token_file) L239
  "We just need the token_file."
  get(self) L243
  "Get the calendar service from the storage file token."

do_authentication(hass, config) L89
"Notify user of actions and authenticate."
step2_exchange(now) L129
"Keep trying to validate the user_code until it expires."
setup(hass, config) L160
"Setup the platform."
setup_services(hass, track_new_found_calendars, calendar_service) L176
"Setup service listeners."
_found_calendar(call) L178
"Check if we know about a calendar and generate PLATFORM_DISCOVER."
_scan_for_calendars(service) L198
"Scan for new calendars."
do_setup(hass, config) L215
"Run the setup after we have everything configured."
get_calendar_info(hass, calendar) L255
"Convert data from Google into DEVICE_SCHEMA."
load_config(path) L269
"Load the google_calendar_devices.yaml."
update_config(path, calendar) L289
"Write the google_calendar_devices.yaml."

homeassistant/components/graphite.py
class GraphiteFeeder(threading.Thread) L58
  "Feed data to Graphite."
  __init__(self, hass, host, port, prefix) L61
  "Initialize the feeder."
  start_listen(self, event) L81
  "Start event-processing thread."
  shutdown(self, event) L87
  "Signal shutdown of processing event."
  event_listener(self, event) L92
  "Queue an event for processing."
  _send_to_graphite(self, data) L101
  "Send data to Graphite."
  _report_attributes(self, entity_id, new_state) L110
  "Report the attributes."
  run(self) L133
  "Run the process to export the data."

setup(hass, config) L37
"Setup the Graphite feeder."

homeassistant/components/hassio.py
class HassIO(object) L163
  "Small API wrapper for HassIO."
  __init__(self, loop, websession, ip) L166
  "Initialze HassIO api."
  is_connected(self) L172
  "Return True if it connected to HassIO supervisor."
  send_command(self, cmd, payload=None, timeout=DEFAULT_TIMEOUT) L180
  "Send request to API."
  send_raw(self, cmd, payload=None, timeout=DEFAULT_TIMEOUT, json=True) L193
  "Send raw request to API."

class HassIOBaseView(HomeAssistantView) L219
  "HassIO view to handle base part."
  __init__(self, hassio, base) L224
  "Initialize a hassio base view."
  get(self, request) L233
  "Get base data."

class HassIOBaseEditView(HassIOBaseView) L241
  "HassIO view to handle base with options support."
  __init__(self, hassio, base) L244
  "Initialize a hassio base edit view."
  post(self, request) L250
  "Set options on host."

class HassIOBaseLogsView(HomeAssistantView) L261
  "HassIO view to handle base logs part."
  __init__(self, hassio, base) L266
  "Initialize a hassio base view."
  get(self, request) L275
  "Get logs."

class HassIOAddonsView(HomeAssistantView) L283
  "HassIO view to handle addons part."
  __init__(self, hassio) L290
  "Initialize a hassio addon view."
  get(self, request, addon) L295
  "Get addon data."
  post(self, request, addon) L304
  "Set options on host."

class HassIOAddonsLogsView(HomeAssistantView) L315
  "HassIO view to handle addons logs part."
  __init__(self, hassio) L322
  "Initialize a hassio addon view."
  get(self, request, addon) L327
  "Get addon data."

async_setup(hass, config) L78
"Setup the hassio component."
async_service_handler(service) L107
"Handle HassIO service calls."

homeassistant/components/hdmi_cec.py
class CecDevice(Entity) L329
  "Representation of a HDMI CEC device entity."
  __init__(self, hass, device, logical) L332
  "Initialize the device."
  update(self) L342
  "Update device status."
  _update(self, device=None) L346
  "Update device status."
  name(self) L366
  "Return the name of the device."
  vendor_id(self) L378
  "ID of device's vendor."
  vendor_name(self) L383
  "Name of device's vendor."
  physical_address(self) L388
  "Physical address of device in HDMI network."
  type(self) L393
  "String representation of device's type."
  type_id(self) L398
  "Type ID of device."
  icon(self) L403
  "Icon for device by its type."
  device_state_attributes(self) L410
  "Return the state attributes."

pad_physical_address(addr) L136
"Right-pad a physical address."
parse_mapping(mapping, parents=None) L141
"Parse configuration device mapping."
setup(hass, base_config) L157
"Setup CEC capability."
_volume(call) L189
"Increase/decrease volume and mute/unmute system."
_process_volume(cmd, att) L208
_tx(call) L224
"Send CEC command."
_standby(call) L253
_power_on(call) L256
_select_device(call) L259
"Select the active device."
_update(call) L284
"Callback called when device update is needed."
_new_device(device) L292
"Called when new device is detected by HDMI network."
_shutdown(call) L301
_start_cec(event) L304
"Register services and start HDMI network to watch for devices."

homeassistant/components/history.py
class HistoryPeriodView(HomeAssistantView) L194
  "Handle history period requests."
  __init__(self, filters) L201
  "Initilalize the history period view."
  get(self, request, datetime=None) L206
  "Return history over a period of time."

class Filters(object) L247
  "Container for the configured include and exclude filters."
  __init__(self) L250
  "Initialise the include and exclude filters."
  apply(self, query, entity_ids=None) L257
  "Apply the include/exclude filter on domains and entities on query."

last_recorder_run(hass) L38
"Retireve the last closed recorder run from the DB."
get_significant_states(hass, start_time, end_time=None, entity_id=None, filters=None) L51
"Return states changes during UTC period start_time - end_time."
state_changes_during_period(hass, start_time, end_time=None, entity_id=None) L85
"Return states changes during UTC period start_time - end_time."
get_states(hass, utc_point_in_time, entity_ids=None, run=None, filters=None) L107
"Return the states at a specific point in time."
states_to_json(hass, states, start_time, entity_id, filters=None) L143
"Convert SQL results into JSON friendly data structure."
get_state(hass, utc_point_in_time, entity_id, run=None) L169
"Return a state at a specific point in time."
setup(hass, config) L176
"Setup the history hooks."
_is_significant(state) L307
"Test if state is significant for history charts."

homeassistant/components/homematic.py
class HMHub(Entity) L577
  "The Homematic hub."
  __init__(self, hass, name, use_variables) L580
  "Initialize Homematic hub."
  name(self) L600
  "Return the name of the device."
  should_poll(self) L605
  "Return false."
  state(self) L610
  "Return the state of the entity."
  state_attributes(self) L615
  "Return the state attributes."
  icon(self) L621
  "Return the icon to use in the frontend, if any."
  _update_hub(self, now) L625
  "Retrieve latest state."
  _update_variables(self, now) L631
  "Retrive all variable data and update hmvariable states."
  hm_set_variable(self, name, value) L648
  "Set variable on homematic controller."

class HMDevice(Entity) L664
  "The Homematic device base object."
  __init__(self, hass, config) L667
  "Initialize a generic Homematic device."
  should_poll(self) L686
  "Return false."
  name(self) L691
  "Return the name of the device."
  assumed_state(self) L696
  "Return true if unable to access real state of the device."
  available(self) L701
  "Return true if device is available."
  device_state_attributes(self) L706
  "Return device specific state attributes."
  link_homematic(self) L727
  "Connect to Homematic."
  _hm_event_callback(self, device, caller, attribute, value) L759
  "Handle all pyhomematic device events."
  _subscribe_homematic_events(self) L783
  "Subscribe all required events to handle job."
  _load_data_from_hm(self) L817
  "Load first value from pyhomematic."
  _hm_set_state(self, value) L835
  "Set data to main datapoint."
  _hm_get_state(self) L840
  "Get data from main datapoint."
  _init_data(self) L846
  "Generate a data dict (self._data) from the Homematic metadata."
  _init_data_struct(self) L855
  "Generate a data dict from the Homematic device metadata."

virtualkey(hass, address, channel, param, proxy=None) L204
"Send virtual keypress to homematic controlller."
set_var_value(hass, entity_id, value) L216
"Change value of homematic system variable."
set_dev_value(hass, address, channel, param, value, proxy=None) L226
"Send virtual keypress to homematic controlller."
reconnect(hass) L239
"Reconnect to CCU/Homegear."
setup(hass, config) L245
"Set up the Homematic component."
_hm_service_virtualkey(service) L302
"Service handle virtualkey services."
_service_handle_value(service) L333
"Set value on homematic variable."
_service_handle_reconnect(service) L357
"Reconnect to all homematic hubs."
_service_handle_device(service) L366
"Service handle set_dev_value services."
_system_callback_handler(hass, config, src, *args) L390
"Callback handler."
_get_devices(hass, discovery_type, keys, proxy) L450
"Get the Homematic devices for given discovery_type."
_create_ha_name(name, channel, param, count) L508
"Generate a unique object name."
_hm_event_handler(hass, proxy, device, caller, attribute, value) L527
"Handle all pyhomematic device events."
_device_from_servicecall(hass, service) L564
"Extract homematic device from service call."

homeassistant/components/http/__init__.py
class HomeAssistantWSGI(object) L176
  "WSGI server for Home Assistant."
  __init__(self, hass, development, api_password, ssl_certificate, ssl_key, server_host, server_port, cors_origins, use_x_forwarded_for, trusted_networks, login_threshold, is_ban_enabled) L179
  "Initialize the WSGI Home Assistant server."
  register_view(self, view) L219
  "Register a view with the WSGI server."
  register_redirect(self, url, redirect_to) L244
  "Register a redirect with the server."
  redirect(request) L253
  "Redirect to location."
  register_static_path(self, url_path, path, cache_headers=True) L259
  "Register a folder or file to serve as a static path."
  serve_file(request) L272
  "Serve file from disk."
  serve_file(request) L277
  "Serve file from disk."
  start(self) L296
  "Start the wsgi server."
  stop(self) L342
  "Stop the wsgi server."

class HomeAssistantView(object) L353
  "Base view for all views."
  json(self, result, status_code=200) L361
  "Return a JSON response."
  json_message(self, error, status_code=200) L368
  "Return a JSON message response."
  file(self, request, fil) L374
  "Return a file."
  register(self, router) L379
  "Register the view with a router."

async_setup(hass, config) L106
"Set up the HTTP API and debug interface."
stop_server(event) L145
"Callback to stop the server."
start_server(event) L150
"Callback to start the server."
request_handler_factory(view, handler) L402
"Factory to wrap our handler classes."
handle(request) L408
"Handle incoming request."

homeassistant/components/http/auth.py
auth_middleware(app, handler) L16
"Authentication middleware."
no_auth_middleware_handler(request) L21
"Auth middleware to approve all requests."
auth_middleware_handler(request) L29
"Auth middleware to check authentication."
is_trusted_ip(request) L54
"Test if request is from a trusted ip."
validate_password(request, api_password) L63
"Test if password is valid."

homeassistant/components/http/ban.py
class IpBan(object) L106
  "Represents banned IP address."
  __init__(self, ip_ban, banned_at=None) L109
  "Initializing Ip Ban object."

ban_middleware(app, handler) L36
"IP Ban middleware."
ban_middleware_handler(request) L47
"Verify if IP is not banned."
process_wrong_login(request) L67
"Process a wrong login attempt."
load_ip_bans_config(path) L115
"Loading list of banned IPs from config file."
update_ip_bans_config(path, ip_ban) L139
"Update config file with new banned IP address."

homeassistant/components/http/static.py
class CachingStaticResource(StaticResource) L16
  "Static Resource handler that will add cache headers."
  _handle(self, request) L20

class CachingFileResponse(FileResponse) L44
  "FileSender class that caches output if not in dev mode."
  __init__(self, *args, **kwargs) L47
  "Initialize the hass file sender."
  sendfile(request, fobj, count) L54
  "Sendfile that includes a cache header."

staticresource_middleware(app, handler) L68
"Middleware to strip out fingerprint from fingerprinted assets."
static_middleware_handler(request) L71
"Strip out fingerprints from resource names."

homeassistant/components/http/util.py
get_real_ip(request) L8
"Get IP address of client."

homeassistant/components/ifttt.py
trigger(hass, event, value1=None, value2=None, value3=None) L43
"Trigger a Maker IFTTT recipe."
setup(hass, config) L54
"Setup the IFTTT service component."
trigger_service(call) L58
"Handle IFTTT trigger service calls."

homeassistant/components/image_processing/__init__.py
class ImageProcessingEntity(Entity) L96
  "Base entity class for image processing."
  camera_entity(self) L102
  "Return camera entity id from process pictures."
  confidence(self) L107
  "Return minimum confidence for do some things."
  process_image(self, image) L111
  "Process image."
  async_process_image(self, image) L115
  "Process image."
  async_update(self) L123
  "Update image and process it."

scan(hass, entity_id=None) L62
"Force process a image."
async_setup(hass, config) L69
"Setup image processing."
async_scan_service(service) L80
"Service handler for scan."

homeassistant/components/image_processing/demo.py
class DemoImageProcessingAlpr(ImageProcessingAlprEntity) L23
  "Demo alpr image processing entity."
  __init__(self, camera_entity, name) L26
  "Initialize demo alpr."
  camera_entity(self) L34
  "Return camera entity id from process pictures."
  confidence(self) L39
  "Return minimum confidence for send events."
  name(self) L44
  "Return the name of the entity."
  process_image(self, image) L48
  "Process image."

class DemoImageProcessingFace(ImageProcessingFaceEntity) L60
  "Demo face identify image processing entity."
  __init__(self, camera_entity, name) L63
  "Initialize demo alpr."
  camera_entity(self) L71
  "Return camera entity id from process pictures."
  confidence(self) L76
  "Return minimum confidence for send events."
  name(self) L81
  "Return the name of the entity."
  process_image(self, image) L85
  "Process image."

setup_platform(hass, config, add_devices, discovery_info=None) L14
"Setup the demo image_processing platform."

homeassistant/components/image_processing/microsoft_face_detect.py
class MicrosoftFaceDetectEntity(ImageProcessingFaceEntity) L66
  "Microsoft face api entity for identify."
  __init__(self, camera_entity, api, attributes, name=None) L69
  "Initialize openalpr local api."
  camera_entity(self) L84
  "Return camera entity id from process pictures."
  name(self) L89
  "Return the name of the entity."
  async_process_image(self, image) L94
  "Process image."

validate_attributes(list_attributes) L37
"Validate face attributes."
async_setup_platform(hass, config, async_add_devices, discovery_info=None) L52
"Set up the microsoft face detection platform."

homeassistant/components/image_processing/openalpr_cloud.py
class OpenAlprCloudEntity(ImageProcessingAlprEntity) L72
  "OpenAlpr cloud entity."
  __init__(self, camera_entity, params, confidence, name=None) L75
  "Initialize openalpr local api."
  confidence(self) L90
  "Return minimum confidence for send events."
  camera_entity(self) L95
  "Return camera entity id from process pictures."
  name(self) L100
  "Return the name of the entity."
  async_process_image(self, image) L105
  "Process image."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L53
"Set up the openalpr cloud api platform."

homeassistant/components/influxdb.py
setup(hass, config) L53
"Setup the InfluxDB component."
influx_event_listener(event) L95
"Listen for new messages on the bus and sends them to Influx."

homeassistant/components/input_boolean.py
class InputBoolean(ToggleEntity) L111
  "Representation of a boolean input."
  __init__(self, object_id, name, initial, icon) L114
  "Initialize a boolean input."
  should_poll(self) L122
  "If entity should be polled."
  name(self) L127
  "Return name of the boolean input."
  icon(self) L132
  "Returh the icon to be used for this entity."
  is_on(self) L137
  "Return true if entity is on."
  async_added_to_hass(self) L142
  "Called when entity about to be added to hass."
  async_turn_on(self, **kwargs) L152
  "Turn the entity on."
  async_turn_off(self, **kwargs) L158
  "Turn the entity off."

is_on(hass, entity_id) L44
"Test if input_boolean is True."
turn_on(hass, entity_id) L49
"Set input_boolean to True."
turn_off(hass, entity_id) L54
"Set input_boolean to False."
toggle(hass, entity_id) L59
"Set input_boolean to False."
async_setup(hass, config) L65
"Set up input boolean."
async_handler_service(service) L85
"Handle a calls to the input boolean services."

homeassistant/components/input_select.py
class InputSelect(Entity) L187
  "Representation of a select input."
  __init__(self, object_id, name, initial, options, icon) L190
  "Initialize a select input."
  async_added_to_hass(self) L199
  "Called when entity about to be added to hass."
  should_poll(self) L211
  "If entity should be polled."
  name(self) L216
  "Return the name of the select input."
  icon(self) L221
  "Return the icon to be used for this entity."
  state(self) L226
  "Return the state of the component."
  state_attributes(self) L231
  "Return the state attributes."
  async_select_option(self, option) L238
  "Select new option."
  async_offset_index(self, offset) L248
  "Offset current index."
  async_set_options(self, options) L256
  "Set options."

_cv_input_select(cfg) L58
"Config validation helper for input select (Voluptuous)."
select_option(hass, entity_id, option) L80
"Set value of input_select."
select_next(hass, entity_id) L88
"Set next value of input_select."
select_previous(hass, entity_id) L95
"Set previous value of input_select."
set_options(hass, entity_id, options) L102
"Set options of input_select."
async_setup(hass, config) L111
"Setup input select."
async_select_option_service(call) L128
"Handle a calls to the input select option service."
async_select_next_service(call) L142
"Handle a calls to the input select next service."
async_select_previous_service(call) L156
"Handle a calls to the input select previous service."
async_set_options_service(call) L170
"Handle a calls to the set options service."

homeassistant/components/input_slider.py
class InputSlider(Entity) L119
  "Represent an slider."
  __init__(self, object_id, name, initial, minimum, maximum, step, icon, unit) L122
  "Initialize a select input."
  should_poll(self) L135
  "If entity should be polled."
  name(self) L140
  "Name of the select input."
  icon(self) L145
  "Icon to be used for this entity."
  state(self) L150
  "State of the component."
  unit_of_measurement(self) L155
  "Unit of measurement of slider."
  state_attributes(self) L160
  "State attributes."
  async_added_to_hass(self) L169
  "Called when entity about to be added to hass."
  async_select_value(self, value) L184
  "Select new value."

_cv_input_slider(cfg) L41
"Config validation helper for input slider (Voluptuous)."
select_value(hass, entity_id, value) L71
"Set input_slider to value."
async_setup(hass, config) L80
"Set up input slider."
async_select_value_service(call) L102
"Handle a calls to the input slider services."

homeassistant/components/insteon_hub.py
setup(hass, config) L31
"Setup Insteon Hub component."

homeassistant/components/insteon_local.py
setup(hass, config) L35
"Set up Insteon Hub component."

homeassistant/components/insteon_plm.py
async_setup(hass, config) L42
"Set up our connection to the PLM."
async_plm_new_device(device) L51
"New device detected from transport to be delegated to platform."
common_attributes(entity) L94
"Return the device state attributes."

homeassistant/components/introduction.py
setup(hass, config=None) L18
"Setup the introduction component."

homeassistant/components/ios.py
class iOSPushConfigView(HomeAssistantView) L265
  "A view that provides the push categories configuration."
  __init__(self, push_config) L271
  "Init the view."
  get(self, request) L276
  "Handle the GET request for the push configuration."

class iOSIdentifyDeviceView(HomeAssistantView) L281
  "A view that accepts device identification requests."
  post(self, request) L288
  "Handle the POST request for device identification."

_load_config(filename) L177
"Load configuration."
_save_config(filename, config) L196
"Save configuration."
devices_with_push() L207
"Return a dictionary of push enabled targets."
enabled_push_ids() L216
"Return a list of push enabled target push IDs."
devices() L226
"Return a dictionary of all identified devices."
device_name_for_push_id(push_id) L231
"Return the device name for the push ID."
setup(hass, config) L239
"Setup the iOS component."

homeassistant/components/isy994.py
class ISYDevice(Entity) L217
  "Representation of an ISY994 device."
  __init__(self, node) L224
  "Initialize the insteon device."
  on_update(self, event) L232
  "Handle the update event from the ISY994 Node."
  domain(self) L237
  "Get the domain of the device."
  unique_id(self) L242
  "Get the unique identifier of the device."
  raw_name(self) L248
  "Get the raw name of the device."
  name(self) L254
  "Get the name of the device."
  should_poll(self) L260
  "No polling required since we're using the subscription."
  value(self) L265
  "Get the current value of the device."
  device_state_attributes(self) L271
  "Get the state attributes for the device."
  hidden(self) L280
  "Get whether the device should be hidden from the UI."
  unit_of_measurement(self) L285
  "Get the device unit of measure."
  _attr_filter(self, attr) L289
  "Filter the attribute."
  update(self) L294
  "Perform an update for the device."

filter_nodes(nodes, units=None, states=None) L67
"Filter a list of ISY nodes based on the units and states provided."
_categorize_nodes(hidden_identifier, sensor_identifier) L91
"Categorize the ISY994 nodes."
_categorize_programs() L114
"Categorize the ISY994 programs."
_categorize_weather() L140
"Categorize the ISY994 weather data."
setup(hass, config) L151
"Set up the ISY 994 platform."
stop(event) L212
"Stop ISY auto updates."

homeassistant/components/joaoapps_join.py
register_device(hass, device_id, api_key, name) L30
"Method to register services for each join device listed."
ring_service(service) L35
"Service to ring devices."
set_wallpaper_service(service) L39
"Service to set wallpaper on devices."
send_file_service(service) L43
"Service to send files to devices."
send_url_service(service) L47
"Service to open url on devices."
send_tasker_service(service) L51
"Service to open url on devices."
send_sms_service(service) L57
"Service to send sms from devices."
setup(hass, config) L73
"Setup Join services."

homeassistant/components/keyboard.py
volume_up(hass) L21
"Press the keyboard button for volume up."
volume_down(hass) L26
"Press the keyboard button for volume down."
volume_mute(hass) L31
"Press the keyboard button for muting volume."
media_play_pause(hass) L36
"Press the keyboard button for play/pause."
media_next_track(hass) L41
"Press the keyboard button for next track."
media_prev_track(hass) L46
"Press the keyboard button for prev track."
setup(hass, config) L51
"Listen for keyboard events."

homeassistant/components/keyboard_remote.py
class KeyboardRemote(threading.Thread) L79
  "This interfaces with the inputdevice using evdev."
  __init__(self, hass, config) L82
  "Construct a KeyboardRemote interface object."
  _get_keyboard_device(self) L118
  run(self) L134
  "Main loop of the KeyboardRemote."

setup(hass, config) L47
"Setup keyboard_remote."
_start_keyboard_remote(_event) L61
_stop_keyboard_remote(_event) L64

homeassistant/components/knx.py
class KNXConfig(object) L76
  "Handle the fetching of configuration from the config file."
  __init__(self, config) L79
  "Initialize the configuration."
  name(self) L96
  "The name given to the entity."
  address(self) L101
  "The address of the device as an integer value."
  state_address(self) L112
  "The group address the device sends its current state to."

class KNXGroupAddress(Entity) L122
  "Representation of devices connected to a KNX group address."
  __init__(self, hass, config) L125
  "Initialize the device."
  handle_knx_message(addr, data) L132
  "Handle an incoming KNX frame."
  name(self) L147
  "The entity's display name."
  config(self) L152
  "The entity's configuration."
  should_poll(self) L157
  "Return the state of the polling, if needed."
  is_on(self) L162
  "Return True if the value is not 0 is on, else False."
  address(self) L167
  "Return the KNX group address."
  state_address(self) L172
  "Return the KNX group address."
  cache(self) L177
  "The name given to the entity."
  group_write(self, value) L181
  "Write to the group address."
  update(self) L185
  "Get the state from KNX bus or cache."

class KNXMultiAddressDevice(Entity) L209
  "Representation of devices connected to a multiple KNX group address."
  __init__(self, hass, config, required, optional=None) L219
  "Initialize the device."
  name(self) L259
  "The entity's display name."
  config(self) L264
  "The entity's configuration."
  should_poll(self) L269
  "Return the state of the polling, if needed."
  cache(self) L274
  "The name given to the entity."
  has_attribute(self, name) L278
  "Check if the attribute with the given name is defined."
  value(self, name) L288
  "Return the value to a given named attribute."
  set_value(self, name, value) L309
  "Set the value of a given named attribute."

setup(hass, config) L36
"Setup the connection to the KNX IP interface."
close_tunnel(_data) L68
"Close the NKX tunnel connection on shutdown."

homeassistant/components/light/__init__.py
class Light(ToggleEntity) L315
  "Representation of a light."
  brightness(self) L321
  "Return the brightness of this light between 0..255."
  xy_color(self) L326
  "Return the XY color value [float, float]."
  rgb_color(self) L331
  "Return the RGB color value [int, int, int]."
  color_temp(self) L336
  "Return the CT color value in mireds."
  white_value(self) L341
  "Return the white value of this light between 0..255."
  effect_list(self) L346
  "Return the list of supported effects."
  effect(self) L351
  "Return the current effect."
  state_attributes(self) L356
  "Return optional state attributes."
  supported_features(self) L375
  "Flag supported features."
  async_added_to_hass(self) L380
  "Component added, restore_state using platforms."

extract_info(state) L128
"Extract light parameters from a state object."
is_on(hass, entity_id=None) L136
"Return if the lights are on based on the statemachine."
turn_on(hass, entity_id=None, transition=None, brightness=None, rgb_color=None, xy_color=None, color_temp=None, white_value=None, profile=None, flash=None, effect=None, color_name=None) L142
"Turn all or specified light on."
async_turn_on(hass, entity_id=None, transition=None, brightness=None, rgb_color=None, xy_color=None, color_temp=None, white_value=None, profile=None, flash=None, effect=None, color_name=None) L153
"Turn all or specified light on."
turn_off(hass, entity_id=None, transition=None) L177
"Turn all or specified light off."
async_turn_off(hass, entity_id=None, transition=None) L183
"Turn all or specified light off."
toggle(hass, entity_id=None, transition=None) L196
"Toggle all or specified light."
async_setup(hass, config) L209
"Expose light control via statemachine and services."
async_handle_light_service(service) L223
"Hande a turn light on or off service call."
_load_profile_data(hass) L288
"Load built-in profiles and custom profiles."

homeassistant/components/light/avion.py
class AvionLight(Light) L48
  "Representation of an Avion light."
  __init__(self, device) L51
  "Initialize the light."
  unique_id(self) L66
  "Return the ID of this light."
  name(self) L71
  "Return the name of the device if any."
  is_on(self) L76
  "Return true if device is on."
  brightness(self) L81
  "Return the brightness of this light between 0..255."
  supported_features(self) L86
  "Flag supported features."
  should_poll(self) L91
  "Don't poll."
  assumed_state(self) L96
  "We can't read the actual state, so assume it matches."
  set_state(self, brightness) L100
  "Set the state of this lamp to the provided brightness."
  turn_on(self, **kwargs) L105
  "Turn the specified or all lights on."
  turn_off(self, **kwargs) L115
  "Turn the specified or all lights off."

setup_platform(hass, config, add_devices, discovery_info=None) L33
"Set up an Avion switch."

homeassistant/components/light/blinksticklight.py
class BlinkStickLight(Light) L45
  "Representation of a BlinkStick light."
  __init__(self, stick, name) L48
  "Initialize the light."
  should_poll(self) L56
  "Polling needed."
  name(self) L61
  "Return the name of the light."
  rgb_color(self) L66
  "Read back the color of the light."
  is_on(self) L71
  "Check whether any of the LEDs colors are non-zero."
  supported_features(self) L76
  "Flag supported features."
  update(self) L80
  "Read back the device state."
  turn_on(self, **kwargs) L84
  "Turn the device on."
  turn_off(self, **kwargs) L95
  "Turn the device off."

setup_platform(hass, config, add_devices, discovery_info=None) L33
"Add device specified by serial number."

homeassistant/components/light/decora.py
class DecoraLight(Light) L48
  "Representation of an Decora light."
  __init__(self, device) L51
  "Initialize the light."
  unique_id(self) L66
  "Return the ID of this light."
  name(self) L71
  "Return the name of the device if any."
  is_on(self) L76
  "Return true if device is on."
  brightness(self) L81
  "Return the brightness of this light between 0..255."
  supported_features(self) L86
  "Flag supported features."
  should_poll(self) L91
  "We can read the device state, so poll."
  assumed_state(self) L96
  "We can read the actual state."
  set_state(self, brightness) L100
  "Set the state of this lamp to the provided brightness."
  turn_on(self, **kwargs) L106
  "Turn the specified or all lights on."
  turn_off(self, **kwargs) L116
  "Turn the specified or all lights off."
  update(self) L121
  "Synchronise internal state with the actual light state."

setup_platform(hass, config, add_devices, discovery_info=None) L33
"Set up an Decora switch."

homeassistant/components/light/demo.py
class DemoLight(Light) L41
  "Representation of a demo light."
  __init__(self, name, state, available=False, rgb=None, ct=None, brightness=180, xy_color=(0.5, 0.5), white=200, effect_list=None, effect=None) L44
  "Initialize the light."
  should_poll(self) L59
  "No polling needed for a demo light."
  name(self) L64
  "Return the name of the light if any."
  available(self) L69
  "Return availability."
  brightness(self) L76
  "Return the brightness of this light between 0..255."
  xy_color(self) L81
  "Return the XY color value [float, float]."
  rgb_color(self) L86
  "Return the RBG color value."
  color_temp(self) L91
  "Return the CT color temperature."
  white_value(self) L96
  "Return the white value of this light between 0..255."
  effect_list(self) L101
  "Return the list of supported effects."
  effect(self) L106
  "Return the current effect."
  is_on(self) L111
  "Return true if light is on."
  supported_features(self) L116
  "Flag supported features."
  turn_on(self, **kwargs) L120
  "Turn the light on."
  turn_off(self, **kwargs) L146
  "Turn the light off."
  async_restore_state(self, is_on, **kwargs) L155
  "Restore the demo state."

setup_platform(hass, config, add_devices_callback, discovery_info=None) L29
"Setup the demo light platform."

homeassistant/components/light/enocean.py
class EnOceanLight(enocean.EnOceanDevice, Light) L45
  "Representation of an EnOcean light source."
  __init__(self, sender_id, devname, dev_id) L48
  "Initialize the EnOcean light source."
  name(self) L59
  "Return the name of the device if any."
  brightness(self) L64
  "Brightness of the light."
  is_on(self) L73
  "If light is on."
  supported_features(self) L78
  "Flag supported features."
  turn_on(self, **kwargs) L82
  "Turn the light source on or sets a specific dimmer value."
  turn_off(self, **kwargs) L97
  "Turn the light source off."
  value_changed(self, val) L105
  "Update the internal state of this device."

setup_platform(hass, config, add_devices, discovery_info=None) L36
"Setup the EnOcean light platform."

homeassistant/components/light/flux_led.py
class FluxLight(Light) L133
  "Representation of a Flux light."
  __init__(self, device) L136
  "Initialize the light."
  unique_id(self) L166
  "Return the ID of this light."
  name(self) L171
  "Return the name of the device if any."
  is_on(self) L176
  "Return true if device is on."
  brightness(self) L181
  "Return the brightness of this light between 0..255."
  rgb_color(self) L186
  "Return the color property."
  supported_features(self) L191
  "Flag supported features."
  effect_list(self) L196
  "Return the list of supported effects."
  turn_on(self, **kwargs) L200
  "Turn the specified or all lights on."
  turn_off(self, **kwargs) L263
  "Turn the specified or all lights off."
  update(self) L267
  "Synchronize state with bulb."

setup_platform(hass, config, add_devices, discovery_info=None) L94
"Setup the Flux lights."

homeassistant/components/light/homematic.py
class HMLight(HMDevice, Light) L34
  "Representation of a Homematic light."
  brightness(self) L38
  "Return the brightness of this light between 0..255."
  is_on(self) L47
  "Return true if light is on."
  supported_features(self) L55
  "Flag supported features."
  turn_on(self, **kwargs) L59
  "Turn the light on."
  turn_off(self, **kwargs) L67
  "Turn the light off."
  _init_data_struct(self) L71
  "Generate a data dict (self._data) from the Homematic metadata."

setup_platform(hass, config, add_devices, discovery_info=None) L20
"Setup the Homematic light platform."

homeassistant/components/light/hue.py
class HueLight(Light) L290
  "Representation of a Hue light."
  __init__(self, light_id, info, bridge, update_lights, bridge_type, allow_unreachable, allow_in_emulated_hue, is_group=False) L293
  "Initialize the light."
  unique_id(self) L312
  "Return the ID of this Hue light."
  name(self) L324
  "Return the name of the Hue light."
  brightness(self) L329
  "Return the brightness of this light between 0..255."
  xy_color(self) L337
  "Return the XY color value."
  color_temp(self) L345
  "Return the CT color value."
  is_on(self) L353
  "Return true if device is on."
  supported_features(self) L365
  "Flag supported features."
  effect_list(self) L370
  "Return the list of supported effects."
  turn_on(self, **kwargs) L374
  "Turn the specified or all lights on."
  turn_off(self, **kwargs) L434
  "Turn the specified or all lights off."
  update(self) L454
  "Synchronize state with bridge."
  device_state_attributes(self) L459
  "Return the device state attributes."

_find_host_from_config(hass, filename=PHUE_CONFIG_FILE) L89
"Attempt to detect host based on existing configuration."
setup_platform(hass, config, add_devices, discovery_info=None) L106
"Setup the Hue lights."
setup_bridge(host, hass, add_devices, filename, allow_unreachable, allow_in_emulated_hue, allow_hue_groups) L141
"Setup a phue bridge based on host parameter."
update_lights() L177
"Update the Hue light objects with latest info from the bridge."
hue_activate_scene(call) L246
"Service to call directly directly into bridge to set scenes."
request_configuration(host, hass, add_devices, filename, allow_unreachable, allow_in_emulated_hue, allow_hue_groups) L261
"Request configuration steps from the user."
hue_configuration_callback(data) L275
"The actions to do when our configuration callback is called."

homeassistant/components/light/hyperion.py
class Hyperion(Light) L52
  "Representation of a Hyperion remote."
  __init__(self, name, host, port, default_color) L55
  "Initialize the light."
  name(self) L64
  "Return the hostname of the server."
  rgb_color(self) L69
  "Return last RGB color value set."
  is_on(self) L74
  "Return true if not black."
  supported_features(self) L79
  "Flag supported features."
  turn_on(self, **kwargs) L83
  "Turn the lights on."
  turn_off(self, **kwargs) L93
  "Disconnect all remotes."
  update(self) L98
  "Get the remote's active color."
  setup(self) L113
  "Get the hostname of the remote."
  json_request(self, request, wait_for_response=False) L121
  "Communicate with the JSON server."

setup_platform(hass, config, add_devices, discovery_info=None) L38
"Setup a Hyperion server remote."

homeassistant/components/light/insteon_hub.py
class InsteonToggleDevice(Light) L27
  "An abstract Class for an Insteon node."
  __init__(self, node) L30
  "Initialize the device."
  name(self) L36
  "Return the the name of the node."
  unique_id(self) L41
  "Return the ID of this insteon node."
  brightness(self) L46
  "Return the brightness of this light between 0..255."
  update(self) L50
  "Update state of the sensor."
  is_on(self) L59
  "Return the boolean response if the node is on."
  supported_features(self) L64
  "Flag supported features."
  turn_on(self, **kwargs) L68
  "Turn device on."
  turn_off(self, **kwargs) L77
  "Turn device off."

setup_platform(hass, config, add_devices, discovery_info=None) L16
"Setup the Insteon Hub light platform."

homeassistant/components/light/insteon_local.py
class InsteonLocalDimmerDevice(Light) L127
  "An abstract Class for an Insteon node."
  __init__(self, node, name) L130
  "Initialize the device."
  name(self) L137
  "Return the the name of the node."
  unique_id(self) L142
  "Return the ID of this Insteon node."
  brightness(self) L147
  "Return the brightness of this light between 0..255."
  update(self) L152
  "Update state of the light."
  is_on(self) L163
  "Return the boolean response if the node is on."
  supported_features(self) L168
  "Flag supported features."
  turn_on(self, **kwargs) L172
  "Turn device on."
  turn_off(self, **kwargs) L180
  "Turn device off."

setup_platform(hass, config, add_devices, discovery_info=None) L31
"Set up the Insteon local light platform."
request_configuration(device_id, insteonhub, model, hass, add_devices_callback) L54
"Request configuration steps from the user."
insteon_light_config_callback(data) L66
"The actions to do when our configuration callback is called."
setup_light(device_id, name, insteonhub, hass, add_devices_callback) L81
"Set up the light."
config_from_file(filename, config=None) L102
"Small configuration file management function."

homeassistant/components/light/insteon_plm.py
class InsteonPLMDimmerDevice(Light) L42
  "A Class for an Insteon device."
  __init__(self, hass, plm, address, name, dimmable) L45
  "Initialize the light."
  should_poll(self) L57
  "No polling needed."
  address(self) L62
  "Return the the address of the node."
  name(self) L67
  "Return the the name of the node."
  brightness(self) L72
  "Return the brightness of this light between 0..255."
  is_on(self) L79
  "Return the boolean response if the node is on."
  supported_features(self) L86
  "Flag supported features."
  device_state_attributes(self) L92
  "Provide attributes for display on device card."
  get_attr(self, key) L97
  "Return specified attribute for this device."
  async_light_update(self, message) L102
  "Receive notification from transport that new data exists."
  async_turn_on(self, **kwargs) L108
  "Turn device on."
  async_turn_off(self, **kwargs) L117
  "Turn device off."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L23
"Set up the INSTEON PLM device class for the hass platform."

homeassistant/components/light/isy994.py
class ISYLightDevice(isy.ISYDevice, Light) L40
  "Representation of an ISY994 light devie."
  __init__(self, node) L43
  "Initialize the ISY994 light device."
  is_on(self) L48
  "Get whether the ISY994 light is on."
  brightness(self) L53
  "Get the brightness of the ISY994 light."
  turn_off(self, **kwargs) L57
  "Send the turn off command to the ISY994 light device."
  turn_on(self, brightness=None, **kwargs) L62
  "Send the turn on command to the ISY994 light device."
  supported_features(self) L68
  "Flag supported features."

setup_platform(hass, config, add_devices, discovery_info=None) L23
"Set up the ISY994 light platform."

homeassistant/components/light/lifx/__init__.py
class LIFXManager(object) L80
  "Representation of all known LIFX entities."
  __init__(self, hass, async_add_devices) L83
  "Initialize the light."
  register(self, device) L90
  "Callback for newly detected bulb."
  ready(self, device, msg) L102
  "Callback that adds the device once all data is retrieved."
  unregister(self, device) L110
  "Callback for disappearing bulb."

class AwaitAioLIFX L119
  "Wait for an aiolifx callback and return the message."
  __init__(self, light) L122
  "Initialize the wrapper."
  callback(self, device, message) L130
  "Callback that aiolifx invokes when the response is received."
  wait(self, method) L137
  "Call an aiolifx method and wait for its response or a timeout."

class LIFXLight(Light) L163
  "Representation of a LIFX light."
  __init__(self, device) L166
  "Initialize the light."
  available(self) L177
  "Return the availability of the device."
  name(self) L182
  "Return the name of the device."
  who(self) L187
  "Return a string identifying the device."
  rgb_color(self) L195
  "Return the RGB value."
  brightness(self) L202
  "Return the brightness of this light between 0..255."
  color_temp(self) L209
  "Return the color temperature."
  is_on(self) L217
  "Return true if device is on."
  effect(self) L223
  "Return the currently running effect."
  supported_features(self) L228
  "Flag supported features."
  effect_list(self) L233
  "Return the list of supported effects."
  update_after_transition(self, now) L238
  "Request new status after completion of the last transition."
  unblock_updates(self, now) L244
  "Allow async_update after the new state has settled on the bulb."
  update_later(self, when) L249
  "Block immediate update requests and schedule one for later."
  async_turn_on(self, **kwargs) L266
  "Turn the device on."
  async_turn_off(self, **kwargs) L298
  "Turn the device off."
  async_update(self) L314
  "Update bulb status (if it is available)."
  stop_effect(self) L321
  "Stop the currently running effect (if any)."
  refresh_state(self) L327
  "Ask the device about its current state and update our copy."
  find_hsbk(self, **kwargs) L335
  "Find the desired color from a number of possible inputs."
  set_power(self, power) L379
  "Set power state value."
  set_color(self, hue, sat, bri, kel) L384
  "Set color state values."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L57
"Setup the LIFX platform."
convert_rgb_to_hsv(rgb) L152
"Convert Home Assistant RGB values to HSV values."

homeassistant/components/light/lifx/effects.py
class LIFXEffectData(object) L154
  "Structure describing a running effect."
  __init__(self, effect, power, color) L157
  "Initialize data structure."

class LIFXEffect(object) L164
  "Representation of a light effect running on a number of lights."
  __init__(self, hass, lights) L167
  "Initialize the effect."
  async_perform(self, **kwargs) L173
  "Do common setup and play the effect."
  async_setup(self, **kwargs) L179
  "Prepare all lights for the effect."
  async_play(self, **kwargs) L197
  "Play the effect."
  async_restore(self, light) L202
  "Restore to the original state (if we are still running)."
  from_poweroff_hsbk(self, light, **kwargs) L215
  "The initial color when starting from a powered off state."

class LIFXEffectBreathe(LIFXEffect) L220
  "Representation of a breathe effect."
  __init__(self, hass, lights) L223
  "Initialize the breathe effect."
  async_play(self, **kwargs) L230
  "Play the effect on all lights."
  async_light_play(self, light, **kwargs) L236
  "Play a light effect on the bulb."
  from_poweroff_hsbk(self, light, **kwargs) L257
  "Initial color is the target color, but no brightness."

class LIFXEffectPulse(LIFXEffectBreathe) L263
  "Representation of a pulse effect."
  __init__(self, hass, lights) L266
  "Initialize the pulse effect."

class LIFXEffectColorloop(LIFXEffect) L273
  "Representation of a colorloop effect."
  __init__(self, hass, lights) L276
  "Initialize the colorloop effect."
  async_play(self, **kwargs) L282
  "Play the effect on all lights."
  from_poweroff_hsbk(self, light, **kwargs) L322
  "Start from a random hue."

class LIFXEffectStop(LIFXEffect) L327
  "A no-op effect, but starting it will stop an existing effect."
  __init__(self, hass, lights) L330
  "Initialize the stop effect."
  async_perform(self, **kwargs) L336
  "Do nothing."

setup(hass, lifx_manager) L67
"Register the LIFX effects as hass service calls."
async_service_handle(service) L70
"Internal func for applying a service."
start_effect(hass, devices, service, **data) L108
"Start a light effect."
default_effect(light, **kwargs) L128
"Start an effect with default parameters."
effect_list() L144
"Return the list of supported effects."

homeassistant/components/light/lifx_legacy.py
class LIFX(object) L63
  "Representation of a LIFX light."
  __init__(self, add_devices_callback, server_addr=None, broadcast_addr=None) L66
  "Initialize the light."
  find_bulb(self, ipaddr) L79
  "Search for bulbs."
  on_device(self, ipaddr, name, power, hue, sat, bri, kel) L88
  "Initialize the light."
  on_color(self, ipaddr, hue, sat, bri, kel) L106
  "Initialize the light."
  on_power(self, ipaddr, power) L114
  "Initialize the light."
  poll(self, now) L123
  "Polling for the light."
  probe(self, address=None) L127
  "Probe the light."

class LIFXLight(Light) L143
  "Representation of a LIFX light."
  __init__(self, liffy, ipaddr, name, power, hue, saturation, brightness, kelvin) L146
  "Initialize the light."
  should_poll(self) L158
  "No polling needed for LIFX light."
  name(self) L163
  "Return the name of the device."
  ipaddr(self) L168
  "Return the IP address of the device."
  rgb_color(self) L173
  "Return the RGB value."
  brightness(self) L180
  "Return the brightness of this light between 0..255."
  color_temp(self) L187
  "Return the color temperature."
  is_on(self) L195
  "Return true if device is on."
  supported_features(self) L201
  "Flag supported features."
  turn_on(self, **kwargs) L205
  "Turn the device on."
  turn_off(self, **kwargs) L243
  "Turn the device off."
  set_name(self, name) L253
  "Set name of the light."
  set_power(self, power) L257
  "Set power state value."
  set_color(self, hue, sat, bri, kel) L262
  "Set color state values."

setup_platform(hass, config, add_devices, discovery_info=None) L50
"Setup the LIFX platform."
convert_rgb_to_hsv(rgb) L132
"Convert Home Assistant RGB values to HSV values."

homeassistant/components/light/limitlessled.py
class LimitlessLEDGroup(Light) L153
  "Representation of a LimitessLED group."
  __init__(self, group) L156
  "Initialize a group."
  factory(group) L164
  "Produce LimitlessLEDGroup objects."
  should_poll(self) L177
  "No polling needed."
  name(self) L182
  "Return the name of the group."
  is_on(self) L187
  "Return true if device is on."
  brightness(self) L192
  "Return the brightness property."
  turn_off(self, transition_time, pipeline, **kwargs) L197
  "Turn off a group."

class LimitlessLEDWhiteGroup(LimitlessLEDGroup) L203
  "Representation of a LimitlessLED White group."
  __init__(self, group) L206
  "Initialize White group."
  color_temp(self) L218
  "Return the temperature property."
  supported_features(self) L223
  "Flag supported features."
  turn_on(self, transition_time, pipeline, **kwargs) L228
  "Turn on (or adjust property of) a group."

class LimitlessLEDRGBWGroup(LimitlessLEDGroup) L243
  "Representation of a LimitlessLED RGBW group."
  __init__(self, group) L246
  "Initialize RGBW group."
  rgb_color(self) L258
  "Return the color property."
  supported_features(self) L263
  "Flag supported features."
  turn_on(self, transition_time, pipeline, **kwargs) L268
  "Turn on (or adjust property of) a group."

class LimitlessLEDRGBWWGroup(LimitlessLEDGroup) L302
  "Representation of a LimitlessLED RGBWW group."
  __init__(self, group) L305
  "Initialize RGBWW group."
  rgb_color(self) L319
  "Return the color property."
  color_temp(self) L324
  "Return the temperature property."
  supported_features(self) L329
  "Flag supported features."
  turn_on(self, transition_time, pipeline, **kwargs) L334
  "Turn on (or adjust property of) a group."

rewrite_legacy(config) L69
"Rewrite legacy configuration to new format."
setup_platform(hass, config, add_devices, discovery_info=None) L97
"Setup the LimitlessLED lights."
state(new_state) L120
"State decorator."
decorator(function) L125
"Decorator function."
wrapper(self, **kwargs) L128
"Wrap a group state change."
_from_hass_temperature(temperature) L377
"Convert Home Assistant color temperature units to percentage."
_to_hass_temperature(temperature) L382
"Convert percentage to Home Assistant color temperature units."
_from_hass_brightness(brightness) L387
"Convert Home Assistant brightness units to percentage."
_to_hass_brightness(brightness) L392
"Convert percentage to Home Assistant brightness units."
_from_hass_color(color) L397
"Convert Home Assistant RGB list to Color tuple."
_to_hass_color(color) L403
"Convert from Color tuple to Home Assistant RGB list."

homeassistant/components/light/litejet.py
class LiteJetLight(Light) L32
  "Representation of a single LiteJet light."
  __init__(self, hass, lj, i, name) L35
  "Initialize a LiteJet light."
  _on_load_changed(self) L48
  "Called on a LiteJet thread when a load's state changes."
  supported_features(self) L54
  "Flag supported features."
  name(self) L59
  "The light's name."
  brightness(self) L64
  "Return the light's brightness."
  is_on(self) L69
  "Return if the light is on."
  should_poll(self) L74
  "Return that lights do not require polling."
  device_state_attributes(self) L79
  "Return the device state attributes."
  turn_on(self, **kwargs) L85
  "Turn on the light."
  turn_off(self, **kwargs) L93
  "Turn off the light."
  update(self) L97
  "Retrieve the light's brightness from the LiteJet system."

setup_platform(hass, config, add_devices, discovery_info=None) L20
"Set up lights for the LiteJet platform."

homeassistant/components/light/lutron.py
class LutronLight(LutronDevice, Light) L36
  "Representation of a Lutron Light, including dimmable."
  __init__(self, area_name, lutron_device, controller) L39
  "Initialize the light."
  supported_features(self) L45
  "Flag supported features."
  brightness(self) L50
  "Return the brightness of the light."
  turn_on(self, **kwargs) L57
  "Turn the light on."
  turn_off(self, **kwargs) L68
  "Turn the light off."
  device_state_attributes(self) L73
  "Return the state attributes."
  is_on(self) L80
  "Return true if device is on."
  update(self) L84
  "Called when forcing a refresh of the device."

setup_platform(hass, config, add_devices, discovery_info=None) L15
"Setup Lutron lights."
to_lutron_level(level) L26
"Convert the given HASS light level (0-255) to Lutron (0.0-100.0)."
to_hass_level(level) L31
"Convert the given Lutron (0.0-100.0) light level to HASS (0-255)."

homeassistant/components/light/lutron_caseta.py
class LutronCasetaLight(LutronCasetaDevice, Light) L30
  "Representation of a Lutron Light, including dimmable."
  supported_features(self) L34
  "Flag supported features."
  brightness(self) L39
  "Return the brightness of the light."
  turn_on(self, **kwargs) L43
  "Turn the light on."
  turn_off(self, **kwargs) L52
  "Turn the light off."
  is_on(self) L57
  "Return true if device is on."
  update(self) L61
  "Called when forcing a refresh of the device."

setup_platform(hass, config, add_devices, discovery_info=None) L18
"Setup Lutron  Caseta lights."

homeassistant/components/light/mqtt.py
class MqttLight(Light) L144
  "MQTT light."
  __init__(self, name, effect_list, topic, templates, qos, retain, payload, optimistic, brightness_scale, white_value_scale) L147
  "Initialize MQTT light."
  async_added_to_hass(self) L199
  "Subscribe mqtt events."
  state_received(topic, payload, qos) L213
  "A new MQTT message has been received."
  brightness_received(topic, payload, qos) L228
  "A new MQTT message for the brightness has been received."
  rgb_received(topic, payload, qos) L246
  "A new MQTT message has been received."
  color_temp_received(topic, payload, qos) L263
  "A new MQTT message for color temp has been received."
  effect_received(topic, payload, qos) L279
  "A new MQTT message for effect has been received."
  white_value_received(topic, payload, qos) L295
  "A new MQTT message for the white value has been received."
  xy_received(topic, payload, qos) L313
  "A new MQTT message has been received."
  brightness(self) L330
  "Return the brightness of this light between 0..255."
  rgb_color(self) L335
  "Return the RGB color value."
  color_temp(self) L340
  "Return the color temperature in mired."
  white_value(self) L345
  "Return the white property."
  xy_color(self) L350
  "Return the RGB color value."
  should_poll(self) L355
  "No polling needed for a MQTT light."
  name(self) L360
  "Return the name of the device if any."
  is_on(self) L365
  "Return true if device is on."
  assumed_state(self) L370
  "Return true if we do optimistic updates."
  effect_list(self) L375
  "Return the list of supported effects."
  effect(self) L380
  "Return the current effect."
  supported_features(self) L385
  "Flag supported features."
  async_turn_on(self, **kwargs) L390
  "Turn the device on."
  async_turn_off(self, **kwargs) L479
  "Turn the device off."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L94
"Add MQTT Light."

homeassistant/components/light/mqtt_json.py
class MqttJson(Light) L104
  "Representation of a MQTT JSON light."
  __init__(self, name, effect_list, topic, qos, retain, optimistic, brightness, color_temp, effect, rgb, white_value, xy, flash_times) L107
  "Initialize MQTT JSON light."
  async_added_to_hass(self) L159
  "Subscribe mqtt events."
  state_received(topic, payload, qos) L165
  "A new MQTT message has been received."
  brightness(self) L237
  "Return the brightness of this light between 0..255."
  color_temp(self) L242
  "Return the color temperature in mired."
  effect(self) L247
  "Return the current effect."
  effect_list(self) L252
  "Return the list of supported effects."
  rgb_color(self) L257
  "Return the RGB color value."
  white_value(self) L262
  "Return the white property."
  xy_color(self) L267
  "Return the XY color value."
  should_poll(self) L272
  "No polling needed for a MQTT light."
  name(self) L277
  "Return the name of the device if any."
  is_on(self) L282
  "Return true if device is on."
  assumed_state(self) L287
  "Return true if we do optimistic updates."
  supported_features(self) L292
  "Flag supported features."
  async_turn_on(self, **kwargs) L297
  "Turn the device on."
  async_turn_off(self, **kwargs) L379
  "Turn the device off."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L73
"Setup a MQTT JSON Light."

homeassistant/components/light/mqtt_template.py
class MqttTemplate(Light) L102
  "Representation of a MQTT Template light."
  __init__(self, hass, name, effect_list, topics, templates, optimistic, qos, retain) L105
  "Initialize MQTT Template light."
  async_added_to_hass(self) L148
  "Subscribe mqtt events."
  state_received(topic, payload, qos) L154
  "A new MQTT message has been received."
  brightness(self) L230
  "Return the brightness of this light between 0..255."
  color_temp(self) L235
  "Return the color temperature in mired."
  rgb_color(self) L240
  "Return the RGB color value [int, int, int]."
  white_value(self) L245
  "Return the white property."
  should_poll(self) L250
  "Return True if entity has to be polled for state."
  name(self) L258
  "Return the name of the entity."
  is_on(self) L263
  "Return True if entity is on."
  assumed_state(self) L268
  "Return True if unable to access real state of the entity."
  effect_list(self) L273
  "Return the list of supported effects."
  effect(self) L278
  "Return the current effect."
  async_turn_on(self, **kwargs) L283
  "Turn the entity on."
  async_turn_off(self, **kwargs) L345
  "Turn the entity off."
  supported_features(self) L369
  "Flag supported features."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L67
"Setup a MQTT Template light."

homeassistant/components/light/mysensors.py
class MySensorsLight(mysensors.MySensorsDeviceEntity, Light) L64
  "Represent the value of a MySensors Light child node."
  __init__(self, *args) L67
  "Setup instance attributes."
  brightness(self) L76
  "Return the brightness of this light between 0..255."
  rgb_color(self) L81
  "Return the RGB color value [int, int, int]."
  white_value(self) L86
  "Return the white value of this light between 0..255."
  assumed_state(self) L91
  "Return true if unable to access real state of entity."
  is_on(self) L96
  "Return true if device is on."
  supported_features(self) L101
  "Flag supported features."
  _turn_on_light(self) L105
  "Turn on light child device."
  _turn_on_dimmer(self, **kwargs) L120
  "Turn on dimmer child device."
  _turn_on_rgb_and_w(self, hex_template, **kwargs) L140
  "Turn on RGB or RGBW child device."
  _turn_off_light(self, value_type=None, value=None) L176
  "Turn off light child device."
  _turn_off_dimmer(self, value_type=None, value=None) L185
  "Turn off dimmer child device."
  _turn_off_rgb_or_w(self, value_type=None, value=None) L194
  "Turn off RGB or RGBW child device."
  _turn_off_main(self, value_type=None, value=None) L204
  "Turn the device off."
  _update_light(self) L222
  "Update the controller with values from light child."
  _update_dimmer(self) L230
  "Update the controller with values from dimmer child."
  _update_rgb_or_w(self) L241
  "Update the controller with values from RGB or RGBW child."
  _update_main(self) L262
  "Update the controller with the latest value from a sensor."

class MySensorsLightDimmer(MySensorsLight) L272
  "Dimmer child class to MySensorsLight."
  turn_on(self, **kwargs) L275
  "Turn the device on."
  turn_off(self, **kwargs) L280
  "Turn the device off."
  update(self) L288
  "Update the controller with the latest value from a sensor."

class MySensorsLightRGB(MySensorsLight) L295
  "RGB child class to MySensorsLight."
  turn_on(self, **kwargs) L298
  "Turn the device on."
  turn_off(self, **kwargs) L304
  "Turn the device off."
  update(self) L314
  "Update the controller with the latest value from a sensor."

class MySensorsLightRGBW(MySensorsLightRGB) L322
  "RGBW child class to MySensorsLightRGB."
  turn_on(self, **kwargs) L325
  "Turn the device on."

setup_platform(hass, config, add_devices, discovery_info=None) L27
"Setup the mysensors platform for sensors."

homeassistant/components/light/mystrom.py
class MyStromLight(Light) L51
  "Representation of the myStrom WiFi Bulb."
  __init__(self, bulb, name) L54
  "Initialize the light."
  name(self) L64
  "Return the display name of this light."
  supported_features(self) L69
  "Flag supported features."
  brightness(self) L74
  "Brightness of the light."
  available(self) L79
  "Return True if entity is available."
  is_on(self) L84
  "Return true if light is on."
  turn_on(self, **kwargs) L88
  "Turn on the light."
  turn_off(self, **kwargs) L102
  "Turn off the bulb."
  update(self) L111
  "Fetch new state data for this light."

setup_platform(hass, config, add_devices, discovery_info=None) L31
"Set up the myStrom Light platform."

homeassistant/components/light/osramlightify.py
class OsramLightifyLight(Light) L86
  "Representation of an Osram Lightify Light."
  __init__(self, light_id, light, update_lights) L89
  "Initialize the light."
  name(self) L102
  "Return the name of the device if any."
  rgb_color(self) L107
  "Last RGB color value set."
  color_temp(self) L114
  "Return the color temperature."
  brightness(self) L119
  "Brightness of this light between 0..255."
  is_on(self) L126
  "Update Status to True if device is on."
  supported_features(self) L133
  "Flag supported features."
  turn_on(self, **kwargs) L137
  "Turn the device on."
  turn_off(self, **kwargs) L191
  "Turn the device off."
  update(self) L211
  "Synchronize state with bridge."

setup_platform(hass, config, add_devices, discovery_info=None) L41
"Setup the Osram Lightify lights."
setup_bridge(bridge, add_devices_callback) L59
"Setup the Lightify bridge."
update_lights() L64
"Update the lights objects with latest info from bridge."

homeassistant/components/light/piglow.py
class PiglowLight(Light) L44
  "Representation of an Piglow Light."
  __init__(self, piglow, name) L47
  "Initialize an PiglowLight."
  name(self) L56
  "Return the display name of this light."
  brightness(self) L61
  "Brightness of the light (an integer in the range 1-255)."
  rgb_color(self) L66
  "Read back the color of the light."
  supported_features(self) L71
  "Flag supported features."
  is_on(self) L76
  "Return true if light is on."
  turn_on(self, **kwargs) L80
  "Instruct the light to turn on."
  turn_off(self, **kwargs) L96
  "Instruct the light to turn off."

setup_platform(hass, config, add_devices, discovery_info=None) L31
"Set up the Piglow Light platform."

homeassistant/components/light/qwikswitch.py
setup_platform(hass, config, add_devices, discovery_info=None) L17
"Add lights from the main Qwikswitch component."

homeassistant/components/light/rflink.py
class RflinkLight(SwitchableRflinkDevice, Light) L173
  "Representation of a Rflink light."

class DimmableRflinkLight(SwitchableRflinkDevice, Light) L179
  "Rflink light device that support dimming."
  async_turn_on(self, **kwargs) L185
  "Turn the device on."
  brightness(self) L195
  "Return the brightness of this light between 0..255."
  supported_features(self) L200
  "Flag supported features."

class HybridRflinkLight(SwitchableRflinkDevice, Light) L205
  "Rflink light device that sends out both dim and on/off commands."
  async_turn_on(self, **kwargs) L223
  "Turn the device on and set dim level."
  brightness(self) L239
  "Return the brightness of this light between 0..255."
  supported_features(self) L244
  "Flag supported features."

class ToggleRflinkLight(SwitchableRflinkDevice, Light) L249
  "Rflink light device which sends out only 'on' commands."
  entity_id(self) L259
  "Return entity id."
  _handle_event(self, event) L263
  "Adjust state if Rflink picks up a remote command for this device."
  async_turn_on(self, **kwargs) L274
  "Turn the device on."
  async_turn_off(self, **kwargs) L279
  "Turn the device off."

entity_type_for_device_id(device_id) L56
"Return entity class for protocol of a given device_id."
entity_class_for_type(entity_type) L70
"Translate entity type to entity class."
devices_from_config(domain_config, hass=None) L92
"Parse configuration and add Rflink light devices."
async_setup_platform(hass, config, async_add_devices, discovery_info=None) L146
"Set up the Rflink light platform."
add_new_device(event) L151
"Check if device is known, otherwise add to list of known devices."

homeassistant/components/light/rfxtrx.py
class RfxtrxLight(rfxtrx.RfxtrxDevice, Light) L46
  "Represenation of a RFXtrx light."
  brightness(self) L50
  "Return the brightness of this light between 0..255."
  supported_features(self) L55
  "Flag supported features."
  turn_on(self, **kwargs) L59
  "Turn the light on."

setup_platform(hass, config, add_devices, discovery_info=None) L22
"Setup the RFXtrx platform."
light_update(event) L29
"Callback for light updates from the RFXtrx gateway."

homeassistant/components/light/rpi_gpio_pwm.py
class PwmSimpleLed(Light) L98
  "Representation of a simple on-color pwm led."
  __init__(self, led, name) L101
  "Initialize led."
  should_poll(self) L109
  "No polling needed."
  name(self) L114
  "Return the name of the group."
  is_on(self) L119
  "Return true if device is on."
  brightness(self) L124
  "Return the brightness property."
  supported_features(self) L129
  "Flag supported features."
  turn_on(self, **kwargs) L133
  "Turn on a led."
  turn_off(self, **kwargs) L151
  "Turn off a led."

class PwmRgbLed(PwmSimpleLed) L164
  "Representation of a rgb(w) pwm led."
  __init__(self, led, name) L167
  "Initialize led."
  rgb_color(self) L173
  "Return the color property."
  supported_features(self) L178
  "Flag supported features."
  turn_on(self, **kwargs) L182
  "Turn on a led."

setup_platform(hass, config, add_devices, discovery_info=None) L57
"Setup the pwm lights."
_from_hass_brightness(brightness) L205
"Convert Home Assistant brightness units to percentage."
_from_hass_color(color) L210
"Convert Home Assistant RGB list to Color tuple."

homeassistant/components/light/scsgate.py
class SCSGateLight(Light) L49
  "representation of a SCSGate light."
  __init__(self, scs_id, name, logger) L52
  "Initialize the light."
  scs_id(self) L60
  "Return the SCS ID."
  should_poll(self) L65
  "No polling needed for a SCSGate light."
  name(self) L70
  "Return the name of the device if any."
  is_on(self) L75
  "Return true if light is on."
  turn_on(self, **kwargs) L79
  "Turn the device on."
  turn_off(self, **kwargs) L89
  "Turn the device off."
  process_event(self, message) L99
  "Handle a SCSGate message related with this light."

setup_platform(hass, config, add_devices, discovery_info=None) L26
"Setup the SCSGate switches."

homeassistant/components/light/tellduslive.py
class TelldusLiveLight(TelldusLiveEntity, Light) L26
  "Representation of a light."
  __init__(self, hass, device_id) L29
  "Initialize the light."
  changed(self) L34
  "A property of the device might have changed."
  brightness(self) L40
  "Return the brightness of this light between 0..255."
  supported_features(self) L45
  "Flag supported features."
  is_on(self) L50
  "Return true if light is on."
  turn_on(self, **kwargs) L54
  "Turn the light on."
  turn_off(self, **kwargs) L60
  "Turn the light off."

setup_platform(hass, config, add_devices, discovery_info=None) L19
"Setup lights."

homeassistant/components/light/tellstick.py
class TellstickLight(TellstickDevice, Light) L37
  "Representation of a Tellstick light."
  __init__(self, tellcore_id, tellcore_registry, signal_repetitions) L40
  "Initialize the light."
  brightness(self) L47
  "Return the brightness of this light between 0..255."
  supported_features(self) L52
  "Flag supported features."
  _parse_ha_data(self, kwargs) L56
  "Turn the value from HA into something useful."
  _parse_tellcore_data(self, tellcore_data) L60
  "Turn the value recieved from tellcore into something useful."
  _update_model(self, new_state, data) L68
  "Update the device entity state to match the arguments."
  _send_device_command(self, requested_state, requested_data) L83
  "Let tellcore update the actual device to the requested state."

setup_platform(hass, config, add_devices, discovery_info=None) L22
"Setup Tellstick lights."

homeassistant/components/light/tikteck.py
class TikteckLight(Light) L49
  "Representation of a Tikteck light."
  __init__(self, device) L52
  "Initialize the light."
  unique_id(self) L71
  "Return the ID of this light."
  name(self) L76
  "Return the name of the device if any."
  is_on(self) L81
  "Return true if device is on."
  brightness(self) L86
  "Return the brightness of this light between 0..255."
  rgb_color(self) L91
  "Return the color property."
  supported_features(self) L96
  "Flag supported features."
  should_poll(self) L101
  "Don't poll."
  assumed_state(self) L106
  "We can't read the actual state, so assume it matches."
  set_state(self, red, green, blue, brightness) L110
  "Set the bulb state."
  turn_on(self, **kwargs) L114
  "Turn the specified light on."
  turn_off(self, **kwargs) L130
  "Turn the specified light off."

setup_platform(hass, config, add_devices, discovery_info=None) L34
"Set up the Tikteck platform."

homeassistant/components/light/tradfri.py
class TradfriGroup(Light) L37
  "The platform class required by hass."
  __init__(self, light) L40
  "Initialize a Group."
  supported_features(self) L46
  "Flag supported features."
  name(self) L51
  "Return the display name of this group."
  is_on(self) L56
  "Return true if group lights are on."
  brightness(self) L61
  "Brightness of the group lights (an integer in the range 1-255)."
  turn_off(self, **kwargs) L65
  "Instruct the group lights to turn off."
  turn_on(self, **kwargs) L69
  "Instruct the group lights to turn on, or dim."
  update(self) L76
  "Fetch new state data for this group."

class Tradfri(Light) L81
  "The platform class required by hass."
  __init__(self, light) L84
  "Initialize a Light."
  supported_features(self) L105
  "Flag supported features."
  name(self) L110
  "Return the display name of this light."
  is_on(self) L115
  "Return true if light is on."
  brightness(self) L120
  "Brightness of the light (an integer in the range 1-255)."
  color_temp(self) L125
  "Return the CT color value in mireds."
  rgb_color(self) L143
  "RGB color of the light."
  turn_off(self, **kwargs) L147
  "Instruct the light to turn off."
  turn_on(self, **kwargs) L151
  "Instruct the light to turn on."
  update(self) L175
  "Fetch new state data for this light."

setup_platform(hass, config, add_devices, discovery_info=None) L22
"Setup the IKEA Tradfri Light platform."

homeassistant/components/light/vera.py
class VeraLight(VeraDevice, Light) L28
  "Representation of a Vera Light, including dimmable."
  __init__(self, vera_device, controller) L31
  "Initialize the light."
  brightness(self) L38
  "Return the brightness of the light."
  supported_features(self) L44
  "Flag supported features."
  turn_on(self, **kwargs) L48
  "Turn the light on."
  turn_off(self, **kwargs) L58
  "Turn the light off."
  is_on(self) L65
  "Return true if device is on."
  update(self) L69
  "Called by the vera device callback to update state."

setup_platform(hass, config, add_devices, discovery_info=None) L22
"Setup Vera lights."

homeassistant/components/light/wemo.py
class WemoLight(Light) L63
  "Representation of a WeMo light."
  __init__(self, device, update_lights) L66
  "Initialize the light."
  unique_id(self) L73
  "Return the ID of this light."
  name(self) L79
  "Return the name of the light."
  brightness(self) L84
  "Return the brightness of this light between 0..255."
  xy_color(self) L89
  "Return the XY color values of this light."
  color_temp(self) L94
  "Return the color temperature of this light in mireds."
  is_on(self) L99
  "True if device is on."
  supported_features(self) L104
  "Flag supported features."
  turn_on(self, **kwargs) L108
  "Turn the light on."
  turn_off(self, **kwargs) L135
  "Turn the light off."
  update(self) L140
  "Synchronize state with bridge."

setup_platform(hass, config, add_devices, discovery_info=None) L28
"Setup WeMo bridges and register connected lights."
setup_bridge(bridge, add_devices) L41
"Setup a WeMo link."
update_lights() L46
"Update the WeMo led objects with latest info from the bridge."

homeassistant/components/light/wink.py
class WinkLight(WinkDevice, Light) L34
  "Representation of a Wink light."
  __init__(self, wink, hass) L37
  "Initialize the Wink device."
  is_on(self) L42
  "Return true if light is on."
  brightness(self) L47
  "Return the brightness of the light."
  rgb_color(self) L55
  "Current bulb color in RGB."
  xy_color(self) L74
  "Current bulb color in CIE 1931 (XY) color space."
  color_temp(self) L81
  "Current bulb color in degrees Kelvin."
  supported_features(self) L89
  "Flag supported features."
  turn_on(self, **kwargs) L93
  "Turn the switch on."
  turn_off(self) L120
  "Turn the switch off."

setup_platform(hass, config, add_devices, discovery_info=None) L24
"Setup the Wink lights."

homeassistant/components/light/x10.py
class X10Light(Light) L53
  "Representation of an X10 Light."
  __init__(self, light) L56
  "Initialize an X10 Light."
  name(self) L64
  "Return the display name of this light."
  brightness(self) L69
  "Brightness of the light (an integer in the range 1-255)."
  is_on(self) L74
  "Return true if light is on."
  supported_features(self) L79
  "Flag supported features."
  turn_on(self, **kwargs) L83
  "Instruct the light to turn on."
  turn_off(self, **kwargs) L89
  "Instruct the light to turn off."
  update(self) L94
  "Fetch update state."

x10_command(command) L31
"Execute X10 command and check output."
get_unit_status(code) L36
"Get on/off status for given unit."
setup_platform(hass, config, add_devices, discovery_info=None) L42
"Setup the x10 Light platform."

homeassistant/components/light/yeelight.py
class YeelightLight(Light) L91
  "Representation of a Yeelight light."
  __init__(self, device, config) L94
  "Initialize the light."
  available(self) L110
  "Return if bulb is available."
  supported_features(self) L115
  "Flag supported features."
  unique_id(self) L120
  "Return the ID of this light."
  color_temp(self) L125
  "Return the color temperature."
  name(self) L130
  "Return the name of the device if any."
  is_on(self) L135
  "Return true if device is on."
  brightness(self) L140
  "Return the brightness of this light between 1..255."
  _get_rgb_from_properties(self) L144
  rgb_color(self) L167
  "Return the color property."
  _properties(self) L172
  _bulb(self) L176
  set_music_mode(self, mode) L191
  "Set the music mode on or off."
  update(self) L198
  "Update properties from the bulb."
  set_brightness(self, brightness, duration) L226
  "Set bulb brightness."
  set_rgb(self, rgb, duration) L234
  "Set bulb's color."
  set_colortemp(self, colortemp, duration) L241
  "Set bulb's color temperature."
  set_default(self) L250
  "Set current options as default."
  set_flash(self, flash) L255
  "Activate flash."
  turn_on(self, **kwargs) L289
  "Turn the bulb on."
  turn_off(self, **kwargs) L334
  "Turn off."

_cmd(func) L56
"A wrapper to catch exceptions from the bulb."
_wrap(self, *args, **kwargs) L58
setup_platform(hass, config, add_devices, discovery_info=None) L69
"Setup the Yeelight bulbs."

homeassistant/components/light/yeelightsunflower.py
class SunflowerBulb(Light) L42
  "Representation of a Yeelight Sunflower Light."
  __init__(self, light) L45
  "Initialize a Yeelight Sunflower bulb."
  name(self) L54
  "Return the display name of this light."
  available(self) L59
  "Return True if entity is available."
  is_on(self) L64
  "Return true if light is on."
  brightness(self) L69
  "HA brightness is 0-255; Yeelight Sunflower's brightness is 0-100."
  rgb_color(self) L74
  "Return the color property."
  supported_features(self) L79
  "Flag supported features."
  turn_on(self, **kwargs) L83
  "Instruct the light to turn on, optionally set colour/brightness."
  turn_off(self, **kwargs) L100
  "Instruct the light to turn off."
  update(self) L104
  "Fetch new state data for this light and update local values."

setup_platform(hass, config, add_devices, discovery_info=None) L28
"Set up the Yeelight Sunflower Light platform."

homeassistant/components/light/zengge.py
class ZenggeLight(Light) L47
  "Representation of a Zengge light."
  __init__(self, device) L50
  "Initialize the light."
  unique_id(self) L69
  "Return the ID of this light."
  name(self) L74
  "Return the name of the device if any."
  is_on(self) L79
  "Return true if device is on."
  rgb_color(self) L84
  "Return the color property."
  white_value(self) L89
  "Return the white property."
  supported_features(self) L94
  "Flag supported features."
  should_poll(self) L99
  "Feel free to poll."
  assumed_state(self) L104
  "We can report the actual state."
  set_rgb(self, red, green, blue) L108
  "Set the rgb state."
  set_white(self, white) L112
  "Set the white state."
  turn_on(self, **kwargs) L116
  "Turn the specified light on."
  turn_off(self, **kwargs) L137
  "Turn the specified light off."
  update(self) L142
  "Synchronise internal state with the actual light state."

setup_platform(hass, config, add_devices, discovery_info=None) L33
"Set up the Zengge platform."

homeassistant/components/light/zha.py
class Light(zha.Entity, light.Light) L35
  "ZHA or ZLL light."
  __init__(self, **kwargs) L40
  "Initialize ZHA light."
  is_on(self) L64
  "Return True if entity is on."
  async_turn_on(self, **kwargs) L71
  "Turn the entity on."
  async_turn_off(self, **kwargs) L109
  "Turn the entity off."
  brightness(self) L115
  "Return the brightness of this light between 0..255."
  xy_color(self) L120
  "Return the XY color value [float, float]."
  color_temp(self) L125
  "Return the CT color value in mireds."
  supported_features(self) L130
  "Flag supported features."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L19
"Setup Zigbee Home Automation lights."

homeassistant/components/light/zigbee.py
class ZigBeeLight(ZigBeeDigitalOut, Light) L30
  "Use ZigBeeDigitalOut as light."

setup_platform(hass, config, add_devices, discovery_info=None) L25
"Create and add an entity based on the configuration."

homeassistant/components/light/zwave.py
class ZwaveDimmer(zwave.ZWaveDeviceEntity, Light) L77
  "Representation of a Z-Wave dimmer."
  __init__(self, values, refresh, delay) L80
  "Initialize the light."
  update_properties(self) L109
  "Update internal properties based on zwave values."
  value_added(self) L114
  "Called when a new value is added to this entity."
  value_changed(self) L120
  "Called when a value for this entity's node has changed."
  _refresh_value() L126
  "Used timer callback for delayed value refresh."
  brightness(self) L140
  "Return the brightness of this light between 0..255."
  is_on(self) L145
  "Return true if device is on."
  supported_features(self) L150
  "Flag supported features."
  _set_duration(self, **kwargs) L154
  "Set the transition time for the brightness value."
  turn_on(self, **kwargs) L185
  "Turn the device on."
  turn_off(self, **kwargs) L200
  "Turn the device off."

class ZwaveColorLight(ZwaveDimmer) L208
  "Representation of a Z-Wave color changing light."
  __init__(self, values, refresh, delay) L211
  "Initialize the light."
  value_added(self) L219
  "Called when a new value is added to this entity."
  update_properties(self) L227
  "Update internal properties based on zwave values."
  rgb_color(self) L293
  "Return the rgb color."
  color_temp(self) L298
  "Return the color temperature."
  turn_on(self, **kwargs) L302
  "Turn the device on."

get_device(node, values, node_config, **kwargs) L47
"Create zwave entity device."
brightness_state(value) L62
"Return the brightness and state."
ct_to_rgb(temp) L70
"Convert color temperature (mireds) to RGB."

homeassistant/components/lirc.py
class LircInterface(threading.Thread) L56
  "This interfaces with the lirc daemon to read IR commands."
  __init__(self, hass) L65
  "Construct a LIRC interface object."
  run(self) L72
  "Main loop of LIRC interface thread."

setup(hass, config) L34
"Setup LIRC capability."
_start_lirc(_event) L44
_stop_lirc(_event) L47

homeassistant/components/litejet.py
setup(hass, config) L32
"Initialize the LiteJet component."
is_ignored(hass, name) L49
"Determine if a load, switch, or scene should be ignored."

homeassistant/components/lock/__init__.py
class LockDevice(Entity) L123
  "Representation of a lock."
  changed_by(self) L127
  "Last change triggered by."
  code_format(self) L133
  "Regex for code format or None if no code is required."
  is_locked(self) L138
  "Return true if the lock is locked."
  lock(self, **kwargs) L142
  "Lock the lock."
  async_lock(self, **kwargs) L146
  "Lock the lock."
  unlock(self, **kwargs) L154
  "Unlock the lock."
  async_unlock(self, **kwargs) L158
  "Unlock the lock."
  state_attributes(self) L167
  "Return the state attributes."
  state(self) L178
  "Return the state."

is_locked(hass, entity_id=None) L44
"Return if the lock is locked based on the statemachine."
lock(hass, entity_id=None, code=None) L50
"Lock all or specified locks."
unlock(hass, entity_id=None, code=None) L61
"Unlock all or specified locks."
async_setup(hass, config) L73
"Track states and offer events for locks."
async_handle_lock_service(service) L81
"Handle calls to the lock services."

homeassistant/components/lock/demo.py
class DemoLock(LockDevice) L20
  "Representation of a Demo lock."
  __init__(self, name, state) L23
  "Initialize the lock."
  should_poll(self) L29
  "No polling needed for a demo lock."
  name(self) L34
  "Return the name of the lock if any."
  is_locked(self) L39
  "Return true if lock is locked."
  lock(self, **kwargs) L43
  "Lock the device."
  unlock(self, **kwargs) L48
  "Unlock the device."

setup_platform(hass, config, add_devices, discovery_info=None) L12
"Setup the Demo lock platform."

homeassistant/components/lock/isy994.py
class ISYLockDevice(isy.ISYDevice, LockDevice) L53
  "Representation of an ISY994 lock device."
  __init__(self, node) L56
  "Initialize the ISY994 lock device."
  is_locked(self) L62
  "Get whether the lock is in locked state."
  state(self) L67
  "Get the state of the lock."
  lock(self, **kwargs) L71
  "Send the lock command to the ISY994 device."
  unlock(self, **kwargs) L83
  "Send the unlock command to the ISY994 device."

class ISYLockProgram(ISYLockDevice) L96
  "Representation of a ISY lock program."
  __init__(self, name, node, actions) L99
  "Initialize the lock."
  is_locked(self) L106
  "Return true if the device is locked."
  state(self) L111
  "Return the state of the lock."
  lock(self, **kwargs) L115
  "Lock the device."
  unlock(self, **kwargs) L120
  "Unlock the device."

setup_platform(hass, config, add_devices, discovery_info=None) L27
"Set up the ISY994 lock platform."

homeassistant/components/lock/lockitron.py
class Lockitron(LockDevice) L43
  "Representation of a Lockitron lock."
  __init__(self, state, access_token, device_id) L49
  "Initialize the lock."
  name(self) L56
  "Return the name of the device."
  is_locked(self) L61
  "Return True if the lock is currently locked, else False."
  lock(self, **kwargs) L65
  "Lock the device."
  unlock(self, **kwargs) L69
  "Unlock the device."
  update(self) L73
  "Update the internal state of the device."
  do_change_request(self, requested_state) L82
  "Execute the change request and pull out the new state."

setup_platform(hass, config, add_devices, discovery_info=None) L30
"Setup the Lockitron platform."

homeassistant/components/lock/mqtt.py
class MqttLock(LockDevice) L63
  "Represents a lock that can be toggled using MQTT."
  __init__(self, name, state_topic, command_topic, qos, retain, payload_lock, payload_unlock, optimistic, value_template) L66
  "Initialize the lock."
  async_added_to_hass(self) L81
  "Subscribe mqtt events."
  message_received(topic, payload, qos) L87
  "A new MQTT message has been received."
  should_poll(self) L107
  "No polling needed."
  name(self) L112
  "The name of the lock."
  is_locked(self) L117
  "Return true if lock is locked."
  assumed_state(self) L122
  "Return true if we do optimistic updates."
  async_lock(self, **kwargs) L127
  "Lock the device."
  async_unlock(self, **kwargs) L141
  "Unlock the device."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L44
"Setup the MQTT lock."

homeassistant/components/lock/nuki.py
class NukiLock(LockDevice) L42
  "Representation of a Nuki lock."
  __init__(self, nuki_lock) L45
  "Initialize the lock."
  name(self) L52
  "Return the name of the lock."
  is_locked(self) L57
  "Return true if lock is locked."
  update(self) L62
  "Update the nuki lock properties."
  lock(self, **kwargs) L68
  "Lock the device."
  unlock(self, **kwargs) L72
  "Unlock the device."

setup_platform(hass, config, add_devices, discovery_info=None) L35
"Setup the Demo lock platform."

homeassistant/components/lock/vera.py
class VeraLock(VeraDevice, LockDevice) L26
  "Representation of a Vera lock."
  __init__(self, vera_device, controller) L29
  "Initialize the Vera device."
  lock(self, **kwargs) L35
  "Lock the device."
  unlock(self, **kwargs) L40
  "Unlock the device."
  is_locked(self) L46
  "Return true if device is on."
  update(self) L50
  "Called by the Vera device callback to update state."

setup_platform(hass, config, add_devices, discovery_info=None) L19
"Find and return Vera locks."

homeassistant/components/lock/verisure.py
class VerisureDoorlock(LockDevice) L30
  "Representation of a Verisure doorlock."
  __init__(self, device_id) L33
  "Initialize the Verisure lock."
  name(self) L41
  "Return the name of the lock."
  state(self) L46
  "Return the state of the lock."
  available(self) L51
  "Return True if entity is available."
  changed_by(self) L56
  "Last change triggered by."
  code_format(self) L61
  "Return the required six digit code."
  update(self) L65
  "Update lock status."
  is_locked(self) L79
  "Return true if lock is locked."
  unlock(self, **kwargs) L83
  "Send unlock command."
  lock(self, **kwargs) L90
  "Send lock command."

setup_platform(hass, config, add_devices, discovery_info=None) L18
"Set up the Verisure platform."

homeassistant/components/lock/volvooncall.py
class VolvoLock(VolvoEntity, LockDevice) L23
  "Represents a car lock."
  is_locked(self) L27
  "Return true if lock is locked."
  lock(self, **kwargs) L31
  "Lock the car."
  unlock(self, **kwargs) L35
  "Unlock the car."

setup_platform(hass, config, add_devices, discovery_info=None) L16
"Setup the lock."

homeassistant/components/lock/wink.py
class WinkLockDevice(WinkDevice, LockDevice) L24
  "Representation of a Wink lock."
  __init__(self, wink, hass) L27
  "Initialize the lock."
  is_locked(self) L32
  "Return true if device is locked."
  lock(self, **kwargs) L36
  "Lock the device."
  unlock(self, **kwargs) L40
  "Unlock the device."

setup_platform(hass, config, add_devices, discovery_info=None) L14
"Setup the Wink platform."

homeassistant/components/lock/zwave.py
class ZwaveLock(zwave.ZWaveDeviceEntity, LockDevice) L206
  "Representation of a Z-Wave Lock."
  __init__(self, values) L209
  "Initialize the Z-Wave lock device."
  update_properties(self) L230
  "Callback on data changes for node values."
  is_locked(self) L280
  "Return true if device is locked."
  lock(self, **kwargs) L284
  "Lock the device."
  unlock(self, **kwargs) L288
  "Unlock the device."
  device_state_attributes(self) L293
  "Return the device specific state attributes."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L124
"Generic Z-Wave platform setup."
set_usercode(service) L133
"Set the usercode to index X on the lock."
get_usercode(service) L152
"Get a usercode at index X on the lock."
clear_usercode(service) L165
"Set usercode to slot X on the lock."
get_device(node, values, **kwargs) L201
"Create zwave entity device."

homeassistant/components/logbook.py
class LogbookView(HomeAssistantView) L111
  "Handle logbook view requests."
  __init__(self, config) L118
  "Initilalize the logbook view."
  get(self, request, datetime=None) L123
  "Retrieve logbook entries."

class Entry(object) L143
  "A human readable version of the log."
  __init__(self, when=None, name=None, message=None, domain=None, entity_id=None) L146
  "Initialize the entry."
  as_dict(self) L155
  "Convert entry to a dict to be used within JSON."

log_entry(hass, name, message, domain=None, entity_id=None) L68
"Add an entry to the logbook."
async_log_entry(hass, name, message, domain=None, entity_id=None) L73
"Add an entry to the logbook."
setup(hass, config) L87
"Listen for download events to download files."
log_message(service) L90
"Handle sending notification message service calls."
humanify(events) L166
"Generator that converts a list of events into Entry objects."
_get_events(hass, start_day, end_day) L276
"Get events for a period of time."
_exclude_events(events, config) L290
"Get lists of excluded entities and platforms."
_entry_message_from_state(domain, state) L363
"Convert a state to a message for the logbook."

homeassistant/components/logentries.py
setup(hass, config) L30
"Setup the Logentries component."
logentries_event_listener(event) L36
"Listen for new messages on the bus and sends them to Logentries."

homeassistant/components/logger.py
class HomeAssistantLogFilter(logging.Filter) L54
  "A log filter."
  __init__(self, logfilter) L58
  "Initialize the filter."
  filter(self, record) L64
  "A filter to use."

set_level(hass, logs) L49
"Set log level for components."
async_setup(hass, config) L79
"Setup the logger component."
set_log_levels(logpoints) L90
"Set the specified log levels."
async_service_handler(service) L122
"Handle logger services."

homeassistant/components/lutron.py
class LutronDevice(Entity) L51
  "Representation of a Lutron device entity."
  __init__(self, area_name, lutron_device, controller) L54
  "Initialize the device."
  async_added_to_hass(self) L61
  "Register callbacks."
  _update_callback(self, _device) L68
  "Callback invoked by pylutron when the device state changes."
  name(self) L73
  "Return the name of the device."
  should_poll(self) L78
  "No polling needed."

setup(hass, base_config) L24
"Setup the Lutron component."

homeassistant/components/lutron_caseta.py
class LutronCasetaDevice(Entity) L56
  "Common base class for all Lutron Caseta devices."
  __init__(self, device, bridge) L59
  "Set up the base class."
  async_added_to_hass(self) L73
  "Register callbacks."
  _update_callback(self) L80
  name(self) L84
  "Return the name of the device."
  device_state_attributes(self) L89
  "Return the state attributes."
  should_poll(self) L96
  "No polling needed."

setup(hass, base_config) L34
"Setup the Lutron component."

homeassistant/components/maxcube.py
class MaxCubeHandle(object) L58
  "Keep the cube instance in one place and centralize the update."
  __init__(self, cube) L61
  "Initialize the Cube Handle."
  update(self) L67
  "Pull the latest data from the MAX!"

setup(hass, config) L35
"Establish connection to MAX!"

homeassistant/components/media_player/anthemav.py
class AnthemAVR(MediaPlayerDevice) L69
  "Entity reading values from Anthem AVR protocol."
  __init__(self, avr, name) L72
  ""Initialize entity with transport."
  _lookup(self, propname, dval=None) L78
  supported_features(self) L82
  "Flag media player features that are supported."
  should_poll(self) L87
  "No polling needed."
  name(self) L92
  "Return name of device."
  state(self) L97
  "Return state of power on/off."
  is_volume_muted(self) L109
  "Return boolean reflecting mute state on device."
  volume_level(self) L114
  "Return volume level from 0 to 1."
  media_title(self) L119
  "Return current input name (closest we have to media title)."
  app_name(self) L124
  "Return details about current video and audio stream."
  source(self) L130
  "Return currently selected input."
  source_list(self) L135
  "Return all active, configured inputs."
  async_select_source(self, source) L140
  "Change AVR to the designated source (by name)."
  async_turn_off(self) L145
  "Turn AVR power off."
  async_turn_on(self) L150
  "Turn AVR power on."
  async_set_volume_level(self, volume) L155
  "Set AVR volume (0 to 1)."
  async_mute_volume(self, mute) L160
  "Engage AVR mute."
  _update_avr(self, propname, value) L164
  "Update a property in the AVR."
  dump_avrdata(self) L170
  "Return state of avr object for debugging forensics."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L39
"Set up our socket to the AVR."
async_anthemav_update_callback(message) L50
"Receive notification from transport that new data exists."

homeassistant/components/media_player/apple_tv.py
class AppleTvDevice(MediaPlayerDevice) L84
  "Representation of an Apple TV device."
  __init__(self, atv, name, is_off) L87
  "Initialize the Apple TV device."
  async_added_to_hass(self) L97
  "Called when entity is about to be added to HASS."
  _set_power_off(self, is_off) L103
  name(self) L114
  "Return the name of the device."
  should_poll(self) L119
  "No polling needed."
  state(self) L124
  "Return the state of the device."
  playstatus_update(self, updater, playing) L146
  "Print what is currently playing when it changes."
  _has_playing_media_changed(self, new_playing) L160
  playstatus_error(self, updater, exception) L168
  "Inform about an error and restart push updates."
  media_content_type(self) L183
  "Content type of current playing media."
  media_duration(self) L196
  "Duration of current playing media in seconds."
  media_position(self) L202
  "Position of current playing media in seconds."
  media_position_updated_at(self) L208
  "Last valid time of media position."
  async_play_media(self, media_type, media_id, **kwargs) L215
  "Send the play_media command to the media player."
  media_image_hash(self) L220
  "Hash value for media image."
  async_get_media_image(self) L226
  "Fetch media image of current playing image."
  media_title(self) L231
  "Title of current playing media."
  supported_features(self) L242
  "Flag media player features that are supported."
  async_turn_on(self) L254
  "Turn the media player on."
  async_turn_off(self) L259
  "Turn the media player off."
  async_media_play_pause(self) L263
  "Pause media on media player."
  async_media_play(self) L275
  "Play media."
  async_media_pause(self) L283
  "Pause the media player."
  async_media_next_track(self) L291
  "Send next track command."
  async_media_previous_track(self) L299
  "Send previous track command."
  async_media_seek(self, position) L307
  "Send seek command."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L47
"Setup the Apple TV platform."
on_hass_stop(event) L75
"Stop push updates when hass stops."

homeassistant/components/media_player/aquostv.py
class SharpAquosTVDevice(MediaPlayerDevice) L117
  "Representation of a Aquos TV."
  __init__(self, name, remote, power_on_enabled=False) L121
  "Initialize the aquos device."
  set_state(self, state) L137
  "Set TV state."
  update(self) L142
  "Retrieve the latest data."
  name(self) L164
  "Return the name of the device."
  state(self) L169
  "Return the state of the device."
  source(self) L174
  "Return the current source."
  source_list(self) L179
  "Return the source list."
  volume_level(self) L184
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L189
  "Boolean if volume is currently muted."
  supported_features(self) L194
  "Flag media player features that are supported."
  turn_off(self) L199
  "Turn off tvplayer."
  volume_up(self) L204
  "Volume up the media player."
  volume_down(self) L209
  "Volume down media player."
  set_volume_level(self, level) L214
  "Set Volume media player."
  mute_volume(self, mute) L219
  "Send mute command."
  turn_on(self) L224
  "Turn the media player on."
  media_play_pause(self) L229
  "Simulate play pause media player."
  media_play(self) L234
  "Send play command."
  media_pause(self) L239
  "Send pause command."
  media_next_track(self) L244
  "Send next track command."
  media_previous_track(self) L249
  "Send the previous track command."
  select_source(self, source) L253
  "Set the input source."

setup_platform(hass, config, add_devices, discovery_info=None) L63
"Setup the Sharp Aquos TV platform."
_retry(func) L100
"Decorator to handle query retries."
wrapper(obj, *args, **kwargs) L102
"Wrapper for all query functions."

homeassistant/components/media_player/braviatv.py
class BraviaTVDevice(MediaPlayerDevice) L184
  "Representation of a Sony Bravia TV."
  __init__(self, host, mac, name, pin) L187
  "Initialize the Sony Bravia device."
  update(self) L219
  "Update TV info."
  _reset_playing_info(self) L258
  _refresh_volume(self) L268
  "Refresh volume information."
  _refresh_channels(self) L277
  name(self) L286
  "Return the name of the device."
  state(self) L291
  "Return the state of the device."
  source(self) L296
  "Return the current input source."
  source_list(self) L301
  "List of available input sources."
  volume_level(self) L306
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L314
  "Boolean if volume is currently muted."
  supported_features(self) L319
  "Flag media player features that are supported."
  media_title(self) L324
  "Title of current playing media."
  media_content_id(self) L334
  "Content ID of current playing media."
  media_duration(self) L339
  "Duration of current playing media in seconds."
  set_volume_level(self, volume) L343
  "Set volume level, range 0..1."
  turn_on(self) L347
  "Turn the media player on."
  turn_off(self) L351
  "Turn off media player."
  volume_up(self) L355
  "Volume up the media player."
  volume_down(self) L359
  "Volume down media player."
  mute_volume(self, mute) L363
  "Send mute command."
  select_source(self, source) L367
  "Set the input source."
  media_play_pause(self) L373
  "Simulate play pause media player."
  media_play(self) L380
  "Send play command."
  media_pause(self) L385
  "Send media pause command to media player."
  media_next_track(self) L390
  "Send next track command."
  media_previous_track(self) L394
  "Send the previous track command."

_get_mac_address(ip_address) L52
"Get the MAC address of the device."
_config_from_file(filename, config=None) L66
"Create the configuration from a file."
setup_platform(hass, config, add_devices, discovery_info=None) L99
"Set up the Sony Bravia TV platform."
setup_bravia(config, pin, hass, add_devices) L121
"Set up a Sony Bravia TV based on host parameter."
request_configuration(config, hass, add_devices) L149
"Request configuration steps from the user."
bravia_configuration_callback(data) L162
"Callback after user enter PIN."

homeassistant/components/media_player/cast.py
class CastDevice(MediaPlayerDevice) L95
  "Representation of a Cast device on the network."
  __init__(self, chromecast) L98
  "Initialize the Cast device."
  should_poll(self) L111
  "No polling needed."
  name(self) L116
  "Return the name of the device."
  state(self) L122
  "Return the state of the player."
  volume_level(self) L138
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L143
  "Boolean if volume is currently muted."
  media_content_id(self) L148
  "Content ID of current playing media."
  media_content_type(self) L153
  "Content type of current playing media."
  media_duration(self) L166
  "Duration of current playing media in seconds."
  media_image_url(self) L171
  "Image url of current playing media."
  media_title(self) L181
  "Title of current playing media."
  media_artist(self) L186
  "Artist of current playing media (Music track only)."
  media_album(self) L191
  "Album of current playing media (Music track only)."
  media_album_artist(self) L196
  "Album arist of current playing media (Music track only)."
  media_track(self) L201
  "Track number of current playing media (Music track only)."
  media_series_title(self) L206
  "The title of the series of current playing media (TV Show only)."
  media_season(self) L211
  "Season of current playing media (TV Show only)."
  media_episode(self) L216
  "Episode of current playing media (TV Show only)."
  app_id(self) L221
  "Return the ID of the current running app."
  app_name(self) L226
  "Name of the current running app."
  supported_features(self) L231
  "Flag media player features that are supported."
  media_position(self) L236
  "Position of current playing media in seconds."
  media_position_updated_at(self) L252
  "When was the position of the current playing media valid."
  turn_on(self) L259
  "Turn on the ChromeCast."
  turn_off(self) L271
  "Turn Chromecast off."
  mute_volume(self, mute) L275
  "Mute the volume."
  set_volume_level(self, volume) L279
  "Set volume level, range 0..1."
  media_play(self) L283
  "Send play commmand."
  media_pause(self) L287
  "Send pause command."
  media_stop(self) L291
  "Send stop command."
  media_previous_track(self) L295
  "Send previous track command."
  media_next_track(self) L299
  "Send next track command."
  media_seek(self, position) L303
  "Seek the media to a specific location."
  play_media(self, media_type, media_id, **kwargs) L307
  "Play media from a URL."
  new_cast_status(self, status) L312
  "Called when a new cast status is received."
  new_media_status(self, status) L317
  "Called when a new media status is received."

setup_platform(hass, config, add_devices, discovery_info=None) L45
"Setup the cast platform."

homeassistant/components/media_player/clementine.py
class ClementineDevice(MediaPlayerDevice) L55
  "Representation of Clementine Player."
  __init__(self, client, name) L58
  "Initialize the Clementine device."
  update(self) L71
  "Retrieve the latest data from the Clementine Player."
  name(self) L101
  "Return the name of the device."
  state(self) L106
  "Return the state of the device."
  volume_level(self) L111
  "Volume level of the media player (0..1)."
  source(self) L116
  "Return  current source name."
  source_list(self) L125
  "List of available input sources."
  select_source(self, source) L130
  "Select input source."
  media_content_type(self) L138
  "Content type of current playing media."
  media_title(self) L143
  "Title of current playing media."
  media_artist(self) L148
  "Artist of current playing media, music track only."
  media_album_name(self) L153
  "Album name of current playing media, music track only."
  supported_features(self) L158
  "Flag media player features that are supported."
  media_image_hash(self) L163
  "Hash value for media image."
  async_get_media_image(self) L171
  "Fetch media image of current playing image."
  volume_up(self) L179
  "Volume up the media player."
  volume_down(self) L184
  "Volume down media player."
  mute_volume(self, mute) L189
  "Send mute command."
  set_volume_level(self, volume) L193
  "Set volume level."
  media_play_pause(self) L197
  "Simulate play pause media player."
  media_play(self) L204
  "Send play command."
  media_pause(self) L209
  "Send media pause command to media player."
  media_next_track(self) L214
  "Send next track command."
  media_previous_track(self) L218
  "Send the previous track command."

setup_platform(hass, config, add_devices, discovery_info=None) L46
"Setup the Clementine platform."

homeassistant/components/media_player/cmus.py
class CmusDevice(MediaPlayerDevice) L58
  "Representation of a running cmus."
  __init__(self, server, password, port, name) L62
  "Initialize the CMUS device."
  update(self) L77
  "Get the latest data and update the state."
  name(self) L86
  "Return the name of the device."
  state(self) L91
  "Return the media state."
  media_content_id(self) L101
  "Content ID of current playing media."
  content_type(self) L106
  "Content type of the current playing media."
  media_duration(self) L111
  "Duration of current playing media in seconds."
  media_title(self) L116
  "Title of current playing media."
  media_artist(self) L121
  "Artist of current playing media, music track only."
  media_track(self) L126
  "Track number of current playing media, music track only."
  media_album_name(self) L131
  "Album name of current playing media, music track only."
  media_album_artist(self) L136
  "Album artist of current playing media, music track only."
  volume_level(self) L141
  "Return the volume level."
  supported_features(self) L152
  "Flag media player features that are supported."
  turn_off(self) L156
  "Service to send the CMUS the command to stop playing."
  turn_on(self) L160
  "Service to send the CMUS the command to start playing."
  set_volume_level(self, volume) L164
  "Set volume level, range 0..1."
  volume_up(self) L168
  "Function to send CMUS the command for volume up."
  volume_down(self) L180
  "Function to send CMUS the command for volume down."
  play_media(self, media_type, media_id, **kwargs) L192
  "Send the play command."
  media_pause(self) L201
  "Send the pause command."
  media_next_track(self) L205
  "Send next track command."
  media_previous_track(self) L209
  "Send next track command."
  media_seek(self, position) L213
  "Send seek command."
  media_play(self) L217
  "Send the play command."
  media_stop(self) L221
  "Send the stop command."

setup_platform(hass, config, add_devices, discover_info=None) L41
"Setup the CMUS platform."

homeassistant/components/media_player/demo.py
class AbstractDemoPlayer(MediaPlayerDevice) L45
  "A demo media players."
  __init__(self, name) L50
  "Initialize the demo device."
  should_poll(self) L58
  "Push an update after each command."
  name(self) L63
  "Return the name of the media player."
  state(self) L68
  "Return the state of the player."
  volume_level(self) L73
  "Return the volume level of the media player (0..1)."
  is_volume_muted(self) L78
  "Return boolean if volume is currently muted."
  turn_on(self) L82
  "Turn the media player on."
  turn_off(self) L87
  "Turn the media player off."
  mute_volume(self, mute) L92
  "Mute the volume."
  set_volume_level(self, volume) L97
  "Set the volume level, range 0..1."
  media_play(self) L102
  "Send play command."
  media_pause(self) L107
  "Send pause command."

class DemoYoutubePlayer(AbstractDemoPlayer) L113
  "A Demo media player that only supports YouTube."
  __init__(self, name, youtube_id=None, media_title=None, duration=360) L118
  "Initialize the demo device."
  media_content_id(self) L128
  "Return the content ID of current playing media."
  media_content_type(self) L133
  "Return the content type of current playing media."
  media_duration(self) L138
  "Return the duration of current playing media in seconds."
  media_image_url(self) L143
  "Return the image url of current playing media."
  media_title(self) L148
  "Return the title of current playing media."
  app_name(self) L153
  "Return the current running application."
  supported_features(self) L158
  "Flag media player features that are supported."
  media_position(self) L163
  "Position of current playing media in seconds."
  media_position_updated_at(self) L177
  "When was the position of the current playing media valid."
  play_media(self, media_type, media_id, **kwargs) L185
  "Play a piece of media."
  media_pause(self) L190
  "Send pause command."

class DemoMusicPlayer(AbstractDemoPlayer) L197
  "A Demo media player that only supports YouTube."
  __init__(self) L224
  "Initialize the demo device."
  media_content_id(self) L230
  "Return the content ID of current playing media."
  media_content_type(self) L235
  "Return the content type of current playing media."
  media_duration(self) L240
  "Return the duration of current playing media in seconds."
  media_image_url(self) L245
  "Return the image url of current playing media."
  media_title(self) L251
  "Return the title of current playing media."
  media_artist(self) L256
  "Return the artist of current playing media (Music track only)."
  media_album_name(self) L261
  "Return the album of current playing media (Music track only)."
  media_track(self) L267
  "Return the track number of current media (Music track only)."
  supported_features(self) L272
  "Flag media player features that are supported."
  media_previous_track(self) L284
  "Send previous track command."
  media_next_track(self) L290
  "Send next track command."
  clear_playlist(self) L296
  "Clear players playlist."

class DemoTVShowPlayer(AbstractDemoPlayer) L304
  "A Demo media player that only supports YouTube."
  __init__(self) L309
  "Initialize the demo device."
  media_content_id(self) L317
  "Return the content ID of current playing media."
  media_content_type(self) L322
  "Return the content type of current playing media."
  media_duration(self) L327
  "Return the duration of current playing media in seconds."
  media_image_url(self) L332
  "Return the image url of current playing media."
  media_title(self) L337
  "Return the title of current playing media."
  media_series_title(self) L342
  "Return the series title of current playing media (TV Show only)."
  media_season(self) L347
  "Return the season of current playing media (TV Show only)."
  media_episode(self) L352
  "Return the episode of current playing media (TV Show only)."
  app_name(self) L357
  "Return the current running application."
  source(self) L362
  "Return the current input source."
  supported_features(self) L367
  "Flag media player features that are supported."
  media_previous_track(self) L379
  "Send previous track command."
  media_next_track(self) L385
  "Send next track command."
  select_source(self, source) L391
  "Set the input source."

setup_platform(hass, config, add_devices, discovery_info=None) L18
"Setup the media player demo platform."

homeassistant/components/media_player/denon.py
class DenonDevice(MediaPlayerDevice) L63
  "Representation of a Denon device."
  __init__(self, name, host) L66
  "Initialize the Denon device."
  _setup_sources(self, telnet) L82
  telnet_request(cls, telnet, command, all_lines=False) L102
  "Execute `command` and return the response."
  telnet_command(self, command) L118
  "Establish a telnet connection and sends `command`."
  update(self) L126
  "Get the latest details from the device."
  name(self) L161
  "Return the name of the device."
  state(self) L166
  "Return the state of the device."
  volume_level(self) L176
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L181
  "Boolean if volume is currently muted."
  source_list(self) L186
  "List of available input sources."
  media_title(self) L191
  "Current media info."
  supported_features(self) L196
  "Flag media player features that are supported."
  source(self) L204
  "Return the current input source."
  turn_off(self) L210
  "Turn off media player."
  volume_up(self) L214
  "Volume up media player."
  volume_down(self) L218
  "Volume down media player."
  set_volume_level(self, volume) L222
  "Set volume level, range 0..1."
  mute_volume(self, mute) L227
  "Mute (true) or unmute (false) media player."
  media_play(self) L231
  "Play media media player."
  media_pause(self) L235
  "Pause media player."
  media_stop(self) L239
  "Pause media player."
  media_next_track(self) L243
  "Send the next track command."
  media_previous_track(self) L247
  "Send the previous track command."
  turn_on(self) L251
  "Turn the media player on."
  select_source(self, source) L255
  "Select input source."

setup_platform(hass, config, add_devices, discovery_info=None) L52
"Setup the Denon platform."

homeassistant/components/media_player/denonavr.py
class DenonDevice(MediaPlayerDevice) L96
  "Representation of a Denon Media Player Device."
  __init__(self, receiver) L99
  "Initialize the device."
  update(self) L117
  "Get the latest status information from device."
  name(self) L138
  "Return the name of the device."
  state(self) L143
  "Return the state of the device."
  is_volume_muted(self) L148
  "Boolean if volume is currently muted."
  volume_level(self) L153
  "Volume level of the media player (0..1)."
  source(self) L160
  "Return the current input source."
  source_list(self) L165
  "List of available input sources."
  supported_features(self) L170
  "Flag media player features that are supported."
  media_content_id(self) L178
  "Content ID of current playing media."
  media_content_type(self) L183
  "Content type of current playing media."
  media_duration(self) L191
  "Duration of current playing media in seconds."
  media_image_url(self) L196
  "Image url of current playing media."
  media_title(self) L204
  "Title of current playing media."
  media_artist(self) L214
  "Artist of current playing media, music track only."
  media_album_name(self) L222
  "Album name of current playing media, music track only."
  media_album_artist(self) L230
  "Album artist of current playing media, music track only."
  media_track(self) L235
  "Track number of current playing media, music track only."
  media_series_title(self) L240
  "Title of series of current playing media, TV show only."
  media_season(self) L245
  "Season of current playing media, TV show only."
  media_episode(self) L250
  "Episode of current playing media, TV show only."
  media_play_pause(self) L254
  "Simulate play pause media player."
  media_previous_track(self) L258
  "Send previous track command."
  media_next_track(self) L262
  "Send next track command."
  select_source(self, source) L266
  "Select input source."
  turn_on(self) L270
  "Turn on media player."
  turn_off(self) L278
  "Turn off media player."
  volume_up(self) L286
  "Volume up the media player."
  volume_down(self) L290
  "Volume down media player."
  set_volume_level(self, volume) L294
  "Set volume level, range 0..1."
  mute_volume(self, mute) L310
  "Send mute command."

setup_platform(hass, config, add_devices, discovery_info=None) L43
"Setup the Denon platform."

homeassistant/components/media_player/directv.py
class DirecTvDevice(MediaPlayerDevice) L67
  "Representation of a DirecTV reciever on the network."
  __init__(self, name, host, port) L70
  "Initialize the device."
  update(self) L78
  "Retrieve latest state."
  name(self) L87
  "Return the name of the device."
  state(self) L93
  "Return the state of the device."
  media_content_id(self) L102
  "Content ID of current playing media."
  media_duration(self) L110
  "Duration of current playing media in seconds."
  media_title(self) L118
  "Title of current playing media."
  media_series_title(self) L126
  "Title of current episode of TV show."
  supported_features(self) L137
  "Flag media player features that are supported."
  media_content_type(self) L142
  "Content type of current playing media."
  media_channel(self) L150
  "Channel current playing media."
  turn_on(self) L159
  "Turn on the reciever."
  turn_off(self) L163
  "Turn off the reciever."
  media_play(self) L167
  "Send play commmand."
  media_pause(self) L171
  "Send pause commmand."
  media_stop(self) L175
  "Send stop commmand."
  media_previous_track(self) L179
  "Send rewind commmand."
  media_next_track(self) L183
  "Send fast forward commmand."

setup_platform(hass, config, add_devices, discovery_info=None) L36
"Setup the DirecTV platform."

homeassistant/components/media_player/dunehd.py
class DuneHDPlayerEntity(MediaPlayerDevice) L47
  "Implementation of the Dune HD player."
  __init__(self, player, name, sources) L50
  "Setup entity to control Dune HD."
  update(self) L59
  "Update internal status of the entity."
  state(self) L66
  "Return player state."
  name(self) L80
  "Return the name of the device."
  volume_level(self) L85
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L90
  "Boolean if volume is currently muted."
  source_list(self) L95
  "List of available input sources."
  supported_features(self) L100
  "Flag media player features that are supported."
  volume_up(self) L104
  "Volume up media player."
  volume_down(self) L108
  "Volume down media player."
  mute_volume(self, mute) L112
  "Mute/unmute player volume."
  turn_off(self) L116
  "Turn off media player."
  turn_on(self) L122
  "Turn off media player."
  media_play(self) L127
  "Play media media player."
  media_pause(self) L132
  "Pause media player."
  media_title(self) L138
  "Current media source."
  __update_title(self) L145
  select_source(self, source) L158
  "Select input source."
  media_previous_track(self) L164
  "Send previous track command."
  media_next_track(self) L169
  "Send next track command."

setup_platform(hass, config, add_devices, discovery_info=None) L36
"Set up the media player demo platform."

homeassistant/components/media_player/emby.py
class EmbyDevice(MediaPlayerDevice) L127
  "Representation of an Emby device."
  __init__(self, emby, device_id) L130
  "Initialize the Emby device."
  async_added_to_hass(self) L144
  "Register callback."
  async_update_callback(self, msg) L150
  "Callback for device updates."
  hidden(self) L165
  "Return True if entity should be hidden from UI."
  set_hidden(self, value) L169
  "Set hidden property."
  available(self) L174
  "Return True if entity is available."
  set_available(self, value) L178
  "Set available property."
  unique_id(self) L183
  "Return the id of this emby client."
  supports_remote_control(self) L188
  "Return control ability."
  name(self) L193
  "Return the name of the device."
  should_poll(self) L199
  "Return True if entity has to be polled for state."
  state(self) L204
  "Return the state of the device."
  app_name(self) L217
  "Return current user as app_name."
  media_content_id(self) L223
  "Content ID of current playing media."
  media_content_type(self) L228
  "Content type of current playing media."
  media_duration(self) L246
  "Duration of current playing media in seconds."
  media_position(self) L251
  "Position of current playing media in seconds."
  media_position_updated_at(self) L256
  "When was the position of the current playing media valid."
  media_image_url(self) L265
  "Image url of current playing media."
  media_title(self) L270
  "Title of current playing media."
  media_season(self) L275
  "Season of curent playing media (TV Show only)."
  media_series_title(self) L280
  "The title of the series of current playing media (TV Show only)."
  media_episode(self) L285
  "Episode of current playing media (TV Show only)."
  media_album_name(self) L290
  "Album name of current playing media (Music track only)."
  media_artist(self) L295
  "Artist of current playing media (Music track only)."
  media_album_artist(self) L300
  "Album artist of current playing media (Music track only)."
  supported_features(self) L305
  "Flag media player features that are supported."
  async_media_play(self) L312
  "Play media."
  async_media_pause(self) L319
  "Pause the media player."
  async_media_stop(self) L326
  "Stop the media player."
  async_media_next_track(self) L333
  "Send next track command."
  async_media_previous_track(self) L340
  "Send next track command."
  async_media_seek(self, position) L347
  "Send seek command."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L54
"Setup the Emby platform."
device_update_callback(data) L75
"Callback for when devices are added to emby."
device_removal_callback(data) L100
"Callback for when devices are removed from emby."
start_emby(event) L111
"Start emby connection."
stop_emby(event) L116
"Stop emby connection."

homeassistant/components/media_player/firetv.py
class FireTV(object) L64
  "The firetv-server client."
  __init__(self, host, port, device_id) L75
  "Initialize the FireTV server."
  state(self) L82
  "Get the device state."
  action(self, action_id) L94
  "Perform an action on the device."

class FireTVDevice(MediaPlayerDevice) L105
  "Representation of an Amazon Fire TV device on the network."
  __init__(self, host, port, device, name) L108
  "Initialize the FireTV device."
  name(self) L115
  "Return the device name."
  should_poll(self) L120
  "Device should be polled."
  supported_features(self) L125
  "Flag media player features that are supported."
  state(self) L130
  "Return the state of the player."
  update(self) L134
  "Get the latest date and update device state."
  turn_on(self) L145
  "Turn on the device."
  turn_off(self) L149
  "Turn off the device."
  media_play(self) L153
  "Send play command."
  media_pause(self) L157
  "Send pause command."
  media_play_pause(self) L161
  "Send play/pause command."
  volume_up(self) L165
  "Send volume up command."
  volume_down(self) L169
  "Send volume down command."
  media_previous_track(self) L173
  "Send previous track command (results in rewind)."
  media_next_track(self) L177
  "Send next track command (results in fast-forward)."

setup_platform(hass, config, add_devices, discovery_info=None) L44
"Setup the FireTV platform."

homeassistant/components/media_player/frontier_silicon.py
class FSAPIDevice(MediaPlayerDevice) L72
  "Representation of a Frontier Silicon device on the network."
  __init__(self, device_url, password) L75
  "Initialize the Frontier Silicon API device."
  fs_device(self) L92
  "Create a fresh fsapi session."
  should_poll(self) L105
  "Device should be polled."
  name(self) L110
  "Return the device name."
  media_title(self) L115
  "Title of current playing media."
  media_artist(self) L120
  "Artist of current playing media, music track only."
  media_album_name(self) L125
  "Album name of current playing media, music track only."
  media_content_type(self) L130
  "Content type of current playing media."
  supported_features(self) L135
  "Flag of media commands that are supported."
  state(self) L140
  "Return the state of the player."
  source_list(self) L146
  "List of available input sources."
  source(self) L151
  "Name of the current input source."
  media_image_url(self) L156
  "Image url of current playing media."
  update(self) L160
  "Get the latest date and update device state."
  turn_on(self) L193
  "Turn on the device."
  turn_off(self) L197
  "Turn off the device."
  media_play(self) L201
  "Send play command."
  media_pause(self) L205
  "Send pause command."
  media_play_pause(self) L209
  "Send play/pause command."
  media_stop(self) L216
  "Send play/pause command."
  media_previous_track(self) L220
  "Send previous track command (results in rewind)."
  media_next_track(self) L224
  "Send next track command (results in fast-forward)."
  is_volume_muted(self) L230
  "Boolean if volume is currently muted."
  mute_volume(self, mute) L234
  "Send mute command."
  volume_up(self) L239
  "Send volume up command."
  volume_down(self) L243
  "Send volume down command."
  set_volume_level(self, volume) L247
  "Set volume command."
  select_source(self, source) L251
  "Select input source."

setup_platform(hass, config, add_devices, discovery_info=None) L44
"Set up the Frontier Silicon platform."

homeassistant/components/media_player/gpmdp.py
class GPMDP(MediaPlayerDevice) L170
  "Representation of a GPMDP."
  __init__(self, name, url, code) L173
  "Initialize the media player."
  get_ws(self) L191
  "Check if the websocket is setup and connected."
  send_gpmdp_msg(self, namespace, method, with_id=True) L206
  "Send ws messages to GPMDP and verify request id in response."
  update(self) L232
  "Get the latest details from the player."
  media_content_type(self) L253
  "Content type of current playing media."
  state(self) L258
  "Return the state of the device."
  media_title(self) L263
  "Title of current playing media."
  media_artist(self) L268
  "Artist of current playing media (Music track only)."
  media_image_url(self) L273
  "Image url of current playing media."
  media_seek_position(self) L278
  "Time in seconds of current seek position."
  media_duration(self) L283
  "Time in seconds of current song duration."
  volume_level(self) L288
  "Volume level of the media player (0..1)."
  name(self) L293
  "Return the name of the device."
  supported_features(self) L298
  "Flag media player features that are supported."
  media_next_track(self) L302
  "Send media_next command to media player."
  media_previous_track(self) L306
  "Send media_previous command to media player."
  media_play(self) L310
  "Send media_play command to media player."
  media_pause(self) L316
  "Send media_pause command to media player."
  media_seek(self, position) L322
  "Send media_seek command to media player."
  volume_up(self) L332
  "Send volume_up command to media player."
  volume_down(self) L340
  "Send volume_down command to media player."
  set_volume_level(self, volume) L348
  "Set volume on media player, range(0..1)."

request_configuration(hass, config, url, add_devices_callback) L49
"Request configuration steps from the user."
gpmdp_configuration_callback(callback_data) L64
"The actions to do when our configuration callback is called."
setup_gpmdp(hass, config, code, add_devices) L108
"Setup gpmdp."
_load_config(filename) L126
"Load configuration."
_save_config(filename, config) L145
"Save configuration."
setup_platform(hass, config, add_devices_callback, discovery_info=None) L156
"Set up the GPMDP platform."

homeassistant/components/media_player/gstreamer.py
class GstreamerDevice(MediaPlayerDevice) L53
  "Representation of a Gstreamer device."
  __init__(self, player, name) L56
  "Initialize the Gstreamer device."
  update(self) L68
  "Update properties."
  set_volume_level(self, volume) L78
  "Set the volume level."
  play_media(self, media_type, media_id, **kwargs) L82
  "Play media."
  media_play(self) L89
  "Play."
  media_pause(self) L93
  "Pause."
  media_next_track(self) L97
  "Next track."
  media_content_id(self) L102
  "Content ID of currently playing media."
  content_type(self) L107
  "Content type of currently playing media."
  name(self) L112
  "Return the name of the device."
  volume_level(self) L117
  "Return the volume level."
  supported_features(self) L122
  "Flag media player features that are supported."
  state(self) L127
  "Return the state of the player."
  media_duration(self) L132
  "Duration of current playing media in seconds."
  media_title(self) L137
  "Media title."
  media_artist(self) L142
  "Media artist."
  media_album_name(self) L147
  "Media album."

setup_platform(hass, config, add_devices, discovery_info=None) L38
"Setup the Gstreamer platform."
_shutdown(call) L45
"Quit the player on shutdown."

homeassistant/components/media_player/hdmi_cec.py
class CecPlayerDevice(CecDevice, MediaPlayerDevice) L34
  "Representation of a HDMI device as a Media palyer."
  __init__(self, hass, device, logical) L37
  "Initialize the HDMI device."
  send_keypress(self, key) L44
  "Send keypress to CEC adapter."
  send_playback(self, key) L54
  "Send playback status to CEC adapter."
  mute_volume(self, mute) L60
  "Mute volume."
  media_previous_track(self) L65
  "Go to previous track."
  turn_on(self) L70
  "Turn device on."
  clear_playlist(self) L75
  "Clear players playlist."
  turn_off(self) L79
  "Turn device off."
  media_stop(self) L84
  "Stop playback."
  play_media(self, media_type, media_id) L90
  "Not supported."
  media_next_track(self) L94
  "Skip to next track."
  media_seek(self, position) L99
  "Not supported."
  set_volume_level(self, volume) L103
  "Set volume level, range 0..1."
  media_pause(self) L107
  "Pause playback."
  select_source(self, source) L113
  "Not supported."
  media_play(self) L117
  "Start playback."
  volume_up(self) L123
  "Increase volume."
  volume_down(self) L129
  "Decrease volume."
  state(self) L136
  "Cached state of device."
  _update(self, device=None) L140
  "Update device status."
  supported_features(self) L161
  "Flag media player features that are supported."

setup_platform(hass, config, add_devices, discovery_info=None) L25
"Find and return HDMI devices as +switches."

homeassistant/components/media_player/itunes.py
class Itunes(object) L44
  "The iTunes API client."
  __init__(self, host, port, use_ssl) L47
  "Initialize the iTunes device."
  _base_url(self) L54
  "Return the base url for endpoints."
  _request(self, method, path, params=None) L66
  "Make the actual request and return the parsed response."
  _command(self, named_command) L86
  "Make a request for a controlling command."
  now_playing(self) L90
  "Return the current state."
  set_volume(self, level) L94
  "Set the volume and returns the current state, level 0-100."
  set_muted(self, muted) L98
  "Mute and returns the current state, muted True or False."
  play(self) L102
  "Set playback to play and returns the current state."
  pause(self) L106
  "Set playback to paused and returns the current state."
  next(self) L110
  "Skip to the next track and returns the current state."
  previous(self) L114
  "Skip back and returns the current state."
  play_playlist(self, playlist_id_or_name) L118
  "Set a playlist to be current and returns the current state."
  artwork_url(self) L132
  "Return a URL of the current track's album art."
  airplay_devices(self) L136
  "Return a list of AirPlay devices."
  airplay_device(self, device_id) L140
  "Return an AirPlay device."
  toggle_airplay_device(self, device_id, toggle) L144
  "Toggle airplay device on or off, id, toggle True or False."
  set_volume_airplay_device(self, device_id, level) L150
  "Set volume, returns current state of device, id,level 0-100."

class ItunesDevice(MediaPlayerDevice) L170
  "Representation of an iTunes API instance."
  __init__(self, name, host, port, use_ssl, add_devices) L173
  "Initialize the iTunes device."
  update_state(self, state_hash) L197
  "Update all the state properties with the passed in dictionary."
  name(self) L210
  "Return the name of the device."
  state(self) L215
  "Return the state of the device."
  update(self) L231
  "Retrieve latest state."
  is_volume_muted(self) L259
  "Boolean if volume is currently muted."
  volume_level(self) L264
  "Volume level of the media player (0..1)."
  media_content_id(self) L269
  "Content ID of current playing media."
  media_content_type(self) L274
  "Content type of current playing media."
  media_image_url(self) L279
  "Image url of current playing media."
  media_title(self) L289
  "Title of current playing media."
  media_artist(self) L294
  "Artist of current playing media (Music track only)."
  media_album_name(self) L299
  "Album of current playing media (Music track only)."
  media_playlist(self) L304
  "Title of the currently playing playlist."
  supported_features(self) L309
  "Flag media player features that are supported."
  set_volume_level(self, volume) L313
  "Set volume level, range 0..1."
  mute_volume(self, mute) L318
  "Mute (true) or unmute (false) media player."
  media_play(self) L323
  "Send media_play command to media player."
  media_pause(self) L328
  "Send media_pause command to media player."
  media_next_track(self) L333
  "Send media_next command to media player."
  media_previous_track(self) L338
  "Send media_previous command media player."
  play_media(self, media_type, media_id, **kwargs) L343
  "Send the play_media command to the media player."

class AirPlayDevice(MediaPlayerDevice) L350
  "Representation an AirPlay device via an iTunes API instance."
  __init__(self, device_id, client) L353
  "Initialize the AirPlay device."
  update_state(self, state_hash) L366
  "Update all the state properties with the passed in dictionary."
  name(self) L394
  "Return the name of the device."
  icon(self) L399
  "Return the icon to use in the frontend, if any."
  state(self) L407
  "Return the state of the device."
  update(self) L414
  "Retrieve latest state."
  volume_level(self) L418
  "Return the volume."
  media_content_type(self) L423
  "Flag of media content that is supported."
  supported_features(self) L428
  "Flag media player features that are supported."
  set_volume_level(self, volume) L432
  "Set volume level, range 0..1."
  turn_on(self) L438
  "Select AirPlay."
  turn_off(self) L445
  "Deselect AirPlay."

setup_platform(hass, config, add_devices, discovery_info=None) L156
"Setup the iTunes platform."

homeassistant/components/media_player/kodi.py
class KodiDevice(MediaPlayerDevice) L183
  "Representation of a XBMC/Kodi device."
  __init__(self, hass, name, host, port, tcp_port, encryption=False, username=None, password=None, turn_off_action=None, websocket=True) L186
  "Initialize the Kodi device."
  on_hass_stop(event) L230
  "Close websocket connection when hass stops."
  async_on_speed_event(self, sender, data) L247
  "Called when player changes between playing and paused."
  async_on_stop(self, sender, data) L261
  "Called when the player stops playback."
  async_on_volume_changed(self, sender, data) L273
  "Called when the volume is changed."
  async_on_quit(self, sender, data) L280
  "Called when the volume is changed."
  _get_players(self) L289
  "Return the active player objects or None."
  state(self) L301
  "Return the state of the device."
  async_ws_connect(self) L315
  "Connect to Kodi via websocket protocol."
  ws_loop_wrapper() L327
  "Catch exceptions from the websocket loop task."
  async_update(self) L341
  "Retrieve latest state."
  server(self) L380
  "Active server for json-rpc requests."
  name(self) L388
  "Return the name of the device."
  should_poll(self) L393
  "Return True if entity has to be polled for state."
  volume_level(self) L398
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L404
  "Boolean if volume is currently muted."
  media_content_id(self) L409
  "Content ID of current playing media."
  media_content_type(self) L414
  "Content type of current playing media."
  media_duration(self) L419
  "Duration of current playing media in seconds."
  media_image_url(self) L435
  "Image url of current playing media."
  media_title(self) L448
  "Title of current playing media."
  media_series_title(self) L455
  "Title of series of current playing media, TV show only."
  media_season(self) L460
  "Season of current playing media, TV show only."
  media_episode(self) L465
  "Episode of current playing media, TV show only."
  media_album_name(self) L470
  "Album name of current playing media, music track only."
  media_artist(self) L475
  "Artist of current playing media, music track only."
  media_album_artist(self) L484
  "Album artist of current playing media, music track only."
  supported_features(self) L493
  "Flag media player features that are supported."
  async_turn_off(self) L504
  "Execute turn_off_action to turn off media player."
  async_volume_up(self) L521
  "Volume up the media player."
  async_volume_down(self) L528
  "Volume down the media player."
  async_set_volume_level(self, volume) L534
  "Set volume level, range 0..1."
  async_mute_volume(self, mute) L542
  "Mute (true) or unmute (false) media player."
  async_set_play_state(self, state) L550
  "Helper method for play/pause/toggle."
  async_media_play_pause(self) L559
  "Pause media on media player."
  async_media_play(self) L567
  "Play media."
  async_media_pause(self) L575
  "Pause the media player."
  async_media_stop(self) L584
  "Stop the media player."
  _goto(self, direction) L592
  "Helper method used for previous/next track."
  async_media_next_track(self) L606
  "Send next track command."
  async_media_previous_track(self) L614
  "Send next track command."
  async_media_seek(self, position) L623
  "Send seek command."
  async_play_media(self, media_type, media_id, **kwargs) L644
  "Send the play_media command to the media player."
  async_set_shuffle(self, shuffle_on) L660
  "Set shuffle mode, for the first player."
  async_add_media_to_playlist(self, media_type, media_id=None, media_name='', artist_name='') L668
  "Add a media to default playlist (i.e. playlistid=0)."
  async_add_all_albums(self, artist_name) L701
  "Add all albums of an artist to default playlist (i.e. playlistid=0)."
  async_clear_playlist(self) L715
  "Clear default playlist (i.e. playlistid=0)."
  async_get_artists(self) L720
  "Get artists list."
  async_get_albums(self, artist_id=None) L725
  "Get albums list."
  async_find_artist(self, artist_name) L734
  "Find artist by name."
  async_get_songs(self, artist_id=None) L742
  "Get songs list."
  async_find_song(self, song_name, artist_name='') L751
  "Find song by name and optionally artist name."
  async_find_album(self, album_name, artist_name='') L765
  "Find album by name and optionally artist name."
  _find(key_word, words) L776

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L110
"Set up the Kodi platform."
async_service_handler(service) L136
"Map services to methods on MediaPlayerDevice."
cmd(func) L163
"Decorator to catch command exceptions."
wrapper(obj, *args, **kwargs) L167
"Wrapper for all command methods."

homeassistant/components/media_player/lg_netcast.py
class LgTVDevice(MediaPlayerDevice) L56
  "Representation of a LG TV."
  __init__(self, client, name) L59
  "Initialize the LG TV device."
  send_command(self, command) L75
  "Send remote control commands to the TV."
  update(self) L85
  "Retrieve the latest data from the LG TV."
  name(self) L121
  "Return the name of the device."
  state(self) L126
  "Return the state of the device."
  is_volume_muted(self) L131
  "Boolean if volume is currently muted."
  volume_level(self) L136
  "Volume level of the media player (0..1)."
  source(self) L141
  "Return the current input source."
  source_list(self) L146
  "List of available input sources."
  media_content_type(self) L151
  "Content type of current playing media."
  media_channel(self) L156
  "Channel currently playing."
  media_title(self) L161
  "Title of current playing media."
  supported_features(self) L166
  "Flag media player features that are supported."
  media_image_url(self) L171
  "URL for obtaining a screen capture."
  turn_off(self) L175
  "Turn off media player."
  volume_up(self) L179
  "Volume up the media player."
  volume_down(self) L183
  "Volume down media player."
  mute_volume(self, mute) L187
  "Send mute command."
  select_source(self, source) L191
  "Select input source."
  media_play_pause(self) L195
  "Simulate play pause media player."
  media_play(self) L202
  "Send play command."
  media_pause(self) L208
  "Send media pause command to media player."
  media_next_track(self) L214
  "Send next track command."
  media_previous_track(self) L218
  "Send the previous track command."

setup_platform(hass, config, add_devices, discovery_info=None) L47
"Setup the LG TV platform."

homeassistant/components/media_player/liveboxplaytv.py
class LiveboxPlayTvDevice(MediaPlayerDevice) L64
  "Representation of an Orange Livebox Play TV."
  __init__(self, host, port, name) L67
  "Initialize the Livebox Play TV device."
  update(self) L82
  "Retrieve the latest data."
  name(self) L98
  "Return the name of the device."
  state(self) L103
  "Return the state of the device."
  is_volume_muted(self) L108
  "Boolean if volume is currently muted."
  source(self) L113
  "Return the current input source."
  source_list(self) L118
  "List of available input sources."
  media_content_type(self) L125
  "Content type of current playing media."
  media_image_url(self) L131
  "Image url of current playing media."
  media_title(self) L136
  "Title of current playing media."
  supported_features(self) L143
  "Flag media player features that are supported."
  refresh_channel_list(self) L147
  "Refresh the list of available channels."
  refresh_state(self) L155
  "Refresh the current media state."
  turn_off(self) L166
  "Turn off media player."
  turn_on(self) L171
  "Turn on the media player."
  volume_up(self) L176
  "Volume up the media player."
  volume_down(self) L180
  "Volume down media player."
  mute_volume(self, mute) L184
  "Send mute command."
  media_play_pause(self) L189
  "Simulate play pause media player."
  select_source(self, source) L193
  "Select input source."
  media_play(self) L198
  "Send play command."
  media_pause(self) L203
  "Send media pause command to media player."
  media_next_track(self) L208
  "Send next track command."
  media_previous_track(self) L212
  "Send the previous track command."

setup_platform(hass, config, add_devices, discovery_info=None) L47
"Setup the Orange Livebox Play TV platform."

homeassistant/components/media_player/mpchc.py
class MpcHcDevice(MediaPlayerDevice) L47
  "Representation of a MPC-HC server."
  __init__(self, name, url) L50
  "Initialize the MPC-HC device."
  update(self) L57
  "Get the latest details."
  _send_command(self, command_id) L74
  "Send a command to MPC-HC via its window message ID."
  name(self) L85
  "Return the name of the device."
  state(self) L90
  "Return the state of the device."
  media_title(self) L104
  "Title of current playing media."
  volume_level(self) L109
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L114
  "Boolean if volume is currently muted."
  media_duration(self) L119
  "Duration of current playing media in seconds."
  supported_features(self) L129
  "Flag media player features that are supported."
  volume_up(self) L133
  "Volume up the media player."
  volume_down(self) L137
  "Volume down media player."
  mute_volume(self, mute) L141
  "Mute the volume."
  media_play(self) L145
  "Send play command."
  media_pause(self) L149
  "Send pause command."
  media_stop(self) L153
  "Send stop command."
  media_next_track(self) L157
  "Send next track command."
  media_previous_track(self) L161
  "Send previous track command."

setup_platform(hass, config, add_devices, discovery_info=None) L39
"Setup the MPC-HC platform."

homeassistant/components/media_player/mpd.py
class MpdDevice(MediaPlayerDevice) L78
  "Representation of a MPD server."
  __init__(self, server, port, password, name) L82
  "Initialize the MPD device."
  update(self) L100
  "Get the latest data and update the state."
  name(self) L123
  "Return the name of the device."
  state(self) L128
  "Return the media state."
  media_content_id(self) L138
  "Content ID of current playing media."
  media_content_type(self) L143
  "Content type of current playing media."
  media_duration(self) L148
  "Duration of current playing media in seconds."
  media_title(self) L154
  "Title of current playing media."
  media_artist(self) L169
  "Artist of current playing media (Music track only)."
  media_album_name(self) L174
  "Album of current playing media (Music track only)."
  volume_level(self) L179
  "Return the volume level."
  supported_features(self) L184
  "Flag media player features that are supported."
  source(self) L189
  "Name of the current input source."
  source_list(self) L194
  "List of available input sources."
  select_source(self, source) L198
  "Choose a different available playlist and play it."
  turn_off(self) L202
  "Service to send the MPD the command to stop playing."
  turn_on(self) L206
  "Service to send the MPD the command to start playing."
  _update_playlists(self, **kwargs) L212
  "Update available MPD playlists."
  set_volume_level(self, volume) L218
  "Set volume of media player."
  volume_up(self) L222
  "Service to send the MPD the command for volume up."
  volume_down(self) L229
  "Service to send the MPD the command for volume down."
  media_play(self) L236
  "Service to send the MPD the command for play/pause."
  media_pause(self) L240
  "Service to send the MPD the command for play/pause."
  media_next_track(self) L244
  "Service to send the MPD the command for next track."
  media_previous_track(self) L248
  "Service to send the MPD the command for previous track."
  play_media(self, media_type, media_id, **kwargs) L252
  "Send the media player the command for playing a playlist."

setup_platform(hass, config, add_devices, discovery_info=None) L46
"Setup the MPD platform."

homeassistant/components/media_player/nad.py
class NAD(MediaPlayerDevice) L63
  "Representation of a NAD Receiver."
  __init__(self, name, nad_receiver, min_volume, max_volume, source_dict) L66
  "Initialize the NAD Receiver device."
  calc_volume(self, decibel) L84
  "Calculate the volume given the decibel."
  calc_db(self, volume) L93
  "Calculate the decibel given the volume."
  name(self) L103
  "Return the name of the device."
  state(self) L108
  "Return the state of the device."
  update(self) L112
  "Retrieve latest state."
  volume_level(self) L129
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L134
  "Boolean if volume is currently muted."
  supported_features(self) L139
  "Flag media player features that are supported."
  turn_off(self) L143
  "Turn the media player off."
  turn_on(self) L147
  "Turn the media player on."
  volume_up(self) L151
  "Volume up the media player."
  volume_down(self) L155
  "Volume down the media player."
  set_volume_level(self, volume) L159
  "Set volume level, range 0..1."
  select_source(self, source) L163
  "Select input source."
  source(self) L168
  "Name of the current input source."
  source_list(self) L173
  "List of available input sources."
  mute_volume(self, mute) L177
  "Mute (true) or unmute (false) media player."

setup_platform(hass, config, add_devices, discovery_info=None) L51
"Setup the NAD platform."

homeassistant/components/media_player/onkyo.py
class OnkyoDevice(MediaPlayerDevice) L69
  "Representation of an Onkyo device."
  __init__(self, receiver, sources, name=None) L72
  "Initialize the Onkyo Receiver."
  command(self, command) L86
  "Run an eiscp command and catch connection errors."
  update(self) L100
  "Get the latest details from the device."
  name(self) L134
  "Return the name of the device."
  state(self) L139
  "Return the state of the device."
  volume_level(self) L144
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L149
  "Boolean if volume is currently muted."
  supported_features(self) L154
  "Flag media player features that are supported."
  source(self) L159
  ""Return the current input source of the device."
  source_list(self) L164
  "List of available input sources."
  turn_off(self) L168
  "Turn off media player."
  set_volume_level(self, volume) L172
  "Set volume level, input is range 0..1."
  mute_volume(self, mute) L176
  "Mute (true) or unmute (false) media player."
  turn_on(self) L183
  "Turn the media player on."
  select_source(self, source) L187
  "Set the input source."

setup_platform(hass, config, add_devices, discovery_info=None) L45
"Setup the Onkyo platform."

homeassistant/components/media_player/openhome.py
class OpenhomeDevice(MediaPlayerDevice) L54
  "Representation of an Openhome device."
  __init__(self, hass, device) L57
  "Initialise the Openhome device."
  update(self) L74
  "Update state of device."
  turn_on(self) L112
  "Bring device out of standby."
  turn_off(self) L116
  "Put device in standby."
  media_pause(self) L120
  "Send pause command."
  media_stop(self) L124
  "Send stop command."
  media_play(self) L128
  "Send play commmand."
  media_next_track(self) L132
  "Send next track command."
  media_previous_track(self) L136
  "Send previous track command."
  select_source(self, source) L140
  "Select input source."
  name(self) L145
  "Return the name of the device."
  supported_features(self) L150
  "Flag of features commands that are supported."
  should_poll(self) L155
  "Polling needed."
  unique_id(self) L160
  "Return an unique ID."
  state(self) L165
  "Return the state of the device."
  source_list(self) L170
  "List of available input sources."
  media_image_url(self) L175
  "Image url of current playing media."
  media_artist(self) L180
  "Artist of current playing media, music track only."
  media_album_name(self) L185
  "Album name of current playing media, music track only."
  media_title(self) L190
  "Title of current playing media."
  source(self) L195
  "Name of the current input source."
  volume_level(self) L200
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L205
  "Return true if volume is muted."
  volume_up(self) L209
  "Volume up media player."
  volume_down(self) L213
  "Volume down media player."
  set_volume_level(self, volume) L217
  "Set volume level, range 0..1."
  mute_volume(self, mute) L221
  "Mute (true) or unmute (false) media player."

setup_platform(hass, config, add_devices, discovery_info=None) L30
"Setup Openhome Platform."

homeassistant/components/media_player/panasonic_viera.py
class PanasonicVieraTVDevice(MediaPlayerDevice) L67
  "Representation of a Panasonic Viera TV."
  __init__(self, mac, name, remote) L70
  "Initialize the Panasonic device."
  update(self) L83
  "Retrieve the latest data."
  send_key(self, key) L92
  "Send a key to the tv and handles exceptions."
  name(self) L103
  "Return the name of the device."
  state(self) L108
  "Return the state of the device."
  volume_level(self) L113
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L118
  "Boolean if volume is currently muted."
  supported_features(self) L123
  "Flag media player features that are supported."
  turn_on(self) L129
  "Turn on the media player."
  turn_off(self) L135
  "Turn off media player."
  volume_up(self) L141
  "Volume up the media player."
  volume_down(self) L145
  "Volume down media player."
  mute_volume(self, mute) L149
  "Send mute command."
  set_volume_level(self, volume) L153
  "Set volume level, range 0..1."
  media_play_pause(self) L162
  "Simulate play pause media player."
  media_play(self) L169
  "Send play command."
  media_pause(self) L174
  "Send media pause command to media player."
  media_next_track(self) L179
  "Send next track command."
  media_previous_track(self) L183
  "Send the previous track command."

setup_platform(hass, config, add_devices, discovery_info=None) L44
"Setup the Panasonic Viera TV platform."

homeassistant/components/media_player/pandora.py
class PandoraMediaPlayer(MediaPlayerDevice) L63
  "A media player that uses the Pianobar interface to Pandora."
  __init__(self, name) L66
  "Initialize the demo device."
  should_poll(self) L81
  "Should be polled for current state."
  name(self) L86
  "Return the name of the media player."
  state(self) L91
  "Return the state of the player."
  turn_on(self) L95
  "Turn the media player on."
  turn_off(self) L125
  "Turn the media player off."
  media_play(self) L143
  "Send play command."
  media_pause(self) L149
  "Send pause command."
  media_next_track(self) L155
  "Go to next track."
  supported_features(self) L161
  "Flag media player features that are supported."
  source(self) L166
  "Name of the current input source."
  source_list(self) L171
  "List of available input sources."
  media_title(self) L176
  "Title of current playing media."
  media_content_type(self) L182
  "Content type of current playing media."
  media_artist(self) L187
  "Artist of current playing media, music track only."
  media_album_name(self) L192
  "Album name of current playing media, music track only."
  media_duration(self) L197
  "Duration of current playing media in seconds."
  select_source(self, source) L201
  "Choose a different Pandora station and play it."
  _send_station_list_command(self) L214
  "Send a station list command."
  update_playing_status(self) L226
  "Query pianobar for info about current media_title, station."
  _query_for_playing_status(self) L235
  "Query system for info about current track."
  _update_current_station(self, response) L266
  "Update current station."
  _update_current_song(self, response) L275
  "Update info about current song."
  _update_song_position(self) L286
  "Get the song position and duration."
  _log_match(self) L306
  "Log grabbed values from console."
  _send_pianobar_command(self, service_cmd) L313
  "Send a command to Pianobar."
  _update_stations(self) L323
  "List defined Pandora stations."
  _clear_buffer(self) L340
  "Clear buffer from pexpect."

setup_platform(hass, config, add_devices, discovery_info=None) L48
"Setup the media player pandora platform."
_stop_pianobar(_event) L56
_pianobar_exists() L357
"Verify that Pianobar is properly installed."

homeassistant/components/media_player/philips_js.py
class PhilipsTV(MediaPlayerDevice) L57
  "Representation of a Philips TV exposing the JointSpace API."
  __init__(self, tv, name) L60
  "Initialize the Philips TV."
  name(self) L79
  "Return the device name."
  should_poll(self) L84
  "Device should be polled."
  supported_features(self) L89
  "Flag media player features that are supported."
  state(self) L97
  "Get the device state."
  source(self) L102
  "Return the current input source."
  source_list(self) L107
  "List of available input sources."
  select_source(self, source) L111
  "Set the input source."
  volume_level(self) L121
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L126
  "Boolean if volume is currently muted."
  turn_off(self) L130
  "Turn off the device."
  volume_up(self) L136
  "Send volume up command."
  volume_down(self) L142
  "Send volume down command."
  mute_volume(self, mute) L148
  "Send mute command."
  media_previous_track(self) L154
  "Send rewind commmand."
  media_next_track(self) L158
  "Send fast forward commmand."
  media_title(self) L163
  "Title of current playing media."
  update(self) L174
  "Get the latest data and update device state."

setup_platform(hass, config, add_devices, discovery_info=None) L45
"Setup the Philips TV platform."

homeassistant/components/media_player/pioneer.py
class PioneerDevice(MediaPlayerDevice) L56
  "Representation of a Pioneer device."
  __init__(self, name, host, port, timeout) L59
  "Initialize the Pioneer device."
  telnet_request(cls, telnet, command, expected_prefix) L73
  "Execute `command` and return the response."
  telnet_command(self, command) L91
  "Establish a telnet connection and sends `command`."
  update(self) L108
  "Get the latest details from the device."
  name(self) L154
  "Return the name of the device."
  state(self) L159
  "Return the state of the device."
  volume_level(self) L169
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L174
  "Boolean if volume is currently muted."
  supported_features(self) L179
  "Flag media player features that are supported."
  source(self) L184
  "Return the current input source."
  source_list(self) L189
  "List of available input sources."
  media_title(self) L194
  "Title of current playing media."
  turn_off(self) L198
  "Turn off media player."
  volume_up(self) L202
  "Volume up media player."
  volume_down(self) L206
  "Volume down media player."
  set_volume_level(self, volume) L210
  "Set volume level, range 0..1."
  mute_volume(self, mute) L215
  "Mute (true) or unmute (false) media player."
  turn_on(self) L219
  "Turn the media player on."
  select_source(self, source) L223
  "Select input source."

setup_platform(hass, config, add_devices, discovery_info=None) L42
"Setup the Pioneer platform."

homeassistant/components/media_player/plex.py
class PlexClient(MediaPlayerDevice) L237
  "Representation of a Plex device."
  __init__(self, config, device, session, plex_sessions, update_devices, update_sessions) L241
  "Initialize the Plex device."
  refresh(self, device, session) L305
  "Refresh key device data."
  _get_thumbnail_url(self, property_value) L470
  "Return full URL (if exists) for a thumbnail property."
  force_idle(self) L483
  "Force client to idle."
  unique_id(self) L489
  "Return the id of this plex client."
  name(self) L495
  "Return the name of the device."
  machine_identifier(self) L500
  "Return the machine identifier of the device."
  app_name(self) L505
  "Return the library name of playing media."
  device(self) L510
  "Return the device, if any."
  session(self) L515
  "Return the session, if any."
  state(self) L520
  "Return the state of the device."
  update(self) L524
  "Get the latest details."
  _convert_na_to_none(self, value) L530
  "Convert PlexAPI _NA() instances to None."
  _active_media_plexapi_type(self) L541
  "Get the active media type required by PlexAPI commands."
  media_content_id(self) L549
  "Content ID of current playing media."
  media_content_type(self) L554
  "Content type of current playing media."
  media_artist(self) L570
  "Artist of current playing media, music track only."
  media_album_name(self) L575
  "Album name of current playing media, music track only."
  media_album_artist(self) L580
  "Album artist of current playing media, music track only."
  media_track(self) L585
  "Track number of current playing media, music track only."
  media_duration(self) L590
  "Duration of current playing media in seconds."
  media_image_url(self) L595
  "Image url of current playing media."
  media_title(self) L600
  "Title of current playing media."
  media_season(self) L605
  "Season of curent playing media (TV Show only)."
  media_series_title(self) L610
  "The title of the series of current playing media (TV Show only)."
  media_episode(self) L615
  "Episode of current playing media (TV Show only)."
  make(self) L620
  "The make of the device (ex."
  supported_features(self) L625
  "Flag media player features that are supported."
  _local_client_control_fix(self) L666
  "Detect if local client and adjust url to allow control."
  set_volume_level(self, volume) L688
  "Set volume level, range 0..1."
  volume_level(self) L697
  "Return the volume level of the client (0..1)."
  is_volume_muted(self) L704
  "Return boolean if volume is currently muted."
  mute_volume(self, mute) L709
  "Mute the volume."
  media_play(self) L727
  "Send play command."
  media_pause(self) L733
  "Send pause command."
  media_stop(self) L739
  "Send stop command."
  turn_off(self) L745
  "Turn the client off."
  media_next_track(self) L750
  "Send next track command."
  media_previous_track(self) L756
  "Send previous track command."
  play_media(self, media_type, media_id, **kwargs) L763
  "Play a piece of media."
  _get_tv_media(self, library_name, show_name, season_number, episode_number) L795
  "Find TV media and return a Plex media object."
  _client_play_media(self, media, delete=False, **params) L840
  "Instruct Plex client to play a piece of media."
  device_state_attributes(self) L869
  "Return the scene state attributes."

config_from_file(filename, config=None) L53
"Small configuration file management function."
setup_platform(hass, config, add_devices_callback, discovery_info=None) L78
"Setup the Plex platform."
setup_plexserver(host, token, hass, config, add_devices_callback) L102
"Setup a plexserver based on host parameter."
update_devices() L139
"Update the devices objects."
update_sessions() L188
"Update the sessions objects."
request_configuration(host, hass, config, add_devices_callback) L206
"Request configuration steps from the user."
plex_configuration_callback(data) L216
"The actions to do when our configuration callback is called."

homeassistant/components/media_player/roku.py
class RokuDevice(MediaPlayerDevice) L83
  "Representation of a Roku device on the network."
  __init__(self, host) L86
  "Initialize the Roku device."
  update(self) L98
  "Retrieve latest state."
  get_source_list(self) L116
  "Get the list of applications to be used as sources."
  should_poll(self) L121
  "Device should be polled."
  name(self) L126
  "Return the name of the device."
  state(self) L134
  "Return the state of the device."
  supported_features(self) L150
  "Flag media player features that are supported."
  media_content_type(self) L155
  "Content type of current playing media."
  media_image_url(self) L167
  "Image url of current playing media."
  app_name(self) L183
  "Name of the current running app."
  app_id(self) L189
  "Return the ID of the current running app."
  source(self) L195
  "Return the current input source."
  source_list(self) L201
  "List of available input sources."
  media_play_pause(self) L205
  "Send play/pause command."
  media_previous_track(self) L210
  "Send previous track command."
  media_next_track(self) L215
  "Send next track command."
  mute_volume(self, mute) L220
  "Mute the volume."
  volume_up(self) L225
  "Volume up media player."
  volume_down(self) L230
  "Volume down media player."
  select_source(self, source) L235
  "Select input source."

setup_platform(hass, config, add_devices, discovery_info=None) L41
"Setup the Roku platform."

homeassistant/components/media_player/russound_rnet.py
class RussoundRNETDevice(MediaPlayerDevice) L74
  "Representation of a Russound RNET device."
  __init__(self, hass, russ, sources, zone_id, extra) L77
  "Initialise the Russound RNET device."
  update(self) L88
  "Retrieve latest state."
  name(self) L109
  "Return the name of the zone."
  state(self) L114
  "Return the state of the device."
  supported_features(self) L119
  "Flag media player features that are supported."
  source(self) L124
  "Get the currently selected source."
  volume_level(self) L129
  "Volume level of the media player (0..1)."
  set_volume_level(self, volume) L137
  "Set volume level."
  turn_on(self) L145
  "Turn the media player on."
  turn_off(self) L149
  "Turn off media player."
  mute_volume(self, mute) L153
  "Send mute command."
  select_source(self, source) L157
  "Set the input source."
  source_list(self) L165
  "List of available input sources."

setup_platform(hass, config, add_devices, discovery_info=None) L47
"Setup the Russound RNET platform."

homeassistant/components/media_player/samsungtv.py
class SamsungTVDevice(MediaPlayerDevice) L84
  "Representation of a Samsung TV."
  __init__(self, host, port, name, timeout, mac) L87
  "Initialize the Samsung device."
  update(self) L119
  "Retrieve the latest data."
  get_remote(self) L124
  "Create or return a remote control instance."
  send_key(self, key) L132
  "Send a key to the tv and handles exceptions."
  name(self) L152
  "Return the name of the device."
  state(self) L157
  "Return the state of the device."
  is_volume_muted(self) L162
  "Boolean if volume is currently muted."
  supported_features(self) L167
  "Flag media player features that are supported."
  turn_off(self) L173
  "Turn off media player."
  volume_up(self) L182
  "Volume up the media player."
  volume_down(self) L186
  "Volume down media player."
  mute_volume(self, mute) L190
  "Send mute command."
  media_play_pause(self) L194
  "Simulate play pause media player."
  media_play(self) L201
  "Send play command."
  media_pause(self) L206
  "Send media pause command to media player."
  media_next_track(self) L211
  "Send next track command."
  media_previous_track(self) L215
  "Send the previous track command."
  turn_on(self) L219
  "Turn the media player on."

setup_platform(hass, config, add_devices, discovery_info=None) L46
"Setup the Samsung TV platform."

homeassistant/components/media_player/snapcast.py
class SnapcastDevice(MediaPlayerDevice) L52
  "Representation of a Snapcast client device."
  __init__(self, client) L55
  "Initialize the Snapcast device."
  name(self) L60
  "Return the name of the device."
  volume_level(self) L65
  "Return the volume level."
  is_volume_muted(self) L70
  "Volume muted."
  supported_features(self) L75
  "Flag media player features that are supported."
  state(self) L80
  "Return the state of the player."
  source(self) L91
  "Return the current input source."
  source_list(self) L96
  "List of available input sources."
  mute_volume(self, mute) L100
  "Send the mute command."
  set_volume_level(self, volume) L104
  "Set the volume level."
  select_source(self, source) L108
  "Set input source."

setup_platform(hass, config, add_devices, discovery_info=None) L36
"Setup the Snapcast platform."

homeassistant/components/media_player/sonos.py
class _ProcessSonosEventQueue L206
  "Queue like object for dispatching sonos events."
  __init__(self, sonos_device) L209
  put(self, item, block=True, timeout=None) L212
  "Queue up event for processing."

class SonosDevice(MediaPlayerDevice) L253
  "Representation of a Sonos device."
  __init__(self, player) L256
  "Initialize the Sonos device."
  async_added_to_hass(self) L293
  "Subscribe sonos events."
  should_poll(self) L298
  "Polling needed."
  unique_id(self) L303
  "Return an unique ID."
  name(self) L308
  "Return the name of the device."
  state(self) L313
  "Return the state of the device."
  is_coordinator(self) L326
  "Return true if player is a coordinator."
  soco(self) L331
  "Return soco device."
  coordinator(self) L336
  "Return coordinator of this player."
  _is_available(self) L340
  _subscribe_to_player_events(self) L351
  update(self) L361
  "Retrieve latest state."
  _format_media_image_url(self, url, fallback_uri) L641
  process_sonos_event(self, event) L652
  "Process a service event coming from the speaker."
  volume_level(self) L701
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L706
  "Return true if volume is muted."
  media_content_id(self) L711
  "Content ID of current playing media."
  media_content_type(self) L719
  "Content type of current playing media."
  media_duration(self) L724
  "Duration of current playing media in seconds."
  media_position(self) L732
  "Position of current playing media in seconds."
  media_position_updated_at(self) L740
  "When was the position of the current playing media valid."
  media_image_url(self) L751
  "Image url of current playing media."
  media_artist(self) L759
  "Artist of current playing media, music track only."
  media_album_name(self) L767
  "Album name of current playing media, music track only."
  media_title(self) L775
  "Title of current playing media."
  supported_features(self) L783
  "Flag media player features that are supported."
  volume_up(self) L805
  "Volume up media player."
  volume_down(self) L810
  "Volume down media player."
  set_volume_level(self, volume) L815
  "Set volume level, range 0..1."
  mute_volume(self, mute) L820
  "Mute (true) or unmute (false) media player."
  select_source(self, source) L826
  "Select input source."
  source_list(self) L843
  "List of available input sources."
  source(self) L862
  "Name of the current input source."
  turn_off(self) L870
  "Turn off media player."
  media_play(self) L876
  "Send play command."
  media_stop(self) L882
  "Send stop command."
  media_pause(self) L888
  "Send pause command."
  media_next_track(self) L894
  "Send next track command."
  media_previous_track(self) L900
  "Send next track command."
  media_seek(self, position) L906
  "Send seek command."
  clear_playlist(self) L912
  "Clear players playlist."
  turn_on(self) L917
  "Turn the media player on."
  play_media(self, media_type, media_id, **kwargs) L923
  "Send the play_media command to the media player."
  join(self, master) L941
  "Join the player to a group."
  unjoin(self) L956
  "Unjoin the player from a group."
  snapshot(self, with_group=True) L962
  "Snapshot the player."
  restore(self, with_group=True) L977
  "Restore snapshot for the player."
  set_sleep_timer(self, sleep_time) L1028
  "Set the timer on the player."
  clear_sleep_timer(self) L1034
  "Clear the timer on the player."
  device_state_attributes(self) L1039
  "Return device specific state attributes."

setup_platform(hass, config, add_devices, discovery_info=None) L94
"Setup the Sonos platform."
service_handle(service) L145
"Internal func for applying a service."
_parse_timespan(timespan) L197
"Parse a time-span into number of seconds."
_get_entity_from_soco(hass, soco) L219
"Return SonosDevice from SoCo."
soco_error(funct) L227
"Decorator to catch soco exceptions."
wrapper(*args, **kwargs) L230
"Wrapper for all soco exception."
soco_coordinator(funct) L241
"Decorator to call funct on coordinator."
wrapper(device, *args, **kwargs) L244
"Wrapper for call to coordinator."

homeassistant/components/media_player/soundtouch.py
class SoundTouchDevice(MediaPlayerDevice) L155
  "Representation of a SoundTouch Bose device."
  __init__(self, name, config) L158
  "Create Soundtouch Entity."
  config(self) L171
  "Return specific soundtouch configuration."
  device(self) L176
  "Return Soundtouch device."
  update(self) L180
  "Retrieve the latest data."
  volume_level(self) L186
  "Volume level of the media player (0..1)."
  name(self) L191
  "Return the name of the device."
  state(self) L196
  "Return the state of the device."
  is_volume_muted(self) L204
  "Boolean if volume is currently muted."
  supported_features(self) L209
  "Flag media player features that are supported."
  turn_off(self) L213
  "Turn off media player."
  turn_on(self) L218
  "Turn on media player."
  volume_up(self) L223
  "Volume up the media player."
  volume_down(self) L228
  "Volume down media player."
  set_volume_level(self, volume) L233
  "Set volume level, range 0..1."
  mute_volume(self, mute) L238
  "Send mute command."
  media_play_pause(self) L243
  "Simulate play pause media player."
  media_play(self) L248
  "Send play command."
  media_pause(self) L253
  "Send media pause command to media player."
  media_next_track(self) L258
  "Send next track command."
  media_previous_track(self) L263
  "Send the previous track command."
  media_image_url(self) L269
  "Image url of current playing media."
  media_title(self) L274
  "Title of current playing media."
  media_duration(self) L284
  "Duration of current playing media in seconds."
  media_artist(self) L289
  "Artist of current playing media."
  media_track(self) L294
  "Artist of current playing media."
  media_album_name(self) L299
  "Album name of current playing media."
  play_media(self, media_type, media_id, **kwargs) L303
  "Play a piece of media."
  create_zone(self, slaves) L315
  "Create a zone (multi-room)  and play on selected devices."
  remove_zone_slave(self, slaves) L329
  "Remove slave(s) from and existing zone (multi-room)."
  add_zone_slave(self, slaves) L347
  "Add slave(s) to and existing zone (multi-room)."

setup_platform(hass, config, add_devices, discovery_info=None) L71
"Setup the Bose Soundtouch platform."
service_handle(service) L109
"Internal func for applying a service."

homeassistant/components/media_player/spotify.py
class SpotifyAuthCallbackView(HomeAssistantView) L98
  "Spotify Authorization Callback View."
  __init__(self, config, add_devices, oauth) L105
  "Initialize."
  get(self, request) L112
  "Receive authorization token."

class SpotifyMediaPlayer(MediaPlayerDevice) L119
  "Representation of a Spotify controller."
  __init__(self, oauth, name) L122
  "Initialize."
  refresh_spotify_instance(self) L138
  "Fetch a new spotify instance."
  update(self) L154
  "Update state and attributes."
  set_volume_level(self, volume) L189
  "Set the volume level."
  media_next_track(self) L193
  "Skip to next track."
  media_previous_track(self) L197
  "Skip to previous track."
  media_play(self) L201
  "Start or resume playback."
  media_pause(self) L205
  "Pause playback."
  select_source(self, source) L209
  "Select playback device."
  play_media(self, media_type, media_id, **kwargs) L213
  "Play media."
  name(self) L229
  "Name."
  icon(self) L234
  "Icon."
  state(self) L239
  "Playback state."
  volume_level(self) L244
  "Device volume."
  source_list(self) L249
  "Playback devices."
  source(self) L254
  "Current playback device."
  media_content_id(self) L259
  "Media URL."
  media_image_url(self) L264
  "Media image url."
  media_artist(self) L269
  "Media artist."
  media_album_name(self) L274
  "Media album."
  media_title(self) L279
  "Media title."
  supported_features(self) L284
  "Media player features that are supported."

request_configuration(hass, config, add_devices, oauth) L63
"Request Spotify authorization."
setup_platform(hass, config, add_devices, discovery_info=None) L74
"Setup the Spotify platform."

homeassistant/components/media_player/squeezebox.py
class LogitechMediaServer(object) L81
  "Representation of a Logitech media server."
  __init__(self, hass, host, port, username, password) L84
  "Initialize the Logitech device."
  create_players(self) L93
  "Create a list of devices connected to LMS."
  async_query(self, *command, player='') L106
  "Abstract out the JSON-RPC connection."

class SqueezeBoxDevice(MediaPlayerDevice) L147
  "Representation of a SqueezeBox device."
  __init__(self, lms, player_id, name) L150
  "Initialize the SqueezeBox device."
  name(self) L160
  "Return the name of the device."
  unique_id(self) L165
  "Return an unique ID."
  state(self) L170
  "Return the state of the device."
  async_query(self, *parameters) L183
  "Send a command to the LMS."
  async_update(self) L192
  "Retrieve the current state of the player."
  volume_level(self) L214
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L220
  "Return true if volume is muted."
  media_content_id(self) L226
  "Content ID of current playing media."
  media_content_type(self) L232
  "Content type of current playing media."
  media_duration(self) L237
  "Duration of current playing media in seconds."
  media_image_url(self) L243
  "Image url of current playing media."
  media_title(self) L271
  "Title of current playing media."
  media_artist(self) L280
  "Artist of current playing media."
  media_album_name(self) L286
  "Album of current playing media."
  supported_features(self) L292
  "Flag media player features that are supported."
  async_turn_off(self) L296
  "Turn off media player."
  async_volume_up(self) L303
  "Volume up media player."
  async_volume_down(self) L310
  "Volume down media player."
  async_set_volume_level(self, volume) L317
  "Set volume level, range 0..1."
  async_mute_volume(self, mute) L325
  "Mute (true) or unmute (false) media player."
  async_media_play_pause(self) L333
  "Send pause command to media player."
  async_media_play(self) L340
  "Send play command to media player."
  async_media_pause(self) L347
  "Send pause command to media player."
  async_media_next_track(self) L354
  "Send next track command."
  async_media_previous_track(self) L361
  "Send next track command."
  async_media_seek(self, position) L368
  "Send seek command."
  async_turn_on(self) L375
  "Turn the media player on."
  async_play_media(self, media_type, media_id, **kwargs) L382
  "Send the play_media command to the media player."
  _play_uri(self, media_id) L394
  "Replace the current play list with the uri."
  _add_uri_to_playlist(self, media_id) L398
  "Add a items to the existing playlist."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L46
"Setup the squeezebox platform."

homeassistant/components/media_player/universal.py
class UniversalMediaPlayer(MediaPlayerDevice) L139
  "Representation of an universal media player."
  __init__(self, hass, name, children, commands, attributes) L142
  "Initialize the Universal media device."
  async_on_dependency_update(*_) L152
  "Update ha state when dependencies update."
  _entity_lkp(self, entity_id, state_attr=None) L162
  "Look up an entity state."
  _override_or_child_attr(self, attr_name) L173
  "Return either the override or the active child for attr_name."
  _child_attr(self, attr_name) L181
  "Return the active child's attributes."
  _async_call_service(self, service_name, service_data=None, allow_override=False) L187
  "Call either a specified or active child's service."
  should_poll(self) L210
  "No polling needed."
  master_state(self) L215
  "Return the master state for entity or None."
  name(self) L225
  "Return the name of universal player."
  state(self) L230
  "Current state of media player."
  volume_level(self) L248
  "Volume level of entity specified in attributes or active child."
  is_volume_muted(self) L253
  "Boolean if volume is muted."
  media_content_id(self) L259
  "Content ID of current playing media."
  media_content_type(self) L264
  "Content type of current playing media."
  media_duration(self) L269
  "Duration of current playing media in seconds."
  media_image_url(self) L274
  "Image url of current playing media."
  entity_picture(self) L279
  "Return image of the media playing."
  media_title(self) L290
  "Title of current playing media."
  media_artist(self) L295
  "Artist of current playing media (Music track only)."
  media_album_name(self) L300
  "Album name of current playing media (Music track only)."
  media_album_artist(self) L305
  "Album artist of current playing media (Music track only)."
  media_track(self) L310
  "Track number of current playing media (Music track only)."
  media_series_title(self) L315
  "The title of the series of current playing media (TV Show only)."
  media_season(self) L320
  "Season of current playing media (TV Show only)."
  media_episode(self) L325
  "Episode of current playing media (TV Show only)."
  media_channel(self) L330
  "Channel currently playing."
  media_playlist(self) L335
  "Title of Playlist currently playing."
  app_id(self) L340
  "ID of the current running app."
  app_name(self) L345
  "Name of the current running app."
  source(self) L350
  ""Return the current input source of the device."
  source_list(self) L355
  "List of available input sources."
  supported_features(self) L360
  "Flag media player features that are supported."
  device_state_attributes(self) L389
  "Return device specific state attributes."
  media_position(self) L396
  "Position of current playing media in seconds."
  media_position_updated_at(self) L401
  "When was the position of the current playing media valid."
  async_turn_on(self) L405
  "Turn the media player on."
  async_turn_off(self) L412
  "Turn the media player off."
  async_mute_volume(self, is_volume_muted) L419
  "Mute the volume."
  async_set_volume_level(self, volume) L428
  "Set volume level, range 0..1."
  async_media_play(self) L437
  "Send play commmand."
  async_media_pause(self) L444
  "Send pause command."
  async_media_stop(self) L451
  "Send stop command."
  async_media_previous_track(self) L458
  "Send previous track command."
  async_media_next_track(self) L465
  "Send next track command."
  async_media_seek(self, position) L472
  "Send seek command."
  async_play_media(self, media_type, media_id, **kwargs) L480
  "Play a piece of media."
  async_volume_up(self) L489
  "Turn volume up for media player."
  async_volume_down(self) L496
  "Turn volume down for media player."
  async_media_play_pause(self) L504
  "Play or pause the media player."
  async_select_source(self, source) L511
  "Set the input source."
  async_clear_playlist(self) L520
  "Clear players playlist."
  async_update(self) L528
  "Update state in HA."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L53
"Setup the universal media players."
validate_config(config) L69
"Validate universal media player configuration."
validate_children(config) L95
"Validate children."
validate_commands(config) L109
"Validate commands."
validate_attributes(config) L121
"Validate attributes."

homeassistant/components/media_player/vlc.py
class VlcDevice(MediaPlayerDevice) L42
  "Representation of a vlc player."
  __init__(self, name, arguments) L45
  "Initialize the vlc device."
  update(self) L58
  "Get the latest details from the device."
  name(self) L80
  "Return the name of the device."
  state(self) L85
  "Return the state of the device."
  volume_level(self) L90
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L95
  "Boolean if volume is currently muted."
  supported_features(self) L100
  "Flag media player features that are supported."
  media_content_type(self) L105
  "Content type of current playing media."
  media_duration(self) L110
  "Duration of current playing media in seconds."
  media_position(self) L115
  "Position of current playing media in seconds."
  media_position_updated_at(self) L120
  "When was the position of the current playing media valid."
  media_seek(self, position) L124
  "Seek the media to a specific location."
  mute_volume(self, mute) L129
  "Mute the volume."
  set_volume_level(self, volume) L134
  "Set volume level, range 0..1."
  media_play(self) L139
  "Send play commmand."
  media_pause(self) L144
  "Send pause command."
  play_media(self, media_type, media_id, **kwargs) L149
  "Play media from a URL or file."

setup_platform(hass, config, add_devices, discovery_info=None) L36
"Setup the vlc platform."

homeassistant/components/media_player/volumio.py
class Volumio(MediaPlayerDevice) L53
  "Volumio Player Object."
  __init__(self, name, host, port, hass) L56
  "Initialize the media player."
  send_volumio_msg(self, method, params=None) L68
  "Send message."
  async_update(self) L96
  "Update state."
  media_content_type(self) L104
  "Content type of current playing media."
  state(self) L109
  "Return the state of the device."
  media_title(self) L120
  "Title of current playing media."
  media_artist(self) L125
  "Artist of current playing media (Music track only)."
  media_album_name(self) L130
  "Artist of current playing media (Music track only)."
  media_image_url(self) L135
  "Image url of current playing media."
  media_seek_position(self) L147
  "Time in seconds of current seek position."
  media_duration(self) L152
  "Time in seconds of current song duration."
  volume_level(self) L157
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L165
  "Boolean if volume is currently muted."
  name(self) L170
  "Return the name of the device."
  supported_features(self) L175
  "Flag of media commands that are supported."
  async_media_next_track(self) L179
  "Send media_next command to media player."
  async_media_previous_track(self) L183
  "Send media_previous command to media player."
  async_media_play(self) L187
  "Send media_play command to media player."
  async_media_pause(self) L191
  "Send media_pause command to media player."
  async_set_volume_level(self, volume) L195
  "Send volume_up command to media player."
  async_mute_volume(self, mute) L200
  "Send mute command to media player."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L44
"Set up the Volumio platform."

homeassistant/components/media_player/webostv.py
class LgWebOSDevice(MediaPlayerDevice) L147
  "Representation of a LG WebOS TV."
  __init__(self, host, mac, name, customize, config) L150
  "Initialize the webos device."
  update(self) L172
  "Retrieve the latest data."
  name(self) L206
  "Return the name of the device."
  state(self) L211
  "Return the state of the device."
  is_volume_muted(self) L216
  "Boolean if volume is currently muted."
  volume_level(self) L221
  "Volume level of the media player (0..1)."
  source(self) L226
  "Return the current input source."
  source_list(self) L231
  "List of available input sources."
  media_content_type(self) L236
  "Content type of current playing media."
  media_image_url(self) L241
  "Image url of current playing media."
  supported_features(self) L251
  "Flag media player features that are supported."
  turn_off(self) L257
  "Turn off media player."
  turn_on(self) L267
  "Turn on the media player."
  volume_up(self) L272
  "Volume up the media player."
  volume_down(self) L276
  "Volume down media player."
  set_volume_level(self, volume) L280
  "Set volume level, range 0..1."
  mute_volume(self, mute) L285
  "Send mute command."
  media_play_pause(self) L290
  "Simulate play pause media player."
  select_source(self, source) L297
  "Select input source."
  media_play(self) L303
  "Send play command."
  media_pause(self) L309
  "Send media pause command to media player."
  media_next_track(self) L315
  "Send next track command."
  media_previous_track(self) L319
  "Send the previous track command."

setup_platform(hass, config, add_devices, discovery_info=None) L64
"Setup the LG WebOS TV platform."
setup_tv(host, mac, name, customize, config, hass, add_devices) L86
"Setup a LG WebOS TV based on host parameter."
request_configuration(host, mac, name, customize, config, hass, add_devices) L123
"Request configuration steps from the user."
lgtv_configuration_callback(data) L135
"The actions to do when our configuration callback is called."

homeassistant/components/media_player/yamaha.py
class YamahaDevice(MediaPlayerDevice) L92
  "Representation of a Yamaha device."
  __init__(self, name, receiver, source_ignore, source_names) L95
  "Initialize the Yamaha Receiver."
  update(self) L113
  "Get the latest details from the device."
  build_source_list(self) L139
  "Build the source list."
  name(self) L150
  "Return the name of the device."
  state(self) L159
  "Return the state of the device."
  volume_level(self) L164
  "Volume level of the media player (0..1)."
  is_volume_muted(self) L169
  "Boolean if volume is currently muted."
  source(self) L174
  "Return the current input source."
  source_list(self) L179
  "List of available input sources."
  supported_features(self) L184
  "Flag media player features that are supported."
  turn_off(self) L199
  "Turn off media player."
  set_volume_level(self, volume) L203
  "Set volume level, range 0..1."
  mute_volume(self, mute) L209
  "Mute (true) or unmute (false) media player."
  turn_on(self) L213
  "Turn the media player on."
  media_play(self) L218
  "Send play commmand."
  media_pause(self) L222
  "Send pause command."
  media_stop(self) L226
  "Send stop command."
  media_previous_track(self) L230
  "Send previous track command."
  media_next_track(self) L234
  "Send next track command."
  _call_playback_function(self, function, function_text) L238
  select_source(self, source) L246
  "Select input source."
  play_media(self, media_type, media_id, **kwargs) L250
  "Play media from an ID."
  media_artist(self) L262
  "Artist of current playing media."
  media_album_name(self) L268
  "Album of current playing media."
  media_content_type(self) L274
  "Content type of current playing media."
  media_title(self) L282
  "Artist of current playing media."

setup_platform(hass, config, add_devices, discovery_info=None) L46
"Setup the Yamaha platform."

homeassistant/components/microsoft_face.py
class MicrosoftFaceGroupEntity(Entity) L264
  "Person-Group state/data Entity."
  __init__(self, hass, api, g_id, name) L267
  "Initialize person/group entity."
  name(self) L275
  "Return the name of the entity."
  entity_id(self) L280
  "Return entity id."
  state(self) L285
  "Return the state of the entity."
  should_poll(self) L290
  "Return True if entity has to be polled for state."
  device_state_attributes(self) L295
  "Return device specific state attributes."

class MicrosoftFace(object) L304
  "Microsoft Face api for HomeAssistant."
  __init__(self, hass, api_key, timeout, entities) L307
  "Initialize Microsoft Face api."
  store(self) L317
  "Store group/person data and IDs."
  update_store(self) L322
  "Load all group/person data into local store."
  call_api(self, method, function, data=None, binary=False, params=None) L345
  "Make a api call."

create_group(hass, name) L75
"Create a new person group."
delete_group(hass, name) L81
"Delete a person group."
train_group(hass, group) L87
"Train a person group."
create_person(hass, group, name) L93
"Create a person in a group."
delete_person(hass, group, name) L99
"Delete a person in a group."
face_person(hass, group, person, camera_entity) L105
"Add a new face picture to a person."
async_setup(hass, config) L113
"Setup microsoft face."
async_create_group(service) L137
"Create a new person group."
async_delete_group(service) L158
"Delete a person group."
async_train_group(service) L177
"Train a person group."
async_create_person(service) L193
"Create a person in a group."
async_delete_person(service) L214
"Delete a person in a group."
async_face_person(service) L235
"Add a new face picture to a person."

homeassistant/components/mochad.py
class MochadCtrl(object) L60
  "Mochad controller."
  __init__(self, host, port) L63
  "Initialize a PyMochad controller."
  host(self) L74
  "The server where mochad is running."
  port(self) L79
  "The port mochad is running on."
  disconnect(self) L83
  "Close the connection to the mochad socket."

setup(hass, config) L33
"Setup the mochad platform."
stop_mochad(event) L48
"Stop the Mochad service."
start_mochad(event) L52
"Start the Mochad service."

homeassistant/components/modbus.py
class ModbusHub(object) L134
  "Thread safe wrapper class for pymodbus."
  __init__(self, modbus_client) L137
  "Initialize the modbus hub."
  close(self) L142
  "Disconnect client."
  connect(self) L147
  "Connect client."
  read_coils(self, unit, address, count) L152
  "Read coils."
  read_input_registers(self, unit, address, count) L161
  "Read input registers."
  read_holding_registers(self, unit, address, count) L170
  "Read holding registers."
  write_coil(self, unit, address, value) L179
  "Write coil."
  write_register(self, unit, address, value) L188
  "Write register."
  write_registers(self, unit, address, values) L197
  "Write registers."

setup(hass, config) L69
"Setup Modbus component."
stop_modbus(event) L100
"Stop Modbus service."
start_modbus(event) L104
"Start Modbus service."
write_register(service) L113
"Write modbus registers."

homeassistant/components/mqtt/discovery.py
async_start(hass, discovery_topic, hass_config) L33
"Initialization of MQTT Discovery."
async_device_message_received(topic, payload, qos) L37
"Process the received message."

homeassistant/components/mqtt/server.py
async_start(hass, server_config) L32
"Initialize MQTT Server."
async_shutdown_mqtt_server(event) L56
"Shut down the MQTT server."
generate_config(hass, passwd) L66
"Generate a configuration based on current Home Assistant instance."

homeassistant/components/mqtt_eventstream.py
async_setup(hass, config) L41
"Setup the MQTT eventstream component."
_event_publisher(event) L49
"Handle events by publishing them on the MQTT queue."
_event_receiver(topic, payload, qos) L86
"Receive events published by and fire them on this hass instance."

homeassistant/components/mysensors.py
class GatewayWrapper(object) L304
  "Gateway wrapper class."
  __init__(self, gateway, optimistic, device) L307
  "Setup class attributes on instantiation."
  __getattr__(self, name) L328
  "See if this object has attribute name."
  __setattr__(self, name, value) L337
  "See if this object has attribute name then set to value."
  callback_factory(self) L346
  "Return a new callback function."
  node_update(msg) L348
  "Callback for node updates from the MySensors gateway."

class MySensorsDeviceEntity(object) L359
  "Represent a MySensors entity."
  __init__(self, gateway, node_id, child_id, name, value_type) L362
  "Set up MySensors device."
  should_poll(self) L374
  "Mysensor gateway pushes its state to HA."
  name(self) L379
  "The name of this entity."
  device_state_attributes(self) L384
  "Return device specific state attributes."
  available(self) L408
  "Return True if entity is available."
  update(self) L412
  "Update the controller with the latest value from a sensor."

is_socket_address(value) L52
"Validate that value is a valid address."
has_parent_dir(value) L61
"Validate that value is in an existing directory which is writetable."
has_all_unique_files(value) L71
"Validate that all persistence files are unique and set if any is set."
is_persistence_file(value) L85
"Validate that persistence file path ends in either .pickle or .json."
is_serial_port(value) L94
"Validate that value is a windows serial port or a unix device."
deprecated(key) L107
"Mark key as deprecated in config."
validator(config) L109
"Check if key is in config, log warning and remove key."
setup(hass, config) L150
"Setup the MySensors component."
setup_gateway(device, persistence_file, baud_rate, tcp_port, in_prefix, out_prefix) L157
"Return gateway after setup of the gateway."
pub_callback(topic, payload, qos, retain) L166
"Call mqtt publish function."
sub_callback(topic, callback, qos) L170
"Call mqtt subscribe function."
gw_start(event) L203
"Callback to trigger start of gateway and any persistence."
pf_callback_factory(map_sv_types, devices, entity_class, add_devices=None) L266
"Return a new callback for the platform."
mysensors_callback(gateway, msg) L268
"Callback for mysensors platform."

homeassistant/components/neato.py
class NeatoHub(object) L99
  "A My Neato hub wrapper class."
  __init__(self, hass, domain_config, neato) L102
  "Initialize the Neato hub."
  login(self) L114
  "Login to My Neato."
  update_robots(self) L126
  "Update the robot states."
  download_map(self, url) L133
  "Download a new map image."

setup(hass, config) L83
"Setup the Neato component."

homeassistant/components/nest.py
class NestDevice(object) L132
  "Structure Nest functions for hass."
  __init__(self, hass, conf, nest) L135
  "Init Nest Devices."
  thermostats(self) L146
  "Generator returning list of thermostats and their location."
  smoke_co_alarms(self) L160
  "Generator returning list of smoke co alarams."
  cameras(self) L174
  "Generator returning list of cameras."

request_configuration(nest, hass, config) L47
"Request configuration steps from the user."
nest_configuration_callback(data) L56
"The actions to do when our configuration callback is called."
setup_nest(hass, nest, config, pin=None) L74
"Setup Nest Devices."
setup(hass, config) L110
"Setup the Nest thermostat component."

homeassistant/components/netatmo.py
class CameraData(object) L69
  "Get the latest data from Netatmo."
  __init__(self, auth, home=None) L72
  "Initialize the data object."
  get_camera_names(self) L81
  "Return all camera available on the API as a list."
  get_module_names(self, camera_name) L94
  "Return all module available on the API as a list."
  get_camera_type(self, camera=None, home=None, cid=None) L105
  "Return all module available on the API as a list."
  update(self) L112
  "Call the Netatmo API to update the data."
  update_event(self) L118
  "Call the Netatmo API to update the events."

setup(hass, config) L46
"Set up the Netatmo devices."

homeassistant/components/notify/__init__.py
class BaseNotificationService(object) L177
  "An abstract class for notification services."
  send_message(self, message, **kwargs) L182
  "Send a message."
  async_send_message(self, message, **kwargs) L189
  "Send a message."

send_message(hass, message, title=None, data=None) L54
"Send a notification message."
async_setup(hass, config) L70
"Setup the notify services."
async_setup_platform(p_type, p_config=None, discovery_info=None) L79
"Set up a notify platform."
async_notify_message(service) L117
"Handle sending notification message service calls."
async_platform_discovered(platform, info) L168
"Callback to load a platform."

homeassistant/components/notify/apns.py
class ApnsDevice(object) L63
  "APNS Device class."
  __init__(self, push_id, name, tracking_device_id=None, disabled=False) L71
  "Initialize Apns Device."
  push_id(self) L79
  "The APNS id for the device."
  name(self) L84
  "The friendly name for the device."
  tracking_device_id(self) L89
  "Device Id."
  full_tracking_device_id(self) L99
  "Fully qualified device id."
  disabled(self) L109
  "Should receive notifications."
  disable(self) L113
  "Disable the device from recieving notifications."
  __eq__(self, other) L117
  "Return the comparision."
  __ne__(self, other) L123
  "Return the comparision."

class ApnsNotificationService(BaseNotificationService) L149
  "Implement the notification service for the APNS service."
  __init__(self, hass, app_name, topic, sandbox, cert_file) L152
  "Initialize APNS application."
  device_state_changed_listener(self, entity_id, from_s, to_s) L184
  "Listener for sate change."
  write_devices(self) L193
  "Write all known devices to file."
  register(self, call) L199
  "Register a device to receive push messages."
  send_message(self, message=None, **kwargs) L225
  "Send push message to registered devices."

get_service(hass, config, discovery_info=None) L46
"Return push service."
_write_device(out, device) L128
"Write a single device to file."

homeassistant/components/notify/aws_lambda.py
class AWSLambda(BaseNotificationService) L66
  "Implement the notification service for the AWS Lambda service."
  __init__(self, lambda_client, context) L69
  "Initialize the service."
  send_message(self, message='', **kwargs) L74
  "Send notification to specified LAMBDA ARN."

get_service(hass, config, discovery_info=None) L39
"Get the AWS Lambda notification service."

homeassistant/components/notify/aws_sns.py
class AWSSNS(BaseNotificationService) L57
  "Implement the notification service for the AWS SNS service."
  __init__(self, sns_client) L60
  "Initialize the service."
  send_message(self, message='', **kwargs) L64
  "Send notification to specified SNS ARN."

get_service(hass, config, discovery_info=None) L36
"Get the AWS SNS notification service."

homeassistant/components/notify/aws_sqs.py
class AWSSQS(BaseNotificationService) L56
  "Implement the notification service for the AWS SQS service."
  __init__(self, sqs_client) L59
  "Initialize the service."
  send_message(self, message='', **kwargs) L63
  "Send notification to specified SQS ARN."

get_service(hass, config, discovery_info=None) L35
"Get the AWS SQS notification service."

homeassistant/components/notify/ciscospark.py
class CiscoSparkNotificationService(BaseNotificationService) L36
  "CiscoSparkNotificationService."
  __init__(self, token, default_room) L39
  "Initialize the service."
  send_message(self, message='', **kwargs) L52
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L29
"Get the CiscoSpark notification service."

homeassistant/components/notify/command_line.py
class CommandLineNotificationService(BaseNotificationService) L32
  "Implement the notification service for the Command Line service."
  __init__(self, command) L35
  "Initialize the service."
  send_message(self, message='', **kwargs) L39
  "Send a message to a command line."

get_service(hass, config, discovery_info=None) L25
"Get the Command Line notification service."

homeassistant/components/notify/demo.py
class DemoNotificationService(BaseNotificationService) L17
  "Implement demo notification service."
  __init__(self, hass) L20
  "Initialize the service."
  targets(self) L25
  "Return a dictionary of registered targets."
  send_message(self, message='', **kwargs) L29
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L12
"Get the demo notification service."

homeassistant/components/notify/discord.py
class DiscordNotificationService(BaseNotificationService) L31
  "Implement the notification service for Discord."
  __init__(self, hass, token) L34
  "Initialize the service."
  async_send_message(self, message, **kwargs) L40
  "Login to Discord, send message to channel(s) and log out."
  on_ready() L47
  "Send the messages when the bot is ready."

get_service(hass, config, discovery_info=None) L25
"Get the Discord notification service."

homeassistant/components/notify/ecobee.py
class EcobeeNotificationService(BaseNotificationService) L33
  "Implement the notification service for the Ecobee thermostat."
  __init__(self, thermostat_index) L36
  "Initialize the service."
  send_message(self, message='', **kwargs) L40
  "Send a message to a command line."

get_service(hass, config, discovery_info=None) L27
"Get the Ecobee notification service."

homeassistant/components/notify/facebook.py
class FacebookNotificationService(BaseNotificationService) L33
  "Implementation of a notification service for the Facebook service."
  __init__(self, access_token) L36
  "Initialize the service."
  send_message(self, message='', **kwargs) L40
  "Send some message."

get_service(hass, config, discovery_info=None) L28
"Get the Facebook notification service."

homeassistant/components/notify/file.py
class FileNotificationService(BaseNotificationService) L36
  "Implement the notification service for the File service."
  __init__(self, hass, filename, add_timestamp) L39
  "Initialize the service."
  send_message(self, message='', **kwargs) L44
  "Send a message to a file."

get_service(hass, config, discovery_info=None) L28
"Get the file notification service."

homeassistant/components/notify/free_mobile.py
class FreeSMSNotificationService(BaseNotificationService) L32
  "Implement a notification service for the Free Mobile SMS service."
  __init__(self, username, access_token) L35
  "Initialize the service."
  send_message(self, message='', **kwargs) L40
  "Send a message to the Free Mobile user cell."

get_service(hass, config, discovery_info=None) L26
"Get the Free Mobile SMS notification service."

homeassistant/components/notify/gntp.py
class GNTPNotificationService(BaseNotificationService) L58
  "Implement the notification service for GNTP."
  __init__(self, app_name, app_icon, hostname, password, port) L61
  "Initialize the service."
  send_message(self, message='', **kwargs) L79
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L42
"Get the GNTP notification service."

homeassistant/components/notify/group.py
class GroupNotifyPlatform(BaseNotificationService) L50
  "Implement the notification service for the group notify platform."
  __init__(self, hass, entities) L53
  "Initialize the service."
  async_send_message(self, message='', **kwargs) L59
  "Send message to all entities in the group."

update(input_dict, update_source) L30
"Deep update a dictionary."
async_get_service(hass, config, discovery_info=None) L45
"Get the Group notification service."

homeassistant/components/notify/html5.py
class JSONBytesDecoder(json.JSONEncoder) L142
  "JSONEncoder to decode bytes objects to unicode."
  default(self, obj) L146
  "Decode object if it's a bytes object, else defer to baseclass."

class HTML5PushRegistrationView(HomeAssistantView) L165
  "Accepts push registrations from a browser."
  __init__(self, registrations, json_path) L171
  "Init HTML5PushRegistrationView."
  post(self, request) L177
  "Accept the POST request for push registrations from a browser."
  delete(self, request) L202
  "Delete a registration."

class HTML5PushCallbackView(HomeAssistantView) L232
  "Accepts push registrations from a browser."
  __init__(self, registrations) L239
  "Init HTML5PushCallbackView."
  decode_jwt(self, token) L243
  "Find the registration that signed this JWT and return it."
  check_authorization_header(self, request) L266
  "Check the authorization header."
  post(self, request) L294
  "Accept the POST request for push registrations event callback."

class HTML5NotificationService(BaseNotificationService) L330
  "Implement the notification service for HTML5."
  __init__(self, gcm_key, registrations) L333
  "Initialize the service."
  targets(self) L339
  "Return a dictionary of registered targets."
  send_message(self, message='', **kwargs) L346
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L100
"Get the HTML5 push notification service."
_load_config(filename) L123
"Load configuration."
_save_config(filename, config) L153
"Save configuration."

homeassistant/components/notify/instapush.py
class InstapushNotificationService(BaseNotificationService) L67
  "Implementation of the notification service for Instapush."
  __init__(self, api_key, app_secret, event, tracker) L70
  "Initialize the service."
  send_message(self, message='', **kwargs) L82
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L39
"Get the Instapush notification service."

homeassistant/components/notify/ios.py
class iOSNotificationService(BaseNotificationService) L58
  "Implement the notification service for iOS."
  __init__(self) L61
  "Initialize the service."
  targets(self) L65
  "Return a dictionary of registered targets."
  send_message(self, message='', **kwargs) L69
  "Send a message to the Lambda APNS gateway."

log_rate_limits(target, resp, level=20) L27
"Output rate limit log line at given level."
get_service(hass, config, discovery_info=None) L42
"Get the iOS notification service."

homeassistant/components/notify/joaoapps_join.py
class JoinNotificationService(BaseNotificationService) L42
  "Implement the notification service for Join."
  __init__(self, device_id, api_key=None) L45
  "Initialize the service."
  send_message(self, message='', **kwargs) L50
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L30
"Get the Join notification service."

homeassistant/components/notify/kodi.py
class KodiNotificationService(BaseNotificationService) L71
  "Implement the notification service for Kodi."
  __init__(self, hass, url, auth=None) L74
  "Initialize the service."
  async_send_message(self, message='', **kwargs) L90
  "Send a message to Kodi."

async_get_service(hass, config, discovery_info=None) L42
"Return the notify service."

homeassistant/components/notify/lannouncer.py
class LannouncerNotificationService(BaseNotificationService) L40
  "Implementation of a notification service for Lannouncer."
  __init__(self, hass, host, port) L43
  "Initialize the service."
  send_message(self, message='', **kwargs) L49
  "Send a message to Lannouncer."

get_service(hass, config, discovery_info=None) L32
"Get the Lannouncer notification service."

homeassistant/components/notify/llamalab_automate.py
class AutomateNotificationService(BaseNotificationService) L38
  "Implement the notification service for LlamaLab Automate."
  __init__(self, secret, recipient, device=None) L41
  "Initialize the service."
  send_message(self, message='', **kwargs) L47
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L29
"Get the LlamaLab Automate notification service."

homeassistant/components/notify/mailgun.py
class MailgunNotificationService(BaseNotificationService) L54
  "Implement a notification service for the Mailgun mail service."
  __init__(self, domain, sandbox, token, sender, recipient) L57
  "Initialize the service."
  initialize_client(self) L66
  "Initialize the connection to Mailgun."
  connection_is_valid(self) L75
  "Check whether the provided credentials are valid."
  send_message(self, message='', **kwargs) L88
  "Send a mail to the recipient."

get_service(hass, config, discovery_info=None) L42
"Get the Mailgun notification service."

homeassistant/components/notify/matrix.py
class MatrixNotificationService(BaseNotificationService) L54
  "Send Notifications to a Matrix Room."
  __init__(self, config_file, homeserver, default_room, verify_ssl, username, password) L57
  "Setup the client."
  get_auth_tokens(self) L75
  "Read sorted authentication tokens from disk."
  store_auth_token(self, token) L100
  "Store authentication token to session and persistent storage."
  login(self) L115
  "Login to the matrix homeserver and return the client instance."
  login_by_token(self) L152
  "Login using authentication token and return the client."
  login_by_password(self) L162
  "Login using password authentication and return the client."
  send_message(self, message, **kwargs) L176
  "Send the message to the matrix server."

get_service(hass, config, discovery_info=None) L37
"Get the Matrix notification service."

homeassistant/components/notify/message_bird.py
class MessageBirdNotificationService(BaseNotificationService) L43
  "Implement the notification service for MessageBird."
  __init__(self, sender, client) L46
  "Initialize the service."
  send_message(self, message=None, **kwargs) L51
  "Send a message to a specified target."

get_service(hass, config, discovery_info=None) L28
"Get the MessageBird notification service."

homeassistant/components/notify/mysensors.py
class MySensorsNotificationDevice(mysensors.MySensorsDeviceEntity) L37
  "Represent a MySensors Notification device."
  send_msg(self, msg) L40
  "Send a message."

class MySensorsNotificationService(BaseNotificationService) L48
  "Implement MySensors notification service."
  __init__(self, platform_devices) L53
  "Initialize the service."
  send_message(self, message='', **kwargs) L57
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L12
"Get the MySensors notification service."

homeassistant/components/notify/nfandroidtv.py
class NFAndroidTVNotificationService(BaseNotificationService) L100
  "Notification service for Notifications for Android TV."
  __init__(self, remoteip, duration, position, transparency, color, interrupt, timeout) L103
  "Initialize the service."
  send_message(self, message='', **kwargs) L117
  "Send a message to a Android TV device."

get_service(hass, config, discovery_info=None) L86
"Get the Notifications for Android TV notification service."

homeassistant/components/notify/nma.py
class NmaNotificationService(BaseNotificationService) L43
  "Implement the notification service for NMA."
  __init__(self, api_key) L46
  "Initialize the service."
  send_message(self, message='', **kwargs) L50
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L27
"Get the NMA notification service."

homeassistant/components/notify/pushbullet.py
class PushBulletNotificationService(BaseNotificationService) L44
  "Implement the notification service for Pushbullet."
  __init__(self, pb) L47
  "Initialize the service."
  refresh(self) L53
  "Refresh devices, contacts, etc."
  send_message(self, message=None, **kwargs) L72
  "Send a message to a specified target."

get_service(hass, config, discovery_info=None) L28
"Get the Pushbullet notification service."

homeassistant/components/notify/pushetta.py
class PushettaNotificationService(BaseNotificationService) L40
  "Implement the notification service for Pushetta."
  __init__(self, api_key, channel_name, send_test_msg) L43
  "Initialize the service."
  send_message(self, message='', **kwargs) L54
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L30
"Get the Pushetta notification service."

homeassistant/components/notify/pushover.py
class PushoverNotificationService(BaseNotificationService) L43
  "Implement the notification service for Pushover."
  __init__(self, user_key, api_token) L46
  "Initialize the service."
  send_message(self, message='', **kwargs) L54
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L30
"Get the Pushover notification service."

homeassistant/components/notify/pushsafer.py
class PushsaferNotificationService(BaseNotificationService) L33
  "Implementation of the notification service for Pushsafer.com."
  __init__(self, private_key) L36
  "Initialize the service."
  send_message(self, message='', **kwargs) L40
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L28
"Get the Pushsafer.com notification service."

homeassistant/components/notify/rest.py
class RestNotificationService(BaseNotificationService) L55
  "Implementation of a notification service for REST."
  __init__(self, resource, method, message_param_name, title_param_name, target_param_name) L58
  "Initialize the service."
  send_message(self, message='', **kwargs) L67
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L42
"Get the RESTful notification service."

homeassistant/components/notify/sendgrid.py
class SendgridNotificationService(BaseNotificationService) L37
  "Implementation the notification service for email via Sendgrid."
  __init__(self, api_key, sender, recipient) L40
  "Initialize the service."
  send_message(self, message='', **kwargs) L50
  "Send an email to a user via SendGrid."

get_service(hass, config, discovery_info=None) L28
"Get the SendGrid notification service."

homeassistant/components/notify/simplepush.py
class SimplePushNotificationService(BaseNotificationService) L33
  "Implementation of the notification service for SimplePush."
  __init__(self, device_key) L36
  "Initialize the service."
  send_message(self, message='', **kwargs) L40
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L28
"Get the Simplepush notification service."

homeassistant/components/notify/slack.py
class SlackNotificationService(BaseNotificationService) L48
  "Implement the notification service for Slack."
  __init__(self, default_channel, api_token, username, icon) L51
  "Initialize the service."
  send_message(self, message='', **kwargs) L66
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L32
"Get the Slack notification service."

homeassistant/components/notify/smtp.py
class MailNotificationService(BaseNotificationService) L73
  "Implement the notification service for E-Mail messages."
  __init__(self, server, port, timeout, sender, starttls, username, password, recipient, debug) L76
  "Initialize the service."
  connect(self) L90
  "Connect/authenticate to SMTP Server."
  connection_is_valid(self) L102
  "Check for valid config, verify connectivity."
  send_message(self, message='', **kwargs) L126
  "Build and send a message to a user."
  _send_email(self, msg) L150
  "Send the message."

get_service(hass, config, discovery_info=None) L54
"Get the mail notification service."
_build_text_msg(message) L167
"Build plaintext email."
_build_multipart_msg(message, images) L173
"Build Multipart message with in-line images."

homeassistant/components/notify/syslog.py
class SyslogNotificationService(BaseNotificationService) L81
  "Implement the syslog notification service."
  __init__(self, facility, option, priority) L84
  "Initialize the service."
  send_message(self, message='', **kwargs) L90
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L70
"Get the syslog notification service."

homeassistant/components/notify/telegram.py
class TelegramNotificationService(BaseNotificationService) L81
  "Implement the notification service for Telegram."
  __init__(self, api_key, chat_id) L84
  "Initialize the service."
  send_message(self, message='', **kwargs) L92
  "Send a message to a user."
  send_keyboard(self, message, keys) L131
  "Display keyboard."
  send_photo(self, data) L143
  "Send a photo."
  send_document(self, data) L161
  "Send a document."
  send_location(self, gps) L179
  "Send a location."

get_service(hass, config, discovery_info=None) L41
"Get the Telegram notification service."
load_data(url=None, file=None, username=None, password=None) L58
"Load photo/document into ByteIO/File container from a source."

homeassistant/components/notify/telstra.py
class TelstraNotificationService(BaseNotificationService) L44
  "Implementation of a notification service for the Telstra SMS API."
  __init__(self, consumer_key, consumer_secret, phone_number) L47
  "Initialize the service."
  send_message(self, message='', **kwargs) L53
  "Send a message to a user."

get_service(hass, config, discovery_info=None) L30
"Get the Telstra SMS API notification service."
_authenticate(consumer_key, consumer_secret) L88
"Authenticate with the Telstra API."

homeassistant/components/notify/twilio_call.py
class TwilioCallNotificationService(BaseNotificationService) L35
  "Implement the notification service for the Twilio Call service."
  __init__(self, twilio_client, from_number) L38
  "Initialize the service."
  send_message(self, message='', **kwargs) L43
  "Call to specified target users."

get_service(hass, config, discovery_info=None) L29
"Get the Twilio Call notification service."

homeassistant/components/notify/twilio_sms.py
class TwilioSMSNotificationService(BaseNotificationService) L34
  "Implement the notification service for the Twilio SMS service."
  __init__(self, twilio_client, from_number) L37
  "Initialize the service."
  send_message(self, message='', **kwargs) L42
  "Send SMS to specified target user cell."

get_service(hass, config, discovery_info=None) L28
"Get the Twilio SMS notification service."

homeassistant/components/notify/twitter.py
class TwitterNotificationService(BaseNotificationService) L42
  "Implementation of a notification service for the Twitter service."
  __init__(self, consumer_key, consumer_secret, access_token_key, access_token_secret, username) L45
  "Initialize the service."
  send_message(self, message='', **kwargs) L53
  "Tweet a message."

get_service(hass, config, discovery_info=None) L33
"Get the Twitter notification service."

homeassistant/components/notify/webostv.py
class LgWebOSNotificationService(BaseNotificationService) L54
  "Implement the notification service for LG WebOS TV."
  __init__(self, client, icon_path) L57
  "Initialize the service."
  send_message(self, message='', **kwargs) L62
  "Send a message to the tv."

get_service(hass, config, discovery_info=None) L33
"Return the notify service."

homeassistant/components/notify/xmpp.py
class XmppNotificationService(BaseNotificationService) L42
  "Implement the notification service for Jabber (XMPP)."
  __init__(self, sender, password, recipient, tls) L45
  "Initialize the service."
  send_message(self, message='', **kwargs) L52
  "Send a message to a user."

class SendNotificationBot(sleekxmpp.ClientXMPP) L65
  "Service for sending Jabber (XMPP) messages."
  __init__(self) L68
  "Initialize the Jabber Bot."
  start(self, event) L81
  "Start the communication and sends the message."
  check_credentials(self, event) L88
  ""Disconnect from the server if credentials are invalid."

get_service(hass, config, discovery_info=None) L33
"Get the Jabber (XMPP) notification service."
send_message(sender, password, recipient, use_tls, message) L61
"Send a message over XMPP."

homeassistant/components/nuimo_controller.py
class NuimoLogger(object) L55
  "Handle Nuimo Controller event callbacks."
  __init__(self, hass, name) L58
  "Initialize Logger object."
  received_gesture_event(self, event) L63
  "Input Event received."

class NuimoThread(threading.Thread) L72
  "Manage one Nuimo controller."
  __init__(self, hass, mac, name) L75
  "Initialize thread object."
  run(self) L85
  "Setup connection or be idle."
  stop(self, event) L99
  "Terminate Thread by unsetting flag."
  _attach(self) L104
  "Create a nuimo object from mac address or discovery."
  _connect(self) L127
  "Build up connection and set event delegator and service."
  handle_write_matrix(call) L143
  "Handle led matrix service."

class DiscoveryLogger(object) L171
  "Handle Nuimo Discovery callbacks."
  discovery_started(self) L175
  "Discovery startet."
  discovery_finished(self) L180
  "Discovery finished."
  controller_added(self, nuimo) L185
  "Controller found."

setup(hass, config) L46
"Setup the Nuimo component."

homeassistant/components/octoprint.py
class OctoPrintAPI(object) L47
  "Simple JSON wrapper for OctoPrint's API."
  __init__(self, api_url, key) L50
  "Initialize OctoPrint API and set headers needed later."
  get_tools(self) L58
  "Get the dynamic list of tools that temperature is monitored on."
  get(self, endpoint) L63
  "Send a get request, and return the response as a dict."
  update(self, sensor_type, end_point, group, tool=None) L94
  "Return the value for sensor_type from the provided endpoint."

setup(hass, config) L30
"Set up OctoPrint API."
get_value_from_json(json_dict, sensor_type, group, tool) L104
"Return the value for sensor_type from the JSON."

homeassistant/components/panel_custom.py
setup(hass, config) L43
"Initialize custom panel."

homeassistant/components/panel_iframe.py
setup(hass, config) L24
"Setup iframe frontend panels."

homeassistant/components/persistent_notification.py
create(hass, message, title=None, notification_id=None) L39
"Generate a notification."
async_create(hass, message, title=None, notification_id=None) L45
"Generate a notification."
async_setup(hass, config) L59
"Setup the persistent notification component."
create_service(call) L62
"Handle a create notification service call."

homeassistant/components/pilight.py
class CallRateDelayThrottle(object) L121
  "Helper class to provide service call rate throttling."
  __init__(self, hass, delay_seconds) L133
  "Initialize the delay handler."
  limited(self, method) L142
  "Decorator to delay calls on a certain method."
  decorated(*args, **kwargs) L145
  "The decorated function."
  action(event) L151
  "The action wrapper that gets scheduled."

setup(hass, config) L55
"Setup the Pilight component."
start_pilight_client(_) L71
"Called once when Home Assistant starts."
stop_pilight_client(_) L77
"Called once when Home Assistant stops."
send_code(call) L84
"Send RF code to the pilight-daemon."
handle_received_code(data) L102
"Called when RF codes are received."

homeassistant/components/proximity.py
class Proximity(Entity) L90
  "Representation of a Proximity."
  __init__(self, hass, zone_friendly_name, dist_to, dir_of_travel, nearest, ignored_zones, proximity_devices, tolerance, proximity_zone, unit_of_measurement) L93
  "Initialize the proximity."
  name(self) L109
  "Return the name of the entity."
  state(self) L114
  "Return the state."
  unit_of_measurement(self) L119
  "Return the unit of measurement of this entity."
  state_attributes(self) L124
  "Return the state attributes."
  check_proximity_state_change(self, entity, old_state, new_state) L131
  "Function to perform the proximity checking."

setup_proximity_component(hass, name, config) L58
"Set up individual proximity component."
setup(hass, config) L82
"Get the zones and offsets from configuration.yaml."

homeassistant/components/qwikswitch.py
class QSToggleEntity(object) L41
  "Representation of a Qwikswitch Entity."
  __init__(self, qsitem, qsusb) L56
  "Initialize the ToggleEntity."
  brightness(self) L67
  "Return the brightness of this light between 0..100."
  should_poll(self) L73
  "No polling needed."
  name(self) L78
  "Return the name of the light."
  is_on(self) L83
  "Check if device is on (non-zero)."
  update_value(self, value) L87
  "Decode the QSUSB value and update the Home assistant state."
  turn_on(self, **kwargs) L95
  "Turn the device on."
  turn_off(self, **kwargs) L104
  "Turn the device off."

class QSSwitch(QSToggleEntity, SwitchDevice) L110
  "Switch based on a Qwikswitch relay module."

class QSLight(QSToggleEntity, Light) L116
  "Light based on a Qwikswitch relay/dimmer module."
  supported_features(self) L120
  "Flag supported features."

setup(hass, config) L125
"Setup the QSUSB component."
_stop(event) L141
"Stop the listener queue and clean up."
qs_callback(item) L171
"Typically a button press or update signal."
_start(event) L191
"Start listening."

homeassistant/components/recorder/__init__.py
class Recorder(threading.Thread) L124
  "A threaded recorder class."
  __init__(self, hass, purge_days, uri, include, exclude) L127
  "Initialize the recorder."
  async_initialize(self) L149
  "Initialize the recorder."
  run(self) L153
  "Start processing events to save."
  connection_failed() L177
  "Connection failed tasks."
  register() L193
  "Post connection initialize."
  shutdown(event) L197
  "Shut down the Recorder."
  notify_hass_started(event) L211
  "Notify that hass has started."
  do_purge(now) L220
  "Event listener for purging data."
  event_listener(self, event) L281
  "Listen for new events and put them in the process queue."
  block_till_done(self) L285
  "Block till all events processed."
  _setup_connection(self) L289
  "Ensure database is ready to fly."
  set_sqlite_pragma(dbapi_connection, connection_record) L302
  "Set sqlite's WAL mode."
  _close_connection(self) L328
  "Close the connection."
  _setup_run(self) L334
  "Log the start of the current run."
  _close_run(self) L354
  "Save end time for current run."

wait_connection_ready(hass) L72
"Wait till the connection is ready."
run_information(hass, point_in_time=None) L81
"Return information about current run."
async_setup(hass, config) L103
"Setup the recorder."

homeassistant/components/recorder/migration.py
migrate_schema(instance) L9
"Check if the schema needs to be upgraded."
_create_index(engine, table_name, index_name) L39
"Create an index for the specified table."
_apply_update(engine, new_version) L57
"Perform operations to bring schema up to date."
_inspect_schema_version(engine, session) L72
"Determine the schema version by inspecting the db structure."

homeassistant/components/recorder/models.py
class Events(Base) L24
  "Event history data."
  from_event(event) L36
  "Create an event database object from a native event."
  to_native(self) L43
  "Convert to a natve HA Event."

class States(Base) L58
  "State change history."
  from_event(event) L81
  "Create object from a state_changed event."
  to_native(self) L105
  "Convert to an HA state object."

class RecorderRuns(Base) L120
  "Representation of recorder run."
  entity_ids(self, point_in_time=None) L132
  "Return the entity ids that existed in this run."
  to_native(self) L154
  "Return self, native format is this model."

class SchemaChanges(Base) L159
  "Representation of schema version changes."

_process_timestamp(ts) L168
"Process a timestamp into datetime object."

homeassistant/components/recorder/purge.py
purge_old_data(instance, purge_days) L12
"Purge events and states older than purge_days ago."

homeassistant/components/recorder/util.py
session_scope(*, hass=None, session=None) L15
"Provide a transactional scope around a series of operations."
commit(session, work) L34
"Commit & retry work: Either a model or in a function."
execute(qry) L52
"Query the database and convert the objects to HA native form."

homeassistant/components/remote/__init__.py
class RemoteDevice(ToggleEntity) L144
  "Representation of a remote."
  send_command(self, **kwargs) L147
  "Send a command to a device."
  async_send_command(self, **kwargs) L151
  "Send a command to a device."

is_on(hass, entity_id=None) L57
"Return if the remote is on based on the statemachine."
turn_on(hass, activity=None, entity_id=None) L63
"Turn all or specified remote on."
turn_off(hass, entity_id=None) L71
"Turn all or specified remote off."
send_command(hass, device, command, entity_id=None) L77
"Send a command to a device."
async_setup(hass, config) L86
"Track states and offer events for remotes."
async_handle_remote_service(service) L93
"Handle calls to the remote services."

homeassistant/components/remote/demo.py
class DemoRemote(RemoteDevice) L20
  "Representation of a demo remote."
  __init__(self, name, state, icon) L23
  "Initialize the Demo Remote."
  should_poll(self) L30
  "No polling needed for a demo remote."
  name(self) L35
  "Return the name of the device if any."
  icon(self) L40
  "Return the icon to use for device if any."
  is_on(self) L45
  "Return true if remote is on."
  turn_on(self, **kwargs) L49
  "Turn the remote on."
  turn_off(self, **kwargs) L54
  "Turn the remote off."

setup_platform(hass, config, add_devices_callback, discovery_info=None) L12
"Setup the demo remotes."

homeassistant/components/remote/harmony.py
class HarmonyRemote(remote.RemoteDevice) L102
  "Remote representation used to control a Harmony device."
  __init__(self, name, host, port, activity, out_path, token) L105
  "Initialize HarmonyRemote class."
  name(self) L127
  "Return the Harmony device's name."
  device_state_attributes(self) L132
  "Add platform specific attributes."
  is_on(self) L137
  "Return False if PowerOff is the current activity, otherwise True."
  update(self) L141
  "Return current activity."
  turn_on(self, **kwargs) L152
  "Start an activity from the Harmony device."
  turn_off(self) L167
  "Start the PowerOff activity."
  send_command(self, **kwargs) L172
  "Send a command to one device."
  sync(self) L179
  "Sync the Harmony device with the web service."

setup_platform(hass, config, add_devices, discovery_info=None) L43
"Set up the Harmony platform."
register_services(hass) L73
"Register all services for harmony devices."
_apply_service(service, service_func, *service_func_args) L83
"Internal func for applying a service."
_sync_service(service) L98

homeassistant/components/remote/itach.py
class ITachIP2IRRemote(remote.RemoteDevice) L74
  "Device that sends commands to an ITachIP2IR device."
  __init__(self, itachip2ir, name) L77
  "Initialize device."
  name(self) L84
  "Return the name of the device."
  is_on(self) L89
  "Return true if device is on."
  turn_on(self, **kwargs) L93
  "Turn the device on."
  turn_off(self) L99
  "Turn the device off."
  send_command(self, **kwargs) L105
  "Send a command to one device."
  update(self) L109
  "Update the device."

setup_platform(hass, config, add_devices, discovery_info=None) L49
"Set up the ITach connection and devices."

homeassistant/components/rest_command.py
async_setup(hass, config) L56
"Setup the rest_command component."
async_register_rest_command(name, command_config) L60
"Create service for rest command."
async_service_handler(service) L85
"Execute a shell command service."

homeassistant/components/rflink.py
class RflinkDevice(Entity) L214
  "Representation of a Rflink device."
  __init__(self, device_id, hass, name=None, aliasses=None, group=True, group_aliasses=None, nogroup_aliasses=None, fire_event=False, signal_repetitions=DEFAULT_SIGNAL_REPETITIONS) L223
  "Initialize the device."
  handle_event(self, event) L239
  "Handle incoming event for device type."
  _handle_event(self, event) L257
  "Platform specific event handler."
  should_poll(self) L262
  "No polling needed."
  name(self) L267
  "Return a name for the device."
  is_on(self) L272
  "Return true if device is on."
  assumed_state(self) L279
  "Assume device state until first device event sets state."

class RflinkCommand(RflinkDevice) L284
  "Singleton class to make Rflink command interface available to entities."
  set_rflink_protocol(cls, protocol, wait_ack=None) L302
  "Set the Rflink asyncio protocol as a class variable."
  is_connected(cls) L309
  "Return connection status."
  _async_handle_command(self, command, *args) L314
  "Do bookkeeping for command, send it to rflink and update state."
  cancel_queued_send_commands(self) L345
  "Cancel queued signal repetition commands."
  _async_send_command(self, cmd, repetitions) L357
  "Send a command for device to Rflink gateway."

class SwitchableRflinkDevice(RflinkCommand) L382
  "Rflink entity which can switch on/off (eg: light, switch)."
  _handle_event(self, event) L385
  "Adjust state if Rflink picks up a remote command for this device."
  async_turn_on(self, **kwargs) L395
  "Turn the device on."
  async_turn_off(self, **kwargs) L399
  "Turn the device off."

identify_event_type(event) L79
"Look at event to determine type of device."
async_setup(hass, config) L93
"Set up the Rflink component."
event_callback(event) L113
"Handle incoming Rflink events."
reconnect(exc=None) L158
"Schedule reconnect after connection has been unexpectedly lost."
connect() L169
"Set up connection and hook it into HA for reconnect/shutdown."

homeassistant/components/rfxtrx.py
class RfxtrxDevice(Entity) L289
  "Represents a Rfxtrx device."
  __init__(self, name, event, datas, signal_repetitions) L295
  "Initialize the device."
  should_poll(self) L305
  "No polling needed for a RFXtrx switch."
  name(self) L310
  "Return the name of the device if any."
  should_fire_event(self) L315
  "Return is the device must fire event."
  is_on(self) L320
  "Return true if device is on."
  assumed_state(self) L325
  "Return true if unable to access real state of entity."
  turn_off(self, **kwargs) L329
  "Turn the device off."
  update_state(self, state, brightness=0) L333
  "Update det state of the device."
  _send_command(self, command, brightness=0) L339

_valid_device(value, device_type) L54
"Validate a dictionary of devices definitions."
valid_sensor(value) L90
"Validate sensor configuration."
_valid_light_switch(value) L95
setup(hass, config) L128
"Setup the RFXtrx component."
handle_receive(event) L131
"Callback all subscribers for RFXtrx gateway."
_shutdown_rfxtrx(event) L164
"Close connection with RFXtrx."
get_rfx_object(packetid) L173
"Return the RFXObject with the packetid."
get_devices_from_config(config, device) L194
"Read rfxtrx configuration."
get_new_device(event, config, device) L217
"Add entity if not exist and the automatic_add is True."
apply_received_command(event) L242
"Apply command from rfxtrx."

homeassistant/components/ring.py
setup(hass, config) L37
"Set up Ring component."

homeassistant/components/rpi_gpio.py
setup(hass, config) L21
"Setup the Raspberry PI GPIO component."
cleanup_gpio(event) L25
"Stuff to do before stopping."
prepare_gpio(event) L29
"Stuff to do when home assistant starts."
setup_output(port) L38
"Setup a GPIO as output."
setup_input(port, pull_mode) L44
"Setup a GPIO as input."
write_output(port, value) L51
"Write a value to a GPIO."
read_input(port) L57
"Read a value from a GPIO."
edge_detect(port, event_callback, bounce) L63
"Add detection for RISING and FALLING events."

homeassistant/components/rss_feed_template.py
class RssView(HomeAssistantView) L63
  "Export states and other values as RSS."
  __init__(self, url, requires_auth, title, items) L72
  "Initialize the rss view."
  get(self, request, entity_id=None) L80
  "Generate the rss view XML."

setup(hass, config) L39
"Setup the RSS feeds."

homeassistant/components/scene/__init__.py
class Scene(Entity) L93
  "A scene is a group of entities and the states we want them to be."
  should_poll(self) L97
  "No polling needed."
  state(self) L102
  "Return the state of the scene."
  activate(self) L106
  "Activate scene."
  async_activate(self) L110
  "Activate scene."

_hass_domain_validator(config) L25
"Validate platform in config for homeassistant domain."
_platform_validator(config) L34
"Validate it is a valid  platform."
activate(hass, entity_id=None) L59
"Activate a scene."
async_setup(hass, config) L70
"Setup scenes."
async_handle_scene_service(service) L78
"Handle calls to the switch services."

homeassistant/components/scene/homeassistant.py
class HomeAssistantScene(Scene) L79
  "A scene is a group of entities and the states we want them to be."
  __init__(self, hass, scene_config) L82
  "Initialize the scene."
  name(self) L88
  "Return the name of the scene."
  device_state_attributes(self) L93
  "Return the scene state attributes."
  async_activate(self) L100
  "Activate scene."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L39
"Setup home assistant scene entries."
_process_config(scene_config) L48
"Process passed in config into a format to work with."

homeassistant/components/scene/hunterdouglas_powerview.py
class PowerViewScene(Scene) L42
  "Representation of a Powerview scene."
  __init__(self, hass, scene_data, room_data, pv_instance) L45
  "Initialize the scene."
  _sync_room_data(self, room_data) L56
  "Sync the room data."
  name(self) L64
  "Return the name of the scene."
  device_state_attributes(self) L69
  "Return the state attributes."
  icon(self) L74
  "Icon to use in the frontend."
  activate(self) L78
  "Activate the scene."

setup_platform(hass, config, add_devices, discovery_info=None) L22
"Setup the powerview scenes stored in a Powerview hub."

homeassistant/components/scene/lifx_cloud.py
class LifxCloudScene(Scene) L70
  "Representation of a LIFX Cloud scene."
  __init__(self, hass, headers, timeout, scene_data) L73
  "Initialize the scene."
  name(self) L82
  "Return the name of the scene."
  async_activate(self) L87
  "Activate the scene."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L34
"Setup the scenes stored in the LIFX Cloud."

homeassistant/components/scene/litejet.py
class LiteJetScene(Scene) L31
  "Representation of a single LiteJet scene."
  __init__(self, lj, i, name) L34
  "Initialize the scene."
  name(self) L41
  "Return the name of the scene."
  should_poll(self) L46
  "Return that polling is not necessary."
  device_state_attributes(self) L51
  "Return the device-specific state attributes."
  activate(self, **kwargs) L57
  "Activate the scene."

setup_platform(hass, config, add_devices, discovery_info=None) L19
"Set up scenes for the LiteJet platform."

homeassistant/components/scene/wink.py
class WinkScene(WinkDevice, Scene) L26
  "Representation of a Wink shortcut/scene."
  __init__(self, wink, hass) L29
  "Initialize the Wink device."
  is_on(self) L34
  "Python-wink will always return False."
  activate(self, **kwargs) L38
  "Activate the scene."

setup_platform(hass, config, add_devices, discovery_info=None) L16
"Setup the Wink platform."

homeassistant/components/script.py
class ScriptEntity(ToggleEntity) L159
  "Representation of a script entity."
  __init__(self, hass, object_id, name, sequence) L162
  "Initialize the script."
  should_poll(self) L169
  "No polling needed."
  name(self) L174
  "Return the name of the entity."
  state_attributes(self) L179
  "Return the state attributes."
  is_on(self) L190
  "Return true if script is on."
  async_turn_on(self, **kwargs) L195
  "Turn the script on."
  async_turn_off(self, **kwargs) L200
  "Turn script off."
  async_remove(self) L204
  "Remove script from HASS."

is_on(hass, entity_id) L55
"Return if the script is on based on the statemachine."
reload(hass) L60
"Reload script component."
turn_on(hass, entity_id, variables=None) L65
"Turn script on."
turn_off(hass, entity_id) L72
"Turn script on."
toggle(hass, entity_id) L77
"Toggle the script."
async_setup(hass, config) L83
"Load the scripts from the configuration."
reload_service(service) L91
"Call a service to reload scripts."
turn_on_service(service) L100
"Call a service to turn script on."
turn_off_service(service) L109
"Cancel a script."
toggle_service(service) L117
"Toggle a script."
_async_process_config(hass, config, component) L135
"Process group configuration."
service_handler(service) L138
"Execute a service call to script.<script name>."

homeassistant/components/scsgate.py
class SCSGate(object) L63
  "The class  for dealing with the SCSGate device via scsgate.Reactor."
  __init__(self, device, logger) L66
  "Initialize the SCSGate."
  handle_message(self, message) L83
  "Method called whenever a message is seen on the bus."
  devices(self) L116
  "Dictionary with known devices."
  add_device(self, device) L123
  "Add the specified device."
  add_devices_to_register(self, devices) L132
  "List of devices to be registered."
  _activate_next_device(self) L139
  "Start the activation of the first device."
  is_device_registered(self, device_id) L150
  "Check whether a device is already registered or not."
  start(self) L162
  "Start the scsgate.Reactor."
  stop(self) L166
  "Stop the scsgate.Reactor."
  append_task(self, task) L170
  "Register a new task to be executed."

setup(hass, config) L40
"Setup the SCSGate component."
stop_monitor(event) L53
"Stop the SCSGate."

homeassistant/components/sensor/__init__.py
async_setup(hass, config) L21
"Track states and offer events for sensors."

homeassistant/components/sensor/alarmdecoder.py
class AlarmDecoderSensor(Entity) L33
  "Representation of an AlarmDecoder keypad."
  __init__(self, hass) L36
  "Initialize the alarm panel."
  async_added_to_hass(self) L46
  "Register callbacks."
  _message_callback(self, message) L52
  icon(self) L58
  "Return the icon if any."
  state(self) L63
  "Return the overall state."
  name(self) L68
  "Return the name of the device."
  should_poll(self) L73
  "No polling needed."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L24
"Perform the setup for AlarmDecoder sensor devices."

homeassistant/components/sensor/amcrest.py
class AmcrestSensor(Entity) L81
  "A sensor implementation for Amcrest IP camera."
  __init__(self, device_info, camera, sensor_type) L84
  "Initialize a sensor for Amcrest camera."
  name(self) L96
  "Return the name of the sensor."
  state(self) L101
  "Return the state of the sensor."
  device_state_attributes(self) L106
  "Return the state attributes."
  icon(self) L111
  "Icon to use in the frontend, if any."
  unit_of_measurement(self) L116
  "Return the units of measurement."
  update(self) L120
  "Get the latest data and updates the state."

setup_platform(hass, config, add_devices, discovery_info=None) L51
"Set up a sensor for an Amcrest IP Camera."

homeassistant/components/sensor/android_ip_webcam.py
class IPWebcamSensor(AndroidIPCamEntity) L35
  "Representation of a IP Webcam sensor."
  __init__(self, name, host, ipcam, sensor) L38
  "Initialize the sensor."
  name(self) L49
  "Return the name of the sensor, if any."
  unit_of_measurement(self) L54
  "Return the unit the value is expressed in."
  state(self) L59
  "Return the state of the sensor."
  async_update(self) L64
  "Retrieve latest state."
  icon(self) L75
  "Return the icon for the sensor."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L17
"Setup the IP Webcam Sensor."

homeassistant/components/sensor/apcupsd.py
class APCUPSdSensor(Entity) L141
  "Representation of a sensor entity for APCUPSd status values."
  __init__(self, data, sensor_type) L144
  "Initialize the sensor."
  name(self) L154
  "Return the name of the UPS sensor."
  icon(self) L159
  "Icon to use in the frontend, if any."
  state(self) L164
  "Return true if the UPS is online, else False."
  unit_of_measurement(self) L169
  "Return the unit of measurement of this entity, if any."
  update(self) L175
  "Get the latest status and use it to update our sensor state."

setup_platform(hass, config, add_entities, discovery_info=None) L107
"Setup the APCUPSd sensors."
infer_unit(value) L128
"If the value ends with any of the units from ALL_UNITS."

homeassistant/components/sensor/api_streams.py
class StreamHandler(logging.Handler) L18
  "Check log messages for stream connect/disconnect."
  __init__(self, entity) L21
  "Initialize handler."
  handle(self, record) L27
  "Handle a log message."

class APICount(Entity) L71
  "Entity to represent how many people are connected to stream API."
  __init__(self) L74
  "Initialize the API count."
  name(self) L79
  "Return name of entity."
  state(self) L84
  "Return current API count."
  unit_of_measurement(self) L89
  "Unit of measurement."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L52
"Set up the API stream platform."
remove_logger(event) L61
"Remove our handlers."

homeassistant/components/sensor/arduino.py
class ArduinoSensor(Entity) L52
  "Representation of an Arduino Sensor."
  __init__(self, name, pin, pin_type) L55
  "Initialize the sensor."
  state(self) L66
  "Return the state of the sensor."
  name(self) L71
  "Get the name of the sensor."
  update(self) L75
  "Get the latest value from the pin."

setup_platform(hass, config, add_devices, discovery_info=None) L37
"Set up the Arduino platform."

homeassistant/components/sensor/arest.py
class ArestSensor(Entity) L108
  "Implementation of an aREST sensor for exposed variables."
  __init__(self, arest, resource, location, name, variable=None, pin=None, unit_of_measurement=None, renderer=None) L111
  "Initialize the sensor."
  name(self) L131
  "Return the name of the sensor."
  unit_of_measurement(self) L136
  "Return the unit the value is expressed in."
  state(self) L141
  "Return the state of the sensor."
  update(self) L152
  "Get the latest data from aREST API."
  available(self) L157
  "Could the device be accessed during the last update call."

class ArestData(object) L162
  "The Class for handling the data retrieval for variables."
  __init__(self, resource, pin=None) L165
  "Initialize the data object."
  update(self) L173
  "Get the latest data from aREST device."

setup_platform(hass, config, add_devices, discovery_info=None) L47
"Set up the aREST sensor."
make_renderer(value_template) L65
"Create a renderer based on variable_template value."
_render(value) L72

homeassistant/components/sensor/arwn.py
class ArwnSensor(Entity) L102
  "Representation of an ARWN sensor."
  __init__(self, name, state_key, units, icon=None) L105
  "Initialize the sensor."
  set_event(self, event) L115
  "Update the sensor with the most recent event."
  state(self) L122
  "Return the state of the device."
  name(self) L127
  "Get the name of the sensor."
  state_attributes(self) L132
  "Return all the state attributes."
  unit_of_measurement(self) L137
  "Unit this state is expressed in."
  should_poll(self) L142
  "Should we poll."
  icon(self) L147
  "Icon of device based on its type."

discover_sensors(topic, payload) L26
"Given a topic, dynamically create the right sensor type."
_slug(name) L50
async_setup_platform(hass, config, async_add_devices, discovery_info=None) L55
"Set up the ARWN platform."
async_sensor_event_received(topic, payload, qos) L58
"Process events as sensors."

homeassistant/components/sensor/bbox.py
class BboxSensor(Entity) L71
  "Implementation of a Bbox sensor."
  __init__(self, bbox_data, sensor_type, name) L74
  "Initialize the sensor."
  name(self) L87
  "Return the name of the sensor."
  state(self) L92
  "Return the state of the sensor."
  unit_of_measurement(self) L97
  "Return the unit of measurement of this entity, if any."
  icon(self) L102
  "Icon to use in the frontend, if any."
  device_state_attributes(self) L107
  "Return the state attributes."
  update(self) L113
  "Get the latest data from Bbox and update the state."

class BboxData(object) L130
  "Get data from the Bbox."
  __init__(self) L133
  "Initialize the data object."
  update(self) L138
  "Get the latest data from the Bbox."

setup_platform(hass, config, add_devices, discovery_info=None) L51
"Set up the Bbox sensor."

homeassistant/components/sensor/bitcoin.py
class BitcoinSensor(Entity) L81
  "Representation of a Bitcoin sensor."
  __init__(self, data, option_type, currency) L84
  "Initialize the sensor."
  name(self) L95
  "Return the name of the sensor."
  state(self) L100
  "Return the state of the sensor."
  unit_of_measurement(self) L105
  "Return the unit the value is expressed in."
  icon(self) L110
  "Return the icon to use in the frontend, if any."
  device_state_attributes(self) L115
  "Return the state attributes of the sensor."
  update(self) L121
  "Get the latest data and updates the states."

class BitcoinData(object) L176
  "Get the latest data and update the states."
  __init__(self) L179
  "Initialize the data object."
  update(self) L185
  "Get the latest data from blockchain.info."

setup_platform(hass, config, add_devices, discovery_info=None) L62
"Set up the Bitcoin sensors."

homeassistant/components/sensor/blink.py
class BlinkSensor(Entity) L41
  "A Blink camera sensor."
  __init__(self, name, sensor_type, index, data) L44
  "A method to initialize sensors from Blink camera."
  name(self) L55
  "A method to return the name of the camera."
  state(self) L60
  "A camera's current state."
  unique_id(self) L65
  "A unique camera sensor identifier."
  unit_of_measurement(self) L70
  "A method to determine the unit of measurement for temperature."
  update(self) L74
  "A method to retrieve sensor data from the camera."

setup_platform(hass, config, add_devices, discovery_info=None) L24
"Set up a Blink sensor."

homeassistant/components/sensor/bloomsky.py
class BloomSkySensor(Entity) L57
  "Representation of a single sensor in a BloomSky device."
  __init__(self, bs, device, sensor_name) L60
  "Initialize a BloomSky sensor."
  name(self) L70
  "The name of the BloomSky device and this sensor."
  unique_id(self) L75
  "Return the unique ID for this sensor."
  state(self) L80
  "The current state, eg."
  unit_of_measurement(self) L85
  "Return the sensor units."
  update(self) L89
  "Request an update from the BloomSky API."

setup_platform(hass, config, add_devices, discovery_info=None) L46
"Setup the available BloomSky weather sensors."

homeassistant/components/sensor/bom.py
class BOMCurrentSensor(Entity) L128
  "Implementation of a BOM current sensor."
  __init__(self, rest, condition, stationname) L131
  "Initialize the sensor."
  name(self) L138
  "Return the name of the sensor."
  state(self) L147
  "Return the state of the sensor."
  device_state_attributes(self) L155
  "Return the state attributes of the device."
  unit_of_measurement(self) L168
  "Return the units of measurement."
  update(self) L172
  "Update current conditions."

class BOMCurrentData(object) L177
  "Get data from BOM."
  __init__(self, hass, station_id) L180
  "Initialize the data object."
  _build_url(self) L187
  update(self) L193
  "Get the latest data from BOM."

validate_station(station) L78
"Check that the station ID is well-formed."
setup_platform(hass, config, add_devices, discovery_info=None) L98
"Set up the BOM sensor."
_get_bom_stations() L215
"Return {CONF_STATION: (lat, lon)} for all stations, for auto-config."
bom_stations(cache_dir) L252
"Return {CONF_STATION: (lat, lon)} for all stations, for auto-config."
closest_station(lat, lon, cache_dir) L268
"Return the ZONE_ID.WMO_ID of the closest station to our lat/lon."
comparable_dist(wmo_id) L274
"A fast key function for psudeo-distance from lat/lon."

homeassistant/components/sensor/broadlink.py
class BroadlinkSensor(Entity) L69
  "Representation of a Broadlink device sensor."
  __init__(self, name, broadlink_data, sensor_type) L72
  "Initialize the sensor."
  name(self) L82
  "Return the name of the sensor."
  state(self) L87
  "Return the state of the sensor."
  unit_of_measurement(self) L92
  "Return the unit this state is expressed in."
  update(self) L96
  "Get the latest data from the sensor."

class BroadlinkData(object) L104
  "Representation of a Broadlink data object."
  __init__(self, interval, ip_addr, mac_addr, timeout) L107
  "Initialize the data object."
  _update(self, retry=3) L124
  _auth(self, retry=3) L139

setup_platform(hass, config, add_devices, discovery_info=None) L51
"Setup the Broadlink device sensors."

homeassistant/components/sensor/cert_expiry.py
class SSLCertificate(Entity) L44
  "Implementation of the certificate expiry sensor."
  __init__(self, sensor_name, server_name, server_port) L47
  "Initialize the sensor."
  name(self) L55
  "Return the name of the sensor."
  unit_of_measurement(self) L60
  "Return the unit this state is expressed in."
  state(self) L65
  "Return the state of the sensor."
  icon(self) L70
  "Icon to use in the frontend, if any."
  update(self) L74
  "Fetch the certificate information."

setup_platform(hass, config, add_devices, discovery_info=None) L35
"Set up certificate expiry sensor."

homeassistant/components/sensor/coinmarketcap.py
class CoinMarketCapSensor(Entity) L62
  "Representation of a CoinMarketCap sensor."
  __init__(self, data) L65
  "Initialize the sensor."
  name(self) L73
  "Return the name of the sensor."
  state(self) L78
  "Return the state of the sensor."
  unit_of_measurement(self) L83
  "Return the unit the value is expressed in."
  icon(self) L88
  "Return the icon to use in the frontend, if any."
  device_state_attributes(self) L93
  "Return the state attributes of the sensor."
  update(self) L106
  "Get the latest data and updates the states."

class CoinMarketCapData(object) L113
  "Get the latest data and update the states."
  __init__(self, currency) L116
  "Initialize the data object."
  update(self) L122
  "Get the latest data from blockchain.info."

setup_platform(hass, config, add_devices, discovery_info=None) L48
"Set up the CoinMarketCap sensor."

homeassistant/components/sensor/comed_hourly_pricing.py
class ComedHourlyPricingSensor(Entity) L60
  "Implementation of a ComEd Hourly Pricing sensor."
  __init__(self, sensor_type, offset, name) L63
  "Initialize the sensor."
  name(self) L75
  "Return the name of the sensor."
  state(self) L80
  "Return the state of the sensor."
  unit_of_measurement(self) L85
  "Return the unit of measurement of this entity, if any."
  device_state_attributes(self) L90
  "Return the state attributes."
  update(self) L96
  "Get the ComEd Hourly Pricing data from the web service."

setup_platform(hass, config, add_devices, discovery_info=None) L49
"Setup the ComEd Hourly Pricing sensor."

homeassistant/components/sensor/command_line.py
class CommandSensor(Entity) L48
  "Representation of a sensor that is using shell commands."
  __init__(self, hass, data, name, unit_of_measurement, value_template) L51
  "Initialize the sensor."
  name(self) L62
  "Return the name of the sensor."
  unit_of_measurement(self) L67
  "Return the unit the value is expressed in."
  state(self) L72
  "Return the state of the device."
  update(self) L76
  "Get the latest data and updates the state."

class CommandSensorData(object) L90
  "The class for handling the data retrieval."
  __init__(self, command) L93
  "Initialize the data object."
  update(self) L98
  "Get the latest data with a shell command."

setup_platform(hass, config, add_devices, discovery_info=None) L35
"Setup the Command Sensor."

homeassistant/components/sensor/cpuspeed.py
class CpuSpeedSensor(Entity) L40
  "Representation of a CPU sensor."
  __init__(self, name) L43
  "Initialize the sensor."
  name(self) L51
  "Return the name of the sensor."
  state(self) L56
  "Return the state of the sensor."
  unit_of_measurement(self) L61
  "Return the unit the value is expressed in."
  device_state_attributes(self) L66
  "Return the state attributes."
  icon(self) L76
  "Return the icon to use in the frontend, if any."
  update(self) L80
  "Get the latest data and updates the state."

setup_platform(hass, config, add_devices, discovery_info=None) L33
"Set up the CPU speed sensor."

homeassistant/components/sensor/crimereports.py
class CrimeReportsSensor(Entity) L55
  "Crime Reports Sensor."
  __init__(self, hass, name, latitude, longitude, radius, include, exclude) L58
  "Initialize the sensor."
  name(self) L74
  "Return the name of the sensor."
  state(self) L79
  "Return the state of the sensor."
  device_state_attributes(self) L84
  "Return the state attributes."
  _incident_event(self, incident) L88
  update(self) L102
  "Update device state."

setup_platform(hass, config, add_devices, discovery_info=None) L44
"Setup the Crime Reports platform."

homeassistant/components/sensor/cups.py
class CupsSensor(Entity) L78
  "Representation of a CUPS sensor."
  __init__(self, data, printer) L81
  "Initialize the CUPS sensor."
  name(self) L89
  "Return the name of the sensor."
  state(self) L94
  "Return the state of the sensor."
  icon(self) L104
  "Return the icon to use in the frontend, if any."
  device_state_attributes(self) L109
  "Return the state attributes of the sensor."
  update(self) L127
  "Get the latest data and updates the states."

class CupsData(object) L134
  "Get the latest data from CUPS and update the state."
  __init__(self, host, port) L137
  "Initialize the data object."
  update(self) L144
  "Get the latest data from CUPS."

setup_platform(hass, config, add_devices, discovery_info=None) L54
"Set up the CUPS sensor."

homeassistant/components/sensor/currencylayer.py
class CurrencylayerSensor(Entity) L63
  "Implementing the Currencylayer sensor."
  __init__(self, rest, base, quote) L66
  "Initialize the sensor."
  name(self) L74
  "Return the name of the sensor."
  icon(self) L79
  "Return the icon to use in the frontend, if any."
  state(self) L84
  "Return the state of the sensor."
  device_state_attributes(self) L89
  "Return the state attributes of the sensor."
  update(self) L95
  "Update current date."

class CurrencylayerData(object) L104
  "Get data from Currencylayer.org."
  __init__(self, resource, parameters) L107
  "Initialize the data object."
  update(self) L114
  "Get the latest data from Currencylayer."

setup_platform(hass, config, add_devices, discovery_info=None) L40
"Set up the Currencylayer sensor."

homeassistant/components/sensor/darksky.py
class DarkSkySensor(Entity) L172
  "Implementation of a Dark Sky sensor."
  __init__(self, forecast_data, sensor_type, name, forecast_day=0) L175
  "Initialize the sensor."
  name(self) L187
  "Return the name of the sensor."
  state(self) L196
  "Return the state of the sensor."
  unit_of_measurement(self) L201
  "Return the unit of measurement of this entity, if any."
  unit_system(self) L206
  "Return the unit system of this entity."
  entity_picture(self) L211
  "Return the entity picture to use in the frontend, if any."
  update_unit_of_measurement(self) L221
  "Update units based on unit system."
  icon(self) L233
  "Icon to use in the frontend, if any."
  device_state_attributes(self) L238
  "Return the state attributes."
  update(self) L244
  "Get the latest data from Dark Sky and updates the states."
  get_state(self, data) L286
  "Helper function that returns a new state based on the type."

class DarkSkyData(object) L324
  "Get the latest data from Darksky."
  __init__(self, api_key, latitude, longitude, units, interval) L327
  "Initialize the data object."
  _update(self) L348
  "Get the latest data from Dark Sky."
  _update_currently(self) L360
  "Update currently data."
  _update_minutely(self) L364
  "Update minutely data."
  _update_hourly(self) L368
  "Update hourly data."
  _update_daily(self) L372
  "Update daily data."

setup_platform(hass, config, add_devices, discovery_info=None) L132
"Setup the Dark Sky sensor."
convert_to_camel(data) L314
"Convert snake case (foo_bar_bat) to camel case (fooBarBat)."

homeassistant/components/sensor/demo.py
class DemoSensor(Entity) L20
  "Representation of a Demo sensor."
  __init__(self, name, state, unit_of_measurement, battery) L23
  "Initialize the sensor."
  should_poll(self) L31
  "No polling needed for a demo sensor."
  name(self) L36
  "Return the name of the sensor."
  state(self) L41
  "Return the state of the sensor."
  unit_of_measurement(self) L46
  "Return the unit this state is expressed in."
  device_state_attributes(self) L51
  "Return the state attributes."

setup_platform(hass, config, add_devices, discovery_info=None) L12
"Setup the Demo sensors."

homeassistant/components/sensor/deutsche_bahn.py
class DeutscheBahnSensor(Entity) L43
  "Implementation of a Deutsche Bahn sensor."
  __init__(self, start, goal) L46
  "Initialize the sensor."
  name(self) L53
  "Return the name of the sensor."
  icon(self) L58
  "Return the icon for the frontend."
  state(self) L63
  "Return the departure time of the next train."
  device_state_attributes(self) L68
  "Return the state attributes."
  update(self) L75
  "Get the latest delay from bahn.de and updates the state."

class SchieneData(object) L83
  "Pull data from the bahn.de web page."
  __init__(self, start, goal) L86
  "Initialize the sensor."
  update(self) L96
  "Update the connection data."

setup_platform(hass, config, add_devices, discovery_info=None) L35
"Setup the Deutsche Bahn Sensor."

homeassistant/components/sensor/dht.py
class DHTSensor(Entity) L83
  "Implementation of the DHT sensor."
  __init__(self, dht_client, sensor_type, temp_unit, name) L86
  "Initialize the sensor."
  name(self) L98
  "Return the name of the sensor."
  state(self) L103
  "Return the state of the sensor."
  unit_of_measurement(self) L108
  "Return the unit of measurement of this entity, if any."
  update(self) L112
  "Get the latest data from the DHT and updates the states."

class DHTClient(object) L129
  "Get the latest data from the DHT sensor."
  __init__(self, adafruit_dht, sensor, pin) L132
  "Initialize the sensor."
  update(self) L140
  "Get the latest data the DHT sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L51
"Setup the DHT sensor."

homeassistant/components/sensor/dnsip.py
class WanIpSensor(Entity) L56
  "Implementation of a DNS IP sensor."
  __init__(self, hass, hostname, resolver, ipv6) L59
  "Initialize the sensor."
  name(self) L70
  "Return the name of the sensor."
  state(self) L75
  "Return the current DNS IP address for hostname."
  async_update(self) L80
  "Get the current DNS IP address for hostname."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L43
"Setup the DNS IP sensor."

homeassistant/components/sensor/dovado.py
class Dovado L62
  "A connection to the router."
  __init__(self) L65
  "Initialize."
  setup(self, hass, config, add_devices) L70
  "Setup the connection."
  send_sms(service) L82
  "Send SMS through the router."
  name(self) L102
  "Name of the router."
  update(self) L107
  "Update device state."

class DovadoSensor(Entity) L122
  "Representation of a Dovado sensor."
  __init__(self, dovado, sensor) L125
  "Initialize the sensor."
  _compute_state(self) L131
  update(self) L148
  "Update sensor values."
  name(self) L154
  "Return the name of the sensor."
  state(self) L160
  "Return the sensor state."
  icon(self) L165
  "Return the icon for the sensor."
  unit_of_measurement(self) L170
  "Return the unit of measurement."
  device_state_attributes(self) L175
  "Return the state attributes."

setup_platform(hass, config, add_devices, discovery_info=None) L57
"Setup the dovado platform for sensors."

homeassistant/components/sensor/dsmr.py
class DSMREntity(Entity) L173
  "Entity reading values from DSMR telegram."
  __init__(self, name, obis) L176
  ""Initialize entity."
  get_dsmr_object_attr(self, attribute) L182
  "Read attribute from last received telegram for this DSMR object."
  name(self) L193
  "Return the name of the sensor."
  icon(self) L198
  "Icon to use in the frontend, if any."
  state(self) L206
  "Return the state of sensor, if available, translate if needed."
  unit_of_measurement(self) L221
  "Return the unit of measurement of this entity, if any."
  translate_tariff(value) L226
  "Convert 2/1 to normal/low."

class DerivativeDSMREntity(DSMREntity) L238
  "Calculated derivative for values where the DSMR doesn't offer one."
  state(self) L252
  "Return the calculated current hourly rate."
  async_update(self) L257
  "Recalculate hourly rate if timestamp has changed."
  unit_of_measurement(self) L284
  "Return the unit of measurement of this entity, per hour, if any."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L69
"Set up the DSMR sensor."
update_entities_telegram(telegram) L109
"Update entities with latests telegram & trigger state update."
connect_and_reconnect() L133
"Connect to DSMR and keep reconnecting until HA stops."

homeassistant/components/sensor/dte_energy_bridge.py
class DteEnergyBridgeSensor(Entity) L38
  "Implementation of a DTE Energy Bridge sensor."
  __init__(self, ip_address, name) L41
  "Initialize the sensor."
  name(self) L49
  "Return the name of th sensor."
  state(self) L54
  "Return the state of the sensor."
  unit_of_measurement(self) L59
  "Return the unit of measurement of this entity, if any."
  icon(self) L64
  "Icon to use in the frontend, if any."
  update(self) L68
  "Get the energy usage data from the DTE energy bridge."

setup_platform(hass, config, add_devices, discovery_info=None) L30
"Setup the DTE energy bridge sensor."

homeassistant/components/sensor/dublin_bus_transport.py
class DublinPublicTransportSensor(Entity) L70
  "Implementation of an Dublin public transport sensor."
  __init__(self, data, stop, route, name) L73
  "Initialize the sensor."
  name(self) L82
  "Return the name of the sensor."
  state(self) L87
  "Return the state of the sensor."
  device_state_attributes(self) L92
  "Return the state attributes."
  unit_of_measurement(self) L110
  "Return the unit this state is expressed in."
  icon(self) L115
  "Icon to use in the frontend, if any."
  update(self) L119
  "Get the latest data from opendata.ch and update the states."

class PublicTransportData(object) L129
  "The Class for handling the data retrieval."
  __init__(self, stop, route) L132
  "Initialize the data object."
  update(self) L141
  "Get the latest data from opendata.ch."

due_in_minutes(timestamp) L49
"Get the time in minutes from a timestamp."
setup_platform(hass, config, add_devices, discovery_info=None) L60
"Get the Dublin public transport sensor."

homeassistant/components/sensor/dweet.py
class DweetSensor(Entity) L63
  "Representation of a Dweet sensor."
  __init__(self, hass, dweet, name, value_template, unit_of_measurement) L66
  "Initialize the sensor."
  name(self) L77
  "Return the name of the sensor."
  unit_of_measurement(self) L82
  "Return the unit the value is expressed in."
  state(self) L87
  "Return the state."
  update(self) L97
  "Get the latest data from REST API."

class DweetData(object) L102
  "The class for handling the data retrieval."
  __init__(self, device) L105
  "Initialize the sensor."
  update(self) L111
  "Get the latest data from Dweet.io."

setup_platform(hass, config, add_devices, discovery_info=None) L39
"Setup the Dweet sensor."

homeassistant/components/sensor/ebox.py
class EBoxSensor(Entity) L97
  "Implementation of a EBox sensor."
  __init__(self, ebox_data, sensor_type, name) L100
  "Initialize the sensor."
  name(self) L111
  "Return the name of the sensor."
  state(self) L116
  "Return the state of the sensor."
  unit_of_measurement(self) L121
  "Return the unit of measurement of this entity, if any."
  icon(self) L126
  "Icon to use in the frontend, if any."
  update(self) L130
  "Get the latest data from EBox and update the state."

class EBoxData(object) L137
  "Get data from Ebox."
  __init__(self, username, password) L140
  "Initialize the data object."
  update(self) L147
  "Get the latest data from Ebox."

setup_platform(hass, config, add_devices, discovery_info=None) L76
"Set up the EBox sensor."

homeassistant/components/sensor/ecobee.py
class EcobeeSensor(Entity) L37
  "Representation of an Ecobee sensor."
  __init__(self, sensor_name, sensor_type, sensor_index) L40
  "Initialize the sensor."
  name(self) L51
  "Return the name of the Ecobee sensor."
  state(self) L56
  "Return the state of the sensor."
  unique_id(self) L61
  "Return the unique ID of this sensor."
  unit_of_measurement(self) L66
  "Return the unit of measurement this sensor expresses itself in."
  update(self) L70
  "Get the latest state of the sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L20
"Setup the Ecobee sensors."

homeassistant/components/sensor/eddystone_temperature.py
class EddystoneTemp(Entity) L93
  "Representation of a temperature sensor."
  __init__(self, name, namespace, instance) L96
  "Initialize a sensor."
  name(self) L105
  "Return the name of the sensor."
  state(self) L110
  "Return the state of the device."
  unit_of_measurement(self) L115
  "Return the unit the value is expressed in."
  should_poll(self) L120
  "Hass should not poll for state."

class Monitor(object) L125
  "Continously scan for BLE advertisements."
  __init__(self, hass, devices, bt_device_id) L128
  "Construct interface object."
  callback(bt_addr, _, packet, additional_info) L137
  "Callback for new packets."
  start(self) L153
  "Continously scan for BLE advertisements."
  process_packet(self, namespace, instance, temperature) L162
  "Assign temperature to hass device."
  stop(self) L173
  "Signal runner to stop and join thread."

setup_platform(hass, config, add_devices, discovery_info=None) L43
"Validate configuration, create devices and start monitoring thread."
monitor_stop(_service_or_event) L64
"Stop the monitor thread."
monitor_start(_service_or_event) L69
"Start the monitor thread."
get_from_conf(config, config_key, length) L82
"Retrieve value from config and validate length."

homeassistant/components/sensor/efergy.py
class EfergySensor(Entity) L78
  "Implementation of an Efergy sensor."
  __init__(self, sensor_type, app_token, utc_offset, period, currency, sid=None) L81
  "Initialize the sensor."
  name(self) L101
  "Return the name of the sensor."
  state(self) L106
  "Return the state of the sensor."
  unit_of_measurement(self) L111
  "Return the unit of measurement of this entity, if any."
  update(self) L115
  "Get the Efergy monitor data from the web service."

setup_platform(hass, config, add_devices, discovery_info=None) L56
"Setup the Efergy sensor."

homeassistant/components/sensor/eliqonline.py
class EliqSensor(Entity) L60
  "Implementation of an ELIQ Online sensor."
  __init__(self, api, channel_id, name) L63
  "Initialize the sensor."
  name(self) L72
  "Return the name of the sensor."
  icon(self) L77
  "Return icon."
  unit_of_measurement(self) L82
  "Return the unit of measurement of this entity, if any."
  state(self) L87
  "Return the state of the device."
  update(self) L91
  "Get the latest data."

setup_platform(hass, config, add_devices, discovery_info=None) L39
"Setup the ELIQ Online sensor."

homeassistant/components/sensor/emoncms.py
class EmonCmsSensor(Entity) L108
  "Implementation of an Emoncms sensor."
  __init__(self, hass, data, name, value_template, unit_of_measurement, sensorid, elem) L111
  "Initialize the sensor."
  name(self) L135
  "Return the name of the sensor."
  unit_of_measurement(self) L140
  "Return the unit of measurement of this entity, if any."
  state(self) L145
  "Return the state of the device."
  device_state_attributes(self) L150
  "Return the atrributes of the sensor."
  update(self) L163
  "Get the latest data and updates the state."

class EmonCmsData(object) L188
  "The class for handling the data retrieval."
  __init__(self, hass, url, apikey, interval) L191
  "Initialize the data object."
  update(self) L200
  "Get the latest data from Emoncms."

get_id(sensorid, feedtag, feedname, feedid, feeduserid) L58
"Return unique identifier for feed / sensor."
setup_platform(hass, config, add_devices, discovery_info=None) L64
"Set up the Emoncms sensor."

homeassistant/components/sensor/enocean.py
class EnOceanSensor(enocean.EnOceanDevice, Entity) L36
  "Representation of an EnOcean sensor device such as a power meter."
  __init__(self, dev_id, devname) L39
  "Initialize the EnOcean sensor device."
  name(self) L50
  "Return the name of the device."
  value_changed(self, value) L54
  "Update the internal state of the device."
  state(self) L60
  "Return the state of the device."
  unit_of_measurement(self) L65
  "Return the unit of measurement."

setup_platform(hass, config, add_devices, discovery_info=None) L28
"Setup an EnOcean sensor device."

homeassistant/components/sensor/envisalink.py
class EnvisalinkSensor(EnvisalinkDevice, Entity) L40
  "Representation of an Envisalink keypad."
  __init__(self, hass, partition_name, partition_number, info, controller) L43
  "Initialize the sensor."
  async_added_to_hass(self) L53
  "Register callbacks."
  icon(self) L61
  "Return the icon if any."
  state(self) L66
  "Return the overall state."
  device_state_attributes(self) L71
  "Return the state attributes."
  _update_callback(self, partition) L76
  "Update the partition state in HA, if needed."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L22
"Perform the setup for Envisalink sensor devices."

homeassistant/components/sensor/fastdotcom.py
class SpeedtestSensor(Entity) L55
  "Implementation of a FAst.com sensor."
  __init__(self, speedtest_data) L58
  "Initialize the sensor."
  name(self) L66
  "Return the name of the sensor."
  state(self) L71
  "Return the state of the device."
  unit_of_measurement(self) L76
  "Return the unit of measurement of this entity, if any."
  update(self) L80
  "Get the latest data and update the states."
  async_added_to_hass(self) L89
  "Called when entity is about to be added to hass."

class SpeedtestData(object) L97
  "Get the latest data from fast.com."
  __init__(self, hass, config) L100
  "Initialize the data object."
  update(self, now) L110
  "Get the latest data from fast.com."

setup_platform(hass, config, add_devices, discovery_info=None) L41
"Setup the Fast.com sensor."
update(call=None) L47
"Update service for manual updates."

homeassistant/components/sensor/fedex.py
class FedexSensor(Entity) L58
  "Fedex Sensor."
  __init__(self, session, name, interval) L61
  "Initialize the sensor."
  name(self) L71
  "Return the name of the sensor."
  state(self) L76
  "Return the state of the sensor."
  _update(self) L80
  "Update device state."
  device_state_attributes(self) L98
  "Return the state attributes."
  icon(self) L103
  "Icon to use in the frontend."

setup_platform(hass, config, add_devices, discovery_info=None) L42
"Setup the Fedex platform."

homeassistant/components/sensor/fido.py
class FidoSensor(Entity) L112
  "Implementation of a Fido sensor."
  __init__(self, fido_data, sensor_type, name, number) L115
  "Initialize the sensor."
  name(self) L127
  "Return the name of the sensor."
  state(self) L132
  "Return the state of the sensor."
  unit_of_measurement(self) L137
  "Return the unit of measurement of this entity, if any."
  icon(self) L142
  "Icon to use in the frontend, if any."
  device_state_attributes(self) L147
  "Return the state attributes of the sensor."
  update(self) L153
  "Get the latest data from Fido and update the state."

class FidoData(object) L166
  "Get data from Fido."
  __init__(self, username, password) L169
  "Initialize the data object."
  update(self) L176
  "Get the latest data from Fido."

setup_platform(hass, config, add_devices, discovery_info=None) L90
"Set up the Fido sensor."

homeassistant/components/sensor/fitbit.py
class FitbitAuthCallbackView(HomeAssistantView) L282
  "Handle OAuth finish callback requests."
  __init__(self, config, add_devices, oauth) L289
  "Initialize the OAuth callback view."
  get(self, request) L296
  "Finish OAuth callback request."

class FitbitSensor(Entity) L348
  "Implementation of a Fitbit sensor."
  __init__(self, client, config_path, resource_type, is_metric) L351
  "Initialize the Fitbit sensor."
  name(self) L380
  "Return the name of the sensor."
  state(self) L385
  "Return the state of the sensor."
  unit_of_measurement(self) L390
  "Return the unit of measurement of this entity, if any."
  icon(self) L395
  "Icon to use in the frontend, if any."
  update(self) L400
  "Get the latest data from the Fitbit API and update the states."

config_from_file(filename, config=None) L127
"Small configuration file management function."
request_app_setup(hass, config, add_devices, config_path, discovery_info=None) L152
"Assist user with configuring the Fitbit dev application."
fitbit_configuration_callback(callback_data) L158
"The actions to do when our configuration callback is called."
request_oauth_completion(hass) L193
"Request user complete Fitbit OAuth2 flow."
fitbit_configuration_callback(callback_data) L203
"The actions to do when our configuration callback is called."
setup_platform(hass, config, add_devices, discovery_info=None) L217
"Set up the Fitbit sensor."

homeassistant/components/sensor/fixer.py
class ExchangeRateSensor(Entity) L61
  "Representation of a Exchange sensor."
  __init__(self, data, name, target) L64
  "Initialize the sensor."
  name(self) L73
  "Return the name of the sensor."
  unit_of_measurement(self) L78
  "Return the unit of measurement of this entity, if any."
  state(self) L83
  "Return the state of the sensor."
  device_state_attributes(self) L88
  "Return the state attributes."
  icon(self) L99
  "Return the icon to use in the frontend, if any."
  update(self) L103
  "Get the latest data and updates the states."

class ExchangeData(object) L109
  "Get the latest data and update the states."
  __init__(self, base_currency, target_currency) L112
  "Initialize the data object."
  update(self) L124
  "Get the latest data from Fixer.io."

setup_platform(hass, config, add_devices, discovery_info=None) L43
"Setup the Fixer.io sensor."

homeassistant/components/sensor/fritzbox_callmonitor.py
class FritzBoxCallSensor(Entity) L99
  "Implementation of a Fritz!Box call monitor."
  __init__(self, name, phonebook) L102
  "Initialize the sensor."
  set_state(self, state) L109
  "Set the state."
  set_attributes(self, attributes) L113
  "Set the state attributes."
  should_poll(self) L118
  "Polling needed only to update phonebook, if defined."
  state(self) L126
  "Return the state of the device."
  name(self) L131
  "Return the name of the sensor."
  device_state_attributes(self) L136
  "Return the state attributes."
  number_to_name(self, number) L140
  "Return a name for a given phone number."
  update(self) L147
  "Update the phonebook if it is defined."

class FritzBoxCallMonitor(object) L153
  "Event listener to monitor calls on the Fritz!Box."
  __init__(self, host, port, sensor) L156
  "Initialize Fritz!Box monitor instance."
  connect(self) L164
  "Connect to the Fritz!Box."
  _listen(self) L176
  "Listen to incoming or outgoing calls."
  _parse(self, line) L199
  "Parse the call information and set the sensor states."

class FritzBoxPhonebook(object) L235
  "This connects to a FritzBox router and downloads its phone book."
  __init__(self, host, port, username, password, phonebook_id=0, prefixes=None) L238
  "Initialize the class."
  update_phonebook(self) L263
  "Update the phone book dictionary."
  get_name(self, number) L271
  "Return a name for a given phone number."

setup_platform(hass, config, add_devices, discovery_info=None) L57
"Setup Fritz!Box call monitor sensor platform."
_stop_listener(_event) L85

homeassistant/components/sensor/fritzbox_netmonitor.py
class FritzboxMonitorSensor(Entity) L73
  "Implementation of a fritzbox monitor sensor."
  __init__(self, fstatus) L76
  "Initialize the sensor."
  name(self) L84
  "Return the name of the sensor."
  icon(self) L89
  "Icon to use in the frontend, if any."
  state(self) L94
  "Return the state of the device."
  state_attributes(self) L99
  "Return the device state attributes."
  update(self) L118
  "Retrieve information from the FritzBox."

setup_platform(hass, config, add_devices, discovery_info=None) L48
"Set up the fritzbox monitor sensors."

homeassistant/components/sensor/glances.py
class GlancesSensor(Entity) L82
  "Implementation of a Glances sensor."
  __init__(self, rest, name, sensor_type) L85
  "Initialize the sensor."
  name(self) L95
  "The name of the sensor."
  unit_of_measurement(self) L103
  "Return the unit the value is expressed in."
  state(self) L108
  "Return the state of the resources."
  update(self) L150
  "Get the latest data from REST API."

class GlancesData(object) L155
  "The class for handling the data retrieval."
  __init__(self, resource) L158
  "Initialize the data object."
  update(self) L164
  "Get the latest data from the Glances REST API."

setup_platform(hass, config, add_devices, discovery_info=None) L56
"Set up the Glances sensor."

homeassistant/components/sensor/google_travel_time.py
class GoogleTravelTimeSensor(Entity) L122
  "Representation of a Google travel time sensor."
  __init__(self, hass, name, api_key, origin, destination, options) L125
  "Initialize the sensor."
  state(self) L155
  "Return the state of the sensor."
  name(self) L168
  "Get the name of the sensor."
  device_state_attributes(self) L173
  "Return the state attributes."
  unit_of_measurement(self) L191
  "Return the unit this state is expressed in."
  update(self) L196
  "Get the latest data from Google."
  _get_location_from_entity(self, entity_id) L232
  "Get the location from the entity state or attributes."
  _get_location_from_attributes(entity) L262
  "Get the lat/long string from an entities attributes."
  _resolve_zone(self, friendly_name) L267

convert_time_to_utc(timestr) L74
"Take a string like 08:00:00 and convert it to a unix timestamp."
setup_platform(hass, config, add_devices_callback, discovery_info=None) L83
"Setup the Google travel time platform."
run_setup(event) L85
"Delay the setup until Home Assistant is fully initialized."

homeassistant/components/sensor/gpsd.py
class GpsdSensor(Entity) L68
  "Representation of a GPS receiver available via GPSD."
  __init__(self, hass, name, host, port) L71
  "Initialize the GPSD sensor."
  name(self) L85
  "Return the name."
  state(self) L91
  "Return the state of GPSD."
  device_state_attributes(self) L101
  "Return the state attributes of the GPS."

setup_platform(hass, config, add_devices, discovery_info=None) L39
"Setup the GPSD component."

homeassistant/components/sensor/gtfs.py
class GTFSDepartureSensor(Entity) L181
  "Implementation of an GTFS departures sensor."
  __init__(self, pygtfs, name, origin, destination) L184
  "Initialize the sensor."
  name(self) L198
  "Return the name of the sensor."
  state(self) L203
  "Return the state of the sensor."
  unit_of_measurement(self) L208
  "Return the unit of measurement of this entity, if any."
  device_state_attributes(self) L213
  "Return the state attributes."
  icon(self) L218
  "Icon to use in the frontend, if any."
  update(self) L222
  "Get the latest data from GTFS and update the states."
  dict_for_table(resource) L253
  "Return a dict for the SQLAlchemy resource given."
  append_keys(resource, prefix=None) L258
  "Properly format key val pairs to append to attributes."

get_next_departure(sched, start_station_id, end_station_id) L44
"Get the next departure for the given schedule."
setup_platform(hass, config, add_devices, discovery_info=None) L151
"Set up the GTFS sensor."

homeassistant/components/sensor/haveibeenpwned.py
class HaveIBeenPwnedSensor(Entity) L52
  "Implementation of a HaveIBeenPwnedSensor."
  __init__(self, data, hass, email) L55
  "Initialize the HaveIBeenPwnedSensor sensor."
  name(self) L64
  "Return the name of the sensor."
  unit_of_measurement(self) L69
  "Return the unit the value is expressed in."
  state(self) L74
  "Return the state of the device."
  device_state_attributes(self) L79
  "Return the atrributes of the sensor."
  update_nothrottle(self, dummy=None) L95
  "Update sensor without throttle."
  update(self) L115
  "Update data and see if it contains data for our email."

class HaveIBeenPwnedData(object) L123
  "Class for handling the data retrieval."
  __init__(self, emails) L126
  "Initialize the data object."
  set_next_email(self) L134
  "Set the next email to be looked up."
  update_no_throttle(self) L139
  "Get the data for a specific email."
  update(self, **kwargs) L144
  "Get the latest data for current email from REST service."

setup_platform(hass, config, add_devices, discovery_info=None) L35
"Set up the HaveIBeenPwnedSensor sensor."

homeassistant/components/sensor/hddtemp.py
class HddTempSensor(Entity) L56
  "Representation of a HDDTemp sensor."
  __init__(self, name, hddtemp) L59
  "Initialize a HDDTemp sensor."
  name(self) L68
  "Return the name of the sensor."
  state(self) L73
  "Return the state of the device."
  unit_of_measurement(self) L78
  "Return the unit the value is expressed in."
  device_state_attributes(self) L86
  "Return the state attributes of the sensor."
  update(self) L93
  "Get the latest data from HDDTemp daemon and updates the state."

class HddTempData(object) L104
  "Get the latest data from HDDTemp and update the states."
  __init__(self, host, port) L107
  "Initialize the data object."
  update(self) L114
  "Get the latest data from hhtemp running as daemon."

setup_platform(hass, config, add_devices, discovery_info=None) L40
"Setup the HDDTemp sensor."

homeassistant/components/sensor/history_stats.py
class HistoryStatsSensor(Entity) L99
  "Representation of a HistoryStats sensor."
  __init__(self, hass, entity_id, entity_state, start, end, duration, sensor_type, name) L102
  "Initialize the HistoryStats sensor."
  force_refresh(*args) L121
  "Force the component to refresh."
  name(self) L132
  "Return the name of the sensor."
  state(self) L137
  "Return the state of the sensor."
  unit_of_measurement(self) L149
  "Return the unit the value is expressed in."
  should_poll(self) L154
  "Polling required."
  device_state_attributes(self) L159
  "Return the state attributes of the sensor."
  icon(self) L170
  "Return the icon to use in the frontend, if any."
  update(self) L174
  "Get the latest data and updates the states."
  update_period(self) L224
  "Parse the templates and store a datetime tuple in _period."

class HistoryStatsHelper L272
  "Static methods to make the HistoryStatsSensor code lighter."
  pretty_duration(hours) L276
  "Format a duration in days, hours, minutes, seconds."
  pretty_ratio(value, period) L292
  "Format the ratio of value / period duration."
  handle_template_exception(ex, field) L301
  "Log an error nicely if the template cannot be interpreted."

exactly_two_period_keys(conf) L53
"Ensure exactly 2 of CONF_PERIOD_KEYS are provided."
setup_platform(hass, config, add_devices, discovery_info=None) L79
"Set up the History Stats sensor."

homeassistant/components/sensor/homematic.py
class HMSensor(HMDevice) L71
  "Represents various Homematic sensors in Home Assistant."
  state(self) L75
  "Return the state of the sensor."
  unit_of_measurement(self) L86
  "Return the unit of measurement of this entity, if any."
  icon(self) L91
  "Return the icon to use in the frontend, if any."
  _init_data_struct(self) L95
  "Generate a data dict (self._data) from hm metadata."

setup_platform(hass, config, add_devices, discovery_info=None) L57
"Setup the platform."

homeassistant/components/sensor/hp_ilo.py
class HpIloSensor(Entity) L85
  "Representation of a HP ILO sensor."
  __init__(self, hp_ilo_data, sensor_type, client_name) L88
  "Initialize the sensor."
  name(self) L103
  "Return the name of the sensor."
  state(self) L108
  "Return the state of the sensor."
  device_state_attributes(self) L113
  "Return the state attributes."
  update(self) L117
  "Get the latest data from HP ILO and updates the states."

class HpIloData(object) L144
  "Gets the latest data from HP ILO."
  __init__(self, host, port, login, password) L147
  "Initialize the data object."
  update(self) L159
  "Get the latest data from HP ILO."

setup_platform(hass, config, add_devices, discovery_info=None) L57
"Set up the HP ILO sensor."

homeassistant/components/sensor/hydroquebec.py
class HydroQuebecSensor(Entity) L119
  "Implementation of a HydroQuebec sensor."
  __init__(self, hydroquebec_data, sensor_type, name) L122
  "Initialize the sensor."
  name(self) L134
  "Return the name of the sensor."
  state(self) L139
  "Return the state of the sensor."
  unit_of_measurement(self) L144
  "Return the unit of measurement of this entity, if any."
  icon(self) L149
  "Icon to use in the frontend, if any."
  update(self) L153
  "Get the latest data from Hydroquebec and update the state."

class HydroquebecData(object) L160
  "Get data from HydroQuebec."
  __init__(self, username, password, contract=None) L163
  "Initialize the data object."
  get_contract_list(self) L172
  "Return the contract list."
  _fetch_data(self) L178
  "Fetch latest data from HydroQuebec."
  update(self) L188
  "Return the latest collected data from HydroQuebec."

setup_platform(hass, config, add_devices, discovery_info=None) L93
"Set up the HydroQuebec sensor."

homeassistant/components/sensor/imap.py
class ImapSensor(Entity) L47
  "Representation of an IMAP sensor."
  __init__(self, name, user, password, server, port) L50
  "Initialize the sensor."
  _login(self) L61
  "Login and return an IMAP connection."
  name(self) L73
  "Return the name of the sensor."
  state(self) L78
  "Return the number of unread emails."
  update(self) L82
  "Check the number of unread emails."
  icon(self) L98
  "Return the icon to use in the frontend."

setup_platform(hass, config, add_devices, discovery_info=None) L33
"Setup the IMAP platform."

homeassistant/components/sensor/imap_email_content.py
class EmailReader L62
  "A class to read emails from an IMAP server."
  __init__(self, user, password, server, port) L65
  "Initialize the Email Reader."
  connect(self) L75
  "Login and setup the connection."
  _fetch_message(self, message_uid) L86
  "Get an email message from a message id."
  read_next(self) L95
  "Read the next email from the email server."

class EmailContentSensor(Entity) L124
  "Representation of an EMail sensor."
  __init__(self, hass, email_reader, name, allowed_senders, value_template) L127
  "Initialize the sensor."
  name(self) L141
  "Return the name of the sensor."
  state(self) L146
  "Return the current email state."
  device_state_attributes(self) L151
  "Return other state attributes for the message."
  render_template(self, email_message) L155
  "Render the message template."
  sender_allowed(self, email_message) L165
  "Check if the sender is in the allowed senders list."
  get_msg_sender(email_message) L171
  "Get the parsed message sender from the email."
  get_msg_subject(email_message) L176
  "Decode the message subject."
  get_msg_text(email_message) L183
  "Get the message text from the email."
  update(self) L215
  "Read emails and publish state change."

setup_platform(hass, config, add_devices, discovery_info=None) L43
"Setup the EMail platform."

homeassistant/components/sensor/influxdb.py
class InfluxSensor(Entity) L86
  "Implementation of a Influxdb sensor."
  __init__(self, hass, influx_conf, query) L89
  "Initialize the sensor."
  name(self) L127
  "Return the name of the sensor."
  state(self) L132
  "Return the state of the sensor."
  unit_of_measurement(self) L137
  "Unit of measurement of this entity, if any."
  should_poll(self) L142
  "Polling needed."
  update(self) L146
  "Get the latest data from influxdb and updates the states."

class InfluxSensorData(object) L159
  "Class for handling the data retrieval."
  __init__(self, influx, query) L162
  "Initialize the data object."
  update(self) L169
  "Get the latest data with a shell command."

setup_platform(hass, config, add_devices, discovery_info=None) L67
"Setup the InfluxDB component."

homeassistant/components/sensor/ios.py
class IOSSensor(Entity) L33
  "Representation of an iOS sensor."
  __init__(self, sensor_type, device_name, device) L36
  "Initialize the sensor."
  name(self) L47
  "Return the name of the iOS sensor."
  state(self) L53
  "Return the state of the sensor."
  unique_id(self) L58
  "Return the unique ID of this sensor."
  unit_of_measurement(self) L64
  "Return the unit of measurement this sensor expresses itself in."
  device_state_attributes(self) L68
  "Return the device state attributes."
  icon(self) L81
  "Return the icon to use in the frontend, if any."
  update(self) L126
  "Get the latest state of the sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L21
"Setup the iOS sensor."

homeassistant/components/sensor/isy994.py
class ISYSensorDevice(isy.ISYDevice) L260
  "Representation of an ISY994 sensor device."
  __init__(self, node) L263
  "Initialize the ISY994 sensor device."
  raw_unit_of_measurement(self) L268
  "Get the raw unit of measurement for the ISY994 sensor device."
  state(self) L283
  "Get the state of the ISY994 sensor device."
  unit_of_measurement(self) L308
  "Get the unit of measurement for the ISY994 sensor device."

class ISYWeatherDevice(isy.ISYDevice) L317
  "Representation of an ISY994 weather device."
  __init__(self, node) L322
  "Initialize the ISY994 weather device."
  unique_id(self) L327
  "Return the unique identifier for the node."
  raw_units(self) L332
  "Return the raw unit of measurement."
  state(self) L341
  "Return the value of the node."
  unit_of_measurement(self) L352
  "Return the unit of measurement for the node."

setup_platform(hass, config, add_devices, discovery_info=None) L239
"Set up the ISY994 sensor platform."

homeassistant/components/sensor/knx.py
class KNXSensorBaseClass L87
  "Sensor Base Class for all KNX Sensors."
  cache(self) L91
  "We don't want to cache any Sensor Value."

class KNXSensorFloatClass(KNXGroupAddress, KNXSensorBaseClass) L96
  "Base Implementation of a 2byte Floating Point KNX Telegram."
  __init__(self, hass, config, unit_of_measurement, minimum_sensor_value, maximum_sensor_value) L103
  "Initialize a KNX Float Sensor."
  state(self) L114
  "Return the Value of the KNX Sensor."
  unit_of_measurement(self) L119
  "Return the defined Unit of Measurement for the KNX Sensor."
  update(self) L123
  "Update KNX sensor."

setup_platform(hass, config, add_entities, discovery_info=None) L35
"Setup the KNX Sensor platform."
update_and_define_min_max(config, minimum_default, maximum_default) L73
"Function help determinate a min/max value defined in config."

homeassistant/components/sensor/kwb.py
class KWBSensor(Entity) L84
  "Representation of a KWB Easyfire sensor."
  __init__(self, easyfire, sensor, client_name) L87
  "Initialize the KWB sensor."
  name(self) L95
  "Return the name."
  available(self) L100
  "Return if sensor is available."
  state(self) L105
  "Return the state of value."
  unit_of_measurement(self) L113
  "Return the unit of measurement of this entity, if any."

setup_platform(hass, config, add_devices, discovery_info=None) L52
"Setup the KWB component."

homeassistant/components/sensor/lastfm.py
class LastfmSensor(Entity) L45
  "A class for the Last.fm account."
  __init__(self, user, lastfm) L48
  "Initialize the sensor."
  name(self) L61
  "Return the name of the sensor."
  entity_id(self) L66
  "Return the entity ID."
  state(self) L71
  "Return the state of the sensor."
  update(self) L76
  "Update device state."
  device_state_attributes(self) L95
  "Return the state attributes."
  entity_picture(self) L104
  "Avatar of the user."
  icon(self) L109
  "Return the icon to use in the frontend."

setup_platform(hass, config, add_devices, discovery_info=None) L34
"Set up the Last.fm platform."

homeassistant/components/sensor/linux_battery.py
class LinuxBatterySensor(Entity) L66
  "Representation of a Linux Battery sensor."
  __init__(self, name, battery_id) L69
  "Initialize the battery sensor."
  name(self) L81
  "Return the name of the sensor."
  state(self) L86
  "Return the state of the sensor."
  unit_of_measurement(self) L91
  "Return the unit the value is expressed in."
  icon(self) L96
  "Return the icon to use in the frontend, if any."
  device_state_attributes(self) L101
  "Return the state attributes of the sensor."
  update(self) L121
  "Get the latest data and updates the states."

setup_platform(hass, config, add_devices, discovery_info=None) L52
"Setup the Linux Battery sensor."

homeassistant/components/sensor/loopenergy.py
class LoopEnergyDevice(Entity) L93
  "Implementation of an Loop Energy base sensor."
  __init__(self, controller) L96
  "Initialize the sensor."
  name(self) L104
  "Return the name of the sensor."
  state(self) L109
  "Return the state of the sensor."
  should_poll(self) L114
  "No polling needed."
  unit_of_measurement(self) L119
  "Return the unit of measurement of this entity, if any."
  _callback(self) L123

class LoopEnergyElec(LoopEnergyDevice) L127
  "Implementation of an Loop Energy Electricity sensor."
  __init__(self, controller) L130
  "Initialize the sensor."
  update(self) L136
  "Get the cached Loop energy."

class LoopEnergyGas(LoopEnergyDevice) L141
  "Implementation of an Loop Energy Gas sensor."
  __init__(self, controller) L144
  "Initialize the sensor."
  update(self) L150
  "Get the cached Loop energy."

setup_platform(hass, config, add_devices, discovery_info=None) L61
"Setup the Loop Energy sensors."
stop_loopenergy(event) L78
"Shutdown loopenergy thread on exit."

homeassistant/components/sensor/lyft.py
class LyftSensor(Entity) L73
  "Implementation of an Lyft sensor."
  __init__(self, sensorType, products, product_id, product) L76
  "Initialize the Lyft sensor."
  name(self) L95
  "Return the name of the sensor."
  state(self) L100
  "Return the state of the sensor."
  unit_of_measurement(self) L105
  "Return the unit of measurement of this entity, if any."
  device_state_attributes(self) L110
  "Return the state attributes."
  icon(self) L151
  "Icon to use in the frontend, if any."
  update(self) L155
  "Get the latest data from the Lyft API and update the states."

class LyftEstimate(object) L179
  "The class for handling the time and price estimate."
  __init__(self, session, start_latitude, start_longitude, end_latitude=None, end_longitude=None) L182
  "Initialize the LyftEstimate object."
  update(self) L194
  "Get the latest product info and estimates from the Lyft API."
  __real_update(self) L198

setup_platform(hass, config, add_devices, discovery_info=None) L45
"Set up the Lyft sensor."

homeassistant/components/sensor/metoffice.py
class MetOfficeCurrentSensor(Entity) L106
  "Implementation of a Met Office current sensor."
  __init__(self, site, data, condition) L109
  "Initialize the sensor."
  name(self) L116
  "Return the name of the sensor."
  state(self) L121
  "Return the state of the sensor."
  unit_of_measurement(self) L134
  "Return the unit of measurement."
  device_state_attributes(self) L139
  "Return the state attributes of the device."
  update(self) L149
  "Update current conditions."

class MetOfficeCurrentData(object) L154
  "Get data from Datapoint."
  __init__(self, hass, datapoint, site) L157
  "Initialize the data object."
  update(self) L165
  "Get the latest data from Datapoint."

setup_platform(hass, config, add_devices, discovery_info=None) L69
"Setup the sensor platform."

homeassistant/components/sensor/mfi.py
class MfiSensor(Entity) L77
  "Representation of a mFi sensor."
  __init__(self, port, hass) L80
  "Initialize the sensor."
  name(self) L86
  "Return the name of th sensor."
  state(self) L91
  "Return the state of the sensor."
  unit_of_measurement(self) L106
  "Return the unit of measurement of this entity, if any."
  update(self) L121
  "Get the latest data."

setup_platform(hass, config, add_devices, discovery_info=None) L52
"Setup mFi sensors."

homeassistant/components/sensor/mhz19.py
class MHZ19Sensor(Entity) L69
  "Representation of an CO2 sensor."
  __init__(self, mhz_client, sensor_type, temp_unit, name) L72
  "Initialize a new PM sensor."
  name(self) L83
  "Return the name of the sensor."
  state(self) L88
  "Return the state of the sensor."
  unit_of_measurement(self) L94
  "Return the unit of measurement of this entity, if any."
  update(self) L98
  "Read from sensor and update the state."
  device_state_attributes(self) L110
  "Return the state attributes."

class MHZClient(object) L120
  "Get the latest data from the DHT sensor."
  __init__(self, co2sensor, serial) L123
  "Initialize the sensor."
  update(self) L130
  "Get the latest data the MH-Z19 sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L45
"Setup the available CO2 sensors."

homeassistant/components/sensor/miflora.py
class MiFloraSensor(Entity) L89
  "Implementing the MiFlora sensor."
  __init__(self, poller, parameter, name, unit, force_update, median) L92
  "Initialize the sensor."
  name(self) L107
  "Return the name of the sensor."
  state(self) L112
  "Return the state of the sensor."
  unit_of_measurement(self) L117
  "Return the units of measurement."
  force_update(self) L122
  "Force update."
  update(self) L126
  "Update current conditions."

setup_platform(hass, config, add_devices, discovery_info=None) L60
"Set up the MiFlora sensor."

homeassistant/components/sensor/min_max.py
class MinMaxSensor(Entity) L101
  "Representation of a min/max sensor."
  __init__(self, hass, entity_ids, name, sensor_type, round_digits) L104
  "Initialize the min/max sensor."
  async_min_max_sensor_state_listener(entity, old_state, new_state) L125
  "Called when the sensor changes state."
  name(self) L155
  "Return the name of the sensor."
  state(self) L160
  "Return the state of the sensor."
  unit_of_measurement(self) L168
  "Return the unit the value is expressed in."
  should_poll(self) L175
  "No polling needed."
  device_state_attributes(self) L180
  "Return the state attributes of the sensor."
  icon(self) L189
  "Return the icon to use in the frontend, if any."
  async_update(self) L194
  "Get the latest data and updates the states."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L55
"Set up the min/max/mean sensor."
calc_min(sensor_values) L68
"Calculate min value, honoring unknown states."
calc_max(sensor_values) L78
"Calculate max value, honoring unknown states."
calc_mean(sensor_values, round_digits) L88
"Calculate mean value, honoring unknown states."

homeassistant/components/sensor/modbus.py
class ModbusRegisterSensor(Entity) L73
  "Modbus resgister sensor."
  __init__(self, name, slave, register, register_type, unit_of_measurement, count, scale, offset, data_type, precision) L76
  "Initialize the modbus register sensor."
  state(self) L93
  "Return the state of the sensor."
  name(self) L98
  "Return the name of the sensor."
  unit_of_measurement(self) L103
  "Return the unit of measurement."
  update(self) L107
  "Update the state of the sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L55
"Set up the Modbus sensors."

homeassistant/components/sensor/modem_callerid.py
class ModemCalleridSensor(Entity) L49
  "Implementation of USB modem callerid sensor."
  __init__(self, hass, name, port, modem) L52
  "Initialize the sensor."
  set_state(self, state) L62
  "Set the state."
  set_attributes(self, attributes) L66
  "Set the state attributes."
  should_poll(self) L71
  "No polling needed."
  icon(self) L76
  "Return icon."
  state(self) L81
  "Return the state of the device."
  name(self) L86
  "Return the name of the sensor."
  device_state_attributes(self) L91
  "Return the state attributes."
  _stop_modem(self, event) L95
  "HA is shutting down, close modem port."
  _incomingcallcallback(self, newstate) L102
  "Callback from modem, process based on new state."

setup_platform(hass, config, add_devices, discovery_info=None) L35
"Setup modem caller id sensor platform."

homeassistant/components/sensor/mold_indicator.py
class MoldIndicator(Entity) L58
  "Represents a MoldIndication sensor."
  __init__(self, hass, name, indoor_temp_sensor, outdoor_temp_sensor, indoor_humidity_sensor, calib_factor) L61
  "Initialize the sensor."
  _update_temp_sensor(state) L100
  "Parse temperature sensor value."
  _update_hum_sensor(state) L123
  "Parse humidity sensor value."
  update(self) L143
  "Calculate latest state."
  _sensor_changed(self, entity_id, old_state, new_state) L153
  "Called when sensor values change."
  _calc_dewpoint(self) L168
  "Calculate the dewpoint for the indoor air."
  _calc_moldindicator(self) L182
  "Calculate the humidity at the (cold) calibration point."
  should_poll(self) L221
  "Polling needed."
  name(self) L226
  "Return the name."
  unit_of_measurement(self) L231
  "Return the unit of measurement."
  state(self) L236
  "Return the state of the entity."
  device_state_attributes(self) L241
  "Return the state attributes."

setup_platform(hass, config, add_devices, discovery_info=None) L45
"Setup MoldIndicator sensor."

homeassistant/components/sensor/moon.py
class MoonSensor(Entity) L40
  "Representation of a Moon sensor."
  __init__(self, name) L43
  "Initialize the sensor."
  name(self) L49
  "Return the name of the device."
  state(self) L54
  "Return the state of the device."
  icon(self) L74
  "Icon to use in the frontend, if any."
  async_update(self) L79
  "Get the time and updates the states."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L32
"Set up the Moon sensor."

homeassistant/components/sensor/mqtt.py
class MqttSensor(Entity) L61
  "Representation of a sensor that can be updated using MQTT."
  __init__(self, name, state_topic, qos, unit_of_measurement, force_update, expire_after, value_template) L64
  "Initialize the sensor."
  async_added_to_hass(self) L77
  "Subscribe mqtt events."
  message_received(topic, payload, qos) L83
  "A new MQTT message has been received."
  value_is_expired(self, *_) L111
  "Triggered when value is expired."
  should_poll(self) L118
  "No polling needed."
  name(self) L123
  "Return the name of the sensor."
  unit_of_measurement(self) L128
  "Return the unit this state is expressed in."
  force_update(self) L133
  "Force update."
  state(self) L138
  "Return the state of the entity."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L41
"Set up MQTT Sensor."

homeassistant/components/sensor/mqtt_room.py
class MQTTRoomSensor(Entity) L71
  "Representation of a room sensor that is updated via MQTT."
  __init__(self, name, state_topic, device_id, timeout, consider_home) L74
  "Initialize the sensor."
  async_added_to_hass(self) L87
  "Subscribe mqtt events."
  update_state(device_id, room, distance) L93
  "Update the sensor state."
  message_received(topic, payload, qos) L102
  "A new MQTT message has been received."
  name(self) L130
  "Return the name of the sensor."
  device_state_attributes(self) L135
  "Return the state attributes."
  state(self) L142
  "Return the current room of the entity."
  update(self) L146
  "Update the state for absent devices."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L60
"Setup MQTT Sensor."
_parse_update_data(topic, data) L154
"Parse the room presence update."

homeassistant/components/sensor/mvglive.py
class MVGLiveSensor(Entity) L75
  "Implementation of an MVG Live sensor."
  __init__(self, station, destinations, directions, lines, products, timeoffset, name) L78
  "Initialize the sensor."
  name(self) L89
  "Return the name of the sensor."
  state(self) L97
  "Return the next departure time."
  state_attributes(self) L102
  "Return the state attributes."
  icon(self) L107
  "Icon to use in the frontend, if any."
  unit_of_measurement(self) L112
  "Return the unit this state is expressed in."
  update(self) L116
  "Get the latest data and update the state."

class MVGLiveData(object) L127
  "Pull data from the mvg-live.de web page."
  __init__(self, station, destinations, directions, lines, products, timeoffset) L130
  "Initialize the sensor."
  update(self) L147
  "Update the connection data."

setup_platform(hass, config, add_devices, discovery_info=None) L58
"Get the MVGLive sensor."

homeassistant/components/sensor/mysensors.py
class MySensorsSensor(mysensors.MySensorsDeviceEntity, Entity) L89
  "Representation of a MySensors Sensor child node."
  force_update(self) L93
  "Return True if state updates should be forced."
  state(self) L102
  "Return the state of the device."
  unit_of_measurement(self) L107
  "Return the unit of measurement of this entity."

setup_platform(hass, config, add_devices, discovery_info=None) L16
"Setup the MySensors platform for sensors."

homeassistant/components/sensor/neato.py
class NeatoConnectedSensor(Entity) L44
  "Neato Connected Sensor."
  __init__(self, hass, robot, sensor_type) L47
  "Initialize the Neato Connected sensor."
  update(self) L70
  "Update the properties of sensor."
  unit_of_measurement(self) L131
  "Return unit for the sensor."
  available(self) L137
  "Return True if sensor data is available."
  state(self) L145
  "Return the sensor state."
  name(self) L153
  "Return the name of the sensor."
  device_state_attributes(self) L158
  "Return the device specific attributes."

setup_platform(hass, config, add_devices, discovery_info=None) L34
"Setup the Neato sensor platform."

homeassistant/components/sensor/nest.py
class NestSensor(Entity) L90
  "Representation of a Nest sensor."
  __init__(self, structure, device, variable) L93
  "Initialize the sensor."
  name(self) L107
  "Return the name of the nest, if any."
  unit_of_measurement(self) L112
  "Return the unit the value is expressed in."

class NestBasicSensor(NestSensor) L117
  "Representation a basic Nest sensor."
  state(self) L121
  "Return the state of the sensor."
  update(self) L125
  "Retrieve latest state."

class NestTempSensor(NestSensor) L135
  "Representation of a Nest Temperature sensor."
  state(self) L139
  "Return the state of the sensor."
  update(self) L143
  "Retrieve latest state."

class NestProtectSensor(NestSensor) L161
  "Return the state of nest protect."
  state(self) L165
  "Return the state of the sensor."
  update(self) L169
  "Retrieve latest state."

setup_platform(hass, config, add_devices, discovery_info=None) L46
"Setup the Nest Sensor."

homeassistant/components/sensor/netatmo.py
class NetAtmoSensor(Entity) L97
  "Implementation of a Netatmo sensor."
  __init__(self, netatmo_data, module_name, sensor_type) L100
  "Initialize the sensor."
  name(self) L118
  "Return the name of the sensor."
  unique_id(self) L123
  "Return the unique ID for this sensor."
  icon(self) L128
  "Icon to use in the frontend, if any."
  state(self) L133
  "Return the state of the device."
  unit_of_measurement(self) L138
  "Return the unit of measurement of this entity, if any."
  update(self) L142
  "Get the latest data from NetAtmo API and updates the states."

class NetAtmoData(object) L288
  "Get the latest data from NetAtmo."
  __init__(self, auth, station) L291
  "Initialize the data object."
  get_module_names(self) L298
  "Return all module available on the API as a list."
  update(self) L304
  "Call the Netatmo API to update the data."

setup_platform(hass, config, add_devices, discovery_info=None) L67
"Setup the available Netatmo weather sensors."

homeassistant/components/sensor/netdata.py
class NetdataSensor(Entity) L92
  "Implementation of a Netdata sensor."
  __init__(self, rest, name, sensor_type) L95
  "Initialize the sensor."
  name(self) L105
  "The name of the sensor."
  unit_of_measurement(self) L110
  "Return the unit the value is expressed in."
  state(self) L115
  "Return the state of the resources."
  update(self) L126
  "Get the latest data from Netdata REST API."

class NetdataData(object) L131
  "The class for handling the data retrieval."
  __init__(self, resource) L134
  "Initialize the data object."
  update(self) L140
  "Get the latest data from the Netdata REST API."

setup_platform(hass, config, add_devices, discovery_info=None) L58
"Set up the Netdata sensor."

homeassistant/components/sensor/neurio_energy.py
class NeurioData(object) L72
  "Stores data retrieved from Neurio sensor."
  __init__(self, api_key, api_secret, sensor_id) L75
  "Initialize the data."
  daily_usage(self) L99
  "Return latest daily usage value."
  active_power(self) L104
  "Return latest active power value."
  get_active_power(self) L108
  "Return current power value."
  get_daily_usage(self) L117
  "Return current daily power usage."

class NeurioEnergy(Entity) L139
  "Implementation of a Neurio energy sensor."
  __init__(self, data, name, sensor_type, update_call) L142
  "Initialize the sensor."
  name(self) L156
  "Return the name of the sensor."
  state(self) L161
  "Return the state of the sensor."
  unit_of_measurement(self) L166
  "Return the unit of measurement of this entity, if any."
  icon(self) L171
  "Icon to use in the frontend, if any."
  update(self) L175
  "Get the latest data, update state."

setup_platform(hass, config, add_devices, discovery_info=None) L45
"Setup the Neurio sensor."
update_daily() L54
"Update the daily power usage."
update_active() L59
"Update the active power usage."

homeassistant/components/sensor/nut.py
class NUTSensor(Entity) L176
  "Representation of a sensor entity for NUT status values."
  __init__(self, name, data, sensor_type) L179
  "Initialize the sensor."
  name(self) L188
  "Return the name of the UPS sensor."
  icon(self) L193
  "Icon to use in the frontend, if any."
  state(self) L198
  "Return entity state from ups."
  unit_of_measurement(self) L203
  "Return the unit of measurement of this entity, if any."
  device_state_attributes(self) L208
  "Return the sensor attributes."
  opp_state(self) L214
  "Return UPS operating state."
  update(self) L226
  "Get the latest status and use it to update our sensor state."

class PyNUTData(object) L238
  "Stores the data retrieved from NUT."
  __init__(self, host, port, alias, username, password) L245
  "Initialize the data oject."
  status(self) L263
  "Get latest update if throttle allows."
  _get_alias(self) L268
  "Get the ups alias from NUT."
  _get_status(self) L276
  "Get the ups status from NUT."
  update(self, **kwargs) L289
  "Fetch the latest status from APCUPSd."

setup_platform(hass, config, add_entities, discovery_info=None) L137
"Setup the NUT sensors."

homeassistant/components/sensor/nzbget.py
class NZBGetSensor(Entity) L82
  "Representation of a NZBGet sensor."
  __init__(self, api, sensor_type, client_name) L85
  "Initialize a new NZBGet sensor."
  name(self) L97
  "Return the name of the sensor."
  state(self) L102
  "Return the state of the sensor."
  unit_of_measurement(self) L107
  "Unit of measurement of this entity, if any."
  update(self) L111
  "Update state of sensor."

class NZBGetAPI(object) L139
  "Simple JSON-RPC wrapper for NZBGet's API."
  __init__(self, api_url, username=None, password=None) L142
  "Initialize NZBGet API and set headers needed later."
  post(self, method, params=None) L154
  "Send a POST request and return the response as a dict."
  update(self) L172
  "Update cached response."

setup_platform(hass, config, add_devices, discovery_info=None) L52
"Setup the NZBGet sensors."

homeassistant/components/sensor/octoprint.py
class OctoPrintSensor(Entity) L74
  "Representation of an OctoPrint sensor."
  __init__(self, api, condition, sensor_type, sensor_name, unit, endpoint, group, tool=None) L77
  "Initialize a new OctoPrint sensor."
  name(self) L98
  "Return the name of the sensor."
  state(self) L103
  "Return the state of the sensor."
  unit_of_measurement(self) L115
  "Unit of measurement of this entity, if any."
  update(self) L119
  "Update state of sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L41
"Setup the available OctoPrint sensors."

homeassistant/components/sensor/ohmconnect.py
class OhmconnectSensor(Entity) L43
  "Representation of a OhmConnect sensor."
  __init__(self, name, ohmid) L46
  "Initialize the sensor."
  name(self) L54
  "The name of the sensor."
  state(self) L59
  "Return the state of the sensor."
  device_state_attributes(self) L67
  "Return the state attributes."
  update(self) L72
  "Get the latest data from OhmConnect."

setup_platform(hass, config, add_devices, discovery_info=None) L35
"Setup the OhmConnect sensor."

homeassistant/components/sensor/onewire.py
class OneWire(Entity) L80
  "Implementation of an One wire Sensor."
  __init__(self, name, device_file) L83
  "Initialize the sensor."
  _read_temp_raw(self) L90
  "Read the temperature as it is returned by the sensor."
  name(self) L98
  "Return the name of the sensor."
  state(self) L103
  "Return the state of the sensor."
  unit_of_measurement(self) L108
  "Return the unit the value is expressed in."
  update(self) L112
  "Get the latest data from the device."

setup_platform(hass, config, add_devices, discovery_info=None) L31
"Setup the one wire Sensors."

homeassistant/components/sensor/openevse.py
class OpenEVSESensor(Entity) L55
  "Implementation of an OpenEVSE sensor."
  __init__(self, sensor_type, charger) L59
  "Initialize the sensor."
  name(self) L68
  "Return the name of the sensor."
  state(self) L73
  "Return the state of the sensor."
  unit_of_measurement(self) L78
  "Return the unit of measurement of this sensor."
  update(self) L82
  "Get the monitored data from the charger."

setup_platform(hass, config, add_devices, discovery_info=None) L39
"Set up the OpenEVSE sensor."

homeassistant/components/sensor/openexchangerates.py
class OpenexchangeratesSensor(Entity) L61
  "Representation of an Open Exchange Rates sensor."
  __init__(self, rest, name, quote) L64
  "Initialize the sensor."
  name(self) L72
  "Return the name of the sensor."
  state(self) L77
  "Return the state of the sensor."
  device_state_attributes(self) L82
  "Return other attributes of the sensor."
  update(self) L89
  "Update current conditions."

class OpenexchangeratesData(object) L96
  "Get data from Openexchangerates.org."
  __init__(self, resource, parameters, quote) L99
  "Initialize the data object."
  update(self) L107
  "Get the latest data from openexchangerates.org."

setup_platform(hass, config, add_devices, discovery_info=None) L38
"Set up the Open Exchange Rates sensor."

homeassistant/components/sensor/opensky.py
class OpenSkySensor(Entity) L62
  "Open Sky Network Sensor."
  __init__(self, hass, name, latitude, longitude, radius) L65
  "Initialize the sensor."
  name(self) L78
  "Return the name of the sensor."
  state(self) L83
  "Return the state of the sensor."
  _handle_boundary(self, callsigns, event) L87
  "Handle flights crossing region boundary."
  update(self) L96
  "Update device state."
  device_state_attributes(self) L127
  "Return the state attributes."
  unit_of_measurement(self) L134
  "Unit of measurement."
  icon(self) L139
  "Icon."

setup_platform(hass, config, add_devices, discovery_info=None) L53
"Setup the Open Sky platform."

homeassistant/components/sensor/openweathermap.py
class OpenWeatherMapSensor(Entity) L86
  "Implementation of an OpenWeatherMap sensor."
  __init__(self, name, weather_data, sensor_type, temp_unit) L89
  "Initialize the sensor."
  name(self) L101
  "Return the name of the sensor."
  state(self) L106
  "Return the state of the device."
  unit_of_measurement(self) L111
  "Return the unit of measurement of this entity, if any."
  device_state_attributes(self) L116
  "Return the state attributes."
  update(self) L122
  "Get the latest data from OWM and updates the states."

class WeatherData(object) L166
  "Get the latest data from OpenWeatherMap."
  __init__(self, owm, forecast, latitude, longitude) L169
  "Initialize the data object."
  update(self) L179
  "Get the latest data from OpenWeatherMap."

setup_platform(hass, config, add_devices, discovery_info=None) L52
"Setup the OpenWeatherMap sensor."

homeassistant/components/sensor/pi_hole.py
class PiHoleSensor(Entity) L73
  "Representation of a Pi-Hole sensor."
  __init__(self, hass, api, name, variable) L76
  "Initialize a Pi-Hole sensor."
  name(self) L89
  "Return the name of the sensor."
  icon(self) L94
  "Icon to use in the frontend, if any."
  unit_of_measurement(self) L99
  "Return the unit the value is expressed in."
  state(self) L105
  "Return the state of the device."
  device_state_attributes(self) L111
  "Return the state attributes of the Pi-Hole."
  update(self) L117
  "Get the latest data from the Pi-Hole API."

class PiHoleAPI(object) L122
  "Get the latest data and update the states."
  __init__(self, host, use_ssl, verify_ssl) L125
  "Initialize the data object."
  update(self) L138
  "Get the latest data from the Pi-Hole."

setup_platform(hass, config, add_devices, discovery_info=None) L54
"Setup the Pi-Hole sensor."

homeassistant/components/sensor/pilight.py
class PilightSensor(Entity) L45
  "Representation of a sensor that can be updated using Pilight."
  __init__(self, hass, name, variable, payload, unit_of_measurement) L48
  "Initialize the sensor."
  should_poll(self) L60
  "No polling needed."
  name(self) L65
  "Return the name of the sensor."
  unit_of_measurement(self) L70
  "Return the unit this state is expressed in."
  state(self) L75
  "Return the state of the entity."
  _handle_code(self, call) L79
  "Handle received code by the pilight-daemon."

setup_platform(hass, config, add_devices, discovery_info=None) L34
"Set up Pilight Sensor."

homeassistant/components/sensor/plex.py
class PlexSensor(Entity) L55
  "Representation of a Plex now playing sensor."
  __init__(self, name, plex_url, plex_user, plex_password, plex_server) L58
  "Initialize the sensor."
  name(self) L79
  "Return the name of the sensor."
  state(self) L84
  "Return the state of the sensor."
  unit_of_measurement(self) L89
  "Return the unit this state is expressed in."
  device_state_attributes(self) L94
  "Return the state attributes."
  update(self) L99
  "Update method for Plex sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L41
"Setup the Plex sensor."

homeassistant/components/sensor/pocketcasts.py
class PocketCastsSensor(Entity) L51
  "Representation of a pocket casts sensor."
  __init__(self, api) L54
  "Initialize the sensor."
  update(self) L60
  "Update sensor values."
  name(self) L69
  "Return the name of the sensor."
  state(self) L74
  "Return the sensor state."
  icon(self) L79
  "Return the icon for the sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L34
"Setup the pocketcasts platform for sensors."

homeassistant/components/sensor/pushbullet.py
class PushBulletNotificationSensor(Entity) L58
  "Representation of a Pushbullet Sensor."
  __init__(self, pb, element) L61
  "Initialize the Pushbullet sensor."
  update(self) L68
  "Fetch the latest data from the sensor."
  name(self) L81
  "Return the name of the sensor."
  state(self) L86
  "Return the current state of the sensor."
  device_state_attributes(self) L91
  "Return all known attributes of the sensor."

class PushBulletNotificationProvider L96
  "Provider for an account, leading to one or more sensors."
  __init__(self, pb) L99
  "Start to retrieve pushes from the given Pushbullet instance."
  on_push(self, data) L109
  "Method to update the current data."
  data(self) L119
  "The current data stored in the provider."
  retrieve_pushes(self) L123
  "The method to run the daemon thread in."

setup_platform(hass, config, add_devices, discovery_info=None) L40
"Set up the Pushbllet Sensor platform."

homeassistant/components/sensor/pvoutput.py
class PvoutputSensor(Entity) L67
  "Representation of a PVOutput sensor."
  __init__(self, rest, name) L70
  "Initialize a PVOutput sensor."
  name(self) L84
  "Return the name of the sensor."
  state(self) L89
  "Return the state of the device."
  device_state_attributes(self) L97
  "Return the state attributes of the monitored installation."
  update(self) L110
  "Get the latest data from the PVOutput API and updates the state."

setup_platform(hass, config, add_devices, discovery_info=None) L43
"Set up the PVOutput sensor."

homeassistant/components/sensor/qnap.py
class QNAPStatsAPI(object) L183
  "Class to interface with the API."
  __init__(self, config) L186
  "Initialize the API wrapper."
  update(self) L204
  "Update API information and store locally."

class QNAPSensor(Entity) L216
  "Base class for a QNAP sensor."
  __init__(self, api, variable, variable_info, monitor_device=None) L219
  "Initialize the sensor."
  name(self) L229
  "Return the name of the sensor, if any."
  icon(self) L242
  "Icon to use in the frontend, if any."
  unit_of_measurement(self) L247
  "Return the unit the value is expressed in."
  update(self) L251
  "Get the latest data for the states."

class QNAPCPUSensor(QNAPSensor) L256
  "A QNAP sensor that monitors CPU stats."
  state(self) L260
  "Return the state of the sensor."

class QNAPMemorySensor(QNAPSensor) L268
  "A QNAP sensor that monitors memory stats."
  state(self) L272
  "Return the state of the sensor."
  device_state_attributes(self) L288
  "Return the state attributes."

class QNAPNetworkSensor(QNAPSensor) L298
  "A QNAP sensor that monitors network stats."
  state(self) L302
  "Return the state of the sensor."
  device_state_attributes(self) L316
  "Return the state attributes."

class QNAPSystemSensor(QNAPSensor) L331
  "A QNAP sensor that monitors overall system health."
  state(self) L335
  "Return the state of the sensor."
  device_state_attributes(self) L344
  "Return the state attributes."

class QNAPDriveSensor(QNAPSensor) L362
  "A QNAP sensor that monitors HDD/SSD drive stats."
  state(self) L366
  "Return the state of the sensor."
  name(self) L377
  "Return the name of the sensor, if any."
  device_state_attributes(self) L388
  "Return the state attributes."

class QNAPVolumeSensor(QNAPSensor) L400
  "A QNAP sensor that monitors storage volume stats."
  state(self) L404
  "Return the state of the sensor."
  device_state_attributes(self) L422
  "Return the state attributes."

setup_platform(hass, config, add_devices, discovery_info=None) L108
"Setup the QNAP NAS sensor."
round_nicely(number) L173
"Round a number based on its size (so it looks nice)."

homeassistant/components/sensor/random.py
class RandomSensor(Entity) L46
  "Representation of a Random number sensor."
  __init__(self, name, minimum, maximum, unit_of_measurement) L49
  "Initialize the sensor."
  name(self) L58
  "Return the name of the device."
  state(self) L63
  "Return the state of the device."
  icon(self) L68
  "Icon to use in the frontend, if any."
  unit_of_measurement(self) L73
  "Return the unit this state is expressed in."
  async_update(self) L78
  "Get a new number and updates the states."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L35
"Setup the Random number sensor."

homeassistant/components/sensor/rest.py
class RestSensor(Entity) L76
  "Implementation of a REST sensor."
  __init__(self, hass, rest, name, unit_of_measurement, value_template) L79
  "Initialize the REST sensor."
  name(self) L90
  "Return the name of the sensor."
  unit_of_measurement(self) L95
  "Return the unit the value is expressed in."
  state(self) L100
  "Return the state of the device."
  update(self) L104
  "Get the latest data from REST API and update the state."

class RestData(object) L118
  "Class for handling the data retrieval."
  __init__(self, method, resource, auth, headers, data, verify_ssl) L121
  "Initialize the data object."
  update(self) L128
  "Get the latest data from REST service with provided method."

setup_platform(hass, config, add_devices, discovery_info=None) L44
"Setup the RESTful sensor."

homeassistant/components/sensor/rflink.py
class RflinkSensor(RflinkDevice) L99
  "Representation of a Rflink sensor."
  __init__(self, device_id, hass, sensor_type, unit_of_measurement, **kwargs) L102
  "Handle sensor specific args and super init."
  _handle_event(self, event) L109
  "Domain specific event handler."
  unit_of_measurement(self) L114
  "Return measurement unit."
  state(self) L119
  "Return value."
  icon(self) L124
  "Return possible sensor specific icon."

lookup_unit_for_sensor_type(sensor_type) L46
"Get unit for sensor type."
devices_from_config(domain_config, hass=None) L57
"Parse configuration and add Rflink sensor devices."
async_setup_platform(hass, config, async_add_devices, discovery_info=None) L74
"Set up the Rflink platform."
add_new_device(event) L79
"Check if device is known, otherwise create device entity."

homeassistant/components/sensor/rfxtrx.py
class RfxtrxSensor(Entity) L103
  "Representation of a RFXtrx sensor."
  __init__(self, event, name, data_type, should_fire_event=False) L106
  "Initialize the sensor."
  __str__(self) L114
  "Return the name of the sensor."
  state(self) L119
  "Return the state of the sensor."
  name(self) L126
  "Get the name of the sensor."
  device_state_attributes(self) L131
  "Return the state attributes."
  unit_of_measurement(self) L138
  "Return the unit this state is expressed in."

setup_platform(hass, config, add_devices_callback, discovery_info=None) L31
"Set up the RFXtrx platform."
sensor_update(event) L58
"Callback for sensor updates from the RFXtrx gateway."

homeassistant/components/sensor/ring.py
class RingSensor(Entity) L66
  "A sensor implementation for Ring device."
  __init__(self, hass, data, sensor_type) L69
  "Initialize a sensor for Ring device."
  name(self) L83
  "Return the name of the sensor."
  state(self) L88
  "Return the state of the sensor."
  device_state_attributes(self) L93
  "Return the state attributes."
  icon(self) L113
  "Icon to use in the frontend, if any."
  unit_of_measurement(self) L118
  "Return the units of measurement."
  update(self) L122
  "Get the latest data and updates the state."

setup_platform(hass, config, add_devices, discovery_info=None) L44
"Set up a sensor for a Ring device."

homeassistant/components/sensor/sabnzbd.py
class SabnzbdSensor(Entity) L91
  "Representation of an SABnzbd sensor."
  __init__(self, sensor_type, sabnzb_client, client_name) L94
  "Initialize the sensor."
  name(self) L104
  "Return the name of the sensor."
  state(self) L109
  "Return the state of the sensor."
  unit_of_measurement(self) L114
  "Return the unit of measurement of this entity, if any."
  refresh_sabnzbd_data(self) L119
  "Call the throttled SABnzbd refresh method."
  update(self) L128
  "Get the latest data and updates the states."

setup_platform(hass, config, add_devices, discovery_info=None) L53
"Setup the SABnzbd sensors."

homeassistant/components/sensor/scrape.py
class ScrapeSensor(Entity) L63
  "Representation of a web scrape sensor."
  __init__(self, hass, rest, name, select, value_template, unit) L66
  "Initialize a web scrape sensor."
  name(self) L77
  "Return the name of the sensor."
  unit_of_measurement(self) L82
  "Return the unit the value is expressed in."
  state(self) L87
  "Return the state of the device."
  update(self) L91
  "Get the latest data from the source and updates the state."

setup_platform(hass, config, add_devices, discovery_info=None) L38
"Set up the Web scrape sensor."

homeassistant/components/sensor/sensehat.py
class SenseHatSensor(Entity) L71
  "Representation of a Sense HAT sensor."
  __init__(self, data, sensor_types) L74
  "Initialize the sensor."
  name(self) L84
  "Return the name of the sensor."
  state(self) L89
  "Return the state of the sensor."
  unit_of_measurement(self) L94
  "Return the unit the value is expressed in."
  update(self) L98
  "Get the latest data and updates the states."

class SenseHatData(object) L113
  "Get the latest data and update."
  __init__(self, is_hat_attached) L116
  "Initialize the data object."
  update(self) L124
  "Get the latest data from Sense HAT."

get_cpu_temp() L42
"Get CPU temperature."
get_average(temp_base) L49
"Use moving average to get better readings."
setup_platform(hass, config, add_devices, discovery_info=None) L61
"Setup the Sense HAT sensor platform."

homeassistant/components/sensor/serial_pm.py
class ParticulateMatterSensor(Entity) L60
  "Representation of an Particulate matter sensor."
  __init__(self, pmDataCollector, name, pmname) L63
  "Initialize a new PM sensor."
  name(self) L71
  "Return the name of the sensor."
  state(self) L76
  "Return the state of the sensor."
  unit_of_measurement(self) L81
  "Return the unit of measurement of this entity, if any."
  update(self) L85
  "Read from sensor and update the state."
  should_poll(self) L93
  "Sensor needs polling."

setup_platform(hass, config, add_devices, discovery_info=None) L30
"Set up the available PM sensors."

homeassistant/components/sensor/skybeacon.py
class SkybeaconHumid(Entity) L59
  "Representation of a Skybeacon humidity sensor."
  __init__(self, name, mon) L62
  "Initialize a sensor."
  name(self) L68
  "Return the name of the sensor."
  state(self) L73
  "Return the state of the device."
  unit_of_measurement(self) L78
  "Return the unit the value is expressed in."
  device_state_attributes(self) L83
  "Return the state attributes of the sensor."

class SkybeaconTemp(Entity) L91
  "Representation of a Skybeacon temperature sensor."
  __init__(self, name, mon) L94
  "Initialize a sensor."
  name(self) L100
  "Return the name of the sensor."
  state(self) L105
  "Return the state of the device."
  unit_of_measurement(self) L110
  "Return the unit the value is expressed in."
  device_state_attributes(self) L115
  "Return the state attributes of the sensor."

class Monitor(threading.Thread) L123
  "Connection handling."
  __init__(self, hass, mac, name) L126
  "Construct interface object."
  run(self) L137
  "Thread that keeps connection alive."
  _update(self, handle, value) L174
  "Notification callback from pygatt."
  terminate(self) L181
  "Signal runner to stop and join thread."

setup_platform(hass, config, add_devices, discovery_info=None) L40
"Set up the Skybeacon sensor."
monitor_stop(_service_or_event) L50
"Stop the monitor thread."

homeassistant/components/sensor/sleepiq.py
class SleepNumberSensor(sleepiq.SleepIQSensor) L28
  "Implementation of a SleepIQ sensor."
  __init__(self, sleepiq_data, bed_id, side) L31
  "Initialize the sensor."
  state(self) L42
  "Return the state of the sensor."
  icon(self) L47
  "Icon to use in the frontend, if any."
  update(self) L51
  "Get the latest data from SleepIQ and updates the states."

setup_platform(hass, config, add_devices, discovery_info=None) L13
"Setup the SleepIQ sensors."

homeassistant/components/sensor/sma.py
class SMAsensor(Entity) L144
  "Representation of a Bitcoin sensor."
  __init__(self, sensor_name, attr, sensor_defs) L147
  "Initialize the sensor."
  name(self) L156
  "Return the name of the sensor."
  state(self) L161
  "Return the state of the sensor."
  unit_of_measurement(self) L166
  "Return the unit the value is expressed in."
  device_state_attributes(self) L171
  "Return the state attributes of the sensor."
  poll(self) L176
  "SMA sensors are updated & don't poll."
  async_update_values(self, key_values) L180
  "Update this sensor using the data."

_check_sensor_schema(conf) L35
"Check sensors and attributes are valid."
async_setup_platform(hass, config, async_add_devices, discovery_info=None) L64
"Set up SMA WebConnect sensor."
async_close_session(event) L104
"Close the session."
async_sma(event) L117
"Update all the SMA sensors."

homeassistant/components/sensor/snmp.py
class SnmpSensor(Entity) L71
  "Representation of a SNMP sensor."
  __init__(self, data, name, unit_of_measurement) L74
  "Initialize the sensor."
  name(self) L83
  "Return the name of the sensor."
  state(self) L88
  "Return the state of the sensor."
  unit_of_measurement(self) L93
  "Return the unit the value is expressed in."
  update(self) L97
  "Get the latest data and updates the states."

class SnmpData(object) L103
  "Get the latest data and update the states."
  __init__(self, host, port, community, baseoid) L106
  "Initialize the data object."
  update(self) L115
  "Get the latest data from the remote SNMP capable host."

setup_platform(hass, config, add_devices, discovery_info=None) L43
"Set up the SNMP sensor."

homeassistant/components/sensor/sonarr.py
class SonarrSensor(Entity) L78
  "Implemention of the Sonarr sensor."
  __init__(self, hass, conf, sensor_type) L81
  "Create Sonarr entity."
  update(self) L110
  "Update the data for the sensor."
  name(self) L170
  "Return the name of the sensor."
  state(self) L175
  "Return sensor state."
  available(self) L180
  "Return sensor availability."
  unit_of_measurement(self) L185
  "Return the unit of the sensor."
  device_state_attributes(self) L190
  "Return the state attributes of the sensor."
  icon(self) L232
  "Return the icon of the sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L69
"Set up the Sonarr platform."
get_date(zone, offset=0) L237
"Get date based on timezone and offset of days."
to_unit(value, unit) L245
"Convert bytes to give unit."

homeassistant/components/sensor/speedtest.py
class SpeedtestSensor(Entity) L82
  "Implementation of a speedtest.net sensor."
  __init__(self, speedtest_data, sensor_type) L85
  "Initialize the sensor."
  name(self) L94
  "Return the name of the sensor."
  state(self) L99
  "Return the state of the device."
  unit_of_measurement(self) L104
  "Return the unit of measurement of this entity, if any."
  icon(self) L109
  "Return icon."
  update(self) L113
  "Get the latest data and update the states."
  async_added_to_hass(self) L127
  "Called when entity is about to be added to hass."

class SpeedtestData(object) L135
  "Get the latest data from speedtest.net."
  __init__(self, hass, config) L138
  "Initialize the data object."
  update(self, now) L149
  "Get the latest data from speedtest.net."

setup_platform(hass, config, add_devices, discovery_info=None) L61
"Set up the Speedtest sensor."
update(call=None) L73
"Update service for manual updates."

homeassistant/components/sensor/statistics.py
class StatisticsSensor(Entity) L58
  "Representation of a Statistics sensor."
  __init__(self, hass, entity_id, name, sampling_size) L61
  "Initialize the Statistics sensor."
  async_stats_sensor_state_listener(entity, old_state, new_state) L82
  "Called when the sensor changes state."
  name(self) L99
  "Return the name of the sensor."
  state(self) L104
  "Return the state of the sensor."
  unit_of_measurement(self) L109
  "Return the unit the value is expressed in."
  should_poll(self) L114
  "No polling needed."
  device_state_attributes(self) L119
  "Return the state attributes of the sensor."
  icon(self) L136
  "Return the icon to use in the frontend, if any."
  async_update(self) L141
  "Get the latest data and updates the states."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L47
"Set up the Statistics sensor."

homeassistant/components/sensor/steam_online.py
class SteamSensor(Entity) L48
  "A class for the Steam account."
  __init__(self, account, steamod) L51
  "Initialize the sensor."
  name(self) L58
  "Return the name of the sensor."
  entity_id(self) L63
  "Return the entity ID."
  state(self) L68
  "Return the state of the sensor."
  update(self) L73
  "Update device state."
  device_state_attributes(self) L99
  "Return the state attributes."
  entity_picture(self) L104
  "Avatar of the account."
  icon(self) L109
  "Return the icon to use in the frontend."

setup_platform(hass, config, add_devices, discovery_info=None) L39
"Setup the Steam platform."

homeassistant/components/sensor/supervisord.py
class SupervisorProcessSensor(Entity) L43
  "Representation of a supervisor-monitored process."
  __init__(self, info, server) L46
  "Initialize the sensor."
  name(self) L53
  "Return the name of the sensor."
  state(self) L58
  "Return the state of the sensor."
  update(self) L62
  "Update device state."
  device_state_attributes(self) L68
  "Return the state attributes."

setup_platform(hass, config, add_devices, discovery_info=None) L27
"Setup the Supervisord platform."

homeassistant/components/sensor/swiss_hydrological_data.py
class SwissHydrologicalDataSensor(Entity) L74
  "Implementation of an Swiss hydrological sensor."
  __init__(self, name, data) L77
  "Initialize the sensor."
  name(self) L85
  "Return the name of the sensor."
  unit_of_measurement(self) L90
  "Return the unit of measurement of this entity, if any."
  state(self) L98
  "Return the state of the sensor."
  device_state_attributes(self) L106
  "Return the state attributes."
  icon(self) L136
  "Icon to use in the frontend, if any."
  update(self) L140
  "Get the latest data and update the states."

class HydrologicalData(object) L150
  "The Class for handling the data retrieval."
  __init__(self, station) L153
  "Initialize the data object."
  update(self) L159
  "Get the latest data from hydrodata.ch."

setup_platform(hass, config, add_devices, discovery_info=None) L53
"Setup the Swiss hydrological sensor."

homeassistant/components/sensor/swiss_public_transport.py
class SwissPublicTransportSensor(Entity) L68
  "Implementation of an Swiss public transport sensor."
  __init__(self, data, journey, name) L71
  "Initialize the sensor."
  name(self) L80
  "Return the name of the sensor."
  state(self) L85
  "Return the state of the sensor."
  device_state_attributes(self) L90
  "Return the state attributes."
  icon(self) L104
  "Icon to use in the frontend, if any."
  update(self) L108
  "Get the latest data from opendata.ch and update the states."

class PublicTransportData(object) L118
  "The Class for handling the data retrieval."
  __init__(self, journey) L121
  "Initialize the data object."
  update(self) L128
  "Get the latest data from opendata.ch."

setup_platform(hass, config, add_devices, discovery_info=None) L46
"Get the Swiss public transport sensor."

homeassistant/components/sensor/synologydsm.py
class SynoApi L137
  "Class to interface with API."
  __init__(self, host, port, username, password, temp_unit) L141
  "Constructor of the API wrapper class."
  update(self) L159
  "Update function for updating api information."

class SynoNasSensor(Entity) L164
  "Representation of a Synology Nas Sensor."
  __init__(self, api, variable, variableInfo, monitor_device=None) L167
  "Initialize the sensor."
  name(self) L177
  "Return the name of the sensor, if any."
  icon(self) L185
  "Icon to use in the frontend, if any."
  unit_of_measurement(self) L190
  "Return the unit the value is expressed in."
  update(self) L198
  "Get the latest data for the states."

class SynoNasUtilSensor(SynoNasSensor) L204
  "Representation a Synology Utilisation Sensor."
  state(self) L208
  "Return the state of the sensor."

class SynoNasStorageSensor(SynoNasSensor) L226
  "Representation a Synology Utilisation Sensor."
  state(self) L230
  "Return the state of the sensor."

setup_platform(hass, config, add_devices_callback, discovery_info=None) L88
"Setup the Synology NAS Sensor."
run_setup(event) L91
"Wait until HASS is fully initialized before creating."

homeassistant/components/sensor/systemmonitor.py
class SystemMonitorSensor(Entity) L81
  "Implementation of a system monitor sensor."
  __init__(self, sensor_type, argument='') L84
  "Initialize the sensor."
  name(self) L94
  "Return the name of the sensor."
  icon(self) L99
  "Icon to use in the frontend, if any."
  state(self) L104
  "Return the state of the device."
  unit_of_measurement(self) L109
  "Return the unit of measurement of this entity, if any."
  update(self) L113
  "Get the latest system information."

setup_platform(hass, config, add_devices, discovery_info=None) L70
"Set up the system monitor sensors."

homeassistant/components/sensor/tado.py
class TadoSensor(Entity) L83
  "Representation of a tado Sensor."
  __init__(self, store, zone_name, zone_id, zone_variable, data_id) L86
  "Initialization of TadoSensor class."
  unique_id(self) L101
  "Return the unique id."
  name(self) L106
  "Return the name of the sensor."
  state(self) L111
  "Return the state of the sensor."
  device_state_attributes(self) L116
  "Return the state attributes."
  unit_of_measurement(self) L121
  "Return the unit of measurement."
  icon(self) L131
  "Icon for the sensor."
  update(self) L138
  "Update method called when should_poll is true."

setup_platform(hass, config, add_devices, discovery_info=None) L25
"Set up the sensor platform."
create_zone_sensor(tado, zone, name, zone_id, variable) L55
"Create a zone sensor."
create_device_sensor(tado, device, name, device_id, variable) L69
"Create a device sensor."

homeassistant/components/sensor/tcp.py
class TcpSensor(Entity) L49
  "Implementation of a TCP socket based sensor."
  __init__(self, hass, config) L54
  "Set all the config values if they exist and get initial state."
  name(self) L77
  "Return the name of this sensor."
  state(self) L85
  "Return the state of the device."
  unit_of_measurement(self) L90
  "Return the unit of measurement of this entity."
  update(self) L94
  "Get the latest value for this sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L44
"Set up the TCP Sensor."

homeassistant/components/sensor/ted5000.py
class Ted5000Sensor(Entity) L57
  "Implementation of a Ted5000 sensor."
  __init__(self, gateway, name, mtu, unit) L60
  "Initialize the sensor."
  name(self) L70
  "The name of the sensor."
  unit_of_measurement(self) L75
  "Return the unit the value is expressed in."
  state(self) L80
  "Return the state of the resources."
  update(self) L87
  "Get the latest data from REST API."

class Ted5000Gateway(object) L92
  "The class for handling the data retrieval."
  __init__(self, url) L95
  "Initialize the data object."
  update(self) L101
  "Get the latest data from the Ted5000 XML API."

setup_platform(hass, config, add_devices, discovery_info=None) L36
"Setup the Ted5000 sensor."

homeassistant/components/sensor/tellduslive.py
class TelldusLiveSensor(TelldusLiveEntity) L45
  "Representation of a Telldus Live sensor."
  device_id(self) L49
  "Return id of the device."
  _type(self) L54
  "Return the type of the sensor."
  _value(self) L59
  "Return value of the sensor."
  _value_as_temperature(self) L64
  "Return the value as temperature."
  _value_as_luminance(self) L69
  "Return the value as luminance."
  _value_as_humidity(self) L74
  "Return the value as humidity."
  name(self) L79
  "Return the name of the sensor."
  state(self) L86
  "Return the state of the sensor."
  quantity_name(self) L100
  "Name of quantity."
  unit_of_measurement(self) L106
  "Return the unit of measurement."
  icon(self) L112
  "Return the icon."

setup_platform(hass, config, add_devices, discovery_info=None) L38
"Setup Tellstick sensors."

homeassistant/components/sensor/tellstick.py
class TellstickSensor(Entity) L96
  "Representation of a Tellstick sensor."
  __init__(self, name, tellcore_sensor, datatype, sensor_info) L99
  "Initialize the sensor."
  name(self) L109
  "Return the name of the sensor."
  state(self) L114
  "Return the state of the sensor."
  unit_of_measurement(self) L119
  "Return the unit of measurement of this entity, if any."
  update(self) L123
  "Update tellstick sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L41
"Setup the Tellstick sensors."

homeassistant/components/sensor/temper.py
class TemperSensor(Entity) L65
  "Representation of a Temper temperature sensor."
  __init__(self, temper_device, temp_unit, name, scaling) L68
  "Initialize the sensor."
  name(self) L78
  "Return the name of the temperature sensor."
  state(self) L83
  "Return the state of the entity."
  unit_of_measurement(self) L88
  "Return the unit of measurement of this entity, if any."
  set_temper_device(self, temper_device) L92
  "Assign the underlying device for this sensor."
  update(self) L102
  "Retrieve latest state."

get_temper_devices() L30
"Scan the Temper devices from temperusb."
setup_platform(hass, config, add_devices, discovery_info=None) L37
"Setup the Temper sensors."
reset_devices() L54
"Re-scan for underlying Temper sensors and assign them to our devices."

homeassistant/components/sensor/template.py
class SensorTemplate(Entity) L77
  "Representation of a Template Sensor."
  __init__(self, hass, device_id, friendly_name, unit_of_measurement, state_template, icon_template, entity_ids) L80
  "Initialize the sensor."
  async_added_to_hass(self) L95
  "Register callbacks."
  template_sensor_state_listener(entity, old_state, new_state) L102
  "Called when the target device changes state."
  template_sensor_startup(event) L107
  "Update template on startup."
  name(self) L118
  "Return the name of the sensor."
  state(self) L123
  "Return the state of the sensor."
  icon(self) L128
  "Return the icon to use in the frontend, if any."
  unit_of_measurement(self) L133
  "Return the unit_of_measurement of the device."
  should_poll(self) L138
  "No polling needed."
  async_update(self) L143
  "Update the state from the template."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L42
"Setup the template sensors."

homeassistant/components/sensor/thinkingcleaner.py
class ThinkingCleanerSensor(Entity) L78
  "Representation of a ThinkingCleaner Sensor."
  __init__(self, tc_object, sensor_type, update_devices) L81
  "Initialize the ThinkingCleaner."
  name(self) L91
  "Return the name of the sensor."
  icon(self) L96
  "Icon to use in the frontend, if any."
  state(self) L101
  "Return the state of the device."
  unit_of_measurement(self) L106
  "Return the unit of measurement of this entity, if any."
  update(self) L110
  "Update the sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L56
"Set up the ThinkingCleaner platform."
update_devices() L64
"Update all devices."

homeassistant/components/sensor/time_date.py
class TimeDateSensor(Entity) L53
  "Implementation of a Time and Date sensor."
  __init__(self, option_type) L56
  "Initialize the sensor."
  name(self) L63
  "Return the name of the sensor."
  state(self) L68
  "Return the state of the sensor."
  icon(self) L73
  "Icon to use in the frontend, if any."
  async_update(self) L83
  "Get the latest data and updates the states."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L39
"Setup the Time and Date sensor."

homeassistant/components/sensor/torque.py
class TorqueReceiveDataView(HomeAssistantView) L61
  "Handle data from Torque requests."
  __init__(self, email, vehicle, sensors, add_devices) L67
  "Initialize a Torque view."
  get(self, request) L75
  "Handle Torque data request."

class TorqueSensor(Entity) L111
  "Representation of a Torque sensor."
  __init__(self, name, unit) L114
  "Initialize the sensor."
  name(self) L121
  "Return the name of the sensor."
  unit_of_measurement(self) L126
  "Return the unit of measurement."
  state(self) L131
  "Return the state of the sensor."
  icon(self) L136
  "Return the default icon of the sensor."
  async_on_update(self, value) L141
  "Receive an update."

convert_pid(value) L44
"Convert pid from hex string to integer."
setup_platform(hass, config, add_devices, discovery_info=None) L50
"Setup Torque platform."

homeassistant/components/sensor/transmission.py
class TransmissionSensor(Entity) L77
  "Representation of a Transmission sensor."
  __init__(self, sensor_type, transmission_client, client_name) L80
  "Initialize the sensor."
  name(self) L90
  "Return the name of the sensor."
  state(self) L95
  "Return the state of the sensor."
  unit_of_measurement(self) L100
  "Return the unit of measurement of this entity, if any."
  refresh_transmission_data(self) L105
  "Call the throttled Transmission refresh method."
  update(self) L115
  "Get the latest data from Transmission and updates the state."

setup_platform(hass, config, add_devices, discovery_info=None) L46
"Setup the Transmission sensors."

homeassistant/components/sensor/twitch.py
class TwitchSensor(Entity) L42
  "Representation of an Twitch channel."
  __init__(self, channel) L45
  "Initialize the sensor."
  should_poll(self) L55
  "Device should be polled."
  name(self) L60
  "Return the name of the sensor."
  state(self) L65
  "Return the state of the sensor."
  entity_picture(self) L70
  "Return preview of current game."
  update(self) L75
  "Update device state."
  device_state_attributes(self) L89
  "Return the state attributes."
  icon(self) L98
  "Icon to use in the frontend, if any."

setup_platform(hass, config, add_devices, discovery_info=None) L35
"Setup the Twitch platform."

homeassistant/components/sensor/uber.py
class UberSensor(Entity) L67
  "Implementation of an Uber sensor."
  __init__(self, sensorType, products, product_id, product) L70
  "Initialize the Uber sensor."
  name(self) L96
  "Return the name of the sensor."
  state(self) L103
  "Return the state of the sensor."
  unit_of_measurement(self) L108
  "Return the unit of measurement of this entity, if any."
  device_state_attributes(self) L113
  "Return the state attributes."
  icon(self) L152
  "Icon to use in the frontend, if any."
  update(self) L156
  "Get the latest data from the Uber API and update the states."

class UberEstimate(object) L172
  "The class for handling the time and price estimate."
  __init__(self, session, start_latitude, start_longitude, end_latitude=None, end_longitude=None) L175
  "Initialize the UberEstimate object."
  update(self) L187
  "Get the latest product info and estimates from the Uber API."

setup_platform(hass, config, add_devices, discovery_info=None) L43
"Set up the Uber sensor."

homeassistant/components/sensor/ups.py
class UPSSensor(Entity) L58
  "UPS Sensor."
  __init__(self, session, name, interval) L61
  "Initialize the sensor."
  name(self) L71
  "Return the name of the sensor."
  state(self) L76
  "Return the state of the sensor."
  _update(self) L80
  "Update device state."
  device_state_attributes(self) L98
  "Return the state attributes."
  icon(self) L103
  "Icon to use in the frontend."

setup_platform(hass, config, add_devices, discovery_info=None) L42
"Setup the UPS platform."

homeassistant/components/sensor/usps.py
class USPSSensor(Entity) L57
  "USPS Sensor."
  __init__(self, session, name, interval) L60
  "Initialize the sensor."
  name(self) L72
  "Return the name of the sensor."
  state(self) L77
  "Return the state of the sensor."
  _update(self) L81
  "Update device state."
  device_state_attributes(self) L98
  "Return the state attributes."
  icon(self) L103
  "Icon to use in the frontend."

setup_platform(hass, config, add_devices, discovery_info=None) L41
"Setup the USPS platform."

homeassistant/components/sensor/vasttrafik.py
class VasttrafikDepartureSensor(Entity) L63
  "Implementation of a Vasttrafik Departure Sensor."
  __init__(self, vasttrafik, planner, name, departure, heading, delay) L66
  "Initialize the sensor."
  name(self) L79
  "Return the name of the sensor."
  icon(self) L84
  "Return the icon for the frontend."
  device_state_attributes(self) L89
  "Return the state attributes."
  state(self) L103
  "Return the next departure time."
  update(self) L116
  "Get the departure board."

setup_platform(hass, config, add_devices, discovery_info=None) L44
"Setup the departure sensor."

homeassistant/components/sensor/vera.py
class VeraSensor(VeraDevice, Entity) L32
  "Representation of a Vera Sensor."
  __init__(self, vera_device, controller) L35
  "Initialize the sensor."
  state(self) L44
  "Return the name of the sensor."
  unit_of_measurement(self) L49
  "Return the unit of measurement of this entity, if any."
  update(self) L60
  "Update the state."

setup_platform(hass, config, add_devices, discovery_info=None) L25
"Perform the setup for Vera controller devices."

homeassistant/components/sensor/verisure.py
class VerisureThermometer(Entity) L50
  "Representation of a Verisure thermometer."
  __init__(self, device_id) L53
  "Initialize the sensor."
  name(self) L58
  "Return the name of the device."
  state(self) L64
  "Return the state of the device."
  available(self) L70
  "Return True if entity is available."
  unit_of_measurement(self) L75
  "Return the unit of measurement of this entity."
  update(self) L79
  "Update the sensor."

class VerisureHygrometer(Entity) L84
  "Representation of a Verisure hygrometer."
  __init__(self, device_id) L87
  "Initialize the sensor."
  name(self) L92
  "Return the name of the sensor."
  state(self) L98
  "Return the state of the sensor."
  available(self) L104
  "Return True if entity is available."
  unit_of_measurement(self) L109
  "Return the unit of measurement of this sensor."
  update(self) L113
  "Update the sensor."

class VerisureMouseDetection(Entity) L118
  "Representation of a Verisure mouse detector."
  __init__(self, device_id) L121
  "Initialize the sensor."
  name(self) L126
  "Return the name of the sensor."
  state(self) L132
  "Return the state of the sensor."
  available(self) L137
  "Return True if entity is available."
  unit_of_measurement(self) L142
  "Return the unit of measurement of this sensor."
  update(self) L146
  "Update the sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L18
"Setup the Verisure platform."

homeassistant/components/sensor/volvooncall.py
class VolvoSensor(VolvoEntity) L22
  "Representation of a Volvo sensor."
  state(self) L26
  "Return the state of the sensor."
  unit_of_measurement(self) L35
  "Return the unit of measurement."
  icon(self) L40
  "Return the icon."

setup_platform(hass, config, add_devices, discovery_info=None) L15
"Setup Volvo sensors."

homeassistant/components/sensor/waqi.py
class WaqiSensor(Entity) L70
  "Implementation of a WAQI sensor."
  __init__(self, data, station_id) L73
  "Initialize the sensor."
  name(self) L81
  "Return the name of the sensor."
  station_name(self) L89
  "Return the name of the station."
  icon(self) L97
  "Icon to use in the frontend, if any."
  state(self) L102
  "Return the state of the device."
  unit_of_measurement(self) L110
  "Return the unit of measurement of this entity, if any."
  device_state_attributes(self) L115
  "Return the state attributes of the last update."
  update(self) L145
  "Get the latest data and updates the states."

class WaqiData(object) L151
  "Get the latest data and update the states."
  __init__(self, station_id, token) L154
  "Initialize the data object."
  update(self) L161
  "Get the data from World Air Quality Index and updates the states."

setup_platform(hass, config, add_devices, discovery_info=None) L51
"Set up the requested World Air Quality Index locations."

homeassistant/components/sensor/wink.py
class WinkSensorDevice(WinkDevice, Entity) L49
  "Representation of a Wink sensor."
  __init__(self, wink, hass) L52
  "Initialize the Wink device."
  state(self) L62
  "Return the state."
  unit_of_measurement(self) L82
  "Return the unit of measurement of this entity, if any."

setup_platform(hass, config, add_devices, discovery_info=None) L19
"Set up the Wink platform."

homeassistant/components/sensor/worldclock.py
class WorldClockSensor(Entity) L42
  "Representation of a World clock sensor."
  __init__(self, time_zone, name) L45
  "Initialize the sensor."
  name(self) L52
  "Return the name of the device."
  state(self) L57
  "Return the state of the device."
  icon(self) L62
  "Icon to use in the frontend, if any."
  async_update(self) L67
  "Get the time and updates the states."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L33
"Set up the World clock sensor."

homeassistant/components/sensor/wsdot.py
class WashingtonStateTransportSensor(Entity) L59
  "Sensor that reads the WSDOT web API."
  __init__(self, name, access_code) L70
  "Initialize the sensor."
  name(self) L78
  "Return the name of the sensor."
  state(self) L83
  "Return the state of the sensor."
  icon(self) L88
  "Icon to use in the frontend, if any."

class WashingtonStateTravelTimeSensor(WashingtonStateTransportSensor) L93
  "Travel time sensor from WSDOT."
  __init__(self, name, access_code, travel_time_id) L100
  "Construct a travel time sensor."
  update(self) L105
  "Get the latest data from WSDOT."
  device_state_attributes(self) L118
  "Return other details about the sensor state."
  unit_of_measurement(self) L130
  "Return the unit this state is expressed in."

setup_platform(hass, config, add_devices, discovery_info=None) L45
"Get the WSDOT sensor."
_parse_wsdot_timestamp(timestamp) L135
"Convert WSDOT timestamp to datetime."

homeassistant/components/sensor/wunderground.py
class WUndergroundSensor(Entity) L135
  "Implementing the WUnderground sensor."
  __init__(self, rest, condition) L138
  "Initialize the sensor."
  name(self) L144
  "Return the name of the sensor."
  state(self) L149
  "Return the state of the sensor."
  device_state_attributes(self) L176
  "Return the state attributes."
  entity_picture(self) L197
  "Return the entity picture."
  unit_of_measurement(self) L204
  "Return the units of measurement."
  update(self) L208
  "Update current conditions."

class WUndergroundData(object) L216
  "Get data from WUnderground."
  __init__(self, hass, api_key, pws_id, lang) L219
  "Initialize the data object."
  _build_url(self, baseurl=_RESOURCE) L230
  update(self) L240
  "Get the latest data from WUnderground."
  update_alerts(self) L254
  "Get the latest alerts data from WUnderground."

setup_platform(hass, config, add_devices, discovery_info=None) L114
"Setup the WUnderground sensor."

homeassistant/components/sensor/xbox_live.py
class XboxSensor(Entity) L48
  "A class for the Xbox account."
  __init__(self, hass, api, xuid) L51
  "Initialize the sensor."
  name(self) L70
  "Return the name of the sensor."
  state(self) L75
  "Return the state of the sensor."
  device_state_attributes(self) L80
  "Return the state attributes."
  entity_picture(self) L92
  "Avatar of the account."
  icon(self) L97
  "Return the icon to use in the frontend."
  update(self) L101
  "Update state data from Xbox API."

setup_platform(hass, config, add_devices, discovery_info=None) L31
"Set up the Xbox platform."

homeassistant/components/sensor/yahoo_finance.py
class YahooFinanceSensor(Entity) L59
  "Representation of a Yahoo Finance sensor."
  __init__(self, data, symbol) L62
  "Initialize the sensor."
  name(self) L72
  "Return the name of the sensor."
  unit_of_measurement(self) L77
  "Return the unit of measurement of this entity, if any."
  state(self) L82
  "Return the state of the sensor."
  device_state_attributes(self) L87
  "Return the state attributes."
  icon(self) L98
  "Return the icon to use in the frontend, if any."
  update(self) L102
  "Get the latest data and updates the states."

class YahooFinanceData(object) L109
  "Get data from Yahoo Finance."
  __init__(self, symbol) L112
  "Initialize the data object."
  update(self) L124
  "Get the latest data and updates the states."

setup_platform(hass, config, add_devices, discovery_info=None) L42
"Set up the Yahoo Finance sensor."

homeassistant/components/sensor/yr.py
class YrSensor(Entity) L91
  "Representation of an Yr.no sensor."
  __init__(self, sensor_type) L94
  "Initialize the sensor."
  name(self) L103
  "Return the name of the sensor."
  state(self) L108
  "Return the state of the device."
  should_poll(self) L113
  "No polling needed."
  entity_picture(self) L118
  "Weather symbol if type is symbol."
  device_state_attributes(self) L126
  "Return the state attributes."
  unit_of_measurement(self) L133
  "Return the unit of measurement of this entity, if any."

class YrData(object) L138
  "Get the latest data and updates the states."
  __init__(self, hass, coordinates, devices) L141
  "Initialize the data object."
  async_update(self, *_) L152
  "Get the latest data from yr.no."
  try_again(err) L156
  "Retry in 15 minutes."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L64
"Setup the Yr.no sensor."

homeassistant/components/sensor/yweather.py
class YahooWeatherSensor(Entity) L100
  "Implementation of an Yahoo!"
  __init__(self, weather_data, name, forecast, sensor_type) L103
  "Initialize the sensor."
  name(self) L118
  "Return the name of the sensor."
  state(self) L123
  "Return the state of the device."
  unit_of_measurement(self) L128
  "Return the unit of measurement of this entity, if any."
  entity_picture(self) L133
  "Return the entity picture to use in the frontend, if any."
  device_state_attributes(self) L141
  "Return the state attributes."
  update(self) L147
  "Get the latest data from Yahoo!"

class YahooWeatherData(object) L181
  "Handle yahoo api object and limit updates."
  __init__(self, woeid, temp_unit) L184
  "Initialize the data object."
  yahoo(self) L192
  "Return yahoo api object."
  update(self) L197
  "Get the latest data from yahoo."

setup_platform(hass, config, add_devices, discovery_info=None) L54
"Setup the Yahoo!"

homeassistant/components/sensor/zabbix.py
class ZabbixTriggerCountSensor(Entity) L92
  "Get the active trigger count for all Zabbix monitored hosts."
  __init__(self, zApi, name='Zabbix') L95
  "Initiate Zabbix sensor."
  name(self) L103
  "Return the name of the sensor."
  state(self) L108
  "Return the state of the sensor."
  unit_of_measurement(self) L113
  "Return the units of measurement."
  _call_zabbix_api(self) L117
  update(self) L123
  "Update the sensor."
  device_state_attributes(self) L130
  "Return the state attributes of the device."

class ZabbixSingleHostTriggerCountSensor(ZabbixTriggerCountSensor) L135
  "Get the active trigger count for a single Zabbix monitored host."
  __init__(self, zApi, hostid, name=None) L138
  "Initiate Zabbix sensor."
  _call_zabbix_api(self) L148

class ZabbixMultipleHostTriggerCountSensor(ZabbixTriggerCountSensor) L156
  "Get the active trigger count for specified Zabbix monitored hosts."
  __init__(self, zApi, hostids, name=None) L159
  "Initiate Zabbix sensor."
  _call_zabbix_api(self) L169

setup_platform(hass, config, add_devices, discovery_info=None) L39
"Set up the Zabbix sensor platform."

homeassistant/components/sensor/zamg.py
class ZamgSensor(Entity) L92
  "Implementation of a ZAMG sensor."
  __init__(self, probe, variable, name) L95
  "Initialize the sensor."
  update(self) L102
  "Delegate update to probe."
  name(self) L107
  "Return the name of the sensor."
  state(self) L112
  "Return the state of the sensor."
  unit_of_measurement(self) L117
  "Return the unit of measurement of this entity, if any."
  device_state_attributes(self) L122
  "Return the state attributes."

class ZamgData(object) L131
  "The class for handling the data retrieval."
  __init__(self, logger, station_id) L139
  "Initialize the probe."
  last_update(self) L146
  "Return the timestamp of the most recent data."
  current_observations(cls) L154
  "Fetch the latest CSV data."
  update(self) L166
  "Get the latest data from ZAMG."
  get_data(self, variable) L187
  "Generic accessor for data."

setup_platform(hass, config, add_devices, discovery_info=None) L68
"Set up the ZAMG sensor platform."
_get_zamg_stations() L192
"Return {CONF_STATION: (lat, lon)} for all stations, for auto-config."
zamg_stations(cache_dir) L211
"Return {CONF_STATION: (lat, lon)} for all stations, for auto-config."
closest_station(lat, lon, cache_dir) L227
"Return the ZONE_ID.WMO_ID of the closest station to our lat/lon."
comparable_dist(zamg_id) L233
"A fast key function for psudeo-distance from lat/lon."

homeassistant/components/sensor/zha.py
class Sensor(zha.Entity) L56
  "Base ZHA sensor."
  __init__(self, **kwargs) L63
  "Initialize ZHA sensor."
  state(self) L68
  "Return the state of the entity."
  attribute_updated(self, attribute, value) L74
  "Handle attribute update from device."

class TemperatureSensor(Sensor) L82
  "ZHA temperature sensor."
  unit_of_measurement(self) L88
  "Return the unit of measurement of this entityy."
  state(self) L93
  "Return the state of the entity."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L21
"Setup Zigbee Home Automation sensors."
make_sensor(discovery_info) L32
"Factory function for ZHA sensors."

homeassistant/components/sensor/zigbee.py
class ZigBeeTemperatureSensor(Entity) L50
  "Representation of XBee Pro temperature sensor."
  __init__(self, hass, config) L53
  "Initialize the sensor."
  name(self) L59
  "Return the name of the sensor."
  state(self) L64
  "Return the state of the sensor."
  unit_of_measurement(self) L69
  "Unit the value is expressed in."
  update(self, *args) L73
  "Get the latest data."

setup_platform(hass, config, add_devices, discovery_info=None) L33
"Setup the ZigBee platform."

homeassistant/components/sensor/zoneminder.py
class ZMSensorMonitors(Entity) L50
  "Get the status of each ZoneMinder monitor."
  __init__(self, monitor_id, monitor_name) L53
  "Initiate monitor sensor."
  name(self) L60
  "Return the name of the sensor."
  state(self) L65
  "Return the state of the sensor."
  update(self) L69
  "Update the sensor."

class ZMSensorEvents(Entity) L80
  "Get the number of events for each monitor."
  __init__(self, monitor_id, monitor_name, include_archived) L83
  "Initiate event sensor."
  name(self) L91
  "Return the name of the sensor."
  unit_of_measurement(self) L96
  "Return the unit of measurement of this entity, if any."
  state(self) L101
  "Return the state of the sensor."
  update(self) L105
  "Update the sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L31
"Set up the ZoneMinder sensor platform."

homeassistant/components/sensor/zwave.py
class ZWaveSensor(zwave.ZWaveDeviceEntity) L32
  "Representation of a Z-Wave sensor."
  __init__(self, values) L35
  "Initialize the sensor."
  update_properties(self) L40
  "Callback on data changes for node values."
  force_update(self) L46
  "Return force_update."
  state(self) L51
  "Return the state of the sensor."
  unit_of_measurement(self) L56
  "Return the unit of measurement the value is expressed in."

class ZWaveMultilevelSensor(ZWaveSensor) L61
  "Representation of a multi level sensor Z-Wave sensor."
  state(self) L65
  "Return the state of the sensor."
  unit_of_measurement(self) L75
  "Return the unit the value is expressed in."

class ZWaveAlarmSensor(ZWaveSensor) L85
  "Representation of a Z-Wave sensor that sends Alarm alerts."

get_device(node, values, **kwargs) L18
"Create zwave entity device."

homeassistant/components/shell_command.py
setup(hass, config) L28
"Setup the shell_command component."
service_handler(call) L34
"Execute a shell command service."

homeassistant/components/sleepiq.py
class SleepIQData(object) L78
  "Gets the latest data from SleepIQ."
  __init__(self, client) L81
  "Initialize the data object."
  update(self) L89
  "Get the latest data from SleepIQ."

class SleepIQSensor(Entity) L97
  "Implementation of a SleepIQ sensor."
  __init__(self, sleepiq_data, bed_id, side) L100
  "Initialize the sensor."
  name(self) L113
  "Return the name of the sensor."
  update(self) L119
  "Get the latest data from SleepIQ and updates the states."

setup(hass, config) L49
"Setup SleepIQ."

homeassistant/components/splunk.py
setup(hass, config) L36
"Setup the Splunk component."
splunk_event_listener(event) L53
"Listen for new messages on the bus and sends them to Splunk."

homeassistant/components/statsd.py
setup(hass, config) L41
"Setup the StatsD component."
statsd_event_listener(event) L54
"Listen for new messages on the bus and sends them to StatsD."

homeassistant/components/sun.py
class Sun(Entity) L262
  "Representation of the Sun."
  __init__(self, hass, location) L267
  "Initialize the sun."
  name(self) L279
  "Return the name."
  state(self) L284
  "Return the state of the sun."
  state_attributes(self) L292
  "Return the state attributes of the sun."
  next_change(self) L306
  "Datetime when the next change to the state is."
  get_next_solar_event(callable_on_astral_location, utc_point_in_time, mod, increment) L312
  "Calculate sun state at a point in UTC time."
  update_as_of(self, utc_point_in_time) L329
  "Update the attributes containing solar events."
  update_sun_position(self, utc_point_in_time) L344
  "Calculate the position of the sun."
  point_in_time_listener(self, now) L358
  "Called when the state of the sun has changed."
  timer_update(self, time) L368
  "Needed to update solar elevation and azimuth."

is_on(hass, entity_id=None) L47
"Test if the sun is currently up based on the statemachine."
next_dawn(hass, entity_id=None) L54
"Local datetime object of the next dawn."
next_dawn_utc(hass, entity_id=None) L64
"UTC datetime object of the next dawn."
next_dusk(hass, entity_id=None) L82
"Local datetime object of the next dusk."
next_dusk_utc(hass, entity_id=None) L92
"UTC datetime object of the next dusk."
next_midnight(hass, entity_id=None) L110
"Local datetime object of the next midnight."
next_midnight_utc(hass, entity_id=None) L120
"UTC datetime object of the next midnight."
next_noon(hass, entity_id=None) L138
"Local datetime object of the next solar noon."
next_noon_utc(hass, entity_id=None) L148
"UTC datetime object of the next noon."
next_setting(hass, entity_id=None) L166
"Local datetime object of the next sun setting."
next_setting_utc(hass, entity_id=None) L176
"UTC datetime object of the next sun setting."
next_rising(hass, entity_id=None) L194
"Local datetime object of the next sun rising."
next_rising_utc(hass, entity_id=None) L204
"UTC datetime object of the next sun rising."
setup(hass, config) L221
"Track the state of the sun."

homeassistant/components/switch/__init__.py
class SwitchDevice(ToggleEntity) L142
  "Representation of a switch."
  current_power_w(self) L147
  "Return the current power usage in W."
  today_energy_kwh(self) L152
  "Return the today total energy usage in kWh."
  is_standby(self) L157
  "Return true if device is in standby."
  state_attributes(self) L162
  "Return the optional state attributes."

is_on(hass, entity_id=None) L50
"Return if the switch is on based on the statemachine."
turn_on(hass, entity_id=None) L59
"Turn all or specified switch on."
async_turn_on(hass, entity_id=None) L65
"Turn all or specified switch on."
turn_off(hass, entity_id=None) L71
"Turn all or specified switch off."
async_turn_off(hass, entity_id=None) L77
"Turn all or specified switch off."
toggle(hass, entity_id=None) L84
"Toggle all or specified switch."
async_setup(hass, config) L91
"Track states and offer events for switches."
async_handle_switch_service(service) L98
"Handle calls to the switch services."

homeassistant/components/switch/acer_projector.py
class AcerSwitch(SwitchDevice) L68
  "Represents an Acer Projector as an switch."
  __init__(self, serial_port, name, timeout, write_timeout, **kwargs) L71
  "Init of the Acer projector."
  _write_read(self, msg) L88
  "Write to the projector and read the return."
  _write_read_format(self, msg) L109
  "Write msg, obtain awnser and format output."
  available(self) L119
  "Return if projector is available."
  name(self) L124
  "Return name of the projector."
  is_on(self) L129
  "Return if the projector is turned on."
  state_attributes(self) L134
  "Return state attributes."
  update(self) L138
  "Get the latest state from the projector."
  turn_on(self) L157
  "Turn the projector on."
  turn_off(self) L163
  "Turn the projector off."

setup_platform(hass, config, add_devices, discovery_info=None) L58
"Connect with serial port and return Acer Projector."

homeassistant/components/switch/android_ip_webcam.py
class IPWebcamSettingsSwitch(AndroidIPCamEntity, SwitchDevice) L36
  "An abstract class for an IP Webcam setting."
  __init__(self, name, host, ipcam, setting) L39
  "Initialize the settings switch."
  name(self) L49
  "Return the the name of the node."
  async_update(self) L54
  "Get the updated status of the switch."
  is_on(self) L59
  "Return the boolean response if the node is on."
  async_turn_on(self, **kwargs) L64
  "Turn device on."
  async_turn_off(self, **kwargs) L78
  "Turn device off."
  icon(self) L92
  "Return the icon for the switch."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L18
"Set up the IP Webcam switch platform."

homeassistant/components/switch/anel_pwrctrl.py
class PwrCtrlSwitch(SwitchDevice) L69
  "Representation of a PwrCtrl switch."
  __init__(self, port, parent_device) L72
  "Initialize the PwrCtrl switch."
  should_poll(self) L78
  "Polling is needed."
  unique_id(self) L83
  "Return the unique ID of the device."
  name(self) L91
  "Return the name of the device."
  is_on(self) L96
  "Return true if the device is on."
  update(self) L100
  "Trigger update for all switches on the parent device."
  turn_on(self) L104
  "Turn the switch on."
  turn_off(self) L108
  "Turn the switch off."

class PwrCtrlDevice(object) L113
  "Device representation for per device throttling."
  __init__(self, device) L116
  "Initialize the PwrCtrl device."
  update(self) L121
  "Update the device and all its switches."

setup_platform(hass, config, add_devices, discovery_info=None) L39
"Setup PwrCtrl devices/switches."

homeassistant/components/switch/arduino.py
class ArduinoSwitch(SwitchDevice) L54
  "Representation of an Arduino switch."
  __init__(self, pin, options) L57
  "Initialize the Pin."
  name(self) L77
  "Get the name of the pin."
  is_on(self) L82
  "Return true if pin is high/on."
  turn_on(self) L86
  "Turn the pin to high/on."
  turn_off(self) L91
  "Turn the pin to low/off."

setup_platform(hass, config, add_devices, discovery_info=None) L39
"Set up the Arduino platform."

homeassistant/components/switch/arest.py
class ArestSwitchBase(SwitchDevice) L68
  "Representation of an aREST switch."
  __init__(self, resource, location, name) L71
  "Initialize the switch."
  name(self) L79
  "Return the name of the switch."
  is_on(self) L84
  "Return true if device is on."
  available(self) L89
  "Could the device be accessed during the last update call."

class ArestSwitchFunction(ArestSwitchBase) L94
  "Representation of an aREST switch."
  __init__(self, resource, location, name, func) L97
  "Initialize the switch."
  turn_on(self, **kwargs) L116
  "Turn the device on."
  turn_off(self, **kwargs) L128
  "Turn the device off."
  update(self) L140
  "Get the latest data from aREST API and update the state."

class ArestSwitchPin(ArestSwitchBase) L152
  "Representation of an aREST switch."
  __init__(self, resource, location, name, pin) L155
  "Initialize the switch."
  turn_on(self, **kwargs) L166
  "Turn the device on."
  turn_off(self, **kwargs) L176
  "Turn the device off."
  update(self) L186
  "Get the latest data from aREST API and update the state."

setup_platform(hass, config, add_devices, discovery_info=None) L38
"Set up the aREST switches."

homeassistant/components/switch/bbb_gpio.py
class BBBGPIOSwitch(ToggleEntity) L48
  "Representation of a BeagleBone Black GPIO."
  __init__(self, pin, params) L51
  "Initialize the pin."
  name(self) L66
  "Return the name of the switch."
  should_poll(self) L71
  "No polling needed."
  is_on(self) L76
  "Return true if device is on."
  turn_on(self) L80
  "Turn the device on."
  turn_off(self) L86
  "Turn the device off."

setup_platform(hass, config, add_devices, discovery_info=None) L38
"Set up the BeagleBone Black GPIO devices."

homeassistant/components/switch/broadlink.py
class BroadlinkRMSwitch(SwitchDevice) L156
  "Representation of an Broadlink switch."
  __init__(self, friendly_name, device, command_on, command_off) L159
  "Initialize the switch."
  name(self) L168
  "Return the name of the switch."
  assumed_state(self) L173
  "Return true if unable to access real state of entity."
  should_poll(self) L178
  "No polling needed."
  is_on(self) L183
  "Return true if device is on."
  turn_on(self, **kwargs) L187
  "Turn the device on."
  turn_off(self, **kwargs) L193
  "Turn the device off."
  _sendpacket(self, packet, retry=2) L199
  "Send packet to device."
  _auth(self, retry=2) L215

class BroadlinkSP1Switch(BroadlinkRMSwitch) L225
  "Representation of an Broadlink switch."
  __init__(self, friendly_name, device) L228
  "Initialize the switch."
  _sendpacket(self, packet, retry=2) L234
  "Send packet to device."

class BroadlinkSP2Switch(BroadlinkSP1Switch) L248
  "Representation of an Broadlink switch."
  __init__(self, friendly_name, device) L251
  "Initialize the switch."
  assumed_state(self) L256
  "Return true if unable to access real state of entity."
  should_poll(self) L261
  "Polling needed."
  update(self) L265
  "Synchronize state with switch."
  _update(self, retry=2) L269

setup_platform(hass, config, add_devices, discovery_info=None) L60
"Setup Broadlink switches."
_learn_command(call) L73
_send_packet(call) L107

homeassistant/components/switch/command_line.py
class CommandSwitch(SwitchDevice) L65
  "Representation a switch that can be toggled using shell commands."
  __init__(self, hass, object_id, friendly_name, command_on, command_off, command_state, value_template) L68
  "Initialize the switch."
  _switch(command) L81
  "Execute the actual commands."
  _query_state_value(command) L93
  "Execute state command for return value."
  _query_state_code(command) L104
  "Execute state command for return code."
  should_poll(self) L110
  "Only poll if we have state command."
  name(self) L115
  "Return the name of the switch."
  is_on(self) L120
  "Return true if device is on."
  assumed_state(self) L125
  "Return true if we do optimistic updates."
  _query_state(self) L129
  "Query for state."
  update(self) L138
  "Update device state."
  turn_on(self, **kwargs) L147
  "Turn the device on."
  turn_off(self, **kwargs) L154
  "Turn the device off."

setup_platform(hass, config, add_devices, discovery_info=None) L35
"Find and return switches controlled by shell commands."

homeassistant/components/switch/demo.py
class DemoSwitch(SwitchDevice) L20
  "Representation of a demo switch."
  __init__(self, name, state, icon, assumed) L23
  "Initialize the Demo switch."
  should_poll(self) L31
  "No polling needed for a demo switch."
  name(self) L36
  "Return the name of the device if any."
  icon(self) L41
  "Return the icon to use for device if any."
  assumed_state(self) L46
  "Return if the state is based on assumptions."
  current_power_w(self) L51
  "Return the current power usage in W."
  today_energy_kwh(self) L57
  "Return the today total energy usage in kWh."
  is_on(self) L62
  "Return true if switch is on."
  turn_on(self, **kwargs) L66
  "Turn the switch on."
  turn_off(self, **kwargs) L71
  "Turn the device off."

setup_platform(hass, config, add_devices_callback, discovery_info=None) L12
"Setup the demo switches."

homeassistant/components/switch/digital_ocean.py
class DigitalOceanSwitch(SwitchDevice) L47
  "Representation of a Digital Ocean droplet switch."
  __init__(self, do, droplet_id) L50
  "Initialize a new Digital Ocean sensor."
  name(self) L60
  "Return the name of the switch."
  is_on(self) L65
  "Return true if switch is on."
  device_state_attributes(self) L70
  "Return the state attributes of the Digital Ocean droplet."
  turn_on(self, **kwargs) L84
  "Boot-up the droplet."
  turn_off(self, **kwargs) L89
  "Shutdown the droplet."
  update(self) L94
  "Get the latest data from the device and update the data."

setup_platform(hass, config, add_devices, discovery_info=None) L30
"Setup the Digital Ocean droplet switch."

homeassistant/components/switch/digitalloggers.py
class DINRelay(SwitchDevice) L84
  "Representation of a individual DIN III relay port."
  __init__(self, name, outletnumber, parent_device) L87
  "Initialize the DIN III Relay switch."
  name(self) L95
  "Return the display name of this relay."
  is_on(self) L100
  "Return true if relay is on."
  should_poll(self) L105
  "Polling is needed."
  turn_on(self, **kwargs) L109
  "Instruct the relay to turn on."
  turn_off(self, **kwargs) L113
  "Instruct the relay to turn off."
  update(self) L117
  "Trigger update for all switches on the parent device."

class DINRelayDevice(object) L129
  "Device representation for per device throttling."
  __init__(self, device) L132
  "Initialize the DINRelay device."
  turn_on(self, **kwargs) L137
  "Instruct the relay to turn on."
  turn_off(self, **kwargs) L141
  "Instruct the relay to turn off."
  update(self) L146
  "Fetch new state data for this device."

setup_platform(hass, config, add_devices, discovery_info=None) L53
"Find and return DIN III Relay switch."

homeassistant/components/switch/dlink.py
class SmartPlugSwitch(SwitchDevice) L59
  "Representation of a D-link Smart Plug switch."
  __init__(self, hass, data, name) L62
  "Initialize the switch."
  name(self) L69
  "Return the name of the Smart Plug, if any."
  device_state_attributes(self) L74
  "Return the state attributes of the device."
  current_power_watt(self) L105
  "Return the current power usage in Watt."
  is_on(self) L113
  "Return true if switch is on."
  turn_on(self, **kwargs) L117
  "Turn the switch on."
  turn_off(self) L121
  "Turn the switch off."
  update(self) L125
  "Get the latest data from the smart plug and updates the states."

class SmartPlugData(object) L130
  "Get the latest data from smart plug."
  __init__(self, smartplug) L133
  "Initialize the data object."
  update(self) L141
  "Get the latest data from the smart plug."

setup_platform(hass, config, add_devices, discovery_info=None) L41
"Setup a D-Link Smart Plug."

homeassistant/components/switch/edimax.py
class SmartPlugSwitch(SwitchDevice) L45
  "Representation an Edimax Smart Plug switch."
  __init__(self, smartplug, name) L48
  "Initialize the switch."
  name(self) L57
  "Return the name of the Smart Plug, if any."
  current_power_w(self) L62
  "Return the current power usage in W."
  today_energy_kwh(self) L67
  "Return the today total energy usage in kWh."
  is_on(self) L72
  "Return true if switch is on."
  turn_on(self, **kwargs) L76
  "Turn the switch on."
  turn_off(self) L80
  "Turn the switch off."
  update(self) L84
  "Update edimax switch."

setup_platform(hass, config, add_devices, discovery_info=None) L34
"Find and return Edimax Smart Plugs."

homeassistant/components/switch/enocean.py
class EnOceanSwitch(enocean.EnOceanDevice, ToggleEntity) L36
  "Representation of an EnOcean switch device."
  __init__(self, dev_id, devname) L39
  "Initialize the EnOcean switch device."
  is_on(self) L50
  "Return whether the switch is on or off."
  name(self) L55
  "Return the device name."
  turn_on(self, **kwargs) L59
  "Turn on the switch."
  turn_off(self, **kwargs) L69
  "Turn off the switch."
  value_changed(self, val) L79
  "Update the internal state of the switch."

setup_platform(hass, config, add_devices, discovery_info=None) L28
"Setup the EnOcean switch platform."

homeassistant/components/switch/flux.py
class FluxSwitch(SwitchDevice) L107
  "Representation of a Flux switch."
  __init__(self, name, hass, state, lights, start_time, stop_time, start_colortemp, sunset_colortemp, stop_colortemp, brightness, disable_brightness_adjust, mode) L110
  "Initialize the Flux switch."
  name(self) L129
  "Return the name of the device if any."
  is_on(self) L134
  "Return true if switch is on."
  turn_on(self, **kwargs) L138
  "Turn on flux."
  turn_off(self, **kwargs) L147
  "Turn off flux."
  flux_update(self, now=None) L156
  "Update all the lights using flux."
  find_start_time(self, now) L217
  "Return sunrise or start_time if given."

set_lights_xy(hass, lights, x_val, y_val, brightness) L62
"Set color of array of lights."
set_lights_temp(hass, lights, mired, brightness) L72
"Set color of array of lights."
setup_platform(hass, config, add_devices, discovery_info=None) L83
"Setup the Flux switches."
update(call=None) L100
"Update lights."

homeassistant/components/switch/fritzdect.py
class FritzDectSwitch(SwitchDevice) L67
  "Representation of a FRITZ!DECT switch."
  __init__(self, hass, data, name) L70
  "Initialize the switch."
  name(self) L77
  "Return the name of the FRITZ!DECT switch, if any."
  device_state_attributes(self) L82
  "Return the state attributes of the device."
  current_power_watt(self) L103
  "Return the current power usage in Watt."
  is_on(self) L111
  "Return true if switch is on."
  turn_on(self, **kwargs) L115
  "Turn the switch on."
  turn_off(self) L120
  "Turn the switch off."
  update(self) L125
  "Get the latest data from the fritz box and updates the states."

class FritzDectSwitchData(object) L130
  "Get the latest data from the fritz box."
  __init__(self, fritz, ain) L133
  "Initialize the data object."
  update(self) L145
  "Get the latest data from the fritz box."

setup_platform(hass, config, add_devices, discovery_info=None) L39
"Add all switches connected to Fritz Box."

homeassistant/components/switch/hdmi_cec.py
class CecSwitchDevice(CecDevice, SwitchDevice) L30
  "Representation of a HDMI device as a Switch."
  __init__(self, hass, device, logical) L33
  "Initialize the HDMI device."
  turn_on(self, **kwargs) L40
  "Turn device on."
  turn_off(self, **kwargs) L45
  "Turn device off."
  toggle(self) L50
  "Toggle the entity."
  is_on(self) L59
  "Return True if entity is on."
  is_standby(self) L64
  "Return true if device is in standby."
  state(self) L69
  "Cached state of device."

setup_platform(hass, config, add_devices, discovery_info=None) L21
"Find and return HDMI devices as switches."

homeassistant/components/switch/hikvisioncam.py
class HikvisionMotionSwitch(ToggleEntity) L61
  "Representation of a switch to toggle on/off motion detection."
  __init__(self, name, hikvision_cam) L64
  "Initialize the switch."
  should_poll(self) L71
  "Poll for status regularly."
  name(self) L76
  "Return the name of the device if any."
  state(self) L81
  "Return the state of the device if any."
  is_on(self) L86
  "Return true if device is on."
  turn_on(self, **kwargs) L90
  "Turn the device on."
  turn_off(self, **kwargs) L95
  "Turn the device off."
  update(self) L100
  "Update Motion Detection state."

setup_platform(hass, config, add_devices, discovery_info=None) L36
"Setup Hikvision camera."

homeassistant/components/switch/homematic.py
class HMSwitch(HMDevice, SwitchDevice) L31
  "Representation of a Homematic switch."
  is_on(self) L35
  "Return True if switch is on."
  today_energy_kwh(self) L43
  "Return the current power usage in kWh."
  turn_on(self, **kwargs) L53
  "Turn the switch on."
  turn_off(self, **kwargs) L57
  "Turn the switch off."
  _init_data_struct(self) L61
  "Generate a data dict (self._data) from the Homematic metadata."

setup_platform(hass, config, add_devices, discovery_info=None) L17
"Setup the Homematic switch platform."

homeassistant/components/switch/hook.py
class HookSmartHome(SwitchDevice) L83
  "Representation of a Hook device, allowing on and off commands."
  __init__(self, hass, token, device_id, device_name) L86
  "Initialize the switch."
  name(self) L97
  "Return the name of the switch."
  is_on(self) L102
  "Return true if device is on."
  _send(self, url) L107
  "Send the url to the Hook API."
  async_turn_on(self) L125
  "Turn the device on asynchronously."
  async_turn_off(self) L134
  "Turn the device off asynchronously."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L35
"Set up Hook by getting the access token and list of actions."

homeassistant/components/switch/insteon_local.py
class InsteonLocalSwitchDevice(SwitchDevice) L123
  "An abstract Class for an Insteon node."
  __init__(self, node, name) L126
  "Initialize the device."
  name(self) L133
  "Return the the name of the node."
  unique_id(self) L138
  "Return the ID of this Insteon node."
  update(self) L143
  "Get the updated status of the switch."
  is_on(self) L154
  "Return the boolean response if the node is on."
  turn_on(self, **kwargs) L158
  "Turn device on."
  turn_off(self, **kwargs) L163
  "Turn device off."

setup_platform(hass, config, add_devices, discovery_info=None) L28
"Set up the Insteon local switch platform."
request_configuration(device_id, insteonhub, model, hass, add_devices_callback) L51
"Request configuration steps from the user."
insteon_switch_config_callback(data) L63
"The actions to do when our configuration callback is called."
setup_switch(device_id, name, insteonhub, hass, add_devices_callback) L78
"Set up the switch."
config_from_file(filename, config=None) L98
"Small configuration file management function."

homeassistant/components/switch/insteon_plm.py
class InsteonPLMSwitchDevice(SwitchDevice) L38
  "A Class for an Insteon device."
  __init__(self, hass, plm, address, name) L41
  "Initialize the switch."
  should_poll(self) L52
  "No polling needed."
  address(self) L57
  "Return the the address of the node."
  name(self) L62
  "Return the the name of the node."
  is_on(self) L67
  "Return the boolean response if the node is on."
  device_state_attributes(self) L74
  "Provide attributes for display on device card."
  get_attr(self, key) L79
  "Return specified attribute for this device."
  async_switch_update(self, message) L84
  "Receive notification from transport that new data exists."
  async_turn_on(self, **kwargs) L90
  "Turn device on."
  async_turn_off(self, **kwargs) L95
  "Turn device off."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L20
"Set up the INSTEON PLM device class for the hass platform."

homeassistant/components/switch/isy994.py
class ISYSwitchDevice(isy.ISYDevice, SwitchDevice) L57
  "Representation of an ISY994 switch device."
  __init__(self, node) L60
  "Initialize the ISY994 switch device."
  is_on(self) L65
  "Get whether the ISY994 device is in the on state."
  state(self) L70
  "Get the state of the ISY994 device."
  turn_off(self, **kwargs) L74
  "Send the turn on command to the ISY994 switch."
  turn_on(self, **kwargs) L79
  "Send the turn off command to the ISY994 switch."

class ISYSwitchProgram(ISYSwitchDevice) L85
  "A representation of an ISY994 program switch."
  __init__(self, name, node, actions) L88
  "Initialize the ISY994 switch program."
  is_on(self) L95
  "Get whether the ISY994 switch program is on."
  turn_on(self, **kwargs) L99
  "Send the turn on command to the ISY994 switch program."
  turn_off(self, **kwargs) L104
  "Send the turn off command to the ISY994 switch program."

setup_platform(hass, config, add_devices, discovery_info=None) L27
"Set up the ISY994 switch platform."

homeassistant/components/switch/kankun.py
class KankunSwitch(SwitchDevice) L57
  "Representation of a Kankun Wifi switch."
  __init__(self, hass, name, host, port, path, user, passwd) L60
  "Initialise the device."
  _switch(self, newstate) L71
  "Switch on or off."
  _query_state(self) L82
  "Query switch state."
  should_poll(self) L94
  "Switch should always be polled."
  name(self) L99
  "The name of the switch."
  is_on(self) L104
  "True if device is on."
  update(self) L108
  "Update device state."
  turn_on(self, **kwargs) L112
  "Turn the device on."
  turn_off(self, **kwargs) L117
  "Turn the device off."

setup_platform(hass, config, add_devices_callback, discovery_info=None) L38
"Set up Kankun Wifi switches."

homeassistant/components/switch/knx.py
class KNXSwitch(KNXGroupAddress, SwitchDevice) L32
  "Representation of a KNX switch device."
  turn_on(self, **kwargs) L35
  "Turn the switch on."
  turn_off(self, **kwargs) L45
  "Turn the switch off."

setup_platform(hass, config, add_devices, discovery_info=None) L27
"Setup the KNX switch platform."

homeassistant/components/switch/litejet.py
class LiteJetSwitch(SwitchDevice) L31
  "Representation of a single LiteJet switch."
  __init__(self, hass, lj, i, name) L34
  "Initialize a LiteJet switch."
  _on_switch_pressed(self) L47
  _on_switch_released(self) L52
  name(self) L58
  "Return the name of the switch."
  is_on(self) L63
  "Return if the switch is pressed."
  should_poll(self) L68
  "Return that polling is not necessary."
  device_state_attributes(self) L73
  "Return the device-specific state attributes."
  turn_on(self, **kwargs) L79
  "Press the switch."
  turn_off(self, **kwargs) L83
  "Release the switch."

setup_platform(hass, config, add_devices, discovery_info=None) L19
"Set up the LiteJet switch platform."

homeassistant/components/switch/lutron_caseta.py
class LutronCasetaLight(LutronCasetaDevice, SwitchDevice) L29
  "Representation of a Lutron Caseta switch."
  turn_on(self, **kwargs) L32
  "Turn the switch on."
  turn_off(self, **kwargs) L36
  "Turn the switch off."
  is_on(self) L41
  "Return true if device is on."
  update(self) L45
  "Called when forcing a refresh of the device."

setup_platform(hass, config, add_devices, discovery_info=None) L15
"Setup Lutron switch."

homeassistant/components/switch/mfi.py
class MfiSwitch(SwitchDevice) L68
  "Representation of an mFi switch-able device."
  __init__(self, port) L71
  "Initialize the mFi device."
  should_poll(self) L77
  "Polling is needed."
  unique_id(self) L82
  "Return the unique ID of the device."
  name(self) L87
  "Return the name of the device."
  is_on(self) L92
  "Return true if the device is on."
  update(self) L96
  "Get the latest state and update the state."
  turn_on(self) L103
  "Turn the switch on."
  turn_off(self) L108
  "Turn the switch off."
  current_power_w(self) L114
  "Return the current power usage in W."
  device_state_attributes(self) L119
  "Return the state attributes fof the device."

setup_platform(hass, config, add_devices, discovery_info=None) L43
"Setup mFi sensors."

homeassistant/components/switch/mochad.py
class MochadSwitch(SwitchDevice) L41
  "Representation of a X10 switch over Mochad."
  __init__(self, hass, ctrl, dev) L44
  "Initialize a Mochad Switch Device."
  name(self) L57
  "Get the name of the switch."
  turn_on(self, **kwargs) L61
  "Turn the switch on."
  turn_off(self, **kwargs) L67
  "Turn the switch off."
  _get_device_status(self) L73
  "Get the status of the switch from mochad."
  is_on(self) L79
  "Return true if switch is on."

setup_platform(hass, config, add_devices, discovery_info=None) L33
"Setup X10 switches over a mochad controller."

homeassistant/components/switch/modbus.py
class ModbusCoilSwitch(ToggleEntity) L43
  "Representation of a Modbus switch."
  __init__(self, name, slave, coil) L46
  "Initialize the switch."
  is_on(self) L54
  "Return true if switch is on."
  name(self) L59
  "Return the name of the switch."
  turn_on(self, **kwargs) L63
  "Set switch on."
  turn_off(self, **kwargs) L67
  "Set switch off."
  update(self) L71
  "Update the state of the switch."

setup_platform(hass, config, add_devices, discovery_info=None) L32
"Read configuration and create Modbus devices."

homeassistant/components/switch/mqtt.py
class MqttSwitch(SwitchDevice) L62
  "Representation of a switch that can be toggled using MQTT."
  __init__(self, name, state_topic, command_topic, qos, retain, payload_on, payload_off, optimistic, value_template) L65
  "Initialize the MQTT switch."
  async_added_to_hass(self) L80
  "Subscribe mqtt events."
  message_received(topic, payload, qos) L86
  "A new MQTT message has been received."
  should_poll(self) L106
  "No polling needed."
  name(self) L111
  "Return the name of the switch."
  is_on(self) L116
  "Return true if device is on."
  assumed_state(self) L121
  "Return true if we do optimistic updates."
  async_turn_on(self, **kwargs) L126
  "Turn the device on."
  async_turn_off(self, **kwargs) L140
  "Turn the device off."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L40
"Set up the MQTT switch."

homeassistant/components/switch/mysensors.py
class MySensorsSwitch(mysensors.MySensorsDeviceEntity, SwitchDevice) L123
  "Representation of the value of a MySensors Switch child node."
  assumed_state(self) L127
  "Return True if unable to access real state of entity."
  is_on(self) L132
  "Return True if switch is on."
  turn_on(self) L138
  "Turn the switch on."
  turn_off(self) L147
  "Turn the switch off."

class MySensorsIRSwitch(MySensorsSwitch) L157
  "IR switch child class to MySensorsSwitch."
  __init__(self, *args) L160
  "Setup instance attributes."
  is_on(self) L166
  "Return True if switch is on."
  turn_on(self, **kwargs) L173
  "Turn the IR switch on."
  turn_off(self) L194
  "Turn the IR switch off."
  update(self) L208
  "Update the controller with the latest value from a sensor."

setup_platform(hass, config, add_devices, discovery_info=None) L30
"Setup the mysensors platform for switches."
send_ir_code_service(service) L95
"Set IR code as device state attribute."

homeassistant/components/switch/mystrom.py
class MyStromSwitch(SwitchDevice) L43
  "Representation of a myStrom switch."
  __init__(self, name, resource) L46
  "Initialize the myStrom switch."
  name(self) L57
  "Return the name of the switch."
  is_on(self) L62
  "Return true if switch is on."
  current_power_w(self) L67
  "Return the current power consumption in W."
  turn_on(self, **kwargs) L71
  "Turn the switch on."
  turn_off(self, **kwargs) L80
  "Turn the switch off."
  update(self) L89
  "Get the latest data from the device and update the data."

setup_platform(hass, config, add_devices, discovery_info=None) L27
"Find and return myStrom switch."

homeassistant/components/switch/neato.py
class NeatoConnectedSwitch(ToggleEntity) L36
  "Neato Connected Switches."
  __init__(self, hass, robot, switch_type) L39
  "Initialize the Neato Connected switches."
  update(self) L55
  "Update the states of Neato switches."
  name(self) L85
  "Return the name of the switch."
  available(self) L90
  "Return True if entity is available."
  is_on(self) L98
  "Return true if switch is on."
  turn_on(self, **kwargs) L109
  "Turn the switch on."
  turn_off(self, **kwargs) L116
  "Turn the switch off."

setup_platform(hass, config, add_devices, discovery_info=None) L26
"Set up the Neato switches."

homeassistant/components/switch/netio.py
class NetioApiView(HomeAssistantView) L88
  "WSGI handler class."
  get(self, request, host) L95
  "Request handler."

class NetioSwitch(SwitchDevice) L125
  "Provide a Netio linked switch."
  __init__(self, netio, outlet, name) L128
  "Defined to handle throttle."
  name(self) L135
  "Netio device's name."
  available(self) L140
  "Return True if entity is available."
  turn_on(self) L144
  "Turn switch on."
  turn_off(self) L148
  "Turn switch off."
  _set(self, value) L152
  is_on(self) L160
  "Return switch's status."
  update(self) L164
  "Called by Home Assistant."
  state_attributes(self) L169
  "Return optional state attributes."
  current_power_w(self) L177
  "Return actual power."
  cumulated_consumption_kwh(self) L182
  "Total enerygy consumption since start_date."
  start_date(self) L187
  "Point in time when the energy accumulation started."

setup_platform(hass, config, add_devices, discovery_info=None) L52
"Configure the Netio platform."
dispose(event) L82
"Close connections to Netio Devices."

homeassistant/components/switch/orvibo.py
class S20Switch(SwitchDevice) L61
  "Representation of an S20 switch."
  __init__(self, name, s20) L64
  "Initialize the S20 device."
  should_poll(self) L74
  "Polling is needed."
  name(self) L79
  "Return the name of the switch."
  is_on(self) L84
  "Return true if device is on."
  update(self) L88
  "Update device state."
  turn_on(self, **kwargs) L95
  "Turn the device on."
  turn_off(self, **kwargs) L102
  "Turn the device off."

setup_platform(hass, config, add_devices_callback, discovery_info=None) L35
"Setup S20 switches."

homeassistant/components/switch/pilight.py
class _ReceiveHandle(object) L81
  __init__(self, config, echo) L82
  "Initialize the handle."
  match(self, code) L87
  "Test if the received code matches the configured values."
  run(self, switch, turn_on) L94
  "Change the state of the switch."

class PilightSwitch(SwitchDevice) L99
  "Representation of a Pilight switch."
  __init__(self, hass, name, code_on, code_off, code_on_receive, code_off_receive) L102
  "Initialize the switch."
  name(self) L124
  "Get the name of the switch."
  should_poll(self) L129
  "No polling needed, state set when correct code is received."
  is_on(self) L134
  "Return true if switch is on."
  _handle_code(self, call) L138
  "Check if received code by the pilight-daemon."
  set_state(self, turn_on, send_code=True) L159
  "Set the state of the switch."
  turn_on(self) L177
  "Turn the switch on by calling pilight.send service with on code."
  turn_off(self) L181
  "Turn the switch on by calling pilight.send service with off code."

setup_platform(hass, config, add_devices, discovery_info=None) L61
"Set up the Pilight platform."

homeassistant/components/switch/pulseaudio_loopback.py
class PAServer L79
  "Representation of a Pulseaudio server."
  __init__(self, host, port, buff_sz, tcp_timeout) L84
  "Simple constructor for reading in our configuration."
  _send_command(self, cmd, response_expected) L91
  "Send a command to the pa server using a socket."
  _get_full_response(self, sock) L108
  "Helper method to get the full response back from pulseaudio."
  update_module_state(self) L121
  "Refresh state in case an alternate process modified this data."
  turn_on(self, sink_name, source_name) L125
  "Send a command to pulseaudio to turn on the loopback."
  turn_off(self, module_idx) L129
  "Send a command to pulseaudio to turn off the loopback."
  get_module_idx(self, sink_name, source_name) L133
  "For a sink/source, return it's module id in our cache, if found."

class PALoopbackSwitch(SwitchDevice) L144
  "Representation the presence or absence of a PA loopback module."
  __init__(self, hass, name, pa_server, sink_name, source_name) L147
  "Initialize the Pulseaudio switch."
  name(self) L157
  "Return the name of the switch."
  is_on(self) L162
  "Tell the core logic if device is on."
  turn_on(self, **kwargs) L166
  "Turn the device on."
  turn_off(self, **kwargs) L177
  "Turn the device off."
  update(self) L188
  "Refresh state in case an alternate process modified this data."

setup_platform(hass, config, add_devices, discovery_info=None) L58
"Read in all of our configuration, and initialize the loopback switch."

homeassistant/components/switch/qwikswitch.py
setup_platform(hass, config, add_devices, discovery_info=None) L17
"Add switched from the main Qwikswitch component."

homeassistant/components/switch/rest.py
class RestSwitch(SwitchDevice) L81
  "Representation of a switch that can be toggled using REST."
  __init__(self, hass, name, resource, body_on, body_off, is_on_template, timeout) L84
  "Initialize the REST switch."
  name(self) L97
  "The name of the switch."
  is_on(self) L102
  "Return true if device is on."
  async_turn_on(self, **kwargs) L107
  "Turn the device on."
  async_turn_off(self, **kwargs) L127
  "Turn the device off."
  async_update(self) L147
  "Get the latest data from REST API and update the state."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L43
"Set up the RESTful switch."

homeassistant/components/switch/rflink.py
class RflinkSwitch(SwitchableRflinkDevice, SwitchDevice) L58
  "Representation of a Rflink switch."

devices_from_config(domain_config, hass=None) L37
"Parse configuration and add Rflink switch devices."
async_setup_platform(hass, config, async_add_devices, discovery_info=None) L53
"Set up the Rflink platform."

homeassistant/components/switch/rfxtrx.py
class RfxtrxSwitch(rfxtrx.RfxtrxDevice, SwitchDevice) L45
  "Representation of a RFXtrx switch."
  turn_on(self, **kwargs) L48
  "Turn the device on."

setup_platform(hass, config, add_devices_callback, discovery_info=None) L19
"Setup the RFXtrx platform."
switch_update(event) L27
"Callback for sensor updates from the RFXtrx gateway."

homeassistant/components/switch/rpi_gpio.py
class RPiGPIOSwitch(ToggleEntity) L49
  "Representation of a  Raspberry Pi GPIO."
  __init__(self, name, port, invert_logic) L52
  "Initialize the pin."
  name(self) L62
  "Return the name of the switch."
  should_poll(self) L67
  "No polling needed."
  is_on(self) L72
  "Return true if device is on."
  turn_on(self) L76
  "Turn the device on."
  turn_off(self) L82
  "Turn the device off."

setup_platform(hass, config, add_devices, discovery_info=None) L38
"Setup the Raspberry PI GPIO devices."

homeassistant/components/switch/rpi_rf.py
class RPiRFSwitch(SwitchDevice) L79
  "Representation of a GPIO RF switch."
  __init__(self, hass, name, rfdevice, protocol, pulselength, signal_repetitions, code_on, code_off) L82
  "Initialize the switch."
  should_poll(self) L96
  "No polling needed."
  name(self) L101
  "Return the name of the switch."
  is_on(self) L106
  "Return true if device is on."
  _send_code(self, code_list, protocol, pulselength) L110
  "Send the code(s) with a specified pulselength."
  turn_on(self) L117
  "Turn the switch on."
  turn_off(self) L123
  "Turn the switch off."

setup_platform(hass, config, add_devices, discovery_info=None) L48
"Find and return switches controlled by a generic RF device via GPIO."

homeassistant/components/switch/scsgate.py
class SCSGateSwitch(SwitchDevice) L84
  "Representation of a SCSGate switch."
  __init__(self, scs_id, name, logger) L87
  "Initialize the switch."
  scs_id(self) L95
  "Return the SCS ID."
  should_poll(self) L100
  "No polling needed."
  name(self) L105
  "Return the name of the device if any."
  is_on(self) L110
  "Return true if switch is on."
  turn_on(self, **kwargs) L114
  "Turn the device on."
  turn_off(self, **kwargs) L124
  "Turn the device off."
  process_event(self, message) L134
  "Handle a SCSGate message related with this switch."

class SCSGateScenarioSwitch(object) L157
  "Provides a SCSGate scenario switch."
  __init__(self, scs_id, name, logger, hass) L164
  "Initialize the scenario."
  scs_id(self) L172
  "Return the SCS ID."
  name(self) L177
  "Return the name of the device if any."
  process_event(self, message) L181
  "Handle a SCSGate message related with this switch."

setup_platform(hass, config, add_devices, discovery_info=None) L33
"Setup the SCSGate switches."
_setup_traditional_switches(logger, config, add_devices_callback) L43
"Add traditional SCSGate switches."
_setup_scenario_switches(logger, config, hass) L65
"Add only SCSGate scenario switches."

homeassistant/components/switch/tellduslive.py
class TelldusLiveSwitch(TelldusLiveEntity, ToggleEntity) L25
  "Representation of a Tellstick switch."
  is_on(self) L29
  "Return true if switch is on."
  turn_on(self, **kwargs) L33
  "Turn the switch on."
  turn_off(self, **kwargs) L38
  "Turn the switch off."

setup_platform(hass, config, add_devices, discovery_info=None) L18
"Setup Tellstick switches."

homeassistant/components/switch/tellstick.py
class TellstickSwitch(TellstickDevice, ToggleEntity) L34
  "Representation of a Tellstick switch."
  _parse_ha_data(self, kwargs) L37
  "Turn the value from HA into something useful."
  _parse_tellcore_data(self, tellcore_data) L41
  "Turn the value recieved from tellcore into something useful."
  _update_model(self, new_state, data) L45
  "Update the device entity state to match the arguments."
  _send_device_command(self, requested_state, requested_data) L49
  "Let tellcore update the actual device to the requested state."
  force_update(self) L57
  "Will trigger anytime the state property is updated."

setup_platform(hass, config, add_devices, discovery_info=None) L19
"Setup Tellstick switches."

homeassistant/components/switch/template.py
class SwitchTemplate(SwitchDevice) L78
  "Representation of a Template switch."
  __init__(self, hass, device_id, friendly_name, state_template, on_action, off_action, entity_ids) L81
  "Initialize the Template switch."
  async_added_to_hass(self) L95
  "Register callbacks."
  template_switch_state_listener(entity, old_state, new_state) L102
  "Called when the target device changes state."
  template_switch_startup(event) L107
  "Update template on startup."
  name(self) L118
  "Return the name of the switch."
  is_on(self) L123
  "Return true if device is on."
  should_poll(self) L128
  "No polling needed."
  available(self) L133
  "If switch is available."
  turn_on(self, **kwargs) L137
  "Fire the on action."
  turn_off(self, **kwargs) L141
  "Fire the off action."
  async_update(self) L146
  "Update the state from the template."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L46
"Setup the Template switch."

homeassistant/components/switch/thinkingcleaner.py
class ThinkingCleanerSwitch(ToggleEntity) L57
  "ThinkingCleaner Switch (dock, clean, find me)."
  __init__(self, tc_object, switch_type, update_devices) L60
  "Initialize the ThinkingCleaner."
  lock_update(self) L72
  "Lock the update since TC clean takes some time to update."
  reset_update_lock(self) L79
  "Reset the update lock."
  set_graceful_lock(self, state) L84
  "Set the graceful state."
  is_update_locked(self) L90
  "Check if the update method is locked."
  name(self) L102
  "Return the name of the sensor."
  is_on(self) L107
  "Return true if device is on."
  turn_on(self, **kwargs) L115
  "Turn the device on."
  turn_off(self, **kwargs) L125
  "Turn the device off."
  update(self) L131
  "Update the switch state (Only for clean)."

setup_platform(hass, config, add_devices, discovery_info=None) L35
"Setup the ThinkingCleaner platform."
update_devices() L43
"Update all devices."

homeassistant/components/switch/tplink.py
class SmartPlugSwitch(SwitchDevice) L43
  "Representation of a TPLink Smart Plug switch."
  __init__(self, smartplug, name) L46
  "Initialize the switch."
  name(self) L62
  "Return the name of the Smart Plug, if any."
  is_on(self) L67
  "Return true if switch is on."
  turn_on(self, **kwargs) L71
  "Turn the switch on."
  turn_off(self) L75
  "Turn the switch off."
  device_state_attributes(self) L80
  "Return the state attributes of the device."
  update(self) L84
  "Update the TP-Link switch's state."

setup_platform(hass, config, add_devices, discovery_info=None) L34
"Setup the TPLink switch platform."

homeassistant/components/switch/transmission.py
class TransmissionSwitch(ToggleEntity) L57
  "Representation of a Transmission switch."
  __init__(self, transmission_client, name) L60
  "Initialize the Transmission switch."
  name(self) L67
  "Return the name of the switch."
  state(self) L72
  "Return the state of the device."
  should_poll(self) L77
  "Poll for status regularly."
  is_on(self) L82
  "Return true if device is on."
  turn_on(self, **kwargs) L86
  "Turn the device on."
  turn_off(self, **kwargs) L91
  "Turn the device off."
  update(self) L96
  "Get the latest data from Transmission and updates the state."

setup_platform(hass, config, add_devices, discovery_info=None) L35
"Setup the Transmission switch."

homeassistant/components/switch/vera.py
class VeraSwitch(VeraDevice, SwitchDevice) L26
  "Representation of a Vera Switch."
  __init__(self, vera_device, controller) L29
  "Initialize the Vera device."
  turn_on(self, **kwargs) L35
  "Turn device on."
  turn_off(self, **kwargs) L41
  "Turn device off."
  current_power_w(self) L48
  "Current power usage in W."
  is_on(self) L55
  "Return true if device is on."
  update(self) L59
  "Called by the vera device callback to update state."

setup_platform(hass, config, add_devices, discovery_info=None) L19
"Find and return Vera switches."

homeassistant/components/switch/verisure.py
class VerisureSmartplug(SwitchDevice) L29
  "Representation of a Verisure smartplug."
  __init__(self, device_id) L32
  "Initialize the Verisure device."
  name(self) L37
  "Return the name or location of the smartplug."
  is_on(self) L42
  "Return true if on."
  available(self) L47
  "Return True if entity is available."
  turn_on(self) L51
  "Set smartplug status on."
  turn_off(self) L57
  "Set smartplug status off."
  update(self) L63
  "Get the latest date of the smartplug."

setup_platform(hass, config, add_devices, discovery_info=None) L16
"Setup the Verisure switch platform."

homeassistant/components/switch/volvooncall.py
class VolvoSwitch(VolvoEntity, ToggleEntity) L24
  "Representation of a Volvo switch."
  is_on(self) L28
  "Return true if switch is on."
  turn_on(self, **kwargs) L32
  "Turn the switch on."
  turn_off(self, **kwargs) L36
  "Turn the switch off."
  icon(self) L41
  "Return the icon."

setup_platform(hass, config, add_devices, discovery_info=None) L17
"Setup Tellstick switches."

homeassistant/components/switch/wake_on_lan.py
class WOLSwitch(SwitchDevice) L50
  "Representation of a wake on lan switch."
  __init__(self, hass, name, host, mac_address, off_action, broadcast_address) L53
  "Initialize the WOL switch."
  should_poll(self) L68
  "Poll for status regularly."
  is_on(self) L73
  "Return true if switch is on."
  name(self) L78
  "The name of the switch."
  turn_on(self) L82
  "Turn the device on."
  turn_off(self) L90
  "Turn the device off if an off action is present."
  update(self) L95
  "Check if device is on and update the state."

setup_platform(hass, config, add_devices, discovery_info=None) L38
"Set up a wake on lan switch."

homeassistant/components/switch/wemo.py
class WemoSwitch(SwitchDevice) L47
  "Representation of a WeMo switch."
  __init__(self, device) L50
  "Initialize the WeMo switch."
  _update_callback(self, _device, _type, _params) L64
  "Called by the Wemo device callback to update state."
  should_poll(self) L77
  "No polling needed with subscriptions."
  unique_id(self) L84
  "Return the ID of this WeMo switch."
  name(self) L89
  "Return the name of the switch if any."
  device_state_attributes(self) L94
  "Return the state attributes of the device."
  as_uptime(_seconds) L133
  "Format seconds into uptime string in the format: 00d 00h 00m 00s."
  current_power_w(self) L142
  "Current power usage in W."
  today_energy_kwh(self) L150
  "Today total energy usage in kWh."
  detail_state(self) L157
  "Return the state of the device."
  is_on(self) L173
  "Return true if switch is on."
  available(self) L178
  "True if switch is available."
  icon(self) L189
  "Icon of device based on its type."
  turn_on(self, **kwargs) L196
  "Turn the switch on."
  turn_off(self) L202
  "Turn the switch off."
  update(self) L208
  "Update WeMo state."
  _update(self, force_update=True) L212

setup_platform(hass, config, add_devices_callback, discovery_info=None) L34
"Setup discovered WeMo switches."

homeassistant/components/switch/wink.py
class WinkToggleDevice(WinkDevice, ToggleEntity) L36
  "Representation of a Wink toggle device."
  __init__(self, wink, hass) L39
  "Initialize the Wink device."
  is_on(self) L44
  "Return true if device is on."
  turn_on(self, **kwargs) L48
  "Turn the device on."
  turn_off(self) L52
  "Turn the device off."
  device_state_attributes(self) L57
  "Return the state attributes."

setup_platform(hass, config, add_devices, discovery_info=None) L14
"Setup the Wink platform."

homeassistant/components/switch/zha.py
class Switch(zha.Entity, SwitchDevice) L27
  "ZHA switch."
  is_on(self) L33
  "Return if the switch is on based on the statemachine."
  async_turn_on(self, **kwargs) L40
  "Turn the entity on."
  async_turn_off(self, **kwargs) L46
  "Turn the entity off."

setup_platform(hass, config, add_devices, discovery_info=None) L18
"Setup Zigbee Home Automation switches."

homeassistant/components/switch/zigbee.py
class ZigBeeSwitch(ZigBeeDigitalOut, SwitchDevice) L32
  "Representation of a ZigBee Digital Out device."

setup_platform(hass, config, add_devices, discovery_info=None) L27
"Setup the ZigBee switch platform."

homeassistant/components/switch/zoneminder.py
class ZMSwitchMonitors(SwitchDevice) L47
  "Representation of a ZoneMinder switch."
  __init__(self, monitor_id, monitor_name, on_state, off_state) L52
  "Initialize the switch."
  name(self) L61
  "Return the name of the switch."
  update(self) L65
  "Update the switch value."
  is_on(self) L74
  "Return True if entity is on."
  turn_on(self) L78
  "Turn the entity on."
  turn_off(self) L85
  "Turn the entity off."

setup_platform(hass, config, add_devices, discovery_info=None) L26
"Set up the ZoneMinder switch platform."

homeassistant/components/switch/zwave.py
class ZwaveSwitch(zwave.ZWaveDeviceEntity, SwitchDevice) L23
  "Representation of a Z-Wave switch."
  __init__(self, values) L26
  "Initialize the Z-Wave switch device."
  update_properties(self) L35
  "Callback on data changes for node values."
  is_on(self) L44
  "Return true if device is on."
  turn_on(self, **kwargs) L48
  "Turn the device on."
  turn_off(self, **kwargs) L52
  "Turn the device off."

get_device(values, **kwargs) L18
"Create zwave entity device."

homeassistant/components/tado.py
class TadoDataStore L62
  "An object to store the Tado data."
  __init__(self, tado) L65
  "Initialize Tado data store."
  update(self) L73
  "Update the internal data from mytado.com."
  add_sensor(self, data_id, sensor) L95
  "Add a sensor to update in _update()."
  get_data(self, data_id) L100
  "Get the cached data."
  get_zones(self) L109
  "Wrapper for getZones()."
  get_capabilities(self, tado_id) L113
  "Wrapper for getCapabilities(..)."
  get_me(self) L117
  "Wrapper for getMet()."
  reset_zone_overlay(self, zone_id) L121
  "Wrapper for resetZoneOverlay(..)."
  set_zone_overlay(self, zone_id, mode, temperature=None, duration=None) L125
  "Wrapper for setZoneOverlay(..)."

setup(hass, config) L41
"Set up of the Tado component."

homeassistant/components/telegram_bot/__init__.py
class BaseTelegramBotEntity L92
  "The base class for the telegram bot."
  __init__(self, hass, allowed_chat_ids) L95
  "Initialize the bot base class."
  process_message(self, data) L100
  "Check for basic message rules and fire an event if message is ok."

async_setup(hass, config) L41
"Setup the telegram bot component."
async_setup_platform(p_type, p_config=None, discovery_info=None) L44
"Setup a telegram bot platform."
async_platform_discovered(platform, info) L83
"Callback to load a platform."

homeassistant/components/telegram_bot/polling.py
class TelegramPoll(BaseTelegramBotEntity) L53
  "asyncio telegram incoming message handler."
  __init__(self, bot, hass, allowed_chat_ids) L56
  "Initialize the polling instance."
  start_polling(self) L67
  "Start the polling task."
  stop_polling(self) L71
  "Stop the polling task."
  get_updates(self, offset) L76
  "Bypass the default long polling method to enable asyncio."
  handle(self) L103
  "" Receiving and processing incoming messages."
  check_incoming(self) L111
  ""Loop which continuously checks for incoming telegram messages."

async_setup_platform(hass, config, async_add_devices, discovery_info=None) L25
"Setup the polling platform."
_start_bot(_event) L32
"Start the bot."
_stop_bot(_event) L37
"Stop the bot."

homeassistant/components/telegram_bot/webhooks.py
class BotPushReceiver(HomeAssistantView, BaseTelegramBotEntity) L70
  "Handle pushes from telegram."
  __init__(self, hass, allowed_chat_ids, trusted_networks) L77
  "Initialize the class."
  post(self, request) L83
  "Accept the POST from telegram."

setup_platform(hass, config, async_add_devices, discovery_info=None) L47
"Setup the polling platform."

homeassistant/components/tellduslive.py
class TelldusLiveClient(object) L65
  "Get the latest data and update the states."
  __init__(self, hass, config) L68
  "Initialize the Tellus data object."
  validate_session(self) L90
  "Make a request to see if the session is valid."
  update(self, now) L95
  "Periodically poll the servers for current state."
  _sync(self) L104
  "Update local list of devices."
  identify_device(device) L109
  "Find out what type of HA component to create."
  discover(device_id, component) L123
  "Discover the component."
  device(self, device_id) L143
  "Return device representation."
  is_available(self, device_id) L147
  "Return device availability."

class TelldusLiveEntity(Entity) L152
  "Base class for all Telldus Live entities."
  __init__(self, hass, device_id) L155
  "Initialize the entity."
  changed(self) L163
  "A property of the device might have changed."
  device_id(self) L170
  "Return the id of the device."
  device(self) L175
  "Return the representaion of the device."
  _state(self) L180
  "Return the state of the device."
  should_poll(self) L185
  "Polling is not needed."
  assumed_state(self) L190
  "Return true if unable to access real state of entity."
  name(self) L195
  "Return name of device."
  available(self) L200
  "Return true if device is not offline."
  device_state_attributes(self) L205
  "Return the state attributes."
  _battery_level(self) L215
  "Return the battery level of a device."
  _last_updated(self) L221
  "Return the last update of a device."

setup(hass, config) L48
"Setup the Telldus Live component."

homeassistant/components/tellstick.py
class TellstickRegistry(object) L92
  "Handle everything around Tellstick callbacks."
  __init__(self, hass, tellcore_lib) L104
  "Initialize the Tellstick mappings and callbacks."
  _tellcore_event_callback(self, tellcore_id, tellcore_command, tellcore_data, cid) L111
  "Handle the actual callback from Tellcore."
  _setup_tellcore_callback(self, hass, tellcore_lib) L119
  "Register the callback handler."
  clean_up_callback(event) L124
  "Unregister the callback bindings."
  register_ha_device(self, tellcore_id, ha_device) L132
  "Register a new HA device to receive callback updates."
  register_tellcore_devices(self, tellcore_devices) L136
  "Register a list of devices."
  get_tellcore_device(self, tellcore_id) L142
  "Return a device by tellcore_id."

class TellstickDevice(Entity) L147
  "Representation of a Tellstick device."
  __init__(self, tellcore_id, tellcore_registry, signal_repetitions) L153
  "Initalize the Tellstick device."
  should_poll(self) L171
  "Tell Home Assistant not to poll this device."
  assumed_state(self) L176
  "Tellstick devices are always assumed state."
  name(self) L181
  "Return the name of the device as reported by tellcore."
  is_on(self) L186
  "Return true if the device is on."
  _parse_ha_data(self, kwargs) L190
  "Turn the value from HA into something useful."
  _parse_tellcore_data(self, tellcore_data) L194
  "Turn the value recieved from tellcore into something useful."
  _update_model(self, new_state, data) L198
  "Update the device entity state to match the arguments."
  _send_device_command(self, requested_state, requested_data) L202
  "Let tellcore update the actual device to the requested state."
  _send_repeated_command(self) L206
  "Send a tellstick command once and decrease the repeat count."
  _change_device_state(self, new_state, data) L219
  "The logic for actually turning on or off the device."
  turn_on(self, **kwargs) L237
  "Turn the switch on."
  turn_off(self, **kwargs) L241
  "Turn the switch off."
  _update_model_from_command(self, tellcore_command, tellcore_data) L245
  "Update the model, from a sent tellcore command and data."
  update_from_callback(self, tellcore_command, tellcore_data) L258
  "Handle updates from the tellcore callback."
  _update_from_tellcore(self) L268
  "Read the current state of the device from the tellcore library."
  update(self) L283
  "Poll the current state of the device."

_discover(hass, config, component_name, found_tellcore_devices) L43
"Set up and send the discovery event."
setup(hass, config) L58
"Setup the Tellstick component."

homeassistant/components/thingspeak.py
setup(hass, config) L35
"Set up the Thingspeak environment."
thingspeak_listener(entity_id, old_state, new_state) L54
"Listen for new events and send them to Thingspeak."

homeassistant/components/tradfri.py
request_configuration(hass, config, host) L36
"Request configuration steps from the user."
configuration_callback(callback_data) L47
"Called when config is submitted."
success() L56
"Set up was successful."
async_setup(hass, config) L75
"Setup Tradfri."
gateway_discovered(service, info) L82
"Called when a gateway is discovered."
_setup_gateway(hass, hass_config, host, key) L101
"Create a gateway."
_read_config(hass) L125
"Read tradfri config."
_write_config(hass, config) L137
"Write tradfri config."

homeassistant/components/tts/__init__.py
class SpeechManager(object) L180
  "Representation of a speech store."
  __init__(self, hass) L183
  "Initialize a speech store."
  async_init_cache(self, use_cache, cache_dir, time_memory) L195
  "Init config folder and load file cache."
  init_tts_cache_dir(cache_dir) L200
  "Init cache folder."
  get_cache_files() L215
  "Return a dict of given engine files."
  async_clear_cache(self) L240
  "Read file cache and delete files."
  remove_files() L244
  "Remove files from filesystem."
  async_register_engine(self, engine, provider, config) L257
  "Register a TTS provider."
  async_get_url(self, engine, message, cache=None, language=None, options=None) L265
  "Get URL for play message."
  async_get_tts_audio(self, engine, key, message, cache, language, options) L315
  "Receive TTS and store for view in cache."
  async_save_tts_audio(self, key, filename, data) L345
  "Store voice data to file and file_cache."
  save_speech() L352
  "Store speech to filesystem."
  async_file_to_mem(self, key) L364
  "Load voice from file cache into memory."
  load_speech() L375
  "Load a speech from filesystem."
  _async_store_to_memcache(self, key, filename, data) L389
  "Store data to memcache and set timer to remove it."
  async_remove_from_mem() L397
  "Cleanup memcache."
  async_read_tts(self, filename) L404
  "Read a voice file and return binary."
  write_tags(filename, data, provider, message, language, options) L425
  "Write ID3 tags to file."

class Provider(object) L456
  "Represent a single provider."
  default_language(self) L463
  "Default language."
  supported_languages(self) L468
  "List of supported languages."
  supported_options(self) L473
  "List of supported options like voice, emotionen."
  default_options(self) L478
  "Dict include default options."
  get_tts_audio(self, message, language, options=None) L482
  "Load tts audio file from provider."
  async_get_tts_audio(self, message, language, options=None) L486
  "Load tts audio file from provider."

class TextToSpeechView(HomeAssistantView) L498
  "TTS view to serve an speech audio."
  __init__(self, tts) L505
  "Initialize a tts view."
  get(self, request, filename) L510
  "Start a get request."

async_setup(hass, config) L82
"Set up TTS."
async_setup_platform(p_type, p_config, disc_info=None) L104
"Set up a tts platform."
async_say_handle(service) L129
"Service handle for say."
async_clear_cache_handle(service) L168
"Handle clear cache service call."

homeassistant/components/tts/amazon_polly.py
class AmazonPollyProvider(Provider) L130
  "Amazon Polly speech api provider."
  __init__(self, polly_client, config, supported_languages, all_voices) L133
  "Initialize Amazon Polly provider for TTS."
  supported_languages(self) L144
  "List of supported languages."
  default_language(self) L149
  "Default language."
  default_options(self) L154
  "Dict include default options."
  supported_options(self) L159
  "List of supported options."
  get_tts_audio(self, message, language=None, options=None) L163
  "Request TTS file from Polly."

get_engine(hass, config) L83
"Setup Amazon Polly speech component."

homeassistant/components/tts/demo.py
class DemoProvider(Provider) L29
  "Demo speech api provider."
  __init__(self, lang) L32
  "Initialize demo provider."
  default_language(self) L38
  "Default language."
  supported_languages(self) L43
  "List of supported languages."
  supported_options(self) L48
  "List of supported options like voice, emotionen."
  get_tts_audio(self, message, language, options=None) L52
  "Load TTS from demo."

get_engine(hass, config) L24
"Setup Demo speech component."

homeassistant/components/tts/google.py
class GoogleProvider(Provider) L48
  "The Google speech API provider."
  __init__(self, hass, lang) L51
  "Init Google TTS service."
  default_language(self) L64
  "Default language."
  supported_languages(self) L69
  "List of supported languages."
  async_get_tts_audio(self, message, language, options=None) L74
  "Load TTS from google."
  _split_message_to_parts(message) L118
  "Split message into single parts."
  split_by_space(fullstring) L128
  "Split a string by space."

async_get_engine(hass, config) L43
"Set up Google speech component."

homeassistant/components/tts/marytts.py
class MaryTTSProvider(Provider) L58
  "MaryTTS speech api provider."
  __init__(self, hass, conf) L61
  "Init MaryTTS TTS service."
  default_language(self) L72
  "Default language."
  supported_languages(self) L77
  "List of supported languages."
  async_get_tts_audio(self, message, language, options=None) L82
  "Load TTS from MaryTTS."

async_get_engine(hass, config) L53
"Setup MaryTTS speech component."

homeassistant/components/tts/picotts.py
class PicoProvider(Provider) L35
  "The Pico TTS API provider."
  __init__(self, lang) L38
  "Initialize Pico TTS provider."
  default_language(self) L44
  "Default language."
  supported_languages(self) L49
  "List of supported languages."
  get_tts_audio(self, message, language, options=None) L53
  "Load TTS using pico2wave."

get_engine(hass, config) L27
"Set up Pico speech component."

homeassistant/components/tts/voicerss.py
class VoiceRSSProvider(Provider) L90
  "The VoiceRSS speech API provider."
  __init__(self, hass, conf) L93
  "Init VoiceRSS TTS service."
  default_language(self) L108
  "Default language."
  supported_languages(self) L113
  "List of supported languages."
  async_get_tts_audio(self, message, language, options=None) L118
  "Load TTS from VoiceRSS."

async_get_engine(hass, config) L85
"Set up VoiceRSS TTS component."

homeassistant/components/tts/yandextts.py
class YandexSpeechKitProvider(Provider) L73
  "VoiceRSS speech api provider."
  __init__(self, hass, conf) L76
  "Init VoiceRSS TTS service."
  default_language(self) L88
  "Default language."
  supported_languages(self) L93
  "List of supported languages."
  async_get_tts_audio(self, message, language, options=None) L98
  "Load TTS from yandex."

async_get_engine(hass, config) L68
"Setup VoiceRSS speech component."

homeassistant/components/twilio.py
class TwilioReceiveDataView(HomeAssistantView) L45
  "Handle data from Twilio inbound messages and calls."
  post(self, request) L52
  "Handle Twilio data post."

setup(hass, config) L35
"Set up the Twilio component."

homeassistant/components/updater.py
_create_uuid(hass, filename=UPDATER_UUID_FILE) L50
"Create UUID and save it in a file."
_load_uuid(hass, filename=UPDATER_UUID_FILE) L58
"Load UUID from a file or return None."
setup(hass, config) L70
"Set up the updater component."
check_newest_version(hass, huuid) L88
"Check if a new version is available and report if one is."
get_newest_version(huuid) L111
"Get the newest Home Assistant version."

homeassistant/components/upnp.py
setup(hass, config) L24
"Register a port mapping for Home Assistant via UPnP."
deregister_port(event) L41
"De-register the UPnP port mapping."

homeassistant/components/vera.py
class VeraDevice(Entity) L131
  "Representation of a Vera device entity."
  __init__(self, vera_device, controller) L134
  "Initialize the device."
  _update_callback(self, _device) L147
  name(self) L152
  "Return the name of the device."
  should_poll(self) L157
  "Get polling requirement from vera device."
  device_state_attributes(self) L162
  "Return the state attributes of the device."

setup(hass, base_config) L58
"Common setup for Vera devices."
stop_subscription(event) L63
"Shutdown Vera subscriptions and subscription thread on exit."
map_vera_device(vera_device, remap) L104
"Map vera  classes to HA types."

homeassistant/components/verisure.py
class VerisureHub(object) L88
  "A Verisure hub wrapper class."
  __init__(self, domain_config, verisure) L91
  "Initialize the Verisure hub."
  login(self) L117
  "Login to Verisure MyPages."
  update_alarms(self) L127
  "Update the status of the alarm."
  update_locks(self) L134
  "Update the status of the locks."
  update_climate(self) L141
  "Update the status of the climate units."
  update_mousedetection(self) L148
  "Update the status of the mouse detectors."
  update_smartplugs(self) L155
  "Update the status of the smartplugs."
  update_smartcam(self) L162
  "Update the status of the smartcam."
  update_smartcam_imagelist(self) L169
  "Update the imagelist for the camera."
  smartcam_capture(self, device_id) L176
  "Capture a new image from a smartcam."
  available(self) L181
  "Return True if hub is available."
  update_component(self, get_function, status) L185
  "Update the status of Verisure components."
  reconnect(self) L197
  "Reconnect to Verisure MyPages."

setup(hass, config) L59
"Setup the Verisure component."
capture_smartcam(service) L74
"Capture a new picture from a smartcam."

homeassistant/components/volvooncall.py
class state L67
  "Namespace to hold state for each vehicle."

class VolvoEntity(Entity) L118
  "Base class for all VOC entities."
  __init__(self, hass, vin, attribute) L121
  "Initialize the entity."
  _state(self) L129
  vehicle(self) L133
  "Return vehicle."
  _vehicle_name(self) L138
  _entity_name(self) L145
  name(self) L149
  "Return full name of the entity."
  should_poll(self) L156
  "Polling is not needed."
  assumed_state(self) L161
  "Return true if unable to access real state of entity."
  device_state_attributes(self) L166
  "Return device specific state attributes."

setup(hass, config) L58
"Setup the VOC component."
discover_vehicle(vehicle) L76
"Load relevant platforms."
update_vehicle(vehicle) L88
"Updated information on vehicle received."
update(now) L100
"Update status from the online service."

homeassistant/components/weather/__init__.py
class WeatherEntity(Entity) L47
  "ABC for a weather data."
  temperature(self) L51
  "Return the platform temperature."
  temperature_unit(self) L56
  "Return the unit of measurement."
  pressure(self) L61
  "Return the pressure."
  humidity(self) L66
  "Return the humidity."
  wind_speed(self) L71
  "Return the wind speed."
  wind_bearing(self) L76
  "Return the wind bearing."
  ozone(self) L81
  "Return the ozone level."
  attribution(self) L86
  "Return the attribution."
  forecast(self) L91
  "Return the forecast."
  state_attributes(self) L96
  "Return the state attributes."
  state(self) L136
  "Return the current state."
  condition(self) L141
  "Return the current condition."
  _temp_for_display(self, temp) L145
  "Convert temperature into preferred units for display purposes."

async_setup(hass, config) L38
"Setup the weather component."

homeassistant/components/weather/bom.py
class BOMWeather(WeatherEntity) L45
  "Representation of a weather condition."
  __init__(self, bom_data, stationname=None) L48
  "Initialise the platform with a data instance and station name."
  update(self) L53
  "Update current conditions."
  name(self) L58
  "Return the name of the sensor."
  condition(self) L63
  "Return the current condition."
  temperature(self) L70
  "Return the platform temperature."
  temperature_unit(self) L75
  "Return the unit of measurement."
  pressure(self) L80
  "Return the mean sea-level pressure."
  humidity(self) L85
  "Return the relative humidity."
  wind_speed(self) L90
  "Return the wind speed."
  wind_bearing(self) L95
  "Return the wind bearing."
  attribution(self) L105
  "Return the attribution."

setup_platform(hass, config, add_devices, discovery_info=None) L27
"Set up the BOM weather platform."

homeassistant/components/weather/demo.py
class DemoWeather(WeatherEntity) L41
  "Representation of a weather condition."
  __init__(self, name, condition, temperature, humidity, pressure, wind_speed, temperature_unit, forecast) L44
  "Initialize the Demo weather."
  name(self) L57
  "Return the name of the sensor."
  should_poll(self) L62
  "No polling needed for a demo weather condition."
  temperature(self) L67
  "Return the temperature."
  temperature_unit(self) L72
  "Return the unit of measurement."
  humidity(self) L77
  "Return the humidity."
  wind_speed(self) L82
  "Return the wind speed."
  pressure(self) L87
  "Return the pressure."
  condition(self) L92
  "Return the weather condition."
  attribution(self) L98
  "Return the attribution."
  forecast(self) L103
  "Return the forecast."

setup_platform(hass, config, add_devices, discovery_info=None) L31
"Setup the Demo weather."

homeassistant/components/weather/metoffice.py
class MetOfficeWeather(WeatherEntity) L64
  "Implementation of a Met Office weather condition."
  __init__(self, site, data, config) L67
  "Initialise the platform with a data instance and site."
  update(self) L72
  "Update current conditions."
  name(self) L77
  "Return the name of the sensor."
  condition(self) L82
  "Return the current condition."
  temperature(self) L90
  "Return the platform temperature."
  temperature_unit(self) L95
  "Return the unit of measurement."
  pressure(self) L100
  "Return the mean sea-level pressure."
  humidity(self) L105
  "Return the relative humidity."
  wind_speed(self) L110
  "Return the wind speed."
  wind_bearing(self) L115
  "Return the wind bearing."
  attribution(self) L120
  "Return the attribution."

setup_platform(hass, config, add_devices, discovery_info=None) L29
"Set up the Met Office weather platform."

homeassistant/components/weather/openweathermap.py
class OpenWeatherMapWeather(WeatherEntity) L75
  "Implementation of an OpenWeatherMap sensor."
  __init__(self, name, owm, temperature_unit) L78
  "Initialize the sensor."
  name(self) L87
  "Return the name of the sensor."
  condition(self) L92
  "Return the current condition."
  temperature(self) L101
  "Return the temperature."
  temperature_unit(self) L106
  "Return the unit of measurement."
  pressure(self) L111
  "Return the pressure."
  humidity(self) L116
  "Return the humidity."
  wind_speed(self) L121
  "Return the wind speed."
  wind_bearing(self) L126
  "Return the wind bearing."
  attribution(self) L131
  "Return the attribution."
  forecast(self) L136
  "Return the forecast array."
  update(self) L143
  "Get the latest data from OWM and updates the states."

class WeatherData(object) L151
  "Get the latest data from OpenWeatherMap."
  __init__(self, owm, latitude, longitude) L154
  "Initialize the data object."
  update(self) L163
  "Get the latest data from OpenWeatherMap."
  update_forecast(self) L173
  "Get the lastest forecast from OpenWeatherMap."

setup_platform(hass, config, add_devices, discovery_info=None) L55
"Setup the OpenWeatherMap weather platform."

homeassistant/components/weather/zamg.py
class ZamgWeather(WeatherEntity) L51
  "Representation of a weather condition."
  __init__(self, zamg_data, stationname=None) L54
  "Initialise the platform with a data instance and station name."
  update(self) L59
  "Update current conditions."
  name(self) L64
  "Return the name of the sensor."
  condition(self) L70
  "Return the current condition."
  attribution(self) L75
  "Return the attribution."
  temperature(self) L80
  "Return the platform temperature."
  temperature_unit(self) L85
  "Return the unit of measurement."
  pressure(self) L90
  "Return the pressure."
  humidity(self) L95
  "Return the humidity."
  wind_speed(self) L100
  "Return the wind speed."
  wind_bearing(self) L105
  "Return the wind bearing."

setup_platform(hass, config, add_devices, discovery_info=None) L30
"Set up the ZAMG sensor platform."

homeassistant/components/weblink.py
class Link(Entity) L47
  "Representation of a link."
  __init__(self, hass, name, url, icon) L50
  "Initialize the link."
  icon(self) L60
  "Return the icon to use in the frontend, if any."
  name(self) L65
  "Return the name of the URL."
  state(self) L70
  "Return the URL."

setup(hass, config) L36
"Setup weblink component."

homeassistant/components/websocket_api.py
class WebsocketAPIView(HomeAssistantView) L186
  "View to serve a websockets endpoint."
  get(self, request) L194
  "Handle an incoming websocket connection."

class ActiveConnection L200
  "Handle an active websocket client connection."
  __init__(self, hass, request) L203
  "Initialize an active connection."
  debug(self, message1, message2='') L210
  "Print a debug message."
  log_error(self, message1, message2='') L214
  "Print an error message."
  send_message(self, message) L218
  "Helper method to send messages."
  handle(self) L224
  "Handle the websocket connection."
  cancel_connection(event) L233
  "Cancel this connection."
  handle_subscribe_events(self, msg) L342
  "Handle subscribe events command."
  forward_events(event) L347
  "Helper to forward events to websocket."
  handle_unsubscribe_events(self, msg) L363
  "Handle unsubscribe events command."
  handle_call_service(self, msg) L377
  "Handle call service command."
  call_service_helper(msg) L382
  "Helper to call a service and fire complete message."
  handle_get_states(self, msg) L394
  "Handle get states command."
  handle_get_services(self, msg) L401
  "Handle get services command."
  handle_get_config(self, msg) L408
  "Handle get config command."
  handle_get_panels(self, msg) L415
  "Handle get panels command."
  handle_ping(self, msg) L422
  "Handle ping command."

auth_ok_message() L115
"Return an auth_ok message."
auth_required_message() L123
"Return an auth_required message."
auth_invalid_message(message) L131
"Return an auth_invalid message."
event_message(iden, event) L139
"Return an event message."
error_message(iden, code, message) L148
"Return an error result message."
pong_message(iden) L161
"Return a pong message."
result_message(iden, result=None) L169
"Return a success result message."
async_setup(hass, config) L180
"Initialize the websocket API."

homeassistant/components/wemo.py
setup(hass, config) L47
"Common setup for WeMo devices."
stop_wemo(event) L55
"Shutdown Wemo subscriptions and subscription thread on exit."
discovery_dispatch(service, discovery_info) L62
"Dispatcher for WeMo discovery events."

homeassistant/components/wink.py
class WinkDevice(Entity) L135
  "Representation a base Wink device."
  __init__(self, wink, hass) L138
  "Initialize the Wink device."
  _pubnub_update(self, message) L148
  name(self) L163
  "Return the name of the device."
  available(self) L168
  "True if connection == True."
  update(self) L172
  "Update state of the device."
  should_poll(self) L177
  "Only poll if we are not subscribed to pubnub."
  device_state_attributes(self) L182
  "Return the state attributes."
  _battery_level(self) L206
  "Return the battery level."
  _manufacturer_device_model(self) L212
  "Return the manufacturer device model."
  _manufacturer_device_id(self) L217
  "Return the manufacturer device id."
  _device_manufacturer(self) L222
  "Return the device manufacturer."
  _model_name(self) L227
  "Return the model name."
  _tamper(self) L232
  "Return the devices tamper status."

setup(hass, config) L62
"Set up the Wink component."
start_subscription(event) L104
"Start the pubnub subscription."
stop_subscription(event) L109
"Stop the pubnub subscription."
force_update(call) L114
"Force all devices to poll the Wink API."
pull_new_devices(call) L121
"Pull new devices added to users Wink account since startup."

homeassistant/components/zabbix.py
setup(hass, config) L37
"Set up the Zabbix component."

homeassistant/components/zeroconf.py
setup(hass, config) L29
"Set up Zeroconf and make Home Assistant discoverable."
stop_zeroconf(event) L56
"Stop Zeroconf."

homeassistant/components/zha/__init__.py
class ApplicationListener L111
  "Handlers for events that happen on the ZigBee application."
  __init__(self, hass, config) L114
  "Initialize the listener."
  device_joined(self, device) L120
  "Handle device joined."
  device_initialized(self, device) L129
  "Handle device joined and basic information discovered."
  async_device_initialized(self, device, join) L134
  "Handle device joined and basic information discovered (async)."

class Entity(entity.Entity) L210
  "A base class for ZHA entities."
  __init__(self, endpoint, clusters, manufacturer, model, **kwargs) L215
  "Initialize ZHA entity."
  attribute_updated(self, attribute, value) L245
  "Handle an attribute updated on this cluster."
  zdo_command(self, aps_frame, tsn, command_id, args) L249
  "Handle a ZDO command received on this cluster."
  device_state_attributes(self) L254
  "Return device specific state attributes."

async_setup(hass, config) L74
"Setup ZHA."
permit(service) L98
"Allow devices to join this network."
_discover_endpoint_info(endpoint) L260
"Find some basic information about an endpoint."
get_discovery_info(hass, discovery_info) L286
"Get the full discovery info for a device."

homeassistant/components/zha/const.py
populate_data() L9
"Populate data using constants from bellows."

homeassistant/components/zigbee.py
class ZigBeeConfig(object) L127
  "Handle the fetching of configuration from the config file."
  __init__(self, config) L130
  "Initialize the configuration."
  name(self) L136
  "The name given to the entity."
  address(self) L141
  "The address of the device."
  should_poll(self) L153
  "No polling needed."

class ZigBeePinConfig(ZigBeeConfig) L158
  "Handle the fetching of configuration from the config file."
  pin(self) L162
  "The GPIO pin number."

class ZigBeeDigitalInConfig(ZigBeePinConfig) L167
  "A subclass of ZigBeePinConfig."
  __init__(self, config) L170
  "Initialise the ZigBee Digital input config."
  boolean_maps(self) L176
  "Create mapping dictionaries for potential inversion of booleans."
  bool2state(self) L197
  "A dictionary mapping the internal value to the ZigBee value."
  state2bool(self) L205
  "A dictionary mapping the ZigBee value to the internal value."

class ZigBeeDigitalOutConfig(ZigBeePinConfig) L213
  "A subclass of ZigBeePinConfig."
  __init__(self, config) L220
  "Initialize the ZigBee Digital out."
  boolean_maps(self) L227
  "Create dicts to map booleans to pin high/low and vice versa."
  bool2state(self) L247
  "A dictionary mapping booleans to GPIOSetting objects."
  state2bool(self) L255
  "A dictionary mapping GPIOSetting objects to booleans."

class ZigBeeAnalogInConfig(ZigBeePinConfig) L263
  "Representation of a ZigBee GPIO pin set to analog in."
  max_voltage(self) L267
  "The voltage at which the ADC will report its highest value."

class ZigBeeDigitalIn(Entity) L272
  "Representation of a GPIO pin configured as a digital input."
  __init__(self, hass, config) L275
  "Initialize the device."
  async_added_to_hass(self) L281
  "Register callbacks."
  handle_frame(frame) L283
  "Handle an incoming frame."
  name(self) L303
  "Return the name of the input."
  config(self) L308
  "The entity's configuration."
  should_poll(self) L313
  "Return the state of the polling, if needed."
  is_on(self) L318
  "Return True if the Entity is on, else False."
  update(self) L322
  "Ask the ZigBee device what state its input pin is in."

class ZigBeeDigitalOut(ZigBeeDigitalIn) L345
  "Representation of a GPIO pin configured as a digital input."
  _set_state(self, state) L348
  "Initialize the ZigBee digital out device."
  turn_on(self, **kwargs) L368
  "Set the digital output to its 'on' state."
  turn_off(self, **kwargs) L372
  "Set the digital output to its 'off' state."
  update(self) L376
  "Ask the ZigBee device what its output is set to."

class ZigBeeAnalogIn(Entity) L395
  "Representation of a GPIO pin configured as an analog input."
  __init__(self, hass, config) L398
  "Initialize the ZigBee analog in device."
  async_added_to_hass(self) L404
  "Register callbacks."
  handle_frame(frame) L406
  "Handle an incoming frame."
  name(self) L430
  "The name of the input."
  config(self) L435
  "The entity's configuration."
  should_poll(self) L440
  "The state of the polling, if needed."
  state(self) L445
  "Return the state of the entity."
  unit_of_measurement(self) L450
  "Return the unit this state is expressed in."
  update(self) L454
  "Get the latest reading from the ADC."

setup(hass, config) L63
"Setup the connection to the ZigBee device."
_frame_received(frame) L100
"Called when a ZigBee frame is received."
close_serial_port(*args) L113
"Close the serial port we're using to communicate with the ZigBee."
frame_is_relevant(entity, frame) L118
"Test whether the frame is relevant to the entity."

homeassistant/components/zoneminder.py
setup(hass, config) L43
"Set up the ZoneMinder component."
login() L71
"Login to the ZoneMinder API."
_zm_request(method, api_url, data=None) L97
"Perform a Zoneminder request."
get_state(api_url) L122
"Get a state from the ZoneMinder API service."
change_state(api_url, post_data) L128
"Update a state using the Zoneminder API."

homeassistant/components/zwave/__init__.py
class ZWaveDeviceEntityValues L625
  "Manages entity access to the underlying zwave value objects."
  __init__(self, hass, schema, primary_value, zwave_config, device_config) L628
  "Initialize the values object with the passed entity schema."
  __getattr__(self, name) L654
  "Get the specified value for this entity."
  __iter__(self) L658
  "Allow iteration over all values."
  check_value(self, value) L662
  "Check if the new value matches a missing value for this entity."
  _check_entity_ready(self) L682
  "Check if all required values are discovered and create entity."
  discover_device(component, device, dict_id) L751
  "Put device in a dictionary and call discovery on it."

class ZWaveDeviceEntity(ZWaveBaseEntity) L760
  "Representation of a Z-Wave node entity."
  __init__(self, values, domain) L763
  "Initialize the z-Wave device."
  network_value_changed(self, value) L782
  "Called when a value has changed on the network."
  value_added(self) L787
  "Called when a new value is added to this entity."
  value_changed(self) L791
  "Called when a value for this entity's node has changed."
  async_added_to_hass(self) L798
  "Add device to dict."
  _update_attributes(self) L805
  "Update the node attributes."
  update_properties(self) L815
  "Callback on data changes for node values."
  should_poll(self) L820
  "No polling needed."
  unique_id(self) L825
  "Return an unique ID."
  name(self) L830
  "Return the name of the device."
  device_state_attributes(self) L835
  "Return the device specific state attributes."
  refresh_from_network(self) L846
  "Refresh all dependent values from zwave network."

_obj_to_dict(obj) L140
"Convert an object into a hash for debug."
_value_name(value) L147
"Return the name of the value."
_node_object_id(node) L152
"Return the object_id of the node."
object_id(value) L158
"Return the object_id of the device value."
nice_print_node(node) L173
"Print a nice formatted node to the output (debug method)."
get_config_value(node, value_index, tries=5) L185
"Return the current configuration value for a specific index."
async_setup_platform(hass, config, async_add_devices, discovery_info=None) L201
"Generic Z-Wave platform setup."
setup(hass, config) L216
"Setup Z-Wave."
log_all(signal, value=None) L262
"Log all the signals."
value_added(node, value) L280
"Called when a value is added to a node on the network."
node_added(node) L300
"Called when a node is added on the network."
scene_activated(node, scene_id) L311
"Called when a scene is activated on any node in the network."
node_event_activated(node, value) L319
"Called when a nodeevent is activated on any node in the network."
network_ready() L326
"Called when all awake nodes have been queried."
network_complete() L333
"Called when all nodes on network have been queried."
add_node(service) L352
"Switch into inclusion mode."
add_node_secure(service) L357
"Switch into secure inclusion mode."
remove_node(service) L362
"Switch into exclusion mode."
cancel_command(service) L367
"Cancel a running controller command."
heal_network(service) L372
"Heal the network."
soft_reset(service) L377
"Soft reset the controller."
test_network(service) L382
"Test the network by sending commands to all the nodes."
stop_network(_service_or_event) L387
"Stop Z-Wave network."
rename_node(service) L394
"Rename a node."
remove_failed_node(service) L403
"Remove failed node."
replace_failed_node(service) L409
"Replace failed node."
set_config_parameter(service) L415
"Set a config parameter to a node."
print_config_parameter(service) L444
"Print a config parameter from a node."
print_node(service) L452
"Print all information about z-wave node."
set_wakeup(service) L458
"Set wake-up interval of a node."
change_association(service) L471
"Change an association in the zwave network."
async_refresh_entity(service) L492
"Refresh values that specific entity depends on."
refresh_node(service) L498
"Refresh all node info."
start_zwave(_service_or_event) L504
"Startup Z-Wave network."

homeassistant/components/zwave/node_entity.py
class ZWaveBaseEntity(Entity) L34
  "Base class for Z-Wave Node and Value entities."
  __init__(self) L37
  "Initialize the base Z-Wave class."
  maybe_schedule_update(self) L41
  "Maybe schedule state update."
  _schedule_update(self) L51
  "Schedule delayed update."
  do_update() L57
  "Really update."

class ZWaveNodeEntity(ZWaveBaseEntity) L71
  "Representation of a Z-Wave node."
  __init__(self, node, network) L74
  "Initialize node."
  network_node_changed(self, node=None, args=None) L98
  "Called when node has changed on the network."
  get_node_statistics(self) L107
  "Retrieve statistics from the node."
  node_changed(self) L112
  "Update node properties."
  state(self) L137
  "Return the state."
  should_poll(self) L154
  "No polling needed."
  name(self) L159
  "Return the name of the device."
  device_state_attributes(self) L164
  "Return the device specific state attributes."

sub_status(status, stage) L66
"Format sub-status."

homeassistant/components/zwave/util.py
check_node_schema(node, schema) L9
"Check if node matches the passed node schema."
check_value_schema(value, schema) L29
"Check if the value matches the passed value schema."
node_name(node) L74
"Return the name of the node."

homeassistant/components/zwave/workaround.py
get_device_component_mapping(value) L70
"Get mapping of value to another component."
get_device_mapping(value) L82
"Get mapping of value to a workaround."

homeassistant/config.py
get_default_config_dir() L130
"Put together the default configuration directory based on OS."
ensure_config_exists(config_dir, detect_location=True) L137
"Ensure a config file exists in given configuration directory."
create_default_config(config_dir, detect_location=True) L153
"Create a default configuration file in given configuration directory."
async_hass_config_yaml(hass) L214
"Load YAML from hass config File."
_load_hass_yaml_config() L222
find_config_file(config_dir) L231
"Look in given directory for supported configuration files."
load_yaml_config_file(config_path) L241
"Parse a YAML configuration file."
process_ha_config_upgrade(hass) L261
"Upgrade config if necessary."
async_log_exception(ex, domain, config, hass) L290
"Generate log exception for config validation."
async_process_ha_core_config(hass, config) L319
"Process the [homeassistant] section from the config."
set_time_zone(time_zone_str) L327
"Helper method to set time zone."
_log_pkg_error(package, component, config, message) L437
"Log an error while merging."
_identify_config_schema(module) L450
"Extract the schema and identify list or dict based."
merge_packages_config(config, packages) L465
"Merge packages into the top-level config."
async_process_component_config(hass, config, domain) L528
"Check component config and return processed config."
async_check_ha_config_file(hass) L589
"Check if HA config file valid."
async_notify_setup_error(hass, component, link=False) L609
"Print a persistent notification."

homeassistant/exceptions.py
class HomeAssistantError(Exception) L4
  "General Home Assistant exception occurred."

class InvalidEntityFormatError(HomeAssistantError) L10
  "When an invalid formatted entity is encountered."

class NoEntitySpecifiedError(HomeAssistantError) L16
  "When no entity is specified."

class TemplateError(HomeAssistantError) L22
  "Error during template rendering."
  __init__(self, exception) L25
  "Initalize the error."

homeassistant/helpers/__init__.py
config_per_platform(config, domain) L18
"Generator to break a component config into different platforms."
extract_domain_configs(config, domain) L42
"Extract keys from config for given domain name."

homeassistant/helpers/aiohttp_client.py
async_get_clientsession(hass, verify_ssl=True) L24
"Return default aiohttp ClientSession."
async_create_clientsession(hass, verify_ssl=True, auto_cleanup=True, **kwargs) L48
"Create a new ClientSession with kwargs, i.e. for cookies."
async_aiohttp_proxy_web(hass, request, web_coro, buffer_size=102400, timeout=10) L74
"Stream websession request to aiohttp web response."
async_aiohttp_proxy_stream(hass, request, stream, content_type, buffer_size=102400, timeout=10) L98
"Stream a stream to aiohttp web response."
_async_register_clientsession_shutdown(hass, clientsession) L127
"Register ClientSession close on homeassistant shutdown."
_async_close_websession(event) L133
"Close websession."
_async_get_connector(hass, verify_ssl=True) L142
"Return the connector pool for aiohttp."
_async_close_connector(event) L166
"Close connector pool."

homeassistant/helpers/config_validation.py
has_at_least_one_key(*keys) L46
"Validator that at least one key exists."
validate(obj) L48
"Test keys exist in dict."
boolean(value) L61
"Validate and coerce a boolean value."
isdevice(value) L73
"Validate that value is a real device."
isfile(value) L82
"Validate that the value is an existing file."
ensure_list(value) L95
"Wrap value in list if it is not one."
entity_id(value) L102
"Validate Entity ID."
entity_ids(value) L110
"Validate Entity IDs."
enum(enumClass) L120
"Create validator for specified enum."
icon(value) L125
"Validate icon."
time_period_str(value) L148
"Validate and transform time offset."
time_period_seconds(value) L183
"Validate and transform seconds to a time offset."
match_all(value) L195
"Validator that matches all values."
platform_validator(domain) L200
"Validate if platform exists for given domain."
validator(value) L202
"Test if platform exists."
positive_timedelta(value) L213
"Validate timedelta is positive."
service(value) L220
"Validate service."
slug(value) L229
"Validate value is a valid slug."
slugify(value) L240
"Coerce a value to a slug."
string(value) L250
"Coerce value to string, except for None."
temperature_unit(value) L257
"Validate and transform temperature unit."
template(value) L271
"Validate a jinja2 template."
template_complex(value) L287
"Validate a complex jinja2 template."
time(value) L301
"Validate time."
datetime(value) L311
"Validate datetime."
time_zone(value) L327
"Validate timezone."
socket_timeout(value) L339
"Validate timeout float > 0.0."
url(value) L358
"Validate an URL."
x10_address(value) L368
"Validate an x10 address."
ordered_dict(value_validator, key_validator=match_all) L376
"Validate an ordered dict validator that maintains ordering."
validator(value) L384
"Validate ordered dict."
ensure_list_csv(value) L399
"Ensure that input is a list or make one from comma-separated string."
key_dependency(key, dependency) L408
"Validate that all dependencies exist for key."
validator(value) L410
"Test dependencies."

homeassistant/helpers/deprecation.py
deprecated_substitute(substitute_name) L7
"Help migrate properties to new names."
decorator(func) L15
"Decorator function."
func_wrapper(self) L17
"Wrapper for original function."
get_deprecated(config, new_name, old_name, default=None) L43
"Allow an old config name to be deprecated with a replacement."

homeassistant/helpers/entity_values.py
class EntityValues(object) L9
  "Class to store entity id based values."
  __init__(self, exact=None, domain=None, glob=None) L12
  "Initialize an EntityConfigDict."
  get(self, entity_id) L27
  "Get config for an entity id."

homeassistant/helpers/location.py
has_location(state) L10
"Test if state contains a valid location."
closest(latitude, longitude, states) L20
"Return closest state to point."

homeassistant/helpers/restore_state.py
_load_restore_cache(hass) L21
"Load the restore cache to be used by other components."
remove_cache(event) L24
"Remove the states cache."
async_get_last_state(hass, entity_id) L52
"Helper to restore state."
async_restore_state(entity, extract_info) L86
"Helper to call entity.async_restore_state with cached info."

homeassistant/helpers/signal.py
async_register_signal_handling(hass) L13
"Register system signal handler for core."
async_signal_handle(exit_code) L17
"Wrap signal handling."

homeassistant/loader.py
prepare(hass) L44
"Prepare the loading of components."
set_component(comp_name, component) L88
"Set a component in the cache."
get_platform(domain, platform) L98
"Try to load specified platform."
get_component(comp_name) L106
"Try to load specified component."
load_order_component(comp_name) L173
"Return an OrderedSet of components in the correct order of loading."
_load_order_component(comp_name, load_order, loading) L184
"Recursive function to get load order of components."
_check_prepared() L225
"Issue a warning if loader.prepare() has never been called."

homeassistant/remote.py
class APIStatus(enum.Enum) L35
  "Represent API status."
  __str__(self) L44
  "Return the state."

class API(object) L49
  "Object to pass around Home Assistant API location and credentials."
  __init__(self, host, api_password=None, port=SERVER_PORT, use_ssl=False) L52
  "Initalize the API."
  validate_api(self, force_validate=False) L77
  "Test if we can communicate with the API."
  __call__(self, method, path, data=None, timeout=5) L84
  "Make a call to the Home Assistant API."
  __repr__(self) L109
  "Return the representation of the API."

class JSONEncoder(json.JSONEncoder) L115
  "JSONEncoder that supports Home Assistant objects."
  default(self, obj) L119
  "Convert Home Assistant objects."

validate_api(api) L144
"Make a call to validate API."
get_event_listeners(api) L162
"List of events that is being listened for."
fire_event(api, event_type, data=None) L176
"Fire an event at remote API."
get_state(api, entity_id) L189
"Query given API for state of entity_id."
get_states(api) L206
"Query given API for all states."
remove_state(api, entity_id) L222
"Call API to remove state for entity_id."
set_state(api, entity_id, new_state, attributes=None, force_update=False) L242
"Tell API to update state for entity_id."
is_state(api, entity_id, state) L271
"Query API to see if entity_id is specified state."
get_services(api) L278
"Return a list of dicts."
call_service(api, domain, service, service_data=None, timeout=5) L295
"Call a service at the remote API."
get_config(api) L310
"Return configuration."

homeassistant/util/__init__.py
class OrderedEnum(enum.Enum) L116
  "Taken from Python 3.4.0 docs."
  __ge__(self, other) L120
  "Return the greater than element."
  __gt__(self, other) L126
  "Return the greater element."
  __le__(self, other) L132
  "Return the lower than element."
  __lt__(self, other) L138
  "Return the lower element."

class OrderedSet(MutableSet) L145
  "Ordered set taken from http://code.activestate.com/recipes/576694/."
  __init__(self, iterable=None) L148
  "Initialize the set."
  __len__(self) L156
  "Return the length of the set."
  __contains__(self, key) L160
  "Check if key is in set."
  add(self, key) L164
  "Add an element to the end of the set."
  promote(self, key) L171
  "Promote element to beginning of the set, add if not there."
  discard(self, key) L180
  "Discard an element from the set."
  __iter__(self) L187
  "Iteration of the set."
  __reversed__(self) L195
  "Reverse the ordering."
  pop(self, last=True) L204
  "Pop element of the end of the set."
  update(self, *args) L215
  "Add elements from args to the set."
  __repr__(self) L220
  "Return the representation."
  __eq__(self, other) L226
  "Return the comparision."

class Throttle(object) L233
  "A class for throttling the execution of tasks."
  __init__(self, min_time, limit_no_throttle=None) L251
  "Initialize the throttle."
  __call__(self, method) L256
  "Caller for the throttle."
  wrapper(*args, **kwargs) L275
  "Wrapper that allows wrapped to be called only once per min_time."

sanitize_filename(filename) L30
"Sanitize a filename by removing .."
sanitize_path(path) L35
"Sanitize a path by removing ~ and .."
slugify(text) L40
"Slugify a given text."
repr_helper(inp) L51
"Help creating a more readable string representation of objects."
convert(value, to_type, default=None) L63
"Convert value to to_type, returns default if fails."
ensure_unique_string(preferred_string, current_strings) L73
"Return a string that is not present in current_strings."
get_local_ip() L92
"Try to determine the local IP address of the machine."
get_random_string(length=10) L108
"Return a random string with letters and digits."

homeassistant/util/color.py
color_name_to_rgb(color_name) L171
"Convert color name to RGB hex value."
color_RGB_to_xy(iR, iG, iB) L187
"Convert from RGB color to XY color."
color_xy_brightness_to_RGB(vX, vY, ibrightness) L223
"Convert from XYZ to RGB."
color_RGB_to_hsv(iR, iG, iB) L264
"Convert an rgb color to its hsv representation."
color_xy_brightness_to_hsv(vX, vY, ibrightness) L271
"Convert an xy brightness color to its hsv representation."
_match_max_scale(input_colors, output_colors) L278
"Match the maximum value of the output to the input."
color_rgb_to_rgbw(r, g, b) L290
"Convert an rgb color to an rgbw representation."
color_rgbw_to_rgb(r, g, b, w) L302
"Convert an rgbw color to an rgb representation."
color_rgb_to_hex(r, g, b) L312
"Return a RGB color from a hex color string."
rgb_hex_to_rgb_list(hex_string) L317
"Return an RGB color value list from a hex color string."
color_temperature_to_rgb(color_temperature_kelvin) L325
"Return an RGB color from a color temperature in Kelvin."
_bound(color_component, minimum=0, maximum=255) L349
"Bound the given color component value between the given min and max values."
_get_red(temperature) L362
"Get the red component of the temperature in RGB space."
_get_green(temperature) L370
"Get the green component of the given color temp in RGB space."
_get_blue(temperature) L379
"Get the blue component of the given color temperature in RGB space."
color_temperature_mired_to_kelvin(mired_temperature) L389
"Convert absolute mired shift to degrees kelvin."
color_temperature_kelvin_to_mired(kelvin_temperature) L394
"Convert degrees kelvin to mired shift."

homeassistant/util/distance.py
convert(value, unit_1, unit_2) L25
"Convert one unit of measurement to another."
__miles_to_meters(miles) L61
"Convert miles to meters."
__feet_to_meters(feet) L66
"Convert feet to meters."
__kilometers_to_meters(kilometers) L71
"Convert kilometers to meters."
__meters_to_miles(meters) L76
"Convert meters to miles."
__meters_to_feet(meters) L81
"Convert meters to feet."
__meters_to_kilometers(meters) L86
"Convert meters to kilometers."

homeassistant/util/dt.py
set_default_time_zone(time_zone) L25
"Set a default time zone to be used when none is specified."
get_time_zone(time_zone_str) L38
"Get time zone from string."
utcnow() L49
"Get now in UTC time."
now(time_zone=None) L54
"Get now in specified time zone."
as_utc(dattim) L59
"Return a datetime as UTC time."
as_timestamp(dt_value) L72
"Convert a date/time into a unix time (seconds since 1970)."
as_local(dattim) L83
"Convert a UTC datetime object to local time zone."
utc_from_timestamp(timestamp) L93
"Return a UTC time from a timestamp."
start_of_local_day(dt_or_d=None) L98
"Return local datetime object of start of day from date or datetime."
parse_datetime(dt_str) L111
"Parse a string and return a datetime.datetime."
parse_date(dt_str) L144
"Convert a date string to a date object."
parse_time(time_str) L152
"Parse a time string (00:20:00) into Time object."
get_age(date) L171
"Take a datetime and return its "age" as a string."
formatn(number, unit) L180
"Add "unit" if it's plural."
q_n_r(first, second) L188
"Return quotient and remaining."

homeassistant/util/location.py
detect_location_info() L36
"Detect location information."
distance(lat1, lon1, lat2, lon2) L52
"Calculate the distance in meters between two points."
elevation(latitude, longitude) L60
"Return elevation for given latitude and longitude."
vincenty(point1, point2, miles=False) L86
"Vincenty formula (inverse method) to calculate the distance."
_get_freegeoip() L156
"Query freegeoip.io for location data."
_get_ip_api() L177
"Query ip-api.com for location data."

homeassistant/util/package.py
install_package(package, upgrade=True, target=None, constraints=None) L17
"Install a package on PyPi."
check_package_exists(package, lib_dir) L49
"Check if a package is installed globally or in lib_dir."

homeassistant/util/temperature.py
fahrenheit_to_celsius(fahrenheit) L10
"Convert a Fahrenheit temperature to Celsius."
celsius_to_fahrenheit(celsius) L15
"Convert a Celsius temperature to Fahrenheit."
convert(temperature, from_unit, to_unit) L20
"Convert a temperature from one unit to another."

homeassistant/util/unit_system.py
class UnitSystem(object) L64
  "A container for units of measure."
  __init__(self, name, temperature, length, volume, mass) L67
  "Initialize the unit system object."
  is_metric(self) L89
  "Determine if this is the metric unit system."
  temperature(self, temperature, from_unit) L93
  "Convert the given temperature to this unit system."
  length(self, length, from_unit) L102
  "Convert the given length to this unit system."
  as_dict(self) L110
  "Convert the unit system to a dictionary."

is_valid_unit(unit, unit_type) L48
"Check if the unit is valid for it's type."

homeassistant/util/yaml.py
class NodeListClass(list) L25
  "Wrapper class to be able to add attributes on a list."

class NodeStrClass(str) L30
  "Wrapper class to be able to add attributes on a string."

class SafeLineLoader(yaml.SafeLoader) L45
  "Loader class that keeps track of line numbers."
  compose_node(self, parent, index) L48
  "Annotate a node with the first line it was seen."

_add_reference(obj, loader, node) L23
"Add file reference information to an object."
load_yaml(fname) L57
"Load a YAML file."
dump(_dict) L72
"Dump yaml to a string and remove null."
clear_secret_cache() L78
"Clear the secret cache."
_include_yaml(loader, node) L86
"Load another YAML file and embeds it using the !include tag."
_is_file_valid(name) L97
"Decide if a file is valid."
_find_files(directory, pattern) L102
"Recursively load files in a directory."
_include_dir_named_yaml(loader, node) L112
"Load multiple files from directory as a dictionary."
_include_dir_merge_named_yaml(loader, node) L123
"Load multiple files from directory as a merged dictionary."
_include_dir_list_yaml(loader, node) L137
"Load multiple files from directory as a list."
_include_dir_merge_list_yaml(loader, node) L145
"Load multiple files from directory as a merged list."
_ordered_dict(loader, node) L160
"Load YAML mappings into an ordered dictionary to preserve key order."
_construct_seq(loader, node) L192
"Add line number and file name to Load YAML sequence."
_env_var_yaml(loader, node) L198
"Load environment variables and embed it into the configuration YAML."
_load_secret_yaml(secret_path) L208
"Load the secrets yaml from path."
_secret_yaml(loader, node) L232
"Load secrets and embed it into the configuration YAML."
represent_odict(dump, tag, mapping, flow_style=None) L279
"Like BaseRepresenter.represent_mapping but does not issue the sort()."
