interpreter/archive/(wip)_model_explorer.py
model_explorer() L5
get_more_models(model_name=None, parameter_level=None) L8
select_model() L13
select_parameter_level(model) L23
select_quality_level(parameter_level) L34

interpreter/archive/cli.py
check_for_update() L34
cli(interpreter) L44
"Takes an instance of interpreter."

interpreter/archive/code_block.py
class CodeBlock L10
  "Code Blocks display code and outputs in different languages."
  __init__(self) L15
  update_from_message(self, message) L25
  end(self) L38
  refresh(self, cursor=True) L43

interpreter/archive/code_interpreter.py
class CodeInterpreter L88
  "Code Interpreters display and run code in different languages."
  __init__(self, language, debug_mode) L95
  start_process(self) L101
  update_active_block(self) L121
  "This will also truncate the output,"
  run(self) L134
  "Executes code."
  add_active_line_prints(self, code) L260
  "This function takes a code snippet and adds print statements before each line,"
  save_and_display_stream(self, stream, is_error_stream) L324

class AddLinePrints(ast.NodeTransformer) L398
  "Transformer to insert print statements indicating the line number"
  insert_print_statement(self, line_number) L404
  "Inserts a print statement for a given line number."
  process_body(self, body) L414
  "Processes a block of statements, adding print calls."
  visit(self, node) L429
  "Overridden visit to transform nodes."

run_html(html_content) L34
truncate_output(data) L376
add_active_line_prints_to_python(code) L450
"Add print statements indicating line numbers to a python string."
wrap_in_try_except(code) L459

interpreter/archive/get_hf_llm.py
get_hf_llm(repo_id, debug_mode, context_window) L34
check_command(command) L176
install_llama(backend) L185
supports_metal() L204
confirm_action(message) L251
list_gguf_files(repo_id) L271
"Fetch all files from a given repository on Hugging Face Model Hub that contain '..."
group_and_combine_splits(models) L308
"Groups filenames based on their base names and combines the sizes and RAM requir..."
actually_combine_files(default_path, base_name, files) L335
"Combines files together and deletes the original split files."
format_quality_choice(model, name_override=None) L351
"Formats the model choice for display in the inquirer prompt."
enough_disk_space(size, path) L361
"Checks the disk to verify there is enough space to download the model."

interpreter/archive/interpreter.py
class Interpreter L100
  __init__(self) L102
  cli(self) L137
  get_info_for_system_message(self) L142
  "Gets relevant information for the system message."
  reset(self) L190
  "Resets the interpreter."
  load(self, messages) L197
  handle_undo(self, arguments) L201
  handle_help(self, arguments) L232
  handle_debug(self, arguments=None) L260
  handle_reset(self, arguments) L271
  default_handle(self, arguments) L275
  handle_save_message(self, json_path) L279
  handle_load_message(self, json_path) L289
  handle_command(self, user_input) L302
  chat(self, message=None, return_messages=False) L320
  verify_api_key(self) L439
  "Makes sure we have an AZURE_API_KEY or OPENAI_API_KEY."
  end_active_block(self) L580
  respond(self) L585
  messages_to_prompt(messages) L705
  _print_welcome_message(self) L1011

interpreter/archive/message_block.py
class MessageBlock L9
  __init__(self) L11
  update_from_message(self, message) L16
  end(self) L21
  refresh(self, cursor=True) L25

textify_markdown_code_blocks(text) L39
"To distinguish CodeBlocks from markdown code, we simply turn all markdown code"

interpreter/archive/utils.py
merge_deltas(original, delta) L4
"Pushes the delta into the original and returns that."
parse_partial_json(s) L23

interpreter/cli/cli.py
cli(interpreter) L78

interpreter/code_interpreters/base_code_interpreter.py
class BaseCodeInterpreter L3
  ".run is a generator that yields a dict with attributes: active_line, output"
  __init__(self) L7
  run(self, code) L10
  terminate(self) L13

interpreter/code_interpreters/create_code_interpreter.py
create_code_interpreter(language) L10

interpreter/code_interpreters/languages/applescript.py
class AppleScript(SubprocessCodeInterpreter) L4
  __init__(self) L5
  preprocess_code(self, code) L9
  "Inserts an end_of_execution marker and adds active line indicators."
  add_active_line_indicators(self, code) L30
  "Adds log commands to indicate the active line of execution in the AppleScript."
  detect_active_line(self, line) L45
  "Detects active line indicator in the output."
  detect_end_of_execution(self, line) L57
  "Detects end of execution marker in the output."

interpreter/code_interpreters/languages/html.py
class HTML(BaseCodeInterpreter) L6
  __init__(self) L7
  run(self, code) L10

interpreter/code_interpreters/languages/javascript.py
class JavaScript(SubprocessCodeInterpreter) L4
  __init__(self) L5
  preprocess_code(self, code) L9
  line_postprocessor(self, line) L12
  detect_active_line(self, line) L23
  detect_end_of_execution(self, line) L28

preprocess_javascript(code) L32
"Add active line markers"

interpreter/code_interpreters/languages/python.py
class Python(SubprocessCodeInterpreter) L6
  __init__(self) L7
  preprocess_code(self, code) L11
  line_postprocessor(self, line) L14
  detect_active_line(self, line) L19
  detect_end_of_execution(self, line) L24

class AddLinePrints(ast.NodeTransformer) L63
  "Transformer to insert print statements indicating the line number"
  insert_print_statement(self, line_number) L69
  "Inserts a print statement for a given line number."
  process_body(self, body) L79
  "Processes a block of statements, adding print calls."
  visit(self, node) L94
  "Overridden visit to transform nodes."

preprocess_python(code) L28
"Add active line markers"
add_active_line_prints(code) L53
"Add print statements indicating line numbers to a python string."
wrap_in_try_except(code) L116

interpreter/code_interpreters/languages/r.py
class R(SubprocessCodeInterpreter) L4
  __init__(self) L5
  preprocess_code(self, code) L9
  "Add active line markers"
  line_postprocessor(self, line) L41
  detect_active_line(self, line) L58
  detect_end_of_execution(self, line) L63

interpreter/code_interpreters/languages/shell.py
class Shell(SubprocessCodeInterpreter) L6
  __init__(self) L7
  preprocess_code(self, code) L16
  line_postprocessor(self, line) L19
  detect_active_line(self, line) L22
  detect_end_of_execution(self, line) L27

preprocess_shell(code) L31
"Add active line markers"
add_active_line_prints(code) L50
"Add echo statements indicating line numbers to a shell string."
wrap_in_trap(code) L61
"Wrap Bash code with a trap to catch errors and display them."

interpreter/code_interpreters/subprocess_code_interpreter.py
class SubprocessCodeInterpreter(BaseCodeInterpreter) L10
  __init__(self) L11
  detect_active_line(self, line) L18
  detect_end_of_execution(self, line) L21
  line_postprocessor(self, line) L24
  preprocess_code(self, code) L27
  "This needs to insert an end_of_execution marker of some kind,"
  terminate(self) L36
  start_process(self) L39
  run(self, code) L57
  handle_stream_output(self, stream, is_error_stream) L115

interpreter/core/core.py
class Interpreter L19
  cli(self) L20
  __init__(self) L23
  chat(self, message=None, display=True, stream=False) L60
  _streaming_chat(self, message=None, display=True) L70
  _respond(self) L108
  reset(self) L111

interpreter/core/respond.py
respond(interpreter) L10
"Yields tokens, but also adds them to interpreter.messages. TBH probably would be..."

interpreter/llm/convert_to_coding_llm.py
convert_to_coding_llm(text_llm, debug_mode=False) L6
"Takes a text_llm"
coding_llm(messages) L12

interpreter/llm/setup_llm.py
setup_llm(interpreter) L8
"Takes an Interpreter (which includes a ton of LLM settings),"

interpreter/llm/setup_local_text_llm.py
setup_local_text_llm(interpreter) L21
check_command(command) L168
install_llama(backend) L177
supports_metal() L196
local_text_llm(messages) L250
"Returns a generator"
messages_to_prompt(messages, model) L322
confirm_action(message) L380
list_gguf_files(repo_id) L397
"Fetch all files from a given repository on Hugging Face Model Hub that contain '..."
group_and_combine_splits(models) L434
"Groups filenames based on their base names and combines the sizes and RAM requir..."
actually_combine_files(default_path, base_name, files) L461
"Combines files together and deletes the original split files."
format_quality_choice(model, name_override=None) L477
"Formats the model choice for display in the inquirer prompt."
enough_disk_space(size, path) L487
"Checks the disk to verify there is enough space to download the model."

interpreter/llm/setup_openai_coding_llm.py
setup_openai_coding_llm(interpreter) L30
"Takes an Interpreter (which includes a ton of LLM settings),"
coding_llm(messages) L36

interpreter/llm/setup_text_llm.py
setup_text_llm(interpreter) L11
"Takes an Interpreter (which includes a ton of LLM settings),"
base_llm(messages) L64
"Returns a generator"

interpreter/rag/get_relevant_procedures.py
get_relevant_procedures(messages) L4

interpreter/terminal_interface/components/base_block.py
class BaseBlock L4
  "a visual "block" on the terminal."
  __init__(self) L8
  update_from_message(self, message) L12
  end(self) L15
  refresh(self, cursor=True) L19

interpreter/terminal_interface/components/code_block.py
class CodeBlock(BaseBlock) L8
  "Code Blocks display code and outputs in different languages. You can also set th..."
  __init__(self) L13
  refresh(self, cursor=True) L25

interpreter/terminal_interface/components/message_block.py
class MessageBlock(BaseBlock) L7
  __init__(self) L9
  refresh(self, cursor=True) L16

textify_markdown_code_blocks(text) L30
"To distinguish CodeBlocks from markdown code, we simply turn all markdown code"

interpreter/terminal_interface/conversation_navigator.py
conversation_navigator(interpreter) L14
open_folder(path) L61

interpreter/terminal_interface/magic_commands.py
handle_undo(self, arguments) L5
handle_help(self, arguments) L36
handle_debug(self, arguments=None) L64
handle_reset(self, arguments) L75
default_handle(self, arguments) L79
handle_save_message(self, json_path) L83
handle_load_message(self, json_path) L93
handle_magic_command(self, user_input) L103

interpreter/terminal_interface/render_past_conversation.py
render_past_conversation(messages) L6

interpreter/terminal_interface/terminal_interface.py
terminal_interface(interpreter, message) L12

interpreter/terminal_interface/validate_llm_settings.py
validate_llm_settings(interpreter) L7
"Interactivley prompt the user for required LLM settings"
display_welcome_message_once() L101
"Displays a welcome message only on its first call."

interpreter/utils/check_for_update.py
check_for_update() L5

interpreter/utils/convert_to_openai_messages.py
convert_to_openai_messages(messages) L3

interpreter/utils/display_markdown_message.py
display_markdown_message(message) L5
"Display markdown message. Works with multiline strings with lots of indentation."

interpreter/utils/get_config.py
get_config() L13

interpreter/utils/get_conversations.py
get_conversations() L7

interpreter/utils/get_local_models_paths.py
get_local_models_paths() L7

interpreter/utils/get_user_info_string.py
get_user_info_string() L5

interpreter/utils/merge_deltas.py
merge_deltas(original, delta) L4
"Pushes the delta into the original and returns that."

interpreter/utils/parse_partial_json.py
parse_partial_json(s) L4

interpreter/utils/truncate_output.py
truncate_output(data, max_output_chars=2000) L1
