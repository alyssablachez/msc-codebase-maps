sklearn/__init__.py
setup_module(module) L98
"Fixture for the tests to assure globally controllable seeding of RNGs"

sklearn/_config.py
get_config() L14
"Retrieve current values for configuration set by :func:`set_config`"
set_config(assume_finite=None, working_memory=None, print_changed_only=None, display=None) L30
"Set global scikit-learn configuration"
config_context(**new_config) L86
"Context manager for global scikit-learn configuration"

sklearn/_loss/glm_distribution.py
class ExponentialDispersionModel L20
  "Base class for reproductive Exponential Dispersion Models (EDM)."
  in_y_range(self, y) L49
  "Returns ``True`` if y is in the valid range of Y~EDM."
  unit_variance(self, y_pred) L69
  "Compute the unit variance function."
  unit_deviance(self, y, y_pred, check_input=False) L91
  "Compute the unit deviance."
  unit_deviance_derivative(self, y, y_pred) L116
  "Compute the derivative of the unit deviance w.r.t."
  deviance(self, y, y_pred, weights=1) L134
  "Compute the deviance."
  deviance_derivative(self, y, y_pred, weights=1) L158
  "Compute the derivative of the deviance w.r.t."

class TweedieDistribution(ExponentialDispersionModel) L178
  "A class for the Tweedie distribution."
  __init__(self, power=0) L203
  power(self) L207
  power(self, power) L211
  unit_variance(self, y_pred) L237
  "Compute the unit variance of a Tweedie distribution"
  unit_deviance(self, y, y_pred, check_input=False) L248
  "Compute the unit deviance."

class NormalDistribution(TweedieDistribution) L326
  "Class for the Normal (aka Gaussian) distribution"
  __init__(self) L328

class PoissonDistribution(TweedieDistribution) L332
  "Class for the scaled Poisson distribution"
  __init__(self) L334

class GammaDistribution(TweedieDistribution) L338
  "Class for the Gamma distribution"
  __init__(self) L340

class InverseGaussianDistribution(TweedieDistribution) L344
  "Class for the scaled InverseGaussianDistribution distribution"
  __init__(self) L346

sklearn/base.py
class BaseEstimator L148
  "Base class for all estimators in scikit-learn"
  _get_param_names(cls) L159
  "Get parameter names for the estimator"
  get_params(self, deep=True) L185
  "Get parameters for this estimator."
  set_params(self, **params) L217
  "Set the parameters of this estimator."
  __repr__(self, N_CHAR_MAX=700) L261
  __getstate__(self) L310
  __setstate__(self, state) L321
  _more_tags(self) L336
  _get_tags(self) L339
  _check_n_features(self, X, reset) L350
  "Set the `n_features_in_` attribute, or check against it."
  _validate_data(self, X, y=None, reset=True, validate_separately=False, **check_params) L379
  "Validate input data and set or check the `n_features_in_` attribute."
  _repr_html_(self) L437
  "HTML representation of estimator."
  _repr_html_inner(self) L450
  "This function is returned by the @property `_repr_html_` to make"
  _repr_mimebundle_(self, **kwargs) L457
  "Mime bundle used by jupyter kernels to display estimator"

class ClassifierMixin L465
  "Mixin class for all classifiers in scikit-learn."
  score(self, X, y, sample_weight=None) L470
  "Return the mean accuracy on the given test data and labels."
  _more_tags(self) L497

class RegressorMixin L501
  "Mixin class for all regression estimators in scikit-learn."
  score(self, X, y, sample_weight=None) L505
  "Return the coefficient of determination R^2 of the prediction."
  _more_tags(self) L550

class ClusterMixin L554
  "Mixin class for all cluster estimators in scikit-learn."
  fit_predict(self, X, y=None) L558
  "Perform clustering on X and returns cluster labels."

class BiclusterMixin L581
  "Mixin class for all bicluster estimators in scikit-learn"
  biclusters_(self) L585
  "Convenient way to get row and column indicators together."
  get_indices(self, i) L592
  "Row and column indices of the i'th bicluster."
  get_shape(self, i) L614
  "Shape of the i'th bicluster."
  get_submatrix(self, i, data) L630
  "Return the submatrix corresponding to bicluster `i`."

class TransformerMixin L656
  "Mixin class for all transformers in scikit-learn."
  fit_transform(self, X, y=None, **fit_params) L659
  "Fit to data, then transform it."

class DensityMixin L693
  "Mixin class for all density estimators in scikit-learn."
  score(self, X, y=None) L697
  "Return the score of the model on the data X"

class OutlierMixin L714
  "Mixin class for all outlier detection estimators in scikit-learn."
  fit_predict(self, X, y=None) L718
  "Perform fit on X and returns labels for X."

class MetaEstimatorMixin L740

class MultiOutputMixin L745
  "Mixin to mark estimators that support multioutput."
  _more_tags(self) L747

class _UnstableArchMixin L751
  "Mark estimators that are non-determinstic on 32bit or PowerPC"
  _more_tags(self) L753

clone(estimator, *, safe=True) L44
"Constructs a new estimator with the same parameters."
_pprint(params, offset=0, printer=repr) L98
"Pretty print the dictionary 'params'"
is_classifier(estimator) L758
"Return True if the given estimator is (probably) a classifier."
is_regressor(estimator) L774
"Return True if the given estimator is (probably) a regressor."
is_outlier_detector(estimator) L790
"Return True if the given estimator is (probably) an outlier detector."

sklearn/calibration.py
class CalibratedClassifierCV(ClassifierMixin, MetaEstimatorMixin, BaseEstimator) L35
  "Probability calibration with isotonic regression or logistic regression."
  __init__(self, base_estimator=None, *, method='sigmoid', cv=None) L158
  fit(self, X, y, sample_weight=None) L163
  "Fit the calibrated model"
  predict_proba(self, X) L258
  "Posterior probabilities of classification"
  predict(self, X) L288
  "Predict the target of new samples."
  _more_tags(self) L306

class _CalibratedClassifier L315
  "Probability calibration with isotonic regression or sigmoid."
  __init__(self, base_estimator, *, method='sigmoid', classes=None) L359
  _preproc(self, X) L364
  fit(self, X, y, sample_weight=None) L383
  "Calibrate the fitted model"
  predict_proba(self, X) L428
  "Posterior probabilities of classification"

class _SigmoidCalibration(RegressorMixin, BaseEstimator) L533
  "Sigmoid regression model."
  fit(self, X, y, sample_weight=None) L544
  "Fit the model using X, y as training data."
  predict(self, T) L570
  "Predict new data by linear interpolation."

_sigmoid_calibration(df, y, sample_weight=None) L470
"Probability Calibration with sigmoid method (Platt 2000)"
objective(AB) L509
grad(AB) L518
calibration_curve(y_true, y_prob, *, normalize=False, n_bins=5, strategy='uniform') L588
"Compute true and predicted probabilities for a calibration curve."

sklearn/cluster/_affinity_propagation.py
class AffinityPropagation(ClusterMixin, BaseEstimator) L256
  "Perform Affinity Propagation Clustering of data."
  __init__(self, *, damping=0.5, max_iter=200, convergence_iter=15, copy=True, preference=None, affinity='euclidean', verbose=False, random_state='warn') L363
  _pairwise(self) L377
  fit(self, X, y=None) L380
  "Fit the clustering from features, or affinity matrix."
  predict(self, X) L426
  "Predict the closest cluster each sample in X belongs to."
  fit_predict(self, X, y=None) L454
  "Fit the clustering from features or affinity matrix, and return"

_equal_similarities_and_preferences(S, preference) L19
all_equal_preferences() L20
all_equal_similarities() L23
affinity_propagation(S, *, preference=None, convergence_iter=15, max_iter=200, damping=0.5, copy=True, verbose=False, return_n_iter=False, random_state='warn') L34
"Perform Affinity Propagation Clustering of data"

sklearn/cluster/_agglomerative.py
class AgglomerativeClustering(ClusterMixin, BaseEstimator) L680
  "Agglomerative Clustering"
  __init__(self, n_clusters=2, *, affinity='euclidean', memory=None, connectivity=None, compute_full_tree='auto', linkage='ward', distance_threshold=None) L795
  fit(self, X, y=None) L807
  "Fit the hierarchical clustering from features, or distance matrix."
  fit_predict(self, X, y=None) L911
  "Fit the hierarchical clustering from features or distance matrix,"

class FeatureAgglomeration(AgglomerativeClustering, AgglomerationTransform) L933
  "Agglomerate features."
  __init__(self, n_clusters=2, *, affinity='euclidean', memory=None, connectivity=None, compute_full_tree='auto', linkage='ward', pooling_func=np.mean, distance_threshold=None) L1048
  fit(self, X, y=None, **params) L1059
  "Fit the hierarchical clustering on the data"
  fit_predict(self) L1083

_fix_connectivity(X, connectivity, affinity) L33
"Fixes the connectivity matrix"
_single_linkage_tree(connectivity, n_samples, n_nodes, n_clusters, n_connected_components, return_distance) L83
"Perform single linkage clustering on sparse data via the minimum"
ward_tree(X, *, connectivity=None, n_clusters=None, return_distance=False) L138
"Ward clustering based on a Feature matrix."
linkage_tree(X, connectivity=None, n_clusters=None, linkage='complete', affinity='euclidean', return_distance=False) L346
"Linkage agglomerative clustering based on a Feature matrix."
_complete_linkage(*args, **kwargs) L606
_average_linkage(*args, **kwargs) L611
_single_linkage(*args, **kwargs) L616
_hc_cut(n_clusters, children, n_leaves) L631
"Function cutting the ward tree for a given number of clusters."

sklearn/cluster/_bicluster.py
class BaseSpectral(BiclusterMixin, BaseEstimator) L86
  "Base class for spectral biclustering."
  __init__(self, n_clusters=3, svd_method='randomized', n_svd_vecs=None, mini_batch=False, init='k-means++', n_init=10, n_jobs='deprecated', random_state=None) L90
  _check_parameters(self) L102
  fit(self, X, y=None) L109
  "Creates a biclustering for X."
  _svd(self, array, n_components, n_discard) L128
  "Returns first `n_components` left and right singular"
  _k_means(self, data, n_clusters) L166

class SpectralCoclustering(BaseSpectral) L182
  "Spectral Co-Clustering algorithm (Dhillon, 2001)."
  __init__(self, n_clusters=3, *, svd_method='randomized', n_svd_vecs=None, mini_batch=False, init='k-means++', n_init=10, n_jobs='deprecated', random_state=None) L288
  _fit(self, X) L300

class SpectralBiclustering(BaseSpectral) L319
  "Spectral biclustering (Kluger, 2003)."
  __init__(self, n_clusters=3, *, method='bistochastic', n_components=6, n_best=3, svd_method='randomized', n_svd_vecs=None, mini_batch=False, init='k-means++', n_init=10, n_jobs='deprecated', random_state=None) L440
  _check_parameters(self) L456
  _fit(self, X) L485
  _fit_best_piecewise(self, vectors, n_best, n_clusters) L524
  "Find the ``n_best`` vectors that are best approximated by piecewise"
  make_piecewise(v) L532
  _project_and_cluster(self, data, vectors, n_clusters) L542
  "Project ``data`` to ``vectors`` and cluster the result."

_scale_normalize(X) L28
"Normalize ``X`` by scaling rows and columns independently."
_bistochastic_normalize(X, max_iter=1000, tol=1e-05) L50
"Normalize rows and columns of ``X`` simultaneously so that all"
_log_normalize(X) L72
"Normalize ``X`` according to Kluger's log-interactions scheme."

sklearn/cluster/_birch.py
class _CFNode L92
  "Each node in a CFTree is called a CFNode."
  __init__(self, *, threshold, branching_factor, is_leaf, n_features) L139
  append_subcluster(self, subcluster) L154
  update_split_subclusters(self, subcluster, new_subcluster1, new_subcluster2) L166
  "Remove a subcluster from a node and update it with the"
  insert_cf_subcluster(self, subcluster) L177
  "Insert a new subcluster into the node."

class _CFSubcluster L245
  "Each subcluster in a CFNode is called a CFSubcluster."
  __init__(self, *, linear_sum=None) L280
  update(self, subcluster) L292
  merge_subcluster(self, nominee_cluster, threshold) L299
  "Check if a cluster is worthy enough to be merged."
  radius(self) L318
  "Return radius of the subcluster"

class Birch(ClusterMixin, TransformerMixin, BaseEstimator) L326
  "Implements the Birch clustering algorithm."
  __init__(self, *, threshold=0.5, branching_factor=50, n_clusters=3, compute_labels=True, copy=True) L437
  fit(self, X, y=None) L445
  "Build a CF Tree for the input data."
  _fit(self, X) L465
  _get_leaves(self) L520
  "Retrieve the leaves of the CF Node."
  partial_fit(self, X=None, y=None) L536
  "Online learning."
  _check_fit(self, X) L564
  predict(self, X) L573
  "Predict data using the ``centroids_`` of subclusters."
  transform(self, X) L598
  "Transform X into subcluster centroids dimension."
  _global_clustering(self, X=None) L618
  "Global clustering for the subclusters obtained after fitting"

_iterate_sparse_X(X) L22
"This little hack returns a densified row when iterating over a sparse"
_split_node(node, threshold, branching_factor) L40
"The node has to be split if there is no place for a new subcluster"

sklearn/cluster/_dbscan.py
class DBSCAN(ClusterMixin, BaseEstimator) L150
  "Perform DBSCAN clustering from vector array or distance matrix."
  __init__(self, eps=0.5, *, min_samples=5, metric='euclidean', metric_params=None, algorithm='auto', leaf_size=30, p=None, n_jobs=None) L276
  fit(self, X, y=None, sample_weight=None) L288
  "Perform DBSCAN clustering from features, or distance matrix."
  fit_predict(self, X, y=None, sample_weight=None) L366
  "Perform DBSCAN clustering from features or distance matrix,"

dbscan(X, eps=0.5, *, min_samples=5, metric='minkowski', metric_params=None, algorithm='auto', leaf_size=30, p=2, sample_weight=None, n_jobs=None) L24
"Perform DBSCAN clustering from vector array or distance matrix."

sklearn/cluster/_feature_agglomeration.py
class AgglomerationTransform(TransformerMixin) L19
  "A class for feature agglomeration via the transform interface"
  transform(self, X) L24
  "Transform a new matrix using the built clustering"
  inverse_transform(self, Xred) L57
  "Inverse the transformation."

sklearn/cluster/_kmeans.py
class KMeans(TransformerMixin, ClusterMixin, BaseEstimator) L745
  "K-Means clustering."
  __init__(self, n_clusters=8, *, init='k-means++', n_init=10, max_iter=300, tol=0.0001, precompute_distances='deprecated', verbose=0, random_state=None, copy_x=True, n_jobs='deprecated', algorithm='auto') L910
  _check_test_data(self, X) L927
  fit(self, X, y=None, sample_weight=None) L939
  "Compute k-means clustering."
  fit_predict(self, X, y=None, sample_weight=None) L1082
  "Compute cluster centers and predict cluster index for each sample."
  fit_transform(self, X, y=None, sample_weight=None) L1107
  "Compute clustering and transform X to cluster-distance space."
  transform(self, X) L1135
  "Transform X to a cluster-distance space."
  _transform(self, X) L1157
  "guts of transform method; no input validation"
  predict(self, X, sample_weight=None) L1161
  "Predict the closest cluster each sample in X belongs to."
  score(self, X, y=None, sample_weight=None) L1190
  "Opposite of the value of X on the K-means objective."
  _more_tags(self) L1218

class MiniBatchKMeans(KMeans) L1436
  "Mini-Batch K-Means clustering."
  __init__(self, n_clusters=8, *, init='k-means++', max_iter=100, batch_size=100, verbose=0, compute_labels=True, random_state=None, tol=0.0, max_no_improvement=10, init_size=None, n_init=3, reassignment_ratio=0.01) L1577
  fit(self, X, y=None, sample_weight=None) L1592
  "Compute the centroids on X by chunking it into mini-batches."
  _labels_inertia_minibatch(self, X, sample_weight) L1748
  "Compute labels and inertia using mini batches."
  partial_fit(self, X, y=None, sample_weight=None) L1780
  "Update k means estimate on a single mini-batch X."
  predict(self, X, sample_weight=None) L1857
  "Predict the closest cluster each sample in X belongs to."
  _more_tags(self) L1883

_k_init(X, n_clusters, x_squared_norms, random_state, n_local_trials=None) L47
"Init n_clusters seeds according to k-means++"
_validate_center_shape(X, n_centers, centers) L146
"Check if centers is compatible with X and n_centers"
_tolerance(X, tol) L159
"Return a tolerance which is independent of the dataset"
_check_normalize_sample_weight(sample_weight, X) L170
"Set sample_weight if None, and check for correct dtype"
k_means(X, n_clusters, *, sample_weight=None, init='k-means++', precompute_distances='deprecated', n_init=10, max_iter=300, verbose=False, tol=0.0001, random_state=None, copy_x=True, n_jobs='deprecated', algorithm='auto', return_n_iter=False) L186
"K-means clustering algorithm."
_kmeans_single_elkan(X, sample_weight, n_clusters, max_iter=300, init='k-means++', verbose=False, x_squared_norms=None, random_state=None, tol=0.0001, n_threads=1) L326
"A single run of k-means elkan, assumes preparation completed prior."
_kmeans_single_lloyd(X, sample_weight, n_clusters, max_iter=300, init='k-means++', verbose=False, x_squared_norms=None, random_state=None, tol=0.0001, n_threads=1) L473
"A single run of k-means lloyd, assumes preparation completed prior."
_labels_inertia(X, sample_weight, x_squared_norms, centers, n_threads=None) L602
"E step of the K-means EM algorithm."
_init_centroids(X, n_clusters=8, init='k-means++', random_state=None, x_squared_norms=None, init_size=None) L663
"Compute the initial centroids"
_mini_batch_step(X, sample_weight, x_squared_norms, centers, weight_sums, old_center_buffer, compute_squared_diff, distances, random_reassign=False, random_state=None, reassignment_ratio=0.01, verbose=False) L1227
"Incremental update of the centers for the Minibatch K-Means algorithm."
_mini_batch_convergence(model, iteration_idx, n_iter, tol, n_samples, centers_squared_diff, batch_inertia, context, verbose=0) L1370
"Helper function to encapsulate the early stopping logic"

sklearn/cluster/_mean_shift.py
class MeanShift(ClusterMixin, BaseEstimator) L239
  "Mean shift clustering using a flat kernel."
  __init__(self, *, bandwidth=None, seeds=None, bin_seeding=False, min_bin_freq=1, cluster_all=True, n_jobs=None, max_iter=300) L352
  fit(self, X, y=None) L362
  "Perform clustering."
  predict(self, X) L448
  "Predict the closest cluster each sample in X belongs to."

estimate_bandwidth(X, *, quantile=0.3, n_samples=None, random_state=0, n_jobs=None) L30
"Estimate the bandwidth to use with the mean-shift algorithm."
_mean_shift_single_seed(my_mean, X, nbrs, max_iter) L88
mean_shift(X, *, bandwidth=None, seeds=None, bin_seeding=False, min_bin_freq=1, cluster_all=True, max_iter=300, n_jobs=None) L111
"Perform mean shift clustering of data using a flat kernel."
get_bin_seeds(X, bin_size, min_bin_freq=1) L192
"Finds seeds for mean_shift."

sklearn/cluster/_optics.py
class OPTICS(ClusterMixin, BaseEstimator) L24
  "Estimate clustering structure from vector array."
  __init__(self, *, min_samples=5, max_eps=np.inf, metric='minkowski', p=2, metric_params=None, cluster_method='xi', eps=None, xi=0.05, predecessor_correction=True, min_cluster_size=None, algorithm='auto', leaf_size=30, n_jobs=None) L207
  fit(self, X, y=None) L225
  "Perform OPTICS clustering."

_validate_size(size, n_samples, param_name) L291
_compute_core_distances_(X, neighbors, min_samples, working_memory) L305
"Compute the k-th nearest neighbor of each sample"
compute_optics_graph(X, *, min_samples, max_eps, metric, p, metric_params, algorithm, leaf_size, n_jobs) L343
"Computes the OPTICS reachability graph."
_set_reach_dist(core_distances_, reachability_, predecessor_, point_index, processed, X, nbrs, metric, metric_params, p, max_eps) L507
cluster_optics_dbscan(*, reachability, core_distances, ordering, eps) L543
"Performs DBSCAN extraction for an arbitrary epsilon."
cluster_optics_xi(*, reachability, predecessor, ordering, min_samples, min_cluster_size=None, xi=0.05, predecessor_correction=True) L582
"Automatically extract clusters according to the Xi-steep method."
_extend_region(steep_point, xward_point, start, min_samples) L650
"Extend the area until it's maximal."
_update_filter_sdas(sdas, mib, xi_complement, reachability_plot) L713
"Update steep down areas (SDAs) using the new maximum in between (mib)"
_correct_predecessor(reachability_plot, predecessor_plot, ordering, s, e) L726
"Correct for predecessors."
_xi_cluster(reachability_plot, predecessor_plot, ordering, xi, min_samples, min_cluster_size, predecessor_correction) L748
"Automatically extract clusters according to the Xi-steep method."
_extract_xi_labels(ordering, clusters) L902
"Extracts the labels from the clusters returned by `_xi_cluster`."

sklearn/cluster/_spectral.py
class SpectralClustering(ClusterMixin, BaseEstimator) L278
  "Apply clustering to a projection of the normalized Laplacian."
  __init__(self, n_clusters=8, *, eigen_solver=None, n_components=None, random_state=None, n_init=10, gamma=1.0, affinity='rbf', n_neighbors=10, eigen_tol=0.0, assign_labels='kmeans', degree=3, coef0=1, kernel_params=None, n_jobs=None) L443
  fit(self, X, y=None) L462
  "Perform spectral clustering from features, or affinity matrix."
  fit_predict(self, X, y=None) L529
  "Perform spectral clustering from features, or affinity matrix,"
  _pairwise(self) L554

discretize(vectors, *, copy=True, max_svd_restarts=30, n_iter_max=20, random_state=None) L22
"Search for a partition matrix (clustering) which is closest to the"
spectral_clustering(affinity, *, n_clusters=8, n_components=None, eigen_solver=None, random_state=None, n_init=10, eigen_tol=0.0, assign_labels='kmeans') L161
"Apply clustering to a projection of the normalized Laplacian."

sklearn/cluster/setup.py
configuration(parent_package='', top_path=None) L8

sklearn/compose/_column_transformer.py
class ColumnTransformer(TransformerMixin, _BaseComposition) L40
  "Applies transformers to columns of an array or pandas DataFrame."
  __init__(self, transformers, *, remainder='drop', sparse_threshold=0.3, n_jobs=None, transformer_weights=None, verbose=False) L177
  _transformers(self) L192
  "Internal list of transformer only containing the name and"
  _transformers(self, value) L202
  get_params(self, deep=True) L207
  "Get parameters for this estimator."
  set_params(self, **kwargs) L223
  "Set the parameters of this estimator."
  _iter(self, fitted=False, replace_strings=False) L235
  "Generate (name, trans, column, weight) tuples."
  _validate_transformers(self) L272
  _validate_column_callables(self, X) L292
  "Converts callable column specifications."
  _validate_remainder(self, X) L303
  "Validates ``remainder`` and defines ``_remainder`` targeting"
  named_transformers_(self) L333
  "Access the fitted transformer by name."
  get_feature_names(self) L345
  "Get feature names from all transformers."
  _update_fitted_transformers(self, transformers) L378
  _validate_output(self, result) L401
  "Ensure that the output of each transformer is 2D."
  _validate_features(self, n_features, feature_names) L414
  "Ensures feature counts and names are the same during fit and"
  _log_message(self, name, idx, total) L442
  _fit_transform(self, X, y, func, fitted=False) L447
  "Private function to fit and/or transform on demand."
  fit(self, X, y=None) L474
  "Fit all transformers using X."
  fit_transform(self, X, y=None) L497
  "Fit all transformers, transform the data and concatenate results."
  transform(self, X) L555
  "Transform X separately by each transformer, concatenate results."
  _hstack(self, Xs) L613
  "Stacks Xs horizontally."
  _sk_visual_block_(self) L641

class make_column_selector L788
  "Create a callable to select columns to be used with"
  __init__(self, pattern=None, *, dtype_include=None, dtype_exclude=None) L842
  __call__(self, df) L848

_check_X(X) L647
"Use check_array only on lists and other non-array-likes / sparse"
_is_empty_column_selection(column) L654
"Return True if the column selection is empty (empty list or all-False"
_get_transformer_list(estimators) L668
"Construct (name, trans, column) tuples from list"
make_column_transformer(*transformers, remainder='drop', sparse_threshold=0.3, n_jobs=None, verbose=False) L680
"Construct a ColumnTransformer from the given transformers."
_is_negative_indexing(key) L778
is_neg(x) L780

sklearn/compose/_target.py
class TransformedTargetRegressor(RegressorMixin, BaseEstimator) L19
  "Meta-estimator to regress on a transformed target."
  __init__(self, regressor=None, *, transformer=None, func=None, inverse_func=None, check_inverse=True) L113
  _fit_transformer(self, y) L121
  "Check transformer and fit transformer."
  fit(self, X, y, **fit_params) L157
  "Fit the model according to the given training data."
  predict(self, X) L211
  "Predict using the base regressor, applying inverse."
  _more_tags(self) L241
  n_features_in_(self) L245

sklearn/conftest.py
pyplot() L10
"Setup and teardown fixture for matplotlib."
pytest_runtest_setup(item) L29
"Set the number of openmp threads based on the number of workers"

sklearn/covariance/_elliptic_envelope.py
class EllipticEnvelope(OutlierMixin, MinCovDet) L13
  "An object for detecting outliers in a Gaussian distributed dataset."
  __init__(self, *, store_precision=True, assume_centered=False, support_fraction=None, contamination=0.1, random_state=None) L124
  fit(self, X, y=None) L134
  "Fit the EllipticEnvelope model."
  decision_function(self, X) L149
  "Compute the decision function of the given observations."
  score_samples(self, X) L169
  "Compute the negative Mahalanobis distances."
  predict(self, X) L185
  "Predict the labels (1 inlier, -1 outlier) of X according to the"
  score(self, X, y, sample_weight=None) L207
  "Returns the mean accuracy on the given test data and labels."

sklearn/covariance/_empirical_covariance.py
class EmpiricalCovariance(BaseEstimator) L101
  "Maximum likelihood covariance estimator"
  __init__(self, *, store_precision=True, assume_centered=False) L149
  _set_covariance(self, covariance) L153
  "Saves the covariance and precision estimates"
  get_precision(self) L174
  "Getter for the precision matrix."
  fit(self, X, y=None) L188
  "Fits the Maximum Likelihood Estimator covariance model"
  score(self, X_test, y=None) L216
  "Computes the log-likelihood of a Gaussian data set with"
  error_norm(self, comp_cov, norm='frobenius', scaling=True, squared=True) L245
  "Computes the Mean Squared Error between two covariance estimators."
  mahalanobis(self, X) L297
  "Computes the squared Mahalanobis distances of given observations."

log_likelihood(emp_cov, precision) L24
"Computes the sample mean of the log_likelihood under a covariance model"
empirical_covariance(X, *, assume_centered=False) L52
"Computes the Maximum likelihood covariance estimator"

sklearn/covariance/_graph_lasso.py
class GraphicalLasso(EmpiricalCovariance) L285
  "Sparse inverse covariance estimation with an l1-penalized estimator."
  __init__(self, alpha=0.01, *, mode='cd', tol=0.0001, enet_tol=0.0001, max_iter=100, verbose=False, assume_centered=False) L368
  fit(self, X, y=None) L378
  "Fits the GraphicalLasso model to X."

class GraphicalLassoCV(GraphicalLasso) L512
  "Sparse inverse covariance w/ cross-validated choice of the l1 penalty."
  __init__(self, *, alphas=4, n_refinements=4, cv=None, tol=0.0001, enet_tol=0.0001, max_iter=100, mode='cd', n_jobs=None, verbose=False, assume_centered=False) L650
  fit(self, X, y=None) L661
  "Fits the GraphicalLasso covariance model to X."

_objective(mle, precision_, alpha) L31
"Evaluation of the graphical-lasso objective function"
_dual_gap(emp_cov, precision_, alpha) L45
"Expression of the dual gap convergence criterion"
alpha_max(emp_cov) L58
"Find the maximum alpha for which there are some non-zeros off-diagonal."
graphical_lasso(emp_cov, alpha, *, cov_init=None, mode='cd', tol=0.0001, enet_tol=0.0001, max_iter=100, verbose=False, return_costs=False, eps=np.finfo(np.float64).eps, return_n_iter=False) L79
"l1-penalized covariance estimator"
graphical_lasso_path(X, alphas, cov_init=None, X_test=None, mode='cd', tol=0.0001, enet_tol=0.0001, max_iter=100, verbose=False) L411
"l1-penalized covariance estimator along a path of decreasing alphas"

sklearn/covariance/_robust_covariance.py
class MinCovDet(EmpiricalCovariance) L516
  "Minimum Covariance Determinant (MCD): robust estimator of covariance."
  __init__(self, *, store_precision=True, assume_centered=False, support_fraction=None, random_state=None) L619
  fit(self, X, y=None) L626
  "Fits a Minimum Covariance Determinant with the FastMCD algorithm."
  correct_covariance(self, data) L674
  "Apply a correction to raw Minimum Covariance Determinant estimates."
  reweight_covariance(self, data) L712
  "Re-weight raw Minimum Covariance Determinant estimates."

c_step(X, n_support, remaining_iterations=30, initial_estimates=None, verbose=False, cov_computation_method=empirical_covariance, random_state=None) L30
"C_step procedure described in [Rouseeuw1984]_ aiming at computing MCD."
_c_step(X, n_support, random_state, remaining_iterations=30, initial_estimates=None, verbose=False, cov_computation_method=empirical_covariance) L97
select_candidates(X, n_support, n_trials, select=1, n_iter=30, verbose=False, cov_computation_method=empirical_covariance, random_state=None) L184
"Finds the best pure subset of observations to compute MCD from it."
fast_mcd(X, support_fraction=None, cov_computation_method=empirical_covariance, random_state=None) L310
"Estimates the Minimum Covariance Determinant matrix."

sklearn/covariance/_shrunk_covariance.py
class ShrunkCovariance(EmpiricalCovariance) L63
  "Covariance estimator with shrinkage"
  __init__(self, *, store_precision=True, assume_centered=False, shrinkage=0.1) L122
  fit(self, X, y=None) L128
  "Fit the shrunk covariance model according to the given training data"

class LedoitWolf(EmpiricalCovariance) L319
  "LedoitWolf Estimator"
  __init__(self, *, store_precision=True, assume_centered=False, block_size=1000) L395
  fit(self, X, y=None) L401
  "Fit the Ledoit-Wolf shrunk covariance model according to the given"

class OAS(EmpiricalCovariance) L499
  "Oracle Approximating Shrinkage Estimator"
  fit(self, X, y=None) L577
  "Fit the Oracle Approximating Shrinkage covariance model"

shrunk_covariance(emp_cov, shrinkage=0.1) L26
"Calculates a covariance matrix shrunk on the diagonal"
ledoit_wolf_shrinkage(X, assume_centered=False, block_size=1000) L162
"Estimates the shrunk Ledoit-Wolf covariance matrix."
ledoit_wolf(X, *, assume_centered=False, block_size=1000) L257
"Estimates the shrunk Ledoit-Wolf covariance matrix."
oas(X, *, assume_centered=False) L435
"Estimate covariance with the Oracle Approximating Shrinkage algorithm."

sklearn/cross_decomposition/_cca.py
class CCA(_UnstableArchMixin, _PLS) L8
  "CCA Canonical Correlation Analysis."
  __init__(self, n_components=2, *, scale=True, max_iter=500, tol=1e-06, copy=True) L107

sklearn/cross_decomposition/_pls.py
class _PLS(TransformerMixin, RegressorMixin, MultiOutputMixin, BaseEstimator) L138
  "Partial Least Squares (PLS)"
  __init__(self, n_components=2, *, scale=True, deflation_mode='regression', mode='A', algorithm='nipals', norm_y_weights=False, max_iter=500, tol=1e-06, copy=True) L256
  fit(self, X, Y) L270
  "Fit model to data."
  transform(self, X, Y=None, copy=True) L416
  "Apply the dimension reduction learned on the train data."
  inverse_transform(self, X) L454
  "Transform data back to its original space."
  predict(self, X, copy=True) L481
  "Apply the dimension reduction learned on the train data."
  fit_transform(self, X, y=None) L506
  "Learn and apply the dimension reduction on the train data."
  _more_tags(self) L525

class PLSRegression(_PLS) L530
  "PLS regression"
  __init__(self, n_components=2, *, scale=True, max_iter=500, tol=1e-06, copy=True) L673

class PLSCanonical(_PLS) L682
  "PLSCanonical implements the 2 blocks canonical PLS of the original Wold"
  __init__(self, n_components=2, *, scale=True, algorithm='nipals', max_iter=500, tol=1e-06, copy=True) L822

class PLSSVD(TransformerMixin, BaseEstimator) L831
  "Partial Least Square SVD"
  __init__(self, n_components=2, *, scale=True, copy=True) L891
  fit(self, X, Y) L896
  "Fit model to data."
  transform(self, X, Y=None) L945
  "Apply the dimension reduction learned on the train data."
  fit_transform(self, X, y=None) L972
  "Learn and apply the dimension reduction on the train data."

_nipals_twoblocks_inner_loop(X, Y, mode='A', max_iter=500, tol=1e-06, norm_y_weights=False) L26
"Inner loop of the iterative NIPALS algorithm."
_svd_cross_product(X, Y) L104
_center_scale_xy(X, Y, scale=True) L112
"Center X, Y and scale if the scale parameter==True"

sklearn/datasets/_base.py
get_data_home(data_home=None) L30
"Return the path of the scikit-learn data dir."
clear_data_home(data_home=None) L59
"Delete all the content of the data home cache."
_convert_data_dataframe(caller_name, data, target, feature_names, target_names) L71
load_files(container_path, *, description=None, categories=None, load_content=True, shuffle=True, encoding=None, decode_error='strict', random_state=0) L85
"Load text files with categories as subfolder names."
load_data(module_path, data_file_name) L230
"Loads data from module_path/data/data_file_name."
load_wine(*, return_X_y=False, as_frame=False) L273
"Load and return the wine dataset (classification)."
load_iris(*, return_X_y=False, as_frame=False) L388
"Load and return the iris dataset (classification)."
load_breast_cancer(*, return_X_y=False, as_frame=False) L503
"Load and return the breast cancer wisconsin dataset (classification)."
load_digits(*, n_class=10, return_X_y=False, as_frame=False) L628
"Load and return the digits dataset (classification)."
load_diabetes(*, return_X_y=False, as_frame=False) L752
"Load and return the diabetes dataset (regression)."
load_linnerud(*, return_X_y=False, as_frame=False) L845
"Load and return the physical excercise linnerud dataset."
load_boston(*, return_X_y=False) L949
"Load and return the boston house-prices dataset (regression)."
load_sample_images() L1036
"Load sample images for image manipulation."
load_sample_image(image_name) L1086
"Load the numpy array of a single sample image"
_pkl_filepath(*args, **kwargs) L1127
"Return filename for Python 3 pickles"
_sha256(path) L1144
"Calculate the sha256 hash of the file at path."
_fetch_remote(remote, dirname=None) L1157
"Helper function to download a remote dataset into path"

sklearn/datasets/_california_housing.py
fetch_california_housing(*, data_home=None, download_if_missing=True, return_X_y=False, as_frame=False) L54
"Load the California housing dataset (regression)."

sklearn/datasets/_covtype.py
fetch_covtype(*, data_home=None, download_if_missing=True, random_state=None, shuffle=False, return_X_y=False, as_frame=False) L63
"Load the covertype dataset (classification)."

sklearn/datasets/_kddcup99.py
fetch_kddcup99(*, subset=None, data_home=None, shuffle=False, random_state=None, percent10=True, download_if_missing=True, return_X_y=False) L49
"Load the kddcup99 dataset (classification)."
_fetch_brute_kddcup99(data_home=None, download_if_missing=True, percent10=True) L180
"Load the kddcup99 dataset, downloading it if necessary."
_mkdirp(d) L310
"Ensure directory d exists (like mkdir -p on Unix)"

sklearn/datasets/_lfw.py
_check_fetch_lfw(data_home=None, funneled=True, download_if_missing=True) L74
"Helper function to download any missing LFW data"
_load_imgs(file_paths, slice_, color, resize) L117
"Internally used to load images"
_fetch_lfw_people(data_folder_path, slice_=None, color=False, resize=None, min_faces_per_person=0) L178
"Perform the actual data loading for the lfw people dataset"
fetch_lfw_people(*, data_home=None, funneled=True, resize=0.5, min_faces_per_person=0, color=False, slice_=(slice(70, 195), slice(78, 172)), download_if_missing=True, return_X_y=False) L220
"Load the Labeled Faces in the Wild (LFW) people dataset (classification)."
_fetch_lfw_pairs(index_file_path, data_folder_path, slice_=None, color=False, resize=None) L339
"Perform the actual data loading for the LFW pairs dataset"
fetch_lfw_pairs(*, subset='train', data_home=None, funneled=True, resize=0.5, color=False, slice_=(slice(70, 195), slice(78, 172)), download_if_missing=True) L391
"Load the Labeled Faces in the Wild (LFW) pairs dataset (classification)."

sklearn/datasets/_olivetti_faces.py
fetch_olivetti_faces(*, data_home=None, shuffle=False, random_state=0, download_if_missing=True, return_X_y=False) L40
"Load the Olivetti faces data-set from AT&T (classification)."

sklearn/datasets/_openml.py
class OpenMLError(ValueError) L132
  "HTTP 412 is a specific OpenML error code, indicating a generic error"

_get_local_path(openml_path, data_home) L44
_retry_with_clean_cache(openml_path, data_home) L48
"If the first call to the decorated function fails, the local cached"
decorator(f) L55
wrapper(*args, **kw) L57
_open_openml_url(openml_path, data_home) L74
"Returns a resource from OpenML.org."
is_gzip_encoded(_fsrc) L93
_get_json_content_from_openml_api(url, error_message, data_home) L137
"Loads json data from the openml api"
_load_json() L166
_split_sparse_columns(arff_data, include_columns) L182
"obtains several columns from sparse arff representation."
_sparse_data_to_array(arff_data, include_columns) L217
_convert_arff_data(arff, col_slice_x, col_slice_y, shape=None) L234
"converts the arff object into the appropriate matrix type (np.array or"
_feature_to_dtype(feature) L294
"Map feature to dtype for pandas DataFrame"
_convert_arff_data_dataframe(arff, columns, features_dict) L311
"Convert the ARFF object into a pandas DataFrame."
_get_data_info_by_name(name, version, data_home) L365
"Utilizes the openml dataset listing api to find a dataset by"
_get_data_description_by_id(data_id, data_home) L431
_get_data_features(data_id, data_home) L443
_get_data_qualities(data_id, data_home) L456
_get_num_samples(data_qualities) L471
"Get the number of samples from data qualities."
_load_arff_response(url, data_home, return_type, encode_nominal, parse_arff, md5_checksum) L492
"Load arff data with url and parses arff response with parse_arff"
_stream_checksum_generator(response) L507
_download_data_to_bunch(url, sparse, data_home, *, as_frame, features_list, data_columns, target_columns, shape, md5_checksum) L533
"Download OpenML ARFF and convert to Bunch of data"
postprocess(frame) L586
parse_arff(arff) L596
postprocess(X, y, nominal_attributes) L607
_verify_target_data_type(features_dict, target_columns) L647
_valid_data_column_names(features_list, target_columns) L675
fetch_openml(name=None, *, version='active', data_id=None, data_home=None, target_column='default-target', cache=True, return_X_y=False, as_frame=False) L690
"Fetch dataset from openml by name or dataset id."

sklearn/datasets/_rcv1.py
fetch_rcv1(*, data_home=None, subset='all', download_if_missing=True, random_state=None, shuffle=False, return_X_y=False) L80
"Load the RCV1 multilabel dataset (classification)."
_inverse_permutation(p) L276
"inverse permutation p"
_find_permutation(a, b) L285
"find the permutation from a to b"

sklearn/datasets/_samples_generator.py
_generate_hypercube(samples, dimensions, rng) L24
"Returns distinct binary samples of length dimensions"
make_classification(n_samples=100, n_features=20, *, n_informative=2, n_redundant=2, n_repeated=0, n_classes=2, n_clusters_per_class=2, weights=None, flip_y=0.01, class_sep=1.0, hypercube=True, shift=0.0, scale=1.0, shuffle=True, random_state=None) L38
"Generate a random n-class classification problem."
make_multilabel_classification(n_samples=100, n_features=20, *, n_classes=5, n_labels=2, length=50, allow_unlabeled=True, sparse=False, return_indicator='dense', return_distributions=False, random_state=None) L267
"Generate a random multilabel classification problem."
sample_example() L368
make_hastie_10_2(n_samples=12000, *, random_state=None) L430
"Generates data for binary classification used in"
make_regression(n_samples=100, n_features=100, *, n_informative=10, n_targets=1, bias=0.0, effective_rank=None, tail_strength=0.5, noise=0.0, shuffle=True, coef=False, random_state=None) L479
"Generate a random regression problem."
make_circles(n_samples=100, *, shuffle=True, noise=None, random_state=None, factor=0.8) L603
"Make a large circle containing a smaller circle in 2d."
make_moons(n_samples=100, *, shuffle=True, noise=None, random_state=None) L680
"Make two interleaving half circles."
make_blobs(n_samples=100, n_features=2, *, centers=None, cluster_std=1.0, center_box=(-10.0, 10.0), shuffle=True, random_state=None, return_centers=False) L744
"Generate isotropic Gaussian blobs for clustering."
make_friedman1(n_samples=100, n_features=10, *, noise=0.0, random_state=None) L902
"Generate the "Friedman #1" regression problem"
make_friedman2(n_samples=100, *, noise=0.0, random_state=None) L966
"Generate the "Friedman #2" regression problem"
make_friedman3(n_samples=100, *, noise=0.0, random_state=None) L1032
"Generate the "Friedman #3" regression problem"
make_low_rank_matrix(n_samples=100, n_features=100, *, effective_rank=10, tail_strength=0.5, random_state=None) L1097
"Generate a mostly low rank matrix with bell-shaped singular values"
make_sparse_coded_signal(n_samples, *, n_components, n_features, n_nonzero_coefs, random_state=None) L1167
"Generate a signal as a sparse combination of dictionary elements."
make_sparse_uncorrelated(n_samples=100, n_features=10, *, random_state=None) L1230
"Generate a random regression problem with sparse uncorrelated design"
make_spd_matrix(n_dim, *, random_state=None) L1283
"Generate a random symmetric, positive-definite matrix."
make_sparse_spd_matrix(dim=1, *, alpha=0.95, norm_diag=False, smallest_coef=0.1, largest_coef=0.9, random_state=None) L1317
"Generate a sparse symmetric definite positive matrix."
make_swiss_roll(n_samples=100, *, noise=0.0, random_state=None) L1392
"Generate a swiss roll dataset."
make_s_curve(n_samples=100, *, noise=0.0, random_state=None) L1445
"Generate an S curve dataset."
make_gaussian_quantiles(*, mean=None, cov=1.0, n_samples=100, n_features=2, n_classes=3, shuffle=True, random_state=None) L1488
"Generate isotropic Gaussian and label samples by quantile"
_shuffle(data, random_state=None) L1574
make_biclusters(shape, n_clusters, *, noise=0.0, minval=10, maxval=100, shuffle=True, random_state=None) L1584
"Generate an array with constant block diagonal structure for"
make_checkerboard(shape, n_clusters, *, noise=0.0, minval=10, maxval=100, shuffle=True, random_state=None) L1676
"Generate an array with block checkerboard structure for"

sklearn/datasets/_species_distributions.py
_load_coverage(F, header_length=6, dtype=np.int16) L78
"Load a coverage file from an open file object."
_load_csv(F) L94
"Load csv file."
construct_grids(batch) L114
"Construct the map grid from the batch object"
fetch_species_distributions(*, data_home=None, download_if_missing=True) L142
"Loader for species distribution dataset from Phillips et."

sklearn/datasets/_svmlight_format_io.py
_load_svmlight_file(*args, **kwargs) L33
load_svmlight_file(f, *, n_features=None, dtype=np.float64, multilabel=False, zero_based='auto', query_id=False, offset=0, length=-1) L42
"Load datasets in the svmlight / libsvm format into sparse CSR matrix"
_gen_open(f) L165
_open_and_load(f, dtype, multilabel, zero_based, query_id, offset=0, length=-1) L182
load_svmlight_files(files, *, n_features=None, dtype=np.float64, multilabel=False, zero_based='auto', query_id=False, offset=0, length=-1) L207
"Load dataset from multiple files in SVMlight format"
_dump_svmlight(X, y, f, multilabel, one_based, comment, query_id) L334
dump_svmlight_file(X, y, f, *, zero_based=True, comment=None, query_id=None, multilabel=False) L392
"Dump the dataset in svmlight / libsvm file format."

sklearn/datasets/_twenty_newsgroups.py
_download_20newsgroups(target_dir, cache_path) L65
"Download the 20 newsgroups data and stored it as a zipped pickle."
strip_newsgroup_header(text) L91
"Given text in "news" format, strip the headers, by removing everything"
strip_newsgroup_quoting(text) L109
"Given text in "news" format, strip lines beginning with the quote"
strip_newsgroup_footer(text) L125
"Given text in "news" format, attempt to remove a signature block."
fetch_20newsgroups(*, data_home=None, subset='train', categories=None, shuffle=True, random_state=42, remove=(), download_if_missing=True, return_X_y=False) L151
"Load the filenames and data from the 20 newsgroups dataset (classification)."
fetch_20newsgroups_vectorized(*, subset='train', remove=(), data_home=None, download_if_missing=True, return_X_y=False, normalize=True) L328
"Load the 20 newsgroups dataset and vectorize it into token counts (classification)."

sklearn/datasets/setup.py
configuration(parent_package='', top_path=None) L7

sklearn/decomposition/_base.py
class _BasePCA(TransformerMixin, BaseEstimator) L20
  "Base class for PCA methods."
  get_covariance(self) L26
  "Compute data covariance with the generative model."
  get_precision(self) L47
  "Compute data precision matrix with the generative model."
  fit(X, y=None) L81
  "Placeholder for fit."
  transform(self, X) L98
  "Apply dimensionality reduction to X."
  inverse_transform(self, X) L135
  "Transform data back to its original space."

sklearn/decomposition/_dict_learning.py
class _BaseSparseCoding(TransformerMixin) L883
  "Base class from SparseCoder and DictionaryLearning algorithms."
  __init__(self, transform_algorithm, transform_n_nonzero_coefs, transform_alpha, split_sign, n_jobs, positive_code, transform_max_iter) L885
  _transform(self, X, dictionary) L896
  "Private method allowing to accomodate both DictionaryLearning and"
  transform(self, X) L917
  "Encode the data as a sparse combination of the dictionary atoms."

class SparseCoder(_BaseSparseCoding, BaseEstimator) L938
  "Sparse coding"
  __init__(self, dictionary, *, transform_algorithm='omp', transform_n_nonzero_coefs=None, transform_alpha=None, split_sign=False, n_jobs=None, positive_code=False, transform_max_iter=1000) L1047
  fit(self, X, y=None) L1058
  "Do nothing and return the estimator unchanged."
  components_(self) L1080
  transform(self, X, y=None) L1083
  "Encode the data as a sparse combination of the dictionary atoms."
  _more_tags(self) L1102
  n_components_(self) L1106
  n_features_in_(self) L1110

class DictionaryLearning(_BaseSparseCoding, BaseEstimator) L1114
  "Dictionary learning"
  __init__(self, n_components=None, *, alpha=1, max_iter=1000, tol=1e-08, fit_algorithm='lars', transform_algorithm='omp', transform_n_nonzero_coefs=None, transform_alpha=None, n_jobs=None, code_init=None, dict_init=None, verbose=False, split_sign=False, random_state=None, positive_code=False, positive_dict=False, transform_max_iter=1000) L1278
  fit(self, X, y=None) L1301
  "Fit the model from data in X."

class MiniBatchDictionaryLearning(_BaseSparseCoding, BaseEstimator) L1342
  "Mini-batch dictionary learning"
  __init__(self, n_components=None, *, alpha=1, n_iter=1000, fit_algorithm='lars', n_jobs=None, batch_size=3, shuffle=True, dict_init=None, transform_algorithm='omp', transform_n_nonzero_coefs=None, transform_alpha=None, verbose=False, split_sign=False, random_state=None, positive_code=False, positive_dict=False, transform_max_iter=1000) L1512
  fit(self, X, y=None) L1536
  "Fit the model from data in X."
  partial_fit(self, X, y=None, iter_offset=None) L1575
  "Updates the model using the data in X as a mini-batch."

_check_positive_coding(method, positive) L25
_sparse_encode(X, dictionary, gram, cov=None, algorithm='lasso_lars', regularization=None, copy_cov=True, init=None, max_iter=1000, check_input=True, verbose=0, positive=False) L33
"Generic sparse coding"
sparse_encode(X, dictionary, *, gram=None, cov=None, algorithm='lasso_lars', n_nonzero_coefs=None, alpha=None, copy_cov=True, init=None, max_iter=1000, n_jobs=None, check_input=True, verbose=0, positive=False) L191
"Sparse coding"
_update_dict(dictionary, Y, code, verbose=False, return_r2=False, random_state=None, positive=False) L346
"Update the dense dictionary factor in place."
dict_learning(X, n_components, *, alpha, max_iter=100, tol=1e-08, method='lars', n_jobs=None, dict_init=None, code_init=None, callback=None, verbose=False, random_state=None, return_n_iter=False, positive_dict=False, positive_code=False, method_max_iter=1000) L429
"Solves a dictionary learning matrix factorization problem."
dict_learning_online(X, n_components=2, *, alpha=1, n_iter=100, return_code=True, dict_init=None, callback=None, batch_size=3, verbose=False, shuffle=True, n_jobs=None, method='lars', iter_offset=0, random_state=None, return_inner_stats=False, inner_stats=None, return_n_iter=False, positive_dict=False, positive_code=False, method_max_iter=1000) L624
"Solves a dictionary learning matrix factorization problem online."

sklearn/decomposition/_factor_analysis.py
class FactorAnalysis(TransformerMixin, BaseEstimator) L35
  "Factor Analysis (FA)"
  __init__(self, n_components=None, *, tol=0.01, copy=True, max_iter=1000, noise_variance_init=None, svd_method='randomized', iterated_power=3, rotation=None, random_state=0) L151
  fit(self, X, y=None) L169
  "Fit the FactorAnalysis model to X using SVD based approach"
  my_svd(X) L214
  my_svd(X) L221
  transform(self, X) L264
  "Apply dimensionality reduction to X using the model."
  get_covariance(self) L294
  "Compute data covariance with the FactorAnalysis model."
  get_precision(self) L310
  "Compute data precision matrix with the FactorAnalysis model."
  score_samples(self, X) L339
  "Compute the log-likelihood of each sample"
  score(self, X, y=None) L362
  "Compute the average log-likelihood of the samples"
  _rotate(self, components, n_components=None, tol=1e-06) L379
  "Rotate the factor analysis solution."

_ortho_rotation(components, method='varimax', tol=1e-06, max_iter=100) L392
"Return rotated components."

sklearn/decomposition/_fastica.py
class FastICA(TransformerMixin, BaseEstimator) L304
  "FastICA: a fast algorithm for Independent Component Analysis."
  __init__(self, n_components=None, *, algorithm='parallel', whiten=True, fun='logcosh', fun_args=None, max_iter=200, tol=0.0001, w_init=None, random_state=None) L396
  _fit(self, X, compute_sources=False) L413
  "Fit the model"
  g(x, fun_args) L447
  fit_transform(self, X, y=None) L545
  "Fit the model and recover the sources from X."
  fit(self, X, y=None) L562
  "Fit the model to X."
  transform(self, X, copy=True) L580
  "Recover the sources from X (apply the unmixing matrix)."
  inverse_transform(self, X, copy=True) L604
  "Transform the sources back to the mixed data (apply mixing matrix)."

_gs_decorrelation(w, W, j) L28
"Orthonormalize w wrt the first j rows of W"
_sym_decorrelation(W) L53
"Symmetric decorrelation"
_ica_def(X, tol, g, fun_args, max_iter, w_init) L63
"Deflationary FastICA using fun approx to neg-entropy function"
_ica_par(X, tol, g, fun_args, max_iter, w_init) L98
"Parallel FastICA."
_logcosh(x, fun_args=None) L127
_exp(x, fun_args) L139
_cube(x, fun_args) L146
fastica(X, n_components=None, *, algorithm='parallel', whiten=True, fun='logcosh', fun_args=None, max_iter=200, tol=0.0001, w_init=None, random_state=None, return_X_mean=False, compute_sources=True, return_n_iter=False) L151
"Perform Fast Independent Component Analysis."

sklearn/decomposition/_incremental_pca.py
class IncrementalPCA(_BasePCA) L16
  "Incremental principal components analysis (IPCA)."
  __init__(self, n_components=None, *, whiten=False, copy=True, batch_size=None) L168
  fit(self, X, y=None) L175
  "Fit the model with X, using minibatches of size batch_size."
  partial_fit(self, X, y=None, check_input=True) L218
  "Incremental fit with X."
  transform(self, X) L317
  "Apply dimensionality reduction to X."

sklearn/decomposition/_kernel_pca.py
class KernelPCA(TransformerMixin, BaseEstimator) L20
  "Kernel Principal component analysis (KPCA)"
  __init__(self, n_components=None, *, kernel='linear', gamma=None, degree=3, coef0=1, kernel_params=None, alpha=1.0, fit_inverse_transform=False, eigen_solver='auto', tol=0, max_iter=None, remove_zero_eig=False, random_state=None, copy_X=True, n_jobs=None) L145
  _pairwise(self) L170
  _get_kernel(self, X, Y=None) L173
  _fit_transform(self, K) L184
  "Fit's using kernel K"
  _fit_inverse_transform(self, X_transformed, X) L255
  fit(self, X, y=None) L266
  "Fit the model from data in X."
  fit_transform(self, X, y=None, **params) L294
  "Fit the model from data in X and transform X."
  transform(self, X) L317
  "Transform X."
  inverse_transform(self, X) L342
  "Transform X back to original space."

sklearn/decomposition/_lda.py
class LatentDirichletAllocation(TransformerMixin, BaseEstimator) L134
  "Latent Dirichlet Allocation with online variational Bayes algorithm"
  __init__(self, n_components=10, *, doc_topic_prior=None, topic_word_prior=None, learning_method='batch', learning_decay=0.7, learning_offset=10.0, max_iter=10, batch_size=128, evaluate_every=-1, total_samples=1000000.0, perp_tol=0.1, mean_change_tol=0.001, max_doc_update_iter=100, n_jobs=None, verbose=0, random_state=None) L296
  _check_params(self) L319
  "Check model parameters."
  _init_latent_vars(self, n_features) L337
  "Initialize latent variables."
  _e_step(self, X, cal_sstats, random_init, parallel=None) L364
  "E-step in EM update."
  _em_step(self, X, total_samples, batch_update, parallel=None) L427
  "EM update for 1 iteration."
  _more_tags(self) L477
  _check_non_neg_array(self, X, reset_n_features, whom) L480
  "check X format"
  partial_fit(self, X, y=None) L495
  "Online VB with Mini-Batch update."
  fit(self, X, y=None) L546
  "Learn model for the data X with variational Bayes method."
  _unnormalized_transform(self, X) L618
  "Transform data X according to fitted model."
  transform(self, X) L649
  "Transform data X according to the fitted model."
  _approx_bound(self, X, doc_topic_distr, sub_sampling) L669
  "Estimate the variational bound."
  _loglikelihood(prior, distr, dirichlet_distr, size) L695
  score(self, X, y=None) L745
  "Calculate approximate log-likelihood as score."
  _perplexity_precomp_distr(self, X, doc_topic_distr=None, sub_sampling=False) L767
  "Calculate approximate perplexity for data X with ability to accept"
  perplexity(self, X, sub_sampling=False) L816
  "Calculate approximate perplexity for data X."

_update_doc_distribution(X, exp_topic_word_distr, doc_topic_prior, max_iters, mean_change_tol, cal_sstats, random_state) L31
"E-step: update document-topic distribution."

sklearn/decomposition/_nmf.py
class NMF(TransformerMixin, BaseEstimator) L1083
  "Non-Negative Matrix Factorization (NMF)"
  __init__(self, n_components=None, *, init=None, solver='cd', beta_loss='frobenius', tol=0.0001, max_iter=200, random_state=None, alpha=0.0, l1_ratio=0.0, verbose=0, shuffle=False) L1239
  _more_tags(self) L1255
  fit_transform(self, X, y=None, W=None, H=None) L1258
  "Learn a NMF model for the data X and returns the transformed data."
  fit(self, X, y=None, **params) L1301
  "Learn a NMF model for the data X."
  transform(self, X) L1318
  "Transform the data X according to the fitted NMF model"
  inverse_transform(self, W) L1343
  "Transform data back to its original space."

norm(x) L27
"Dot product-based Euclidean norm implementation"
trace_dot(X, Y) L40
"Trace of np.dot(X, Y.T)."
_check_init(A, shape, whom) L53
_beta_divergence(X, W, H, beta, square_root=False) L63
"Compute the beta-divergence of X and dot(W, H)."
_special_sparse_dot(W, H, X) L169
"Computes np.dot(W, H), only where X is non zero."
_compute_regularization(alpha, l1_ratio, regularization) L189
"Compute L1 and L2 regularization coefficients for W and H"
_check_string_param(solver, regularization, beta_loss, init) L205
_beta_loss_to_float(beta_loss) L235
"Convert string beta_loss to float"
_initialize_nmf(X, n_components, init=None, eps=1e-06, random_state=None) L250
"Algorithms for NMF initialization."
_update_coordinate_descent(X, W, Ht, l1_reg, l2_reg, shuffle, random_state) L396
"Helper function for _fit_coordinate_descent"
_fit_coordinate_descent(X, W, H, tol=0.0001, max_iter=200, l1_reg_W=0, l1_reg_H=0, l2_reg_W=0, l2_reg_H=0, update_H=True, verbose=0, shuffle=False, random_state=None) L427
"Compute Non-negative Matrix Factorization (NMF) with Coordinate Descent"
_multiplicative_update_w(X, W, H, beta_loss, l1_reg_W, l2_reg_W, gamma, H_sum=None, HHt=None, XHt=None, update_H=True) L533
"update W in Multiplicative Update NMF"
_multiplicative_update_h(X, W, H, beta_loss, l1_reg_H, l2_reg_H, gamma) L628
"update H in Multiplicative Update NMF"
_fit_multiplicative_update(X, W, H, beta_loss='frobenius', max_iter=200, tol=0.0001, l1_reg_W=0, l1_reg_H=0, l2_reg_W=0, l2_reg_H=0, update_H=True, verbose=0) L710
"Compute Non-negative Matrix Factorization with Multiplicative Update"
non_negative_factorization(X, W=None, H=None, n_components=None, *, init=None, update_H=True, solver='cd', beta_loss='frobenius', tol=0.0001, max_iter=200, alpha=0.0, l1_ratio=0.0, regularization=None, random_state=None, verbose=0, shuffle=False) L845
"Compute Non-negative Matrix Factorization (NMF)"

sklearn/decomposition/_pca.py
class PCA(_BasePCA) L112
  "Principal component analysis (PCA)."
  __init__(self, n_components=None, *, copy=True, whiten=False, svd_solver='auto', tol=0.0, iterated_power='auto', random_state=None) L323
  fit(self, X, y=None) L334
  "Fit the model with X."
  fit_transform(self, X, y=None) L354
  "Fit the model with X and apply the dimensionality reduction on X."
  _fit(self, X) L388
  "Dispatch to the right submethod depending on the chosen solver."
  _fit_full(self, X, n_components) L430
  "Fit the model by computing full SVD on X"
  _fit_truncated(self, X, n_components, svd_solver) L496
  "Fit the model by computing truncated SVD (by ARPACK or randomized)"
  score_samples(self, X) L567
  "Return the log-likelihood of each sample."
  score(self, X, y=None) L595
  "Return the average log-likelihood of all samples."

_assess_dimension(spectrum, rank, n_samples) L31
"Compute the log-likelihood of a rank ``rank`` dataset."
_infer_dimension(spectrum, n_samples) L100
"Infers the dimension of a dataset with a given spectrum."

sklearn/decomposition/_sparse_pca.py
class SparsePCA(TransformerMixin, BaseEstimator) L15
  "Sparse Principal Components Analysis (SparsePCA)"
  __init__(self, n_components=None, *, alpha=1, ridge_alpha=0.01, max_iter=1000, tol=1e-08, method='lars', n_jobs=None, U_init=None, V_init=None, verbose=False, random_state=None) L113
  fit(self, X, y=None) L128
  "Fit the model from data in X."
  transform(self, X) L177
  "Least Squares projection of the data onto the sparse components."

class MiniBatchSparsePCA(SparsePCA) L209
  "Mini-batch Sparse Principal Components Analysis"
  __init__(self, n_components=None, *, alpha=1, ridge_alpha=0.01, n_iter=100, callback=None, batch_size=3, verbose=False, shuffle=True, n_jobs=None, method='lars', random_state=None) L306
  fit(self, X, y=None) L318
  "Fit the model from data in X."

sklearn/decomposition/_truncated_svd.py
class TruncatedSVD(TransformerMixin, BaseEstimator) L24
  "Dimensionality reduction using truncated SVD (aka LSA)."
  __init__(self, n_components=2, *, algorithm='randomized', n_iter=5, random_state=None, tol=0.0) L122
  fit(self, X, y=None) L130
  "Fit LSI model on training data X."
  fit_transform(self, X, y=None) L148
  "Fit LSI model to X and perform dimensionality reduction on X."
  transform(self, X) L201
  "Perform dimensionality reduction on X."
  inverse_transform(self, X) L218
  "Transform X back to its original space."

sklearn/decomposition/setup.py
configuration(parent_package='', top_path=None) L6

sklearn/discriminant_analysis.py
class LinearDiscriminantAnalysis(LinearClassifierMixin, TransformerMixin, BaseEstimator) L131
  "Linear Discriminant Analysis"
  __init__(self, *, solver='svd', shrinkage=None, priors=None, n_components=None, store_covariance=False, tol=0.0001) L249
  _solve_lsqr(self, X, y, shrinkage) L258
  "Least squares solver."
  _solve_eigen(self, X, y, shrinkage) L297
  "Eigenvalue solver."
  _solve_svd(self, X, y) L346
  "SVD solver."
  fit(self, X, y) L407
  "Fit LinearDiscriminantAnalysis model according to the given"
  transform(self, X) L480
  "Project data to maximize class separation."
  predict_proba(self, X) L506
  "Estimate probability."
  predict_log_proba(self, X) L528
  "Estimate log probability."
  decision_function(self, X) L545
  "Apply decision function to an array of samples."

class QuadraticDiscriminantAnalysis(ClassifierMixin, BaseEstimator) L569
  "Quadratic Discriminant Analysis"
  __init__(self, *, priors=None, reg_param=0.0, store_covariance=False, tol=0.0001) L660
  fit(self, X, y) L667
  "Fit the model according to the given training data and parameters."
  _decision_function(self, X) L733
  decision_function(self, X) L749
  "Apply decision function to an array of samples."
  predict(self, X) L775
  "Perform classification on an array of test vectors X."
  predict_proba(self, X) L792
  "Return posterior probabilities of classification."
  predict_log_proba(self, X) L812
  "Return log of posterior probabilities of classification."

_cov(X, shrinkage=None) L32
"Estimate covariance matrix (using optional shrinkage)."
_class_means(X, y) L72
"Compute class means."
_class_cov(X, y, priors, shrinkage=None) L96
"Compute weighted within-class covariance matrix."

sklearn/dummy.py
class DummyClassifier(MultiOutputMixin, ClassifierMixin, BaseEstimator) L23
  "DummyClassifier is a classifier that makes predictions using simple rules."
  __init__(self, *, strategy='prior', random_state=None, constant=None) L97
  fit(self, X, y, sample_weight=None) L103
  "Fit the random classifier."
  predict(self, X) L188
  "Perform classification on test vectors X."
  predict_proba(self, X) L263
  "Return probability estimates for the test vectors X."
  predict_log_proba(self, X) L326
  "Return log probability estimates for the test vectors X."
  _more_tags(self) L348
  score(self, X, y, sample_weight=None) L357
  "Returns the mean accuracy on the given test data and labels."

class DummyRegressor(MultiOutputMixin, RegressorMixin, BaseEstimator) L389
  "DummyRegressor is a regressor that makes predictions using"
  __init__(self, *, strategy='mean', constant=None, quantile=None) L446
  fit(self, X, y, sample_weight=None) L451
  "Fit the random regressor."
  predict(self, X, return_std=False) L531
  "Perform classification on test vectors X."
  _more_tags(self) L567
  score(self, X, y, sample_weight=None) L570
  "Returns the coefficient of determination R^2 of the prediction."

sklearn/ensemble/_bagging.py
class BaseBagging(BaseEnsemble) L186
  "Base class for Bagging meta-estimator."
  __init__(self, base_estimator=None, n_estimators=10, *, max_samples=1.0, max_features=1.0, bootstrap=True, bootstrap_features=False, oob_score=False, warm_start=False, n_jobs=None, random_state=None, verbose=0) L194
  fit(self, X, y, sample_weight=None) L220
  "Build a Bagging ensemble of estimators from the training"
  _parallel_args(self) L245
  _fit(self, X, y, max_samples=None, max_depth=None, sample_weight=None) L248
  "Build a Bagging ensemble of estimators from the training"
  _set_oob_score(self, X, y) L394
  "Calculate out of bag predictions and score."
  _validate_y(self, y) L397
  _get_estimators_indices(self) L403
  estimators_samples_(self) L416
  "The subset of drawn samples for each base estimator."

class BaggingClassifier(ClassifierMixin, BaseBagging) L432
  "A Bagging classifier."
  __init__(self, base_estimator=None, n_estimators=10, *, max_samples=1.0, max_features=1.0, bootstrap=True, bootstrap_features=False, oob_score=False, warm_start=False, n_jobs=None, random_state=None, verbose=0) L582
  _validate_estimator(self) L608
  "Check the estimator and set the base_estimator_ attribute."
  _set_oob_score(self, X, y) L613
  _validate_y(self, y) L650
  predict(self, X) L658
  "Predict class for X."
  predict_proba(self, X) L680
  "Predict class probabilities for X."
  predict_log_proba(self, X) L733
  "Predict class log-probabilities for X."
  decision_function(self, X) L792
  "Average of the decision functions of the base classifiers."

class BaggingRegressor(RegressorMixin, BaseBagging) L841
  "A Bagging regressor."
  __init__(self, base_estimator=None, n_estimators=10, *, max_samples=1.0, max_features=1.0, bootstrap=True, bootstrap_features=False, oob_score=False, warm_start=False, n_jobs=None, random_state=None, verbose=0) L982
  predict(self, X) L1007
  "Predict regression target for X."
  _validate_estimator(self) L1047
  "Check the estimator and set the base_estimator_ attribute."
  _set_oob_score(self, X, y) L1052

_generate_indices(random_state, bootstrap, n_population, n_samples) L34
"Draw randomly sampled indices."
_generate_bagging_indices(random_state, bootstrap_features, bootstrap_samples, n_features, n_samples, max_features, max_samples) L46
"Randomly draw feature and sample indices."
_parallel_build_estimators(n_estimators, ensemble, X, y, sample_weight, seeds, total_n_estimators, verbose) L62
"Private function used to build a batch of estimators within a job."
_parallel_predict_proba(estimators, estimators_features, X, n_classes) L121
"Private function used to compute (proba-)predictions within a job."
_parallel_predict_log_proba(estimators, estimators_features, X, n_classes) L147
"Private function used to compute log probabilities within a job."
_parallel_decision_function(estimators, estimators_features, X) L172
"Private function used to compute decisions within a job."
_parallel_predict_regression(estimators, estimators_features, X) L179
"Private function used to compute predictions within a job."

sklearn/ensemble/_base.py
class BaseEnsemble(MetaEstimatorMixin, BaseEstimator) L81
  "Base class for all ensemble classes."
  __init__(self, base_estimator, *, n_estimators=10, estimator_params=tuple()) L112
  _validate_estimator(self, default=None) L123
  "Check the estimator and the n_estimator attribute."
  _make_estimator(self, append=True, random_state=None) L144
  "Make and configure a copy of the `base_estimator_` attribute."
  __len__(self) L162
  "Return the number of estimators in the ensemble."
  __getitem__(self, index) L166
  "Return the index'th estimator in the ensemble."
  __iter__(self) L170
  "Return iterator over estimators in the ensemble."

class _BaseHeterogeneousEnsemble(MetaEstimatorMixin, _BaseComposition) L189
  "Base class for heterogeneous ensemble of learners."
  named_estimators(self) L212
  __init__(self, estimators) L216
  _validate_estimators(self) L219
  set_params(self, **params) L249
  "Set the parameters of an estimator from the ensemble."
  get_params(self, deep=True) L267
  "Get the parameters of an estimator from the ensemble."

_fit_single_estimator(estimator, X, y, sample_weight=None, message_clsname=None, message=None) L23
"Private function used to fit an estimator within a job."
_set_random_states(estimator, random_state=None) L43
"Set fixed random_state parameters for an estimator."
_partition_estimators(n_estimators, n_jobs) L175
"Private function used to partition estimators between jobs."

sklearn/ensemble/_forest.py
class BaseForest(MultiOutputMixin, BaseEnsemble) L175
  "Base class for forests of trees."
  __init__(self, base_estimator, n_estimators=100, *, estimator_params=tuple(), bootstrap=False, oob_score=False, n_jobs=None, random_state=None, verbose=0, warm_start=False, class_weight=None, max_samples=None) L184
  apply(self, X) L210
  "Apply trees in the forest to X, return leaf indices."
  decision_path(self, X) L235
  "Return the decision path in the forest."
  fit(self, X, y, sample_weight=None) L272
  "Build a forest of trees from the training set (X, y)."
  _set_oob_score(self, X, y) L408
  "Calculate out of bag predictions and score."
  _validate_y_class_weight(self, y) L412
  _validate_X_predict(self, X) L416
  "Validate X whenever one tries to predict, apply, predict_proba."
  feature_importances_(self) L424
  "The impurity-based feature importances."

class ForestClassifier(ClassifierMixin, BaseForest) L475
  "Base class for forest of trees-based classifiers."
  __init__(self, base_estimator, n_estimators=100, *, estimator_params=tuple(), bootstrap=False, oob_score=False, n_jobs=None, random_state=None, verbose=0, warm_start=False, class_weight=None, max_samples=None) L484
  _set_oob_score(self, X, y) L509
  "Compute out-of-bag score."
  _validate_y_class_weight(self, y) L557
  predict(self, X) L608
  "Predict class for X."
  predict_proba(self, X) L648
  "Predict class probabilities for X."
  predict_log_proba(self, X) L696
  "Predict class log-probabilities for X."

class ForestRegressor(RegressorMixin, BaseForest) L730
  "Base class for forest of trees-based regressors."
  __init__(self, base_estimator, n_estimators=100, *, estimator_params=tuple(), bootstrap=False, oob_score=False, n_jobs=None, random_state=None, verbose=0, warm_start=False, max_samples=None) L739
  predict(self, X) L762
  "Predict regression target for X."
  _set_oob_score(self, X, y) L805
  "Compute out-of-bag scores."
  _compute_partial_dependence_recursion(self, grid, target_features) L852
  "Fast partial dependence computation."

class RandomForestClassifier(ForestClassifier) L883
  "A random forest classifier."
  __init__(self, n_estimators=100, *, criterion='gini', max_depth=None, min_samples_split=2, min_samples_leaf=1, min_weight_fraction_leaf=0.0, max_features='auto', max_leaf_nodes=None, min_impurity_decrease=0.0, min_impurity_split=None, bootstrap=True, oob_score=False, n_jobs=None, random_state=None, verbose=0, warm_start=False, class_weight=None, ccp_alpha=0.0, max_samples=None) L1153

class RandomForestRegressor(ForestRegressor) L1202
  "A random forest regressor."
  __init__(self, n_estimators=100, *, criterion='mse', max_depth=None, min_samples_split=2, min_samples_leaf=1, min_weight_fraction_leaf=0.0, max_features='auto', max_leaf_nodes=None, min_impurity_decrease=0.0, min_impurity_split=None, bootstrap=True, oob_score=False, n_jobs=None, random_state=None, verbose=0, warm_start=False, ccp_alpha=0.0, max_samples=None) L1444

class ExtraTreesClassifier(ForestClassifier) L1491
  "An extra-trees classifier."
  __init__(self, n_estimators=100, *, criterion='gini', max_depth=None, min_samples_split=2, min_samples_leaf=1, min_weight_fraction_leaf=0.0, max_features='auto', max_leaf_nodes=None, min_impurity_decrease=0.0, min_impurity_split=None, bootstrap=False, oob_score=False, n_jobs=None, random_state=None, verbose=0, warm_start=False, class_weight=None, ccp_alpha=0.0, max_samples=None) L1755

class ExtraTreesRegressor(ForestRegressor) L1804
  "An extra-trees regressor."
  __init__(self, n_estimators=100, *, criterion='mse', max_depth=None, min_samples_split=2, min_samples_leaf=1, min_weight_fraction_leaf=0.0, max_features='auto', max_leaf_nodes=None, min_impurity_decrease=0.0, min_impurity_split=None, bootstrap=False, oob_score=False, n_jobs=None, random_state=None, verbose=0, warm_start=False, ccp_alpha=0.0, max_samples=None) L2036

class RandomTreesEmbedding(BaseForest) L2083
  "An ensemble of totally random trees."
  __init__(self, n_estimators=100, *, max_depth=5, min_samples_split=2, min_samples_leaf=1, min_weight_fraction_leaf=0.0, max_leaf_nodes=None, min_impurity_decrease=0.0, min_impurity_split=None, sparse_output=True, n_jobs=None, random_state=None, verbose=0, warm_start=False) L2249
  _set_oob_score(self, X, y) L2288
  fit(self, X, y=None, sample_weight=None) L2291
  "Fit estimator."
  fit_transform(self, X, y=None, sample_weight=None) L2320
  "Fit estimator and transform dataset."
  transform(self, X) L2358
  "Transform dataset."

_get_n_samples_bootstrap(n_samples, max_samples) L77
"Get the number of samples in a bootstrap sample."
_generate_sample_indices(random_state, n_samples, n_samples_bootstrap) L116
"Private function used to _parallel_build_trees function."
_generate_unsampled_indices(random_state, n_samples, n_samples_bootstrap) L126
"Private function used to forest._set_oob_score function."
_parallel_build_trees(tree, forest, X, y, sample_weight, tree_idx, n_trees, verbose=0, class_weight=None, n_samples_bootstrap=None) L139
"Private function used to fit a single tree in parallel."
_accumulate_prediction(predict, X, out, lock) L459
"This is a utility function for joblib's Parallel."

sklearn/ensemble/_gb.py
class VerboseReporter L59
  "Reports verbose output to stdout."
  __init__(self, verbose) L69
  init(self, est, begin_at_stage=0) L72
  "Initialize reporter"
  update(self, j, est) L103
  "Update reporter with new iteration."

class BaseGradientBoosting(BaseEnsemble) L133
  "Abstract base class for Gradient Boosting."
  __init__(self, *, loss, learning_rate, n_estimators, criterion, min_samples_split, min_samples_leaf, min_weight_fraction_leaf, max_depth, min_impurity_decrease, min_impurity_split, init, subsample, max_features, ccp_alpha, random_state, alpha=0.9, verbose=0, max_leaf_nodes=None, warm_start=False, validation_fraction=0.1, n_iter_no_change=None, tol=0.0001) L137
  _fit_stage(self, i, X, y, raw_predictions, sample_weight, sample_mask, random_state, X_csc=None, X_csr=None) L168
  "Fit another stage of ``n_classes_`` trees to the boosting model."
  _check_params(self) L222
  "Check validity of parameters and raise ValueError if not valid."
  _init_state(self) L301
  "Initialize model state and allocate model state data structures."
  _clear_state(self) L316
  "Clear the state of the gradient boosting model."
  _resize_state(self) L329
  "Add additional ``n_estimators`` entries to all attributes."
  _is_initialized(self) L349
  _check_initialized(self) L352
  "Check that the estimator is initialized, raising an error if not."
  fit(self, X, y, sample_weight=None, monitor=None) L356
  "Fit the gradient boosting model."
  _fit_stages(self, X, y, raw_predictions, sample_weight, random_state, X_val, y_val, sample_weight_val, begin_at_stage=0, monitor=None) L500
  "Iteratively fits the stages."
  _make_estimator(self, append=True) L585
  _raw_predict_init(self, X) L589
  "Check input and compute raw predictions of the init estimator."
  _raw_predict(self, X) L604
  "Return the sum of the trees raw predictions (+ init estimator)."
  _staged_raw_predict(self, X) L611
  "Compute raw predictions of ``X`` for each iteration."
  feature_importances_(self) L640
  "The impurity-based feature importances."
  _compute_partial_dependence_recursion(self, grid, target_features) L676
  "Fast partial dependence computation."
  _validate_y(self, y, sample_weight) L714
  apply(self, X) L723
  "Apply trees in the ensemble to X, return leaf indices."

class GradientBoostingClassifier(ClassifierMixin, BaseGradientBoosting) L759
  "Gradient Boosting for classification."
  __init__(self, *, loss='deviance', learning_rate=0.1, n_estimators=100, subsample=1.0, criterion='friedman_mse', min_samples_split=2, min_samples_leaf=1, min_weight_fraction_leaf=0.0, max_depth=3, min_impurity_decrease=0.0, min_impurity_split=None, init=None, random_state=None, max_features=None, verbose=0, max_leaf_nodes=None, warm_start=False, validation_fraction=0.1, n_iter_no_change=None, tol=0.0001, ccp_alpha=0.0) L1054
  _validate_y(self, y, sample_weight) L1078
  decision_function(self, X) L1090
  "Compute the decision function of ``X``."
  staged_decision_function(self, X) L1115
  "Compute decision function of ``X`` for each iteration."
  predict(self, X) L1139
  "Predict class for X."
  staged_predict(self, X) L1159
  "Predict class at each stage for X."
  predict_proba(self, X) L1182
  "Predict class probabilities for X."
  predict_log_proba(self, X) L1212
  "Predict class log-probabilities for X."
  staged_predict_proba(self, X) L1236
  "Predict class probabilities at each stage for X."

class GradientBoostingRegressor(RegressorMixin, BaseGradientBoosting) L1264
  "Gradient Boosting for regression."
  __init__(self, *, loss='ls', learning_rate=0.1, n_estimators=100, subsample=1.0, criterion='friedman_mse', min_samples_split=2, min_samples_leaf=1, min_weight_fraction_leaf=0.0, max_depth=3, min_impurity_decrease=0.0, min_impurity_split=None, init=None, random_state=None, max_features=None, alpha=0.9, verbose=0, max_leaf_nodes=None, warm_start=False, validation_fraction=0.1, n_iter_no_change=None, tol=0.0001, ccp_alpha=0.0) L1555
  predict(self, X) L1578
  "Predict regression target for X."
  staged_predict(self, X) L1597
  "Predict regression target at each stage for X."
  apply(self, X) L1618
  "Apply trees in the ensemble to X, return leaf indices."

sklearn/ensemble/_gb_losses.py
class LossFunction L17
  "Abstract base class for various loss functions."
  __init__(self, n_classes) L35
  init_estimator(self) L38
  "Default ``init`` estimator for loss function."
  __call__(self, y, raw_predictions, sample_weight=None) L43
  "Compute the loss."
  negative_gradient(self, y, raw_predictions, **kargs) L59
  "Compute the negative gradient."
  update_terminal_regions(self, tree, X, y, residual, raw_predictions, sample_weight, sample_mask, learning_rate=0.1, k=0) L72
  "Update the terminal regions (=leaves) of the given tree and"
  _update_terminal_region(self, tree, terminal_regions, leaf, X, y, residual, raw_predictions, sample_weight) L121
  "Template method for updating terminal regions (i.e., leaves)."
  get_init_raw_predictions(self, X, estimator) L126
  "Return the initial raw predictions."

class RegressionLossFunction(LossFunction) L147
  "Base class for regression loss functions."
  __init__(self, n_classes) L155
  check_init_estimator(self, estimator) L161
  "Make sure estimator has the required fit and predict methods."
  get_init_raw_predictions(self, X, estimator) L175

class LeastSquaresError(RegressionLossFunction) L180
  "Loss function for least squares (LS) estimation."
  init_estimator(self) L190
  __call__(self, y, raw_predictions, sample_weight=None) L193
  "Compute the least squares loss."
  negative_gradient(self, y, raw_predictions, **kargs) L213
  "Compute the negative gradient."
  update_terminal_regions(self, tree, X, y, residual, raw_predictions, sample_weight, sample_mask, learning_rate=0.1, k=0) L227
  "Least squares does not need to update terminal regions."
  _update_terminal_region(self, tree, terminal_regions, leaf, X, y, residual, raw_predictions, sample_weight) L260

class LeastAbsoluteError(RegressionLossFunction) L265
  "Loss function for least absolute deviation (LAD) regression."
  init_estimator(self) L273
  __call__(self, y, raw_predictions, sample_weight=None) L276
  "Compute the least absolute error."
  negative_gradient(self, y, raw_predictions, **kargs) L296
  "Compute the negative gradient."
  _update_terminal_region(self, tree, terminal_regions, leaf, X, y, residual, raw_predictions, sample_weight) L313
  "LAD updates terminal regions to median estimates."

class HuberLossFunction(RegressionLossFunction) L324
  "Huber loss function for robust regression."
  __init__(self, n_classes, alpha=0.9) L343
  init_estimator(self) L348
  __call__(self, y, raw_predictions, sample_weight=None) L351
  "Compute the Huber loss."
  negative_gradient(self, y, raw_predictions, sample_weight=None, **kargs) L390
  "Compute the negative gradient."
  _update_terminal_region(self, tree, terminal_regions, leaf, X, y, residual, raw_predictions, sample_weight) L420

class QuantileLossFunction(RegressionLossFunction) L434
  "Loss function for quantile regression."
  __init__(self, n_classes, alpha=0.9) L448
  init_estimator(self) L453
  __call__(self, y, raw_predictions, sample_weight=None) L456
  "Compute the Quantile loss."
  negative_gradient(self, y, raw_predictions, **kargs) L485
  "Compute the negative gradient."
  _update_terminal_region(self, tree, terminal_regions, leaf, X, y, residual, raw_predictions, sample_weight) L502

class ClassificationLossFunction(LossFunction) L513
  "Base class for classification loss functions."
  _raw_prediction_to_proba(self, raw_predictions) L516
  "Template method to convert raw predictions into probabilities."
  _raw_prediction_to_decision(self, raw_predictions) L532
  "Template method to convert raw predictions to decisions."
  check_init_estimator(self, estimator) L547
  "Make sure estimator has fit and predict_proba methods."

class BinomialDeviance(ClassificationLossFunction) L563
  "Binomial deviance loss function for binary classification."
  __init__(self, n_classes) L574
  init_estimator(self) L581
  __call__(self, y, raw_predictions, sample_weight=None) L586
  "Compute the deviance (= 2 * negative log-likelihood)."
  negative_gradient(self, y, raw_predictions, **kargs) L611
  "Compute the residual (= negative gradient)."
  _update_terminal_region(self, tree, terminal_regions, leaf, X, y, residual, raw_predictions, sample_weight) L625
  "Make a single Newton-Raphson step."
  _raw_prediction_to_proba(self, raw_predictions) L650
  _raw_prediction_to_decision(self, raw_predictions) L656
  get_init_raw_predictions(self, X, estimator) L660

class MultinomialDeviance(ClassificationLossFunction) L670
  "Multinomial deviance loss function for multi-class classification."
  __init__(self, n_classes) L684
  init_estimator(self) L690
  __call__(self, y, raw_predictions, sample_weight=None) L693
  "Compute the Multinomial deviance."
  negative_gradient(self, y, raw_predictions, k=0, **kwargs) L719
  "Compute negative gradient for the ``k``-th class."
  _update_terminal_region(self, tree, terminal_regions, leaf, X, y, residual, raw_predictions, sample_weight) L737
  "Make a single Newton-Raphson step."
  _raw_prediction_to_proba(self, raw_predictions) L757
  _raw_prediction_to_decision(self, raw_predictions) L762
  get_init_raw_predictions(self, X, estimator) L766

class ExponentialLoss(ClassificationLossFunction) L774
  "Exponential loss function for binary classification."
  __init__(self, n_classes) L788
  init_estimator(self) L795
  __call__(self, y, raw_predictions, sample_weight=None) L798
  "Compute the exponential loss"
  negative_gradient(self, y, raw_predictions, **kargs) L820
  "Compute the residual (= negative gradient)."
  _update_terminal_region(self, tree, terminal_regions, leaf, X, y, residual, raw_predictions, sample_weight) L835
  _raw_prediction_to_proba(self, raw_predictions) L853
  _raw_prediction_to_decision(self, raw_predictions) L859
  get_init_raw_predictions(self, X, estimator) L862

sklearn/ensemble/_hist_gradient_boosting/binning.py
class _BinMapper(TransformerMixin, BaseEstimator) L88
  "Transformer that maps a dataset into integer-valued bins."
  __init__(self, n_bins=256, subsample=int(200000.0), random_state=None) L137
  fit(self, X, y=None) L142
  "Fit data X by computing the binning thresholds."
  transform(self, X) L178
  "Bin data X."

_find_binning_thresholds(data, max_bins, subsample, random_state) L19
"Extract feature-wise quantiles from numerical data."

sklearn/ensemble/_hist_gradient_boosting/gradient_boosting.py
class BaseHistGradientBoosting(BaseEstimator, ABC) L29
  "Base class for histogram-based gradient boosting estimators."
  __init__(self, loss, *, learning_rate, max_iter, max_leaf_nodes, max_depth, min_samples_leaf, l2_regularization, max_bins, monotonic_cst, warm_start, early_stopping, scoring, validation_fraction, n_iter_no_change, tol, verbose, random_state) L33
  _validate_parameters(self) L56
  "Validate parameters passed to __init__."
  fit(self, X, y, sample_weight=None) L97
  "Fit the gradient boosting model."
  _is_fitted(self) L450
  _clear_state(self) L453
  "Clear the state of the gradient boosting model."
  _get_small_trainset(self, X_binned_train, y_train, sample_weight_train, seed) L459
  "Compute the indices of the subsample set and return this set."
  _check_early_stopping_scorer(self, X_binned_small_train, y_small_train, sample_weight_small_train, X_binned_val, y_val, sample_weight_val) L486
  "Check if fitting should be early-stopped based on scorer."
  _check_early_stopping_loss(self, raw_predictions, y_train, sample_weight_train, raw_predictions_val, y_val, sample_weight_val) L522
  "Check if fitting should be early-stopped based on loss."
  _should_stop(self, scores) L546
  "Return True (do early stopping) if the last n scores aren't better"
  _bin_data(self, X, is_training_data) L566
  "Bin data X."
  _print_iteration_stats(self, iteration_start_time) L592
  "Print info about the current fitting iteration."
  _raw_predict(self, X) L632
  "Return the sum of the leaves values over all predictors."
  _predict_iterations(self, X, predictors, raw_predictions, is_binned) L665
  "Add the predictions of the predictors to raw_predictions."
  _staged_raw_predict(self, X) L678
  "Compute raw predictions of ``X`` for each iteration."
  _compute_partial_dependence_recursion(self, grid, target_features) L718
  "Fast partial dependence computation."
  _more_tags(self) L756
  _get_loss(self, sample_weight) L760
  _encode_y(self, y=None) L764
  n_iter_(self) L768

class HistGradientBoostingRegressor(RegressorMixin, BaseHistGradientBoosting) L773
  "Histogram-based Gradient Boosting Regression Tree."
  __init__(self, loss='least_squares', *, learning_rate=0.1, max_iter=100, max_leaf_nodes=31, max_depth=None, min_samples_leaf=20, l2_regularization=0.0, max_bins=255, monotonic_cst=None, warm_start=False, early_stopping='auto', scoring='loss', validation_fraction=0.1, n_iter_no_change=10, tol=1e-07, verbose=0, random_state=None) L926
  predict(self, X) L944
  "Predict values for X."
  staged_predict(self, X) L962
  "Predict regression target for each iteration"
  _encode_y(self, y) L981
  _get_loss(self, sample_weight) L992

class HistGradientBoostingClassifier(BaseHistGradientBoosting, ClassifierMixin) L996
  "Histogram-based Gradient Boosting Classification Tree."
  __init__(self, loss='auto', *, learning_rate=0.1, max_iter=100, max_leaf_nodes=31, max_depth=None, min_samples_leaf=20, l2_regularization=0.0, max_bins=255, monotonic_cst=None, warm_start=False, early_stopping='auto', scoring='loss', validation_fraction=0.1, n_iter_no_change=10, tol=1e-07, verbose=0, random_state=None) L1153
  predict(self, X) L1170
  "Predict classes for X."
  staged_predict(self, X) L1187
  "Predict classes at each iteration."
  predict_proba(self, X) L1207
  "Predict class probabilities for X."
  staged_predict_proba(self, X) L1223
  "Predict class probabilities at each iteration."
  decision_function(self, X) L1243
  "Compute the decision function of ``X``."
  staged_decision_function(self, X) L1264
  "Compute decision function of ``X`` for each iteration."
  _encode_y(self, y) L1288
  _get_loss(self, sample_weight) L1303

sklearn/ensemble/_hist_gradient_boosting/grower.py
class TreeNode L26
  "Tree Node class used in TreeGrower."
  __init__(self, depth, sample_indices, sum_gradients, sum_hessians, parent=None, value=None) L90
  set_children_bounds(self, lower, upper) L102
  "Set children values bounds to respect monotonic constraints."
  __lt__(self, other_node) L111
  "Comparison for priority queue."

class TreeGrower L128
  "Tree grower class used to build a tree."
  __init__(self, X_binned, gradients, hessians, max_leaf_nodes=None, max_depth=None, min_samples_leaf=20, min_gain_to_split=0.0, n_bins=256, n_bins_non_missing=None, has_missing_values=False, monotonic_cst=None, l2_regularization=0.0, min_hessian_to_split=0.001, shrinkage=1.0) L179
  _validate_parameters(self, X_binned, max_leaf_nodes, max_depth, min_samples_leaf, min_gain_to_split, l2_regularization, min_hessian_to_split) L254
  "Validate parameters passed to __init__."
  grow(self) L287
  "Grow the tree, from root to leaves."
  _apply_shrinkage(self) L294
  "Multiply leaves values by shrinkage parameter."
  _intilialize_root(self, gradients, hessians, hessians_are_constant) L306
  "Initialize root node and finalize it if needed."
  _compute_best_split_and_push(self, node) L338
  "Compute the best possible split (SplitInfo) of a given node."
  split_next(self) L357
  "Split the node with highest potential gain."
  _finalize_leaf(self, node) L491
  "Make node a leaf of the tree being grown."
  _finalize_splittable_nodes(self) L497
  "Transform all splittable nodes into leaves."
  make_predictor(self, bin_thresholds=None) L506
  "Make a TreePredictor object out of the current tree."

_fill_predictor_node_array(predictor_nodes, grower_node, bin_thresholds, n_bins_non_missing, next_free_idx=0) L524
"Helper used in make_predictor to set the TreePredictor fields."

sklearn/ensemble/_hist_gradient_boosting/loss.py
class BaseLoss(ABC) L26
  "Base class for a loss."
  __init__(self, hessians_are_constant) L29
  __call__(self, y_true, raw_predictions, sample_weight) L32
  "Return the weighted average loss"
  pointwise_loss(self, y_true, raw_predictions) L38
  "Return loss value for each input"
  init_gradients_and_hessians(self, n_samples, prediction_dim, sample_weight) L51
  "Return initial gradients and hessians."
  get_baseline_prediction(self, y_train, sample_weight, prediction_dim) L96
  "Return initial predictions (before the first iteration)."
  update_gradients_and_hessians(self, gradients, hessians, y_true, raw_predictions, sample_weight) L118
  "Update gradients and hessians arrays, inplace."

class LeastSquares(BaseLoss) L147
  "Least squares loss, for regression."
  __init__(self, sample_weight) L159
  pointwise_loss(self, y_true, raw_predictions) L165
  get_baseline_prediction(self, y_train, sample_weight, prediction_dim) L172
  inverse_link_function(raw_predictions) L176
  update_gradients_and_hessians(self, gradients, hessians, y_true, raw_predictions, sample_weight) L179

class LeastAbsoluteDeviation(BaseLoss) L194
  "Least absolute deviation, for regression."
  __init__(self, sample_weight) L202
  pointwise_loss(self, y_true, raw_predictions) L218
  get_baseline_prediction(self, y_train, sample_weight, prediction_dim) L225
  inverse_link_function(raw_predictions) L232
  update_gradients_and_hessians(self, gradients, hessians, y_true, raw_predictions, sample_weight) L235
  update_leaves_values(self, grower, y_true, raw_predictions, sample_weight) L249

class Poisson(BaseLoss) L272
  "Poisson deviance loss with log-link, for regression."
  __init__(self, sample_weight) L284
  pointwise_loss(self, y_true, raw_predictions) L289
  get_baseline_prediction(self, y_train, sample_weight, prediction_dim) L299
  update_gradients_and_hessians(self, gradients, hessians, y_true, raw_predictions, sample_weight) L305

class BinaryCrossEntropy(BaseLoss) L317
  "Binary cross-entropy loss, for binary classification."
  __init__(self, sample_weight) L329
  pointwise_loss(self, y_true, raw_predictions) L334
  get_baseline_prediction(self, y_train, sample_weight, prediction_dim) L342
  update_gradients_and_hessians(self, gradients, hessians, y_true, raw_predictions, sample_weight) L355
  predict_proba(self, raw_predictions) L365

class CategoricalCrossEntropy(BaseLoss) L375
  "Categorical cross-entropy loss, for multiclass classification."
  __init__(self, sample_weight) L383
  pointwise_loss(self, y_true, raw_predictions) L386
  get_baseline_prediction(self, y_train, sample_weight, prediction_dim) L396
  update_gradients_and_hessians(self, gradients, hessians, y_true, raw_predictions, sample_weight) L407
  predict_proba(self, raw_predictions) L412

sklearn/ensemble/_hist_gradient_boosting/predictor.py
class TreePredictor L14
  "Tree class used for predictions."
  __init__(self, nodes) L22
  get_n_leaf_nodes(self) L25
  "Return number of leaves."
  get_max_depth(self) L29
  "Return maximum depth among all leaves."
  predict(self, X) L33
  "Predict raw values for non-binned data."
  predict_binned(self, X, missing_values_bin_idx) L50
  "Predict raw values for binned data."
  compute_partial_dependence(self, grid, target_features, out) L71
  "Fast partial dependence computation."

sklearn/ensemble/_iforest.py
class IsolationForest(OutlierMixin, BaseBagging) L27
  "Isolation Forest Algorithm."
  __init__(self, *, n_estimators=100, max_samples='auto', contamination='auto', max_features=1.0, bootstrap=False, n_jobs=None, random_state=None, verbose=0, warm_start=False) L181
  _set_oob_score(self, X, y) L209
  _parallel_args(self) L212
  fit(self, X, y=None, sample_weight=None) L219
  "Fit estimator."
  predict(self, X) L294
  "Predict if a particular sample is an outlier or not."
  decision_function(self, X) L317
  "Average anomaly score of X of the base classifiers."
  score_samples(self, X) L349
  "Opposite of the anomaly score defined in the original paper."
  _compute_chunked_score_samples(self, X) L389
  _compute_score_samples(self, X, subsample_features) L421
  "Compute the score of each samples in X going through the extra trees."
  _more_tags(self) L457

_average_path_length(n_samples_leaf) L466
"The average path length in a n_samples iTree, which is equal to"

sklearn/ensemble/_stacking.py
class _BaseStacking(TransformerMixin, _BaseHeterogeneousEnsemble) L37
  "Base class for stacking method."
  __init__(self, estimators, final_estimator=None, *, cv=None, stack_method='auto', n_jobs=None, verbose=0, passthrough=False) L42
  _clone_final_estimator(self, default) L53
  _concatenate_predictions(self, X, predictions) L59
  "Concatenate the predictions of each first layer learner and"
  _method_name(name, estimator, method) L94
  fit(self, X, y, sample_weight=None) L110
  "Fit the estimators."
  _transform(self, X) L200
  "Concatenate and return the predictions of the estimators."
  predict(self, X, **predict_params) L211
  "Predict target for X."
  _sk_visual_block_(self, final_estimator) L237

class StackingClassifier(ClassifierMixin, _BaseStacking) L246
  "Stack of estimators with a final classifier."
  __init__(self, estimators, final_estimator=None, *, cv=None, stack_method='auto', n_jobs=None, passthrough=False, verbose=0) L382
  _validate_final_estimator(self) L395
  fit(self, X, y, sample_weight=None) L403
  "Fit the estimators."
  predict(self, X, **predict_params) L430
  "Predict target for X."
  predict_proba(self, X) L454
  "Predict class probabilities for X using"
  decision_function(self, X) L474
  "Predict decision function for samples in X using"
  transform(self, X) L493
  "Return class labels or probabilities for X for each estimator."
  _sk_visual_block_(self) L510

class StackingRegressor(RegressorMixin, _BaseStacking) L520
  "Stack of estimators with a final regressor."
  __init__(self, estimators, final_estimator=None, *, cv=None, n_jobs=None, passthrough=False, verbose=0) L630
  _validate_final_estimator(self) L642
  fit(self, X, y, sample_weight=None) L650
  "Fit the estimators."
  transform(self, X) L674
  "Return the predictions for X for each estimator."
  _sk_visual_block_(self) L690

sklearn/ensemble/_voting.py
class _BaseVoting(TransformerMixin, _BaseHeterogeneousEnsemble) L38
  "Base class for voting."
  _log_message(self, name, idx, total) L45
  _weights_not_none(self) L51
  "Get the weights of not `None` estimators."
  _predict(self, X) L58
  "Collect results from clf.predict calls."
  fit(self, X, y, sample_weight=None) L63
  "Get common fit operations."
  n_features_in_(self) L95
  _sk_visual_block_(self) L108

class VotingClassifier(ClassifierMixin, _BaseVoting) L113
  "Soft Voting/Majority Rule classifier for unfitted estimators."
  __init__(self, estimators, *, voting='hard', weights=None, n_jobs=None, flatten_transform=True, verbose=False) L216
  fit(self, X, y, sample_weight=None) L225
  "Fit the estimators."
  predict(self, X) L264
  "Predict class labels for X."
  _collect_probas(self, X) L292
  "Collect results from clf.predict calls."
  _predict_proba(self, X) L296
  "Predict class probabilities for X in 'soft' voting."
  predict_proba(self) L304
  "Compute probabilities of possible outcomes for samples in X."
  transform(self, X) L322
  "Return class labels or probabilities for X for each estimator."

class VotingRegressor(RegressorMixin, _BaseVoting) L356
  "Prediction voting regressor for unfitted estimators."
  __init__(self, estimators, *, weights=None, n_jobs=None, verbose=False) L423
  fit(self, X, y, sample_weight=None) L430
  "Fit the estimators."
  predict(self, X) L455
  "Predict regression target for X."
  transform(self, X) L475
  "Return predictions for X for each estimator."

sklearn/ensemble/_weight_boosting.py
class BaseWeightBoosting(BaseEnsemble) L52
  "Base class for AdaBoost estimators."
  __init__(self, base_estimator=None, *, n_estimators=50, estimator_params=tuple(), learning_rate=1.0, random_state=None) L60
  _check_X(self, X) L75
  fit(self, X, y, sample_weight=None) L79
  "Build a boosted classifier/regressor from the training set (X, y)."
  _boost(self, iboost, X, y, sample_weight, random_state) L160
  "Implement a single boost."
  staged_score(self, X, y, sample_weight=None) L199
  "Return staged scores for X, y."
  feature_importances_(self) L231
  "The impurity-based feature importances."

class AdaBoostClassifier(ClassifierMixin, BaseWeightBoosting) L285
  "An AdaBoost classifier."
  __init__(self, base_estimator=None, *, n_estimators=50, learning_rate=1.0, algorithm='SAMME.R', random_state=None) L403
  fit(self, X, y, sample_weight=None) L418
  "Build a boosted classifier from the training set (X, y)."
  _validate_estimator(self) L446
  "Check the estimator and set the base_estimator_ attribute."
  _boost(self, iboost, X, y, sample_weight, random_state) L464
  "Implement a single boost."
  _boost_real(self, iboost, X, y, sample_weight, random_state) L510
  "Implement a single boost using the SAMME.R real algorithm."
  _boost_discrete(self, iboost, X, y, sample_weight, random_state) L568
  "Implement a single boost using the SAMME discrete algorithm."
  predict(self, X) L615
  "Predict classes for X."
  staged_predict(self, X) L641
  "Return staged predictions for X."
  decision_function(self, X) L676
  "Compute the decision function of ``X``."
  staged_decision_function(self, X) L716
  "Compute decision function of ``X`` for each boosting iteration."
  _compute_proba_from_decision(decision, n_classes) L770
  "Compute probabilities from the decision function."
  predict_proba(self, X) L788
  "Predict class probabilities for X."
  staged_predict_proba(self, X) L818
  "Predict class probabilities for X."
  predict_log_proba(self, X) L849
  "Predict class log-probabilities for X."

class AdaBoostRegressor(RegressorMixin, BaseWeightBoosting) L872
  "An AdaBoost regressor."
  __init__(self, base_estimator=None, *, n_estimators=50, learning_rate=1.0, loss='linear', random_state=None) L967
  fit(self, X, y, sample_weight=None) L983
  "Build a boosted regressor from the training set (X, y)."
  _validate_estimator(self) L1011
  "Check the estimator and set the base_estimator_ attribute."
  _boost(self, iboost, X, y, sample_weight, random_state) L1016
  "Implement a single boost for regression"
  _get_median_predict(self, X, limit) L1112
  predict(self, X) L1130
  "Predict regression value for X."
  staged_predict(self, X) L1152
  "Return staged predictions for X."

_samme_proba(estimator, n_classes, X) L265
"Calculate algorithm 4, step 2, equation c) of Zhu et al [1]."

sklearn/ensemble/setup.py
configuration(parent_package='', top_path=None) L5

sklearn/exceptions.py
class NotFittedError(ValueError, AttributeError) L21
  "Exception class to raise if estimator is used before fitting."

class ChangedBehaviorWarning(UserWarning) L45
  "Warning class used to notify the user of any change in the behavior."

class ConvergenceWarning(UserWarning) L53
  "Custom warning to capture convergence problems"

class DataConversionWarning(UserWarning) L61
  "Warning used to notify implicit data conversions happening in the code."

class DataDimensionalityWarning(UserWarning) L79
  "Custom warning to notify potential issues with data dimensionality."

class EfficiencyWarning(UserWarning) L93
  "Warning used to notify the user of inefficient computation."

class FitFailedWarning(RuntimeWarning) L104
  "Warning class used if there is an error while fitting the estimator."

class NonBLASDotWarning(EfficiencyWarning) L118
  "Warning used when the dot operation does not use BLAS."

class SkipTestWarning(UserWarning) L129
  "Warning class used to notify the user of a test that was skipped."

class UndefinedMetricWarning(UserWarning) L138
  "Warning used when the metric is invalid"

class PositiveSpectrumWarning(UserWarning) L146
  "Warning raised when the eigenvalues of a PSD matrix have issues"

sklearn/feature_extraction/_dict_vectorizer.py
class DictVectorizer(TransformerMixin, BaseEstimator) L25
  "Transforms lists of feature-value mappings to vectors."
  __init__(self, *, dtype=np.float64, separator='=', sparse=True, sort=True) L94
  fit(self, X, y=None) L101
  "Learn a list of feature name -> indices mappings."
  _transform(self, X, fitting) L135
  fit_transform(self, X, y=None) L210
  "Learn a list of feature name -> indices mappings and transform X."
  inverse_transform(self, X, dict_type=dict) L230
  "Transform array or sparse matrix X back to feature mappings."
  transform(self, X) L271
  "Transform feature->value dicts to array or sparse matrix."
  get_feature_names(self) L309
  "Returns a list of feature names, ordered by their indices."
  restrict(self, support, indices=False) L317
  "Restrict the features to those in support using feature selection."
  _more_tags(self) L363

_tosequence(X) L17
"Turn X into a sequence or ndarray, avoiding a copy if possible."

sklearn/feature_extraction/_hash.py
class FeatureHasher(TransformerMixin, BaseEstimator) L28
  "Implements feature hashing, aka the hashing trick."
  __init__(self, n_features=2 ** 20, *, input_type='dict', dtype=np.float64, alternate_sign=True) L93
  _validate_params(n_features, input_type) L103
  fit(self, X=None, y=None) L116
  "No-op."
  transform(self, raw_X) L135
  "Transform a sequence of instances to a scipy.sparse matrix."
  _more_tags(self) L172

_hashing_transform(*args, **kwargs) L16
_iteritems(d) L23
"Like d.iteritems, but accepts any collections.Mapping."

sklearn/feature_extraction/image.py
class PatchExtractor(BaseEstimator) L449
  "Extracts patches from a collection of images"
  __init__(self, *, patch_size=None, max_patches=None, random_state=None) L487
  fit(self, X, y=None) L493
  "Do nothing and return the estimator unchanged."
  transform(self, X) L506
  "Transforms the image samples in X into a matrix of patch data."
  _more_tags(self) L549

_make_edges_3d(n_x, n_y, n_z=1) L32
"Returns a list of edges for a 3D image."
_compute_gradient_3d(edges, img) L54
_mask_edges_weights(mask, edges, weights=None) L67
"Apply a mask to edges (weighted or not)"
_to_graph(n_x, n_y, n_z, mask=None, img=None, return_as=sparse.coo_matrix, dtype=None) L88
"Auxiliary function for img_to_graph and grid_to_graph"
img_to_graph(img, *, mask=None, return_as=sparse.coo_matrix, dtype=None) L134
"Graph of the pixel-to-pixel gradient connections"
grid_to_graph(n_x, n_y, n_z=1, *, mask=None, return_as=sparse.coo_matrix, dtype=int) L171
"Graph of the pixel-to-pixel connections"
_compute_n_patches(i_h, i_w, p_h, p_w, max_patches=None) L210
"Compute the number of patches that will be extracted in an image."
_extract_patches(arr, patch_shape=8, extraction_step=1) L250
"Extracts patches of any n-dimensional array in place using strides."
extract_patches_2d(image, patch_size, *, max_patches=None, random_state=None) L309
"Reshape a 2D image into a collection of patches"
reconstruct_from_patches_2d(patches, image_size) L405
"Reconstruct the image from all of its patches."

sklearn/feature_extraction/setup.py
configuration(parent_package='', top_path=None) L5

sklearn/feature_extraction/text.py
class _VectorizerMixin L188
  "Provides common code for text vectorizers (tokenization logic)."
  decode(self, doc) L193
  "Decode the input into a string of unicode symbols."
  _word_ngrams(self, tokens, stop_words=None) L224
  "Turn tokens into a sequence of n-grams after stop words filtering"
  _char_ngrams(self, text_document) L255
  "Tokenize text_document into a sequence of character n-grams"
  _char_wb_ngrams(self, text_document) L278
  "Whitespace sensitive char-n-gram tokenization."
  build_preprocessor(self) L306
  "Return a function to preprocess the text before tokenization."
  build_tokenizer(self) L334
  "Return a function that splits a string into a sequence of tokens."
  get_stop_words(self) L347
  "Build or fetch the effective stop words list."
  _check_stop_words_consistency(self, stop_words, preprocess, tokenize) L357
  "Check if stop words are consistent"
  build_analyzer(self) L394
  "Return a callable that handles preprocessing, tokenization"
  _validate_vocabulary(self) L434
  _check_vocabulary(self) L462
  "Check if vocabulary is empty or missing (not fitted)"
  _validate_params(self) L472
  "Check validity of ngram_range parameter"
  _warn_for_unused_params(self) L481

class HashingVectorizer(TransformerMixin, _VectorizerMixin, BaseEstimator) L508
  "Convert a collection of text documents to a matrix of token occurrences"
  __init__(self, *, input='content', encoding='utf-8', decode_error='strict', strip_accents=None, lowercase=True, preprocessor=None, tokenizer=None, stop_words=None, token_pattern='(?u)\\b\\w\\w+\\b', ngram_range=(1, 1), analyzer='word', n_features=2 ** 20, binary=False, norm='l2', alternate_sign=True, dtype=np.float64) L676
  partial_fit(self, X, y=None) L700
  "Does nothing: this transformer is stateless."
  fit(self, X, y=None) L713
  "Does nothing: this transformer is stateless."
  transform(self, X) L733
  "Transform a sequence of documents to a document-term matrix."
  fit_transform(self, X, y=None) L763
  "Transform a sequence of documents to a document-term matrix."
  _get_hasher(self) L783
  _more_tags(self) L788

class CountVectorizer(_VectorizerMixin, BaseEstimator) L800
  "Convert a collection of text documents to a matrix of token counts"
  __init__(self, *, input='content', encoding='utf-8', decode_error='strict', strip_accents=None, lowercase=True, preprocessor=None, tokenizer=None, stop_words=None, token_pattern='(?u)\\b\\w\\w+\\b', ngram_range=(1, 1), analyzer='word', max_df=1.0, min_df=1, max_features=None, vocabulary=None, binary=False, dtype=np.int64) L998
  _sort_features(self, X, vocabulary) L1031
  "Sort features by name"
  _limit_features(self, X, vocabulary, high=None, low=None, limit=None) L1045
  "Remove too rare or too common features."
  _count_vocab(self, raw_documents, fixed_vocab) L1086
  "Create sparse feature matrix, and vocabulary where fixed_vocab=False"
  fit(self, raw_documents, y=None) L1146
  "Learn a vocabulary dictionary of all tokens in the raw documents."
  fit_transform(self, raw_documents, y=None) L1162
  "Learn the vocabulary dictionary and return document-term matrix."
  transform(self, raw_documents) L1220
  "Transform documents to document-term matrix."
  inverse_transform(self, X) L1248
  "Return terms per document with nonzero entries in X."
  get_feature_names(self) L1279
  "Array mapping from feature integer indices to feature name."
  _more_tags(self) L1293

class TfidfTransformer(TransformerMixin, BaseEstimator) L1302
  "Transform a count matrix to a normalized tf or tf-idf representation"
  __init__(self, *, norm='l2', use_idf=True, smooth_idf=True, sublinear_tf=False) L1410
  fit(self, X, y=None) L1417
  "Learn the idf vector (global term weights)."
  transform(self, X, copy=True) L1449
  "Transform a count matrix to a tf or tf-idf representation"
  idf_(self) L1496
  idf_(self, value) L1502
  _more_tags(self) L1508

class TfidfVectorizer(CountVectorizer) L1512
  "Convert a collection of raw documents to a matrix of TF-IDF features."
  __init__(self, *, input='content', encoding='utf-8', decode_error='strict', strip_accents=None, lowercase=True, preprocessor=None, tokenizer=None, analyzer='word', stop_words=None, token_pattern='(?u)\\b\\w\\w+\\b', ngram_range=(1, 1), max_df=1.0, min_df=1, max_features=None, vocabulary=None, binary=False, dtype=np.float64, norm='l2', use_idf=True, smooth_idf=True, sublinear_tf=False) L1716
  norm(self) L1742
  norm(self, value) L1746
  use_idf(self) L1750
  use_idf(self, value) L1754
  smooth_idf(self) L1758
  smooth_idf(self, value) L1762
  sublinear_tf(self) L1766
  sublinear_tf(self, value) L1770
  idf_(self) L1774
  idf_(self, value) L1778
  _check_params(self) L1787
  fit(self, raw_documents, y=None) L1794
  "Learn vocabulary and idf from training set."
  fit_transform(self, raw_documents, y=None) L1815
  "Learn vocabulary and idf, return document-term matrix."
  transform(self, raw_documents) L1840
  "Transform documents to document-term matrix."
  _more_tags(self) L1861

_preprocess(doc, accent_function=None, lower=False) L49
"Chain together an optional series of text preprocessing steps to"
_analyze(doc, analyzer=None, tokenizer=None, ngrams=None, preprocessor=None, decoder=None, stop_words=None) L75
"Chain together an optional series of text processing steps to go from"
strip_accents_unicode(s) L115
"Transform accentuated unicode symbols into their simple counterpart"
strip_accents_ascii(s) L143
"Transform accentuated unicode symbols into ascii or nothing"
strip_tags(s) L163
"Basic regexp based HTML / XML tag stripper function"
_check_stop_list(stop) L177
_document_frequency(X) L792
"Count the number of non-zero values for each feature in sparse X."
_make_int_array() L1297
"Construct an array.array of a type suitable for scipy.sparse indices."

sklearn/feature_selection/_base.py
class SelectorMixin(TransformerMixin) L20
  "Transformer mixin that performs feature selection given a support mask"
  get_support(self, indices=False) L29
  "Get a mask, or integer index, of the features selected"
  _get_support_mask(self) L53
  "Get the boolean mask indicating which features are selected"
  transform(self, X) L64
  "Reduce X to the selected features."
  inverse_transform(self, X) L90
  "Reverse the transformation operation"

_get_feature_importances(estimator, getter, transform_func=None, norm_order=1) L129
"Retrieve and aggregate (ndim > 1)  the feature importances"

sklearn/feature_selection/_from_model.py
class SelectFromModel(MetaEstimatorMixin, SelectorMixin, BaseEstimator) L61
  "Meta-transformer for selecting features based on importance weights."
  __init__(self, estimator, *, threshold=None, prefit=False, norm_order=1, max_features=None, importance_getter='auto') L159
  _get_support_mask(self) L169
  fit(self, X, y=None, **fit_params) L193
  "Fit the SelectFromModel meta-transformer."
  threshold_(self) L229
  partial_fit(self, X, y=None, **fit_params) L237
  "Fit the SelectFromModel meta-transformer only once."
  n_features_in_(self) L264
  _more_tags(self) L277

_calculate_threshold(estimator, importances, threshold) L17
"Interpret the threshold value"

sklearn/feature_selection/_mutual_info.py
_compute_mi_cc(x, y, n_neighbors) L18
"Compute mutual information between two continuous variables."
_compute_mi_cd(c, d, n_neighbors) L79
"Compute mutual information between continuous and discrete variables."
_compute_mi(x, y, x_discrete, y_discrete, n_neighbors=3) L150
"Compute mutual information between two variables."
_iterate_columns(X, columns=None) L166
"Iterate over columns of a matrix."
_estimate_mi(X, y, discrete_features='auto', discrete_target=False, n_neighbors=3, copy=True, random_state=None) L196
"Estimate mutual information between the features and the target."
mutual_info_regression(X, y, *, discrete_features='auto', n_neighbors=3, copy=True, random_state=None) L295
"Estimate mutual information for a continuous target variable."
mutual_info_classif(X, y, *, discrete_features='auto', n_neighbors=3, copy=True, random_state=None) L375
"Estimate mutual information for a discrete target variable."

sklearn/feature_selection/_rfe.py
class RFE(SelectorMixin, MetaEstimatorMixin, BaseEstimator) L39
  "Feature ranking with recursive feature elimination."
  __init__(self, estimator, *, n_features_to_select=None, step=1, verbose=0, importance_getter='auto') L144
  _estimator_type(self) L153
  classes_(self) L157
  fit(self, X, y) L160
  "Fit the RFE model and then the underlying estimator on the selected"
  _fit(self, X, y, step_score=None) L174
  predict(self, X) L266
  "Reduce X to the selected features and then predict using the"
  score(self, X, y) L284
  "Reduce X to the selected features and then return the score of the"
  _get_support_mask(self) L299
  decision_function(self, X) L304
  "Compute the decision function of ``X``."
  predict_proba(self, X) L326
  "Predict class probabilities for X."
  predict_log_proba(self, X) L346
  "Predict class log-probabilities for X."
  _more_tags(self) L363

class RFECV(RFE) L371
  "Feature ranking with recursive feature elimination and cross-validated"
  __init__(self, estimator, *, step=1, min_features_to_select=1, cv=None, scoring=None, verbose=0, n_jobs=None, importance_getter='auto') L516
  fit(self, X, y, groups=None) L528
  "Fit the RFE model and automatically tune the number of selected"

_rfe_single_fit(rfe, estimator, X, y, train, test, scorer) L28
"Return the score for a fit across one fold."

sklearn/feature_selection/_univariate_selection.py
class _BaseFilter(SelectorMixin, BaseEstimator) L315
  "Initialize the univariate feature selection."
  __init__(self, score_func) L325
  fit(self, X, y) L328
  "Run score function on (X, y) and get the appropriate features."
  _check_params(self, X, y) L365
  _more_tags(self) L368

class SelectPercentile(_BaseFilter) L375
  "Select features according to a percentile of the highest scores."
  __init__(self, score_func=f_classif, *, percentile=10) L432
  _check_params(self, X, y) L436
  _get_support_mask(self) L441

class SelectKBest(_BaseFilter) L461
  "Select features according to the k highest scores."
  __init__(self, score_func=f_classif, *, k=10) L520
  _check_params(self, X, y) L524
  _get_support_mask(self) L530

class SelectFpr(_BaseFilter) L547
  "Filter: Select the pvalues below alpha based on a FPR test."
  __init__(self, score_func=f_classif, *, alpha=0.05) L601
  _get_support_mask(self) L605

class SelectFdr(_BaseFilter) L611
  "Filter: Select the p-values for an estimated false discovery rate"
  __init__(self, score_func=f_classif, *, alpha=0.05) L669
  _get_support_mask(self) L673

class SelectFwe(_BaseFilter) L685
  "Filter: Select the p-values corresponding to Family-wise error rate"
  __init__(self, score_func=f_classif, *, alpha=0.05) L734
  _get_support_mask(self) L738

class GenericUnivariateSelect(_BaseFilter) L750
  "Univariate feature selector with configurable strategy."
  __init__(self, score_func=f_classif, *, mode='percentile', param=1e-05) L810
  _make_selector(self) L815
  _check_params(self, X, y) L826
  _get_support_mask(self) L835

_clean_nans(scores) L24
"Fixes Issue #1240: NaNs can't be properly compared, so change them to the"
f_oneway(*args) L43
"Performs a 1-way ANOVA."
f_classif(X, y) L123
"Compute the ANOVA F-value for the provided sample."
_chisquare(f_obs, f_exp) L154
"Fast replacement for scipy.stats.chisquare."
chi2(X, y) L173
"Compute chi-squared stats between each non-negative feature and class."
f_regression(X, y, *, center=True) L233
"Univariate linear regression tests."

sklearn/feature_selection/_variance_threshold.py
class VarianceThreshold(SelectorMixin, BaseEstimator) L11
  "Feature selector that removes all low-variance features."
  __init__(self, threshold=0.0) L48
  fit(self, X, y=None) L51
  "Learn empirical variances from X."
  _get_support_mask(self) L96
  _more_tags(self) L101

sklearn/gaussian_process/_gpc.py
class _BinaryGaussianProcessClassifierLaplace(BaseEstimator) L36
  "Binary Gaussian process classification based on Laplace approximation."
  __init__(self, kernel=None, *, optimizer='fmin_l_bfgs_b', n_restarts_optimizer=0, max_iter_predict=100, warm_start=False, copy_X_train=True, random_state=None) L149
  fit(self, X, y) L160
  "Fit Gaussian process classification model"
  obj_func(theta, eval_gradient=True) L202
  predict(self, X) L248
  "Perform classification on an array of test vectors X."
  predict_proba(self, X) L271
  "Return probability estimates for the test vector X."
  log_marginal_likelihood(self, theta=None, eval_gradient=False, clone_kernel=True) L311
  "Returns log-marginal likelihood of theta for training data."
  _posterior_mode(self, K, return_temporaries=False) L388
  "Mode-finding for binary Laplace GPC and fixed kernel."
  _constrained_optimization(self, obj_func, initial_theta, bounds) L441

class GaussianProcessClassifier(ClassifierMixin, BaseEstimator) L457
  "Gaussian process classification (GPC) based on Laplace approximation."
  __init__(self, kernel=None, *, optimizer='fmin_l_bfgs_b', n_restarts_optimizer=0, max_iter_predict=100, warm_start=False, copy_X_train=True, random_state=None, multi_class='one_vs_rest', n_jobs=None) L597
  fit(self, X, y) L611
  "Fit Gaussian process classification model"
  predict(self, X) L674
  "Perform classification on an array of test vectors X."
  predict_proba(self, X) L696
  "Return probability estimates for the test vector X."
  kernel_(self) L725
  log_marginal_likelihood(self, theta=None, eval_gradient=False, clone_kernel=True) L733
  "Returns log-marginal likelihood of theta for training data."

sklearn/gaussian_process/_gpr.py
class GaussianProcessRegressor(MultiOutputMixin, RegressorMixin, BaseEstimator) L23
  "Gaussian process regression (GPR)."
  __init__(self, kernel=None, *, alpha=1e-10, optimizer='fmin_l_bfgs_b', n_restarts_optimizer=0, normalize_y=False, copy_X_train=True, random_state=None) L154
  fit(self, X, y) L165
  "Fit Gaussian process regression model."
  obj_func(theta, eval_gradient=True) L222
  predict(self, X, return_std=False, return_cov=False) L278
  "Predict using the Gaussian process regression model"
  sample_y(self, X, n_samples=1, random_state=0) L381
  "Draw samples from Gaussian process and evaluate at X."
  log_marginal_likelihood(self, theta=None, eval_gradient=False, clone_kernel=True) L417
  "Returns log-marginal likelihood of theta for training data."
  _constrained_optimization(self, obj_func, initial_theta, bounds) L499
  _more_tags(self) L514

sklearn/gaussian_process/kernels.py
class Hyperparameter L47
  "A kernel hyperparameter's specification in form of a namedtuple."
  __new__(cls, name, value_type, bounds, n_elements=1, fixed=None) L115
  __eq__(self, other) L133

class Kernel L141
  "Base class for all kernels."
  get_params(self, deep=True) L147
  "Get parameters of this kernel."
  set_params(self, **params) L187
  "Set the parameters of this kernel."
  clone_with_theta(self, theta) L224
  "Returns a clone of self with given hyperparameters theta."
  n_dims(self) L237
  "Returns the number of non-fixed hyperparameters of the kernel."
  hyperparameters(self) L242
  "Returns a list of all hyperparameter specifications."
  theta(self) L249
  "Returns the (flattened, log-transformed) non-fixed hyperparameters."
  theta(self, theta) L273
  "Sets the (flattened, log-transformed) non-fixed hyperparameters."
  bounds(self) L302
  "Returns the log-transformed bounds on the theta."
  __add__(self, b) L318
  __radd__(self, b) L323
  __mul__(self, b) L328
  __rmul__(self, b) L333
  __pow__(self, b) L338
  __eq__(self, b) L341
  __repr__(self) L351
  __call__(self, X, Y=None, eval_gradient=False) L356
  "Evaluate the kernel."
  diag(self, X) L360
  "Returns the diagonal of the kernel k(X, X)."
  is_stationary(self) L379
  "Returns whether the kernel is stationary."
  requires_vector_input(self) L383
  "Returns whether the kernel is defined on fixed-length feature"

class NormalizedKernelMixin L390
  "Mixin for kernels which are normalized: k(X, X)=1."
  diag(self, X) L396
  "Returns the diagonal of the kernel k(X, X)."

class StationaryKernelMixin L416
  "Mixin for kernels which are stationary: k(X, Y)= f(X-Y)."
  is_stationary(self) L422
  "Returns whether the kernel is stationary."

class GenericKernelMixin L427
  "Mixin for kernels which operate on generic objects such as variable-"
  requires_vector_input(self) L435
  "Whether the kernel works only on fixed-length feature vectors."

class CompoundKernel(Kernel) L440
  "Kernel which is composed of a set of other kernels."
  __init__(self, kernels) L466
  get_params(self, deep=True) L469
  "Get parameters of this kernel."
  theta(self) L486
  "Returns the (flattened, log-transformed) non-fixed hyperparameters."
  theta(self, theta) L502
  "Sets the (flattened, log-transformed) non-fixed hyperparameters."
  bounds(self) L515
  "Returns the log-transformed bounds on the theta."
  __call__(self, X, Y=None, eval_gradient=False) L525
  "Return the kernel k(X, Y) and optionally its gradient."
  __eq__(self, b) L569
  is_stationary(self) L575
  "Returns whether the kernel is stationary."
  requires_vector_input(self) L580
  "Returns whether the kernel is defined on discrete structures."
  diag(self, X) L585
  "Returns the diagonal of the kernel k(X, X)."

class KernelOperator(Kernel) L605
  "Base class for all kernel operators."
  __init__(self, k1, k2) L611
  get_params(self, deep=True) L615
  "Get parameters of this kernel."
  hyperparameters(self) L639
  "Returns a list of all hyperparameter."
  theta(self) L654
  "Returns the (flattened, log-transformed) non-fixed hyperparameters."
  theta(self, theta) L670
  "Sets the (flattened, log-transformed) non-fixed hyperparameters."
  bounds(self) L683
  "Returns the log-transformed bounds on the theta."
  __eq__(self, b) L697
  is_stationary(self) L703
  "Returns whether the kernel is stationary."
  requires_vector_input(self) L708
  "Returns whether the kernel is stationary."

class Sum(KernelOperator) L714
  "The `Sum` kernel takes two kernels :math:`k_1` and :math:`k_2`"
  __call__(self, X, Y=None, eval_gradient=False) L753
  "Return the kernel k(X, Y) and optionally its gradient."
  diag(self, X) L788
  "Returns the diagonal of the kernel k(X, X)."
  __repr__(self) L807

class Product(KernelOperator) L811
  "The `Product` kernel takes two kernels :math:`k_1` and :math:`k_2`"
  __call__(self, X, Y=None, eval_gradient=False) L851
  "Return the kernel k(X, Y) and optionally its gradient."
  diag(self, X) L887
  "Returns the diagonal of the kernel k(X, X)."
  __repr__(self) L906

class Exponentiation(Kernel) L910
  "The Exponentiation kernel takes one base kernel and a scalar parameter"
  __init__(self, kernel, exponent) L951
  get_params(self, deep=True) L955
  "Get parameters of this kernel."
  hyperparameters(self) L976
  "Returns a list of all hyperparameter."
  theta(self) L987
  "Returns the (flattened, log-transformed) non-fixed hyperparameters."
  theta(self, theta) L1003
  "Sets the (flattened, log-transformed) non-fixed hyperparameters."
  bounds(self) L1014
  "Returns the log-transformed bounds on the theta."
  __eq__(self, b) L1024
  __call__(self, X, Y=None, eval_gradient=False) L1029
  "Return the kernel k(X, Y) and optionally its gradient."
  diag(self, X) L1066
  "Returns the diagonal of the kernel k(X, X)."
  __repr__(self) L1085
  is_stationary(self) L1088
  "Returns whether the kernel is stationary."
  requires_vector_input(self) L1093
  "Returns whether the kernel is defined on discrete structures."

class ConstantKernel(StationaryKernelMixin, GenericKernelMixin, Kernel) L1098
  "Constant kernel."
  __init__(self, constant_value=1.0, constant_value_bounds=(1e-05, 100000.0)) L1148
  hyperparameter_constant_value(self) L1153
  __call__(self, X, Y=None, eval_gradient=False) L1157
  "Return the kernel k(X, Y) and optionally its gradient."
  diag(self, X) L1202
  "Returns the diagonal of the kernel k(X, X)."
  __repr__(self) L1222

class WhiteKernel(StationaryKernelMixin, GenericKernelMixin, Kernel) L1226
  "White kernel."
  __init__(self, noise_level=1.0, noise_level_bounds=(1e-05, 100000.0)) L1267
  hyperparameter_noise_level(self) L1272
  __call__(self, X, Y=None, eval_gradient=False) L1276
  "Return the kernel k(X, Y) and optionally its gradient."
  diag(self, X) L1320
  "Returns the diagonal of the kernel k(X, X)."
  __repr__(self) L1340

class RBF(StationaryKernelMixin, NormalizedKernelMixin, Kernel) L1345
  "Radial-basis function kernel (aka squared-exponential kernel)."
  __init__(self, length_scale=1.0, length_scale_bounds=(1e-05, 100000.0)) L1407
  anisotropic(self) L1412
  hyperparameter_length_scale(self) L1416
  __call__(self, X, Y=None, eval_gradient=False) L1424
  "Return the kernel k(X, Y) and optionally its gradient."
  __repr__(self) L1484

class Matern(RBF) L1494
  "Matern kernel."
  __init__(self, length_scale=1.0, length_scale_bounds=(1e-05, 100000.0), nu=1.5) L1573
  __call__(self, X, Y=None, eval_gradient=False) L1578
  "Return the kernel k(X, Y) and optionally its gradient."
  f(theta) L1666
  __repr__(self) L1677

class RationalQuadratic(StationaryKernelMixin, NormalizedKernelMixin, Kernel) L1689
  "Rational Quadratic kernel."
  __init__(self, length_scale=1.0, alpha=1.0, length_scale_bounds=(1e-05, 100000.0), alpha_bounds=(1e-05, 100000.0)) L1751
  hyperparameter_length_scale(self) L1759
  hyperparameter_alpha(self) L1764
  __call__(self, X, Y=None, eval_gradient=False) L1767
  "Return the kernel k(X, Y) and optionally its gradient."
  __repr__(self) L1834

class ExpSineSquared(StationaryKernelMixin, NormalizedKernelMixin, Kernel) L1839
  "Exp-Sine-Squared kernel (aka periodic kernel)."
  __init__(self, length_scale=1.0, periodicity=1.0, length_scale_bounds=(1e-05, 100000.0), periodicity_bounds=(1e-05, 100000.0)) L1893
  hyperparameter_length_scale(self) L1902
  "Returns the length scale"
  hyperparameter_periodicity(self) L1908
  __call__(self, X, Y=None, eval_gradient=False) L1912
  "Return the kernel k(X, Y) and optionally its gradient."
  __repr__(self) L1975

class DotProduct(Kernel) L1980
  "Dot-Product kernel."
  __init__(self, sigma_0=1.0, sigma_0_bounds=(1e-05, 100000.0)) L2036
  hyperparameter_sigma_0(self) L2041
  __call__(self, X, Y=None, eval_gradient=False) L2044
  "Return the kernel k(X, Y) and optionally its gradient."
  diag(self, X) L2090
  "Returns the diagonal of the kernel k(X, X)."
  is_stationary(self) L2109
  "Returns whether the kernel is stationary."
  __repr__(self) L2113

class PairwiseKernel(Kernel) L2131
  "Wrapper for kernels in sklearn.metrics.pairwise."
  __init__(self, gamma=1.0, gamma_bounds=(1e-05, 100000.0), metric='linear', pairwise_kernels_kwargs=None) L2187
  hyperparameter_gamma(self) L2195
  __call__(self, X, Y=None, eval_gradient=False) L2198
  "Return the kernel k(X, Y) and optionally its gradient."
  f(gamma) L2238
  diag(self, X) L2246
  "Returns the diagonal of the kernel k(X, X)."
  is_stationary(self) L2266
  "Returns whether the kernel is stationary."
  __repr__(self) L2270

_check_length_scale(X, length_scale) L36
_approx_fprime(xk, f, epsilon, args=()) L2119

sklearn/impute/_base.py
class _BaseImputer(TransformerMixin, BaseEstimator) L65
  "Base class for all imputers."
  __init__(self, *, missing_values=np.nan, add_indicator=False) L71
  _fit_indicator(self, X) L75
  "Fit a MissingIndicator."
  _transform_indicator(self, X) L85
  "Compute the indicator mask.'"
  _concatenate_indicator(self, X_imputed, X_indicator) L99
  "Concatenate indicator mask with the imputed data."
  _more_tags(self) L114

class SimpleImputer(_BaseImputer) L118
  "Imputation transformer for completing missing values."
  __init__(self, *, missing_values=np.nan, strategy='mean', fill_value=None, verbose=0, copy=True, add_indicator=False) L212
  _validate_input(self, X, in_fit) L223
  fit(self, X, y=None) L272
  "Fit the imputer on X."
  _sparse_fit(self, X, strategy, missing_values, fill_value) L325
  "Fit the transformer on sparse data."
  _dense_fit(self, X, strategy, missing_values, fill_value) L362
  "Fit the transformer on dense data."
  transform(self, X) L413
  "Impute all missing values in X."
  inverse_transform(self, X) L472
  "Convert the data back to the original representation."

class MissingIndicator(TransformerMixin, BaseEstimator) L531
  "Binary indicators for missing values."
  __init__(self, *, missing_values=np.nan, features='missing-only', sparse='auto', error_on_new=True) L599
  _get_missing_features_info(self, X) L606
  "Compute the imputer mask and the indices of the features"
  _validate_input(self, X, in_fit) L661
  _fit(self, X, y=None) L687
  "Fit the transformer on X."
  fit(self, X, y=None) L720
  "Fit the transformer on X."
  transform(self, X) L738
  "Generate missing values indicator for X."
  fit_transform(self, X, y=None) L775
  "Generate missing values indicator for X."
  _more_tags(self) L798

_check_inputs_dtype(X, missing_values) L22
_most_frequent(array, extra_value, n_repeat) L31
"Compute the most frequent value in a 1d array extended with"

sklearn/impute/_iterative.py
class IterativeImputer(_BaseImputer) L27
  "Multivariate imputer that estimates each feature from all the others."
  __init__(self, estimator=None, *, missing_values=np.nan, sample_posterior=False, max_iter=10, tol=0.001, n_nearest_features=None, initial_strategy='mean', imputation_order='ascending', skip_complete=False, min_value=-np.inf, max_value=np.inf, verbose=0, random_state=None, add_indicator=False) L211
  _impute_one_feature(self, X_filled, mask_missing_values, feat_idx, neighbor_feat_idx, estimator=None, fit_mode=True) L244
  "Impute a single feature from the others provided."
  _get_neighbor_feat_idx(self, n_features, feat_idx, abs_corr_mat) L345
  "Get a list of other features to predict ``feat_idx``."
  _get_ordered_idx(self, mask_missing_values) L385
  "Decide in what order we will update the features."
  _get_abs_corr_mat(self, X_filled, tolerance=1e-06) L432
  "Get absolute correlation matrix between features."
  _initial_imputation(self, X) L471
  "Perform initial imputation for input X."
  _validate_limit(limit, limit_type, n_features) L520
  "Validate the limits (min/max) of the feature values"
  fit_transform(self, X, y=None) L551
  "Fits the imputer on X and return the transformed X."
  transform(self, X) L674
  "Imputes all missing values in X."
  fit(self, X, y=None) L724
  "Fits the imputer on X and return self."

sklearn/impute/_knn.py
class KNNImputer(_BaseImputer) L20
  "Imputation for completing missing values using k-Nearest Neighbors."
  __init__(self, *, missing_values=np.nan, n_neighbors=5, weights='uniform', metric='nan_euclidean', copy=True, add_indicator=False) L101
  _calc_impute(self, dist_pot_donors, n_neighbors, fit_X_col, mask_fit_X_col) L113
  "Helper function to impute a single column."
  fit(self, X, y=None) L159
  "Fit the imputer on X."
  transform(self, X) L194
  "Impute all missing values in X."
  process_chunk(dist_chunk, start) L239

sklearn/inspection/_partial_dependence.py
_grid_from_X(X, percentiles, grid_resolution) L39
"Generate a grid of points based on the percentiles of X."
_partial_dependence_recursion(est, grid, features) L107
_partial_dependence_brute(est, grid, features, X, response_method) L118
partial_dependence(estimator, X, features, *, response_method='auto', percentiles=(0.05, 0.95), grid_resolution=100, method='auto', kind='legacy') L207
"Partial dependence of ``features``."

sklearn/inspection/_permutation_importance.py
_calculate_permutation_scores(estimator, X, y, col_idx, random_state, n_repeats, scorer) L13
"Calculate score when `col_idx` is permuted."
permutation_importance(estimator, X, y, *, scoring=None, n_repeats=5, n_jobs=None, random_state=None) L42
"Permutation importance for feature evaluation [BRE]_."

sklearn/inspection/_plot/partial_dependence.py
class PartialDependenceDisplay L380
  "Partial Dependence Plot (PDP)"
  __init__(self, pd_results, *, features, feature_names, target_idx, pdp_lim, deciles, kind='average', subsample=1000) L501
  _get_sample_count(self, n_samples) L512
  plot(self, ax=None, n_cols=3, line_kw=None, contour_kw=None) L521
  "Plot partial dependence plots."

plot_partial_dependence(estimator, X, features, *, feature_names=None, target=None, response_method='auto', n_cols=3, grid_resolution=100, percentiles=(0.05, 0.95), method='auto', n_jobs=None, verbose=0, line_kw=None, contour_kw=None, ax=None, kind='average', subsample=1000) L20
"Partial dependence (PD) and individual conditional expectation (ICE)"
convert_feature(fx) L268

sklearn/inspection/setup.py
configuration(parent_package='', top_path=None) L4

sklearn/isotonic.py
class IsotonicRegression(RegressorMixin, TransformerMixin, BaseEstimator) L134
  "Isotonic regression model."
  __init__(self, *, y_min=None, y_max=None, increasing=True, out_of_bounds='nan') L219
  _check_input_data_shape(self, X) L226
  _build_f(self, X, y) L232
  "Build the f_ interp1d function."
  _build_y(self, X, y, sample_weight, trim_duplicates=True) L249
  "Build the y_ IsotonicRegression."
  fit(self, X, y, sample_weight=None) L296
  "Fit the model using X, y as training data."
  transform(self, T) L340
  "Transform new data by linear interpolation"
  predict(self, T) L380
  "Predict new data by linear interpolation."
  __getstate__(self) L395
  "Pickle-protocol - return state of the estimator."
  __setstate__(self, state) L402
  "Pickle-protocol - set state of the estimator."
  _more_tags(self) L411

check_increasing(x, y) L22
"Determine whether y is monotonically correlated with x."
isotonic_regression(y, *, sample_weight=None, y_min=None, y_max=None, increasing=True) L80
"Solve the isotonic regression model."

sklearn/kernel_approximation.py
class RBFSampler(TransformerMixin, BaseEstimator) L25
  "Approximates feature map of an RBF kernel by Monte Carlo approximation"
  __init__(self, *, gamma=1.0, n_components=100, random_state=None) L85
  fit(self, X, y=None) L90
  "Fit the model with X."
  transform(self, X) L118
  "Apply the approximate feature map to X."

class SkewedChi2Sampler(TransformerMixin, BaseEstimator) L141
  "Approximates feature map of the "skewed chi-squared" kernel by Monte"
  __init__(self, *, skewedness=1.0, n_components=100, random_state=None) L201
  fit(self, X, y=None) L206
  "Fit the model with X."
  transform(self, X) L234
  "Apply the approximate feature map to X."

class AdditiveChi2Sampler(TransformerMixin, BaseEstimator) L265
  "Approximate feature map for additive chi2 kernel."
  __init__(self, *, sample_steps=2, sample_interval=None) L332
  fit(self, X, y=None) L336
  "Set the parameters"
  transform(self, X) L368
  "Apply approximate feature map to X."
  _transform_dense(self, X) L397
  _transform_sparse(self, X) L423
  _more_tags(self) L451

class Nystroem(TransformerMixin, BaseEstimator) L456
  "Approximate a kernel map using a subset of the training data."
  __init__(self, kernel='rbf', *, gamma=None, coef0=None, degree=None, kernel_params=None, n_components=100, random_state=None) L549
  fit(self, X, y=None) L559
  "Fit estimator to data."
  transform(self, X) L601
  "Apply feature map to X."
  _get_kernel_params(self) L627

sklearn/kernel_ridge.py
class KernelRidge(MultiOutputMixin, RegressorMixin, BaseEstimator) L16
  "Kernel ridge regression."
  __init__(self, alpha=1, *, kernel='linear', gamma=None, degree=3, coef0=1, kernel_params=None) L118
  _get_kernel(self, X, Y=None) L127
  _pairwise(self) L138
  fit(self, X, y=None, sample_weight=None) L141
  "Fit Kernel Ridge regression model"
  predict(self, X) L185
  "Predict using the kernel ridge model"

sklearn/linear_model/_base.py
class LinearModel(BaseEstimator) L208
  "Base class for Linear Models"
  fit(self, X, y) L212
  "Fit model."
  _decision_function(self, X) L215
  predict(self, X) L222
  "Predict using the linear model."
  _set_intercept(self, X_offset, y_offset, X_scale) L240
  "Set the intercept_"
  _more_tags(self) L249

class LinearClassifierMixin(ClassifierMixin) L255
  "Mixin for linear classifiers."
  decision_function(self, X) L261
  "Predict confidence scores for samples."
  predict(self, X) L293
  "Predict class labels for samples in X."
  _predict_proba_lr(self, X) L314
  "Probability estimation for OvR logistic regression."

class SparseCoefMixin L331
  "Mixin for converting coef_ to and from CSR format."
  densify(self) L337
  "Convert coefficient matrix to dense array format."
  sparsify(self) L357
  "Convert coefficient matrix to sparse format."

class LinearRegression(MultiOutputMixin, RegressorMixin, LinearModel) L389
  "Ordinary least squares Linear Regression."
  __init__(self, *, fit_intercept=True, normalize=False, copy_X=True, n_jobs=None) L474
  fit(self, X, y, sample_weight=None) L481
  "Fit linear model."
  matvec(b) L524
  rmatvec(b) L527

make_dataset(X, y, sample_weight, random_state=None) L49
"Create ``Dataset`` abstraction for sparse and dense inputs."
_preprocess_data(X, y, fit_intercept, normalize=False, copy=True, sample_weight=None, return_mean=False, check_input=True) L102
"Center and scale data."
_rescale_data(X, y, sample_weight) L184
"Rescale data sample-wise by square root of sample_weight."
_pre_fit(X, y, Xy, precompute, normalize, fit_intercept, copy, check_input=True, sample_weight=None) L556
"Aux function used at beginning of fit in linear models"

sklearn/linear_model/_bayes.py
class BayesianRidge(RegressorMixin, LinearModel) L23
  "Bayesian ridge regression."
  __init__(self, *, n_iter=300, tol=0.001, alpha_1=1e-06, alpha_2=1e-06, lambda_1=1e-06, lambda_2=1e-06, alpha_init=None, lambda_init=None, compute_score=False, fit_intercept=True, normalize=False, copy_X=True, verbose=False) L150
  fit(self, X, y, sample_weight=None) L168
  "Fit the model"
  predict(self, X, return_std=False) L294
  "Predict using the linear model."
  _update_coef_(self, X, y, n_samples, n_features, XT_y, U, Vh, eigen_vals_, alpha_, lambda_) L326
  "Update posterior mean and compute corresponding rmse."
  _log_marginal_likelihood(self, n_samples, n_features, eigen_vals, alpha_, lambda_, coef, rmse) L350
  "Log marginal likelihood."

class ARDRegression(RegressorMixin, LinearModel) L385
  "Bayesian ARD regression."
  __init__(self, *, n_iter=300, tol=0.001, alpha_1=1e-06, alpha_2=1e-06, lambda_1=1e-06, lambda_2=1e-06, compute_score=False, threshold_lambda=10000.0, fit_intercept=True, normalize=False, copy_X=True, verbose=False) L495
  fit(self, X, y) L512
  "Fit the ARDRegression model according to the given training data"
  update_coeff(X, y, coef_, alpha_, keep_lambda, sigma_) L558
  _update_sigma_woodbury(self, X, alpha_, lambda_, keep_lambda) L616
  _update_sigma(self, X, alpha_, lambda_, keep_lambda) L633
  predict(self, X, return_std=False) L644
  "Predict using the linear model."

sklearn/linear_model/_coordinate_descent.py
class ElasticNet(MultiOutputMixin, RegressorMixin, LinearModel) L558
  "Linear regression with combined L1 and L2 priors as regularizer."
  __init__(self, alpha=1.0, *, l1_ratio=0.5, fit_intercept=True, normalize=False, precompute=False, max_iter=1000, copy_X=True, tol=0.0001, warm_start=False, positive=False, random_state=None, selection='cyclic') L702
  fit(self, X, y, sample_weight=None, check_input=True) L719
  "Fit model with coordinate descent."
  sparse_coef_(self) L870
  "sparse representation of the fitted ``coef_``"
  _decision_function(self, X) L874
  "Decision function of the linear model"

class Lasso(ElasticNet) L897
  "Linear Model trained with L1 prior as regularizer (aka the Lasso)"
  __init__(self, alpha=1.0, *, fit_intercept=True, normalize=False, precompute=False, copy_X=True, max_iter=1000, tol=0.0001, warm_start=False, positive=False, random_state=None, selection='cyclic') L1016

class LinearModelCV(MultiOutputMixin, LinearModel) L1138
  "Base class for iterative model fitting along a regularization path"
  __init__(self, eps=0.001, n_alphas=100, alphas=None, fit_intercept=True, normalize=False, precompute='auto', max_iter=1000, tol=0.0001, copy_X=True, cv=None, verbose=False, n_jobs=None, positive=False, random_state=None, selection='cyclic') L1142
  _get_estimator(self) L1163
  "Model to be fitted after the best alpha has been determined."
  _is_multitask(self) L1167
  "Bool indicating if class is meant for multidimensional target."
  fit(self, X, y) L1170
  "Fit linear model with coordinate descent"

class LassoCV(RegressorMixin, LinearModelCV) L1348
  "Lasso linear model with iterative fitting along a regularization path."
  __init__(self, *, eps=0.001, n_alphas=100, alphas=None, fit_intercept=True, normalize=False, precompute='auto', max_iter=1000, tol=0.0001, copy_X=True, cv=None, verbose=False, n_jobs=None, positive=False, random_state=None, selection='cyclic') L1502
  _get_estimator(self) L1514
  _is_multitask(self) L1517
  _more_tags(self) L1520

class ElasticNetCV(RegressorMixin, LinearModelCV) L1524
  "Elastic Net model with iterative fitting along a regularization path."
  __init__(self, *, l1_ratio=0.5, eps=0.001, n_alphas=100, alphas=None, fit_intercept=True, normalize=False, precompute='auto', max_iter=1000, tol=0.0001, cv=None, copy_X=True, verbose=0, n_jobs=None, positive=False, random_state=None, selection='cyclic') L1709
  _get_estimator(self) L1731
  _is_multitask(self) L1734
  _more_tags(self) L1737

class MultiTaskElasticNet(Lasso) L1744
  "Multi-task ElasticNet model trained with L1/L2 mixed-norm as regularizer"
  __init__(self, alpha=1.0, *, l1_ratio=0.5, fit_intercept=True, normalize=False, copy_X=True, max_iter=1000, tol=0.0001, warm_start=False, random_state=None, selection='cyclic') L1861
  fit(self, X, y) L1875
  "Fit MultiTaskElasticNet model with coordinate descent"
  _more_tags(self) L1944

class MultiTaskLasso(MultiTaskElasticNet) L1948
  "Multi-task Lasso model trained with L1/L2 mixed-norm as regularizer."
  __init__(self, alpha=1.0, *, fit_intercept=True, normalize=False, copy_X=True, max_iter=1000, tol=0.0001, warm_start=False, random_state=None, selection='cyclic') L2055

class MultiTaskElasticNetCV(RegressorMixin, LinearModelCV) L2070
  "Multi-task L1/L2 ElasticNet with built-in cross-validation."
  __init__(self, *, l1_ratio=0.5, eps=0.001, n_alphas=100, alphas=None, fit_intercept=True, normalize=False, max_iter=1000, tol=0.0001, cv=None, copy_X=True, verbose=0, n_jobs=None, random_state=None, selection='cyclic') L2238
  _get_estimator(self) L2258
  _is_multitask(self) L2261
  _more_tags(self) L2264

class MultiTaskLassoCV(RegressorMixin, LinearModelCV) L2268
  "Multi-task Lasso model trained with L1/L2 mixed-norm as regularizer."
  __init__(self, *, eps=0.001, n_alphas=100, alphas=None, fit_intercept=True, normalize=False, max_iter=1000, tol=0.0001, copy_X=True, cv=None, verbose=False, n_jobs=None, random_state=None, selection='cyclic') L2419
  _get_estimator(self) L2431
  _is_multitask(self) L2434
  _more_tags(self) L2437

_set_order(X, y, order='C') L33
"Change the order of X and y if necessary."
_alpha_grid(X, y, Xy=None, l1_ratio=1.0, fit_intercept=True, eps=0.001, n_alphas=100, normalize=False, copy_X=True) L80
"Compute the grid of alpha values for elastic net parameter search"
lasso_path(X, y, *, eps=0.001, n_alphas=100, alphas=None, precompute='auto', Xy=None, copy_X=True, coef_init=None, verbose=False, return_n_iter=False, positive=False, **params) L172
"Compute Lasso path with coordinate descent"
enet_path(X, y, *, l1_ratio=0.5, eps=0.001, n_alphas=100, alphas=None, precompute='auto', Xy=None, copy_X=True, coef_init=None, verbose=False, return_n_iter=False, positive=False, check_input=True, **params) L318
"Compute elastic net path with coordinate descent."
_path_residuals(X, y, train, test, path, path_params, alphas=None, l1_ratio=1, X_order=None, dtype=None) L1031
"Returns the MSE for the models computed by 'path'"

sklearn/linear_model/_glm/glm.py
class GeneralizedLinearRegressor(BaseEstimator, RegressorMixin) L51
  "Regression via a penalized Generalized Linear Model (GLM)."
  __init__(self, *, alpha=1.0, fit_intercept=True, family='normal', link='auto', solver='lbfgs', max_iter=100, tol=0.0001, warm_start=False, verbose=0) L127
  fit(self, X, y, sample_weight=None) L141
  "Fit a Generalized Linear Model."
  func(coef, X, y, weights, alpha, family, link) L261
  _linear_predictor(self, X) L298
  "Compute the linear_predictor = `X @ coef_ + intercept_`."
  predict(self, X) L317
  "Predict using GLM with feature matrix X."
  score(self, X, y, sample_weight=None) L335
  "Compute D^2, the percentage of deviance explained."
  _more_tags(self) L376

class PoissonRegressor(GeneralizedLinearRegressor) L389
  "Generalized Linear Model with a Poisson distribution."
  __init__(self, *, alpha=1.0, fit_intercept=True, max_iter=100, tol=0.0001, warm_start=False, verbose=0) L451
  family(self) L459
  family(self, value) L464

class GammaRegressor(GeneralizedLinearRegressor) L469
  "Generalized Linear Model with a Gamma distribution."
  __init__(self, *, alpha=1.0, fit_intercept=True, max_iter=100, tol=0.0001, warm_start=False, verbose=0) L531
  family(self) L539
  family(self, value) L544

class TweedieRegressor(GeneralizedLinearRegressor) L549
  "Generalized Linear Model with a Tweedie distribution."
  __init__(self, *, power=0.0, alpha=1.0, fit_intercept=True, link='auto', max_iter=100, tol=0.0001, warm_start=False, verbose=0) L642
  family(self) L652
  family(self, value) L661

_safe_lin_pred(X, coef) L30
"Compute the linear predictor taking care if intercept is present."
_y_pred_deviance_derivative(coef, X, y, weights, family, link) L38
"Compute y_pred and the derivative of the deviance w.r.t coef."

sklearn/linear_model/_glm/link.py
class BaseLink L14
  "Abstract base class for Link functions."
  __call__(self, y_pred) L18
  "Compute the link function g(y_pred)."
  derivative(self, y_pred) L31
  "Compute the derivative of the link g'(y_pred)."
  inverse(self, lin_pred) L41
  "Compute the inverse link function h(lin_pred)."
  inverse_derivative(self, lin_pred) L54
  "Compute the derivative of the inverse link function h'(lin_pred)."

class IdentityLink(BaseLink) L64
  "The identity link function g(x)=x."
  __call__(self, y_pred) L67
  derivative(self, y_pred) L70
  inverse(self, lin_pred) L73
  inverse_derivative(self, lin_pred) L76

class LogLink(BaseLink) L80
  "The log link function g(x)=log(x)."
  __call__(self, y_pred) L83
  derivative(self, y_pred) L86
  inverse(self, lin_pred) L89
  inverse_derivative(self, lin_pred) L92

class LogitLink(BaseLink) L96
  "The logit link function g(x)=logit(x)."
  __call__(self, y_pred) L99
  derivative(self, y_pred) L102
  inverse(self, lin_pred) L105
  inverse_derivative(self, lin_pred) L108

sklearn/linear_model/_huber.py
class HuberRegressor(LinearModel, RegressorMixin, BaseEstimator) L125
  "Linear regression model that is robust to outliers."
  __init__(self, *, epsilon=1.35, max_iter=100, alpha=0.0001, warm_start=False, fit_intercept=True, tol=1e-05) L227
  fit(self, X, y, sample_weight=None) L236
  "Fit the model according to the given training data."

_huber_loss_and_gradient(w, X, y, epsilon, alpha, sample_weight=None) L17
"Returns the Huber loss and the gradient."

sklearn/linear_model/_least_angle.py
class Lars(MultiOutputMixin, RegressorMixin, LinearModel) L759
  "Least Angle Regression model a.k.a."
  __init__(self, *, fit_intercept=True, verbose=False, normalize=True, precompute='auto', n_nonzero_coefs=500, eps=np.finfo(float).eps, copy_X=True, fit_path=True, jitter=None, random_state=None) L862
  _get_gram(precompute, X, y) L878
  _fit(self, X, y, max_iter, alpha, fit_path, Xy=None) L887
  "Auxiliary method to fit the model using X, y as training data"
  fit(self, X, y, Xy=None) L945
  "Fit the model using X, y as training data."

class LassoLars(Lars) L987
  "Lasso model fit with Least Angle Regression a.k.a."
  __init__(self, alpha=1.0, *, fit_intercept=True, verbose=False, normalize=True, precompute='auto', max_iter=500, eps=np.finfo(float).eps, copy_X=True, fit_path=True, positive=False, jitter=None, random_state=None) L1118

class LarsCV(Lars) L1260
  "Cross-validated Least Angle Regression model."
  __init__(self, *, fit_intercept=True, verbose=False, max_iter=500, normalize=True, precompute='auto', cv=None, max_n_alphas=1000, n_jobs=None, eps=np.finfo(float).eps, copy_X=True) L1376
  _more_tags(self) L1390
  fit(self, X, y) L1393
  "Fit the model using X, y as training data."

class LassoLarsCV(LarsCV) L1474
  "Cross-validated Lasso, using the LARS algorithm."
  __init__(self, *, fit_intercept=True, verbose=False, max_iter=500, normalize=True, precompute='auto', cv=None, max_n_alphas=1000, n_jobs=None, eps=np.finfo(float).eps, copy_X=True, positive=False) L1618

class LassoLarsIC(LassoLars) L1637
  "Lasso model fit with Lars using BIC or AIC for model selection"
  __init__(self, criterion='aic', *, fit_intercept=True, verbose=False, normalize=True, precompute='auto', max_iter=500, eps=np.finfo(float).eps, copy_X=True, positive=False) L1752
  _more_tags(self) L1766
  fit(self, X, y, copy_X=None) L1769
  "Fit the model using X, y as training data."

lars_path(X, y, Xy=None, *, Gram=None, max_iter=500, alpha_min=0, method='lar', copy_X=True, eps=np.finfo(float).eps, copy_Gram=True, verbose=0, return_path=True, return_n_iter=False, positive=False) L33
"Compute Least Angle Regression or Lasso path using LARS algorithm [1]"
lars_path_gram(Xy, Gram, *, n_samples, max_iter=500, alpha_min=0, method='lar', copy_X=True, eps=np.finfo(float).eps, copy_Gram=True, verbose=0, return_path=True, return_n_iter=False, positive=False) L163
"lars_path in the sufficient stats mode [1]"
_lars_path_solver(X, y, Xy=None, Gram=None, n_samples=None, max_iter=500, alpha_min=0, method='lar', copy_X=True, eps=np.finfo(float).eps, copy_Gram=True, verbose=0, return_path=True, return_n_iter=False, positive=False) L279
"Compute Least Angle Regression or Lasso path using LARS algorithm [1]"
_check_copy_and_writeable(array, copy=False) L1139
_lars_path_residues(X_train, y_train, X_test, y_test, Gram=None, copy=True, method='lars', verbose=False, fit_intercept=True, normalize=True, max_iter=500, eps=np.finfo(float).eps, positive=False) L1145
"Compute the residues on left-out data for a full LARS path"

sklearn/linear_model/_logistic.py
class LogisticRegression(LinearClassifierMixin, SparseCoefMixin, BaseEstimator) L1011
  "Logistic Regression (aka logit, MaxEnt) classifier."
  __init__(self, penalty='l2', *, dual=False, tol=0.0001, C=1.0, fit_intercept=True, intercept_scaling=1, class_weight=None, random_state=None, solver='lbfgs', max_iter=100, multi_class='auto', verbose=0, warm_start=False, n_jobs=None, l1_ratio=None) L1254
  fit(self, X, y, sample_weight=None) L1276
  "Fit the model according to the given training data."
  predict_proba(self, X) L1437
  "Probability estimates."
  predict_log_proba(self, X) L1480
  "Predict logarithm of probability estimates."

class LogisticRegressionCV(LogisticRegression, LinearClassifierMixin, BaseEstimator) L1502
  "Logistic Regression CV (aka logit, MaxEnt) classifier."
  __init__(self, *, Cs=10, fit_intercept=True, cv=None, dual=False, penalty='l2', scoring=None, solver='lbfgs', tol=0.0001, max_iter=100, class_weight=None, n_jobs=None, verbose=0, refit=True, intercept_scaling=1.0, multi_class='auto', random_state=None, l1_ratios=None) L1746
  fit(self, X, y, sample_weight=None) L1769
  "Fit the model according to the given training data."
  score(self, X, y, sample_weight=None) L2064
  "Returns the score using the `scoring` option on the given"
  _more_tags(self) L2090

_intercept_dot(w, X, y) L46
"Computes y * np.dot(X, w)."
_logistic_loss_and_grad(w, X, y, alpha, sample_weight=None) L84
"Computes the logistic loss and gradient."
_logistic_loss(w, X, y, alpha, sample_weight=None) L135
"Computes the logistic loss."
_logistic_grad_hess(w, X, y, alpha, sample_weight=None) L171
"Computes the gradient and the Hessian, in the case of a logistic loss."
Hs(s) L233
_multinomial_loss(w, X, Y, alpha, sample_weight) L248
"Computes multinomial loss and class probabilities."
_multinomial_loss_grad(w, X, Y, alpha, sample_weight) L304
"Computes the multinomial loss, gradient and class probabilities."
_multinomial_grad_hess(w, X, Y, alpha, sample_weight) L357
"Computes the gradient and the Hessian, in the case of a multinomial loss."
hessp(v) L406
_check_solver(solver, penalty, dual) L430
_check_multi_class(multi_class, solver, n_classes) L460
_logistic_regression_path(X, y, pos_class=None, Cs=10, fit_intercept=True, max_iter=100, tol=0.0001, verbose=0, solver='lbfgs', coef=None, class_weight=None, dual=False, penalty='l2', intercept_scaling=1.0, multi_class='auto', random_state=None, check_input=True, max_squared_sum=None, sample_weight=None, l1_ratio=None) L477
"Compute a Logistic Regression model for a list of regularization"
func(x, *args) L735
func(x, *args) L737
grad(x, *args) L738
grad(x, *args) L747
_log_reg_scoring_path(X, y, train, test, pos_class=None, Cs=10, scoring=None, fit_intercept=False, max_iter=100, tol=0.0001, class_weight=None, verbose=0, solver='lbfgs', penalty='l2', dual=False, intercept_scaling=1.0, multi_class='auto', random_state=None, max_squared_sum=None, sample_weight=None, l1_ratio=None) L822
"Computes scores across logistic_regression_path"

sklearn/linear_model/_omp.py
class OrthogonalMatchingPursuit(MultiOutputMixin, RegressorMixin, LinearModel) L546
  "Orthogonal Matching Pursuit model (OMP)"
  __init__(self, *, n_nonzero_coefs=None, tol=None, fit_intercept=True, normalize=True, precompute='auto') L624
  fit(self, X, y) L632
  "Fit the model using X, y as training data."

class OrthogonalMatchingPursuitCV(RegressorMixin, LinearModel) L761
  "Cross-validated Orthogonal Matching Pursuit model (OMP)."
  __init__(self, *, copy=True, fit_intercept=True, normalize=True, max_iter=None, cv=None, n_jobs=None, verbose=False) L862
  fit(self, X, y) L872
  "Fit the model using X, y as training data."

_cholesky_omp(X, y, n_nonzero_coefs, tol=None, copy_X=True, return_path=False) L27
"Orthogonal Matching Pursuit step using the Cholesky decomposition."
_gram_omp(Gram, Xy, n_nonzero_coefs, tol_0=None, tol=None, copy_Gram=True, copy_Xy=True, return_path=False) L140
"Orthogonal Matching Pursuit step on a precomputed Gram matrix."
orthogonal_mp(X, y, *, n_nonzero_coefs=None, tol=None, precompute=False, copy_X=True, return_path=False, return_n_iter=False) L267
"Orthogonal Matching Pursuit (OMP)"
orthogonal_mp_gram(Gram, Xy, *, n_nonzero_coefs=None, tol=None, norms_squared=None, copy_Gram=True, copy_Xy=True, return_path=False, return_n_iter=False) L411
"Gram Orthogonal Matching Pursuit (OMP)"
_omp_path_residues(X_train, y_train, X_test, y_test, copy=True, fit_intercept=True, normalize=True, max_iter=100) L684
"Compute the residues on left-out data for a full LARS path"

sklearn/linear_model/_passive_aggressive.py
class PassiveAggressiveClassifier(BaseSGDClassifier) L10
  "Passive Aggressive Classifier"
  __init__(self, *, C=1.0, fit_intercept=True, max_iter=1000, tol=0.001, early_stopping=False, validation_fraction=0.1, n_iter_no_change=5, shuffle=True, verbose=0, loss='hinge', n_jobs=None, random_state=None, warm_start=False, class_weight=None, average=False) L169
  partial_fit(self, X, y, classes=None) L194
  "Fit linear model with Passive Aggressive algorithm."
  fit(self, X, y, coef_init=None, intercept_init=None) L234
  "Fit linear model with Passive Aggressive algorithm."

class PassiveAggressiveRegressor(BaseSGDRegressor) L262
  "Passive Aggressive Regressor"
  __init__(self, *, C=1.0, fit_intercept=True, max_iter=1000, tol=0.001, early_stopping=False, validation_fraction=0.1, n_iter_no_change=5, shuffle=True, verbose=0, loss='epsilon_insensitive', epsilon=DEFAULT_EPSILON, random_state=None, warm_start=False, average=False) L397
  partial_fit(self, X, y) L422
  "Fit linear model with Passive Aggressive algorithm."
  fit(self, X, y, coef_init=None, intercept_init=None) L445
  "Fit linear model with Passive Aggressive algorithm."

sklearn/linear_model/_perceptron.py
class Perceptron(BaseSGDClassifier) L8
  "Perceptron"
  __init__(self, *, penalty=None, alpha=0.0001, fit_intercept=True, max_iter=1000, tol=0.001, shuffle=True, verbose=0, eta0=1.0, n_jobs=None, random_state=0, early_stopping=False, validation_fraction=0.1, n_iter_no_change=5, class_weight=None, warm_start=False) L152

sklearn/linear_model/_ransac.py
class RANSACRegressor(MetaEstimatorMixin, RegressorMixin, MultiOutputMixin, BaseEstimator) L57
  "RANSAC (RANdom SAmple Consensus) algorithm."
  __init__(self, base_estimator=None, *, min_samples=None, residual_threshold=None, is_data_valid=None, is_model_valid=None, max_trials=100, max_skips=np.inf, stop_n_inliers=np.inf, stop_score=np.inf, stop_probability=0.99, loss='absolute_loss', random_state=None) L208
  fit(self, X, y, sample_weight=None) L228
  "Fit estimator using RANSAC algorithm."
  predict(self, X) L466
  "Predict using the estimated model."
  score(self, X, y) L484
  "Returns the score of the prediction."
  _more_tags(self) L506

_dynamic_max_trials(n_inliers, n_samples, min_samples, probability) L23
"Determine number trials such that at least one outlier-free subset is"

sklearn/linear_model/_ridge.py
class _BaseRidge(LinearModel) L521
  __init__(self, alpha=1.0, *, fit_intercept=True, normalize=False, copy_X=True, max_iter=None, tol=0.001, solver='auto', random_state=None) L524
  fit(self, X, y, sample_weight=None) L536

class Ridge(MultiOutputMixin, RegressorMixin, _BaseRidge) L603
  "Linear least squares with l2 regularization."
  __init__(self, alpha=1.0, *, fit_intercept=True, normalize=False, copy_X=True, max_iter=None, tol=0.001, solver='auto', random_state=None) L734
  fit(self, X, y, sample_weight=None) L743
  "Fit Ridge regression model."

class RidgeClassifier(LinearClassifierMixin, _BaseRidge) L765
  "Classifier using Ridge regression."
  __init__(self, alpha=1.0, *, fit_intercept=True, normalize=False, copy_X=True, max_iter=None, tol=0.001, class_weight=None, solver='auto', random_state=None) L892
  fit(self, X, y, sample_weight=None) L901
  "Fit Ridge classifier model."
  classes_(self) L949

class _X_CenterStackOp(sparse.linalg.LinearOperator) L987
  "Behaves as centered and scaled X with an added intercept column."
  __init__(self, X, X_mean, sqrt_sw) L994
  _matvec(self, v) L1001
  _matmat(self, v) L1007
  _transpose(self) L1013

class _XT_CenterStackOp(sparse.linalg.LinearOperator) L1017
  "Behaves as transposed centered and scaled X with an intercept column."
  __init__(self, X, X_mean, sqrt_sw) L1024
  _matvec(self, v) L1031
  _matmat(self, v) L1042

class _IdentityRegressor L1053
  "Fake regressor which will directly output the prediction."
  decision_function(self, y_predict) L1056
  predict(self, y_predict) L1059

class _IdentityClassifier(LinearClassifierMixin) L1063
  "Fake classifier which will directly output the prediction."
  __init__(self, classes) L1069
  decision_function(self, y_predict) L1072

class _RidgeGCV(LinearModel) L1076
  "Ridge regression with built-in Generalized Cross-Validation."
  __init__(self, alphas=(0.1, 1.0, 10.0), *, fit_intercept=True, normalize=False, scoring=None, copy_X=True, gcv_mode=None, store_cv_values=False, is_clf=False) L1119
  _decomp_diag(v_prime, Q) L1134
  _diag_dot(D, B) L1139
  _compute_gram(self, X, sqrt_sw) L1146
  "Computes the Gram matrix XX^T with possible centering."
  _compute_covariance(self, X, sqrt_sw) L1196
  "Computes covariance matrix X^TX with possible centering."
  _sparse_multidot_diag(self, X, A, X_mean, sqrt_sw) L1241
  "Compute the diagonal of (X - X_mean).dot(A).dot((X - X_mean).T)"
  _eigen_decompose_gram(self, X, y, sqrt_sw) L1279
  "Eigendecomposition of X.X^T, used when n_samples <= n_features."
  _solve_eigen_gram(self, alpha, y, sqrt_sw, X_mean, eigvals, Q, QT_y) L1293
  "Compute dual coefficients and diagonal of G^-1."
  _eigen_decompose_covariance(self, X, y, sqrt_sw) L1316
  "Eigendecomposition of X^T.X, used when n_samples > n_features"
  _solve_eigen_covariance_no_intercept(self, alpha, y, sqrt_sw, X_mean, eigvals, V, X) L1341
  "Compute dual coefficients and diagonal of G^-1."
  _solve_eigen_covariance_intercept(self, alpha, y, sqrt_sw, X_mean, eigvals, V, X) L1358
  "Compute dual coefficients and diagonal of G^-1."
  _solve_eigen_covariance(self, alpha, y, sqrt_sw, X_mean, eigvals, V, X) L1388
  "Compute dual coefficients and diagonal of G^-1."
  _svd_decompose_design_matrix(self, X, y, sqrt_sw) L1401
  _solve_svd_design_matrix(self, alpha, y, sqrt_sw, X_mean, singvals_sq, U, UT_y) L1415
  "Compute dual coefficients and diagonal of G^-1."
  fit(self, X, y, sample_weight=None) L1436
  "Fit Ridge regression model with gcv."

class _BaseRidgeCV(LinearModel) L1551
  __init__(self, alphas=(0.1, 1.0, 10.0), *, fit_intercept=True, normalize=False, scoring=None, cv=None, gcv_mode=None, store_cv_values=False) L1553
  fit(self, X, y, sample_weight=None) L1565
  "Fit Ridge regression model with cv."

class RidgeCV(MultiOutputMixin, RegressorMixin, _BaseRidgeCV) L1630
  "Ridge regression with built-in cross-validation."

class RidgeClassifierCV(LinearClassifierMixin, _BaseRidgeCV) L1747
  "Ridge classifier with built-in cross-validation."
  __init__(self, alphas=(0.1, 1.0, 10.0), *, fit_intercept=True, normalize=False, scoring=None, cv=None, class_weight=None, store_cv_values=False) L1862
  fit(self, X, y, sample_weight=None) L1870
  "Fit Ridge classifier with cv."
  classes_(self) L1910
  _more_tags(self) L1913

_solve_sparse_cg(X, y, alpha, max_iter=None, tol=0.001, verbose=0, X_offset=None, X_scale=None) L38
_get_rescaled_operator(X) L41
matvec(b) L45
rmatvec(b) L48
create_mv(curr_alpha) L66
_mv(x) L67
create_mv(curr_alpha) L71
_mv(x) L72
_solve_lsqr(X, y, alpha, max_iter=None, tol=0.001) L117
_solve_cholesky(X, y, alpha) L135
_solve_cholesky_kernel(K, y, alpha, sample_weight=None, copy=False) L159
_solve_svd(X, y, alpha) L220
_get_valid_accept_sparse(is_X_sparse, solver) L231
ridge_regression(X, y, alpha, *, sample_weight=None, solver='auto', max_iter=None, tol=0.001, verbose=0, random_state=None, return_n_iter=False, return_intercept=False, check_input=True) L239
"Solve the ridge equation by the method of normal equations."
_ridge_regression(X, y, alpha, sample_weight=None, solver='auto', max_iter=None, tol=0.001, verbose=0, random_state=None, return_n_iter=False, return_intercept=False, X_scale=None, X_offset=None, check_input=True) L380
_check_gcv_mode(X, gcv_mode) L953
_find_smallest_angle(query, vectors) L969
"Find the column of vectors that is most aligned with the query."

sklearn/linear_model/_sag.py
get_auto_step_size(max_squared_sum, alpha_scaled, loss, fit_intercept, n_samples=None, is_saga=False) L20
"Compute automatic step size for SAG solver"
sag_solver(X, y, sample_weight=None, loss='log', alpha=1.0, beta=0.0, max_iter=1000, tol=0.001, verbose=0, random_state=None, check_input=True, max_squared_sum=None, warm_start_mem=None, is_saga=False) L89
"SAG solver for Ridge and LogisticRegression"

sklearn/linear_model/_stochastic_gradient.py
class _ValidationScoreCallback L50
  "Callback for early stopping based on validation score"
  __init__(self, estimator, X_val, y_val, sample_weight_val, classes=None) L53
  __call__(self, coef, intercept) L63

class BaseSGD(SparseCoefMixin, BaseEstimator) L70
  "Base class for SGD classification and regression."
  __init__(self, loss, *, penalty='l2', alpha=0.0001, C=1.0, l1_ratio=0.15, fit_intercept=True, max_iter=1000, tol=0.001, shuffle=True, verbose=0, epsilon=0.1, random_state=None, learning_rate='optimal', eta0=0.0, power_t=0.5, early_stopping=False, validation_fraction=0.1, n_iter_no_change=5, warm_start=False, average=False) L73
  set_params(self, **kwargs) L103
  "Set and validate the parameters of estimator."
  fit(self, X, y) L121
  "Fit model."
  _validate_params(self, for_partial_fit=False) L124
  "Validate input params."
  _get_loss_function(self, loss) L157
  "Get concrete ``LossFunction`` object for str ``loss``."
  _get_learning_rate_type(self, learning_rate) L169
  _get_penalty_type(self, penalty) L176
  _allocate_parameter_mem(self, n_classes, n_features, coef_init=None, intercept_init=None) L183
  "Allocate mem for parameters; initialize if provided."
  _make_validation_split(self, y) L244
  "Split the dataset between training set and validation set."
  _make_validation_score_cb(self, validation_mask, X, y, sample_weight, classes=None) L282
  standard_coef_(self) L295
  standard_intercept_(self) L304
  average_coef_(self) L311
  average_intercept_(self) L318

class BaseSGDClassifier(LinearClassifierMixin, BaseSGD) L449
  __init__(self, loss='hinge', *, penalty='l2', alpha=0.0001, l1_ratio=0.15, fit_intercept=True, max_iter=1000, tol=0.001, shuffle=True, verbose=0, epsilon=DEFAULT_EPSILON, n_jobs=None, random_state=None, learning_rate='optimal', eta0=0.0, power_t=0.5, early_stopping=False, validation_fraction=0.1, n_iter_no_change=5, class_weight=None, warm_start=False, average=False) L466
  _partial_fit(self, X, y, alpha, C, loss, learning_rate, max_iter, classes, sample_weight, coef_init, intercept_init) L486
  _fit(self, X, y, alpha, C, loss, learning_rate, coef_init=None, intercept_init=None, sample_weight=None) L533
  _fit_binary(self, X, y, alpha, C, sample_weight, learning_rate, max_iter) L576
  "Fit a binary classifier on X and y."
  _fit_multiclass(self, X, y, alpha, C, learning_rate, sample_weight, max_iter) L603
  "Fit a multi-class classifier by combining binary classifiers"
  partial_fit(self, X, y, classes=None, sample_weight=None) L647
  "Perform one epoch of stochastic gradient descent on given samples."
  fit(self, X, y, coef_init=None, intercept_init=None, sample_weight=None) L696
  "Fit linear model with Stochastic Gradient Descent."

class SGDClassifier(BaseSGDClassifier) L731
  "Linear classifiers (SVM, logistic regression, etc.) with SGD training."
  __init__(self, loss='hinge', *, penalty='l2', alpha=0.0001, l1_ratio=0.15, fit_intercept=True, max_iter=1000, tol=0.001, shuffle=True, verbose=0, epsilon=DEFAULT_EPSILON, n_jobs=None, random_state=None, learning_rate='optimal', eta0=0.0, power_t=0.5, early_stopping=False, validation_fraction=0.1, n_iter_no_change=5, class_weight=None, warm_start=False, average=False) L963
  _check_proba(self) L981
  predict_proba(self) L987
  "Probability estimates."
  _predict_proba(self, X) L1026
  predict_log_proba(self) L1070
  "Log of probability estimates."
  _predict_log_proba(self, X) L1095
  _more_tags(self) L1098

class BaseSGDRegressor(RegressorMixin, BaseSGD) L1107
  __init__(self, loss='squared_loss', *, penalty='l2', alpha=0.0001, l1_ratio=0.15, fit_intercept=True, max_iter=1000, tol=0.001, shuffle=True, verbose=0, epsilon=DEFAULT_EPSILON, random_state=None, learning_rate='invscaling', eta0=0.01, power_t=0.25, early_stopping=False, validation_fraction=0.1, n_iter_no_change=5, warm_start=False, average=False) L1119
  _partial_fit(self, X, y, alpha, C, loss, learning_rate, max_iter, sample_weight, coef_init, intercept_init) L1135
  partial_fit(self, X, y, sample_weight=None) L1164
  "Perform one epoch of stochastic gradient descent on given samples."
  _fit(self, X, y, alpha, C, loss, learning_rate, coef_init=None, intercept_init=None, sample_weight=None) L1195
  fit(self, X, y, coef_init=None, intercept_init=None, sample_weight=None) L1223
  "Fit linear model with Stochastic Gradient Descent."
  _decision_function(self, X) L1254
  "Predict using the linear model"
  predict(self, X) L1274
  "Predict using the linear model"
  _fit_regressor(self, X, y, alpha, C, loss, learning_rate, sample_weight, max_iter) L1288

class SGDRegressor(BaseSGDRegressor) L1362
  "Linear model fitted by minimizing a regularized empirical loss with SGD"
  __init__(self, loss='squared_loss', *, penalty='l2', alpha=0.0001, l1_ratio=0.15, fit_intercept=True, max_iter=1000, tol=0.001, shuffle=True, verbose=0, epsilon=DEFAULT_EPSILON, random_state=None, learning_rate='invscaling', eta0=0.01, power_t=0.25, early_stopping=False, validation_fraction=0.1, n_iter_no_change=5, warm_start=False, average=False) L1572
  _more_tags(self) L1588

_prepare_fit_binary(est, y, i) L322
"Initialization for fit_binary."
fit_binary(est, i, X, y, alpha, C, learning_rate, max_iter, pos_weight, neg_weight, sample_weight, validation_mask=None, random_state=None) L354
"Fit a single binary classifier."

sklearn/linear_model/_theil_sen.py
class TheilSenRegressor(RegressorMixin, LinearModel) L196
  "Theil-Sen Estimator: robust multivariate regression model."
  __init__(self, *, fit_intercept=True, copy_X=True, max_subpopulation=10000.0, n_subsamples=None, max_iter=300, tol=0.001, random_state=None, n_jobs=None, verbose=False) L295
  _check_subparams(self, n_samples, n_features) L308
  fit(self, X, y) L345
  "Fit linear model."

_modified_weiszfeld_step(X, x_old) L29
"Modified Weiszfeld step."
_spatial_median(X, max_iter=300, tol=0.001) L77
"Spatial median (L1 median)."
_breakdown_point(n_samples, n_subsamples) L131
"Approximation of the breakdown point."
_lstsq(X, y, indices, fit_intercept) L151
"Least Squares Estimator for TheilSenRegressor class."

sklearn/linear_model/setup.py
configuration(parent_package='', top_path=None) L7

sklearn/manifold/_isomap.py
class Isomap(TransformerMixin, BaseEstimator) L16
  "Isomap Embedding"
  __init__(self, *, n_neighbors=5, n_components=2, eigen_solver='auto', tol=0, max_iter=None, path_method='auto', neighbors_algorithm='auto', n_jobs=None, metric='minkowski', p=2, metric_params=None) L127
  _fit_transform(self, X) L143
  reconstruction_error(self) L171
  "Compute the reconstruction error for the embedding."
  fit(self, X, y=None) L195
  "Compute the embedding vectors for data X"
  fit_transform(self, X, y=None) L214
  "Fit the model from data in X and transform X."
  transform(self, X) L232
  "Transform X."

sklearn/manifold/_locally_linear.py
class LocallyLinearEmbedding(TransformerMixin, _UnstableArchMixin, BaseEstimator) L520
  "Locally Linear Embedding"
  __init__(self, *, n_neighbors=5, n_components=2, reg=0.001, eigen_solver='auto', tol=1e-06, max_iter=100, method='standard', hessian_tol=0.0001, modified_tol=1e-12, neighbors_algorithm='auto', random_state=None, n_jobs=None) L635
  _fit_transform(self, X) L652
  fit(self, X, y=None) L669
  "Compute the embedding vectors for data X"
  fit_transform(self, X, y=None) L686
  "Compute the embedding vectors for data X and transform X."
  transform(self, X) L703
  "Transform new points into embedding space."

barycenter_weights(X, Z, reg=0.001) L21
"Compute barycenter weights of X from Y along the first axis"
barycenter_kneighbors_graph(X, n_neighbors, reg=0.001, n_jobs=None) L68
"Computes the barycenter weighted graph of k-Neighbors for points in X"
null_space(M, k, k_skip=1, eigen_solver='arpack', tol=1e-06, max_iter=100, random_state=None) L111
"Find the null space of a matrix M."
locally_linear_embedding(X, *, n_neighbors, n_components, reg=0.001, eigen_solver='auto', tol=1e-06, max_iter=100, method='standard', hessian_tol=0.0001, modified_tol=1e-12, random_state=None, n_jobs=None) L188
"Perform a Locally Linear Embedding analysis on the data."

sklearn/manifold/_mds.py
class MDS(BaseEstimator) L277
  "Multidimensional scaling"
  __init__(self, n_components=2, *, metric=True, n_init=4, max_iter=300, verbose=0, eps=0.001, n_jobs=None, random_state=None, dissimilarity='euclidean') L374
  _pairwise(self) L388
  fit(self, X, y=None, init=None) L391
  "Computes the position of the points in the embedding space"
  fit_transform(self, X, y=None, init=None) L412
  "Fit the data from X, and returns the embedded coordinates"

_smacof_single(dissimilarities, metric=True, n_components=2, init=None, max_iter=300, verbose=0, eps=0.001, random_state=None) L20
"Computes multidimensional scaling using SMACOF algorithm"
smacof(dissimilarities, *, metric=True, n_components=2, init=None, n_init=8, n_jobs=None, max_iter=300, verbose=0, eps=0.001, random_state=None, return_n_iter=False) L134
"Computes multidimensional scaling using the SMACOF algorithm."

sklearn/manifold/_spectral_embedding.py
class SpectralEmbedding(BaseEstimator) L355
  "Spectral embedding for non-linear dimensionality reduction."
  __init__(self, n_components=2, *, affinity='nearest_neighbors', gamma=None, random_state=None, eigen_solver=None, n_neighbors=None, n_jobs=None) L449
  _pairwise(self) L461
  _get_affinity_matrix(self, X, Y=None) L465
  "Calculate the affinity matrix from data"
  fit(self, X, y=None) L519
  "Fit the model from data in X."
  fit_transform(self, X, y=None) L560
  "Fit the model from data in X and transform X."

_graph_connected_component(graph, node_id) L26
"Find the largest graph connected components that contains one"
_graph_is_connected(graph) L69
"Return whether the graph is connected (True) or Not (False)"
_set_diag(laplacian, value, norm_laplacian) L92
"Set the diagonal of the laplacian matrix and convert it to a"
spectral_embedding(adjacency, *, n_components=8, eigen_solver=None, random_state=None, eigen_tol=0.0, norm_laplacian=True, drop_first=True) L137
"Project the sample on the first eigenvectors of the graph Laplacian."

sklearn/manifold/_t_sne.py
class TSNE(BaseEstimator) L477
  "t-distributed Stochastic Neighbor Embedding."
  __init__(self, n_components=2, *, perplexity=30.0, early_exaggeration=12.0, learning_rate=200.0, n_iter=1000, n_iter_without_progress=300, min_grad_norm=1e-07, metric='euclidean', init='random', verbose=0, random_state=None, method='barnes_hut', angle=0.5, n_jobs=None) L641
  _fit(self, X, skip_num_points=0) L662
  "Private function to fit the model using X as training data."
  _tsne(self, P, degrees_of_freedom, n_samples, X_embedded, neighbors=None, skip_num_points=0) L806
  "Runs t-SNE."
  fit_transform(self, X, y=None) L872
  "Fit X into an embedded space and return that transformed"
  fit(self, X, y=None) L896
  "Fit X into an embedded space."

_joint_probabilities(distances, desired_perplexity, verbose) L34
"Compute joint probabilities p_ij from distances."
_joint_probabilities_nn(distances, desired_perplexity, verbose) L66
"Compute joint probabilities p_ij from distances using just nearest"
_kl_divergence(params, P, degrees_of_freedom, n_samples, n_components, skip_num_points=0, compute_error=True) L121
"t-SNE objective function: gradient of the KL divergence"
_kl_divergence_bh(params, P, degrees_of_freedom, n_samples, n_components, angle=0.5, skip_num_points=0, verbose=False, compute_error=True, num_threads=1) L193
"t-SNE objective function: KL divergence of p_ijs and q_ijs."
_gradient_descent(objective, p0, it, n_iter, n_iter_check=1, n_iter_without_progress=300, momentum=0.8, learning_rate=200.0, min_gain=0.01, min_grad_norm=1e-07, verbose=0, args=None, kwargs=None) L272
"Batch gradient descent with momentum and individual gains."
trustworthiness(X, X_embedded, *, n_neighbors=5, metric='euclidean') L402
"Expresses to what extent the local structure is retained."

sklearn/manifold/setup.py
configuration(parent_package='', top_path=None) L6

sklearn/metrics/_base.py
_average_binary_score(binary_metric, y_true, y_score, average, sample_weight=None) L23
"Average a binary metric for multilabel classification"
_average_multiclass_ovo_score(binary_metric, y_true, y_score, average='macro') L134
"Average one-versus-one scores for multiclass classification."

sklearn/metrics/_classification.py
_check_zero_division(zero_division) L45
_check_targets(y_true, y_pred) L54
"Check that y_true and y_pred belong to the same classification task"
_weighted_sum(sample_score, sample_weight, normalize=False) L116
accuracy_score(y_true, y_pred, *, normalize=True, sample_weight=None) L126
"Accuracy classification score."
confusion_matrix(y_true, y_pred, *, labels=None, sample_weight=None, normalize=None) L199
"Compute confusion matrix to evaluate the accuracy of a classification."
multilabel_confusion_matrix(y_true, y_pred, *, sample_weight=None, labels=None, samplewise=False) L343
"Compute a confusion matrix for each class or sample"
cohen_kappa_score(y1, y2, *, labels=None, weights=None, sample_weight=None) L547
"Cohen's kappa: a statistic that measures inter-annotator agreement."
jaccard_score(y_true, y_pred, *, labels=None, pos_label=1, average='binary', sample_weight=None) L629
"Jaccard similarity coefficient score"
matthews_corrcoef(y_true, y_pred, *, sample_weight=None) L769
"Compute the Matthews correlation coefficient (MCC)"
zero_one_loss(y_true, y_pred, *, normalize=True, sample_weight=None) L859
"Zero-one classification loss."
f1_score(y_true, y_pred, *, labels=None, pos_label=1, average='binary', sample_weight=None, zero_division='warn') L930
"Compute the F1 score, also known as balanced F-score or F-measure"
fbeta_score(y_true, y_pred, *, beta, labels=None, pos_label=1, average='binary', sample_weight=None, zero_division='warn') L1055
"Compute the F-beta score"
_prf_divide(numerator, denominator, metric, modifier, average, warn_for, zero_division='warn') L1183
"Performs division and handles divide-by-zero."
_warn_prf(average, modifier, msg_start, result_size) L1230
_check_set_wise_labels(y_true, y_pred, average, labels, pos_label) L1244
"Validation associated with set-wise metrics"
precision_recall_fscore_support(y_true, y_pred, *, beta=1.0, labels=None, pos_label=1, average=None, warn_for=('precision', 'recall', 'f-score'), sample_weight=None, zero_division='warn') L1279
"Compute precision, recall, F-measure and support for each class"
precision_score(y_true, y_pred, *, labels=None, pos_label=1, average='binary', sample_weight=None, zero_division='warn') L1512
"Compute the precision"
recall_score(y_true, y_pred, *, labels=None, pos_label=1, average='binary', sample_weight=None, zero_division='warn') L1632
"Compute the recall"
balanced_accuracy_score(y_true, y_pred, *, sample_weight=None, adjusted=False) L1750
"Compute the balanced accuracy"
classification_report(y_true, y_pred, *, labels=None, target_names=None, sample_weight=None, digits=2, output_dict=False, zero_division='warn') L1830
"Build a text report showing the main classification metrics."
hamming_loss(y_true, y_pred, *, sample_weight=None) L2031
"Compute the average Hamming loss."
log_loss(y_true, y_pred, *, eps=1e-15, normalize=True, sample_weight=None, labels=None) L2123
"Log loss, aka logistic loss or cross-entropy loss."
hinge_loss(y_true, pred_decision, *, labels=None, sample_weight=None) L2250
"Average hinge loss (non-regularized)"
brier_score_loss(y_true, y_prob, *, sample_weight=None, pos_label=None) L2372
"Compute the Brier score."

sklearn/metrics/_plot/base.py
_check_classifier_response_method(estimator, response_method) L6
"Return prediction method from the response_method"
_get_response(X, estimator, response_method, pos_label=None) L48
"Return response and positive label."

sklearn/metrics/_plot/confusion_matrix.py
class ConfusionMatrixDisplay L11
  "Confusion Matrix visualization."
  __init__(self, confusion_matrix, *, display_labels=None) L62
  plot(self, *, include_values=True, cmap='viridis', xticks_rotation='horizontal', values_format=None, ax=None, colorbar=True) L67
  "Plot visualization."

plot_confusion_matrix(estimator, X, y_true, *, labels=None, sample_weight=None, normalize=None, display_labels=None, include_values=True, xticks_rotation='horizontal', values_format=None, cmap='viridis', ax=None, colorbar=True) L158
"Plot Confusion Matrix."

sklearn/metrics/_plot/precision_recall_curve.py
class PrecisionRecallDisplay L10
  "Precision Recall visualization."
  __init__(self, precision, recall, *, average_precision=None, estimator_name=None, pos_label=None) L68
  plot(self, ax=None, *, name=None, **kwargs) L77
  "Plot visualization."

plot_precision_recall_curve(estimator, X, y, *, sample_weight=None, response_method='auto', name=None, ax=None, pos_label=None, **kwargs) L137
"Plot Precision Recall Curve for binary classifiers."

sklearn/metrics/_plot/roc_curve.py
class RocCurveDisplay L10
  "ROC Curve visualization."
  __init__(self, *, fpr, tpr, roc_auc=None, estimator_name=None, pos_label=None) L65
  plot(self, ax=None, *, name=None, **kwargs) L74
  "Plot visualization"

plot_roc_curve(estimator, X, y, *, sample_weight=None, drop_intermediate=True, response_method='auto', name=None, ax=None, pos_label=None, **kwargs) L130
"Plot Receiver operating characteristic (ROC) curve."

sklearn/metrics/_ranking.py
auc(x, y) L42
"Compute Area Under the Curve (AUC) using the trapezoidal rule"
average_precision_score(y_true, y_score, *, average='macro', pos_label=1, sample_weight=None) L106
"Compute average precision (AP) from prediction scores"
_binary_uninterpolated_average_precision(y_true, y_score, pos_label=1, sample_weight=None) L196
_binary_roc_auc_score(y_true, y_score, sample_weight=None, max_fpr=None) L221
"Binary roc auc score"
roc_auc_score(y_true, y_score, *, average='macro', sample_weight=None, max_fpr=None, multi_class='raise', labels=None) L250
"Compute Area Under the Receiver Operating Characteristic Curve (ROC AUC)"
_multiclass_roc_auc_score(y_true, y_score, labels, multi_class, average, sample_weight) L404
"Multiclass roc auc score"
_binary_clf_curve(y_true, y_score, pos_label=None, sample_weight=None) L504
"Calculate true and false positives per binary classification threshold."
precision_recall_curve(y_true, probas_pred, *, pos_label=None, sample_weight=None) L604
"Compute precision-recall pairs for different probability thresholds"
roc_curve(y_true, y_score, *, pos_label=None, sample_weight=None, drop_intermediate=True) L697
"Compute Receiver operating characteristic (ROC)"
label_ranking_average_precision_score(y_true, y_score, *, sample_weight=None) L830
"Compute ranking-based average precision"
coverage_error(y_true, y_score, *, sample_weight=None) L920
"Coverage error measure"
label_ranking_loss(y_true, y_score, *, sample_weight=None) L980
"Compute Ranking loss measure"
_dcg_sample_scores(y_true, y_score, k=None, log_base=2, ignore_ties=False) L1065
"Compute Discounted Cumulative Gain."
_tie_averaged_dcg(y_true, y_score, discount_cumsum) L1126
"Compute DCG by averaging over possible permutations of ties."
_check_dcg_target_type(y_true) L1175
dcg_score(y_true, y_score, *, k=None, log_base=2, sample_weight=None, ignore_ties=False) L1186
"Compute Discounted Cumulative Gain."
_ndcg_sample_scores(y_true, y_score, k=None, ignore_ties=False) L1292
"Compute Normalized Discounted Cumulative Gain."
ndcg_score(y_true, y_score, *, k=None, sample_weight=None, ignore_ties=False) L1344
"Compute Normalized Discounted Cumulative Gain."

sklearn/metrics/_regression.py
_check_reg_targets(y_true, y_pred, multioutput, dtype='numeric') L54
"Check that y_true and y_pred belong to the same regression task"
mean_absolute_error(y_true, y_pred, *, sample_weight=None, multioutput='uniform_average') L126
"Mean absolute error regression loss"
mean_absolute_percentage_error(y_true, y_pred, sample_weight=None, multioutput='uniform_average') L197
"Mean absolute percentage error regression loss"
mean_squared_error(y_true, y_pred, *, sample_weight=None, multioutput='uniform_average', squared=True) L273
"Mean squared error regression loss"
mean_squared_log_error(y_true, y_pred, *, sample_weight=None, multioutput='uniform_average') L354
"Mean squared logarithmic error regression loss"
median_absolute_error(y_true, y_pred, *, multioutput='uniform_average', sample_weight=None) L422
"Median absolute error regression loss"
explained_variance_score(y_true, y_pred, *, sample_weight=None, multioutput='uniform_average') L497
"Explained variance regression score function"
r2_score(y_true, y_pred, *, sample_weight=None, multioutput='uniform_average') L590
"R^2 (coefficient of determination) regression score function."
max_error(y_true, y_pred) L728
"max_error metric calculates the maximum residual error."
mean_tweedie_deviance(y_true, y_pred, *, sample_weight=None, power=0) L762
"Mean Tweedie deviance regression loss."
mean_poisson_deviance(y_true, y_pred, *, sample_weight=None) L827
"Mean Poisson deviance regression loss."
mean_gamma_deviance(y_true, y_pred, *, sample_weight=None) L865
"Mean Gamma deviance regression loss."

sklearn/metrics/_scorer.py
class _MultimetricScorer L62
  "Callable for multimetric scoring used to avoid repeated calls"
  __init__(self, **scorers) L75
  __call__(self, estimator, *args, **kwargs) L78
  "Evaluate predicted target values."
  _use_cache(self, estimator) L93
  "Return True if using a cache is beneficial."

class _BaseScorer L124
  __init__(self, score_func, sign, kwargs) L125
  __repr__(self) L130
  __call__(self, estimator, X, y_true, sample_weight=None) L138
  "Evaluate predicted target values for X relative to y_true."
  _factory_args(self) L164
  "Return non-default make_scorer arguments for repr."

class _PredictScorer(_BaseScorer) L169
  _score(self, method_caller, estimator, X, y_true, sample_weight=None) L170
  "Evaluate predicted target values for X relative to y_true."

class _ProbaScorer(_BaseScorer) L208
  _score(self, method_caller, clf, X, y, sample_weight=None) L209
  "Evaluate predicted probabilities for X relative to y_true."
  _factory_args(self) L255

class _ThresholdScorer(_BaseScorer) L259
  _score(self, method_caller, clf, X, y, sample_weight=None) L260
  "Evaluate decision function output for X relative to y_true."
  _factory_args(self) L327

_cached_call(cache, estimator, method, *args, **kwargs) L49
"Call estimator with method and args and kwargs."
get_scorer(scoring) L331
"Get a scorer from string."
_passthrough_scorer(estimator, *args, **kwargs) L358
"Function that wraps estimator.score"
check_scoring(estimator, scoring=None, *, allow_none=False) L364
"Determine scorer from user options."
_check_multimetric_scoring(estimator, scoring=None) L426
"Check the scoring parameter in cases when multiple metrics are allowed"
make_scorer(score_func, *, greater_is_better=True, needs_proba=False, needs_threshold=False, **kwargs) L522
"Make a scorer from a performance metric or loss function."

sklearn/metrics/cluster/_bicluster.py
_check_rows_and_columns(a, b) L10
"Unpacks the row and column arrays and checks their shape."
_jaccard(a_rows, a_cols, b_rows, b_cols) L20
"Jaccard coefficient on the elements of the two biclusters."
_pairwise_similarity(a, b, similarity) L31
"Computes pairwise similarity matrix."
consensus_score(a, b, *, similarity='jaccard') L49
"The similarity of two sets of biclusters."

sklearn/metrics/cluster/_supervised.py
_comb2(n) L30
check_clusterings(labels_true, labels_pred) L36
"Check that the labels arrays are 1D and of same dimension."
_generalized_average(U, V, average_method) L66
"Return a particular mean of two numbers."
contingency_matrix(labels_true, labels_pred, *, eps=None, sparse=False) L82
"Build a contingency matrix describing the relationship between labels."
adjusted_rand_score(labels_true, labels_pred) L141
"Rand index adjusted for chance."
homogeneity_completeness_v_measure(labels_true, labels_pred, *, beta=1.0) L247
"Compute the homogeneity and completeness and V-Measure scores at once."
homogeneity_score(labels_true, labels_pred) L329
"Homogeneity metric of a cluster labeling given a ground truth."
completeness_score(labels_true, labels_pred) L399
"Completeness metric of a cluster labeling given a ground truth."
v_measure_score(labels_true, labels_pred, *, beta=1.0) L470
"V-measure cluster labeling given a ground truth."
mutual_info_score(labels_true, labels_pred, *, contingency=None) L571
"Mutual Information between two clusterings."
adjusted_mutual_info_score(labels_true, labels_pred, *, average_method='arithmetic') L658
"Adjusted Mutual Information between two clusterings."
normalized_mutual_info_score(labels_true, labels_pred, *, average_method='arithmetic') L780
"Normalized Mutual Information between two clusterings."
fowlkes_mallows_score(labels_true, labels_pred, *, sparse=False) L881
"Measure the similarity of two clusterings of a set of points."
entropy(labels) L960
"Calculates the entropy for a labeling."

sklearn/metrics/cluster/_unsupervised.py
check_number_of_labels(n_labels, n_samples) L22
"Check that number of labels are valid."
silhouette_score(X, labels, *, metric='euclidean', sample_size=None, random_state=None, **kwds) L39
"Compute the mean Silhouette Coefficient of all samples."
_silhouette_reduce(D_chunk, start, labels, label_freqs) L120
"Accumulate silhouette statistics for vertical chunk of X"
silhouette_samples(X, labels, *, metric='euclidean', **kwds) L153
"Compute the Silhouette Coefficient for each sample."
calinski_harabasz_score(X, labels) L251
"Compute the Calinski and Harabasz score."
davies_bouldin_score(X, labels) L303
"Computes the Davies-Bouldin score."

sklearn/metrics/cluster/setup.py
configuration(parent_package='', top_path=None) L7

sklearn/metrics/pairwise.py
_return_float_dtype(X, Y) L38
"1."
check_pairwise_arrays(X, Y, *, precomputed=False, dtype=None, accept_sparse='csr', force_all_finite=True, copy=False) L63
"Set X and Y appropriately and checks inputs"
check_paired_arrays(X, Y) L165
"Set X and Y appropriately and checks inputs for paired distances"
euclidean_distances(X, Y=None, *, Y_norm_squared=None, squared=False, X_norm_squared=None) L201
"Considering the rows of X (and Y=X) as vectors, compute the"
nan_euclidean_distances(X, Y=None, *, squared=False, missing_values=np.nan, copy=True) L324
"Calculate the euclidean distances in the presence of missing values."
_euclidean_distances_upcast(X, XX=None, Y=None, YY=None, batch_size=None) L439
"Euclidean distances between X and Y"
_argmin_min_reduce(dist, start) L508
pairwise_distances_argmin_min(X, Y, *, axis=1, metric='euclidean', metric_kwargs=None) L515
"Compute minimum distances between one point and a set of points."
pairwise_distances_argmin(X, Y, *, axis=1, metric='euclidean', metric_kwargs=None) L602
"Compute minimum distances between one point and a set of points."
haversine_distances(X, Y=None) L676
"Compute the Haversine distance between samples in X and Y"
manhattan_distances(X, Y=None, *, sum_over_features=True) L725
"Compute the L1 distances between the vectors in X and Y."
cosine_distances(X, Y=None) L806
"Compute cosine distance between samples in X and Y."
paired_euclidean_distances(X, Y) L846
"Computes the paired euclidean distances between X and Y"
paired_manhattan_distances(X, Y) L866
"Compute the L1 distances between the vectors in X and Y."
paired_cosine_distances(X, Y) L890
"Computes the paired cosine distances between X and Y"
paired_distances(X, Y, *, metric='euclidean', **kwds) L925
"Computes the paired distances between X and Y."
linear_kernel(X, Y=None, dense_output=True) L983
"Compute the linear kernel between X and Y."
polynomial_kernel(X, Y=None, degree=3, gamma=None, coef0=1) L1009
"Compute the polynomial kernel between X and Y::"
sigmoid_kernel(X, Y=None, gamma=None, coef0=1) L1045
"Compute the sigmoid kernel between X and Y::"
rbf_kernel(X, Y=None, gamma=None) L1079
"Compute the rbf (gaussian) kernel between X and Y::"
laplacian_kernel(X, Y=None, gamma=None) L1112
"Compute the laplacian kernel between X and Y."
cosine_similarity(X, Y=None, dense_output=True) L1146
"Compute cosine similarity between samples in X and Y."
additive_chi2_kernel(X, Y=None) L1196
"Computes the additive chi-squared kernel between observations in X and Y"
chi2_kernel(X, Y=None, gamma=1.0) L1257
"Computes the exponential chi-squared kernel X and Y."
distance_metrics() L1321
"Valid metrics for pairwise_distances."
_dist_wrapper(dist_func, dist_matrix, slice_, *args, **kwargs) L1349
"Write in-place to a slice of a distance matrix"
_parallel_pairwise(X, Y, func, n_jobs, **kwds) L1354
"Break the pairwise matrix in n_jobs even slices"
_pairwise_callable(X, Y, metric, force_all_finite=True, **kwds) L1380
"Handle the callable case for pairwise_{distances,kernels}"
_check_chunk_size(reduced, chunk_size) L1423
"Checks chunk is a sequence of expected size or a tuple of same"
_precompute_metric_params(X, Y, metric=None, **kwds) L1444
"Precompute data-derived metric parameters if not provided"
pairwise_distances_chunked(X, Y=None, *, reduce_func=None, metric='euclidean', n_jobs=None, working_memory=None, **kwds) L1469
"Generate a distance matrix chunk by chunk with optional reduction"
pairwise_distances(X, Y=None, metric='euclidean', *, n_jobs=None, force_all_finite=True, **kwds) L1632
"Compute the distance matrix from a vector array X and optional Y."
kernel_metrics() L1810
"Valid metrics for pairwise_kernels"
pairwise_kernels(X, Y=None, metric='linear', *, filter_params=False, n_jobs=None, **kwds) L1851
"Compute the kernel between arrays X and optional array Y."

sklearn/metrics/setup.py
configuration(parent_package='', top_path=None) L6

sklearn/mixture/_base.py
class BaseMixture(DensityMixin, BaseEstimator) L65
  "Base class for mixture models."
  __init__(self, n_components, tol, reg_covar, max_iter, n_init, init_params, random_state, warm_start, verbose, verbose_interval) L72
  _check_initial_parameters(self, X) L86
  "Check values of the basic parameters."
  _check_parameters(self, X) L123
  "Check initial parameters of the derived class."
  _initialize_parameters(self, X, random_state) L132
  "Initialize the model parameters."
  _initialize(self, X, resp) L160
  "Initialize the model parameters of the derived class."
  fit(self, X, y=None) L171
  "Estimate model parameters with the EM algorithm."
  fit_predict(self, X, y=None) L196
  "Estimate model parameters using X and predict the labels for X."
  _e_step(self, X) L282
  "E step."
  _m_step(self, X, log_resp) L302
  "M step."
  _get_parameters(self) L316
  _set_parameters(self, params) L320
  score_samples(self, X) L323
  "Compute the weighted log probabilities for each sample."
  score(self, X, y=None) L342
  "Compute the per-sample average log-likelihood of the given data X."
  predict(self, X) L358
  "Predict the labels for the data samples in X using trained model."
  predict_proba(self, X) L376
  "Predict posterior probability of each component given the data."
  sample(self, n_samples=1) L396
  "Generate random samples from the fitted Gaussian distribution."
  _estimate_weighted_log_prob(self, X) L445
  "Estimate the weighted log-probabilities, log P(X | Z) + log weights."
  _estimate_log_weights(self) L459
  "Estimate log-weights in EM algorithm, E[ log pi ] in VB algorithm."
  _estimate_log_prob(self, X) L469
  "Estimate the log-probabilities log P(X | Z)."
  _estimate_log_prob_resp(self, X) L484
  "Estimate log probabilities and responsibilities for each sample."
  _print_verbose_msg_init_beg(self, n_init) L510
  "Print verbose message on initialization."
  _print_verbose_msg_iter_end(self, n_iter, diff_ll) L519
  "Print verbose message on initialization."
  _print_verbose_msg_init_end(self, ll) L530
  "Print verbose message on the end of iteration."

_check_shape(param, param_shape, name) L22
"Validate the shape of the input parameter 'param'."
_check_X(X, n_components=None, n_features=None, ensure_min_samples=1) L39
"Check the input data X."

sklearn/mixture/_bayesian_mixture.py
class BayesianGaussianMixture(BaseMixture) L65
  "Variational Bayesian estimation of a Gaussian mixture."
  __init__(self, *, n_components=1, covariance_type='full', tol=0.001, reg_covar=1e-06, max_iter=100, n_init=1, init_params='kmeans', weight_concentration_prior_type='dirichlet_process', weight_concentration_prior=None, mean_precision_prior=None, mean_prior=None, degrees_of_freedom_prior=None, covariance_prior=None, random_state=None, warm_start=False, verbose=0, verbose_interval=10) L324
  _check_parameters(self, X) L346
  "Check that the parameters are well defined."
  _check_weights_parameters(self) L372
  "Check the parameter of the Dirichlet distribution."
  _check_means_parameters(self, X) L384
  "Check the parameters of the Gaussian distribution."
  _check_precision_parameters(self, X) L410
  "Check the prior parameters of the precision distribution."
  _checkcovariance_prior_parameter(self, X) L428
  "Check the `covariance_prior_`."
  _initialize(self, X, resp) L469
  "Initialization of the mixture parameters."
  _estimate_weights(self, nk) L485
  "Estimate the parameters of the Dirichlet distribution."
  _estimate_means(self, nk, xk) L503
  "Estimate the parameters of the Gaussian distribution."
  _estimate_precisions(self, nk, xk, sk) L517
  "Estimate the precisions parameters of the precision distribution."
  _estimate_wishart_full(self, nk, xk, sk) L542
  "Estimate the full Wishart distribution parameters."
  _estimate_wishart_tied(self, nk, xk, sk) L576
  "Estimate the tied Wishart distribution parameters."
  _estimate_wishart_diag(self, nk, xk, sk) L606
  "Estimate the diag Wishart distribution parameters."
  _estimate_wishart_spherical(self, nk, xk, sk) L635
  "Estimate the spherical Wishart distribution parameters."
  _m_step(self, X, log_resp) L664
  "M step."
  _estimate_log_weights(self) L683
  _estimate_log_prob(self, X) L696
  _compute_lower_bound(self, log_resp, log_prob_norm) L711
  "Estimate the lower bound of the model."
  _get_parameters(self) L760
  _set_parameters(self, params) L766

_log_dirichlet_norm(dirichlet_concentration) L21
"Compute the log of the Dirichlet distribution normalization term."
_log_wishart_norm(degrees_of_freedom, log_det_precisions_chol, n_features) L38
"Compute the log of the Wishart distribution normalization term."

sklearn/mixture/_gaussian_mixture.py
class GaussianMixture(BaseMixture) L434
  "Gaussian Mixture."
  __init__(self, n_components=1, *, covariance_type='full', tol=0.001, reg_covar=1e-06, max_iter=100, n_init=1, init_params='kmeans', weights_init=None, means_init=None, precisions_init=None, random_state=None, warm_start=False, verbose=0, verbose_interval=10) L602
  _check_parameters(self, X) L618
  "Check the Gaussian mixture parameters are well defined."
  _initialize(self, X, resp) L641
  "Initialization of the Gaussian mixture parameters."
  _m_step(self, X, log_resp) L674
  "M step."
  _estimate_log_prob(self, X) L693
  _estimate_log_weights(self) L697
  _compute_lower_bound(self, _, log_prob_norm) L700
  _get_parameters(self) L703
  _set_parameters(self, params) L707
  _n_parameters(self) L725
  "Return the number of free parameters in the model."
  bic(self, X) L739
  "Bayesian information criterion for the current model on the input X."
  aic(self, X) L754
  "Akaike information criterion for the current model on the input X."

_check_weights(weights, n_components) L20
"Check the user provided 'weights'."
_check_means(means, n_components, n_features) L53
"Validate the provided 'means'."
_check_precision_positivity(precision, covariance_type) L76
"Check a precision vector is positive-definite."
_check_precision_matrix(precision, covariance_type) L83
"Check a precision matrix is symmetric and positive-definite."
_check_precisions_full(precisions, covariance_type) L91
"Check the precision matrices are symmetric and positive-definite."
_check_precisions(precisions, covariance_type, n_components, n_features) L97
"Validate user provided precisions."
_estimate_gaussian_covariances_full(resp, X, nk, means, reg_covar) L142
"Estimate the full covariance matrices."
_estimate_gaussian_covariances_tied(resp, X, nk, means, reg_covar) L171
"Estimate the tied covariance matrix."
_estimate_gaussian_covariances_diag(resp, X, nk, means, reg_covar) L199
"Estimate the diagonal covariance vectors."
_estimate_gaussian_covariances_spherical(resp, X, nk, means, reg_covar) L225
"Estimate the spherical variance values."
_estimate_gaussian_parameters(X, resp, reg_covar, covariance_type) L249
"Estimate the Gaussian distribution parameters."
_compute_precision_cholesky(covariances, covariance_type) L288
"Compute the Cholesky decomposition of the precisions."
_compute_log_det_cholesky(matrix_chol, covariance_type, n_features) L340
"Compute the log-det of the cholesky decomposition of matrices."
_estimate_log_gaussian_prob(X, means, precisions_chol, covariance_type) L380
"Estimate the log Gaussian probability."

sklearn/model_selection/_search.py
class ParameterGrid L47
  "Grid of parameters with a discrete number of values for each."
  __init__(self, param_grid) L92
  __iter__(self) L115
  "Iterate over the points in the grid."
  __len__(self) L135
  "Number of points on the grid."
  __getitem__(self, ind) L142
  "Get the parameters that would be ``ind``th in iteration"

class ParameterSampler L184
  "Generator on parameters sampled from given distributions."
  __init__(self, param_distributions, n_iter, *, random_state=None) L240
  __iter__(self) L264
  __len__(self) L302
  "Number of points that will be sampled."

class BaseSearchCV(MetaEstimatorMixin, BaseEstimator) L400
  "Abstract base class for hyper parameter search with cross-validation."
  __init__(self, estimator, *, scoring=None, n_jobs=None, refit=True, cv=None, verbose=0, pre_dispatch='2*n_jobs', error_score=np.nan, return_train_score=True) L406
  _estimator_type(self) L422
  _pairwise(self) L426
  score(self, X, y=None) L430
  "Returns the score on the given data, if the estimator has been refit."
  score_samples(self, X) L460
  "Call score_samples on the estimator with the best found parameters."
  _check_is_fitted(self, method_name) L479
  predict(self, X) L492
  "Call predict on the estimator with the best found parameters."
  predict_proba(self, X) L509
  "Call predict_proba on the estimator with the best found parameters."
  predict_log_proba(self, X) L526
  "Call predict_log_proba on the estimator with the best found parameters."
  decision_function(self, X) L543
  "Call decision_function on the estimator with the best found parameters."
  transform(self, X) L560
  "Call transform on the estimator with the best found parameters."
  inverse_transform(self, Xt) L577
  "Call inverse_transform on the estimator with the best found params."
  n_features_in_(self) L594
  classes_(self) L608
  _run_search(self, evaluate_candidates) L612
  "Repeatedly calls `evaluate_candidates` to conduct a search."
  fit(self, X, y=None, *, groups=None, **fit_params) L647
  "Run fit with all sets of parameters."
  evaluate_candidates(candidate_params) L718
  _format_results(self, candidate_params, scorers, n_splits, out) L805
  _store(key_name, array, weights=None, splits=False, rank=False) L811
  "A small helper to store the scores/times to the cv_results_"

class GridSearchCV(BaseSearchCV) L871
  "Exhaustive search over specified parameter values for an estimator."
  __init__(self, estimator, param_grid, *, scoring=None, n_jobs=None, refit=True, cv=None, verbose=0, pre_dispatch='2*n_jobs', error_score=np.nan, return_train_score=False) L1175
  _run_search(self, evaluate_candidates) L1187
  "Search all candidates in param_grid"

class RandomizedSearchCV(BaseSearchCV) L1192
  "Randomized search on hyper parameters."
  __init__(self, estimator, param_distributions, *, n_iter=10, scoring=None, n_jobs=None, refit=True, cv=None, verbose=0, pre_dispatch='2*n_jobs', random_state=None, error_score=np.nan, return_train_score=False) L1505
  _run_search(self, evaluate_candidates) L1519
  "Search n_iter candidates from param_distributions"

fit_grid_point(X, y, estimator, parameters, train, test, scorer, verbose, error_score=np.nan, **fit_params) L312
"Run fit on one set of parameters."
_check_param_grid(param_grid) L379

sklearn/model_selection/_split.py
class BaseCrossValidator L49
  "Base class for all cross-validators"
  split(self, X, y=None, groups=None) L54
  "Generate indices to split data into training and test set."
  _iter_test_masks(self, X=None, y=None, groups=None) L87
  "Generates boolean masks corresponding to test sets."
  _iter_test_indices(self, X=None, y=None, groups=None) L97
  "Generates integer indices corresponding to test sets."
  get_n_splits(self, X=None, y=None, groups=None) L102
  "Returns the number of splitting iterations in the cross-validator"
  __repr__(self) L105

class LeaveOneOut(BaseCrossValidator) L109
  "Leave-One-Out cross-validator"
  _iter_test_indices(self, X, y=None, groups=None) L156
  get_n_splits(self, X, y=None, groups=None) L165
  "Returns the number of splitting iterations in the cross-validator"

class LeavePOut(BaseCrossValidator) L190
  "Leave-P-Out cross-validator"
  __init__(self, p) L236
  _iter_test_indices(self, X, y=None, groups=None) L239
  get_n_splits(self, X, y=None, groups=None) L249
  "Returns the number of splitting iterations in the cross-validator"

class _BaseKFold(BaseCrossValidator) L269
  "Base class for KFold, GroupKFold, and StratifiedKFold"
  __init__(self, n_splits, *, shuffle, random_state) L274
  split(self, X, y=None, groups=None) L302
  "Generate indices to split data into training and test set."
  get_n_splits(self, X=None, y=None, groups=None) L337
  "Returns the number of splitting iterations in the cross-validator"

class KFold(_BaseKFold) L359
  "K-Folds cross-validator"
  __init__(self, n_splits=5, *, shuffle=False, random_state=None) L429
  _iter_test_indices(self, X, y=None, groups=None) L434

class GroupKFold(_BaseKFold) L450
  "K-fold iterator variant with non-overlapping groups."
  __init__(self, n_splits=5) L500
  _iter_test_indices(self, X, y, groups) L503
  split(self, X, y=None, groups=None) L540
  "Generate indices to split data into training and test set."

class StratifiedKFold(_BaseKFold) L567
  "Stratified K-Folds cross-validator"
  __init__(self, n_splits=5, *, shuffle=False, random_state=None) L637
  _make_test_folds(self, X, y=None) L641
  _iter_test_masks(self, X, y=None, groups=None) L694
  split(self, X, y, groups=None) L699
  "Generate indices to split data into training and test set."

class TimeSeriesSplit(_BaseKFold) L737
  "Time Series cross-validator"
  __init__(self, n_splits=5, *, max_train_size=None, test_size=None, gap=0) L823
  split(self, X, y=None, groups=None) L834
  "Generate indices to split data into training and test set."

class LeaveOneGroupOut(BaseCrossValidator) L889
  "Leave One Group Out cross-validator"
  _iter_test_masks(self, X, y, groups) L931
  get_n_splits(self, X=None, y=None, groups=None) L944
  "Returns the number of splitting iterations in the cross-validator"
  split(self, X, y=None, groups=None) L971
  "Generate indices to split data into training and test set."

class LeavePGroupsOut(BaseCrossValidator) L998
  "Leave P Group(s) Out cross-validator"
  __init__(self, n_groups) L1054
  _iter_test_masks(self, X, y, groups) L1057
  get_n_splits(self, X=None, y=None, groups=None) L1075
  "Returns the number of splitting iterations in the cross-validator"
  split(self, X, y=None, groups=None) L1102
  "Generate indices to split data into training and test set."

class _RepeatedSplits L1129
  "Repeated splits for an arbitrary randomized CV splitter."
  __init__(self, cv, *, n_repeats=10, random_state=None, **cvargs) L1153
  split(self, X, y=None, groups=None) L1169
  "Generates indices to split data into training and test set."
  get_n_splits(self, X=None, y=None, groups=None) L1202
  "Returns the number of splitting iterations in the cross-validator"
  __repr__(self) L1229

class RepeatedKFold(_RepeatedSplits) L1233
  "Repeated K-Fold cross validator."
  __init__(self, *, n_splits=5, n_repeats=10, random_state=None) L1281

class RepeatedStratifiedKFold(_RepeatedSplits) L1287
  "Repeated Stratified K-Fold cross validator."
  __init__(self, *, n_splits=5, n_repeats=10, random_state=None) L1337

class BaseShuffleSplit L1343
  "Base class for ShuffleSplit and StratifiedShuffleSplit"
  __init__(self, n_splits=10, *, test_size=None, train_size=None, random_state=None) L1346
  split(self, X, y=None, groups=None) L1354
  "Generate indices to split data into training and test set."
  _iter_indices(self, X, y=None, groups=None) L1389
  "Generate (train, test) indices"
  get_n_splits(self, X=None, y=None, groups=None) L1392
  "Returns the number of splitting iterations in the cross-validator"
  __repr__(self) L1413

class ShuffleSplit(BaseShuffleSplit) L1417
  "Random permutation cross-validator"
  __init__(self, n_splits=10, *, test_size=None, train_size=None, random_state=None) L1480
  _iter_indices(self, X, y=None, groups=None) L1489

class GroupShuffleSplit(ShuffleSplit) L1504
  "Shuffle-Group(s)-Out cross-validation iterator"
  __init__(self, n_splits=5, *, test_size=None, train_size=None, random_state=None) L1570
  _iter_indices(self, X, y, groups) L1579
  split(self, X, y=None, groups=None) L1593
  "Generate indices to split data into training and test set."

class StratifiedShuffleSplit(BaseShuffleSplit) L1626
  "Stratified ShuffleSplit cross-validator"
  __init__(self, n_splits=10, *, test_size=None, train_size=None, random_state=None) L1686
  _iter_indices(self, X, y, groups=None) L1695
  split(self, X, y, groups=None) L1756
  "Generate indices to split data into training and test set."

class PredefinedSplit(BaseCrossValidator) L1864
  "Predefined split cross-validator"
  __init__(self, test_fold) L1902
  split(self, X=None, y=None, groups=None) L1908
  "Generate indices to split data into training and test set."
  _iter_test_masks(self) L1936
  "Generates boolean masks corresponding to test sets."
  get_n_splits(self, X=None, y=None, groups=None) L1944
  "Returns the number of splitting iterations in the cross-validator"

class _CVIterableWrapper(BaseCrossValidator) L1966
  "Wrapper class for old style cv objects and iterables."
  __init__(self, cv) L1968
  get_n_splits(self, X=None, y=None, groups=None) L1971
  "Returns the number of splitting iterations in the cross-validator"
  split(self, X=None, y=None, groups=None) L1992
  "Generate indices to split data into training and test set."

_validate_shuffle_split(n_samples, test_size, train_size, default_test_size=None) L1794
"Validation helper to check if the test/test sizes are meaningful wrt to the"
check_cv(cv=5, y=None, *, classifier=False) L2019
"Input checker utility for building a cross-validator"
train_test_split(*arrays, test_size=None, train_size=None, random_state=None, shuffle=True, stratify=None) L2073
"Split arrays or matrices into random train and test subsets"
_build_repr(self) L2205

sklearn/model_selection/_validation.py
cross_validate(estimator, X, y=None, *, groups=None, scoring=None, cv=None, n_jobs=None, verbose=0, fit_params=None, pre_dispatch='2*n_jobs', return_train_score=False, return_estimator=False, error_score=np.nan) L41
"Evaluate metric(s) by cross-validation and also record fit/score times."
cross_val_score(estimator, X, y=None, *, groups=None, scoring=None, cv=None, n_jobs=None, verbose=0, fit_params=None, pre_dispatch='2*n_jobs', error_score=np.nan) L275
"Evaluate a score by cross-validation"
_fit_and_score(estimator, X, y, scorer, train, test, verbose, parameters, fit_params, return_train_score=False, return_parameters=False, return_n_test_samples=False, return_times=False, return_estimator=False, split_progress=None, candidate_progress=None, error_score=np.nan) L410
"Fit estimator and compute scores for a given dataset split."
_score(estimator, X_test, y_test, scorer) L612
"Compute the score(s) of an estimator on a given test set."
cross_val_predict(estimator, X, y=None, *, groups=None, cv=None, n_jobs=None, verbose=0, fit_params=None, pre_dispatch='2*n_jobs', method='predict') L648
"Generate cross-validated estimates for each input data point"
_fit_and_predict(estimator, X, y, train, test, verbose, fit_params, method) L826
"Fit estimator and predict values for a given dataset split."
_enforce_prediction_order(classes, predictions, n_classes, method) L901
"Ensure that prediction arrays have correct column order"
_check_is_permutation(indices, n_samples) L958
"Check whether indices is a reordering of the array np.arange(n_samples)"
permutation_test_score(estimator, X, y, *, groups=None, cv=None, n_permutations=100, n_jobs=None, random_state=0, verbose=0, scoring=None) L983
"Evaluate the significance of a cross-validated score with permutations"
_permutation_test_score(estimator, X, y, groups, cv, scorer) L1101
"Auxiliary function for permutation_test_score"
_shuffle(y, groups, random_state) L1112
"Return a shuffled copy of y eventually shuffle among same groups."
learning_curve(estimator, X, y, *, groups=None, train_sizes=np.linspace(0.1, 1.0, 5), cv=None, scoring=None, exploit_incremental_learning=False, n_jobs=None, pre_dispatch='all', verbose=0, shuffle=False, random_state=None, error_score=np.nan, return_times=False) L1125
"Learning curve."
_translate_train_sizes(train_sizes, n_max_training_samples) L1323
"Determine absolute sizes of training subsets and validate 'train_sizes'."
_incremental_fit_estimator(estimator, X, y, classes, train, test, train_sizes, scorer, verbose, return_times) L1382
"Train estimator on training subsets incrementally and compute scores."
validation_curve(estimator, X, y, *, param_name, param_range, groups=None, cv=None, scoring=None, n_jobs=None, pre_dispatch='all', verbose=0, error_score=np.nan) L1417
"Validation curve."
_aggregate_score_dicts(scores) L1538
"Aggregate the list of dict to dict of np ndarray"

sklearn/multiclass.py
class _ConstantPredictor(BaseEstimator) L111
  fit(self, X, y) L113
  predict(self, X) L117
  decision_function(self, X) L122
  predict_proba(self, X) L127

class OneVsRestClassifier(MultiOutputMixin, ClassifierMixin, MetaEstimatorMixin, BaseEstimator) L134
  "One-vs-the-rest (OvR) multiclass strategy."
  __init__(self, estimator, *, n_jobs=None) L218
  fit(self, X, y) L222
  "Fit underlying estimators."
  partial_fit(self, X, y, classes=None) L259
  "Partially fit underlying estimators"
  predict(self, X) L314
  "Predict multi-class targets using underlying estimators."
  predict_proba(self, X) L356
  "Probability estimates."
  decision_function(self, X) L395
  "Returns the distance of each sample from the decision boundary for"
  multilabel_(self) L419
  "Whether this is a multilabel classifier"
  n_classes_(self) L424
  coef_(self) L428
  intercept_(self) L439
  _pairwise(self) L447
  "Indicate if wrapped estimator is using a precomputed Gram matrix"
  _first_estimator(self) L452
  n_features_in_(self) L456

class OneVsOneClassifier(MetaEstimatorMixin, ClassifierMixin, BaseEstimator) L494
  "One-vs-one multiclass strategy"
  __init__(self, estimator, *, n_jobs=None) L553
  fit(self, X, y) L557
  "Fit underlying estimators."
  partial_fit(self, X, y, classes=None) L592
  "Partially fit underlying estimators"
  predict(self, X) L643
  "Estimate the best class label for each sample in X."
  decision_function(self, X) L665
  "Decision function for the OneVsOneClassifier."
  n_classes_(self) L704
  _pairwise(self) L708
  "Indicate if wrapped estimator is using a precomputed Gram matrix"

class OutputCodeClassifier(MetaEstimatorMixin, ClassifierMixin, BaseEstimator) L713
  "(Error-Correcting) Output-Code multiclass strategy"
  __init__(self, estimator, *, code_size=1.5, random_state=None, n_jobs=None) L796
  fit(self, X, y) L803
  "Fit underlying estimators."
  predict(self, X) L852
  "Predict multi-class targets using underlying estimators."

_fit_binary(estimator, X, y, classes=None) L67
"Fit a single binary estimator."
_partial_fit_binary(estimator, X, y) L85
"Partially fit a single binary estimator."
_predict_binary(estimator, X) L91
"Make predictions using a single binary estimator."
_check_estimator(estimator) L103
"Make sure that an estimator implements the necessary methods."
_fit_ovo_binary(estimator, X, y, i, j) L469
"Fit a single binary estimator (one-vs-one)."
_partial_fit_ovo_binary(estimator, X, y, i, j) L482
"Partially fit a single binary estimator(one-vs-one)."

sklearn/multioutput.py
class _MultiOutputEstimator(MetaEstimatorMixin, BaseEstimator) L63
  __init__(self, estimator, *, n_jobs=None) L68
  partial_fit(self, X, y, classes=None, sample_weight=None) L73
  "Incrementally fit the model to data."
  fit(self, X, y, sample_weight=None, **fit_params) L126
  "Fit the model to data."
  predict(self, X) L179
  "Predict multi-output variable using a model"
  _more_tags(self) L207

class MultiOutputRegressor(RegressorMixin, _MultiOutputEstimator) L211
  "Multi target regression"
  __init__(self, estimator, *, n_jobs=None) L255
  partial_fit(self, X, y, sample_weight=None) L259
  "Incrementally fit the model to data."

class MultiOutputClassifier(ClassifierMixin, _MultiOutputEstimator) L284
  "Multi target classification"
  __init__(self, estimator, *, n_jobs=None) L328
  fit(self, X, Y, sample_weight=None, **fit_params) L331
  "Fit the model to data matrix X and targets Y."
  predict_proba(self) L356
  "Probability estimates."
  _predict_proba(self, X) L387
  score(self, X, y) L392
  "Returns the mean accuracy on the given test data and labels."
  _more_tags(self) L420

class _BaseChain(BaseEstimator) L425
  __init__(self, base_estimator, *, order=None, cv=None, random_state=None) L427
  fit(self, X, Y, **fit_params) L435
  "Fit the model to data matrix X and targets Y."
  predict(self, X) L501
  "Predict on the data matrix X using the ClassifierChain model."

class ClassifierChain(MetaEstimatorMixin, ClassifierMixin, _BaseChain) L536
  "A multi-label model that arranges binary classifiers into a chain."
  fit(self, X, Y) L636
  "Fit the model to data matrix X and targets Y."
  predict_proba(self, X) L657
  "Predict probability estimates."
  decision_function(self, X) L686
  "Evaluate the decision_function of the models in the chain."
  _more_tags(self) L716

class RegressorChain(MetaEstimatorMixin, RegressorMixin, _BaseChain) L721
  "A multi-label model that arranges regressions into a chain."
  fit(self, X, Y, **fit_params) L802
  "Fit the model to data matrix X and targets Y."
  _more_tags(self) L823

_fit_estimator(estimator, X, y, sample_weight=None, **fit_params) L35
_partial_fit_estimator(estimator, X, y, classes=None, sample_weight=None, first_time=True) L44

sklearn/naive_bayes.py
class _BaseNB(ClassifierMixin, BaseEstimator) L42
  "Abstract base class for naive Bayes estimators"
  _joint_log_likelihood(self, X) L46
  "Compute the unnormalized posterior log probability of X"
  _check_X(self, X) L57
  "To be overridden in subclasses with the actual checks."
  predict(self, X) L60
  "Perform classification on an array of test vectors X."
  predict_log_proba(self, X) L78
  "Return log-probability estimates for the test vector X."
  predict_proba(self, X) L100
  "Return probability estimates for the test vector X."

class GaussianNB(_BaseNB) L118
  "Gaussian Naive Bayes (GaussianNB)"
  __init__(self, *, priors=None, var_smoothing=1e-09) L181
  fit(self, X, y, sample_weight=None) L185
  "Fit Gaussian Naive Bayes according to X, y"
  _check_X(self, X) L212
  _update_mean_variance(n_past, mu, var, X, sample_weight=None) L216
  "Compute online update of Gaussian mean and variance."
  partial_fit(self, X, y, classes=None, sample_weight=None) L289
  "Incremental fit on a batch of samples."
  _partial_fit(self, X, y, classes=None, _refit=False, sample_weight=None) L331
  "Actual implementation of Gaussian NB fitting."
  _joint_log_likelihood(self, X) L448

class _BaseDiscreteNB(_BaseNB) L464
  "Abstract base class for naive Bayes on discrete/categorical data"
  _check_X(self, X) L473
  _check_X_y(self, X, y) L476
  _update_class_log_prior(self, class_prior=None) L479
  _check_alpha(self) L499
  partial_fit(self, X, y, classes=None, sample_weight=None) L513
  "Incremental fit on a batch of samples."
  fit(self, X, y, sample_weight=None) L593
  "Fit Naive Bayes classifier according to X, y"
  _init_counters(self, n_effective_classes, n_features) L644
  coef_(self) L653
  intercept_(self) L661
  _more_tags(self) L665

class MultinomialNB(_BaseDiscreteNB) L669
  "Naive Bayes classifier for multinomial models"
  __init__(self, *, alpha=1.0, fit_prior=True, class_prior=None) L759
  _more_tags(self) L764
  _count(self, X, Y) L767
  "Count and smooth feature occurrences."
  _update_feature_log_prob(self, alpha) L773
  "Apply smoothing to raw counts and recompute log probabilities"
  _joint_log_likelihood(self, X) L781
  "Calculate the posterior log probability of the samples X"

class ComplementNB(_BaseDiscreteNB) L787
  "The Complement Naive Bayes classifier described in Rennie et al."
  __init__(self, *, alpha=1.0, fit_prior=True, class_prior=None, norm=False) L878
  _more_tags(self) L885
  _count(self, X, Y) L888
  "Count feature occurrences."
  _update_feature_log_prob(self, alpha) L895
  "Apply smoothing to raw counts and compute the weights."
  _joint_log_likelihood(self, X) L907
  "Calculate the class scores for the samples in X."

class BernoulliNB(_BaseDiscreteNB) L915
  "Naive Bayes classifier for multivariate Bernoulli models."
  __init__(self, *, alpha=1.0, binarize=0.0, fit_prior=True, class_prior=None) L1001
  _check_X(self, X) L1008
  _check_X_y(self, X, y) L1014
  _count(self, X, Y) L1020
  "Count and smooth feature occurrences."
  _update_feature_log_prob(self, alpha) L1025
  "Apply smoothing to raw counts and recompute log probabilities"
  _joint_log_likelihood(self, X) L1033
  "Calculate the posterior log probability of the samples X"

class CategoricalNB(_BaseDiscreteNB) L1050
  "Naive Bayes classifier for categorical features"
  __init__(self, *, alpha=1.0, fit_prior=True, class_prior=None) L1113
  fit(self, X, y, sample_weight=None) L1118
  "Fit Naive Bayes classifier according to X, y"
  partial_fit(self, X, y, classes=None, sample_weight=None) L1144
  "Incremental fit on a batch of samples."
  _more_tags(self) L1188
  _check_X(self, X) L1191
  _check_X_y(self, X, y) L1197
  _init_counters(self, n_effective_classes, n_features) L1203
  _count(self, X, Y) L1208
  _update_cat_count_dims(cat_count, highest_feature) L1209
  _update_cat_count(X_feature, Y, cat_count, n_classes) L1216
  _update_feature_log_prob(self, alpha) L1236
  _joint_log_likelihood(self, X) L1246

sklearn/neighbors/_base.py
class NeighborsBase(MultiOutputMixin, BaseEstimator) L289
  "Base class for nearest neighbors estimators."
  __init__(self, n_neighbors=None, radius=None, algorithm='auto', leaf_size=30, metric='minkowski', p=2, metric_params=None, n_jobs=None) L293
  _check_algorithm_metric(self) L307
  _fit(self, X) L350
  _pairwise(self) L482

class KNeighborsMixin L496
  "Mixin for k-neighbors searches"
  _kneighbors_reduce_func(self, dist, start, n_neighbors, return_distance) L499
  "Reduce a chunk of distances to the nearest neighbors"
  kneighbors(self, X=None, n_neighbors=None, return_distance=True) L534
  "Finds the K-neighbors of a point."
  kneighbors_graph(self, X=None, n_neighbors=None, mode='connectivity') L708
  "Computes the (weighted) graph of k-Neighbors for points in X"

class RadiusNeighborsMixin L794
  "Mixin for radius-based neighbors searches"
  _radius_neighbors_reduce_func(self, dist, start, radius, return_distance) L797
  "Reduce a chunk of distances to the nearest neighbors"
  radius_neighbors(self, X=None, radius=None, return_distance=True, sort_results=False) L831
  "Finds the neighbors within a given radius of a point or points."
  radius_neighbors_graph(self, X=None, radius=None, mode='connectivity', sort_results=False) L1005
  "Computes the (weighted) graph of Neighbors for points in X"

class SupervisedFloatMixin L1093
  fit(self, X, y) L1094
  "Fit the model using X as training data and y as target values"
  _more_tags(self) L1113

class SupervisedIntegerMixin L1117
  fit(self, X, y) L1118
  "Fit the model using X as training data and y as target values"
  _more_tags(self) L1160

class UnsupervisedMixin L1164
  fit(self, X, y=None) L1165
  "Fit the model using X as training data"

_check_weights(weights) L53
"Check to make sure weights are valid"
_get_weights(dist, weights) L64
"Get the weights from an array of distances and a parameter ``weights``"
_is_sorted_by_data(graph) L108
"Returns whether the graph's non-zero entries are sorted by data"
_check_precomputed(X) L134
"Check precomputed distance matrix"
_kneighbors_from_graph(graph, n_neighbors, return_distance) L192
"Decompose a nearest neighbors sparse graph into distances and indices"
extract(a) L227
_radius_neighbors_from_graph(graph, radius, return_distance) L242
"Decompose a nearest neighbors sparse graph into distances and indices"
_tree_query_parallel_helper(tree, *args, **kwargs) L487
"Helper for the Parallel calls in KNeighborsMixin.kneighbors"
_tree_query_radius_parallel_helper(tree, *args, **kwargs) L785
"Helper for the Parallel calls in RadiusNeighborsMixin.radius_neighbors"

sklearn/neighbors/_classification.py
class KNeighborsClassifier(NeighborsBase, KNeighborsMixin, SupervisedIntegerMixin, ClassifierMixin) L26
  "Classifier implementing the k-nearest neighbors vote."
  __init__(self, n_neighbors=5, *, weights='uniform', algorithm='auto', leaf_size=30, p=2, metric='minkowski', metric_params=None, n_jobs=None, **kwargs) L150
  predict(self, X) L162
  "Predict the class labels for the provided data."
  predict_proba(self, X) L204
  "Return probability estimates for the test data X."

class RadiusNeighborsClassifier(NeighborsBase, RadiusNeighborsMixin, SupervisedIntegerMixin, ClassifierMixin) L259
  "Classifier implementing a vote among neighbors within a given radius"
  __init__(self, radius=1.0, *, weights='uniform', algorithm='auto', leaf_size=30, p=2, metric='minkowski', outlier_label=None, metric_params=None, n_jobs=None, **kwargs) L390
  fit(self, X, y) L403
  "Fit the model using X as training data and y as target values"
  predict(self, X) L466
  "Predict the class labels for the provided data."
  predict_proba(self, X) L508
  "Return probability estimates for the test data X."

sklearn/neighbors/_graph.py
class KNeighborsTransformer(KNeighborsMixin, UnsupervisedMixin, TransformerMixin, NeighborsBase) L198
  "Transform X into a (weighted) graph of k nearest neighbors"
  __init__(self, *, mode='distance', n_neighbors=5, algorithm='auto', leaf_size=30, metric='minkowski', p=2, metric_params=None, n_jobs=1) L304
  transform(self, X) L313
  "Computes the (weighted) graph of Neighbors for points in X"
  fit_transform(self, X, y=None) L334
  "Fit to data, then transform it."

class RadiusNeighborsTransformer(RadiusNeighborsMixin, UnsupervisedMixin, TransformerMixin, NeighborsBase) L358
  "Transform X into a (weighted) graph of neighbors nearer than a radius"
  __init__(self, *, mode='distance', radius=1.0, algorithm='auto', leaf_size=30, metric='minkowski', p=2, metric_params=None, n_jobs=1) L462
  transform(self, X) L471
  "Computes the (weighted) graph of Neighbors for points in X"
  fit_transform(self, X, y=None) L491
  "Fit to data, then transform it."

_check_params(X, metric, p, metric_params) L15
"Check the validity of the input parameters"
_query_include_self(X, include_self, mode) L28
"Return the query based on include_self param"
kneighbors_graph(X, n_neighbors, *, mode='connectivity', metric='minkowski', p=2, metric_params=None, include_self=False, n_jobs=None) L41
"Computes the (weighted) graph of k-Neighbors for points in X"
radius_neighbors_graph(X, radius, *, mode='connectivity', metric='minkowski', p=2, metric_params=None, include_self=False, n_jobs=None) L118
"Computes the (weighted) graph of Neighbors for points in X"

sklearn/neighbors/_kde.py
class KernelDensity(BaseEstimator) L27
  "Kernel Density Estimation."
  __init__(self, *, bandwidth=1.0, algorithm='auto', kernel='gaussian', metric='euclidean', atol=0, rtol=0, breadth_first=True, leaf_size=40, metric_params=None) L99
  _choose_algorithm(self, algorithm, metric) L122
  fit(self, X, y=None, sample_weight=None) L142
  "Fit the Kernel Density model on the data."
  score_samples(self, X) L180
  "Evaluate the log density model on the data."
  score(self, X, y=None) L212
  "Compute the total log probability density under the model."
  sample(self, n_samples=1, random_state=None) L233
  "Generate random samples from the model."
  _more_tags(self) L283

sklearn/neighbors/_lof.py
class LocalOutlierFactor(KNeighborsMixin, UnsupervisedMixin, OutlierMixin, NeighborsBase) L20
  "Unsupervised Outlier Detection using Local Outlier Factor (LOF)"
  __init__(self, n_neighbors=20, *, algorithm='auto', leaf_size=30, metric='minkowski', p=2, metric_params=None, contamination='auto', novelty=False, n_jobs=None) L181
  fit_predict(self) L193
  "Fits the model to the training set X and returns the labels."
  _fit_predict(self, X, y=None) L225
  "Fits the model to the training set X and returns the labels."
  fit(self, X, y=None) L248
  "Fit the model using X as training data."
  predict(self) L302
  "Predict the labels (1 inlier, -1 outlier) of X according to LOF."
  _predict(self, X=None) L329
  "Predict the labels (1 inlier, -1 outlier) of X according to LOF."
  decision_function(self) L359
  "Shifted opposite of the Local Outlier Factor of X."
  _decision_function(self, X) L395
  "Shifted opposite of the Local Outlier Factor of X."
  score_samples(self) L424
  "Opposite of the Local Outlier Factor of X."
  _score_samples(self, X) L460
  "Opposite of the Local Outlier Factor of X."
  _local_reachability_density(self, distances_X, neighbors_indices) L500
  "The local reachability density (LRD)"

sklearn/neighbors/_nca.py
class NeighborhoodComponentsAnalysis(TransformerMixin, BaseEstimator) L30
  "Neighborhood Components Analysis"
  __init__(self, n_components=None, *, init='auto', warm_start=False, max_iter=50, tol=1e-05, callback=None, verbose=0, random_state=None) L166
  fit(self, X, y) L178
  "Fit the model according to the given training data."
  transform(self, X) L246
  "Applies the learned transformation to the given data."
  _validate_params(self, X, y) L270
  "Validate parameters as soon as :meth:`fit` is called."
  _initialize(self, X, y, init) L379
  "Initialize the transformation."
  _callback(self, transformation) L445
  "Called after each iteration of the optimizer."
  _loss_grad_lbfgs(self, transformation, X, same_class_mask, sign=1.0) L458
  "Compute the loss and the loss gradient w.r.t."
  _more_tags(self) L526

sklearn/neighbors/_nearest_centroid.py
class NearestCentroid(ClassifierMixin, BaseEstimator) L24
  "Nearest centroid classifier."
  __init__(self, metric='euclidean', *, shrink_threshold=None) L90
  fit(self, X, y) L94
  "Fit the NearestCentroid model according to the given training data."
  predict(self, X) L180
  "Perform classification on an array of test vectors X."

sklearn/neighbors/_regression.py
class KNeighborsRegressor(NeighborsBase, KNeighborsMixin, SupervisedFloatMixin, RegressorMixin) L24
  "Regression based on k-nearest neighbors."
  __init__(self, n_neighbors=5, *, weights='uniform', algorithm='auto', leaf_size=30, p=2, metric='minkowski', metric_params=None, n_jobs=None, **kwargs) L147
  _pairwise(self) L159
  predict(self, X) L163
  "Predict the target for the provided data"

class RadiusNeighborsRegressor(NeighborsBase, RadiusNeighborsMixin, SupervisedFloatMixin, RegressorMixin) L203
  "Regression based on neighbors within a fixed radius."
  __init__(self, radius=1.0, *, weights='uniform', algorithm='auto', leaf_size=30, p=2, metric='minkowski', metric_params=None, n_jobs=None, **kwargs) L319
  predict(self, X) L331
  "Predict the target for the provided data"

sklearn/neighbors/_unsupervised.py
class NearestNeighbors(KNeighborsMixin, RadiusNeighborsMixin, UnsupervisedMixin, NeighborsBase) L9
  "Unsupervised learner for implementing neighbor searches."
  __init__(self, *, n_neighbors=5, radius=1.0, algorithm='auto', leaf_size=30, metric='minkowski', p=2, metric_params=None, n_jobs=None) L115

sklearn/neighbors/setup.py
configuration(parent_package='', top_path=None) L4

sklearn/neural_network/_base.py
inplace_identity(X) L13
"Simply leave the input array unchanged."
inplace_logistic(X) L25
"Compute the logistic function inplace."
inplace_tanh(X) L36
"Compute the hyperbolic tan function inplace."
inplace_relu(X) L47
"Compute the rectified linear unit function inplace."
inplace_softmax(X) L58
"Compute the K-way softmax function inplace."
inplace_identity_derivative(Z, delta) L78
"Apply the derivative of the identity function: do nothing."
inplace_logistic_derivative(Z, delta) L93
"Apply the derivative of the logistic sigmoid function."
inplace_tanh_derivative(Z, delta) L112
"Apply the derivative of the hyperbolic tanh function."
inplace_relu_derivative(Z, delta) L130
"Apply the derivative of the relu function."
squared_loss(y_true, y_pred) L154
"Compute the squared loss for regression."
log_loss(y_true, y_prob) L173
"Compute Logistic loss for classification."
binary_log_loss(y_true, y_prob) L201
"Compute binary logistic loss for classification."

sklearn/neural_network/_multilayer_perceptron.py
class BaseMultilayerPerceptron(BaseEstimator) L42
  "Base class for MLP classification and regression."
  __init__(self, hidden_layer_sizes, activation, solver, alpha, batch_size, learning_rate, learning_rate_init, power_t, max_iter, loss, shuffle, random_state, tol, verbose, warm_start, momentum, nesterovs_momentum, early_stopping, validation_fraction, beta_1, beta_2, epsilon, n_iter_no_change, max_fun) L52
  _unpack(self, packed_parameters) L83
  "Extract the coefficients and intercepts from packed_parameters."
  _forward_pass(self, activations) L92
  "Perform a forward pass on the network by computing the values"
  _forward_pass_fast(self, X) L118
  "Predict using the trained model"
  _compute_loss_grad(self, layer, n_samples, activations, deltas, coef_grads, intercept_grads) L151
  "Compute the gradient of loss with respect to coefs and intercept for"
  _loss_grad_lbfgs(self, packed_coef_inter, X, y, activations, deltas, coef_grads, intercept_grads) L165
  "Compute the MLP loss function and its corresponding derivatives"
  _backprop(self, X, y, activations, deltas, coef_grads, intercept_grads) L213
  "Compute the MLP loss function and its corresponding derivatives"
  _initialize(self, y, layer_units, dtype) L292
  _init_coef(self, fan_in, fan_out, dtype) L332
  _fit(self, X, y, incremental=False) L349
  _validate_hyperparameters(self) L407
  _fit_lbfgs(self, X, y, activations, deltas, coef_grads, intercept_grads, layer_units) L459
  _fit_stochastic(self, X, y, activations, deltas, coef_grads, intercept_grads, layer_units, incremental) L503
  _update_no_improvement_count(self, early_stopping, X_val, y_val) L625
  fit(self, X, y) L656
  "Fit the model to data matrix X and target(s) y."
  partial_fit(self) L675
  "Update the model with a single iteration over the given data."
  _partial_fit(self, X, y) L696

class MLPClassifier(ClassifierMixin, BaseMultilayerPerceptron) L700
  "Multi-layer Perceptron classifier."
  __init__(self, hidden_layer_sizes=(100,), activation='relu', *, solver='adam', alpha=0.0001, batch_size='auto', learning_rate='constant', learning_rate_init=0.001, power_t=0.5, max_iter=200, shuffle=True, random_state=None, tol=0.0001, verbose=False, warm_start=False, momentum=0.9, nesterovs_momentum=True, early_stopping=False, validation_fraction=0.1, beta_1=0.9, beta_2=0.999, epsilon=1e-08, n_iter_no_change=10, max_fun=15000) L950
  _validate_input(self, X, y, incremental) L973
  predict(self, X) L1003
  "Predict using the multi-layer perceptron classifier"
  fit(self, X, y) L1024
  "Fit the model to data matrix X and target(s) y."
  partial_fit(self) L1044
  "Update the model with a single iteration over the given data."
  _partial_fit(self, X, y, classes=None) L1073
  predict_log_proba(self, X) L1085
  "Return the log of probability estimates."
  predict_proba(self, X) L1103
  "Probability estimates."

class MLPRegressor(RegressorMixin, BaseMultilayerPerceptron) L1129
  "Multi-layer Perceptron regressor."
  __init__(self, hidden_layer_sizes=(100,), activation='relu', *, solver='adam', alpha=0.0001, batch_size='auto', learning_rate='constant', learning_rate_init=0.001, power_t=0.5, max_iter=200, shuffle=True, random_state=None, tol=0.0001, verbose=False, warm_start=False, momentum=0.9, nesterovs_momentum=True, early_stopping=False, validation_fraction=0.1, beta_1=0.9, beta_2=0.999, epsilon=1e-08, n_iter_no_change=10, max_fun=15000) L1373
  predict(self, X) L1397
  "Predict using the multi-layer perceptron model."
  _validate_input(self, X, y, incremental) L1416

_pack(coefs_, intercepts_) L37
"Pack the parameters into a single vector."

sklearn/neural_network/_rbm.py
class BernoulliRBM(TransformerMixin, BaseEstimator) L26
  "Bernoulli Restricted Boltzmann Machine (RBM)."
  __init__(self, n_components=256, *, learning_rate=0.1, batch_size=10, n_iter=10, verbose=0, random_state=None) L110
  transform(self, X) L119
  "Compute the hidden layer activation probabilities, P(h=1|v=X)."
  _mean_hiddens(self, v) L137
  "Computes the probabilities P(h=1|v)."
  _sample_hiddens(self, v, rng) L154
  "Sample from the distribution P(h|v)."
  _sample_visibles(self, h, rng) L173
  "Sample from the distribution P(v|h)."
  _free_energy(self, v) L194
  "Computes the free energy F(v) = - log sum_h exp(-E(v,h))."
  gibbs(self, v) L211
  "Perform one Gibbs sampling step."
  partial_fit(self, X, y=None) L232
  "Fit the model to the data X which should contain a partial"
  _fit(self, v_pos, rng) L266
  "Inner fit for one mini-batch."
  score_samples(self, X) L296
  "Compute the pseudo-likelihood of X."
  fit(self, X, y=None) L334
  "Fit the model to the data X."
  _more_tags(self) L381

sklearn/neural_network/_stochastic_optimizers.py
class BaseOptimizer L10
  "Base (Stochastic) gradient descent optimizer"
  __init__(self, params, learning_rate_init=0.1) L29
  update_params(self, grads) L34
  "Update parameters with given gradients"
  iteration_ends(self, time_step) L47
  "Perform update to learning rate and potentially other states at the"
  trigger_stopping(self, msg, verbose) L53
  "Decides whether it is time to stop training"

class SGDOptimizer(BaseOptimizer) L74
  "Stochastic gradient descent optimizer with momentum"
  __init__(self, params, learning_rate_init=0.1, lr_schedule='constant', momentum=0.9, nesterov=True, power_t=0.5) L122
  iteration_ends(self, time_step) L132
  "Perform updates to learning rate and potential other states at the"
  trigger_stopping(self, msg, verbose) L146
  _get_updates(self, grads) L163
  "Get the values used to update params with given gradients"

class AdamOptimizer(BaseOptimizer) L188
  "Stochastic gradient descent optimizer with Adam"
  __init__(self, params, learning_rate_init=0.001, beta_1=0.9, beta_2=0.999, epsilon=1e-08) L235
  _get_updates(self, grads) L246
  "Get the values used to update params with given gradients"

sklearn/pipeline.py
class Pipeline(_BaseComposition) L31
  "Pipeline of transforms with a final estimator."
  __init__(self, steps, *, memory=None, verbose=False) L109
  get_params(self, deep=True) L115
  "Get parameters for this estimator."
  set_params(self, **kwargs) L131
  "Set the parameters of this estimator."
  _validate_steps(self) L143
  _iter(self, with_final=True, filter_passthrough=True) L171
  "Generate (idx, (name, trans)) tuples from self.steps"
  __len__(self) L188
  "Returns the length of the Pipeline"
  __getitem__(self, ind) L194
  "Returns a sub-pipeline or a single esimtator in the pipeline"
  _estimator_type(self) L215
  named_steps(self) L219
  _final_estimator(self) L224
  _log_message(self, step_idx) L228
  _check_fit_params(self, **fit_params) L237
  _fit(self, X, y=None, **fit_params_steps) L254
  fit(self, X, y=None, **fit_params) L302
  "Fit the model"
  fit_transform(self, X, y=None, **fit_params) L338
  "Fit the model and transform with the final estimator"
  predict(self, X, **predict_params) L381
  "Apply transforms to the data, and predict with the final estimator"
  fit_predict(self, X, y=None, **fit_params) L410
  "Applies fit_predict of last step in pipeline after transforms."
  predict_proba(self, X) L447
  "Apply transforms, and predict_proba of the final estimator"
  decision_function(self, X) L466
  "Apply transforms, and decision_function of the final estimator"
  score_samples(self, X) L485
  "Apply transforms, and score_samples of the final estimator."
  predict_log_proba(self, X) L504
  "Apply transforms, and predict_log_proba of the final estimator"
  transform(self) L523
  "Apply transforms, and transform with the final estimator"
  _transform(self, X) L545
  inverse_transform(self) L552
  "Apply inverse transformations in reverse order"
  _inverse_transform(self, X) L575
  score(self, X, y=None, sample_weight=None) L583
  "Apply transforms, and score with the final estimator"
  classes_(self) L613
  _pairwise(self) L617
  n_features_in_(self) L622
  _sk_visual_block_(self) L626
  _get_name(name, est) L629

class FeatureUnion(TransformerMixin, _BaseComposition) L757
  "Concatenates results of multiple transformer objects."
  __init__(self, transformer_list, *, n_jobs=None, transformer_weights=None, verbose=False) L819
  get_params(self, deep=True) L827
  "Get parameters for this estimator."
  set_params(self, **kwargs) L843
  "Set the parameters of this estimator."
  _validate_transformers(self) L855
  _iter(self) L871
  "Generate (name, trans, weight) tuples excluding None and"
  get_feature_names(self) L881
  "Get feature names from all transformers."
  fit(self, X, y=None, **fit_params) L899
  "Fit all transformers using X."
  fit_transform(self, X, y=None, **fit_params) L923
  "Fit all transformers, transform the data and concatenate results."
  _log_message(self, name, idx, total) L955
  _parallel_func(self, X, y, fit_params, func) L960
  "Runs func in parallel on X and y"
  transform(self, X) L973
  "Transform X separately by each transformer, concatenate results."
  _update_transformer_list(self, transformers) L1000
  n_features_in_(self) L1007
  _sk_visual_block_(self) L1011

_name_estimators(estimators) L642
"Generate names for estimators."
make_pipeline(*steps, memory=None, verbose=False) L667
"Construct a Pipeline from the given estimators."
_transform_one(transformer, X, y, weight, **fit_params) L712
_fit_transform_one(transformer, X, y, weight, message_clsname='', message=None, **fit_params) L720
"Fits ``transformer`` to ``X`` and ``y``."
_fit_one(transformer, X, y, weight, message_clsname='', message=None, **fit_params) L743
"Fits ``transformer`` to ``X`` and ``y``."
make_union(*transformers, n_jobs=None, verbose=False) L1016
"Construct a FeatureUnion from the given transformers."

sklearn/preprocessing/_data.py
class MinMaxScaler(TransformerMixin, BaseEstimator) L218
  "Transform features by scaling each feature to a given range."
  __init__(self, feature_range=(0, 1), *, copy=True) L314
  _reset(self) L318
  "Reset internal data-dependent state of the scaler, if necessary."
  fit(self, X, y=None) L334
  "Compute the minimum and maximum to be used for later scaling."
  partial_fit(self, X, y=None) L356
  "Online computation of min and max on X for later scaling."
  transform(self, X) L410
  "Scale features of X according to feature_range."
  inverse_transform(self, X) L432
  "Undo the scaling of X according to feature_range."
  _more_tags(self) L454

class StandardScaler(TransformerMixin, BaseEstimator) L553
  "Standardize features by removing the mean and scaling to unit variance"
  __init__(self, *, copy=True, with_mean=True, with_std=True) L669
  _reset(self) L674
  "Reset internal data-dependent state of the scaler, if necessary."
  fit(self, X, y=None) L688
  "Compute the mean and std to be used for later scaling."
  partial_fit(self, X, y=None) L710
  "Online computation of mean and std on X for later scaling."
  transform(self, X, copy=None) L819
  "Perform standardization by centering and scaling"
  inverse_transform(self, X, copy=None) L856
  "Scale back the data to the original representation"
  _more_tags(self) L896

class MaxAbsScaler(TransformerMixin, BaseEstimator) L900
  "Scale each feature by its maximum absolute value."
  __init__(self, *, copy=True) L962
  _reset(self) L965
  "Reset internal data-dependent state of the scaler, if necessary."
  fit(self, X, y=None) L978
  "Compute the maximum absolute value to be used for later scaling."
  partial_fit(self, X, y=None) L999
  "Online computation of max absolute value of X for later scaling."
  transform(self, X) L1043
  "Scale the data"
  inverse_transform(self, X) L1067
  "Scale back the data to the original representation"
  _more_tags(self) L1091

class RobustScaler(TransformerMixin, BaseEstimator) L1173
  "Scale features using statistics that are robust to outliers."
  __init__(self, *, with_centering=True, with_scaling=True, quantile_range=(25.0, 75.0), copy=True, unit_variance=False) L1272
  fit(self, X, y=None) L1280
  "Compute the median and quantiles to be used for scaling."
  transform(self, X) L1344
  "Center and scale the data."
  inverse_transform(self, X) L1372
  "Scale back the data to the original representation"
  _more_tags(self) L1400

class PolynomialFeatures(TransformerMixin, BaseEstimator) L1512
  "Generate polynomial and interaction features."
  __init__(self, degree=2, *, interaction_only=False, include_bias=True, order='C') L1584
  _combinations(n_features, degree, interaction_only, include_bias) L1592
  powers_(self) L1599
  get_feature_names(self, input_features=None) L1608
  "Return feature names for output features"
  fit(self, X, y=None) L1637
  "Compute number of output features."
  transform(self, X) L1664
  "Transform data to polynomial features"

class Normalizer(TransformerMixin, BaseEstimator) L1894
  "Normalize samples individually to unit norm."
  __init__(self, norm='l2', *, copy=True) L1954
  fit(self, X, y=None) L1958
  "Do nothing and return the estimator unchanged"
  transform(self, X, copy=None) L1980
  "Scale each non zero row of X to unit norm"
  _more_tags(self) L2001

class Binarizer(TransformerMixin, BaseEstimator) L2055
  "Binarize data (set feature values to 0 or 1) according to a threshold"
  __init__(self, *, threshold=0.0, copy=True) L2110
  fit(self, X, y=None) L2114
  "Do nothing and return the estimator unchanged"
  transform(self, X, copy=None) L2136
  "Binarize each element of X"
  _more_tags(self) L2157

class KernelCenterer(TransformerMixin, BaseEstimator) L2161
  "Center a kernel matrix"
  __init__(self) L2201
  fit(self, K, y=None) L2205
  "Fit KernelCenterer"
  transform(self, K, copy=True) L2234
  "Center kernel matrix."
  _pairwise(self) L2263

class QuantileTransformer(TransformerMixin, BaseEstimator) L2324
  "Transform features using quantiles information."
  __init__(self, *, n_quantiles=1000, output_distribution='uniform', ignore_implicit_zeros=False, subsample=int(100000.0), random_state=None, copy=True) L2424
  _dense_fit(self, X, random_state) L2434
  "Compute percentiles for dense matrices."
  _sparse_fit(self, X, random_state) L2464
  "Compute percentiles for sparse matrices."
  fit(self, X, y=None) L2513
  "Compute the quantiles used for transforming."
  _transform_col(self, X_col, quantiles, inverse) L2570
  "Private function to transform a single feature"
  _check_inputs(self, X, in_fit, accept_sparse_negative=False, copy=False) L2639
  "Check inputs before fit and transform"
  _check_is_fitted(self, X) L2671
  "Check the inputs before transforming"
  _transform(self, X, inverse=False) L2681
  "Forward and inverse transform."
  transform(self, X) L2714
  "Feature-wise transformation of the data."
  inverse_transform(self, X) L2735
  "Back-projection to the original space."
  _more_tags(self) L2757

class PowerTransformer(TransformerMixin, BaseEstimator) L2897
  "Apply a power transform featurewise to make data more Gaussian-like."
  __init__(self, method='yeo-johnson', *, standardize=True, copy=True) L2981
  fit(self, X, y=None) L2986
  "Estimate the optimal parameter lambda for each feature."
  fit_transform(self, X, y=None) L3008
  _fit(self, X, y=None, force_transform=False) L3011
  transform(self, X) L3041
  "Apply the power transform to each feature using the fitted lambdas."
  inverse_transform(self, X) L3070
  "Apply the inverse power transformation using the fitted lambdas."
  _box_cox_inverse_tranform(self, x, lmbda) L3116
  "Return inverse-transformed input x following Box-Cox inverse"
  _yeo_johnson_inverse_transform(self, x, lmbda) L3127
  "Return inverse-transformed input x following Yeo-Johnson inverse"
  _yeo_johnson_transform(self, x, lmbda) L3149
  "Return transformed input x following Yeo-Johnson transform with"
  _box_cox_optimize(self, x) L3171
  "Find and return optimal lambda parameter of the Box-Cox transform by"
  _yeo_johnson_optimize(self, x) L3183
  "Find and return optimal lambda parameter of the Yeo-Johnson"
  _neg_log_likelihood(lmbda) L3190
  "Return the negative log likelihood of the observed data x as a"
  _check_input(self, X, in_fit, check_positive=False, check_shape=False, check_method=False) L3207
  "Validate the input before fit and transform."
  _more_tags(self) L3249

_handle_zeros_in_scale(scale, copy=True) L63
"Makes sure that whenever scale is zero, we handle it correctly."
scale(X, *, axis=0, with_mean=True, with_std=True, copy=True) L82
"Standardize a dataset along any axis"
minmax_scale(X, feature_range=(0, 1), *, axis=0, copy=True) L459
"Transform features by scaling each feature to a given range."
maxabs_scale(X, *, axis=0, copy=True) L1096
"Scale each feature to the [-1, 1] range without breaking the sparsity."
robust_scale(X, *, axis=0, with_centering=True, with_scaling=True, quantile_range=(25.0, 75.0), copy=True, unit_variance=False) L1405
"Standardize a dataset along any axis"
normalize(X, norm='l2', *, axis=1, copy=True, return_norm=False) L1795
"Scale input vectors individually to unit norm (vector length)."
binarize(X, *, threshold=0.0, copy=True) L2006
"Boolean thresholding of array-like or scipy.sparse matrix"
add_dummy_feature(X, value=1.0) L2267
"Augment dataset with an additional dummy feature."
quantile_transform(X, *, axis=0, n_quantiles=1000, output_distribution='uniform', ignore_implicit_zeros=False, subsample=int(100000.0), random_state=None, copy=True) L2762
"Transform features using quantiles information."
power_transform(X, method='yeo-johnson', *, standardize=True, copy=True) L3254
"Power transforms are a family of parametric, monotonic transformations"

sklearn/preprocessing/_discretization.py
class KBinsDiscretizer(TransformerMixin, BaseEstimator) L21
  "Bin continuous data into intervals."
  __init__(self, n_bins=5, encode='onehot', strategy='quantile', dtype=None) L127
  fit(self, X, y=None) L134
  "Fit the estimator."
  _validate_n_bins(self, n_features) L237
  "Returns n_bins_, the number of bins per feature."
  transform(self, X) L271
  "Discretize the data."
  inverse_transform(self, Xt) L322
  "Transform discretized data back to original feature space."

sklearn/preprocessing/_encoders.py
class _BaseEncoder(TransformerMixin, BaseEstimator) L22
  "Base class for encoders that includes the code to categorize and"
  _check_X(self, X) L29
  "Perform custom check_array:"
  _get_feature(self, X, feature_idx) L66
  _fit(self, X, handle_unknown='error') L73
  _transform(self, X, handle_unknown='error') L101
  _more_tags(self) L146

class OneHotEncoder(_BaseEncoder) L150
  "Encode categorical features as a one-hot numeric array."
  __init__(self, *, categories='auto', drop=None, sparse=True, dtype=np.float64, handle_unknown='error') L298
  _validate_keywords(self) L306
  _compute_drop_idx(self) L320
  fit(self, X, y=None) L366
  "Fit OneHotEncoder to X."
  fit_transform(self, X, y=None) L388
  "Fit OneHotEncoder to X, then transform X."
  transform(self, X) L411
  "Transform X using one-hot encoding."
  inverse_transform(self, X) L472
  "Convert the data back to the original representation."
  get_feature_names(self, input_features=None) L560
  "Return feature names for output features."

class OrdinalEncoder(_BaseEncoder) L596
  "Encode categorical features as an integer array."
  __init__(self, *, categories='auto', dtype=np.float64) L660
  fit(self, X, y=None) L664
  "Fit the OrdinalEncoder to X."
  transform(self, X) L685
  "Transform X to ordinal codes."
  inverse_transform(self, X) L702
  "Convert the data back to the original representation."

sklearn/preprocessing/_function_transformer.py
class FunctionTransformer(TransformerMixin, BaseEstimator) L14
  "Constructs a transformer from an arbitrary callable."
  __init__(self, func=None, inverse_func=None, *, validate=False, accept_sparse=False, check_inverse=True, kw_args=None, inv_kw_args=None) L88
  _check_input(self, X) L99
  _check_inverse_transform(self, X) L104
  "Check that func and inverse_func are the inverse."
  fit(self, X, y=None) L114
  "Fit transformer by checking X."
  transform(self, X) L134
  "Transform X using the forward function."
  inverse_transform(self, X) L149
  "Transform X using the inverse function."
  _transform(self, X, func=None, kw_args=None) L165
  _more_tags(self) L173

_identity(X) L8
"The identity function."

sklearn/preprocessing/_label.py
class LabelEncoder(TransformerMixin, BaseEstimator) L38
  "Encode target labels with value between 0 and n_classes-1."
  fit(self, y) L90
  "Fit label encoder"
  fit_transform(self, y) L106
  "Fit label encoder and return encoded labels"
  transform(self, y) L122
  "Transform labels to normalized encoding."
  inverse_transform(self, y) L142
  "Transform labels back to original encoding."
  _more_tags(self) L167

class LabelBinarizer(TransformerMixin, BaseEstimator) L171
  "Binarize labels in a one-vs-all fashion"
  __init__(self, *, neg_label=0, pos_label=1, sparse_output=False) L263
  fit(self, y) L278
  "Fit label binarizer"
  fit_transform(self, y) L302
  "Fit label binarizer and transform multi-class labels to binary"
  transform(self, y) L324
  "Transform multi-class labels to binary labels"
  inverse_transform(self, Y, threshold=None) L355
  "Transform binary labels back to multi-class labels"
  _more_tags(self) L404

class MultiLabelBinarizer(TransformerMixin, BaseEstimator) L658
  "Transform between iterable of iterables and a multilabel format"
  __init__(self, *, classes=None, sparse_output=False) L722
  fit(self, y) L726
  "Fit the label sets binarizer, storing :term:`classes_`"
  fit_transform(self, y) L754
  "Fit the label sets binarizer and transform the given label sets"
  transform(self, y) L797
  "Transform the given label sets"
  _build_cache(self) L823
  _transform(self, y, class_mapping) L830
  "Transforms the label sets with a given mapping"
  inverse_transform(self, yt) L864
  "Transform the given indicator matrix into label sets"
  _more_tags(self) L898

label_binarize(y, *, classes, neg_label=0, pos_label=1, sparse_output=False) L409
"Binarize labels in a one-vs-all fashion"
_inverse_binarize_multiclass(y, classes) L570
"Inverse label binarization transformation for multiclass."
_inverse_binarize_thresholding(y, output_type, classes, threshold) L614
"Inverse label binarization transformation using thresholding."

sklearn/preprocessing/setup.py
configuration(parent_package='', top_path=None) L4

sklearn/random_projection.py
class BaseRandomProjection(TransformerMixin, BaseEstimator) L292
  "Base class for random projections."
  __init__(self, n_components='auto', *, eps=0.1, dense_output=False, random_state=None) L300
  _make_random_matrix(self, n_components, n_features) L308
  "Generate the random projection matrix"
  fit(self, X, y=None) L326
  "Generate a sparse random projection matrix"
  transform(self, X) L390
  "Project the data by using matrix product with the random matrix"

class GaussianRandomProjection(BaseRandomProjection) L418
  "Reduce dimensionality through Gaussian random projection"
  __init__(self, n_components='auto', *, eps=0.1, random_state=None) L480
  _make_random_matrix(self, n_components, n_features) L487
  "Generate the random projection matrix"

class SparseRandomProjection(BaseRandomProjection) L510
  "Reduce dimensionality through sparse random projection"
  __init__(self, n_components='auto', *, density='auto', eps=0.1, dense_output=False, random_state=None) L619
  _make_random_matrix(self, n_components, n_features) L629
  "Generate the random projection matrix"

johnson_lindenstrauss_min_dim(n_samples, *, eps=0.1) L52
"Find a 'safe' number of components to randomly project to"
_check_density(density, n_features) L135
"Factorize density check according to Li et al."
_check_input_size(n_components, n_features) L146
"Factorize argument checking for random matrix generation"
_gaussian_random_matrix(n_components, n_features, random_state=None) L156
"Generate a dense Gaussian random matrix."
_sparse_random_matrix(n_components, n_features, density='auto', random_state=None) L196
"Generalized Achlioptas random sparse matrix for random projection"

sklearn/semi_supervised/_label_propagation.py
class BaseLabelPropagation(ClassifierMixin, BaseEstimator) L75
  "Base class for label propagation module."
  __init__(self, kernel='rbf', *, gamma=20, n_neighbors=7, alpha=1, max_iter=30, tol=0.001, n_jobs=None) L110
  _get_kernel(self, X, y=None) L126
  _build_graph(self) L153
  predict(self, X) L157
  "Performs inductive inference across the model."
  predict_proba(self, X) L173
  "Predict probability for each possible outcome."
  fit(self, X, y) L208
  "Fit a semi-supervised label propagation model based"

class LabelPropagation(BaseLabelPropagation) L307
  "Label Propagation classifier"
  __init__(self, kernel='rbf', *, gamma=20, n_neighbors=7, max_iter=1000, tol=0.001, n_jobs=None) L384
  _build_graph(self) L390
  "Matrix representing a fully connected graph between each sample"
  fit(self, X, y) L406

class LabelSpreading(BaseLabelPropagation) L410
  "LabelSpreading model for semi-supervised learning"
  __init__(self, kernel='rbf', *, gamma=20, n_neighbors=7, alpha=0.2, max_iter=30, tol=0.001, n_jobs=None) L498
  _build_graph(self) L506
  "Graph matrix for Label Spreading computes the graph laplacian"

sklearn/setup.py
configuration(parent_package='', top_path=None) L7

sklearn/svm/_base.py
class BaseLibSVM(BaseEstimator) L62
  "Base class for estimators that use libsvm as backing library"
  __init__(self, kernel, degree, gamma, coef0, tol, C, nu, epsilon, shrinking, probability, cache_size, class_weight, verbose, max_iter, random_state) L76
  _pairwise(self) L106
  fit(self, X, y, sample_weight=None) L110
  "Fit the SVM model according to the given training data."
  _validate_targets(self, y) L233
  "Validation of y and class_weight."
  _warn_from_fit_status(self) L243
  _dense_fit(self, X, y, sample_weight, solver_type, kernel, random_seed) L251
  _sparse_fit(self, X, y, sample_weight, solver_type, kernel, random_seed) L280
  predict(self, X) L318
  "Perform regression on samples in X."
  _dense_predict(self, X) L337
  _sparse_predict(self, X) L359
  _compute_kernel(self, X) L382
  "Return the data transformed by a callable kernel"
  _decision_function(self, X) L393
  "Evaluates the decision function for the samples in X."
  _dense_decision_function(self, X) L423
  _sparse_decision_function(self, X) L439
  _validate_for_predict(self, X) L461
  coef_(self) L490
  _get_coef(self) L507
  n_support_(self) L511

class BaseSVC(ClassifierMixin, BaseLibSVM) L526
  "ABC for LibSVM-based classifiers."
  __init__(self, kernel, degree, gamma, coef0, tol, C, nu, shrinking, probability, cache_size, class_weight, verbose, max_iter, decision_function_shape, random_state, break_ties) L529
  _validate_targets(self, y) L542
  decision_function(self, X) L557
  "Evaluates the decision function for the samples in X."
  predict(self, X) L588
  "Perform classification on samples in X."
  _check_proba(self) L622
  predict_proba(self) L631
  "Compute probabilities of possible outcomes for samples in X."
  _predict_proba(self, X) L660
  predict_log_proba(self) L670
  "Compute log probabilities of possible outcomes for samples in X."
  _predict_log_proba(self, X) L700
  _dense_predict_proba(self, X) L703
  _sparse_predict_proba(self, X) L720
  _get_coef(self) L742
  probA_(self) L758
  probB_(self) L762

_one_vs_one_coef(dual_coef, n_support, support_vectors) L29
"Generate primal coefficients from dual coefficients"
_get_liblinear_solver_type(multi_class, penalty, loss, dual) L766
"Find the liblinear magic number for the solver."
_fit_liblinear(X, y, C, fit_intercept, intercept_scaling, class_weight, penalty, dual, verbose, max_iter, tol, random_state=None, multi_class='ovr', loss='logistic_regression', epsilon=0.1, sample_weight=None) L826
"Used by Logistic Regression (and CV) and LinearSVC/LinearSVR."

sklearn/svm/_bounds.py
l1_min_c(X, y, *, loss='squared_hinge', fit_intercept=True, intercept_scaling=1.0) L14
"Return the lowest bound for C such that for C in (l1_min_C, infinity)"

sklearn/svm/_classes.py
class LinearSVC(LinearClassifierMixin, SparseCoefMixin, BaseEstimator) L13
  "Linear Support Vector Classification."
  __init__(self, penalty='l2', loss='squared_hinge', *, dual=True, tol=0.0001, C=1.0, multi_class='ovr', fit_intercept=True, intercept_scaling=1, class_weight=None, verbose=0, random_state=None, max_iter=1000) L183
  fit(self, X, y, sample_weight=None) L200
  "Fit the model according to the given training data."
  _more_tags(self) L248

class LinearSVR(RegressorMixin, LinearModel) L257
  "Linear Support Vector Regression."
  __init__(self, *, epsilon=0.0, tol=0.0001, C=1.0, loss='epsilon_insensitive', fit_intercept=True, intercept_scaling=1.0, dual=True, verbose=0, random_state=None, max_iter=1000) L380
  fit(self, X, y, sample_weight=None) L395
  "Fit the model according to the given training data."
  _more_tags(self) L436

class SVC(BaseSVC) L445
  "C-Support Vector Classification."
  __init__(self, *, C=1.0, kernel='rbf', degree=3, gamma='scale', coef0=0.0, shrinking=True, probability=False, tol=0.001, cache_size=200, class_weight=None, verbose=False, max_iter=-1, decision_function_shape='ovr', break_ties=False, random_state=None) L654
  _more_tags(self) L670

class NuSVC(BaseSVC) L679
  "Nu-Support Vector Classification."
  __init__(self, *, nu=0.5, kernel='rbf', degree=3, gamma='scale', coef0=0.0, shrinking=True, probability=False, tol=0.001, cache_size=200, class_weight=None, verbose=False, max_iter=-1, decision_function_shape='ovr', break_ties=False, random_state=None) L877
  _more_tags(self) L892

class SVR(RegressorMixin, BaseLibSVM) L904
  "Epsilon-Support Vector Regression."
  __init__(self, *, kernel='rbf', degree=3, gamma='scale', coef0=0.0, tol=0.001, C=1.0, epsilon=0.1, shrinking=True, cache_size=200, verbose=False, max_iter=-1) L1046
  probA_(self) L1061
  probB_(self) L1069
  _more_tags(self) L1072

class NuSVR(RegressorMixin, BaseLibSVM) L1081
  "Nu Support Vector Regression."
  __init__(self, *, nu=0.5, C=1.0, kernel='rbf', degree=3, gamma='scale', coef0=0.0, shrinking=True, tol=0.001, cache_size=200, verbose=False, max_iter=-1) L1216
  _more_tags(self) L1226

class OneClassSVM(OutlierMixin, BaseLibSVM) L1235
  "Unsupervised Outlier Detection."
  __init__(self, *, kernel='rbf', degree=3, gamma='scale', coef0=0.0, tol=0.001, nu=0.5, shrinking=True, cache_size=200, verbose=False, max_iter=-1) L1351
  fit(self, X, y=None, sample_weight=None, **params) L1360
  "Detects the soft boundary of the set of samples X."
  decision_function(self, X) L1390
  "Signed distance to the separating hyperplane."
  score_samples(self, X) L1408
  "Raw scoring function of the samples."
  predict(self, X) L1423
  "Perform classification on samples in X."
  probA_(self) L1448
  probB_(self) L1456
  _more_tags(self) L1459

sklearn/svm/setup.py
configuration(parent_package='', top_path=None) L6

sklearn/tree/_classes.py
class BaseDecisionTree(MultiOutputMixin, BaseEstimator) L78
  "Base class for decision trees."
  __init__(self, *, criterion, splitter, max_depth, min_samples_split, min_samples_leaf, min_weight_fraction_leaf, max_features, max_leaf_nodes, random_state, min_impurity_decrease, min_impurity_split, class_weight=None, ccp_alpha=0.0) L87
  get_depth(self) L115
  "Return the depth of the decision tree."
  get_n_leaves(self) L129
  "Return the number of leaves of the decision tree."
  fit(self, X, y, sample_weight=None, check_input=True, X_idx_sorted='deprecated') L140
  _validate_X_predict(self, X, check_input) L383
  "Validate X whenever one tries to predict, apply, predict_proba"
  predict(self, X, check_input=True) L401
  "Predict class or regression value for X."
  apply(self, X, check_input=True) L453
  "Return the index of the leaf that each sample is predicted as."
  decision_path(self, X, check_input=True) L481
  "Return the decision path in the tree."
  _prune_tree(self) L506
  "Prune tree using Minimal Cost-Complexity Pruning."
  cost_complexity_pruning_path(self, X, y, sample_weight=None) L529
  "Compute the pruning path during Minimal Cost-Complexity Pruning."
  feature_importances_(self) L569
  "Return the feature importances."

class DecisionTreeClassifier(ClassifierMixin, BaseDecisionTree) L595
  "A decision tree classifier."
  __init__(self, *, criterion='gini', splitter='best', max_depth=None, min_samples_split=2, min_samples_leaf=1, min_weight_fraction_leaf=0.0, max_features=None, random_state=None, max_leaf_nodes=None, min_impurity_decrease=0.0, min_impurity_split=None, class_weight=None, ccp_alpha=0.0) L819
  fit(self, X, y, sample_weight=None, check_input=True, X_idx_sorted='deprecated') L848
  "Build a decision tree classifier from the training set (X, y)."
  predict_proba(self, X, check_input=True) L892
  "Predict class probabilities of the input samples X."
  predict_log_proba(self, X) L940
  "Predict class log-probabilities of the input samples X."

class DecisionTreeRegressor(RegressorMixin, BaseDecisionTree) L969
  "A decision tree regressor."
  __init__(self, *, criterion='mse', splitter='best', max_depth=None, min_samples_split=2, min_samples_leaf=1, min_weight_fraction_leaf=0.0, max_features=None, random_state=None, max_leaf_nodes=None, min_impurity_decrease=0.0, min_impurity_split=None, ccp_alpha=0.0) L1166
  fit(self, X, y, sample_weight=None, check_input=True, X_idx_sorted='deprecated') L1193
  "Build a decision tree regressor from the training set (X, y)."
  _compute_partial_dependence_recursion(self, grid, target_features) L1236
  "Fast partial dependence computation."

class ExtraTreeClassifier(DecisionTreeClassifier) L1262
  "An extremely randomized tree classifier."
  __init__(self, *, criterion='gini', splitter='random', max_depth=None, min_samples_split=2, min_samples_leaf=1, min_weight_fraction_leaf=0.0, max_features='auto', random_state=None, max_leaf_nodes=None, min_impurity_decrease=0.0, min_impurity_split=None, class_weight=None, ccp_alpha=0.0) L1478

class ExtraTreeRegressor(DecisionTreeRegressor) L1508
  "An extremely randomized tree regressor."
  __init__(self, *, criterion='mse', splitter='random', max_depth=None, min_samples_split=2, min_samples_leaf=1, min_weight_fraction_leaf=0.0, max_features='auto', random_state=None, min_impurity_decrease=0.0, min_impurity_split=None, max_leaf_nodes=None, ccp_alpha=0.0) L1696

sklearn/tree/_export.py
class Sentinel L73
  __repr__(self) L74

class _BaseTreeExporter L196
  __init__(self, max_depth=None, feature_names=None, class_names=None, label='all', filled=False, impurity=True, node_ids=False, proportion=False, rotate=False, rounded=False, precision=3, fontsize=None) L197
  get_color(self, value) L215
  get_fill_color(self, tree, node_id) L238
  node_to_str(self, tree, node_id, criterion) L263

class _DOTTreeExporter(_BaseTreeExporter) L368
  __init__(self, out_file=SENTINEL, max_depth=None, feature_names=None, class_names=None, label='all', filled=False, leaves_parallel=False, impurity=True, node_ids=False, proportion=False, rotate=False, rounded=False, special_characters=False, precision=3) L369
  export(self, decision_tree) L407
  tail(self) L428
  head(self) L437
  recurse(self, tree, node_id, criterion, parent=None, depth=0) L464

class _MPLTreeExporter(_BaseTreeExporter) L526
  __init__(self, max_depth=None, feature_names=None, class_names=None, label='all', filled=False, impurity=True, node_ids=False, proportion=False, rotate=False, rounded=False, precision=3, fontsize=None) L527
  _make_tree(self, node_id, et, criterion, depth=0) L561
  export(self, decision_tree, ax=None) L575
  recurse(self, node, tree, ax, scale_x, scale_y, height, depth=0) L626

_color_brew(n) L31
"Generate n colors with equally spaced hues."
plot_tree(decision_tree, *, max_depth=None, feature_names=None, class_names=None, label='all', filled=False, impurity=True, node_ids=False, proportion=False, rotate='deprecated', rounded=False, precision=3, ax=None, fontsize=None) L82
"Plot a decision tree."
export_graphviz(decision_tree, out_file=None, *, max_depth=None, feature_names=None, class_names=None, label='all', filled=False, leaves_parallel=False, impurity=True, node_ids=False, proportion=False, rotate=False, rounded=False, special_characters=False, precision=3) L665
"Export a decision tree in DOT format."
_compute_depth(tree, node) L796
"Returns the depth of the subtree rooted in node."
compute_depth_(current_node, current_depth, children_left, children_right, depths) L800
export_text(decision_tree, *, feature_names=None, max_depth=10, spacing=3, decimals=2, show_weights=False) L817
"Build a text report showing the rules of a decision tree."
_add_leaf(value, class_name, indent) L912
print_tree_recurse(node, depth) L923

sklearn/tree/_reingold_tilford.py
class DrawTree L7
  __init__(self, tree, parent=None, depth=0, number=1) L8
  left(self) L24
  right(self) L27
  lbrother(self) L30
  get_lmost_sibling(self) L40
  __str__(self) L47
  __repr__(self) L50
  max_extents(self) L53

class Tree L181
  __init__(self, label='', node_id=-1, *children) L182

buchheim(tree) L59
third_walk(tree, n) L67
first_walk(v, distance=1.0) L73
apportion(v, default_ancestor, distance) L98
move_subtree(wl, wr, shift) L135
execute_shifts(v) L147
ancestor(vil, v, default_ancestor) L157
second_walk(v, m=0, depth=0, min=None) L168

sklearn/tree/setup.py
configuration(parent_package='', top_path=None) L7

sklearn/utils/__init__.py
class Bunch(dict) L63
  "Container object exposing keys as attributes"
  __init__(self, **kwargs) L85
  __setattr__(self, key, value) L88
  __dir__(self) L91
  __getattr__(self, key) L94
  __setstate__(self, state) L100

safe_mask(X, mask) L112
"Return a mask which is safe to use on X."
axis0_safe_slice(X, mask, len_mask) L137
"This mask is safer than safe_mask since it returns an"
_array_indexing(array, key, key_dtype, axis) L172
"Index an array or scipy.sparse consistently across NumPy version."
_pandas_indexing(X, key, key_dtype, axis) L184
"Index a pandas dataframe or a series."
_list_indexing(X, key, key_dtype) L198
"Index a Python list."
_determine_key_type(key, accept_slice=True) L210
"Determine the data type of key."
_safe_indexing(X, indices, *, axis=0) L273
"Return rows, items or columns of X using indices."
_get_column_indices(X, key) L351
"Get feature column indices for input data X and key."
resample(*arrays, replace=True, n_samples=None, random_state=None, stratify=None) L416
"Resample arrays or sparse matrices in a consistent way."
shuffle(*arrays, random_state=None, n_samples=None) L568
"Shuffle arrays or sparse matrices in a consistent way"
safe_sqr(X, *, copy=True) L638
"Element wise squaring of array-likes and sparse matrices."
_chunk_generator(gen, chunksize) L666
"Chunk generator, ``gen`` into lists of length ``chunksize``."
gen_batches(n, batch_size, *, min_batch_size=0) L678
"Generator to create slices containing batch_size elements, from 0 to n."
gen_even_slices(n, n_packs, *, n_samples=None) L728
"Generator to create n_packs slices going up to n."
tosequence(x) L773
"Cast iterable x to a Sequence, avoiding a copy if possible."
_to_object_array(sequence) L788
"Convert sequence to a 1-D NumPy array of object dtype."
indices_to_mask(indices, mask_length) L827
"Convert list of indices to boolean mask."
_message_with_time(source, message, time) L859
"Create one line message for logging purposes"
_print_elapsed_time(source, message=None) L887
"Log elapsed time to stdout when the context is exited"
get_chunk_n_rows(row_bytes, *, max_n_rows=None, working_memory=None) L914
"Calculates how many rows can be processed within working_memory"
is_scalar_nan(x) L952
"Tests if x is NaN"
_approximate_mode(class_counts, n_draws, rng) L984
"Computes approximate mode of multivariate hypergeometric."
check_matplotlib_support(caller_name) L1053
"Raise ImportError with detailed error message if mpl is not installed."
check_pandas_support(caller_name) L1073
"Raise ImportError with detailed error message if pandsa is not"
all_estimators(type_filter=None) L1094
"Get a list of all estimators from sklearn."
is_abstract(c) L1122

sklearn/utils/_encode.py
_unique(values, *, return_inverse=False) L4
"Helper function to find unique values with support for python objects."
_unique_python(values, *, return_inverse) L33
_encode(values, *, uniques, check_unknown=True) L52
"Helper function to encode values into [0, n_uniques - 1]."
_check_unknown(values, known_values, return_mask=False) L96
"Helper function to check for unknowns in values to be encoded."

sklearn/utils/_estimator_html_repr.py
class _VisualBlock L10
  "HTML Representation of Estimator"
  __init__(self, kind, estimators, *, names=None, name_details=None, dash_wrapped=True) L37
  _sk_visual_block_(self) L52

_write_label_html(out, name, name_details, outer_class='sk-label-container', inner_class='sk-label', checked=False) L56
"Write labeled html with or without a dropdown with named details"
_get_visual_block(estimator) L79
"Generate information about how to display an estimator."
_write_estimator_html(out, estimator, estimator_label, estimator_label_details, first_call=False) L107
"Write estimator to html in serial, parallel, or by itself (single)."
estimator_html_repr(estimator) L288
"Build a HTML representation of an estimator."

sklearn/utils/_mask.py
_get_mask(X, value_to_mask) L7
"Compute the boolean mask X == value_to_mask."

sklearn/utils/_mocking.py
class ArraySlicingWrapper L7
  "Parameters"
  __init__(self, array) L13
  __getitem__(self, aslice) L16

class MockDataFrame L20
  "Parameters"
  __init__(self, array) L27
  __len__(self) L35
  __array__(self, dtype=None) L38
  __eq__(self, other) L44
  __ne__(self, other) L47

class CheckingClassifier(ClassifierMixin, BaseEstimator) L51
  "Dummy classifier to test pipelining and meta-estimators."
  __init__(self, *, check_y=None, check_y_params=None, check_X=None, check_X_params=None, methods_to_check='all', foo_param=0, expected_fit_params=None) L111
  _check_X_y(self, X, y=None, should_be_fitted=True) L122
  "Validate X and y and make extra check."
  fit(self, X, y, **fit_params) L157
  "Fit classifier."
  predict(self, X) L199
  "Predict the first class seen in `classes_`."
  predict_proba(self, X) L217
  "Predict probabilities for each class."
  decision_function(self, X) L240
  "Confidence score."
  score(self, X=None, Y=None) L266
  "Fake score."
  _more_tags(self) L293

class NoSampleWeightWrapper(BaseEstimator) L297
  "Wrap estimator which will not expose `sample_weight`."
  __init__(self, est=None) L305
  fit(self, X, y) L308
  predict(self, X) L311
  predict_proba(self, X) L314
  _more_tags(self) L317

sklearn/utils/_pprint.py
class KeyValTuple(tuple) L75
  "Dummy class for correctly rendering key-value tuples from dicts."
  __repr__(self) L77

class KeyValTupleParam(KeyValTuple) L82
  "Dummy class for correctly rendering key-value tuples from parameters."

class _EstimatorPrettyPrinter(pprint.PrettyPrinter) L106
  "Pretty Printer class for estimator objects."
  __init__(self, indent=1, width=80, depth=None, stream=None, *, compact=False, indent_at_name=True, n_max_elements_to_show=None) L157
  format(self, object, context, maxlevels, level) L170
  _pprint_estimator(self, object, stream, indent, allowance, context, level) L174
  _format_dict_items(self, items, stream, indent, allowance, context, level) L192
  _format_params(self, items, stream, indent, allowance, context, level) L197
  _format_params_or_dict_items(self, object, stream, indent, allowance, context, level, is_dict) L201
  "Format dict items or parameters respecting the compact=True"
  _format_items(self, items, stream, indent, allowance, context, level) L262
  "Format the items of an iterable (list, tuple...)."
  _pprint_key_val_tuple(self, object, stream, indent, allowance, context, level) L311
  "Pretty printing for key-value tuples from dict or parameters."

_changed_params(estimator) L87
"Return dict (param_name: value) of parameters that were given to"
_safe_repr(object, context, maxlevels, level, changed_only=False) L335
"Same as the builtin _safe_repr, with added support for Estimator"

sklearn/utils/_show_versions.py
_get_sys_info() L15
"System information"
_get_deps_info() L35
"Overview of the installed version of main dependencies"
get_version(module) L57
show_versions() L76
"Print useful debugging information""

sklearn/utils/_testing.py
class _IgnoreWarnings L273
  "Improved and simplified Python warnings context manager and decorator."
  __init__(self, category) L286
  __call__(self, fn) L293
  "Decorator to catch and hide warnings without visual nesting."
  wrapper(*args, **kwargs) L296
  __repr__(self) L303
  __enter__(self) L312
  __exit__(self, *exc_info) L321

class TempMemmap L486
  "Parameters"
  __init__(self, data, mmap_mode='r') L493
  __enter__(self) L497
  __exit__(self, exc_type, exc_val, exc_tb) L502

assert_warns(warning_class, func, *args, **kw) L71
"Test that a certain warning occurs."
assert_warns_message(warning_class, message, func, *args, **kw) L114
"Test that a certain warning occurs and with a certain message."
check_in_message(msg) L168
assert_warns_div0(func, *args, **kw) L182
"Assume that numpy's warning for divide by zero is raised"
assert_no_warnings(func, *args, **kw) L206
"Parameters"
ignore_warnings(obj=None, category=Warning) L231
"Context manager and decorator to ignore warnings."
assert_raise_message(exceptions, message, function, *args, **kwargs) L329
"Helper function to test the message raised in an exception."
assert_allclose_dense_sparse(x, y, rtol=1e-07, atol=1e-09, err_msg='') L371
"Assert allclose for sparse and dense data."
set_random_state(estimator, random_state=0) L412
"Set random state of an estimator if it has the `random_state` param."
check_skip_network() L466
_delete_folder(folder_path, warn=False) L471
"Utility function to cleanup a temporary folder if still existing."
create_memmap_backed_data(data, mmap_mode='r', return_folder=False) L506
"Parameters"
_get_args(function, varargs=False) L527
"Helper to get function arguments"
_get_func_name(func) L547
"Get function full name"
check_docstring_parameters(func, doc=None, ignore=None) L573
"Helper to check docstring"
assert_run_python_script(source_code, timeout=60) L699
"Utility to check assertions in an independent Python subprocess."
_convert_container(container, constructor_name, columns_name=None) L752

sklearn/utils/class_weight.py
compute_class_weight(class_weight, *, classes, y) L11
"Estimate class weights for unbalanced datasets."
compute_sample_weight(class_weight, y, *, indices=None) L76
"Estimate sample weights by class for unbalanced datasets."

sklearn/utils/deprecation.py
class deprecated L8
  "Decorator to mark a function or class as deprecated."
  __init__(self, extra='') L34
  __call__(self, obj) L37
  "Call method"
  _decorate_class(self, cls) L58
  wrapped(*args, **kwargs) L66
  _decorate_fun(self, fun) L77
  "Decorate function fun"
  wrapped(*args, **kwargs) L85
  _decorate_property(self, prop) L96
  wrapped(*args, **kwargs) L100
  _update_doc(self, olddoc) L106

_is_deprecated(func) L115
"Helper to check if func is wrapped by our deprecated decorator"

sklearn/utils/estimator_checks.py
class _NotAnArray L599
  "An object that is convertible to an array"
  __init__(self, data) L608
  __array__(self, dtype=None) L611
  __array_function__(self, func, types, args, kwargs) L614

_yield_checks(estimator) L61
_yield_classifier_checks(classifier) L110
check_supervised_y_no_nan(name, estimator_orig) L143
_yield_regressor_checks(regressor) L165
_yield_transformer_checks(transformer) L191
_yield_clustering_checks(clusterer) L211
_yield_outliers_checks(estimator) L223
_yield_all_checks(estimator) L240
_set_check_estimator_ids(obj) L290
"Create pytest ids for checks."
_construct_instance(Estimator) L329
"Construct Estimator instance if possible"
_mark_xfail_checks(estimator, check, pytest) L347
"Mark (estimator, check) pairs with xfail according to the"
_skip_if_xfail(estimator, check) L363
wrapped(*args, **kwargs) L373
parametrize_with_checks(estimators) L381
"Pytest specific decorator for parametrizing estimator checks."
check_estimator(Estimator, generate_only=False) L437
"Check if estimator adheres to scikit-learn conventions."
_regression_dataset() L504
_set_checking_parameters(estimator) L516
_is_pairwise(estimator) L621
"Returns True if estimator has a _pairwise attribute set to True."
_is_pairwise_metric(estimator) L637
"Returns True if estimator accepts pairwise metric."
_pairwise_estimator_convert_X(X, estimator, kernel=linear_kernel) L655
_generate_sparse_matrix(X_csr) L665
"Generate sparse matrices with {32,64}bit indices of diverse format"
check_estimator_sparse_data(name, estimator_orig) L698
check_sample_weights_pandas_series(name, estimator_orig) L754
check_sample_weights_not_an_array(name, estimator_orig) L781
check_sample_weights_list(name, estimator_orig) L798
check_sample_weights_shape(name, estimator_orig) L815
check_sample_weights_invariance(name, estimator_orig, kind='ones') L840
check_dtype_object(name, estimator_orig) L892
check_complex_data(name, estimator_orig) L927
check_dict_unchanged(name, estimator_orig) L938
_is_public_parameter(attr) L977
check_dont_overwrite_parameters(name, estimator_orig) L982
check_fit2d_predict1d(name, estimator_orig) L1035
_apply_on_subsets(func, X) L1063
check_methods_subset_invariance(name, estimator_orig) L1083
check_fit2d_1sample(name, estimator_orig) L1115
check_fit2d_1feature(name, estimator_orig) L1149
check_fit1d(name, estimator_orig) L1183
check_transformer_general(name, transformer, readonly_memmap=False) L1205
check_transformer_data_not_an_array(name, transformer) L1219
check_transformers_unfitted(name, transformer) L1235
_check_transformer(name, transformer_orig, X, y) L1246
check_pipeline_consistency(name, estimator_orig) L1325
check_fit_score_takes_y(name, estimator_orig) L1354
check_estimators_dtypes(name, estimator_orig) L1383
check_estimators_empty_data_messages(name, estimator_orig) L1406
check_estimators_nan_inf(name, estimator_orig) L1429
check_nonsquare_error(name, estimator_orig) L1500
"Test that error is thrown when non-square data provided"
check_estimators_pickle(name, estimator_orig) L1513
"Test that we can pickle all estimators"
check_estimators_partial_fit_n_features(name, estimator_orig) L1557
check_classifier_multioutput(name, estimator) L1584
check_regressor_multioutput(name, estimator) L1642
check_clustering(name, clusterer_orig, readonly_memmap=False) L1665
check_clusterer_compute_labels_predict(name, clusterer_orig) L1724
"Check that predict is invariant of compute_labels"
check_classifiers_one_label(name, classifier_orig) L1739
check_classifiers_train(name, classifier_orig, readonly_memmap=False, X_dtype='float64') L1773
check_outlier_corruption(num_outliers, expected_outliers, decision) L1898
check_outliers_train(name, estimator_orig, readonly_memmap=True) L1918
check_classifiers_multilabel_representation_invariance(name, classifier_orig) L1994
check_estimators_fit_returns_self(name, estimator_orig, readonly_memmap=False) L2028
"Check if self is returned when calling fit"
check_estimators_unfitted(name, estimator_orig) L2047
"Check that predict raises an exception in an unfitted estimator."
check_supervised_y_2d(name, estimator_orig) L2063
check_classifiers_predictions(X, y, name, classifier_orig) L2100
_choose_check_classifiers_labels(name, y, y_names) L2143
check_classifiers_classes(name, classifier_orig) L2147
check_regressors_int(name, regressor_orig) L2185
check_regressors_train(name, regressor_orig, readonly_memmap=False, X_dtype=np.float64) L2213
check_regressors_no_decision_function(name, regressor_orig) L2259
check_class_weight_classifiers(name, classifier_orig) L2285
check_class_weight_balanced_classifiers(name, classifier_orig, X_train, y_train, X_test, y_test, weights) L2331
check_class_weight_balanced_linear_classifier(name, Classifier) L2351
"Test class weights with non-contiguous class labels."
check_estimators_overwrite_params(name, estimator_orig) L2390
check_no_attributes_set_in_init(name, estimator_orig) L2425
"Check setting during init."
check_sparsify_coefficients(name, estimator_orig) L2459
check_classifier_data_not_an_array(name, estimator_orig) L2483
check_regressor_data_not_an_array(name, estimator_orig) L2495
check_estimators_data_not_an_array(name, estimator_orig, X, y, obj_type) L2505
check_parameters_default_constructible(name, Estimator) L2547
param_filter(p) L2572
"Identify hyper parameters of an estimator"
_enforce_estimator_tags_y(estimator, y) L2616
_enforce_estimator_tags_x(estimator, X) L2633
check_non_transformer_estimators_n_iter(name, estimator_orig) L2650
check_transformer_n_iter(name, estimator_orig) L2684
check_get_params_invariance(name, estimator_orig) L2710
check_set_params(name, estimator_orig) L2722
check_classifiers_regression_target(name, estimator_orig) L2776
check_decision_proba_consistency(name, estimator_orig) L2789
check_outliers_fit_predict(name, estimator_orig) L2811
check_fit_non_negative(name, estimator_orig) L2858
check_fit_idempotent(name, estimator_orig) L2868
check_n_features_in(name, estimator_orig) L2923
check_requires_y_none(name, estimator_orig) L2961

sklearn/utils/extmath.py
squared_norm(x) L26
"Squared Euclidean or Frobenius norm of x."
row_norms(X, squared=False) L49
"Row-wise (squared) Euclidean norm of X."
fast_logdet(A) L81
"Compute log(det(A)) for A symmetric"
density(w, **kwargs) L98
"Compute density of a sparse vector"
safe_sparse_dot(a, b, *, dense_output=False) L119
"Dot product that handle the sparse matrix case correctly"
randomized_range_finder(A, *, size, n_iter, power_iteration_normalizer='auto', random_state=None) L161
"Computes an orthonormal matrix whose range approximates the range of A."
randomized_svd(M, n_components, *, n_oversamples=10, n_iter='auto', power_iteration_normalizer='auto', transpose='auto', flip_sign=True, random_state=0) L246
"Computes a truncated randomized SVD"
weighted_mode(a, w, *, axis=0) L378
"Returns an array of the weighted modal (most common) value in a"
cartesian(arrays, out=None) L451
"Generate a cartesian product of input arrays."
svd_flip(u, v, u_based_decision=True) L500
"Sign correction to ensure deterministic output from SVD."
log_logistic(X, out=None) L546
"Compute the log of the logistic function, ``log(1 / (1 + e ** -x))``."
softmax(X, copy=True) L591
"Calculate the softmax function."
make_nonnegative(X, min_value=0) L625
"Ensure `X.min()` >= `min_value`."
_safe_accumulator_op(op, x, *args, **kwargs) L659
"This function provides numpy accumulator functions with a float64 dtype"
_incremental_mean_and_var(X, last_mean, last_variance, last_sample_count) L688
"Calculate mean update and a Youngs and Cramer variance update."
_deterministic_vector_sign_flip(u) L766
"Modify the sign of vectors for reproducibility"
stable_cumsum(arr, axis=None, rtol=1e-05, atol=1e-08) L788
"Use high precision for cumsum and check that final value matches sum"

sklearn/utils/fixes.py
class loguniform(scipy.stats.reciprocal) L108
  "A class supporting log-uniform random variables."

class MaskedArray(_MaskedArray) L161

_object_dtype_isnan(X) L44
_astype_copy_false(X) L49
"Returns the copy=False parameter for"
_joblib_parallel_args(**kwargs) L60
"Set joblib.Parallel arguments in a compatible way for 0.11 and 0.12+"
_take_along_axis(arr, indices, axis) L165
"Implements a simplified version of np.take_along_axis if numpy"

sklearn/utils/graph.py
single_source_shortest_path_length(graph, source, *, cutoff=None) L23
"Return the shortest path length from source to all reachable nodes."

sklearn/utils/metaestimators.py
class _BaseComposition(BaseEstimator) L18
  "Handles parameter management for classifiers composed of named estimators."
  __init__(self) L24
  _get_params(self, attr, deep=True) L27
  _set_params(self, attr, **params) L39
  _replace_estimator(self, attr, name, new_val) L56
  _validate_names(self, names) L65

class _IffHasAttrDescriptor L79
  "Implements a conditional property using the descriptor protocol."
  __init__(self, fn, delegate_names, attribute_name) L94
  __get__(self, obj, type=None) L102

if_delegate_has_method(delegate) L125
"Create a decorator for methods that are delegated to a sub-estimator"
_safe_split(estimator, X, y, indices, train_indices=None) L148
"Create subset of dataset and properly handle kernels."

sklearn/utils/multiclass.py
_unique_multiclass(y) L22
_unique_indicator(y) L29
unique_labels(*ys) L42
"Extract an ordered array of unique labels"
_is_integral_float(y) L108
is_multilabel(y) L112
"Check if ``y`` is in a multilabel format."
check_classification_targets(y) L158
"Ensure that target y is of a non-regression type."
type_of_target(y) L175
"Determine the type of data indicated by the target."
_check_partial_fit_first_call(clf, classes=None) L296
"Private helper function for factorizing common classes param logic"
class_distribution(y, sample_weight=None) L331
"Compute class priors from multioutput-multiclass target data"
_ovr_decision_function(predictions, confidences, n_classes) L407
"Compute a continuous, tie-breaking OvR decision function from OvO."

sklearn/utils/optimize.py
class _LineSearchError(RuntimeError) L23

_line_search_wolfe12(f, fprime, xk, pk, gfk, old_fval, old_old_fval, **kwargs) L27
"Same as line_search_wolfe1, but fall back to line_search_wolfe2 if"
_cg(fhess_p, fgrad, maxiter, tol) L55
"Solve iteratively the linear system 'fhess_p ."
_newton_cg(grad_hess, func, grad, x0, args=(), tol=0.0001, maxiter=100, maxinner=200, line_search=True, warn=True) L114
"Minimization of scalar function of one or more variables using the"
_check_optimize_result(solver, result, max_iter=None, extra_warning_msg=None) L207
"Check the OptimizeResult for successful convergence"

sklearn/utils/random.py
_random_choice_csc(n_samples, classes, class_probability=None, random_state=None) L14
"Generate a sparse random matrix given column class distributions"

sklearn/utils/setup.py
configuration(parent_package='', top_path=None) L7

sklearn/utils/sparsefuncs.py
_raise_typeerror(X) L16
"Raises a TypeError if X is not a CSR or CSC matrix"
_raise_error_wrong_axis(axis) L23
inplace_csr_column_scale(X, scale) L29
"Inplace column scaling of a CSR matrix."
inplace_csr_row_scale(X, scale) L47
"Inplace row scaling of a CSR matrix."
mean_variance_axis(X, axis) L65
"Compute mean and variance along an axix on a CSR or CSC matrix"
incr_mean_variance_axis(X, *, axis, last_mean, last_var, last_n) L103
"Compute incremental mean and variance along an axix on a CSR or"
inplace_column_scale(X, scale) L166
"Inplace column scaling of a CSC/CSR matrix."
inplace_row_scale(X, scale) L188
"Inplace row scaling of a CSR or CSC matrix."
inplace_swap_row_csc(X, m, n) L210
"Swaps two rows of a CSC matrix in-place."
inplace_swap_row_csr(X, m, n) L239
"Swaps two rows of a CSR matrix in-place."
inplace_swap_row(X, m, n) L294
"Swaps two rows of a CSC/CSR matrix in-place."
inplace_swap_column(X, m, n) L317
"Swaps two columns of a CSC/CSR matrix in-place."
_minor_reduce(X, ufunc) L344
_min_or_max_axis(X, axis, min_or_max) L355
_sparse_min_or_max(X, axis, min_or_max) L378
_sparse_min_max(X, axis) L397
_sparse_nan_min_max(X, axis) L402
min_max_axis(X, axis, ignore_nan=False) L407
"Compute minimum and maximum along an axis on a CSR or CSC matrix and"
count_nonzero(X, axis=None, sample_weight=None) L442
"A variant of X.getnnz() with extension to weighting on axis 0"
_get_median(data, n_zeros) L491
"Compute the median of data with n_zeros additional zeros."
_get_elem_at_rank(rank, data, n_negative, n_zeros) L510
"Find the value in data augmented with n_zeros for the given rank"
csc_median_axis_0(X) L519
"Find the median across axis 0 of a CSC matrix."

sklearn/utils/stats.py
_weighted_percentile(array, sample_weight, percentile=50) L7
"Compute weighted percentile"

sklearn/utils/validation.py
_deprecate_positional_args(f) L35
"Decorator for methods that issues warnings for positional arguments"
inner_f(*args, **kwargs) L57
_assert_all_finite(X, allow_nan=False, msg_dtype=None) L73
"Like assert_all_finite, but only for ndarray."
assert_all_finite(X, *, allow_nan=False) L105
"Throw a ValueError if X contains NaN or infinity."
as_float_array(X, *, copy=True, force_all_finite=True) L118
"Converts an array-like to an array of floats."
_is_arraylike(x) L170
"Returns whether the input is array-like"
_num_samples(x) L177
"Return number of samples in array-like x."
check_memory(memory) L205
"Check that ``memory`` is joblib.Memory-like."
check_consistent_length(*arrays) L238
"Check that all arrays have consistent first dimensions."
_make_indexable(iterable) L256
"Ensure iterable supports indexing or convert to an indexable variant."
indexable(*iterables) L276
"Make arrays indexable for cross-validation."
_ensure_sparse_format(spmatrix, accept_sparse, dtype, copy, force_all_finite, accept_large_sparse) L293
"Convert a sparse matrix to a given format."
_ensure_no_complex_data(array) L387
check_array(array, accept_sparse=False, *, accept_large_sparse=True, dtype='numeric', order=None, copy=False, force_all_finite=True, ensure_2d=True, allow_nd=False, ensure_min_samples=1, ensure_min_features=1, estimator=None) L395
"Input validation on an array, list, sparse matrix or similar."
_check_large_sparse(X, accept_large_sparse=False) L666
"Raise a ValueError if X has 64bit indices and accept_large_sparse=False"
check_X_y(X, y, accept_sparse=False, *, accept_large_sparse=True, dtype='numeric', order=None, copy=False, force_all_finite=True, ensure_2d=True, allow_nd=False, multi_output=False, ensure_min_samples=1, ensure_min_features=1, y_numeric=False, estimator=None) L686
"Input validation for standard estimators."
column_or_1d(y, *, warn=False) L815
"Ravel column or 1d numpy array, else raises an error"
check_random_state(seed) L847
"Turn seed into a np.random.RandomState instance"
has_fit_parameter(estimator, parameter) L868
"Checks whether the estimator's fit method supports the given parameter."
check_symmetric(array, *, tol=1e-10, raise_warning=True, raise_exception=False) L896
"Make sure that array is 2D, square and symmetric."
check_is_fitted(estimator, attributes=None, *, msg=None, all_or_any=all) L953
"Perform is_fitted validation for estimator."
check_non_negative(X, whom) L1019
"Check if there is any negative value in an array."
check_scalar(x, name, target_type, *, min_val=None, max_val=None) L1046
"Validate scalar parameters type and value."
_check_psd_eigenvalues(lambdas, enable_warnings=False) L1088
"Check the eigenvalues of a positive semidefinite (PSD) matrix."
_check_sample_weight(sample_weight, X, dtype=None) L1251
"Validate sample weights."
_allclose_dense_sparse(x, y, rtol=1e-07, atol=1e-09) L1304
"Check allclose for sparse and dense data."
_check_fit_params(X, fit_params, indices=None) L1340
"Check and validate the parameters passed during `fit`."
