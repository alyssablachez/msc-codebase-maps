% Requires: \usepackage{listings}
% Source: repo_maps/requests/0/ (ast_map.json, ctags_map.json, compact_map.txt),
% generated from requests at the base commit for issue 0, filtered to the
% HTTPAdapter/BaseAdapter/send records to match across all three map types.

% --- AST map (raw NDJSON) ---
\begin{lstlisting}[
    basicstyle=\ttfamily\footnotesize,
    breaklines=true,
    breakatwhitespace=false,
    columns=fullflexible,
    frame=single,
    caption={Example AST map output for \texttt{requests} issue 0 (NDJSON).},
    label={lst:ast-map-example}
]
{"type": "class", "file": "requests/adapters.py", "name": "HTTPAdapter", "line": 78, "docstring": "The built-in HTTP Adapter for urllib3.", "bases": ["BaseAdapter"]}
{"type": "function", "file": "requests/adapters.py", "name": "send", "line": 57, "signature": "(self, request, stream=False, timeout=None, verify=True, cert=None, proxies=None)", "docstring": "Sends PreparedRequest object. Returns Response object.", "class": "BaseAdapter"}
\end{lstlisting}

% --- ctags map (raw NDJSON) ---
\begin{lstlisting}[
    basicstyle=\ttfamily\footnotesize,
    breaklines=true,
    breakatwhitespace=false,
    columns=fullflexible,
    frame=single,
    caption={Example ctags map output for \texttt{requests} issue 0 (NDJSON).},
    label={lst:ctags-map-example}
]
{"_type": "tag", "name": "HTTPAdapter", "path": "requests/adapters.py", "pattern": "/^class HTTPAdapter(BaseAdapter):$/", "kind": "class"}
{"_type": "tag", "name": "send", "path": "requests/adapters.py", "pattern": "/^    def send(self, request, stream=False, timeout=None, verify=True, cert=None, proxies=None):$/", "kind": "member", "scope": "HTTPAdapter", "scopeKind": "class", "signature": "(self, request, stream=False, timeout=None, verify=True, cert=None, proxies=None)"}
\end{lstlisting}

% --- ast-compact map ---
\begin{lstlisting}[
    basicstyle=\ttfamily\footnotesize,
    breaklines=true,
    breakatwhitespace=false,
    columns=fullflexible,
    frame=single,
    caption={Example ast-compact map output for \texttt{requests} issue 0.},
    label={lst:ast-compact-map-example}
]
requests/adapters.py
class BaseAdapter(object) L51
  "The Base Transport Adapter"
  __init__(self) L54
  send(self, request, stream=False, timeout=None, verify=True, cert=None, proxies=None) L57
  "Sends PreparedRequest object. Returns Response object."
  close(self) L73
  "Cleans up adapter specific items."

class HTTPAdapter(BaseAdapter) L78
  "The built-in HTTP Adapter for urllib3."
  __init__(self, pool_connections=DEFAULT_POOLSIZE, pool_maxsize=DEFAULT_POOLSIZE, max_retries=DEFAULT_RETRIES, pool_block=DEFAULT_POOLBLOCK) L107
  ...
  send(self, request, stream=False, timeout=None, verify=True, cert=None, proxies=None) L375
  "Sends PreparedRequest object. Returns Response object."
\end{lstlisting}
